# 开发环境现状与优化待办

> 范围：**只讲开发环境与工具链，不涉及 HR 业务内容**。
> 业务侧看 `01`~`06` 与 `openspec/`。
> 日期：2026-08-12

---

## 一、环境全貌

| 层 | 现状 |
|---|---|
| 仓库 | `Raytheoner/zhuopin-ai-recruiting`（private），本地 `/Users/paulshao/Projects/HumanResource` |
| 开发机 | Mac Studio（zpzndeMac-Studio），macOS |
| 目标服务器 | `192.168.100.51`，**Windows**，SSH 别名 `zp51`，User `Administrator`，免密已通 |
| 运行时 | 服务器 **Python 3.14**；`pyproject.toml` 写 `requires-python = ">=3.11"` |
| 部署形态 | Python venv + Windows 计划任务（SYSTEM + AtStartup + 失败重启 3 次）+ 防火墙，**无 Docker** |
| 端口 | 8095（过渡期），路由 `/hr/recruit-agent`，`root_path` 由应用自己挂载 |
| 工具链 | OpenSpec（需求与契约）+ Superpowers（实现），接缝在 **spec → plan** |
| 测试 | 81 个用例，`python -m pytest` |
| 变更包 | `openspec/changes/m1-job-profile-intake`（未 archive） |

## 二、规则真源在哪

改规则时改这些地方，别改别的：

| 内容 | 真源 |
|---|---|
| 工程铁律、合规红线、部署约束、指令强制格式 | `CLAUDE.md`（每会话自动加载） |
| 工具链分工与接缝 | `03-工具链协作规则.md` |
| 发布操作步骤 | `05-发布运行手册.md` |
| 四个自定义 skill | `.claude/skills/{kickoff,env-ready,spec-to-plan,run-build}/SKILL.md` |
| 部署脚本 | `deploy-server.ps1`（服务器上跑）、`sync-to-server.sh`（Mac 上跑） |

**`.claude/commands/` 是遗留格式、只在 Claude Code 终端生效**，本项目下只留了三个一行的入口文件指向 skill。**改规则改 skill，不要改 commands。**

## 三、已踩过的坑（最有价值的部分，别再踩一遍）

### 界面与工具链

1. **`.claude/commands/` 在 Desktop Chat/Cowork 里不生效** —— 会报 "Some commands only work in the Claude Code terminal"。`.claude/skills/` 三个界面都能用，且支持按描述自动触发。旁证：OpenSpec 自己同时装了两套。
2. **worktree 分支名不能指定** —— Claude Code Desktop 勾选后自己生成 `claude/<任务名>-dev-<hash>`。Opener 里写死分支名只会导致多建一个 worktree。
3. **Cowork 的 bash 在隔离 Linux VM 里** —— 对 `.git/` 只能写不能删（`index.lock` 删不掉），**git 操作必失败**；`npm install -g` 装的东西不落到 Mac 上。
4. **Cowork 看不到 CC 会话**（2026-08-12 复测，结论已细化）—— `list_sessions` **现在能列出会话了**，但列出来的全是 cwd 指向 `local-agent-mode-sessions/…/outputs` 的 **Cowork** 会话；**Claude Code tab 的会话（cwd = 仓库根）依然不在列表里**。所以"跨界面靠会话通信"这条路仍然走不通，跨界面交换信息只能走文件系统——**而文件系统本来就更可靠**。已据此建了 `scripts/hoff` 握手通道，见第七节。
   *注意*：`read_transcript` 对能看到的 Cowork 会话是可用的，能读到完整对话。所以"另一条 Cowork 线在干什么"是可查的，"CC 跑出了什么"不可查。
5. **提交 main 上的未提交改动必须不勾 worktree** —— worktree 从提交点切出，未提交内容不在里面。
6. **进度台账路径是 `.superpowers/sdd/<计划文件名去掉.md>/progress.md`**，按计划分子目录，不是扁平的 `sdd/progress.md`。查错地方会误判成"没跑过"，然后从头重跑。

### worktree 里缺东西

建完 worktree 手工补三样，都被 `.gitignore` 挡着、git 不会带过去，**而缺失时的报错完全不像"文件没拷"**：

| 缺什么 | 症状 |
|---|---|
| `.env` | 报"无 API key"，看起来像代码 bug |
| 本地 SQLite / `data/` | 启动即报文件不存在 |
| Python venv | `ModuleNotFoundError` |

### 编码（Mac ↔ Windows 之间最密集的坑区）

7. **本机 shell 默认 locale 是 `C`** —— 中文文件名传给 `scp` 会被静默改写成八进制转义，远端报 "No such file or directory"，且**中途失败不回滚**。修法：脚本里显式 `export LANG/LC_ALL=en_US.UTF-8`，不依赖调用者环境。
8. **服务器 Windows PowerShell 5.1 按系统 ANSI 读脚本** —— `.ps1` 含中文注释而无 UTF-8 BOM 会直接语法错误。
9. **服务器 `.env` 非 UTF-8 会让应用崩溃**。
10. **服务器是 GBK 代码页** —— 中文文件名同步失败。现已改用白名单，文档根本不推服务器，问题绕过。

### 部署验证

11. **`ssh zp51 'curl localhost:8095'` 测不出防火墙问题** —— 那是服务器访问自己。必须**从开发机**直连 `192.168.100.51:8095`。企业AI转型踩过的原话：「本机冒烟 200 但外部全部超时」。
12. **"F12 看 Network 无 404" 对单文件内联页面是恒真的**，什么也证明不了。正确做法是**触发一次真实 API 调用，看它命中哪个 URL** —— 那才是路径前缀工作的证据。
13. **Windows OpenSSH：管理员账户的公钥放普通路径会被 sshd 直接忽略** —— 必须放 `C:\ProgramData\ssh\administrators_authorized_keys`，且权限只能有 `Administrators` 和 `SYSTEM`，多一个用户就静默拒绝。
14. **计划任务用 AtLogOn + 交互式账户，会话断开后起不来** —— 必须 SYSTEM + AtStartup。（企业AI转型有过 24h49m 停摆的实证）
15. **脚本里写了 ≠ 服务器上跑过** —— `deploy-server.ps1` 给日志目录设 ACL 那段（第 68-83 行）写完之后**从未真实执行过一次**：`.51` 上的 `logs\` 是应用自己 `mkdir` 出来的，首装脚本没再重跑；CI 也不跑 PowerShell，所以这段代码零次执行。判据很硬——**看目录上有没有非继承（`IsInherited=False`）的 ACE**：2026-08-20 查 `.51`，`logs\` 六条 ACE 全部 `inherited=True`，一条显式 ACE 都没有，等于宣告 `Set-Acl` 从没落过。**通用做法：凡是"部署脚本里的某段"，别问"写了吗"，问"它在目标机器上留下了什么可观测痕迹"。** 没有痕迹的代码 = 没跑过的代码。

16. **「现在是深夜」曾经是开发机的深夜，不是服务器的（根因已消除，判据保留）** —— 2026-08-20 放行 OP-0820-9 整机重启时，开发机 Mac 在 EDT（`-0400`）、`.51` 在 China Standard Time，**差 12 小时**：Mac 侧显示 08-19 23:31 深夜、看着像天然低峰，服务器侧却是 **08-20 周四上午 11:31 上班高峰**，7 个业务服务（报关/供应链/财务 Fi2/清单 B/SC2/指挥中心/统一门户网关）正在被用。**当日已把开发机时区改为 `Asia/Shanghai`（`+0800`），与 `.51` 对齐**，两边显示同一个钟点，误判来源消除；附带收益是 git commit 时间戳也变成 `+0800`，可与服务器日志、任务计划历史直接对齐。**但判据保留：凡是涉及停机窗口的决定，仍先 `ssh zp51` 取服务器本地时间与星期，再谈低峰。** 对齐是当前这台机器的状态，不是系统性保证——换机器、出差改时区、别人代跑，都会让它重新失效，而失效时没有任何症状。

17. **默认关闭的可观测通道，要在动作之前打开，不是之后** —— `.51` 上 `Microsoft-Windows-TaskScheduler/Operational` 出厂即 `IsEnabled: False`。若直接重启再去查"计划任务到底被触发过没有"，那批 100/129/102 事件压根不会存在，一次停机窗口的证据就白丢了，而且**不会报错**，只会查出一片空。**通用做法：验证性动作开跑前，先确认你打算据以采信的那条通道是开着的。** 与第 15 条同源——第 15 条问"留下了什么痕迹"，这条问"痕迹的记录器开着吗"。

### 同步脚本设计

18. **黑名单会把整个仓库根目录推到生产服务器** —— 策略文档、`openspec/`、`.claude/` 全推过去，既没必要又触发编码 bug。已改为**白名单**（`SYNC_PATHS`），`.env` 的防护也从"运行时判断跳过"变成"结构上不在列表里"。**构造上防住优于检查防住。**

---

## 四、已知缺口与待办

按优先级排。**均属环境层面，不涉及业务功能。**

### P0 —— 会造成真实损失

| # | 缺口 | 为什么要紧 |
|---|---|---|
| 1 | **无 pre-commit 钩子** | `.env`、真实简历、评测集全靠人肉 `git status` 检查。**git 历史里的泄露是永久的**，改 `.gitignore` 也删不掉。 |
| 2 | **无 CI** | 81 个测试全靠人记得跑。`.github/workflows/` 不存在。 |
| 3 | **Python 版本偏斜未对齐** | 服务器 3.14，`pyproject` 只写 `>=3.11`，本地跑测试的多半不是 3.14。pydantic 被迫从 2.10.4 升到 2.13.4 就是第一个症状——**本地 81 全绿 ≠ 服务器行为一致**。 |

### P1 —— 会浪费时间

| # | 缺口 | 说明 |
|---|---|---|
| 4 | **`env-ready` skill 从未被真正触发过** | 那次自检跑的是临时检查（只查了 4 项），不是 skill 里定义的 8 项。需要验证 skill 能被正常调起。 |
| 5 | **第 7 条重启验证未做，阻断已清、只差窗口** | 2026-08-20 完成重启前全量采集，详见 `docs/findings/2026-08-20-51整机重启验证-重启前采集.md`。配置层面确认无误：`ZhuopinRecruitAgent` = SYSTEM + Boot 触发 + 失败重启 3 次 + `WorkingDirectory=C:\apps\zhuopin-recruit-agent`。四条发现：①任务计划程序历史通道原本 `IsEnabled: False`，**已启用**（阻断解除）；②爆炸半径是**另外 7 个服务不是 5 个**（含统一门户网关），知会范围按 7 算；③`.51` 上次开机 2026-05-26，8 个服务的 Boot 触发**全部从未被真实验证过**，这次重启一次验 8 个；④`CBS RebootPending=True` + 85 天未重启，**停机时长不可按普通重启预估**。**未执行重启**——放行时刻服务器侧是周四上午 11:31 上班高峰（见第三节第 16 条），不满足低峰前置。推荐窗口：2026-08-22（周六）08:00 CST，次选 08-21（周五）07:00 CST。 |
| 6 | **`.openspec-test-*` 残留文件** | 仓库根目录一个空文件，Cowork 权限删不掉，需在 CC 里删。 |
| 7 | **无统一入口命令** | 没有 Makefile 或等价物，装依赖/跑测试/发版各记一套命令。 |

### P2 —— 值得做但不急

| # | 缺口 | 说明 |
|---|---|---|
| 8 | **06 清单里的 🟢 尚未逐条处理** | 见 `06-企业AI转型资产借鉴清单.md`。**1.1（防火墙 `-Profile Any`）已落地并于 2026-08-20 完成外部实测验证**；其余待办中第六节（工作流纪律）多条是直接改 skill/CLAUDE.md 就能落地的。 |
| 9 | **openspec 变更包未 archive** | `m1-job-profile-intake` 的 tasks 还差 0.11，archive 要等它勾完。 |
| 10 | **CLAUDE.md 防膨胀规则未定** | 06 清单 5.3 条建议现在就定，不要等膨胀了再瘦身。当前已有 7 份根目录文档且在增长。 |

---

## 五、不要碰的东西

- **`~/Library/CloudStorage/OneDrive-Personal/Projects/企业AI转型/`** —— 可以完全读取借鉴，**绝不得修改、删除或 lock**。它由另一台 Windows 机器通过 OneDrive 维护，跨端写入会冲突覆盖。
- **服务器上的 `.env`** —— 独立维护的生产凭据，不从开发机同步。
- **`deploy-server.ps1` 的 UTF-8 BOM** —— 去掉会让服务器上的 PowerShell 5.1 直接语法错误。
- **`sync-to-server.sh` 的 `LANG/LC_ALL` 两行** —— 去掉会让中文文件名同步崩溃。

---

## 六、给下一个会话的建议顺序

1. **先做 P0 的 1 和 2**（pre-commit + CI）—— 这两件是"每天都在承担风险但看不见"的类型，做完一劳永逸
2. **P0 的 3**（Python 版本对齐）—— 建议把 `pyproject.toml` 的 `requires-python` 收紧到与服务器一致，并在本地建对应版本的 venv
3. **P1 的 4 和 6** —— 顺手能清
4. **P1 的 5 重启验证** —— 需要协调时间窗口，单独安排
5. **P2 按需**

---

## 七、CC ↔ Cowork 握手通道（2026-08-12 建立）

两个界面之间没有会话级通信（见第三节第 4 条），但**挂载的是同一个仓库目录**，所以用文件系统做握手。目的是消掉「CC 跑完 → 人复制输出 → 粘进 Cowork」和「Cowork 出指令 → 人复制 → 粘进 CC」这两段人肉搬运。

### 协议

| 方向 | 载体 | 谁写 | 谁读 |
|---|---|---|---|
| Cowork → CC | `.claude/handoff/_next.md` | Cowork 写下一步的完整指令块 | 在 CC 里说「执行 `.claude/handoff/_next.md`」 |
| CC → Cowork | `.claude/handoff/<步骤名>.log` | `scripts/hoff` 自动落盘 | 在 Cowork 里说「`<步骤名>` 跑完了」 |

### 用法

```bash
scripts/hoff <步骤名> '<要跑的命令>'
```

命令照常在终端实时输出（`tee`），同时带表头（时间/主机/cwd/原命令/退出码）落到日志。**退出码单独记一行**——`tee` 会吃掉管道的退出码，脚本里用 `PIPESTATUS` 取回来了，所以失败不会被伪装成成功。

### 约定

- `.claude/handoff/` 已加进 `.gitignore`——过程产物，用完即弃，不入库
- 脚本里 `export LANG/LC_ALL=en_US.UTF-8` **两行不要删**，理由同 `sync-to-server.sh`（本机默认 locale 是 `C`，中文会被静默转义）
- 步骤名用短横线小写，跟 `_next.md` 里写的对上，Cowork 才知道去读哪个文件
