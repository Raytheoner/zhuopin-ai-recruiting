# OpenSpec × Superpowers 协作规则

> 日期：2026-08-06 ｜ 结论基于两边 SKILL.md 的实际内容，非推测
> 适用：本项目全体成员（5-6 人）

---

## 0. 先纠正一个错误结论

我此前建议"以 `tasks.md` 为准，跳过 `writing-plans`，直接进 `subagent-driven-development`"。**这个做法行不通**，原因是 `subagent-driven-development` 与 `writing-plans` 的产物是强耦合的：

| 耦合点 | 具体依赖 |
|---|---|
| `scripts/task-brief PLAN_FILE N` | 按任务编号从 plan 文件抽取任务全文，依赖 `### Task N:` 结构 |
| Reviewer 注意力透镜 | 从 plan 的 **Global Constraints** 段逐字复制给 reviewer。`tasks.md` 没有这一段 |
| 模型分级派发 | "当 plan 文本含完整代码时，实现即誊写+测试，用最便宜档"。`tasks.md` 不含代码，会被迫全程用贵模型 |
| 步骤粒度 | 要求 2-5 分钟/步，含显式 TDD 五步（写失败测试 → 跑到失败 → 最小实现 → 跑到通过 → 提交） |

对比一下粒度差距。`tasks.md` 里的一条：

> `5.4 多轮追问 Agent（纯函数）：每轮至多 3 个问题，上限 5 轮`

这是半天的活，没有文件路径、没有代码、没有测试。而 `writing-plans` 要求的是精确到「在 `tests/agents/test_intake.py` 写这个断言，跑这条命令，期望这个报错」。**差一个数量级。**

---

## 1. 两者各自独有什么

### OpenSpec 独有

- **`spec.md` 行为契约**：`### Requirement:` + `#### Scenario:` 的 WHEN/THEN 结构。这是可以直接给法务看、可以直接转成验收测试的东西。Superpowers 完全没有对应物——它的 `brainstorming` 产出的是设计文档，不是契约。
- **`archive` 活文档树**：变更归档后 spec 折进 `openspec/specs/`，跨变更累积。Superpowers 的 plan 是 `docs/superpowers/plans/YYYY-MM-DD-*.md`，日期命名的一次性产物。
- **`validate --strict` CLI 强校验**：机器可执行的门禁。
- **跨会话、跨人持久**：5-6 人共享同一份需求真源。

### Superpowers 独有

- **执行机器**：每任务派发全新 subagent（上下文隔离）、两阶段 review（spec 合规 + 代码质量）、修复循环、进度台账（抗 compaction）、TDD 强制、worktree 隔离、按任务复杂度分级派发模型。
- 对比 OpenSpec 的 `apply`——它的核心循环就一句话："for each pending task: make the code changes, mark complete"。**无 TDD、无 review gate、无上下文隔离。**

**一句话**：OpenSpec 强在"说清楚要什么并且留得住"，Superpowers 强在"把它高质量地做出来"。这个项目两样都需要。

---

## 2. 冲突点（都是硬冲突）

| # | 冲突 | 表现 |
|---|---|---|
| 1 | 两份"计划" | `tasks.md` vs `plans/*.md`，粒度差一个数量级，不明确真源就会打架 |
| 2 | 两个执行入口 | `/opsx:apply` vs `subagent-driven-development`，同时存在必然有人走错 |
| 3 | 两份设计文档 | OpenSpec `design.md` vs brainstorming 的 `docs/superpowers/specs/*-design.md` |
| 4 | 前置门禁抢跑 | Superpowers 的 `brainstorming` 带 `<HARD-GATE>`：任何创造性工作前必须先跑它。会和 `/opsx:propose` 抢同一个位置 |

---

## 3. 正确的接缝：spec → plan

关键证据在 `writing-plans` 自己的定义里：

> **description**: "Use when you have a **spec** or requirements for a multi-step task, before touching code"
>
> **Self-Review**: "**Spec coverage**: Skim each section/requirement in the spec. Can you point to a task that implements it?"

它**本来就是设计成吃 spec 的**。OpenSpec 的 `spec.md` 正好是它想要的那个输入，而且比 brainstorming 产出的散文式设计文档结构化得多。

```
探索期（可选）  superpowers:brainstorming        ← 只在还没想清楚时用，产出回灌给 propose
      ↓
需求与契约     /opsx:propose                     ← proposal · specs · design · tasks(WBS)
      ↓        openspec validate --strict        ← 门禁
实现计划       superpowers:writing-plans         ← 输入 = spec.md，逐条覆盖 Requirement
      ↓        （每份 plan 带 Global Constraints）
执行           superpowers:subagent-driven-development
      ↓        每任务新 subagent + 两阶段 review + 进度台账
收口           勾 tasks.md checkbox → /opsx:archive
```

### 粒度映射

**`tasks.md` 的一个章节 = 一个 superpowers plan = 一条 worktree 分支 = 一个可独立测试并合并的交付单元。**

以 M1 为例，9 个章节可合并成 5-6 份 plan，与四条轨道对齐：

| tasks.md 章节 | plan | 轨道 |
|---|---|---|
| 1 数据层 + 4 编排骨架与幂等 | `m1-foundation-orchestration` | A |
| 2 LLM 网关 | `m1-llm-gateway` | A/B |
| 3 企业微信通道 | `m1-wecom-channel` | A |
| 5 需求解析 Agent | `m1-intake-agent` | A |
| 6 确认断点 | `m1-approval-interrupt` | A |
| 7 JD 生成 | `m1-jd-generation` | C |
| 8 Web 界面 | `m1-web-ui` | E |

章节的 checkbox **在该 plan 的 final whole-branch review 通过后才勾**，不是写完代码就勾。

---

## 4. 操作规则

### 用

- `/opsx:propose <change-name>` —— 新模块开工的第一步
- `openspec validate <change> --strict` —— 提案完成后必跑
- `superpowers:writing-plans` —— 输入指定为对应的 `spec.md` 路径
- `superpowers:subagent-driven-development` —— 执行
- `superpowers:using-git-worktrees` —— 每份 plan 一条分支
- `/opsx:archive` —— 变更完成后收口

### 不用

- ❌ `/opsx:apply`
- ❌ 把 `tasks.md` 当 plan 喂给执行层
- ❌ `superpowers:brainstorming` 走主流程（仅探索期用，产出必须回灌成 propose 的输入）

### Global Constraints 的来源

每份 plan 必须有 Global Constraints 段，内容从 `CLAUDE.md` 的「工程铁律」与「合规红线」**逐字复制**。这不是形式主义——`subagent-driven-development` 会把这段原样交给 reviewer 作为注意力透镜。铁律不进 plan，reviewer 就查不到"这个 `effect_*` 节点没加幂等键"。

### 归档时限（硬纪律）

**变更包 `tasks.md` 全部勾选后当场跑 `openspec-archive-change`。不允许「代码完成但变更包未归档」的中间态持续超过一个工作 session。**

机械判据，不需要临场判断：

- 每次新开 session 的开工前自检加一条：`openspec/changes/` 下是否存在 tasks 已全 `[x]` 但仍未 archive 的变更包。**存在就先归档，再干别的。**
- 「一个工作 session」按 session 边界计，不按自然日计。同一个 session 内跑完 final review 就顺手归档，这是常态路径，不是加急。

*为什么*：拖延归档是 SDD 流程最常见的单一可信源漂移根因——代码已经变了，`openspec/specs/` 还停在旧契约上，此后所有人读到的"需求真源"都是过期的，**而且没有任何东西会报错提示这件事**。

> **这条规则为什么落在本文件，而不是写进 skill**：`.claude/skills/openspec-archive-change/` 是 OpenSpec 自带的生成物，`openspec init` / `openspec update` 会整份覆盖，改了会**静默丢失**。企业AI转型踩过实例：`openspec update` 把 `.claude/commands/opsx/propose.md` 里的强制门禁段整段删除，零提示零报错，混在 10 个文件的上游 diff 里。**凡 OpenSpec 自带的 `openspec-*` skill 一律不改**，我们自己的规则只落在本文件、`CLAUDE.md`、以及 `.claude/skills/{kickoff,env-ready,spec-to-plan,run-build}/` 这四个自建 skill 里。

---

## 5. 代价与风险

诚实地说，跑两套是有成本的：

| 风险 | 缓解 |
|---|---|
| 5-6 人要学两套命令 | 规则写进 `CLAUDE.md`，agent 会自动执行；人只需记住"propose 定需求、plan 定实现" |
| spec 改了但 plan 不会自动失效 | 纪律：spec 变更必须重跑对应 plan 的 writing-plans，不允许手改 plan |
| 两个文档树（`openspec/` 与 `docs/superpowers/plans/`） | 明确真源：**需求真源是 `openspec/specs/`，实现计划是过程产物**，冲突时以 spec 为准 |
| 流程重，小改动被拖累 | 见下方"轻量通道" |

### 轻量通道

不是所有改动都值得走全流程。判断标准是**有没有改变外部可观察行为**：

- **改了行为** → 走全流程（propose → plan → 执行 → archive）
- **纯重构、修 bug、改文档、调参数** → 跳过 OpenSpec，直接 `writing-plans` + `subagent-driven-development`；或极小改动直接 TDD 改完提交
- OpenSpec 自己也支持这条：`.openspec.yaml` 里设 `skip_specs: true`

---

## 6. 要不要只用一个？

我认真考虑过，结论是**这个项目两个都需要**。

**只用 OpenSpec** —— 失去 TDD 强制和 review gate。对本项目是实质损失：`evidence_ref` 非空、幂等键生效、`reason_type='ai_score'` 恒为 0 这些断言，**必须真的被测到**，不能靠"我写的时候注意了"。`opsx:apply` 那个朴素循环给不了这个保证。

**只用 Superpowers** —— 失去持久 spec 层。对本项目同样是实质损失：法务要审需求、5-6 人要有共享真源、"当时按什么标准筛的"三年后要能答得上来。`docs/superpowers/plans/2026-08-06-*.md` 这种日期命名的一次性文件承载不了。

**如果一定要砍一个**：砍 OpenSpec，保 Superpowers。因为执行质量的损失是不可逆的（坏代码留在系统里），而需求文档可以用人工纪律补（写好 md 文件也行）。但既然两个都已经装好、规则也已写死，没有理由砍。

---

## 7. 落地动作

- [x] `CLAUDE.md` 建立，含工具链规则 + 工程铁律 + 合规红线
- [x] `openspec/config.yaml` 的 context 与 CLAUDE.md 保持一致
- [ ] M1 的 `tasks.md` 章节按第 3 节映射拆成 5-6 份 plan（在 Claude Code 里跑 `writing-plans`，输入指定 spec.md）
- [ ] 第一份 plan 建议从 `m1-foundation-orchestration` 开始——幂等约定要最先立起来，后面所有代码都继承它
- [ ] 团队约定：spec 变更必须重跑 writing-plans，不手改 plan
