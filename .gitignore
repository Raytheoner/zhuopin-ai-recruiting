# === 敏感信息（硬红线，绝不入库）===
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
secrets/

# === 候选人数据（个人信息，绝不入库）===
data/
data/resumes/
data/candidates/
data/recordings/
*.resume.pdf
evalset/          # 200 份真实简历评测集，本地保存，走安全通道分发

# === Python 工件（Task 1 检查清单）===
__pycache__/
*.pyc
.pytest_cache/

# === Superpowers 运行时 scratch ===
.superpowers/
# 注意：docs/superpowers/plans/ 要入库，那是实现计划，不是 scratch

# === CC ↔ Cowork 握手通道（过程产物，不入库）===
# scripts/hoff 的命令输出与 _next.md 指令，两边靠文件系统交换，用完即弃
.claude/handoff/

# === OpenSpec 临时文件 ===
.openspec-test-*

# === Python ===
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
htmlcov/
.coverage

# === Node ===
node_modules/
dist/
.next/

# === 编辑器与系统 ===
.DS_Store
.idea/
.vscode/
*.swp
