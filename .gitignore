# === 敏感信息（硬红线，绝不入库）===
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
secrets/

# === 候选人数据（个人信息，绝不入库）===
data/
data/resumes/
data/candidates/
data/recordings/
*.resume.pdf
evalset/          # 200 份真实简历评测集，本地保存，走安全通道分发
# setup_logging 在 app/main.py import 时创建，日志是个人信息的第二份拷贝
# （脱敏是尽力而为的兜底，不是零风险保证）；本地跑真实数据后 git add -A
# 会把它连带带进去，和 data/ 是同一类风险
logs/


# === Python 工件（Task 1 检查清单）===
__pycache__/
*.pyc
.pytest_cache/

# === Superpowers 运行时 scratch ===
.superpowers/
# 注意：docs/superpowers/plans/ 要入库，那是实现计划，不是 scratch

# === CC ↔ Cowork 握手通道（过程产物，不入库）===
# scripts/hoff 的命令输出与 _next.md 指令，两边靠文件系统交换，用完即弃
.claude/handoff/

# === 受限会话投递中继（过程产物，不入库；0918H）===
# handoff-inbox/ 是 .claude/** 只读的受限会话（远端文件工具）投递 commit/launch/event
# 请求的落点，scripts/handoff_relay.py 会把合格件搬进 .claude/handoff/**；目录结构保留。
# ⛔ 不写 handoff-inbox/**/.gitkeep 一条了事：git 不会下钻一个已被通配符整体忽略的目录
# 去找里面的取消忽略规则，rejected/ 必须单独取消忽略才能让其内的 .gitkeep 生效。
handoff-inbox/*
!handoff-inbox/.gitkeep
!handoff-inbox/rejected/
handoff-inbox/rejected/*
!handoff-inbox/rejected/.gitkeep

# === OpenSpec 临时文件 ===
.openspec-test-*

# === 对外汇报材料（决定不入库，留本地）===
# 二进制大文件、diff 不可读，且内容随每次汇报重写；不入库避免 git add -A 误带
卓品智能AI招聘智能体-阶段一进展汇报.pptx

# === Python ===
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
htmlcov/
.coverage

# === Node ===
node_modules/
dist/
.next/

# === 编辑器与系统 ===
.DS_Store
.idea/
.vscode/
*.swp
.claude/settings.local.json
