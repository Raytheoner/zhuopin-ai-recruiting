# 部署与 51 门户挂载

> 更新：2026-08-06（依据 51 服务器只读调查结果重写，此前的 Docker 方案已作废）
> **硬约束：绝不读取或修改 `OneDrive-Personal/Projects/企业AI转型/` 下任何内容。**

---

## 1. 已查清的事实

| 项 | 现状 |
|---|---|
| 服务器 | `192.168.100.51`，**Windows**（具体版本未记录） |
| 门户技术栈 | 纯 Python 标准库静态文件服务器（`http.server`），零第三方依赖 |
| 门户源码位置 | **在「企业AI转型」仓库里** → 导航改动必须由 Paul 在那台机器上做 |
| 容器 | **没有 Docker**。全部服务是裸 Python + venv |
| 发版方式 | 手工 scp 推送 + PowerShell 脚本；首次部署需 RDP 跑一次 |
| 保活 | Windows 计划任务，SYSTEM 账户 + AtStartup，失败重启 3 次 |
| 反向代理 | **不存在**（设计已批准，未实现） |
| 鉴权 | **无正式鉴权**。共享明文口令 `ZP_GATE_PASSWORD`，四服务通用，不区分人员、无访问痕迹 |
| 出网 | **可以**。ERP / SRM / 企微 走 HTTPS 443 已实测；**LLM API 域名未测** |
| 已占端口 | 8080 / 8090 / 8091 / 8092(门户) / 8093 / 8094 |
| 访问范围 | LAN 全网段，无 VPN 限制 |

**Docker 方案作废**：服务器没有 Docker，现有 5 个服务全是 venv + 计划任务。为一个新板块单独引入容器运行时，等于让我们成为唯一的异类，运维得记两套。改为**沿用现有模式**。

---

## 2. 路由：一个需要 Paul 拍板的冲突

### 冲突

Shao Peishen 2026-07-29 定的硬约束：

> 新场景一律不准新起独立端口，必须注册到统一路由 `/{域}/{场景}` 下（如 `/hr/recruit-agent`），并预留网关鉴权接入点。

但**统一网关本身还不存在**（反向代理层设计已批准、代码未实现）。字面执行这条规则的结果是：在网关上线前，任何新场景都无法部署。这显然不是规则的本意。

### 建议的解法：过渡端口 + 路由就绪

规则的**目的**是防止端口蔓延、保证一切可被网关收编。所以按目的执行：

1. **应用从第一天就是路径前缀感知的** —— FastAPI 设 `root_path=/hr/recruit-agent`，所有前端资源与接口用相对路径。网关上线时只需加一条 `proxy_pass`，应用本身零改动
2. **路由名现在就占住** `/hr/recruit-agent`
3. **鉴权中间件留空壳接入点**，签名对齐未来的企微 OAuth SSO
4. **过渡期占用 8095**，并把「迁移到网关」登记为技术债，触发条件写明：统一门户网关上线即迁移

**为什么这条值得现在就做**：路径前缀适配是典型的"第一天几乎零成本、事后retrofit 要翻遍所有前端资源路径"的事情。硬编码 `/static/xxx` 和 `/api/xxx` 的应用，搬到 `/hr/recruit-agent/` 下面会全线 404。

> ⚠️ 这个解法需要 Paul 确认——因为规则是你定的，我不能替你解释它。

### 门户导航挂法：外链跳转，不用 iframe

现有板块两种都有（SC8 用 iframe，QD-B / FI2 用外链）。我们选**外链跳转**：

- 我们的界面是多轮对话 + 表单，iframe 里空间局促，移动端更糟
- iframe 跨端口会引入 Cookie / SameSite 问题，而鉴权方案本就还在变
- 将来接 SSO 时，顶层跳转比嵌套 iframe 干净得多

**导航那一行由 Paul 改**（门户源码在「企业AI转型」仓库里，我不能碰）。我们这边提供：路由 `/hr/recruit-agent`、板块名「HR·招聘智能体」、以及过渡期的直连地址。

---

## 3. 部署形态

沿用现有模式，不引入新运行时。

```
卓品招聘智能体
├─ Python venv（独立于其他服务）
├─ FastAPI + 单页前端，uvicorn 托管
│    root_path = /hr/recruit-agent   ← 路径前缀感知
├─ SQLite（demo 阶段）→ 正式版换 Postgres
├─ 过渡期监听 8095，绑 0.0.0.0
├─ Windows 计划任务保活：SYSTEM 账户 + AtStartup + 失败重启 3 次
└─ JSONL 访问日志（对齐现有四服务的做法，不采集个人身份信息）
```

**交付物**：`deploy-server.ps1`（首次部署：建 venv、装依赖、注册计划任务、开防火墙）+ `sync-to-server.ps1`（日常发版：scp + 远程重启）+ 部署说明。

> `ZhuopinDeploy.psm1` 那个共享模块在「企业AI转型」仓库里，我不能读也不能引用。我们的脚本自包含实现同样的动作。**如果 Paul 希望复用该模块，需要你自己在那台机器上做适配**——不要把模块拷过来，那等于把两个仓库耦合起来。

**保活的教训直接采纳**：用 SYSTEM 账户 + AtStartup，不用 AtLogOn + 交互式账户（2026-07-23 那次断线故障的结论）。

---

## 4. 两个必须现在处理的风险

### 风险一：LLM API 出网未验证 —— 本周就测

已验证出网的目标是 ERP、携客云 SRM、企微 API。**没有针对 LLM API 域名的实测记录。** 同属 HTTPS/443 协议族，大概率可行，但这是推断不是事实。

我们走境内模型，需要测的是这几个域名（W1 模型对比实测选定后以实际为准）：

```powershell
# 在 .51 上跑，逐个确认
curl.exe -I --max-time 10 https://ark.cn-beijing.volces.com   # 火山方舟（豆包）
curl.exe -I --max-time 10 https://api.deepseek.com            # DeepSeek
curl.exe -I --max-time 10 https://dashscope.aliyuncs.com      # 阿里百炼（通义）
```

**这条阻塞整个部署方案。** 若不通，要么请 IT 开白名单（只开选定供应商域名），要么重新论证私有化部署模型——那是另一个量级的投入（单卡机器 5-8 万起）。**建议本周就测，不要等到 Task 11。**

### 风险二：鉴权缺口会卡住 M2，不是 M1

Demo（M1 第 0 章）只处理岗位需求，不碰候选人个人信息，用现有共享口令**没问题**。

但 **M2 开始处理真实简历，共享明文口令就不够了**：

- 四服务共用一个明文口令、不区分人员、无访问痕迹
- 而简历是敏感个人信息，PIPL 要求可追溯的访问控制
- 「谁在什么时候看了谁的简历」必须答得上来，共享口令答不上来

时间线对不上：

| 时间 | 事件 |
|---|---|
| 2026-08-06 | 今天 |
| ~2026-10 初 | 我们 W8，M2 上线，开始处理真实简历 |
| 最迟 2026-10-31 | 止血口令的复审日期；企微 OAuth SSO 仍未实现 |

**统一网关大概率赶不上我们的 W8。** 所以需要 Paul 现在就决定：

- **A. 我们自建鉴权**（企微 OAuth 或简单账号体系，只管我们这个板块）——不阻塞，但和未来的统一网关会有重叠，将来要拆
- **B. 推动统一网关优先做完** —— 架构更干净，但引入一个我们控制不了的外部依赖
- **C. M2 延后到网关就绪** —— 不可接受，那是整个项目的主要价值

我的建议是 **A**，并且把接口签名对齐未来的 SSO，将来迁移只换实现不换调用方。合规上站得住的最小集：**可识别到人的登录 + 简历访问留痕**。

---

## 5. 安全红线

- `.env` 绝不入库、绝不出现在门户可访问路径下
- Demo 页面必须标注「演示环境，不进入正式招聘流程」
- **处理真实简历前必须完成 PIA 评估**，且访问范围收紧到 HR + 相关业务经理
- 若需开出站白名单，只开选定的 LLM 供应商域名
- 访问日志对齐现有做法（JSONL，不采集个人身份信息）；但 M2 起**必须**额外记录简历访问行为，这与"不采集个人身份信息"的现有原则冲突——冲突以合规要求为准

---

## 6. 待 Paul 决策

1. **路由冲突怎么解**（§2）：接受"过渡端口 8095 + 路径前缀就绪 + 网关上线即迁移"？还是有别的意图？
2. **鉴权走哪条路**（§4 风险二）：A 自建 / B 等网关 / C 延后？
3. **LLM 域名连通性测试**谁来跑、什么时候跑？建议本周。

---

## 7. 对实现计划的影响

`docs/superpowers/plans/2026-08-06-m1-chapter0-demo.md` 的 **Task 11「Docker 化与部署」已失效**，需要改为 Windows venv + 计划任务，并加上 `root_path` 路径前缀适配。

按项目规则**不手改 plan**，应重跑 `spec-to-plan` 重新生成。run-build 尚未开始，现在重跑成本最低。

Task 1（项目脚手架）也受影响——`root_path` 必须从脚手架阶段就配好，事后加要翻遍所有前端资源路径。
