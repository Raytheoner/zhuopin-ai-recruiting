# 企业AI转型资产借鉴清单

调查日期 2026-08-10。范围：`~/Library/CloudStorage/OneDrive-Personal/Projects/企业AI转型/` 的
5-平台底座（zhuopin_platform / deploy-tools / wecom-aibot-service）、0-学习与工具（文档治理）、
CLAUDE.md §5 工作流纪律、CLAUDE.md §7 + 3-治理与合规（合规红线）。全程只读，未修改/删除/加锁该目录任何文件。

**这是决策清单，不是资产目录。** 每条结论三选一：🟢 直接照搬做法　🟡 借设计不借实现　🔴 不适用。
引入方式一律是"读取参考 + 本仓库自建实现"，不跨仓库引用、不拷贝文件。

---

## 一、部署踩坑清单（0.10 部署前处理，最高优先级）

对照对象：`5-平台底座/deploy-tools/ZhuopinDeploy.psm1`（2026-08-04 收拢五个服务各自踩过的坑而成的共享模块）
vs 我们的 [deploy-server.ps1](deploy-server.ps1)、[sync-to-server.sh](sync-to-server.sh)。

### 1.1 🟢 防火墙规则补 `-Profile Any`

参考模块里 `Register-ZhuopinFirewallRule` 函数有个不对称写法：首次创建规则（`New-NetFirewallRule`）没显式指定
`-Profile`，但"规则已存在"分支的 `Set-NetFirewallRule` 却显式加了 `-Profile Any` 去"确保放行"。没找到直接写明
"本机200外部超时是因为Profile"的踩坑原文，但这个不对称本身就是信号——大概率是踩过一次之后，只改了幂等分支没
回头补首建分支。

我们 [deploy-server.ps1:91-97](deploy-server.ps1:91) 的 `New-NetFirewallRule` 调用同样没有 `-Profile`，跟参考
模块的缺口完全一致。加一行成本接近零，不应该赌 Windows/PS 版本组合下的默认值一定生效。

**要改的文件**：`deploy-server.ps1`，`New-NetFirewallRule` 调用加 `-Profile Any`。
**工作量**：5 分钟改代码 + 部署时从**另一台机器**（不是 .51 本机）实测一次 `curl http://192.168.100.51:8095/...`
才能真正验证——本机冒烟测不出这个坑，这一步不能省。

### 1.2 🟡 发版重启：轮询确认 vs 固定 sleep

参考模块头部注释记录了一次真实回归："2026-07-15 SC8 那份修了一次重启可靠性 bug（固定等2秒→轮询确认端口释放
/新进程存活），但企微机器人那份没同步到（复制粘贴出来的代码天然不会自动同步）。"现在的
`Restart-ZhuopinTask` 做法：`schtasks /End` 后轮询 `Get-NetTCPConnection -LocalPort $port -State Listen`
直到端口真释放，超时报 `OLD_STILL_BUSY`；再 `schtasks /Run` 后轮询确认新 PID 出现，超时报
`RESTART_FAILED_NO_INSTANCE`。

我们 [sync-to-server.sh:58-64](sync-to-server.sh:58) 目前是 `schtasks /end & schtasks /run` 后固定
`sleep 5`，正是参考项目点名批评过的模式——`schtasks /end` 不保证旧进程已经真正释放端口，负载高或优雅关闭慢时
5 秒可能不够，且后面健康检查只做一次、不重试，可能把"新实例其实没起来、旧实例还在响应"误报成发版成功。

**为什么不是 🟢**：轮询确认要用到的 PowerShell 具体写法（`Get-NetTCPConnection` 判断、两段超时阈值）可以照抄
思路，但我们的 `sync-to-server.sh` 是 bash 脚本经 ssh 远程执行 Windows 命令，不能直接搬 psm1 里的 PowerShell
代码，需要用 bash 里嵌 PowerShell one-liner 或者干脆把这段逻辑挪到远端一个小 .ps1 脚本里，属于"借设计重新实现"。
**工作量**：半天内可完成，不阻塞 0.10（当前固定 sleep 5 在 Demo 量级下大概率够用，只是没有确定性保证），
建议 0.10 之后、0.11 业务经理试跑之前补上。

### 1.3 已确认无需改动的三项（供确认，不是行动项）

- **UTF-8 BOM**：参考项目原文"避免……被按系统 ANSI 误读中文而语法报错（同 2026-07-21 命令中心已踩坑）"，
  我们的 `deploy-server.ps1` 在最近一次提交（`dae2bd9`）已经加了 BOM（`xxd` 确认头 3 字节 `EF BB BF`），提交
  信息里的踩坑描述与参考项目原文高度一致。**已规避，无需再做。**
- **计划任务 SYSTEM+AtStartup**：参考模块与我们的写法完全一致（`New-ScheduledTaskTrigger -AtStartup` +
  `LogonType ServiceAccount` + `RunLevel Highest`）。额外发现一条我们没问到但也已经躲开的关联坑——参考项目记录
  "SYSTEM 账户只认机器级 PATH，裸命令名会 0x80070002 找不到文件"，我们的 `$venvUvicorn` 本来就是
  `Join-Path` 拼出的绝对路径，天然规避。**已规避，无需再做。**
- **SSH 非交互会话下健康检查假阳性**：参考项目用 `$ProgressPreference='SilentlyContinue'` + `-UseBasicParsing`
  给 `Invoke-WebRequest` 打补丁规避"无控制台句柄"问题；我们的 `sync-to-server.sh` 直接用 `curl.exe`，从根上
  不存在这个依赖。**规避得比参考项目更彻底，无需再做。**

---

## 二、审计留痕设计（对应 `analysis_run` 技术债）

对照对象：`5-平台底座/zhuopin_platform/zhuopin_platform/audit/`（events.py / logger.py / sinks.py）
vs 我们 [app/llm/gateway.py](app/llm/gateway.py) 里目前只有 `NoopAuditHook` 空实现的 `AuditHook` Protocol。

### 2.1 🟡 借 Protocol 解耦 + JsonlSink 的 hash-chain 防篡改思路

参考项目用 `AuditSink` 一个 `Protocol`（`write`/`read_all` 两个方法，鸭子类型，不要求显式继承）解耦
`JsonlSink` 和 `ClickHouseSink`。`JsonlSink` 本身工程质量高、值得整体照抄逻辑重写：

- 按**落盘原始字节**（不是重新序列化后的 canonical JSON）算 `prev_hash`，避免 key 顺序变化导致哈希校验失效
- genesis 豁免只对第 1 行生效，第 2 行起缺 `prev_hash` 直接判篡改——专门堵"删光所有行的 prev_hash 字段、让
  每行都被误判成合法 genesis"这种攻击（代码里标注为"A3 修复"，有专项测试 `test_verify_stripped_prev_hash_attack_detected`）
- 类级共享锁 + 类级共享"上一条哈希"，支持同一文件的多实例交替写不断链

**为什么不是 🟢**：`AuditEvent` 这个 dataclass 字段集（`scenario`/`decision: dict`/`evidence...`）比较松散，
是自由 dict 而非强类型，不满足我们工程铁律 #3/#4 对 `criterion_score` 必须有 `evidence_ref`、评分字段要完整持久化
的强约束，直接套用会退化成"什么都往 dict 里塞"。应该按我们自己的字段要求重新定义一个 Pydantic model，只借
hash-chain 那一层实现思路。

**要改的文件**：新建 `app/audit/` 模块（sinks.py + logger.py 思路参考，字段按工程铁律 #3/#4 重新设计），
替换 `NoopAuditHook`。**工作量**：1-2 天（含防篡改链的单测覆盖）。这是 M2（真实简历处理，需要留痕）前必须
补的技术债，不阻塞 0.10 Demo 部署。

### 2.2 🔴 ClickHouseSink"同接口切换"目前是空壳，不要当作已验证经验

`ClickHouseSink.write()`/`read_all()` 目前都是 `raise NotImplementedError`，注释写"计划 2026-09 随 U9C 数据
汇聚一并落地"。如果之前有印象说"参考 zhuopin_platform 的 ClickHouse 迁移经验"，需要澄清：那边只是接口形状
对齐了，双写灰度验证还没做，我们不该把这当成"已经跑通、可以照抄"的东西。**结论：等对方 9 月真正落地后再看，
现阶段不适用。**

### 2.3 🟡 借"轻量访问痕迹 vs 场景层合规决策物理隔离"的分层原则

参考项目里 `connector_audit.py`（记"调用了谁"，字段是 source/action/target/timestamp）和 `audit/`（记"做了
什么决策"，含 evaluator/automation_level/override_reason）是两套物理隔离的存储，`test_connector_audit.py`
专门测试两者字段不得混用。对应到我们场景：LLM 调用记录（已有 `AuditHook`）应该和评分决策记录（`analysis_run`
审计，需要 `evidence_ref`）保持类似的物理隔离，不要混进同一张表或同一个日志流。**工作量**：这是 2.1 落地时
顺带遵守的设计原则，不是独立工作项。

---

## 三、企微通道设计（M2 起接入前参考）

对照对象：`5-平台底座/wecom-aibot-service/`（企微"智能机器人"WS长连接协议服务）+
`zhuopin_platform/shared_tools/notifiers/wecom.py`（群机器人 webhook）。

**先厘清一个架构差异**：`wecom-aibot-service` 用的是企微"智能机器人"开放平台 WebSocket 长连接协议（纯出站、
无入站端口、鉴权靠 SDK 内置 BotID/Secret），**不是**我们工程铁律第 6 条默认假设的"自建应用 + HTTP 回调 URL"
模型。这条协议路径下根本不存在"签名校验+落库+返回200"这一步，不是他们没做好，是协议本身绕开了它。

### 3.1 🔴 白名单机制对候选人不适用，且与"一次性链接"原则相反

`whitelist.py` 用一个硬编码 `frozenset`（6 个 userid）做 fail-closed 准入控制，未命中一律拒绝。这个模式是给
"内部、少量、事先已知身份"的人用的。**候选人是外部、批量、身份未知直到投递那一刻的公众个体，不可能被穷举进
一张 userid 表**。我们合规红线"候选人入口一律用一次性邀请链接，避免被认定'向境内公众提供'"正是针对这个场景
的替代方案——白名单的本质是"预先圈定的固定名单"，与一次性链接"用后即焚"的精神相反。**明确结论：不要把
白名单模式套到候选人通道上**，这是本次调查里最值得写进决策记录、防止团队想当然照搬的一条。

### 3.2 🟡 若面试官走企微内部通道，可借鉴"零入站端口"部署模式

如果未来给面试官（内部员工）也开企微交互通道，`wecom-aibot-service` 的纯出站 WS 部署模式（不需要防火墙入站
规则、不需要额外的 HTTP 端点）对 Windows 无 Docker 环境运维负担更小，值得参考部署形态，但 SDK 选型和消息协议
需要按面试官场景重新设计，不能直接复用代码。**不是当前 M1/M2 阻塞项，留到面试官交互功能规划时再评估。**

### 3.3 🟢 webhook 发送封装（urllib + JSON + errcode 检查 + 密钥分离）可直接照搬做法

`shared_tools/notifiers/wecom.py` 的 `send_markdown`/`send_text` 是标准库 `urllib.request` 直接 POST，逻辑
简单可靠：`errcode != 0` 抛异常、10 秒超时、markdown 子集格式（无 `#` 标题无表格、约 4096 字符上限，这是企微
官方限制不是设计选择）。`department_group_mapping.yaml` 只存环境变量名不存真实 webhook URL（真实 URL 落
`.env`），这个秘密隔离模式干净可取。

**但要补强两个参考项目也没做的空白**（照搬时一并解决，不要重蹈同样的坑）：
- 无重试、无限流——企微群机器人官方限流 20 条/分钟（errcode 45009），参考代码完全没处理，触发即直接失败。
  候选人批量投递场景下"评分完成后通知招聘群"这类通知，短时间内触发限流的概率比对方（4个专员、人工节奏）
  高得多，必须自己加简单退避重试 + 节流/合并通知。
- 无超长内容截断/分片保护，4096 字符上限被超出时企微服务端会直接拒绝，需要自己补截断逻辑。

**要改的文件**：如果 M2 起接入企微通知，新建类似 `app/notifiers/wecom.py`，按上述封装 + 补强两点重新实现。
**工作量**：半天（封装本身简单，补强限流/截断各约 1-2 小时）。

### 3.4 🔴 幂等/先落库后处理，没有现成实现可借，需按工程铁律自建

`wecom-aibot-service` 的 `msgid` 只用作归档文件名去重后缀，**不是**用来防止"同一条消息被重复处理"的幂等键；
持久化落点是 append-only 日志，不做去重约束；`gap_alert.py` 更是明确写死"企微 aibot 协议没有离线消息补推能力，
断线期间的消息永久丢失"——这个服务从设计上就是 at-most-once、不重放。这和我们基于 LangGraph checkpointer 的
at-least-once + 幂等 effect 节点模型是两种不同的可靠性哲学，不是对方设计不够好，是协议保证不同。**结论：企微
回调"先落库再处理"的具体实现需要完全自己按工程铁律第 1/6 条设计，这份参考代码在这一点上帮不上忙。**

六个真实生产 bug 值得对照自查（详见该服务 CLAUDE.md）：归档文件名粗粒度导致同人同天多条消息互相覆盖、队列
插入越界写坏其他表格、二进制文件被当文本做 UTF-8 解码校验误判为损坏——这几类"粗粒度键被并发覆盖"" 二进制/
文本假设"的坑在我们做归档/回执类功能时同样容易踩，做的时候留意。

### 3.5 补充澄清（2026-08-10，Paul 更正）：这套机器人是开发期值守工具，跟 §3.1 的候选人场景不冲突

§3.1/§3.2 最初把这个服务框在"未来给候选人或面试官接企微通道"的产品场景里讨论。Paul 更正：`wecom-aibot-service`
实际是他自己在"企业AI转型"项目**开发过程中**，24 小时值守跟采购部/财务部/质量部专员收材料、传信息的**个人
协调工具**，从头到尾不进任何部门的产品代码，也不会被候选人/面试官接触到——跟 HumanResource 最终交付给候选
人/面试官用的通道是两个不相交的问题域。

**结论：确实不冲突**，§3.1"白名单模式不能套到候选人身上"依然成立——白名单约束的是"谁能联系上 Paul 本人这个
私人值守机器人"，跟"候选人怎么进入招聘系统"从来不是同一件事，谈不上矛盾。

**能否"复用"取决于具体想要什么**：
- 只是想要**架构参考**（设计模式、六个真实生产 bug）——§3.1-§3.4 已覆盖，看这份文档即可，无需额外动作。
- 想要**真的搭一个同类机器人**，让 Paul 在 HumanResource 开发过程中也能 24 小时值守跟 HR/业务经理收材料传
  信息（用法对应采购/财务/质量三个专员）——这不是改文档能完成的，有三个我无法代劳的前置项：
  1. **企微 aibot 平台侧注册**（BotID/Secret）——账号级操作，只能 Paul 本人在企业微信管理后台完成。
  2. **落点在 HumanResource 自己的仓库**，参考 `wecom-aibot-service` 的架构设计重新实现一份，**不跨仓库引用
     它的代码，也不去改「企业AI转型」目录里的 `whitelist.py`/`department_mapping.yaml`**——那是本项目
     CLAUDE.md 定的硬约束（该目录由另一台 Windows 机器通过 OneDrive 实时同步维护，跨端写入有真实的文件冲突
     /覆盖风险），不因为这次是 Paul 本人要求就解除。
  3. **走项目自己定的开发流程**：CLAUDE.md 规定写代码必须在 CC、新开 session、勾选 worktree，且要先过
     `openspec-propose` 定范围再进实现——本 session 开场声明是"只读分析、不写代码、不勾 worktree"，不适合
     在这里直接开始写机器人代码。

---

## 四、连接器与 shared_tools 架构模式

### 4.1 🟡 借 `DataConnector(ABC)` + 专用异常分层的思路

`shared_tools/connector.py` 定义抽象基类 + 五个抽象方法，各连接器实现返回统一 dataclass；`connector_errors.py`
定义 `ConnectorValidationError`/`RateLimitError`/`InsecureTLSError`/`RealEndpointNotReadyError` 四个专用异常
类型（而非裸 `RuntimeError`/`ValueError`），配合"real 模式下 fail-loud、绝不静默回退 mock"的设计。**如果
HumanResource 将来要接企微 API 或其他外部数据源，这套"Pydantic 边界校验 + 专用异常分层 + 访问痕迹与合规决策
物理分离"的架构模式值得参考**，属于通用工程模式而非领域代码。**不是当前阻塞项**，等真正需要接第三方系统时
再落地。

### 4.2 🔴 ERP/SRM 连接器（ZpConnector/XkySrmConnector）不适用

供应链领域模型（BOM/库存/采购单/生产计划），与招聘业务无关，不需要看实现细节。

### 4.3 🔴 doc_parser 不存在，帮不上忙

`shared_tools/__init__.py` 明确列为"待建"，全仓 grep 简历解析相关关键词（PaddleOCR/MinerU/OCR）零命中。我们
简历 PDF/Word 结构化抽取只能自己从零设计，不存在"抄一半"的空间。唯一有效信息是意图对齐（"文档→结构化字段"
要走统一接口），没有可读代码可参考。

### 4.4 🔴 data_isolation_layer（OEM 隔离路由）不适用

`OEMRouter` 是给多租户 RAG 向量库做客户数据隔离的设计，`REGISTERED_OEMS` 硬编码映射 + fail-closed 拒绝未注册
客户。HumanResource 目前服务单一企业客户，没有多租户隔离需求，符合预判，不需要深挖。

---

## 五、文档治理六规则 R1-R6

对照对象：`1-转型规划/0-全景路线图/文档治理规范与规整执行清单-2026-07-07.md` + `工具-文档台账生成.py`。

### 5.1 🔴 文档台账自动生成脚本——现在引入为时过早

该机制的前提是全库底数 **124 份 md + 26 份 docx**，分跨 Cowork/CC 两桌、18 个月周期、多 worktree 并行——文档
多到"翻目录找不到哪份是权威版"才需要自动化索引。我们目前只有 `00-05` 六份编号文档 + CLAUDE.md，翻目录成本
接近零。**结论：不做，记住这是一个规模触发的机制，文档数破 30 份左右再重新评估，不要提前引入。**

### 5.2 🟡 R1 状态头思路可轻量借鉴，R4 命名律已天然合规

- R1（六枚举状态头 + 自动化台账）：不需要六枚举那一整套，但可以考虑给 `00-05` 加一个简单的"生效/草稿"标记，
  成本很低，可选，不紧急。
- R4（命名四律：主题-对象-日期-事项、文件名不含会变数字）：我们现有 `0N-主题.md` 编号命名法已经天然满足，
  **无需改动**。

### 5.3 🟢 R5 教训——CLAUDE.md 防膨胀规则应现在就定，不要等膨胀了再瘦身

对方 CLAUDE.md 顶部进度流水账一度膨胀到 **154KB 中占 49%**，把真正的规则挤到注意力边缘，这是他们 2026-08-09
才实测出的根因（"规则语料只增不减会稀释所有既有规则的注意力份额"）。我们的 CLAUDE.md 目前干净，但工程铁律和
合规红线两节本身也只在增长。**结论：现在就定一条"进度类内容不进 CLAUDE.md 正文，只放指针"的规则**，防患于
未然，成本是写一句话，不是事后重构。**建议加入 [CLAUDE.md](CLAUDE.md) 顶部说明段。**

---

## 六、工作流纪律（对照 CLAUDE.md §5）

### 6.1 🟢 kickoff skill 补"收工列文件清单 + commit message"字段

对方开场词纪律要求收工段必带 git 处置：不管任务多小，收工时都要列出本次新增/修改文件清单 + 建议 commit
message，避免"未提交悬置"。核对我们 [.claude/skills/kickoff/SKILL.md](.claude/skills/kickoff/SKILL.md)
第 76 行"### 通用（每份 Opener 都要有）"小节，目前**没有这一条通用要求**，只有任务专属验收项（如 `run-build`
要求回勾 tasks.md）。

**要改的文件**：`.claude/skills/kickoff/SKILL.md`，在"通用（每份 Opener 都要有）"小节加一条：完成标准必须
包含"列出本次新增/修改文件清单 + 建议 commit message"。**工作量**：改一份 skill 文档，10 分钟。

### 6.2 🟢 openspec-archive-change 补"不超 1 个工作 session"时限

对方纪律："变更包所有 tasks 全 [x] 后当场跑归档，不允许代码完成但变更包未归档的中间态持续超过 1 个工作
session"，理由是"拖延归档是 SDD 流程最常见的单一可信源漂移根因"。我们工具链流程图已经画了
`run-build → openspec-archive-change`，但没有写出这个量化时限，目前是隐含义务。

**要改的文件**：[03-工具链协作规则.md](03-工具链协作规则.md) 或 CLAUDE.md 工具链分工段，补一句"tasks 全部
勾选后当场归档，不超过 1 个工作 session"。**工作量**：改一句话。

### 6.3 🟡 发布收口标准补"用原始 case 端到端复现"

对方有三次真实复发事故（"代码改好了、通知也发了，但生产环境其实没更新"），教训是"验证要用触发 bug 的原始
案例端到端复现，而不是泛化冒烟测试"。我们 [04-部署与门户挂载.md](04-部署与门户挂载.md) 已有路径前缀/端口
等硬指标，但没有这条验证标准。**为什么不是 🟢**：对方的"三条发送硬前置"是给多人协作、专员举证场景设计的
完整发布门禁体系，我们单人开发场景不需要照搬全部，只需要借"任何 bug 修复对外宣称已修复前，必须用触发该
bug 的原始输入走一遍端到端"这一条原则。

**要改的文件**：[05-发布运行手册.md](05-发布运行手册.md) 的验证清单段，补这条原则。**工作量**：改几句话，
不紧急，M2 前补上即可（M1 Demo 阶段 bug 修复量还小，价值暂时有限）。

### 6.4 🟢 补决策代理条款

核实我们 CLAUDE.md 全文，**没有任何"决策代理/决策路由"条款**——没写明本项目决策人（对应企业AI转型里 Paul
这个角色）缺席时谁能代批、代批边界在哪。对方模板："可代"design 审查/批间 Triage/L2 日常审批，"不可代"对客
外发开闸/预算变更/合规红线变更。

**要改的文件**：CLAUDE.md 补一节"决策代理"，"不可代"清单至少应含：**合规红线变更、候选人淘汰规则的例外
情况、候选人对外通道（邀请链接/拒信）的开关**。**工作量**：写一段话，10-15 分钟，但需要 Paul 本人确认谁是
代理人、边界怎么划，不是我能替他决定的部分。

### 6.5 🟡 M2 上线前安排一次外部对抗性评审

对方纪律：每个阶段收口前安排独立外部只读评审，2026-06-08 那次评审验证了"拉第二双眼睛做对抗性审查"能捕捉
内部自审漏掉的 P0 项（**TLS 默认不校验、L2 门禁 fail-open**）。核实我们 CLAUDE.md，没有任何外部评审条目。
考虑到 HumanResource 的合规风险面（PIPL、简历数据、AI 评分可解释性）不比对方轻，而"鉴权中间件空壳接入点"
"企微回调签名校验"这类"看起来做了、实际 fail-open"的高发区正是我们项目里已知的技术债，值得引入。

**为什么不是 🟢**：对方是排期型机制（按 S1/S2/S3 里程碑固定安排），触发方式和评审范围需要按我们的项目节奏
重新设计，不是照搬一个时间表就行。我们本身有 `/code-review ultra`（ultrareview）可以承担"外部对抗性评审"
这个角色，**这条本质是"排期"而不是"造新工具"**。

**建议**：在 M2（处理真实简历）上线前，安排一次 ultrareview，覆盖鉴权中间件、企微回调签名校验、七条合规
红线的落地情况。**不阻塞 0.10。**

### 6.6 🟡 规则退休制

对方纪律：任一"人守"条目被违反 3 次即达阈值，必须机制化（挂到不可绕过的 CI 门禁上）或删除，理由是"规则
语料只增不减会稀释所有既有规则的注意力份额"。这条对我们的工程铁律和合规红线同样成立——目前这两节只在增长，
没有退休/机制化的路径。**不是现在就要做的事**，先记住这个原则：如果某条铁律被违反 3 次都是靠下游测试/评审
拦下来的，应该考虑做成 CI 门禁而不是继续加文字提醒。**建议记入 CLAUDE.md 工具链分工段作为一条元规则，不
需要额外工作量。**

---

## 七、合规红线缺口（对照 CLAUDE.md §7 现有 7 条）

调查确认对方的合规体系完全围绕 IATF 16949 / ISO 26262（汽车功能安全）与 OEM 保密义务展开，**不涉及 PIPL、
人脸识别限制、AI生成内容标识**这类 2025 年新规——这不是对方比我们完善，是业务域不同（B2B供应链/质量，非
直接处理个人求职者数据）导致适用法规不同，不能简单类比"他们有我们没有"。以下缺口是从他们的**设计原则**里
抽象出来、经适配后对我们仍然成立的部分，不是照搬他们的具体法规条文。

### 7.1 🟢 审计留存年限 + 人工确认动作本身也要留痕

对方："所有 AI 决策写平台 audit（append-only，**3 年留存**，可追溯）"。我们工程铁律 #3 只规定了 AI 评分要
持久化哪些字段，**没有规定留存多久，也没有规定"人工确认/淘汰动作本身"（谁、何时、依据什么理由）是否要按
同等标准写入审计**。PIPL 要求"谁在什么时候看了谁的简历"可查，"谁确认了淘汰"同样应该可查。

**要改的文件**：CLAUDE.md 工程铁律 #3 或合规红线段，补充留存年限（建议至少比照劳动仲裁时效，法务确认后
定具体年限）+ 明确人工确认动作纳入同等审计标准。**工作量**：写清楚要求本身不难，但年限数字需要法务口径，
不是我能替 Paul 定的部分，标记为待确认项。

### 7.2 🟡 数据分级与例外路由通道

对方有 D1-D4 四级数据分类（公开/合成 → 财务与最高敏感），D3/D4"不出网，只进本地确定性引擎"，且有一条受控
例外通道："任何越级路由须双签、audit 留痕、事后销毁记录"。我们目前只有"境内/不出境"一刀切,没有分级，也没有
受控例外通道——万一未来真出现"需要用某个境外能力测评 API"这类需求，现在的红线只会说"不行"，没有留一个
留痕的例外口子，实务中容易被绕开而不留痕。

**为什么不是 🟢**：分级标准需要按简历数据的实际敏感字段（姓名/联系方式/身份证号 vs 学历/工作经历这类相对
公开信息）重新设计，不能直接照搬对方财务/技术数据的分级维度。**不阻塞 0.10/M2**，建议在真正出现跨境需求
之前不必抢先做，但要记住这个设计模式存在。

### 7.3 🟡 fail-closed 访问控制 + 违规企图本身即审计事件

对方 OEM 隔离规范："未注册客户、空客户上下文一律拒绝并抛异常，不做模糊匹配放行……每次触发必须写审计——
违规企图本身就是审计事件"。我们现有红线只规定"简历访问要留痕"（正常访问记录),没规定"未授权访问尝试被拒绝
时，这次拒绝本身也要写入审计"。**当前 HumanResource 是单租户系统，这条价值有限**，等未来出现更严格的部门
级权限隔离需求时再引入更合适，标 🟡 而非 🟢 是因为现在没有多角色权限体系可以挂载这条规则。

### 7.4 🟢 人工确认独立性要求（防 AI 建议锚定）

对方 ISO26262 文档："安全证据必须由资质人员完全独立产出，脱离 AI 的手工复核"。我们现有红线"AI只做推荐、
人工确认淘汰"管住了"谁做最终动作"，但**没有管"人工确认时是否必须独立判断，而不是看到 AI 建议就顺势通过"**。
这正是 Amazon 2018 教训的现代变种——我们红线已经防了"用历史录用结果做训练信号"，但没防"人工审核被AI建议
锚定沦为橡皮图章"。

**要改的文件**：CLAUDE.md 合规红线段，补一句"人工确认淘汰前应先看候选人原始材料，而非只看 AI 给出的排序
理由"。**工作量**：写一句话，但落地需要 UI 设计配合（比如排序理由默认折叠、需要点开才看到），标记为产品
设计阶段要考虑的约束,不是纯文档改动。

### 7.5 见 §6.5（外部对抗性评审），不重复列出

### 7.6 🟡 输出侧敏感信息反泄露过滤

对方《企业AI治理清单》待办项："输出反向过滤：防止模型记住并输出训练数据中的敏感信息"。我们红线里没有类似
要求——AI 生成 JD/拒信/面试反馈摘要时，理论上存在意外引用或泄露其他候选人简历片段的风险（尤其是用 few-shot
示例或检索增强时）。**这是对方也还在"待办"状态、尚未有具体实现的条目**，标 🟡 是因为原则成立但双方都没有
现成方案，需要我们自己在 M2 设计评分/生成 pipeline 时留意，不是能直接抄的东西。

---

## 八、账号级 Skill 检视：zhuopin-kickoff-prompt

2026-08-10 补充调查。上一版清单只确认了这个 skill**存在**（`anthropic-skills:zhuopin-kickoff-prompt`），没有看内容。
本次实际调用了一次，用参数明确限定"只检视设计、不生成真实 opener、不碰任何文件"——它是**指令加载型** skill（把
指令文本载入当前对话让我照做），不是子代理自动执行型，调用过程本身没有产生任何读写，符合限定。

**结论先行：能为我们所用的是 4 条具体规则，不是整套机制。** 这个 skill 是为"4 条业务线 + 1 条总线、Cowork/CC
跨桌协作、共享任务队列文件、多 worktree 并行、迭代一个月从 v1.1 到 v1.7"的复杂度打磨出来的，我们目前单人 +
单仓库 + 单主线的项目，直接照搬整套机制会是明显的过度设计——这与本清单前面几节反复出现的"规模不匹配"判断
一致，不是低估这个 skill 的成熟度。

### 8.1 🟢 标题行硬规则——直接照搬做法

原文："每个 opener 代码块前必须有一行标题……聊天中重发单个 opener 时同样带上。"成因是真实反馈：改版重发
opener 时只给代码块、没给标题行，对方原话"虽然我可以推断，第一张图是哪个任务的 Opener 不明晰"。自检一问：
"把这个代码块单独发给一个没读过背景的人，他能一眼说出这是哪件任务吗？"

我们 kickoff skill 目前【设置】行只在 fenced code block 内部，没有强制"标题行"这一层——如果同一份 opener 因
报错或范围收窄被重新发送，Paul 拿到一个孤立代码块时会遇到同样的问题。

**要改的文件**：[.claude/skills/kickoff/SKILL.md](.claude/skills/kickoff/SKILL.md)，输出格式里补一条：每次
交付/重发 opener，代码块前必须有一行标题（简化格式即可，如 `### 【界面】<任务名>（<日期/触发原因>）`，不需要
对方那套"计划编号+队列行号"，我们没有共享队列）。**工作量**：10 分钟。

### 8.2 🟢 多 opener 并行/顺序矩阵——直接照搬做法

原文："一次给出 ≥2 个 opener/派单件/可开工任务时，必须附一张矩阵，逐项写明：能否立即开工、能否与哪几项并行
（判据＝触碰区是否重叠）、若必须串行谁先谁后及理由。不得只把 opener 并列列出、让他自己推断先后。"

这条直接用得上——本次会话我自己在同一条消息里并行派发了 4 个 Agent 调查任务，凭的是"我知道它们互不冲突"，
但如果换成要发给 Paul 的多个可执行 opener（比如同时布置"部署 0.10"和"写业务经理试跑说明"），目前 kickoff
skill 没有强制说明这两件事能不能同时做、有没有文件触碰重叠，全靠 Paul 自己猜。

**要改的文件**：[.claude/skills/kickoff/SKILL.md](.claude/skills/kickoff/SKILL.md)，补一条通用规则：一次给出
≥2 个 opener 时，必须先给一张顺序/并行说明（能否并行的判据＝改动文件/分支是否重叠）。**工作量**：15 分钟。

### 8.3 🟢 定时任务 prompt 绝对禁止提问——直接照搬做法，且现在就该定

原文（D 类）："定时任务 prompt（巡逻/巡检/每日提醒等 cron 驱动）绝对禁止写入任何需要人当场回答的指令……非
交互 session 没人能回答，写了即空转。"

这条对我们不是假设性的——本会话就用到了 `ScheduleWakeup`/`/loop` 这类会在无人值守场景下重新触发的机制。如果
将来给某个定时巡检（比如"每天检查 .51 服务是否存活"）写 prompt，一旦混入"如果异常，先问 Paul 要不要重启"这
种句子，定时触发的那次运行就没有人能回答，直接卡死或空转。

**要改的文件**：CLAUDE.md 或 kickoff skill，补一条硬规则：任何将在无人值守/定时触发场景下运行的 prompt，禁止
包含需要人当场回答的问题，只能靠"预案覆盖"（列举所有分支的固定处理方式）。**工作量**：10 分钟，现在定成本
最低，等真出现一次定时任务卡死才想起来就晚了。

### 8.4 🟡 "改本机工具链要更严格对待"的教训——借设计不借实现

原文的真实事故："`openspec update` 曾把 `.claude/commands/opsx/propose.md` 里的强制门禁段整段静默删除，零
提示零报错，混在 10 个文件的上游 diff 里。"结论：会重写生成物的命令，即便产出看起来只是 `.md`，也要按更严格
的流程处理。

我们没有"Cowork/CC 环境分级"这个概念，直接照搬"标记为CC执行"没有意义，但**"重写生成物的命令可能静默吃掉自
定义内容"这个具体风险是通用的**——我们的 `.claude/commands/` 目录本身就是"一行入口文件"（CLAUDE.md 已说明
"规则真源在 skills，不在 commands"），以后跑 `openspec update` 之类命令，同样有被覆盖的风险。

**要改的文件**：[03-工具链协作规则.md](03-工具链协作规则.md)，补一句："跑 `openspec update`/`opsx sync` 等
会重写生成物的命令后，必须 `git diff` 逐文件确认没有静默删除自定义内容（尤其是 Global Constraints 注入相关
的段落）。"**工作量**：5 分钟。

### 8.5 🔴 不适用的部分（规模不匹配，不建议引入）

- 共享任务队列 + 编辑锁协议（acquire/release、退出码检查、WIP 上限）——给多线/多人异步协作设计的机制，我们
  是单人+单AI协作，没有"别人在改同一份文件"的并发场景。
- opener 文件化落盘（写成 `开场prompt-*.md` 文件、frontmatter 状态头）——我们 session 密度低，聊天里直接给
  fenced 代码块已经够用，落盘反而增加维护成本。
- A/B/C/D 转场类别里"跨线交接必问/总线派活"这类多线路由判断——我们只有一条主线，没有"域专线 vs 总线"的路由
  问题。
- 状态头必须写在字段开头而非行尾——这是队列表格驱动的约定，我们没有这张表。

**与 §6.1 的交叉验证**：之前只从 CLAUDE.md 的文字描述反推出"kickoff skill 该补收工文件清单+commit message"，
这次读到源头 skill 原文——收工段确实要求"列出本次全部新产出/修改文件清单 + 建议 commit message"，与之前的
结论完全吻合，不是过度解读。

---

## 九、账号级 Skill 检视：zhuopin-followup-letter

2026-08-10 补充调查，同一天第二个账号级 skill。同样以指令加载型方式检视，未生成任何真实跟进信、未碰任何文件。

**结论先行**：这次找到的不是一个"格式模板"，是一套**方法论**，而且这套方法论跟我们已有的合规红线正向耦合——
值得认真对待，不是随手抄一个信件模板那么简单。整套"部门 AI 专员"多人异步协作调度机制（README 状态机 + 批准
脚本 + 10 分钟冷却窗口 + 每日自动批处理 + 跨部门连续编号）是给 5 个专员、数月持续协作、需要"起草人不能自己
批准自己"这种治理问题设计的，跟我们"3 位业务经理、一次性 0.11 试跑、Paul 本人既定方向又拍板"的规模完全不
匹配，**不要跟着搬机制**。但核心方法论（判例批改法）非常值得为 0.11 试跑专门准备一版。

### 9.1 🟢 判例批改法——直接照搬做法，且与合规红线正向耦合（本次最重要的发现）

原文硬规则：口径/需求确认类内容（答案会写进代码或影响判断逻辑的）**禁止抽象设问**——"不得直接问'这条规则该
怎么定''关闭状态应该存哪'，业务专员是实操者不是需求分析师"。一律转成**判例批改表**：挑 ≤10 条真实（脱敏）
案例，让专员对着真实场景打 **✅对／❌错／✏️改判＋一句话理由**，规则条文由专线**从批改结果反推**，再回发一句
"我这样理解对吗"确认。

**证据链很扎实**：原文引用"2026-07 采购域 6 轮往返仍有开放点，而 63 案例对照表/线下看真实看板/真实截图三次
均在几小时内收敛"——不是一个理论主张，是同一个专员用两种方法的真实对比。

**这条对 HumanResource 不只是"好用"，是跟我们已有合规红线正向耦合**：我们的红线"绝不用历史录用结果做监督
信号，只用显式岗位能力 rubric"要求的正是"显式、可追溯、非黑箱"的规则来源——判例批改法恰好是把这条红线**操
作化**的具体方法：业务经理对着真实简历案例给出的✅/❌/改判理由，本身就是"显式判断"，不是"从历史结果里学出
来的隐藏偏好"。

**0.11 试跑的具体用法**：不要问业务经理"什么样的候选人该进入下一轮面试"这类抽象问题，改为挑 8-10 份脱敏的
真实简历样本 + AI 给出的初步判断，让业务经理逐条打✅/❌/✏️改判理由，再从批改结果反推硬门槛/软技能关键词
草案发回确认。

**要改的文件**：建议在 [05-发布运行手册.md](05-发布运行手册.md) 里补一节"0.11 业务经理反馈收集方法"，或
新建一份轻量文档，模板只保留表结构本身（场景/背景/判例对照表/规则反推初稿/开放点计数），**不要把他们那套
README状态机、编号规则、docx真复选框机制一起搬进来**。**工作量**：半天（写模板 + 挑选/脱敏测试用简历案例），
建议在 0.11 试跑开始前完成。

### 9.2 🟢 一信 ≤3 个决策点 + 判据类禁止"超时默认生效"——直接照搬做法

原文两条硬约束：① 一次沟通最多 3 个决策点，超过就拆开或转会议；② 纯展示/交互类可以标"超时未回复按拟改方案
先做"，但**判据/口径/阈值/计算规则类绝对不可**默认生效，必须升级到线下推动确认。

第二条跟我们的合规立场完全一致——评分口径、硬门槛规则这类"判据类"决策，即便业务经理逾期没回复，也不能
"默认按 AI 建议生效"，必须主动催办而不是沉默当作同意。这跟我们红线"AI 只做推荐，淘汰需人工确认"是同一个
精神在"需求确认阶段"的延伸。

**要改的文件**：同 9.1，写进"业务经理反馈收集方法"文档：涉及打分口径/硬门槛的确认，业务经理逾期未回不能
默认采纳，需要主动催办。**工作量**：几句话，跟 9.1 一起写。

### 9.3 🟢 开放点计数——直接照搬做法，成本极低

原文："每封 A 类信末尾写明本场景还剩 N 项待确认……专员当前体感是'信没完没了'，显式计数把它变成'还剩3项就
收工'。"这是本次两个 skill 检视里成本最低、收益最直接的一条——不需要任何机制配合，纯格式要求。

**要改的文件**：写进 9.1 的模板要求里即可，不需要单独改动。**工作量**：0（随 9.1 一起落地）。

### 9.4 🟡 ≥2 个开放校准点时转线下/视频微会——借设计不借实现

原文："某场景同时有 ≥2 个开放口径点时，优先约 15-30 分钟微会，屏幕共享真实看板/报告当场定，跟进信降级为
会前材料+会后纪要。"原则通用：书面反馈来回超过一轮还没收敛时，应该主动约业务经理开个短会，而不是继续写信
拖时间。

**为什么不是 🟢**：原文配套的"专线当场记纪要、24h 内回发专员一句确认"是给正式跨部门协作设计的仪式感，我们
0.11 是一次性试跑，Paul 本人当场对齐后口头+补一句书面确认即可，不需要照搬完整的会前材料/会后纪要两段式流程。
**建议**：仅作为一条原则记在反馈收集方法文档里——"判例批改表来回超过一轮仍有分歧，优先约当面/视频对齐"。

### 9.5 🔴 不适用的部分（规模不匹配，不建议引入）

- README 状态机（`⏳待你审`/`🆕待发`/`⏸暂缓`/`📥已回件并回灌`四态）+ `approve_followup_letter.py` 批准脚本
  + 10 分钟冷却窗口——这是为防止"起草人自己批准自己发出"设计的治理机制，我们的反馈收集是 Paul 本人主导，
  没有这个角色分离问题。
- 跨专员共用、换人不重置的部门连续编号——我们没有多个"AI专员"角色需要区分编号，3位业务经理是同一批一次性
  试跑对象。
- 每日 09:30 自动批处理发送——我们不需要把反馈请求做成定时批量任务。
- md2word 原生复选框——除非 0.11 反馈确实要用纸质/docx 表单收集（如果真是这样，这条才值得重新评估为🟡），
  否则更可能用网页表单或口头/聊天直接问。
- 一线实操者先初标、专员只裁分歧项——我们的候选人评分目前没有"人工初标层"，rubric 本身就是给 AI 用的判据，
  不适用；且原文本身也提醒"用前必须先核实这一层是否真的在位，不能当既有资源直接许诺"，这条警示本身比这个
  分层设计更值得记住。

---

## 建议引入顺序

**0.10 部署前必须做（今天/明天内，成本以小时计）**：
1. §1.1 `deploy-server.ps1` 补 `-Profile Any`，并从非本机的另一台设备实测冒烟
2. §5.3 CLAUDE.md 顶部加一句"进度类内容不进正文、只放指针"的防膨胀规则（防患于未然，成本极低）

**0.10 部署后、0.11 业务经理试跑前（本周内）**：
3. §1.2 发版重启改轮询确认端口释放/新进程存活，替代固定 sleep 5
4. §6.1 kickoff skill 补"收工列文件清单+commit message"字段
5. §6.2 openspec-archive-change 补"不超1个工作session"时限
6. §6.4 CLAUDE.md 补决策代理条款（需要 Paul 确认代理人和边界）
7. §8.1 kickoff skill 补"标题行硬规则"（防止 opener 被单独重发时失去身份）
8. §8.2 kickoff skill 补"多 opener 并行/顺序矩阵"（一次派多个任务时不能让 Paul 自己猜顺序）
9. §8.3 补"定时任务 prompt 禁止提问"硬规则（`ScheduleWakeup`/`/loop` 已在用，现在定成本最低）
10. §8.4 `03-工具链协作规则.md` 补"重写生成物的命令后必须 diff 核对"（`openspec update` 类命令的通用风险）
11. **§9.1 判例批改法——写一份"0.11 业务经理反馈收集方法"文档，含判例表模板**（本清单目前最高价值的单条
    发现，且直接服务 0.11，必须在业务经理试跑开始前就位，不是可选项）
12. §9.2 同一文档里补"判据类内容禁止超时默认生效，需主动催办"
13. §9.3 同一文档里补"每次反馈末尾写清还剩几项待确认"（随 9.1 一起写，零增量成本）
14. §9.4 同一文档里补"来回超一轮未收敛，优先约当面/视频对齐"原则

**M2（处理真实简历）之前必须完成（合规硬门槛）**：
15. §2.1 `analysis_run` 审计留痕实现（借 hash-chain 防篡改思路，按工程铁律 #3/#4 字段重新设计）
16. §7.1 审计留存年限 + 人工确认动作留痕（需要法务口径定年限）
17. §7.4 人工确认独立性要求（需要 UI 设计配合）
18. §6.5 安排一次 ultrareview，覆盖鉴权空壳接入点、企微回调签名校验、七条合规红线落地情况

**企微通道接入时（M2 或之后，视排期）**：
19. §3.3 webhook 封装照搬 + 补限流/截断
20. §3.4 按工程铁律自建"先落库再处理"的幂等实现（无现成代码可借）
21. §3.1 明确不要把白名单模式套到候选人通道（设计评审时的检查项，不是独立工作量）

**触发条件达到后再评估，现在不做**：
22. §5.1 文档台账自动生成——文档数破 30 份左右再评估
23. §2.2 ClickHouseSink——等对方 2026-09 真正落地双写验证后再看
24. §7.2 数据分级例外路由——真出现跨境需求时再设计
25. §7.3 fail-closed 违规留痕——出现多角色权限体系时再引入

**确认不适用，无需跟踪**：ERP/SRM 连接器（§4.2）、doc_parser 无代码可用（§4.3）、data_isolation_layer 多
租户隔离（§4.4）、wecom-aibot 白名单套用候选人场景（§3.1，已在上面列为设计检查项而非工作量）、§8.5 共享
任务队列/编辑锁/opener文件化落盘/多线路由判断、§9.5 跟进信 README 状态机/批准脚本/自动批处理/部门编号
（规模不匹配，均为多人长期协作场景专用机制）。
