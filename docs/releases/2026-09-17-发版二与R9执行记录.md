# `.51` 发版 ② 与 R-9 执行记录（2026-09-17）

执行：`[Mac]0917BB`（run-lanes 无头，模型 Opus）｜ 派发：Cowork·HR业务线-接力0917D ｜ 备料：`docs/releases/Q14-计划任务安装说明.md`、`docs/releases/2026-09-发版清单-51.md` §三.2

## 零、授权核查

| 项 | 判据 | 结果 |
|---|---|---|
| G3「发版 ② Q-14 每日快照计划任务」 | `gates.gate_state('G3','发版 ② Q-14 每日快照计划任务')` | **`已放行`**（Q-28 答复「发」）✅ |
| Q-27（R-9 VC++） | 定夺队列状态 `已答` 且答复含「放行」 | **`已答`／「放行：派无头任务在 `.51` 装 VC++ 2015–2022 x64 Redistributable（不重启服务）…」**（Shao Peishen 2026-09-17 21:5x）✅ |

两段均获授权，全部执行。全程 ⛔ 未重启服务／整机、未改 8095 进程与生产 `.venv`、未动数据库。

## 一、发版 ②：Q-14 每日快照计划任务 `ZhuopinDailySnapshot`

| 步 | 命令／动作 | 结果 |
|---|---|---|
| 前置 | `ssh -o BatchMode=yes -o ConnectTimeout=8 zp51` ＋ `curl :8095/hr/recruit-agent/` | `SSH_OK`，`HTTP 200` ✅；`scripts\snapshot_data.py` 在 `.51` **尚不存在**（发版 ① 时脚本还没合 main） |
| scp | `scp scripts/snapshot_data.py zp51:C:/apps/zhuopin-recruit-agent/scripts/` | 6,272 B；两端 SHA256 = `b4d7c13d…76544b` 一致 ✅（只推这一个文件，⛔ 未跑 `sync-to-server.sh`——它会重启计划任务） |
| 手工首跑 | `.venv\Scripts\python.exe scripts\snapshot_data.py --data-dir … --backup-root C:\apps\backups --keep 14` | **`EXIT=0`**：`daily-20260917-2205\demo.db`（1222 页，integrity ok）＋ `audit/` ＋ `candidate_outbound.switch` ＋ `[snapshot] 完成` ✅ |
| `schtasks /create`（第一次） | 照安装说明 §二.2 原样 | `CREATE_EXIT=0`，**但** `要运行的任务` 里 `> snapshot-last.log 2>&1` 被剥掉——远端 cmd.exe 把 `\"…\"` 内的 `>` 当成 ssh 命令自身的重定向（安装说明的引号层级在 cmd 下不成立，`snapshot-last.log` 里写的是「成功创建计划任务」这句） |
| `schtasks /create`（第二次，`/f` 覆盖） | 同上，重定向改 `^>` `2^>^&1` 转义 | `CREATE_EXIT=0`；`要运行的任务` = `cmd /c "…python.exe …snapshot_data.py --data-dir C:\apps\zhuopin-recruit-agent\data --backup-root C:\apps\backups --keep 14 > C:\apps\backups\snapshot-last.log 2>&1"` ✅；`作为用户运行: SYSTEM`，`计划类型: 每天 3:00:00`，`下次运行时间: 2026/9/18 3:00:00` |
| `schtasks /run` | 立即触发 | 22:06:18 起跑（`timeout /t` 在 ssh 管道不生效，本机 `sleep 15` 后复查） |
| 验收 ① | `上次结果` | **`0`** ✅ |
| 验收 ② | `snapshot-last.log` 末行 | `[snapshot] 完成`（整份日志 5 行，`daily-20260917-2206\demo.db` 1222 页 integrity ok）✅ |
| 验收 ③ | `daily-20260917-2206\demo.db` 以 `mode=ro` URI 只读打开 | `PRAGMA integrity_check` = **ok**，`sqlite_master` 13 张表；`manifest.json`：`created 2026-09-17T22:06:19`，`demo.db 1222 页 / 5,005,312 B`，side = `audit/`、`candidate_outbound.switch` ✅ |
| 服务复核 | `curl :8095/hr/recruit-agent/` | **`HTTP 200`** ✅（服务未被触碰） |

**发版 ② 结果：成功，未回滚。** `C:\apps\backups` 现有：8 份发版前手工快照（`20260903-1003` … `20260917-2118`，永不触碰）＋ `daily-20260917-2205`（手工首跑）＋ `daily-20260917-2206`（计划任务首跑）。下次自动运行 2026-09-18 03:00。

回写安装说明：§二.2 的 `schtasks /create` 命令须把 `>` 与 `2>&1` 写成 `^>`／`2^>^&1`（本记录已实证，见 `docs/releases/Q14-计划任务安装说明.md` 更新）。

## 二、R-9：`.51` 装 VC++ 2015–2022 x64 Redistributable

| 步 | 结果 |
|---|---|
| 旧版本 | `HKLM\SOFTWARE\Microsoft\VisualStudio\14.0\VC\Runtimes\x64` Version = **v14.27.29016.00**；`System32\vcruntime140.dll`／`vcruntime140_1.dll`／`msvcp140.dll` = 14.27.29016.0；ARP：`Microsoft Visual C++ 2015-2019 Redistributable (x64) - 14.27.29016` |
| 下载 | `.51` 直连 `curl.exe -L https://aka.ms/vs/17/release/vc_redist.x64.exe` → `C:\apps\tmp\vc_redist.x64.exe`，25,635,768 B，`CURL_EXIT=0` |
| 签名 | `Get-AuthenticodeSignature` = **Valid**，`CN=Microsoft Corporation`；文件版本 14.44.35211.0 |
| 安装 | `vc_redist.x64.exe /install /quiet /norestart /log C:\apps\tmp\vc_redist_install.log` ⇒ **`INSTALL_EXIT=0`**（22:07:56） |
| 新版本 | 注册表 Version = **v14.44.35211.00**；三个 DLL = 14.44.35211.0；ARP：`Microsoft Visual C++ 2015-2022 Redistributable (x64) - 14.44.35211`（x86 侧未动，仍 14.27） |
| 是否需重启 | 安装日志 `Apply complete, result: 0x0, restart: None, ba requested restart: No`、`Exit code: 0x0, restarting: No` ⇒ **不需要**。`CBS RebootPending=True` 是安装前已有的状态（Q-15 09-20 窗口承接）；`PendingFileRenameOperations` 新增 `C:\Config.Msi\*.rbf` 五个 MSI 回滚临时文件待删，不影响运行库生效（探针实证见下） |
| 服务复核 | `curl :8095/hr/recruit-agent/` = **200**（安装全程服务在线） |

### 2.1 `smoke_m2_deps` 重跑（隔离 venv `C:\apps\m2-smoke\.venv`，Python 3.14.5）

R-10 取命令行 `set PYTHONIOENCODING=utf-8`（不改代码）；`EXIT=0`，`smoke-Windows.json`／`.md` 已覆盖为本次。

| 包 | 可导入 | 版本 | 体积 MB | import 耗时 ms |
|---|---|---|---|---|
| numpy / pypdf / python-docx / reportlab / pillow / pymupdf | ✅ ×6 | 同 `0917AX` | 同 | 310 / 172 / 125 / 35 / 20 / 105 |
| paddlepaddle / paddleocr | ❌ | — | — | `ModuleNotFoundError`（cp314 无 wheel，结论不变，D14 退路） |
| **torch** | **✅** | 2.14.0 | 539.3 | **6633** |
| **FlagEmbedding** | **✅** | 1.4.2 | 2.1 | **24754** |

⇒ R-9 判据「`import torch` 成功、探针 torch/FlagEmbedding 行 ✅」达成。`WinError 1114 c10.dll` 根因确为 VC++ 运行库过旧。

### 2.2 `bench_bge_m3` 在 `.51`

准备：`C:\apps\m2-smoke\repo\`（`app/` 整包＋`scripts/{bench_bge_m3,compare_models_m2,__init__}.py`＋合成样本 `data/eval/m2-pilot/{truth.json,S01–S20.txt}`）；隔离 venv 补装 `pydantic==2.13.4 openai==1.59.6 httpx==0.28.1`（bench 经 `compare_models_m2` 拉 `app.schemas`／`app.llm` 链）；`HF_ENDPOINT=https://hf-mirror.com`、`HF_HOME=C:\apps\m2-smoke\hf-cache`。以 `wmic process call create` 脱离 ssh 会话运行（`Start-Process` 随 ssh 断开被带走，第一次未起）。

| 步 | 结果 |
|---|---|
| 第一次起跑（hf-mirror 整仓 `snapshot_download`） | 22:12 起，22:14:52 **`EXIT=1`**：`HfHubHTTPError: 403 Forbidden … imgs/.DS_Store`（hf-mirror 对该文件 403，整仓拉取必败）；且此前 2 分钟仅拉到 23 MB（≈116 KB/s） |
| 三源测速（`curl -o NUL --max-time 20`） | `huggingface.co` 直连：连接超时 ❌ ｜ hf-mirror `pytorch_model.bin`：**116 KB/s** ｜ ModelScope `pytorch_model.bin`：**24.8 MB/s** |
| 权重获取 | ModelScope 只拉 11 个推理文件到 `C:\apps\m2-smoke\bge-m3\`（`fetch-bge.cmd`，2.27 GB 约 90 s）；五个 LFS 文件 SHA256 与 HF 仓库 `5617a9f` `?blobs=true` 元数据**逐一相符**（`pytorch_model.bin b5e0ce34…6aad38` 等） |
| 第二次起跑 | `HF_HUB_OFFLINE=1`，`MODEL_ID` 在进程内改指本地目录（⛔ 未改脚本），连跑两轮：**`EXIT=0` / `EXIT2=0`**（22:19:10 → 22:19:46） |
| 结果（两轮） | 画像向量 **493.8 / 492.1 ms** ｜ 单份简历（batch_size=4 摊薄）**116.2 / 117.9 ms**（20 份 2324 / 2358 ms）｜ 模型加载到首推 ≈ 15 s ｜ **recall@10 = 40.0%**，top-10 顺序与 cosine 与 Mac 逐位一致 |
| 机器 | Intel Xeon Gold 5318Y @ 2.10 GHz，16 逻辑核，16 GB；≈ Mac M-series 的 1/2 速度 |

⇒ tasks 1.1 十项全部有 `.51` 结论；D7「BGE-M3 在 `.51` 本地 CPU」前置条件已满足，单份 ~118 ms 可接受（1000 份约 2 分钟）。生产口径：权重走 ModelScope ＋ 本地目录 ＋ SHA256 核对，⛔ 不依赖 HF hub 在线解析。原始 JSON 取回 `data/eval/m2-pilot/bench-bge-m3-51.json`／`-run2.json`（`data/` 不入 git），表格誊入 `docs/m2-model-comparison.md`。

## 三、落档

- 本文件
- `docs/audit-and-outbound-ops.md` §五：追加「八次发版记录（发版 ②）」，§五第 1 项（`data\` 备份任务）改 ✅
- `docs/m2-model-comparison.md`「`.51` 同款 Windows」节与「BGE-M3 本地 CPU 召回」节填 `.51` 数据
- `docs/session接力.md` R-9 状态；R-10 状态（本次以 `PYTHONIOENCODING` 绕过，脚本改动仍待 U1）
- `docs/releases/Q14-计划任务安装说明.md` §二.2 命令修正
- `docs/releases/2026-09-发版清单-51.md` §三.2 标【已闭环】
- `openspec/changes/m2-resume-parse-and-rank/tasks.md` 1.1（已勾，追加 `.51` torch 结论）

## 四、清理

- `C:\apps\tmp\vc_redist.x64.exe`（25 MB）与安装日志保留至 Q-15 窗口后再删（若届时要回退运行库可用它 `/uninstall`）
- `C:\apps\m2-smoke\`（隔离 venv ≈1.5 GB ＋ `repo\` 副本 ＋ `bge-m3\` 权重 2.3 GB ＋ 失败的 `hf-cache\` 残片 23 MB）与生产无交集，随时 `Remove-Item -Recurse` 可删；M2 U3 上线前保留供复测（`bge-m3\` 即可作生产权重目录的来源，SHA256 已核）
