# `.51` 发版 ① 执行记录（2026-09-17）

执行：`[Mac]0917AX`（run-lanes 无头，模型 Opus）｜ 派发：Cowork·HR业务线-接力0917D ｜ 授权：定夺队列 Q-24 答复「发」（Shao Peishen 2026-09-17 21:1x CST）｜ 清单：`docs/releases/2026-09-发版清单-51.md` §五

开工闸：`gates.gate_state('G3','.51')` = `已放行`（21:17:34 CST）

## 步 0 前置闸

| 项 | 命令 | 结果 |
|---|---|---|
| G-a 同步 | `git status -sb` | `## main...origin/main`，HEAD = `f81cc9d`（= `origin/main`）；白名单路径 porcelain 为空 ✅（工作区只有 `docs/roadmap/任务台账.yaml` 改动与 `_to_delete/`，均不在白名单） |
| 现算范围 | `git diff --stat 0aa1af4..main -- app/ scripts/ requirements.txt pyproject.toml .env.example deploy-server.ps1 sync-to-server.sh` | **32 files changed, 5923 insertions(+), 1 deletion(-)** —— 与清单（按 `b44c2a4` 算）逐文件一致，`b44c2a4..f81cc9d` 只动了 `docs/` ✅ |
| G-d ssh | `ssh -o BatchMode=yes zp51 hostname` | `WIN-5CPUJV008C7`，exit 0 ✅ |
| G-d 基线 | `curl …:8095/hr/recruit-agent/` | `200` ✅ |
| 本机回归 | `pytest tests/test_transaction_ownership.py tests/test_outbound_effects.py tests/test_web_*.py -q` | `51 passed` ✅ |

## 步 1 快照

`ssh zp51 powershell … Copy-Item app + requirements.txt` ⇒ **`C:\apps\backups\20260917-2118`**（`app\` 133 文件含 `__pycache__`），exit 0 ✅

## 步 2 先装 numpy

`.venv\Scripts\python.exe -m pip install numpy==2.5.3 | Select-Object -Last 2` ⇒ `Installing collected packages: numpy` / `Successfully installed numpy-2.5.3`。
ssh 退出码 1（PowerShell 把 pip 的 `[notice]` stderr 行当错误记录，非安装失败）；复核 `python -c "import numpy; print(numpy.__version__)"` ⇒ `2.5.3`，`EXIT=0` ✅

## 步 3 sync

`./sync-to-server.sh`（21:19 CST）⇒ scp 7 项白名单 → 计划任务终止/运行成功 → 等待监听 → `HTTP 200` → `发版完成` ✅

## 步 4 闸 G-e

| 条 | 结果 |
|---|---|
| `LastTaskResult` | `267009`（运行中），`LastRunTime` 2026-09-17 21:19:19 ✅ |
| `:8095 Listen` 计数 | `1` ✅ |
| `app.log` 尾 30 行 | `2026-09-17 21:19:18,100 INFO uvicorn.error: Application startup complete.`；无 `Traceback` ✅ |

## 步 5 冒烟

| # | 判据 | 结果 |
|---|---|---|
| ① | `GET /hr/recruit-agent/` = 200 | `200` ✅ |
| ② | `GET /hr/recruit-agent/api/jobs` = JSON 列表（405 即失败） | `200`，JSON `{"jobs":[…]}` 20 条 ✅ |
| ③ | `server.py` 含 `discard_thread_checkpoints 失败` 计数 = 1 | `1` ✅（TD-13 方案 C 已在现网） |
| ④ | `python -m app.audit.assertions --db data\demo.db --mirror data\audit\decisions.jsonl` | `EXIT=0`，6 条断言全 `[OK]`（ssh 回显 GBK 乱码，不影响判定）✅ |

**发版 ① 结果：成功。现网 = `f81cc9d`（发版包范围 = `0aa1af4..f81cc9d` 白名单 32 文件）。无回滚。**

## 步 7 顺带项

### 7.1 tasks 1.1 `.51` 同款 Windows 冒烟（隔离 venv `C:\apps\m2-smoke`）

| 步 | 结果 |
|---|---|
| scp `requirements-m2-u0.txt` + `scripts/smoke_m2_deps.py` → `C:\apps\m2-smoke\` | exit 0 |
| 派生隔离 venv ＋ 轻依赖六项 | `pip list` 六项齐（numpy 2.5.3 / pypdf 6.19.0 / python-docx 1.2.0 / reportlab 5.0.1 / pillow 12.3.0 / pymupdf 1.28.2）✅ |
| `paddlepaddle==3.4.0` | `No matching distribution`（cp314 无 wheel）❌ |
| `paddleocr==3.7.0` | `paddlex 3.7.x depends on PyYAML==6.0.2` → 源码编译 → `Microsoft Visual C++ 14.0 or greater is required`；`--only-binary` 复核 `ResolutionImpossible` ❌ |
| `FlagEmbedding==1.4.2` | pip 成功（torch 2.14.0，< 15 分钟）；探针 `import torch` ⇒ `WinError 1114 … c10.dll`。`.51` VC++ Runtime = v14.27.29016（VS 2019），torch 2.14 需 VS 2022 运行库 ⚠️ |
| 探针首跑 | `UnicodeEncodeError: 'gbk' codec can't encode '\u2705'`（脚本 print 到 GBK 控制台）；改 `$env:PYTHONUTF8=1` 重跑 `EXIT=0`，`smoke-Windows.json` 取回 `data/eval/m2-pilot/`（`data/` 在 .gitignore，表格已誊进 `docs/m2-model-comparison.md`「.51 同款 Windows」节） |

处置：`docs/m2-model-comparison.md`「环境」节已填、「待裁决」#5 改写；tasks 1.1 回勾（十项均有结论）。**新留步**：⏸ `.51` 需装 VC++ 2015–2022 x64 Redistributable 后重跑 torch/FlagEmbedding 探针与 `bench_bge_m3`——改 `.51` 系统组件，本 opener 未授权，待派单独 opener。隔离目录 `C:\apps\m2-smoke` 保留（约 1.5 GB，与生产无交集，随时 `Remove-Item` 可删）。

### 7.2 Q-12 Mac 侧 `data/liaison.db` 重建 `liaison_group_notify` 补 CHECK

| 步 | 结果 |
|---|---|
| `launchctl bootout gui/$UID …liaison.plist` | exit 0，`launchctl print` 确认已卸载（原 pid 57874） |
| `PRAGMA wal_checkpoint(TRUNCATE)` + `cp` | 备份 **`data/liaison.db.bak-20260917-2135`**（167,936 B） |
| 行数复核 | `liaison_group_notify` = 0 行 ⇒ 直接重建 |
| `drop table` + `init_schema` | `init_schema ok`；`sqlite_master` 含 `(state = 'rejected') = (mode = 'reject')` ×1、`limit_bytes > 0` ×1；索引 `idx_liaison_group_notify_state` 在；`PRAGMA integrity_check` = ok |
| 负向实证（在 `/tmp` 副本上） | `state='rejected'+mode='direct'` ⇒ `CHECK constraint failed: (state = 'rejected') = (mode = 'reject')`；`byte_length=-5` ⇒ `CHECK constraint failed: attempts >= 0 AND …` ✅ |
| `launchctl bootstrap` | exit 0，`state = running`，**pid 89987**，`last exit code = (never exited)` |

值守中断 < 1 分钟（21:35 CST）。回滚路径未触发。
