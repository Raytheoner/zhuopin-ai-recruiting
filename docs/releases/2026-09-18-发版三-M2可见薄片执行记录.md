# `.51` 发版 ③：M2 可见薄片 执行记录（2026-09-18）

执行：`[Mac]0918AM`（run-lanes 无头，模型 Sonnet）｜ 派发：Cowork·HR业务线-接力0918G ｜ 授权：定夺队列 `Q-44`【G3 发布】`M2 可见薄片` 答复「发」（Shao Peishen 2026-09-18 07:3x「三单元开跑，上线」）｜ 清单：`docs/releases/2026-09-发版清单-51.md`（流程沿用，携带范围本次重算）

## 零、授权与预授权条件①②④自核

| 条件 | 判据 | 结果 |
|---|---|---|
| ① U1/U2/U2.5 全部合 main 且回勾 | `git merge-base --is-ancestor` 核 U1(`66e3b97`)／U2(`42b999c`)／U2.5(`413fc98`) 均在 main 历史；`openspec/changes/m2-resume-parse-and-rank/tasks.md` 逐条实读 2.4–2.8＋8.1、3.1–3.10、9.1–9.5 全部 `[x]` | ✅ |
| ② 全量 pytest 0 失败 | 用项目 `venv`（非系统 python3，系统 python3 缺 PIL 属环境差异非回归）跑 `python3 -m pytest -q` | **3456 passed, 10 skipped, 0 failed**（82.17s）✅ |
| ④ 入库闸保持关闭 | `app/config.py:68 live_resume_intake_enabled: bool = False`；无 `.env`／环境变量覆盖 | ✅ |

三条全成立，进入发版。

## 一、锁定发版 commit 与携带范围

**本次发版 commit（写死）：`67772ba`**（Merge branch 'lane-voice-interview-u2-prep-question-engine'，2026-09-18 19:54:10 +0800）。
**现网原 commit：`f81cc9d`**（2026-09-17 七次发版基线；八次发版 `0917BB` 只装 Q-14 计划任务与 VC++ 运行库，未改 `app/` 代码，基线不变）。

⚠️ 备料时（自核阶段）本机 HEAD 为 `67772ba`；执行到收口阶段发现 main 又前进到 `29b6f67`（另一并发的 `task-dispatcher` 台账提交 `70adfc9`／`29b6f67`）。核实 `git diff --stat 67772ba..29b6f67 -- app/ scripts/ requirements.txt pyproject.toml .env.example deploy-server.ps1 sync-to-server.sh` **为空**——两个新提交只动台账/文档，不碰同步白名单路径，`sync-to-server.sh` 实际 scp 出去的内容与锁定的 `67772ba` 逐字节一致，⇒ 发版 commit 仍按 `67772ba` 记录，未因并发合入而改变本次授权范围。

`f81cc9d..67772ba` 同步白名单改动：**41 文件 / +5719 −81**。除已授权的 M2 三单元（U1 必需子集＋U2＋U2.5）外，超出范围的携带项：

| 携带项 | 涉及文件 | 现网行为核验 |
|---|---|---|
| U3 硬门槛引擎（`m2-u3`，tasks.md 第 4 章，已合 main 但不在 Q-44 授权的①条件范围内） | `app/agents/hard_requirement_screening.py`、`app/storage/{appeal,rejection}.py`、`app/graph/screening_nodes.py`，新增 `/api/applications/{id}/appeal`、`/api/rejections/{id}/appeal/transition` | 不改变现网行为：`screen_and_persist` 只被 `queue_reapplication_screening` 调用，后者是 U2.5 已授权任务 3.10「触发该投递重判」自身要求的实现（原为空壳，本次落地为真实调用）；调用点在 `app/web/server.py::review_field` 内 `try/except`，重判失败不影响字段校对确认本身返回 `ok=True`；两个新增端点纯增量，不改任何既有路由行为 |
| 语音面试包（`m3-u0`／`m3-u1`／`m3-prep`） | `app/agents/interview_prep.py`、`app/graph/interview_prep_nodes.py`、`app/schemas/interview_ai_input.py`、`app/web/static/interview_prep_review.html`、新增 `/api/applications/{id}/prep/*` 五个端点＋`/applications/{id}/prep/{version}/review` 页面、`scripts/probe_m3_voice.py` | 不改变现网行为：全部为新增端点，`index.html` diff 只新增了「上传简历」→`resumes/upload` 一个入口链接（M2 本身），未新增任何指向 prep 页面的链接；`probe_m3_voice.py` 未被任何 `.51` 计划任务或 `app.main` import 链引用，是惰性文件 |
| Mac 侧调度/中继脚本 | `scripts/{dispatcher_backlog,dispatcher_answers,dispatcher_event.sh,commit_request,check_boundary,handoff_relay,install_handoff_relay}` | 不改变现网行为：均为 Mac 本机工具，`.51` 无任何计划任务引用（grep 核实），随代码上传但不会被执行 |
| `scripts/snapshot_data.py` | 内容 | 不改变现网行为：`git diff 813fdea..67772ba -- scripts/snapshot_data.py` 为空——本次 sync 覆盖的是与发版②（`0917BB`）单独 scp 部署到 `.51` 的**同一份内容**，`ZhuopinDailySnapshot` 计划任务不受影响 |
| 新依赖 `python-multipart`／`pypdf`／`python-docx`（+`lxml`／`typing_extensions` 传递依赖） | `requirements.txt` | Windows cp314 win_amd64 全部有轮子（`pip download --only-binary=:all:` 本机验证），且均为 M2 U2 tasks 3.3/3.4 授权范围内所需 |
| `AuthMiddleware` 改为真实鉴权 | `app/middleware/auth.py` | 不改变现网行为：`PROTECTED_PATH_PREFIXES` 显式排除 `/api/jobs*` 与静态资源（代码注释「M1 的岗位画像流程本单元不改变可见性」），M1 现有流程不受影响；新保护前缀（`/api/candidates`／`/api/resumes`／`/api/applications`／`/api/rejections`）对应的端点在本次发版前 `.51` 上根本不存在 |
| `app/storage/db.py` 新表/加列 | `application.kanban_state` 加列（走既有 `_ADDED_COLUMNS` 幂等机制），其余全部 `CREATE TABLE IF NOT EXISTS` | 不改变现网行为：老库升级路径已被 `tests/test_db_m2_schema.py`（含「老库升级后既有表一行不改」用例）覆盖，属②全量 pytest 通过范围 |

结论：**全部携带项核验通过，均不改变现网现有行为**，未触发 §二.3 的停链条款。

## 二、执行（严格按 `docs/releases/2026-09-发版清单-51.md` §五 流程）

| 阶段 | 命令/动作 | 结果 | 判定 |
|---|---|---|---|
| 0 前置闸 G-a/G-d | `git status -sb`（`## main...origin/main`，仅 `_to_delete/`／`Claude outputs/` 等未跟踪，不在白名单）；`ssh -o BatchMode=yes zp51 hostname` → `WIN-5CPUJV008C7`；`curl :8095/hr/recruit-agent/` → `200` | 全过 | ✅ |
| 1 快照 `app\`（`data\` 不拷，同既往理由） | `ssh zp51 powershell … Copy-Item app + requirements.txt` | `C:\apps\backups\20260918-2001` | ✅ |
| 2 先装依赖 | `.venv\Scripts\python.exe -m pip install python-multipart==0.0.20 pypdf==6.19.0 python-docx==1.2.0` | ssh 退出码 1（PowerShell 把 pip `[notice]` stderr 行当错误，非安装失败，同 `0917AX` 先例）；stdout 含 `Successfully installed lxml-6.1.3 pypdf-6.19.0 python-docx-1.2.0 python-multipart-0.0.20`；复核 `import pypdf, docx, multipart` → `OK 6.19.0 1.2.0`，`EXIT=0` | ✅ |
| 3 sync | `./sync-to-server.sh` | scp 7 项白名单 → 计划任务终止/运行成功 → `HTTP 200` → `发版完成` | ✅ |
| 4 闸 G-e | `LastTaskResult`=`267009`（运行中）；`:8095 Listen` 计数=`1`；`app.log` 尾 30 行含 `2026-09-18 20:02:09,157 INFO uvicorn.error: Application startup complete.`，无 `Traceback` | 三条全过 | ✅ |
| 5 冒烟①–④ | ① `curl …/hr/recruit-agent/` = `200`；② `curl …/api/jobs` 返回 JSON 岗位列表（非 405）；③ 🔴目标验收：`resumes/upload` = `200`、`login` = `200`、未登录 `/api/candidates` = `401`（M2 可见薄片入口与鉴权均已在现网生效）；④ 巡检裸跑：`python -m app.audit.assertions --db data\demo.db --mirror data\audit\decisions.jsonl` → 合规断言 6 条全过，`EXIT=0` | 全过 | ✅ |

**未触发回滚。**

## 三、入库闸复验（红线：发版前后各一次，全程关闭）

- 发版前：`app/config.py:68 live_resume_intake_enabled: bool = False`，无覆盖。
- 发版后：`.51` 上 `Select-String app\config.py -Pattern 'live_resume_intake_enabled: bool = '` → `False`；`.env` 无 `LIVE_RESUME` 相关键；机器级环境变量 `LIVE_RESUME_INTAKE_ENABLED` 未设置。
- 结论：**全程保持关闭**，未在本次发版内打开。

## 四、落档

- 本文件
- `docs/session接力.md` M2 行：追加本次发版结果一行
