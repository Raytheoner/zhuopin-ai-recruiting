# `.51` 发版四：M2·U3 硬门槛引擎 执行记录（2026-09-20）

执行：`[Mac]0920A`（run-lanes 无头，模型 Sonnet）｜ 派发：Cowork·HR业务线-接力0918AO ｜ 授权：定夺队列 `Q-51`【G3 发布】答复「发」（Shao Peishen 2026-09-20）｜ 清单：`docs/releases/2026-09-发版清单-51.md`（流程沿用，携带范围本次重算）

前情：`0919O` 首次尝试发版时自核拦停（pytest 4 failed，与 `Q-49` 引用的旧名单不符）；`0919T` 已修复全部四条（`merge --no-ff` 回 main，commit `a6df909`），`Q-51` 据此重新走闸放行。

## 零、自核五条（开工第一步）

| # | 判据 | 结果 |
|---|---|---|
| 1 | `a6df909` 在 main 祖先链上；`openspec/changes/m2-resume-parse-and-rank/tasks.md` 4.1–4.6 全部 `[x]` | ✅（`git merge-base --is-ancestor` 确认；tasks.md 逐条实读全勾） |
| 2 | 重新跑一次全量 `pytest -q`（`venv` 内） | ✅ **3716 passed, 10 skipped, 0 failed**（87.16s），与 `Q-51` 引用数字一致 |
| 3 | `app/storage/rejection.py::write_rejection` 只认 `hard_rule`/`human_decision`；`app/audit/assertions.py::COMPLIANCE_ASSERTIONS` 4 条断言都在 | ✅ 逐行读过，`_VALID_REASON_TYPES = frozenset({"hard_rule", "human_decision"})`；`COMPLIANCE_ASSERTIONS` 元组含 4 个断言函数 |
| 4 | `live_resume_intake_enabled` 默认仍是 `False` 且无覆盖 | ✅ `app/config.py:68` 默认 `False`；仓库内 grep 无环境变量覆盖 |
| 5 | 任一条不通过即停 | 不适用（五条全过） |

## 一、锁定发版 commit 与携带范围

**本次发版 commit（写死）：`8596829`**（main HEAD，2026-09-20）。

**现网原 commit：`67772ba`**（2026-09-18 发版三 `0918AM` 基线）。发车前用 `.51` 关键文件哈希与文件计数实测复核，`app/web/server.py`／`requirements.txt` SHA256 与 `git show 67772ba` 逐字节一致，`app/` 75 个 `.py`、`scripts/` 32 个 `.py`，两侧计数吻合 ⇒ **现网确为 `67772ba`**，未在两次发版之间被其它渠道改动过。

`67772ba..8596829` 同步白名单改动：**28 文件 / +3168 −6**（98 个提交，含大量 `task-dispatcher` 台账刷新/文档提交，实际触碰同步白名单路径的仅上述 28 文件）。

| 携带项 | 说明 | 现网行为核验 |
|---|---|---|
| M2·U3 硬门槛引擎申诉/标记流程接线 | `app/web/server.py` 新增 `POST /api/applications/{application_id}/appeal`、`POST /api/rejections/{rejection_id}/appeal/transition` 两个端点（本次发版目标，非"不改变行为"的携带项） | 本次**有意**接线，属发版目标本身 |
| M3 语音面试遗留缺口修复（`0919T`/`a6df909`） | effect 节点测试覆盖补齐，无新增运行时路径 | 不改变现网行为：纯测试补齐 |
| 调度器脚本修复（`0919P`/`0919S` 等） | `scripts/dispatcher_*`、`scripts/report_m3_latency.py`、`scripts/run_interview_live_sync.py`、`scripts/run_interview_post_scoring.py` 等 | 不改变现网行为：Mac 侧工具／新脚本，`.51` 无计划任务引用，惰性文件 |
| 若干 docs-only 提交 | 不在同步白名单内 | 不涉及 |

- 新增 pip 依赖：**无**——`git log 67772ba..8596829 -- requirements.txt` 为空
- `sync-to-server.sh` 白名单（`app`/`scripts`/`requirements.txt`/`pyproject.toml`/`.env.example`/`deploy-server.ps1`/`sync-to-server.sh`）覆盖全部新文件，无需改白名单本身；新增顶层 `import` 全部是仓库内模块或标准库（`httpx`/`hmac`/`secrets` 等既有依赖或 stdlib），无外部新依赖

## 二、执行（按 `docs/releases/2026-09-发版清单-51.md` §五 标准五阶段）

| 阶段 | 命令/动作 | 结果 | 判定 |
|---|---|---|---|
| 0 前置闸 | `git status -sb`（`## main...origin/main`，仅未跟踪目录不在白名单）；`ssh -o BatchMode=yes zp51 hostname` → `WIN-5CPUJV008C7`；`curl :8095/hr/recruit-agent/` → `200` | 全过 | ✅ |
| 1 快照 | `Copy-Item app + requirements.txt` | `C:\apps\backups\20260920-0844` | ✅ |
| 2 装依赖 | 无新依赖，跳过 | — | — |
| 3 sync | `./sync-to-server.sh` | scp 7 项白名单 → 计划任务终止/运行成功 → `HTTP 200` → `发版完成` | ✅ |
| 4 闸 G-e | `LastTaskResult=267009`（运行中）；`:8095 Listen` 计数=`1`；`app.log` 尾 30 行含 `2026-09-20 08:44:40,745 INFO uvicorn.error: Application startup complete.`，无 `Traceback` | 三条全过 | ✅ |
| 5 冒烟①–④＋两条额外 | ① 首页 `curl` = `200`；② `/api/jobs` = `200` + JSON 岗位列表（非 405）；③ 目标验收：`Select-String app\web\server.py -Pattern 'api/applications/\{application_id\}/appeal'` = `1`，`'api/rejections/\{rejection_id\}/appeal/transition'` = `1`；④ 巡检裸跑 `python -m app.audit.assertions --db data\demo.db --mirror data\audit\decisions.jsonl` → `EXIT=0`，6 条全 `[OK]`；⑤ 隔离报告 `assert_no_ai_score_rejections`：`✅ 以 AI 评分为理由的拒绝记录数恒为 0`，0 违例；⑥ `appeal_event` 表：`table_exists=True`、`row_count=0` | 全过 | ✅ |

**未触发回滚。**

## 三、红线复验

- 发版前：`app/config.py:68 live_resume_intake_enabled: bool = False`，无覆盖
- 发版后：`.51` 上 `Select-String app\config.py -Pattern 'live_resume_intake_enabled: bool = '` → `False`；`.env` 无 `LIVE_RESUME` 相关键；机器级环境变量 `LIVE_RESUME_INTAKE_ENABLED` 未设置
- 结论：**全程保持关闭**，未在本次发版内打开
- 未对外发送（企微/邮件/跟进信一律未碰）；未改 `scripts/gates.py`；未动 `.51` 现网既有系统组件、未碰真实简历数据

## 四、落档

- 本文件
- `docs/session接力.md`「二、下一步」追加一行
- `定夺队列.md` `Q-51` 答复列追加执行结果引用
