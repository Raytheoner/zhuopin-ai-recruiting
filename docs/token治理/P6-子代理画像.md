# P6 子代理画像（2026-09-16，`0916I`，只量不改）

窗口：2026-09-01 至 2026-09-15，634 个日志文件、416 个会话、总成本 $767.3258（`scripts/token_ledger.py --since 2026-09-01 --until 2026-09-15`）。

## 一、角色表

| 角色 | 个数 | 调用 | 平均调用/个 | 峰值上下文中位数 | 成本$ |
|---|---:|---:|---:|---:|---:|
| final-review | 83 | 1366 | 16.5 | 76k | 61 |
| implementer | 75 | 1981 | 26.4 | 72k | 47 |
| 其他 | 76 | 1452 | 19.1 | 78k | 43 |
| quality-review | 14 | 216 | 15.4 | 79k | 10 |
| spec-review | 20 | 277 | 13.8 | 60k | 9 |
| fix | 25 | 233 | 9.3 | 52k | 7 |
| explore | 3 | 22 | 7.3 | 71k | 1 |

子代理总成本 = 178（Σ上表「成本$」）。「其他」43/178 ≈ 24.2%，低于 30% 校正门槛，本轮未补丁 `ROLE_PATTERNS`（样例见账本 `/tmp/p6/ledger-0901-0915.md` 末尾，均为 worktree Task 委派与 review 委派的长句开头，已能被现有分类命中大半，未命中的是长尾措辞，成本占比小不值得为之改分类器）。

子代理成本 Top 15 父会话（原样见 `/tmp/p6/ledger-0901-0915.md`「子代理成本 Top 15 父会话」节，此处不重复贴）。

## 二、三个判断

**a. 修复循环是否是大头 —— 否，比值均远低于 1.5 阈值。**
`fix ÷ implementer` = 25/75 ≈ **0.33**；`quality-review ÷ implementer` = 14/75 ≈ **0.19**；`spec-review ÷ implementer` = 20/75 ≈ **0.27**。三个比值都明显低于「评审没过反复重做」的 1.5 阈值，数据不支持"返工循环吃掉大头成本"这个假设。

**b. 单个子代理是否在滚雪球 —— 否，无一角色峰值中位数过 120k 阈值。**
七个角色峰值上下文中位数落在 52k–79k 区间（quality-review 79k 最高，fix 52k 最低），全部低于 120k 门槛。`implementer` 平均调用/个最高（26.4），但并未伴随峰值飙升（72k，反而低于 quality-review/其他/final-review），不支持"任务书切得太大"假设。

**c. 模板重读次数与体积**（来自账本「被 Read 最多的文件」节 Top10 采样）：
- `global-constraints.md`（本项目 SDD 生成，4437B）：**16 次**，Top10 中读取次数最高的单文件
- `implementer-prompt.md`（superpowers 6.2.0 插件模板，5717B）：**14 次**
- `task-reviewer-prompt.md`（superpowers 6.2.0 插件模板，7816B，三模板中最大）：**12 次**

## 三、结论与建议动作（只给一条，本件不实施）

a、b 两个假设均未被数据证实（比值远低于 1.5、峰值远低于 120k），说明当前子代理成本大头（final-review $61 + implementer $47 = $108，占子代理总成本 178 的 60.7%）是正常工作量，不是循环浪费——不建议往"减少返工"或"切小任务粒度"方向动。

唯一同时满足「高频重读」+「项目自己可控（不受"不改 superpowers 插件"红线约束）」两个条件的目标是 **`global-constraints.md`**：它是本项目 `spec-to-plan`/`run-build` 技能按规则从 CLAUDE.md「工程铁律」段逐字复制生成的，本轮采样中被 Read 次数最高（16 次，高于两个插件模板）。若按角色精简（例如 review 类子代理只需要能触发判定的 2–3 条铁律摘要，不需要全文 7 条+由来），比照 CLAUDE.md 已验证的字节→token 比例（10301 token / 23053 byte ≈ **0.447 token/byte**，见 `docs/token治理/P5-启动底座实测.md`），精简 40%（-1775B）预计每次读取省约 **793 token**，16 次合计约省 **12.7k token**，对应量级 $0.3–0.6——绝对值不大，但这是本轮唯一有数据支撑、且不触碰插件/不改判据的动作。⛔ 本件不实施，留给后续 Phase 评估是否值得为此改 `spec-to-plan`/`run-build` 的 Global Constraints 注入逻辑。
