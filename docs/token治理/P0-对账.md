# P0 对账：外部报告 vs 实测账本（`scripts/token_ledger.py`，窗口 2026-09-01～09-15）

> 实测数据来源：`docs/token治理/baseline-0901-0915.md`/`.json`，全部含子代理（416 会话）；部分行另给「仅主会话」口径（120 会话）以便和报告对齐。

## 依赖日志数字的核验条目（路线图「一」核验表）

| # | 报告说法 | 实测值 | 成立？ | 一句话说明 |
|---|---|---|---|---|
| 4 | `run-lanes.sh` 未指定模型，默认透传 Opus——实际跑的模型待 P0 确认 | 无头泳道（`泳道/opener` 类，102 会话）93 个以 opus 为主导模型，按调用数 opus:4902 / sonnet:250（95% 是 opus） | ✅ 成立 | 代码判断 + 实跑数据一致，无头泳道绝大多数确实在跑 Opus |
| 9 | 子代理必继承 74k 上下文（报告称首轮 74k）——子代理真实首轮待 P0 量出 | 子代理首轮上下文中位数 31k（主会话中位数 43k） | ✅ 已量出 | 子代理首轮远小于主会话，不存在"继承 74k"；这条已可回填进路线图 |

## 报告正文数字对账

| 报告值 | 实测值 | 成立？ | 一句话说明 |
|---|---|---|---|
| 9 月会话数 165 | 416（含子代理）／120（仅主会话） | ❌ 口径不明 | 两个口径都对不上 165；且我们窗口只到 09-15（半月），报告口径覆盖到哪天不明 |
| 总 cache read 26.8 亿 | 11.43 亿（09-01～09-15，含子代理）／7.60 亿（仅主会话） | ⚠️ 口径不同 | 26.8/11.43 ≈ 2.35 倍，量级上接近"半月→整月"缩放，但仅主会话口径对不上（3.5 倍），无法坐实是纯窗口差异 |
| 总 output 1818 万 | 688 万（含子代理）／620 万（仅主会话） | ⚠️ 口径不同 | 1818/688 ≈ 2.64 倍，与 cache read 的缩放比例（2.35 倍）量级相近，支持"报告窗口更长"的猜测，但不能证实 |
| Bash 6520 次 / Agent 343 次 | 含子代理：Bash 9631 / Agent 296；仅主会话：Bash 5085 / Agent 295 | ⚠️ 口径不同 | 仅主会话口径下 Bash（5085 vs 6520，差 28%）、Agent（295 vs 343，差 16%）量级接近，明显比"含子代理"口径更贴近报告；但和 cache read/output 需要的"含子代理"假设矛盾——报告的统计范围本身可能不一致 |
| Opus 129 会话 12.56 亿 | 主会话按主导模型：opus 105 会话，cache_read 6.58 亿；含子代理按主导模型：opus 158 会话 | ❌ 不成立 | 两种口径的会话数（105/158）都对不上 129，cache_read 也对不上 12.56 亿 |
| Sonnet 28 会话 8.94 亿 | 主会话：sonnet 15 会话，cache_read 1.02 亿；含子代理：sonnet 250 会话 | ❌ 不成立 | 会话数与 cache_read 均严重不符，250 vs 28 相差近 9 倍 |
| `<synthetic>` 14 会话 5.34 亿 | 含至少 1 条 synthetic 消息的会话数＝**14**（精确吻合）；这 14 个会话里有真实 assistant 调用的仅 2 个，cache_read 合计仅 1570 万 | ⚠️ 会话数吻合，金额不成立 | "14 会话"这个数字被我们独立复现，但 5.34 亿 cache_read 对不上——synthetic 本身不产生 usage，报告可能把这 14 个会话的"所在批次/关联会话"金额也算了进去，口径不明 |
| 首轮上下文 74k | 主会话首轮中位数 43k；但 `0909A*` 一类续接会话（TD 系列）普遍在 73–75k | ⚠️ 口径不同 | 74k 精确匹配某一类特定 opener（如 `0909AC`74k、`0909AJ`74k、`0909AE`73k），不是全体主会话的中位数（43k）；报告很可能是抽样了这类会话 |
| ≥10KB 大回显 920 次 | 787 次（全部来源合计，含子代理与主会话） | ⚠️ 接近但不等 | 差 15%，方向一致（同量级），不排除窗口边界或去重差异 |
| `cat` 全文 395 次 | 全部 `cat` 调用（不论输出大小）1314 次；仅"输出 ≥10KB 的 cat 调用"150 次（95 个不同文件/命令） | ❌ 不成立 | 两种口径（1314 / 150）都对不上 395，"全文"的定义不明确（是否排除 `cat -n`/`cat file | head` 这类) |
| 峰值会话 `e455a5b6`（sonnet 967 轮 395M） | 该会话时间戳 **2026-08-09～2026-08-18，完全在 8 月**，不在 09-01～09-15 窗口内；即便不限窗口读全文件，也只有 469 次调用、1.92 亿 cache_read（sonnet），约为报告数字的一半 | ❌ 不成立 | 这条是最实的一个反例：该会话根本不属于本次基线窗口，且报告数字（967/395M）约是我们实测（469/192M）的 2 倍，疑似报告统计时未按 `message.id` 去重（同一条 assistant 消息在 JSONL 里按内容块重复出现，未去重会近似翻倍） |
| 峰值会话 `0904Z` | 09-04「泳道批次看护」，sonnet 119 次，peak_ctx 352k，cache_read 27.2M，成本 $7.9（进 Top25） | ✅ 成立 | 会话确实存在、确实是峰值梯队之一 |
| 峰值会话 `0909AJ` | 09-09「TD39真实断网复验」，opus 93 次，peak_ctx 236k，cache_read 13.9M，成本 $11.6（Top25 第 7） | ✅ 成立 | 同上 |
| 峰值会话 `0908B` | 09-08「51四次发版与巡检」，opus 122 次，peak_ctx 233k，cache_read 17.8M，成本 $13.0（Top25 第 5） | ✅ 成立 | 同上 |
| 峰值会话 `8b375086` | 即「[Mac]0909B-值守第4章消息归档开工」，opus 125 次，cache_read 17.6M，成本 $13.1（Top25 第 4） | ✅ 成立 | 报告用哈希文件名指代，实测比对确认是同一会话 |
| 峰值会话 `f0be567b` | 即「[Mac]0908D-网关双供应商与重试转人工开工」，opus 105 次，cache_read 16.4M，成本 $12.5（Top25 第 6） | ✅ 成立 | 同上 |

## 结论

1. **成本 Top 3 来源（按会话类型）**：泳道/opener $528（69%）＞ 子代理 $177（23%）＞ 看护 $60（8%）；交互/其他仅 $3（0.4%）。泳道/opener + 子代理合计占 92%。
2. **首轮上下文中位数**：主会话 43k ｜ 子代理 31k——子代理首轮明显小于主会话，路线图「一」#9 的"由 P0 量出"已回填，报告"子代理继承 74k"的说法不成立。
3. **大回显 Top 3 来源（按字节）**：`Bash:sed` 114 次／1.72MB ＞ `Read:2026-09-03-ai-audit-trail-unitU7-boundary-ci.md` 9 次／0.56MB ＞ `Read:task-1-brief.md` 29 次／0.49MB。
