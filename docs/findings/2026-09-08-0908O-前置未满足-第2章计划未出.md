# 0908O 前置未满足：第 2 章 run-build 空跑，计划文件不存在

- **日期**：2026-09-08 15:18（CST）
- **登记人**：`[Mac]0908O-值守第2章存储基座开工`（run-lanes.sh 无头起）
- **结论**：**未执行任何 run-build**。opener【一】的前置 1、2 双双不满足，按「任一不满足即登记『⏸ 留步』并停」停在该点。
- **本条不是取消**，是等前置。第 2 章范围（2.1–2.6）一条未减。
- **前置文档**：`docs/findings/2026-09-08-0908N-前置未满足-等第1章合并.md`（该文 §4 已预判「0908O 必然空跑」，本次实测坐实）

---

## 1. 前置判据实测值

| # | opener【一】的判据 | 期望 | 实测 | 结论 |
|---|---|---|---|---|
| 1 | `grep -c '^### Task ' docs/superpowers/plans/2026-09-08-hr-wecom-aibot-liaison-unit2-storage-and-idempotency.md` | ≥ 3 | **文件不存在** | ❌ |
| 2 | `ls tools/liaison/requirements.txt`（主工作区） | 存在 | **不存在**（`tools/` 整个目录不存在于 `main`） | ❌ |
| 3 | `pytest -q` 基线 | 记下 | **未跑**（1、2 已否决，不做无意义基线） | — |

判据 1 的文件是 `0908N` 本该产出、但因其自身前置未满足而未产出的那份计划。**run-build 的唯一输入不存在，无从执行。**

⛔ 未自行补写第 2 章计划顶上——那是 `0908N`（spec-to-plan）的职责，且必须落在第 1 章确立的模块布局上；本 session 是 run-build，越界写计划＝绕过 plan 阶段的两阶段 review。

## 2. 第 1 章的实时进度（截至 15:18，仍在推进）

第 1 章没有失败，是**没跑完**，且比 `0908N` 15:17 的快照又前进了：

```
分支      claude/liaison-unit1-channel-skeleton-dev-0908M   （未合并，main..branch = 3）
worktree  /private/tmp/wt-0908M
进程      pid=32888  ELAPSED 16:13  STAT SN  （存活）
```

`0908N` 记录时 worktree 只有骨架与 `requirements.txt`；15:18 已新增 `config.py`、`errors.py`、`__main__.py`（mtime 均为 15:18，即本分钟仍在写）。commit 尚停在 `d54f587`（Task 3/7），新文件未提交。

⛔ 未 kill 该进程、⛔ 未动 `/private/tmp/wt-0908M`、⛔ 未碰该分支、⛔ 未从该分支取文件到 main——口径同 `index.lock`「别人在用就别删」。

## 3. 解除阻塞的判据（谁都能核）

顺序只能是 **第 1 章合并 → 0908N（出计划）→ 0908O（本条）**。重跑本条前四条全过，无需改 opener：

1. `git rev-list --count main..claude/liaison-unit1-channel-skeleton-dev-0908M` = **0**（实测 3）
2. `grep -c '^- \[x\]' openspec/changes/hr-wecom-aibot-liaison/tasks.md` ≥ **6**（实测 0）
3. 主工作区 `tools/liaison/requirements.txt` 存在且 SDK 版本已钉死（第 1 章已定路线①，`wecom-aibot-python-sdk==1.0.2`）
4. `docs/superpowers/plans/2026-09-08-hr-wecom-aibot-liaison-unit2-storage-and-idempotency.md` 存在且 `^### Task ` ≥ 3（实测：文件不存在）

## 4. 本次未落地的守则（重跑时仍需全部执行）

opener【三】四条一条未做，原样留给重跑：

- 2.5 反证：业务写抛异常 → `effect_log` 无记录、重跑重新尝试；变异（幂等记录挪到事务外）必须变红
- 2.3 单连接断言：该连接上不存在第二个事务管理者（monkeypatch 探测）
- 2.6 结构断言要能抓到故意在 `app/` 里加一行 `import tools.liaison`（进程内 monkeypatch，不改文件）
- 只动 `tools/liaison/` 与测试；不碰 `app/`、`scripts/`

## 5. 机制侧遗留（不在本 session 修）

根因仍是 `run-lanes.sh` 缺发车去重导致第 1 章停摆连累整条链。两道建议（发车前 `sort|uniq -d`、发车时 `mkdir` 原子锁）见 `docs/findings/2026-09-08-run-lanes-0908M-重复派发.md`。⛔ 本 session 未实现——该文件由机制泳道在改。

**编排侧的教训**：`0908N`（出计划）与 `0908O`（执行该计划）之间是硬依赖，却被排进了同一批并发发车。同批内有前后依赖的条目应彻底串行，不能靠"排在后面"来指望时序。
