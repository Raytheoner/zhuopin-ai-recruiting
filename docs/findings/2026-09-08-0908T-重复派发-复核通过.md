# `0908T` 被派了第二遍：run-build 早已全量落地，本轮只**独立复核**不重做

**日期**：2026-09-08 15:33–15:40（Asia/Shanghai） ｜ **发现者**：`[Mac]0908T` 无头 session（第二次）
**影响面**：`run-lanes.sh --full-auto` 同一次运行内的轮次接续 ｜ **严重度**：中（不损坏数据，但会静默销毁上一轮日志）

## 一句话

`0908T`（run-build「幂等装饰器撞唯一键改短路」）在同一个 LOGDIR
`.claude/handoff/lanes-20260908-144038/` 里**已经跑过一遍**（14:52–15:20，33 分钟，记 `PARTIAL`），
5 个 Task 与收口两项**全部落地并已在 `main` 上**；15:33 该编号被**再次派发**，就是本 session。

本 session **不重做、不覆盖任何已有产物**，改为做一件第一轮做不到的事：
**在 scratch worktree 里把修法回退掉，独立验证「先红」与「变异」两道判据真的咬得住**，
然后落这份 findings。处置口径与同日的 `0908M` / `0908R` / `0908S` 一致。

## 一、第一轮的产物：5 个 Task 全在 main 上

| 计划 Task | commit | 时间 | 文件 |
|---|---|---|---|
| Task 1 先红 | `80ecea4` | 14:52 | `tests/test_idempotency.py` |
| Task 2 转绿 | `8ea7bd2` | 14:56 | `app/storage/idempotency.py` |
| Task 3 两条边界 | `f12a1ed` | 15:00 | `tests/test_idempotency.py` |
| Task 4 端到端 | `f972e15` | 15:04 | `tests/test_confirm_replay.py`（新建） |
| （review 返工）| `2853116` | 15:18 | `app/storage/idempotency.py` |
| 收口·audit 改 ✅ | `c2ace02` | 15:20 | `docs/audit-and-outbound-ops.md` |

六个 commit 全部 `merge-base --is-ancestor … main` = yes。
**触碰文件与 opener 白名单逐字吻合**，一个越界文件都没有（`tools/liaison/`、`run-lanes.sh` 均未被碰）。

收口四项复核：

- ✅ 合回 main：六个 commit 均在 main 上，无残留未合分支携带本计划的改动
- ✅ `docs/audit-and-outbound-ops.md` §五 已是「✅ **顺带查实的遗留缺陷（早于本次发版）—— 已修 `8ea7bd2` + `2853116`**」
- ✅ ⛔ 未发版（`.51` 无任何操作痕迹）
- ✅ Task 4 的四条断言**全部保持原强度**，没有用计划里许可的"放宽到最小形态"那条口子

**第一轮记 `PARTIAL` 不是失败**：计划「红灯与观察项」明文登记了 3 条 ⛔ 本轮不修
（`_jd_payload` 竞态半截读、fn 内部自撞唯一约束、M2 迁 Postgres 时须换 `UniqueViolation`），
外加 ⛔ 不发版。有未尽项 ⇒ `OPENER_PARTIAL` 正是哨兵的正确取值。

## 二、本轮真正新增的价值：两道判据的独立验证

第一轮的红灯原文与变异输出**随日志一起被本次派发销毁**（见 §三），无从复核。
故本 session 在 `git worktree add --detach /tmp/wt-0908T-verify` 里重建了这两道，**主工作区代码一字未动**。

### ① 先红 —— 回退到 `8ea7bd2^` 的装饰器，新用例红且红在正确的地方

```
$ git checkout 80ecea4 -- app/storage/idempotency.py     # 即 8ea7bd2^ 的装饰器
$ ./venv/bin/python -m pytest tests/test_idempotency.py -q -x -k "race or non_unique"

>       conn.execute(
            "INSERT INTO effect_log (effect_key, thread_id, node_name, business_key, applied_at) "
            "VALUES (?, ?, ?, ?, datetime('now'))",
            (effect_key, thread_id, node_name, business_key),
        )
E       sqlite3.IntegrityError: UNIQUE constraint failed: effect_log.effect_key

app/storage/idempotency.py:70: IntegrityError
FAILED tests/test_idempotency.py::test_unique_key_race_short_circuits_and_rolls_back_this_write
1 failed, 7 deselected in 0.04s
```

红的异常类型、消息、**以及行号 `idempotency.py:70`**，与现网 2026-09-08 13:18 的 traceback
（`docs/audit-and-outbound-ops.md` §五）**逐字同一处**。⇒ 这条用例复现的确实是现网那个 bug，
不是另造了一个长得像的。恢复修法后同文件 **9 passed**。

### ② 变异 —— 把「撞键即 rollback」改成「撞键照常 commit」，恒等式当场变红

```
$ sed  # IntegrityError 分支里 conn.rollback() → conn.commit()   # MUTATION
$ ./venv/bin/python -m pytest tests/test_idempotency.py -q -k "race_short_circuits"

>       assert [r[0] for r in conn.execute("SELECT id FROM job ORDER BY id")] == ["job-winner"]
E       AssertionError: assert ['job-loser', 'job-winner'] == ['job-winner']
E         At index 0 diff: 'job-loser' != 'job-winner'
1 failed, 8 deselected in 0.03s
```

**`job` 表 2 行、`effect_log` 1 行 —— 正是工程铁律 1 恒等式被破的形态。**
⇒ 断言③ 咬住的是不变式本身，不是走过场；计划里 ⛔ `INSERT OR IGNORE` 那条禁令有测试兜底。

⚠️ 注意变异体下 WARNING 日志**照常打印**（`caplog` 断言仍绿）。
所以日志断言单独不足以证明修法正确，**能证明的只有行数恒等那一条**。

## 三、顺带查实的一处损失（与 `0908S` 同型，今日第二次）

`.claude/handoff/lanes-20260908-144038/修复-0908T.log` **已被本次派发截断**
（现 97 字节、mtime 15:34，只剩一行 start 时间戳）。第一轮那 33 分钟的完整日志
——含 review 结论、`2853116` 那次返工的起因——**不可恢复**。

`results.tsv` 是追加的（`0908T PARTIAL 33` 那行还在），日志却按 `<泳道>-<id>.log` 固定名**覆盖**写。
⇒ 重复派发销毁上一轮现场，`docs/findings/2026-09-08-0908S-重复派发-计划已存在.md`
§「顺带查实的一处损失」提的建议 2（日志改 `-r<轮次>` 或改追加）**今天第二次被同一机制兑现**。

## 四、回归实跑（本 session，worktree 内）

| 套件 | 结果 |
|---|---|
| 全量 `pytest -q`（主工作区，基线） | **1240 passed, 1 skipped, 0 failed** |
| `tests/test_idempotency.py` | 9 passed |
| `-m compliance` | 70 passed, 1171 deselected |
| `test_effect_idempotency_suite` + `test_graph_idempotency` + `test_transaction_ownership` + `test_boundary_guard` | **96 passed**（`0908H` 的幂等专项清单守卫一行未改） |
| `tests/test_confirm_replay.py` | 4 passed |

`superpowers:subagent-driven-development` **本轮未调用**——本轮不执行计划，只复核，无 SDD 协议可走。
（该技能在本项目仍取不到，见 memory「superpowers 技能在本项目取不到」。）

## 五、登记，⛔ 本轮不改

1. `tests/test_confirm_replay.py:15` 有一个未使用的 `import json`。纯 lint 级 nit，
   改它会在别人已完成的产物上制造一次无功能收益的 diff，故只登记。
2. 计划「红灯与观察项」的 3 条按计划原样保留，⛔ 本轮不修——尤其第 3 条
   （M2 迁 Postgres 时 `except sqlite3.IntegrityError` 必须同步换 `psycopg.errors.UniqueViolation`，
   否则这个 500 在 Postgres 上原样复活）是迁移时的必查项。

## 相关

- `docs/findings/2026-09-08-0908S-重复派发-计划已存在.md` —— 同日、上游编号、同一机制
- `docs/findings/2026-09-08-run-lanes-0908M-重复派发.md` —— 跨泳道版本，含"每编号一把落盘锁"的建议
- `docs/superpowers/plans/2026-09-08-idempotent-effect-unique-race-short-circuit.md` —— 本轮本该执行、而早已执行完的那份
- `docs/audit-and-outbound-ops.md` §五 —— 现网 traceback 与「✅ 已修」标注
