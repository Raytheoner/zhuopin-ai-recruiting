# 0908N 前置未满足：第 2 章存储基座计划未出，等第 1 章合并

- **日期**：2026-09-08 15:17（CST）
- **登记人**：`[Mac]0908N-值守第2章存储基座计划`（run-lanes.sh 无头起）
- **结论**：**未产出** `docs/superpowers/plans/2026-09-08-hr-wecom-aibot-liaison-unit2-storage-and-idempotency.md`。按 opener 写明的预案「前置不满足 → 停，登记『等 0908M』」停在该点。
- **前置文档**：`docs/findings/2026-09-08-run-lanes-0908M-重复派发.md`（0908M 本体的让位记录）

---

## 1. 判据实测值

opener 的前置：`grep -c '^- \[x\]' openspec/changes/hr-wecom-aibot-liaison/tasks.md` 应 ≥ 6（第 1 章 1.1–1.6 已合并并回勾）。

| 项 | 期望 | 实测 |
|---|---|---|
| `tasks.md` 已勾条数 | ≥ 6 | **0**（本地与 `origin/main` 同值） |
| 主工作区 `tools/liaison/` | 存在 | **不存在**（`tools/` 整个目录不存在） |
| 第 1 章代码在 `main` 上 | 是 | **否** |

不满足。⛔ 未按不存在的骨架推测着写计划——第 2 章计划要落到第 1 章确立的模块布局、`requirements.txt` 与测试目录上，对未合并且仍在变动的分支写计划＝制造返工。

## 2. 第 1 章的真实进度（截至 15:17）

第 1 章没有失败，是**没跑完**。0908M 让位后，重复体接手仍在推进：

```
分支  claude/liaison-unit1-channel-skeleton-dev-0908M   （未合并，main..branch = 3）
worktree  /private/tmp/wt-0908M
d54f587 feat(liaison): Task 3 路线① — 钉死 SDK 1.0.2 发行版本及冒烟测试
8c4e7a1 docs(liaison): SDK 在 Python 3.14 兼容性实测——路线①（判据 A/B/C 全过）
8fe3218 feat(liaison): 第 1 章 1.1 任务——包结构骨架、边界 README 与隔离断言
```

- 已落 10 个文件 / 549 行：`tools/liaison/` 骨架、边界 README、`requirements.txt`、`probe_sdk_py314.py`、两份测试
- **SDK 路线已定＝路线①**（`wecom-aibot-python-sdk` 在 Python 3.14 可用，版本钉死 `1.0.2`），WBS 1.3 的二选一已有结论
- 进度约 **Task 3 / 7**，尚差 1.4 凭据校验、1.5 `.env.example`、1.6 单测与两阶段 review
- 执行进程 `pid=32888` 15:17 时存活（ELAPSED 13:58，状态 `SN`），确在推进

⛔ 未 kill 该进程、⛔ 未动 `/private/tmp/wt-0908M`、⛔ 未碰该分支——口径同 `index.lock`「别人在用就别删」。

## 3. 解除阻塞的判据（谁都能核）

第 1 章合并后重跑 0908N 即可，无需改 opener。放行判据三条全过：

1. `git rev-list --count main..claude/liaison-unit1-channel-skeleton-dev-0908M` = **0**
2. `grep -c '^- \[x\]' openspec/changes/hr-wecom-aibot-liaison/tasks.md` ≥ **6**
3. 主工作区 `tools/liaison/` 存在，且 `tools/liaison/requirements.txt` 里 SDK 版本号已钉死

## 4. 连带影响

- **`0908O`（第 2 章 run-build）必然空跑**：它的输入就是本次未产出的那份计划。重跑顺序只能是 第 1 章合并 → `0908N` → `0908O`。
- 本条**不是取消**，是等前置。第 2 章范围（2.1–2.6）一条未减。

## 5. 机制侧遗留（不在本 session 修）

第 1 章这次停摆的根因是 `run-lanes.sh` 缺发车去重，同一 opener 被派成两个并发 session；两道建议（发车前 `sort|uniq -d`、发车时 `mkdir` 原子锁）已写在 `docs/findings/2026-09-08-run-lanes-0908M-重复派发.md`，⛔ 本 session 未实现——该文件由机制泳道在改。
