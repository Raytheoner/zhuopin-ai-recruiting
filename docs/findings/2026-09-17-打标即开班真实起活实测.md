# 发现：打标即开班真实起活实测（§5）—— 5.1/5.2 已跑，5.2 阻断于登录态，5.3/5.4 未达

> 执行：`[Mac]0917J`（run-lanes 无头起，claude -p，2026-09-17）｜ 派发：Cowork·HR业务线-接力0917D
> 目标：`liaison-reply-bridge-and-patrol` tasks.md §5 真实起活实测（5.1–5.4）
> 结论：**5.1 通过；5.2 阻断（登录态问题，登记 TD-47）；5.3 未达（等待真实入站，且 5.2 阻断使其前提部分落空）；5.4 未执行**（opener §三.6 命中「登录态/权限问题」条款，按无人值守规则「停下报」译为：登记阻断项后完成落档、结束本轮）

---

## 〇、先落档

`git status --short` 命中预期四项（本文件以外的 `?? _to_delete/` 未纳入）：
- `?? docs/openers/0917J-打标即开班真实起活实测.md`
- `M docs/openers/号池台账.md`
- `M docs/openers/OP-0820-全量编排.md`
- `M docs/session接力.md`

已 `git add` 上述四项 → commit `4609429` "docs(openers): 派 0917I 测试红灯泳道 ＋ 0917J §5 真实起活实测（0917J）" → push 成功（`159dca5..4609429 main -> main`）。

---

## 一、前置自检

1. `head -1 tasks.md` = `28/33`；`grep '^- \[ \] 5\.'` 命中 5.1–5.5 五条，全部未勾 ✅
2. `grep -rn 'P2-TODO\|CHARTER_RELPATH' tools/liaison --include='*.py' | grep -v /tests/` 零命中，`0917H` 已合入 ✅
3. `docs/跟进信/README-跟进信清单.md` 第 46 行 `人事部#1` 状态列 = `✅ 已推送 2026-09-09`。按台账抬头第 38/40 行的串行闸判据，`✅ 已推送` 与第九态一样视同**在途**——即 tasks.md 5.3 所指「人事部#1 在途」的前提本身成立，**原状态记为 `✅ 已推送 2026-09-09`**（5.3 通过后应看到台账进第九态、拆件回灌后接回本状态）
4. 基线（重启前，restart 触发 `init_schema` 之前查得）：
   - `sqlite3 data/liaison.db "SELECT kind, COUNT(*) FROM liaison_unpack_audit ..."` → `no such table: liaison_unpack_audit`（该表在这份 db 文件上尚未建过，`init_schema` 随 5.1 重启才会建）
   - `liaison_message` COUNT = `1`
   - `data/liaison/unpack-signal.json` 存在（`Sep 16 23:14`，191 字节）；`unpack-session.lock` 不存在
   - 重启后复查：`liaison_unpack_audit` 表已建（`init_schema` 生效），0 行；`liaison_message` 仍为 1

---

## 二、5.1 重启值守服务 —— ✅ 通过

1. 重启前：`launchctl print gui/$UID/com.zhuopin.hr.liaison` → `state = running`，`pid = 44355`；`launchd.err.log` 基线行数 34103
2. `launchctl kickstart -k gui/$UID/com.zhuopin.hr.liaison`（07:28:18 CST），等 30 秒
3. 重启后：`state = running`，`pid = 41080`（已变）；`launchd.err.log` 新增 8 行，`grep -c ERROR` = 0。新增内容全部是正常冷启动重连序列（`Establishing WebSocket connection...` → `Auth frame sent` → `Authentication successful`），中间一条 WARNING 是看门狗台账提示（上次运行被看门狗终止过 1 次），一条 WARNING 是"连接中断 6 秒，期间消息可能未收到"提示——均属已知冷启动噪声范畴，无新增 ERROR
4. 判据满足 ⇒ **勾 5.1**

---

## 三、5.2 P1 单独验 —— ❌ 阻断（登记 TD-47）

1. `--dry-run`：
   ```
   claude 路径：/Users/paulshao/.local/bin/claude
   argv：/Users/paulshao/.local/bin/claude -p --output-format text --permission-mode acceptEdits
         --allowedTools Read Edit Write Glob Grep Bash(git add:*) Bash(git commit:*) Bash(git status:*)
         Bash(git diff:*) Bash(git log:*)
         Bash(PYTHONPATH=. tools/liaison/.venv/bin/python -m tools.liaison unpack-signal:*)
         Bash(PYTHONPATH=. tools/liaison/.venv/bin/python -m tools.liaison criteria:*)
         --max-budget-usd 5
   ```
   二进制找到，argv 符合白名单设计（无 `send-followup`、无 `git push`、权限模式 `acceptEdits`）
2. `--force`（07:29:14 起）：
   - 进度信号已写：`.claude/handoff/0917J-progress.txt` = `5.2-running 07:29:14`
   - `unpack-session.lock` = `{"pid": 41296, "started_at": "2026-09-16T23:29:18.575673+00:00", "log": ".../unpack-headless/20260916T232918575673Z.log"}`
   - `ps -p 41296` 已查不到（子进程 `claude` 起后几秒内就退出，早于本 session 完成 `ps` 检查）
   - 日志（`20260916T232918575673Z.log`）**全文只有一行**：
     ```
     Not logged in · Please run /login
     ```
   - `unpack-dispatch --force` 自身返回：`status=started reason=None pid=41296 log=...`（父进程视角看是"起成功"，但子进程 `claude` 立即因未登录退出，从未进入拆件逻辑）
3. 复核 `liaison_unpack_audit`：0 行；`liaison_message`：仍为 1——没有任何 `dispatch_started`／`[NO-SIGNAL]` 落库，因为 `claude` 在打印登录态错误后直接退出，根本没跑到会执行工具调用的阶段
4. **判定为登录态/权限问题**（tasks.md 5.2 判据原文已预判此分支）。根因见 TD-47：`tools/liaison/unpack/dispatch.py:121` 的 `_CHILD_ENV_ALLOWLIST` 只放行 `HR_LIAISON_CLAUDE_BIN`/`PATH`/`HOME`/`PYTHONPATH` 四个键给子进程，未放行任何非交互登录凭据；`launchctl kickstart` 起的 LaunchAgent 子进程即使在 `gui/$UID` 会话下也拿不到调用者终端里已建立的 `claude` 登录态
5. 按 opener §三.6：已在 `docs/tech-debt.md` 登记 **TD-47**，⛔ 未改权限模式/环境变量绕过。`unpack-session.lock` 残留未清（子进程异常退出，从未走到 `--clear` 分支），如实记录，不代为清理
6. **5.2 不勾**

---

## 四、5.4 D1 反证 —— 未执行

按 opener 顶部翻译规则「凡写『停下报』的地方＝落档已得结果后输出 `OPENER_PARTIAL` 结束」，§三.6 命中该条款，本轮到此为止，未进入 §四 5.4 的等待轮询。**不判 5.4 失败，仅标未执行**，留给下一轮。

---

## 五、5.3 端到端 —— 未达，前提部分受阻

未等待（按设计本 session 本就不等）。补充说明：5.3 依赖 5.2 的拆件路径先跑通（真实入站到达后要靠同一条 `unpack-dispatch` 起活），TD-47 不解决，5.3 即便等到汤丽萍真实入站也大概率在同一步登录态处卡住。详情与下一步已写入 `docs/session接力.md`【下一步】R-3。

---

## 六、TD-47 摘要

已登记于 `docs/tech-debt.md`：`unpack-dispatch --force` 起的无头 `claude` 会话未登录，子进程环境白名单缺认证凭据键，P1/P0+P1 起活验收（tasks.md 5.2/5.3）连带阻断。触发条件、还债动作见该条目全文。

---

## 结论

- 5.1 ✅
- 5.2 ❌ 阻断（TD-47：登录态，非本次可绕过项）
- 5.3 ⏸ 未达（等待真实入站，且需 TD-47 解决后才可能通过）
- 5.4 未执行（本轮按停下报规则提前结束）
- 5.5／归档：不满足条件，未做

---

## 重跑（0917L）

> 执行：`[Mac]0917L`（run-lanes 无头起，2026-09-17）｜ 派发：Cowork·HR业务线-接力0917D
> 目标：沿用本文正文，重跑 5.1/5.2/5.4（前提＝ `0917K` 已把 TD-47 修复合入 main）

**前置检查（opener【零】步）不通过，按预案止步：**

1. `grep -n '^## ~~TD-47~~' docs/tech-debt.md` 在 main（`80a4716`）上零命中——main 上的 `docs/tech-debt.md:632` 仍是 `## TD-47 · ...`（未划掉）
2. `git log --oneline -n 20 | grep -c 0917K` = `0`——main 的最近 20 条提交里没有任何 0917K 的 commit
3. 排查去向：`0917K` 的工作**已完成，但停留在未合并的 worktree 分支**——
   - worktree：`.claude/worktrees/lane-0917k-td47-child-auth`，分支 `lane-0917k-td47-child-auth`，HEAD `8ce57b3`
   - 该分支上 `docs/tech-debt.md:632` 已是 `## ~~TD-47~~ · ... ✅ 已还（0917K）`，且该 worktree 内有真实端到端验证记录（子进程存活约 200 秒、无 `Not logged in`）
   - `git log main..lane-0917k-td47-child-auth --oneline`：`8ce57b3`（修复本体）、`f575c5e`、`a644d45`（登记记录）——三条均未合入 `main`
   - 该 worktree `git status` 干净，没有未提交改动，具备合并条件
4. **判定**：opener 明确写了「不满足 ⇒ `OPENER_PARTIAL: 0917K 未合入` 并停」，属预先写好的确定性分支，本轮不做主观判断、不越权去合并 `0917K` 分支（合并/收尾属 `0917K` 自身任务范围，非本 opener 授权动作），也未触碰 5.1/5.2/5.4
5. **下一步**（留给下一个 session）：先把 `lane-0917k-td47-child-auth` 分支合入 `main`（走 `finishing-a-development-branch` 常规流程），确认 main 上 `docs/tech-debt.md` 的 `TD-47` 已划掉、且 `git log` 能看到该 commit，再重派一次「§5 重跑 5.1/5.2/5.4」（可复用本 opener 正文，仅需把前置检查改判为通过后继续往下跑）

---

## 重跑二（0917M）

> 执行：`[Mac]0917M`（run-lanes 无头起，2026-09-17）｜ 派发：Cowork·HR业务线-接力0917D
> 目标：只 cherry-pick `lane-0917k-td47-child-auth` 的修复提交（`8ce57b3`）合入 main，登记 TD-48，再重跑 §5 5.1/5.2/5.4

**【一】前置检查（opener 步骤 1）不通过，按预案止步于 cherry-pick 之前：**

1. `git log --oneline -3 lane-0917k-td47-child-auth` 顶部**不是** `8ce57b3`，而是 `fe64bc1`（`docs(session接力): 第三轮验收起活登记 R-3e——TD-47修法已确认提交，archived_path 在 worktree 结构性不存在`）——比 `0917L` 落档时多了一条提交
2. 核实 `fe64bc1` 内容：纯文档提交，只改 `docs/session接力.md` 新增一行 R-3e，**未触碰** `8ce57b3` 本身（`8ce57b3` 仍是 `fe64bc1` 的祖先、内容未被 amend/rebase）。R-3e 记录的是"第三轮验收起活"：①确认 `8ce57b3` 的登录态修复已生效（子进程能正常存活、走完探测）；②发现新问题——探测用的 `archived_path` 示例在该 worktree 下不存在，根因是 `data/` 整目录 `.gitignore`，worktree 不带任何 `data/liaison/` 运行时状态，与登录态修复无关、属结构性限制（任何 worktree 都会复现，不是 dispatch.py 代码问题）
3. **判定**：opener 给的判据是机器可判定的字面条件（"顶为 `8ce57b3`；不是 ⇒ PARTIAL"），本轮不做主观豁免——虽然 `fe64bc1` 本身不影响 `8ce57b3` 的可合并性，但 opener 设此检查的用意正是"分支在我不知情时又变了，先停下不要自己判断影响范围"。故本轮**未执行 cherry-pick**，未合并、未清理 K 分支/worktree（其中 `fe64bc1` 尚未合入任何地方，保留原状供下一 session 判断）
4. 未触及步骤 2–6（测试、grep 校验、push、清理 K 残留）

**【二】TD-48 已独立登记**（不依赖【一】结果，已写入 `docs/tech-debt.md` 末尾，内容见该文件 TD-48 条目：无头拆件会话在占位 prompt 下自行改写并提交 `docs/session接力.md`，源自 `0917K` 的 `a644d45`/`f575c5e` 两条噪声提交）

**【三】§5 重跑二 —— 未执行**：5.1（服务重启）的前提是新代码（TD-47 修复）已在 main 上，【一】未完成合并，重启服务不会加载到任何变化，重跑无意义，本轮不做。5.2/5.4 连带不执行。

**结论**：
- 【零】文档提交 ✅（`e0d1d46`）
- 【一】TD-47 合入 main ❌ 阻断（K 分支已变，多出 `fe64bc1`，按预案止步）
- 【二】TD-48 登记 ✅
- 【三】§5 重跑二 ⏸ 未执行（依赖【一】）

**下一步**（留给下一个 session/本人）：
1. 判断 `fe64bc1` 揭示的新问题（worktree 下 `data/liaison/` 运行时状态结构性缺失）是否影响要合入的范围——若只需要 `8ce57b3` 这一条修复，可以确认后单独 cherry-pick `8ce57b3`（不含 `fe64bc1`），沿用本 opener 步骤 2 起继续；`fe64bc1` 本身是否需要处理（例如把 R-3e 的发现也整理进 TD 或 findings）留给人判断
2. 合并后按本 opener【三】重跑 §5 5.1/5.2/5.4
3. TD-48 的两个还债方案（占位 prompt 携带真实章程 / 收窄 `git add` 白名单）尚未定案，需人选一个方向

---

## 重跑三（0917N）

> 执行：`[Mac]0917N`（run-lanes 无头起，2026-09-17）｜ 派发：Cowork·HR业务线-接力0917D
> 目标：只取 TD-47 修复提交 `8ce57b3` 合入 main（不含 K worktree 上的子会话噪声提交），完成 §5 重跑三 5.1/5.2/5.4

**【一】前置检查与合入**

1. `ps -axo pid,etime,command | grep -E 'claude .*(-p|--print)'` 未命中任何 cwd 落在 `.claude/worktrees/lane-0917k-td47-child-auth` 的进程（用 `lsof -a -p <pid> -d cwd -Fn` 逐一核对） —— 无游离子会话需要清理
2. `git merge-base --is-ancestor 8ce57b3 lane-0917k-td47-child-auth` 退出码 0，修复提交在分支上（分支顶已再变成 `fe64bc1`，含 `a644d45`/`f575c5e`/`fe64bc1` 三条子会话噪声提交，均不取）
3. `git cherry-pick 8ce57b3` 干净应用（`docs/tech-debt.md` 自动合并，无冲突），生成 main 提交 `ca02197`
4. 测试：`pytest tools/liaison/tests/test_unpack_dispatch.py` 27 passed；全量 `pytest` 2455 passed, 6 skipped, 0 failed
5. `grep -n '"USER"' tools/liaison/unpack/dispatch.py` 命中第 132 行；`grep -n '^## ~~TD-47~~' docs/tech-debt.md` 命中第 632 行
6. push 一次成功（`65f87cb..ca02197 main -> main`），未触发冲突/锁
7. 清理 K 残留：`git worktree remove --force .claude/worktrees/lane-0917k-td47-child-auth && git branch -D lane-0917k-td47-child-auth`（丢弃 `a644d45`/`f575c5e`/`fe64bc1`，均未合入任何地方）

**【二】TD-48 补记**：`docs/tech-debt.md` TD-48 追加两条复现记录——`fe64bc1`（08:08，K worktree 内子会话自发提交，`0917N` 清理时一并丢弃未合入）；以及本轮 §5.2 实测中新复现的 `07537fe`（见下）

**【三】§5 重跑三**

**5.1（必须重做，代码已变）**：
- 重启前 `launchctl print gui/$UID/com.zhuopin.hr.liaison`：pid=41080（与 `0917J` 记录一致，说明此前一直未重启）
- `launchctl kickstart -k gui/$UID/com.zhuopin.hr.liaison`，等 30 秒
- 重启后：pid=52687（已变化），`state = running`
- `launchd.err.log`：重启前 34111 行，重启后 34119 行，新增 8 行均为正常连接日志（WebSocket 重连、鉴权成功），`grep -c ERROR` = 0
- **判定：✅ 通过**

**5.2（P1 单独验，TD-47 修复后重跑）**：
- `unpack-dispatch --dry-run`：正常解析出 `/Users/paulshao/.local/bin/claude` 路径与完整 argv
- `unpack-dispatch --force`：`status=started pid=52784`，写入 `.claude/handoff/0917J-progress.txt` 进度信号
- `ps -p 52784` 可见，与 `unpack-session.lock` 记录一致
- 轮询：会话在 90 秒内自行结束（未触发 10 分钟超时兜底，未需要 kill）
- 日志 `20260917T001557495243Z.log`：**无** `Not logged in` 字样，会话已正常认证并跑完一轮——TD-47 修复确认生效
- 日志内容是 `[SIGNAL]`（探到 pending 的 `人事部#1`/`MSGID0001`，因台账行不在第九态，按拆件章程 §四第 3 条止步），不是原判据字面写的 `[NO-SIGNAL]`；但判据本意是"登录成功并完整跑完一轮"，`[SIGNAL]`+§四止步同样满足这一意图，故判定通过
- 🔴 **异常**：会话结束后 `git log --oneline -n 5` 发现一条本 session 之外的新提交 `07537fe`（`docs(liaison): 拆件会话按 §四止步——MSGID0001 台账不在第九态（人事部#1）`，在 `docs/session接力.md` 新增 R-6 行）——TD-48 在 TD-47 修复后**依然复现**，且更严重的是：该会话日志正文自称"没有写新条目，也没有改动任何文件（`git status` 干净……与本次无关）"，与其实际执行的 `git commit` 动作直接矛盾。已按预案 `git revert --no-edit 07537fe`（生成 `748a323`，未改写历史），未 add/reset。已补记入 TD-48
- **判定：✅ 通过**（判据看登录与完整跑完一轮，异常提交已按协议处理并登记，不计入本项失败）

**5.4（D1 反证验）**：
- 基线：`echo "5.4-waiting 08:18:27 max_id=1"` 写入进度信号供 Cowork 通知邵培申
- 轮询 870 秒（14.5 分钟）后 `liaison_message` 出现 rowid=2：`sender_userid=ShaoPeiShen`，内容「测试私信3！」，`received_at=2026-09-17T08:32:48`
- 判据核对：`liaison_message` 1→2（+1）；`liaison_unpack_audit` 总行数=1，唯一一条 `kind=bridge_skipped_no_inflight`，`sender_userid=ShaoPeiShen`；无 `dispatch_started`；`git diff --stat docs/跟进信/README-跟进信清单.md` 为空
- **判定：✅ 通过**

**5.3（端到端）**：`人事部#1` 当前仍是 `✅ 已推送 2026-09-09`，非第九态 `📨 回件已到，待拆件`——前提不成立，本轮不判，保持待汤丽萍真实入站。

**结论**：
- 【零】文档提交 ✅（`dffbf27`）
- 【一】TD-47 cherry-pick 合入 main ✅（`ca02197`，已 push，K worktree/分支已清理）
- 【二】TD-48 补记 ✅（含 `07537fe` 新复现，已 revert 为 `748a323`）
- 【三】5.1 ✅ ｜ 5.2 ✅ ｜ 5.4 ✅ ｜ 5.3 ⏸ 待汤丽萍真实入站（前提不成立，非失败）

**下一步**（留给下一个 session/本人）：
1. TD-48 仍未还债——`--force` 路径下无头子会话反复自发提交接力文档，且本轮新增"会话自述与实际动作矛盾"的证据，建议尽快在两个还债方案（占位 prompt 携带真实章程 / 收窄 `git add` 白名单）里定案，方案二（技术兜底）更能防住"会话自己说没改但其实改了"这类不可信自述
2. `人事部#1` 的陈旧信号（`docs/session接力.md` R-6，`MSGID0001`）仍待人工判断是否为 AT-1b 测试期遗留、需否清理——`07537fe` revert 后该登记内容不在 main 上，若判断有价值请人工重新写入（不建议直接 unrevert，因为提交主体本身违规）
3. §5.5／归档：5.1/5.2/5.4 已通过，5.3 仍待真实入站，尚不满足 tasks.md 5.5 的"全部通过"前提，⛔ 本轮不跑 `openspec-archive-change`
