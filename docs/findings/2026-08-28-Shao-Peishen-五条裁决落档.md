# 2026-08-28 Shao Peishen 五条裁决落档

> **批准人**：Shao Peishen（本项目唯一决策人）
> **时间**：2026-08-28
> **事项**：本文件第一至第五条，全部
> **依据**：本人逐条答复原话——「一、继续不设；二、派U3；三、直接挂到 8.1 底下当告警；
> 四、部署脚本里锁定工作目录；五、把 7.1 从 U7 拎出来提前做」
> 留痕格式按 `CLAUDE.md`「决策代理」的要求。⛔ 无留痕的代批视为未决。

本文件是这五条的**索引与依据**；每条的可执行形态已就地写进各自的真源文件，见「落点」列。

| # | 事项 | 结论 | 落点 |
|---|---|---|---|
| 1 | 决策代理人人选 | **继续不设** | `CLAUDE.md` 决策代理表 |
| 2 | 下一步派工 | **派 U3**（⚠️ 见下方风险登记） | 本文件 §2 |
| 3 | `_metrics_counter` 计数膨胀的登记去向 | **挂到第 8 章 8.1 底下当告警，⛔ 不新开 TD 条目** | `m1-intake-quality-fixes/tasks.md` 8.1 |
| 4 | 开关文件路径按进程 CWD 解析 | **取「部署脚本里锁定工作目录」，代码不动** | `ai-audit-trail-and-outbound-gate/tasks.md` 第 1 章「遗留二」＋ 第 5 章章头 |
| 5 | 开关文件编码约束的排期 | **从 U7 拎出来，改为 U5 的前置项** | 同上 `tasks.md` 7.3 ＋ `delivery-units.md` §一/§2.U7 |

---

## 1. 决策代理人：继续不设

`CLAUDE.md` 代理人那格由「⬜ 待 Shao Peishen 指定」改为「⬜ **继续不设**（2026-08-28 Shao Peishen 决定）」。

⛔ **这不是"已指定"。**「代理人未指定期间的默认：上表「可代」项同样一律挂起等本人」
那条规则**原样继续有效**——留空与明确不设，对下游的约束完全相同，
区别只在于以后再看到那格是空的，不必再问一次"是不是漏填了"。

## 2. 下一步派工：派 U3

Shao Peishen 另起 session 跑 U3 的 run-build，本 session 不执行，仅登记。

> ⚠️ **风险登记（本 session 于落档当次提出，未获答复前不作数）**：
> **U3 在这条裁决作出之前已经完成。** `ai-audit-trail-and-outbound-gate/tasks.md`
> 第 3 章 **7/7**，两轮 review 通过，九笔 `6bdf6e6`→`6861ab2` 已在 `main` 且已推 origin
> （回勾 commit `6861ab2`）。
> 这条裁决回应的是本 session 第一份状态报告里的问题 A2——当时第 3 章确实是 0/7；
> 从提问到答复之间，仓库又前进了 13 笔。
> **按字面执行会重做已交付的工作**，且新 session 要改的 `app/llm/gateway.py`、
> `app/audit/recorder.py`、`app/main.py` 与第 3 章正是已交付的部分。
> **当前包内真正无人认领的是 U5（第 5 章 0/9）** ——且 U5 已有计划、已开工一轮（`0828B`，
> PARTIAL），成果挂在 `worktree-audit-u5-queue-and-wiring`（真未合 9 笔，未合入 main）。
> 处置建议：改派 U5 接续，或先确认 U3 是否指别的东西。**等 Shao Peishen 定，本 session 不擅自改派。**

## 3. `_metrics_counter` 按 O(q·R²/2) 膨胀：挂 8.1 当告警

**不新开 TD 条目**（`docs/tech-debt.md` 现有编号已到 TD-7，本条明确不占号）。
警告实质搬进 `m1-intake-quality-fixes/tasks.md` 的 8.1 条目下，源注释仍留在
`app/agents/intake_question.py:38-64` 作为代码侧的现场说明，两处并存不算重复登记
——tasks.md 那份是给做 8.1 的人看的，源码那份是给改 `derive_question_id` 的人看的。

**为什么必须进 tasks.md**：做 8.1 的人读的是 WBS，不是 `intake_question.py` 的第 38 行。
这条不进 WBS，8.1 会拿一份被轮次位置加权的比例当"修复前后对比"写进
`docs/m1-demo-pilot-feedback.md`——**不报错、不红灯，直接产出一个错结论**。
这是本轮清单里唯一一条**沉默出错**的。

## 4. 开关文件路径按 CWD 解析：锁定工作目录

第 1 章「遗留二」由「未裁决」改为已拍板。**取「在部署脚本里锁定工作目录」**，
⛔ 不改用绝对路径配置、⛔ 不动代码。U5 接线时按此口径，不再重开这个决定。

## 5. 编码约束排期：从 U7 提前到 U5 之前

⚠️ **编号订正**：裁决原话说的是「7.1」，实际条目号是 **7.3**（7.1/7.2 是"禁止
`zhuopin_platform` 依赖"那两条 CI 检查，与开关文件编码无关）。本 session 在提交
裁决前的最后一份报告里已作过这处订正，裁决的括注「开关文件编码约束的运维文档」
指向明确，故按 **7.3** 落地。

`delivery-units.md` 原把整个第 7 章排在最后，而 `tasks.md` 7.3 写着
「**U5 接线前必须确认本条已落地**」——排期与前置条件互相打架。本条裁决解掉这处矛盾：
7.3（连同同属纯文档的 7.4）**不再属于 U7 的尾部排期，改列为 U5 的前置项**。

**执行现状**：7.3 / 7.4 已于 2026-08-28 由 `0828C` 落地并回勾（`db6596e`），
产出 `docs/audit-and-outbound-ops.md`。该页第五节尚有 **三项 `.51` 上机留步**，
其中第 3 项（在 `.51` 上按 4.1 实际创建一次开关文件）**仍是 U5 接线前的未闭合前置**
——那属「生产服务器 `.51`」范围，是不可代项。
