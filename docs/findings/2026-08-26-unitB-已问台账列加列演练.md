# 交付单元 B 加列演练留痕（`job_profile.asked_questions`）

- 日期：2026-08-26
- 对应：`docs/superpowers/plans/2026-08-19-…-unitB-fallback-options-and-budget.md` Task 7 Step 1，
  design.md Migration Plan 第 1 条
- 执行人：Claude（run-build 会话，分支 `claude/delivery-unit-b-run-build-dd9e53`）

## 为什么要留这份

演练是一次性动作，跑没跑过不留痕就无法证实——上一轮收尾就卡在这里。测试
（`tests/test_db_migration.py`）证明的是"合成老库能升级"，这份证明的是"**本机
那个真的有 22 行历史数据的 `data/demo.db` 能升级**"，两者不互相替代。

## 怎么跑的

`data/` 在 `.gitignore` 里，worktree 中没有这个目录，因此从主检出
`/Users/paulshao/Projects/HumanResource/data/` 复制了 `demo.db` 及其 `-wal` / `-shm`
到 session scratchpad（**只读原库、全程只动副本**；演练前后原库三个文件的 mtime
未变：`demo.db` Aug 10 18:06、`demo.db-wal` Aug 18 13:00、`demo.db-shm` Aug 25 06:44）。
`-wal` 必须一起复制，否则读到的是 checkpoint 之前的旧快照。

副本上跑 `app.storage.db.init_schema()`（即 `apply_column_migrations` 的幂等加列路径），
连跑两次。

## 副本演练前

```
cols_before: ['created_at', 'id', 'job_id', 'profile_json', 'status', 'unspecified_fields', 'version']
job_before: 5
job_profile_before: 22
```

即 2026-08-18 之前的老形态，7 列，`is_productive` / `asked_questions` 都还不存在。

## 副本演练后（原始输出）

```
asked_questions in cols: True
jobs: 5
job_profile rows: 22
rows with default ledger: 22
productive count query works: 22
cols_after: ['asked_questions', 'created_at', 'derived_unspecified_fields', 'id', 'is_productive', 'job_id', 'llm_latency_ms', 'llm_response_model', 'profile_json', 'status', 'turn_started_at', 'ungrounded_fields', 'unspecified_fields', 'version']
second init_schema ok, rows still: 22
profile_json 抽样（老数据一字不动）: {"job_title": "嵌入式开发工程师", "core_skills": [{"name": "驱动开发",
```

## 结论

1. 加列成功，7 列 → 14 列，`asked_questions` 在内。
2. 历史数据一行不少：5 个 job、22 行 `job_profile`，`profile_json` 内容未变。
3. 全部 22 行拿到默认台账 `'[]'`，**不需要回填**——读台账的代码不必为老库写特例。
4. `SELECT COUNT(*) … WHERE is_productive=1` 在老库上可用，返回 22（默认值按"有产出"
   算，与升级前的行为一致）。
5. `init_schema` 二次执行不报错、行数不变，幂等成立。

`.51` 现网升级仍按第 8 章 8.3 的顺序做（先备份再升级），这份只覆盖本机 demo 库。
