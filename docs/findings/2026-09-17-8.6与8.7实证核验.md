# 发现：8.6/8.7 实证核验（`hr-wecom-aibot-liaison`）

> 执行：`[Mac]0917V`（run-lanes 无头起，2026-09-17 12:29 CST）｜ 派发：Cowork·HR业务线-接力0917D
> 目标：逐条对照 `openspec/changes/hr-wecom-aibot-liaison/tasks.md` 8.6、8.7 原文判据，用真实证据核，不推断、不代批
> 结论：**8.7 通过，勾选**；**8.6 三条链路里只有「群里发文字 → 入队」有真实证据，「私信发文档 → 归档」与「群通知 → 回推」均未发生过一次真实投递，不勾**

---

## 一、证据源与取数命令（只读，`mode=ro`）

```
sqlite3 "file:data/liaison.db?mode=ro" \
  "SELECT msgid, thread_id, sender_userid, received_at, msgtype, attachments_json \
   FROM liaison_message ORDER BY received_at;"
```

| msgid（前 8 位） | thread_id | sender_userid | received_at | msgtype | attachments_json |
|---|---|---|---|---|---|
| `2a376681` | ShaoPeiShen | ShaoPeiShen | 2026-09-16T13:55:09+08:00 | text | `[]` |
| `ee4cac00` | ShaoPeiShen | ShaoPeiShen | 2026-09-17T08:32:48+08:00 | text | `[]` |
| `b8b523c7` | wrvDL_DAAAnkeGLkk1_bu2Ne1oQfc4BA（群） | **TangLiPing** | 2026-09-17T09:45:44+08:00 | text | `[]` |
| `a932acca` | TangLiPing（私信） | **TangLiPing** | 2026-09-17T09:46:13+08:00 | text | `[]` |

```
sqlite3 "file:data/liaison.db?mode=ro" \
  "SELECT id, msgid, thread_id, sender_userid, send_status, pushed_at FROM liaison_task ORDER BY id;"
```

四条队列行，`send_status` **全部 = `pending`，`pushed_at` 全部 = `NULL`**（无一例外）。

```
sqlite3 "file:data/liaison.db?mode=ro" "SELECT COUNT(*) FROM liaison_group_notify;"   -- 0
```

```
find data/liaison/archive -type f
  data/liaison/archive/ShaoPeiShen/20260917/ee4cac003bff178f83a7c044e04d4bcb__正文.txt
  data/liaison/archive/TangLiPing/20260917/a932acca20f0ce42ab267c29f4fb5bbe__正文.txt
  data/liaison/archive/wrvDL_DAAAnkeGLkk1_bu2Ne1oQfc4BA/20260917/b8b523c7b96a4ac73f7a799431e8c570__正文.txt
```

```python
PYTHONPATH=. python3 -c "
import sqlite3, pathlib
from tools.liaison import consistency
conn = sqlite3.connect('file:data/liaison.db?mode=ro', uri=True)
print(consistency.verify_ledger_against_archive(conn, archive_root=pathlib.Path('data/liaison/archive')))
"
# => []
```

```
launchctl print gui/$UID/com.zhuopin.hr.liaison | grep -E "state|pid"
# state = running, pid = 72504（本次核证时刻仍在线）
```

```
printenv HR_LIAISON_GROUP_WEBHOOK   # 无输出，未设置
grep -rn HR_LIAISON_GROUP_WEBHOOK .env tools/liaison/launchd/*.plist*   # 零命中
```

---

## 二、8.6 逐条判定

tasks.md 原文：「单机灰度：名单先只放邵培申自己，自测归档／入队／通报三条链路，核对恒等不变式」，并拆分验收面为：**群里发文字 → 入队**；**私信发文档 → 归档**；**群通知 → 回推**。

### 2.1 群里发文字 → 入队 —— ✅ 有真实证据
- `b8b523c7`（TangLiPing 在群 `wrvDL_DAAAnkeGLkk1_bu2Ne1oQfc4BA` 发「@MAC机器人 人力保障组测试」，2026-09-17 09:45:44）已落 `liaison_message`，且对应 `liaison_task` id=3 已入队（`send_status='pending'`，等待被推送，符合"入队即挂起待送"的语义）。

### 2.2 私信发文档 → 归档 —— ❌ 未发生过，不满足
- 三条私信记录（`2a376681`／`ee4cac00`／`a932acca`）**全部是纯文字**，`attachments_json` 均为 `[]`。**从未有一条带附件的私信被处理过**，附件归档链路（区别于纯文字归档）**零证据**。
- `consistency.verify_ledger_against_archive()` 返回空列表，但这是**空对空**的平凡通过——遍历的是"带附件的记录"，而当前一条带附件的记录都不存在，该核对本身尚未被真正触发过，不能当作附件链路已验证的证据。
- 已知去向：`0917W`（号池台账「私信附件进归档与回件桥（G1）」）专门收口这一半，**不重复开新 TD**。

### 2.3 群通知 → 回推 —— ❌ 未发生过，不满足
- 四条 `liaison_task` 行 `send_status` 全部停在 `pending`，`pushed_at` 全部为 `NULL`；`liaison_group_notify` 表 0 行。**没有任何一条消息真正被回推到群**。
- 根因：`HR_LIAISON_GROUP_WEBHOOK` 在 `.env`、launchd plist 渲染产物、当前进程环境三处均未设置（`printenv` 空输出，`grep` 零命中）——`tools/liaison/notify/webhook.py` 的 `send_markdown`/`send_file` 结构上依赖这个地址，地址不存在时回推链路无从发生。
- 这与 tasks.md 8.6 前置说明原文一致（「第 6 章同样只到『给了 URL 就能发』：真实群 webhook 尚未写进 `.env`，未做过任何一次真实投递，**灰度真发同在 8.6 由 Shao Peishen 本人操作**」）——写入真实 webhook 凭据属于对外凭据配置，不在本轮无头会话可代范围内，如实登记、不代填。

### 2.4 附带观察（不影响判定，供参考）
- 8.6 原文「名单先只放邵培申自己」这一分阶步骤未见字面执行痕迹：白名单 `whitelist.yaml` 在 `e969f1b`（2026-09-09）一次提交里同时填入汤丽萍与邵培申两条 userid，未见"先只放邵培申、验证后再加汤丽萍"的中间提交。功能性验收（2.1）未受影响，仅记录此procedural偏离，不单独开 TD。

### 2.5 判定
**8.6 三条验收面里只满足 1/3**（群里文字→入队），私信文档归档与群通知回推均为 0 次真实发生。**不勾选**。

---

## 三、8.7 逐条判定

tasks.md 原文：「加入汤丽萍（配置加一行 + 重启），并告知她『照原样在群里发即可、不用改任何习惯』」。

1. **配置加一行**：`tools/liaison/config/whitelist.yaml` 含 `userid: "TangLiPing"`，非空、非占位（`git log` 显示 2026-09-09 `e969f1b` 提交）。
2. **+ 重启**：功能性证据——2026-09-17 09:45/09:46，汤丽萍的两条真实消息（群 + 私信）被服务正常接收并落库（`liaison_message` 表 `b8b523c7`／`a932acca`），说明白名单条目已在**运行中的进程**里生效（fail-closed 设计下，userid 为空的条目会被拒收；她能被正常处理，证明配置已加载，即隐含「重启」这一动作已完成，且远早于本次核证——不需要另找一条独立的"重启时刻"日志）。
3. **告知她「照原样在群里发即可、不用改任何习惯」**：`docs/跟进信/人事部-汤丽萍-跟进-2026-09-09-AI招聘值守机制启用与配合方式.md` 第 25 行原文：「🔴 这一步不需要你配合。现在往组里发的内容，系统还收不到，所以请照你现在的习惯发，不要为它改格式、也不要等它。」——措辞与判据字面一致。该信已推送（`README-跟进信清单.md` 第 46 行 `人事部#1` 状态 `✅ 已推送 2026-09-09`）。

### 判定
三项判据全部有真实证据支撑。**8.7 通过，勾选**。

---

## 四、结论

| 条目 | 判定 | 依据 |
|---|---|---|
| 8.6 | ❌ 不勾 | 私信文档归档、群通知回推均 0 次真实发生（归 `0917W` 与 Shao Peishen 本人配置 webhook 两项前置） |
| 8.7 | ✅ 勾 | 配置、重启（隐含证据）、告知信三项均有真实证据 |
