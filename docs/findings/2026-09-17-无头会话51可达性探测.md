# 无头会话 .51 可达性只读探测

**日期**：2026-09-17
**执行**：0917AV（无头 CC，run-lanes 起）
**派发**：Cowork·HR业务线-接力0917D
**红线遵守**：全程只读，未 scp／pip／重启服务／改计划任务／写服务器文件，未打印密钥或私钥路径以外内容

## 一、探测结果表

| 序号 | 检查项 | 命令 | 退出码 | 耗时 | 结果摘要 |
|---|---|---|---|---|---|
| ① | 本机网络路由 | `route -n get 192.168.100.51` | 0 | <1s | 走网关 `10.11.43.254`／`en1`，目的网段 `192.168.0.0/16`（非直连网段，经路由可达） |
| ① | TCP 22 连通性 | `nc -z -G 5 192.168.100.51 22` | 0 | <1s | `succeeded` — 22 口通 |
| ② | SSH 配置字段 | `ssh -G zp51 \| grep -E '^(hostname\|user\|identityfile\|identitiesonly) '` | 0 | <1s | `hostname=192.168.100.51`／`user=Administrator`／`identitiesonly=yes`／`identityfile=~/.ssh/id_ed25519_zp51`（文件存在，`-rw-------`，未读内容） |
| ③ | 免交互认证 | `ssh -o BatchMode=yes -o ConnectTimeout=8 zp51 hostname` | 0 | 0s | 返回 `WIN-5CPUJV008C7`，认证直接成功，无需人工交互 |
| ④ | 只读远端查询 | `ssh … zp51 "powershell … Get-ScheduledTask \| Where TaskName -like '*recruit*' …"` | 0 | 2s | `ZhuopinRecruitAgent` → `Running` |
| ⑤ | 服务健康（本机发起） | `curl -s -o /dev/null -w '%{http_code}' --max-time 8 http://192.168.100.51:8095/hr/recruit-agent/` | curl exit 0 | 1s | `HTTP 200`。该地址即 `docs/deploy-51-server.md` 第 5b 步指定地址，无需另测第二地址 |
| ⑥ | 环境差异 | `echo $SSH_AUTH_SOCK` ／ `ssh-add -l` | ssh-add exit 1 | <1s | `SSH_AUTH_SOCK` 存在（launchd socket），但 `ssh-add -l` 返回「The agent has no identities.」（exit 1＝agent 无 key，非 exit 2 无 agent） |

## 二、总判定

**可达**。本机网络路由、TCP 22、SSH 免交互认证、远端只读查询、服务 HTTP 健康检查全部一次性通过，无需修复。

**关于 ⑥ 的补充说明**：假设中「launchd 无钥匙串 agent 导致认证失败」**未成立**——虽然 `ssh-add -l` 显示 agent 里没有加载任何 key（exit 1），但 ③ 的 `BatchMode=yes` 认证仍然成功。原因是 `ssh -G zp51` 显示该 host 配置了 `IdentityFile ~/.ssh/id_ed25519_zp51` 且 `IdentitiesOnly yes`——SSH 客户端直接从磁盘读取该私钥文件完成认证，不依赖 ssh-agent 转发。该私钥文件应为无口令（或系统钥匙串已代为解锁且不经由标准 agent 协议），故本次无头会话认证未受阻。**不需要给出修复方案**（探测项要求「若认证失败给出修复候选」，本次认证未失败，此段不适用）。

## 三、收工

- **总判定**：可达
- **失败项编号**：无（①～⑥ 全部通过）
- **findings 路径**：`docs/findings/2026-09-17-无头会话51可达性探测.md`

结论：**1.1 冒烟、Q-12、Q-14 可由无头发版任务执行（仍过 G3）**。
