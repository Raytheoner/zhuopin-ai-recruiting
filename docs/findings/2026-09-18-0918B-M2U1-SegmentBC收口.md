# 2026-09-18 `0918B` M2·U1 数据模型 Segment B＋C 收口记录（无头 run-build）

**结论**：U1 全部 8 个 Task 已合 main（`66e3b97`，ff 合入，push 成功，`git cherry` 0 行），全量 pytest **3092 passed · 6 skipped · 0 failed**，tasks 2.4–2.8 与 8.1 已回勾。计划：`docs/superpowers/plans/2026-09-17-m2-resume-parse-and-rank-unit1-data-model.md`。

## 合入范围（9 个 commit，rebase 两次后 hash 以 main 为准）
`test(m2-u1) .51 demo.db pre-U1 schema fixture` → Task 4 `resume_access_log/field_review_queue/screening_flag` → Task 5 `resume_embedding/eval_*/hr_account` → Task 6 `app/audit/run_type.py`+`evidence_ref.py` → Task 7 老库升级回归 → Task 8 `app/storage/hr_account.py`+`scripts/create_hr_account.py`（＋两轮 review 修复）→ tasks.md 回勾。

## 老库基线（opener §二）
按 opener 用 ssh 只读取回 `.51` 现网 `demo.db` 的 `sqlite_master.sql`（DDL，无数据行，未下载 db、未改 .51 任何文件），落 `tests/fixtures/zp51_demo_db_schema_pre_u1.sql`。⚠️ 首跑 GBK 编码报错，加 `-X utf8` ＋ `[Console]::OutputEncoding=UTF8` 后成功。**未降级**。真实形态比计划内联 DDL 多 `checkpoints`／`writes`／`sqlite_sequence`，终审用它实测 `init_schema` 后 19 个既有对象 0 变更。

## 本次裁决（无人值守，均已按「保守方向」处置，可推翻）
1. Task 7 老库基线用夹具文件而非计划内联常量 —— opener 明令；夹具是超集。
2. Task 8 PBKDF2 迭代 200 000 → **600 000**（计划注释「OWASP 2023 下限」事实错误，OWASP 2023 对 PBKDF2-HMAC-SHA256 推荐 600 000）。代价：verify 约 100–200 ms。
3. 终审 Important #1：CLI 对首尾空白用户名误报「已创建」→ 脚本入口统一 `strip()`；保留 `upsert_account -> str` 契约不改成 tuple。
4. 终审 Important #2：口令哈希改自描述格式 `pbkdf2_sha256$<iters>$<hex>`，`verify_password` 按存储的迭代数复算；不加列、签名不变。未来提高迭代数老账号仍可校验。
5. 8.1 一并回勾：Task 3（Segment A，`906ee01`）已交付并过 task review，计划明写 8.1 与 2.3 同一交付单元。

## 终审遗留（Minor，全部延后；U2／U4 接手时处理）
- `verify_password` 对存储摘要段含非 ASCII 的损坏行会抛 `TypeError` 而非返回 False（正常写入路径不可达）；一行 `bytes.fromhex` 守卫即可
- `hr_account.username` 无非空 trim CHECK、`UNIQUE` 区分大小写（可考虑 `COLLATE NOCASE`）；应用层 `upsert_account` 已校验非空
- `resume_access_log.accessor` 存 `hr_account.id` 还是 `username` 未在 DDL 注释写明——U2 写入方与 U7 审计查询须对齐
- `field_review_queue.confidence` 无 `BETWEEN 0 AND 1` CHECK（D5 定义为 0–1）
- `parse_evidence_ref` 不校验类型／范围（负偏移、`start>end`、JSON 数组），U4 落库前须校验
- `create_hr_account.py --db-path` 打错路径会静默新建整库；建议无 `--create` 时拒绝不存在的路径
- `eval_sample`／`eval_annotation` 表注释「同上」在单表 `sqlite_master.sql` 里无所指
- `tests/test_db_m2_schema.py` 一处 docstring「结构性幂等guard」缺空格
- 计划层：`hr_account` 无 `is_active` 停用位，离职 HR 只能删行 → `resume_access_log.accessor` 悬空；接 AuthMiddleware（D12）的单元宜补

## 观察（不改动，只登记）
- `docs/session接力.md` §二 有一节标题写「`0918B` S-M3 立包 voice-structured-interview」，但号池台账 `0918B` 登记的是本条（M2U1 Segment B＋C）；疑为该节抄错编号，归属待 task-dispatcher 核对。
- SDD 台账已拷回主检出 `.superpowers/sdd/2026-09-17-m2-resume-parse-and-rank-unit1-data-model/progress.md`（git-ignored），worktree 删除后仍可查。
