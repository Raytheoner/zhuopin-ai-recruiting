# `[Mac]0919O` M2·U3 硬门槛引擎发版到 `.51`（G3 已放行 `Q-49`「发」）

【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: main ｜ worktree: ❌ 不勾（发版操作在主检出，不写业务代码）｜ 工作区: 仓库根 ｜ 派发: Cowork·HR业务线-接力0918AO ｜ 模型: Sonnet

## 零、授权

定夺队列 `Q-49`【G3 发布】`M2 U3 硬门槛引擎（标记＋依据＋申诉）`：**状态＝已答，答复＝「发」**（Shao Peishen 2026-09-19 22:0x，经 Cowork·0918AO 巡检转达）。产出依据：`0918X/Y/Z` 三条 opener 建造记录，合入 `commit 182383d`；`tasks.md` 4.1–4.6 全部回勾；已接线到 `.51` 现网既有的上传/重解析流程（触发点：上传后判定＋只读查询接口＋申诉登记/流转接口，鉴权已接入）；新增表 `appeal_event`（`CREATE TABLE IF NOT EXISTS`，非破坏性迁移）。⚠️ 与上次发版（`0918AM`，`Q-44`）不同：`Q-44` 预授权仅覆盖 U1/U2/U2.5，未覆盖本单元；本条是本单元第一次真正放行上线。

## 一、🔴 开工第一步：自己核，不许信任何转述

1. **build 真身**：`git log` 实证 `182383d`（`Merge branch 'lane-m2-u3-hard-requirement-screening'`）在 main 历史里；`openspec/changes/m2-resume-parse-and-rank/tasks.md` 的 4.1–4.6 逐条实读确认为 `[x]`。
2. **全量测试必须重跑，不得引用历史报数**：在主检出实跑全量 `pytest`。历史报告（`0918Z`）曾是 3322 passed／5 failed（标「已知无关既有缺口」）／6 skipped，但此后 main 上又合入了大量语音面试包 U2/U3/U4 代码与调度器修复，**必须重新核实那 5 个失败还是同一批、且未新增失败**——逐条比对失败用例名，任何一条不在原「已知无关」名单里 ⇒ ⛔ 停，写 `OPENER_PARTIAL: 全量 pytest 出现新增失败：<用例名>`，不得发版。
3. **合规防线在应用层实读一遍**（不是信任代码注释）：`app/storage/rejection.py::write_rejection` 对 `reason_type` 的校验逻辑仍在（只接受 `hard_rule`／`human_decision`，拒绝 `ai_score`）；`app/audit/assertions.py::COMPLIANCE_ASSERTIONS` 四条（含 `assert_no_ai_score_rejections`）仍在标准断言集合里。
4. **真实简历入库闸**：实读 `live_resume_intake_enabled` 配置，确认为关闭（本条发版不改变这个闸）。

任一条不成立 ⇒ ⛔ 立即停，写 `OPENER_PARTIAL: 预授权条件 <编号> 不成立：<实证>`，**不得发版**、⛔ 不得顺手去修（修是另一条 opener 的事）。

## 二、锁定发版 commit 并申报携带范围

1. 取当前 `main` HEAD 作为**本次发版 commit 并写死**（写这条 opener 时是 `c7a2978`，执行时可能已前进——以执行时刻的 HEAD 为准，写进执行记录），后续所有阶段都针对这个 commit，⛔ 中途不得因为别的泳道合入而换 commit。
2. 在执行记录里列出：本次发版 commit、现网原 commit（`67772ba`，见 `docs/session接力.md`「一、今天最要紧」）、以及**超出 U3 范围的携带项**（逐个变更包点名——预期至少含语音面试包 U2 出题引擎、U3 候选人邀请/同意、U4 语音链路/live 的全部代码，以及 task-dispatcher 自身脚本修复）。
3. 🔴 对每个携带项核验「对现网无行为改变」：
   - 语音面试相关的新端点／表必须是默认关闭或未接线状态（`live_interview_enabled` 默认 `False`，见 `voice-structured-interview/tasks.md` 4.9），`.51` 现有行为不因它们改变。
   - `app/middleware/auth.py` 本次改动（+1 行，`182383d` 引入）需确认只是给 U3 新端点补鉴权，不影响既有端点的鉴权逻辑。
   - 任一携带项会改变现网行为 ⇒ ⛔ 停，写 `OPENER_PARTIAL: 携带项 <名> 会改变现网行为`，交 Shao Peishen 定夺。
4. **依赖检查**：`git log 182383d..<本次HEAD> -- requirements.txt` 核实无新增 pip 依赖（写这条 opener 时核实过是空，执行时以实测为准）；`sync-to-server.sh` 白名单是目录级（`app`／`scripts`），U3 新文件全部落在 `app/` 下，理论上不需要改白名单——仍要实读一遍确认没有新文件落在白名单目录之外。若发现新依赖 ⇒ 按 `05-发布运行手册.md` 阶段 D「先装依赖再 sync」，⛔ 顺序不可颠倒。

## 三、执行

严格按 `docs/releases/2026-09-发版清单-51.md`「四、发版步骤」逐阶段执行并逐阶段留证：**快照 → （如有新依赖则装依赖）→ sync → G-e 三条 → 冒烟**。新开记录 `docs/releases/2026-09-19-发版四-M2U3硬门槛引擎执行记录.md`，体例参照 `2026-09-18-发版三-M2可见薄片执行记录.md`，每阶段写：做了什么、实际输出、判定（过／不过）。`.51` 经免交互 `ssh zp51` 可达。

**冒烟额外增加两条（本单元第一次接线真实拒绝/申诉逻辑，标准四条之外再加）**：
- ⑤ `ssh zp51` 上跑 `python -m app.audit.assertions --db data\demo.db --mirror data\audit\decisions.jsonl`，**逐条**核实四条断言里 `assert_no_ai_score_rejections` 的结果，⛔ 不能只看整体 `EXIT=0` 就过（标准 G-e 步骤④已含此断言集合，但本条要求把这一条的结果单独摘出写进执行记录，因为这是本单元第一次真正跑起来）。
- ⑥ 实读一次 `appeal_event` 表已建（`sqlite3` 查 schema 或用现有巡检脚本），且是空表（还没有真实候选人数据，符合入库闸关闭的预期）。

**⛔ 不过即回滚停链**：任一阶段判定不过 ⇒ 立刻按清单回滚到快照点，核验回滚后现网可用，然后停，写 `OPENER_PARTIAL: <阶段> 不过：<原因>＋回滚结果`。⛔ 不得"再试一次"、不得跳过阶段、不得边修边发。

## 四、红线

⛔ 入库闸发版前后各核验一次，全程保持关闭，任何情况下不得在本条里打开它。⛔ 不动 `.51` 系统组件、不装或升级系统级运行库。⛔ 不碰真实候选人简历数据、不导出任何库内数据行。⛔ 不改 `scripts/gates.py` 判据、不改台账状态放行。⛔ 不对外发送任何消息。⛔ 不得把任务交给后台子代理后结束回合——子代理一律前台等到返回，哨兵由本会话自己顶格打印。

## 五、收口

执行记录与 `docs/session接力.md` 的一行发版结果、`docs/roadmap/定夺队列.md` `Q-49` 状态行（答复列已是「发」，本条只需在旁追加一句执行结果引用），走提交通道或直接 `git add` 这两个路径提交并 push。

【并发协议】
1. 只 `git add` 本条产出的路径；⛔ 禁止 `git add -A`／`git add .`／`git commit -a`／`git stash`
2. `git status` 里出现别人的改动是正常的，不要停下、不要问、不要顺手提交
3. push 被拒 → `git pull --rebase --autostash origin main` 后重试，最多 3 次
4. 报 `.git/index.lock` 已存在 → 等 5 秒重试最多 5 次，⛔ 默认绝不删除该锁

收工六行：①②③④ 四条前置的实测结论 ｜ 本次发版 commit（现网原 → 新）与超范围携带项清单 ｜ 各阶段判定 ｜ 冒烟标准四条 + 新增两条结果 ｜ 入库闸发版后复验结论 ｜ 三处收口落档是否完成。最后一行由本会话自己顶格输出 `OPENER_DONE` 或 `OPENER_PARTIAL: <原因>`。⛔ 不提问（无人值守）。
