[Mac]0909AE-首次真实建连验证
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（起真实进程、要读真实 .env 与真实库，必须在主工作区）｜ 工作区: 仓库根 ｜ 派发: Cowork·HR业务线-接力0909Q

> 本文件是 `[Mac]0909AE` 的正文（引用式 opener）。目标＝**让值守服务第一次真的连上企微并保持在线**（8.6 的第一步）。
> 前置已全部就绪：`0909AC` 已合入（`4802371` / merge `2b16922`，TD-19 适配 `client.run()`）；三个凭据在 `.env`；
> `tools/liaison/.venv` 已建（SDK 1.0.2）；`whitelist.yaml` 两人 `userid` 均非空。
> 🔴 **这是本项目第一次让机器人真正上线。** 一旦连上，名单内（汤丽萍／邵培申）的消息会被真实处理并落库；
> **名单外的人若给机器人发消息会收到"您不在受理名单内"的礼貌回复**——这是设计行为，但属对外发送，
> 所以本 session ⛔ **不主动发任何消息**、⛔ 不发通知、⛔ 不回复任何人；只被动观察。

【一、开跑前记基线】⛔ 不改任何代码。
  cd /Users/paulshao/Projects/HumanResource && git pull --rebase --autostash origin main
  git log -1 --format=%h（应含 0909AC 的合入）
  ls -l data/liaison.db data/liaison/liveness.json 2>&1（预期两者都不存在）
  wc -c data/liaison/logs/liaison.log（预期 0）
  for k in HR_LIAISON_BOT_ID HR_LIAISON_BOT_SECRET HR_LIAISON_GROUP_WEBHOOK; do v=$(grep -E "^$k=" .env | head -1 | cut -d= -f2-); [ -n "$v" ] && echo "$k 非空 len=${#v}" || echo "$k 为空 → 停"; done
  ⛔ 只报 len，不回显取值。任一为空 → 停下报。

【二、前台起一次，观察 3 分钟】🔴 前台跑，别后台——要能随时 Ctrl-C 停下。
  PYTHONPATH=. tools/liaison/.venv/bin/python -m tools.liaison
判读（按出现顺序）：
- **退非 0 且 stderr 点名 `client.run()`** ⇒ TD-19 没修对，⛔ 不要手工改代码绕过，停下报，把 stderr 全文贴出来
- **退 3（SDK 不可用）／退 4** ⇒ 停下报，贴原始输出
- **进程不退、日志出现"已连接"一类的行、`data/liaison/liveness.json` 被写出且 `state` 为 `connected`** ⇒ ✅ **真实建连成功**，这是本条的核心判据
观察 3 分钟，其间**什么都不要做**（⛔ 不发消息、不 @ 机器人）。然后 Ctrl-C 停，记下：进程存活时长、日志全文、`liveness.json` 内容、`data/liaison.db` 是否被建出来。

【三、落证据】写 `docs/findings/2026-09-09-首次真实建连实测.md`，含：
起停时刻、命令原文、日志全文（⛔ 脱敏：不得出现 BotID/Secret/webhook key）、`liveness.json` 原文、
`data/liaison.db` 的表清单（`sqlite3 data/liaison.db ".tables"`）、以及**探针实测的表面与代码常量是否一致**。
成功则在 `docs/tech-debt.md` 给 TD-19 打销账行（`~~TD-19~~ ✅ 已还（<commit>）`）；失败则 ⛔ 不销，写清卡在哪一步。

【四、⛔ 不做的】
- ⛔ **不装 launchd**（`tools/liaison/scripts/install_launchd.py` 由 Shao Peishen 在 Terminal 自己跑，起 LaunchAgent 属安全配置）
- ⛔ **不主动发任何消息**，包括群通知、私信、测试消息
- ⛔ 不请任何人发消息配合（那一步等本条报成功后，由 Shao Peishen 决定时点）
- ⛔ 不改 `session_client.py` / `whitelist.yaml` / `.env`

【五、提交】只 add：`docs/findings/`、`docs/tech-debt.md`（若销账）。⛔ 禁止 -A / . / -a；⛔ 绝不 add `.env`、`data/`（核 `git status` 里不出现它们）。commit + push。

【六、收工】报告含：建连成功与否、日志全文（脱敏后）、`liveness.json`、库表清单、TD-19 是否销账、commit hash，并单列一行「⏸ 下一步：Shao Peishen 在 Terminal 跑 `python3 tools/liaison/scripts/install_launchd.py` 装常驻；装完才是『开机即在线』」。
