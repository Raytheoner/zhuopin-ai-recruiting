[Mac]0903K-TD9立变更包
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（只出 planning 产物与文档，不写代码不建分支）｜ 工作区: 仓库根

> 本文件是 `[Mac]0903K` 的正文（引用式 opener，规则见 `.claude/skills/kickoff/SKILL.md`「引用式 Opener」）。
> 聊天里贴的 4 行引用块指向这里。文件随任务一起提交，即留痕。

【零、依据】
Shao Peishen 2026-09-03 14:05 裁决：TD-9 走正式 openspec-propose，⛔ 不塞进泳道顺手改。
本条只出 planning 产物（proposal / specs delta / design / tasks），⛔ 一行业务代码都不写、⛔ 不进 spec-to-plan / run-build。

【一、先读】
- docs/tech-debt.md 的 TD-9 全文（成因两条叠加、"怎么还"两处一起改）——这是需求的真源
- openspec/specs/ 与 openspec/changes/ai-audit-trail-and-outbound-gate/specs/ 里 outbound-approval-gate 的
  「外发与拦截动作强制留痕」条款与 Scenario「总开关关闭时已确认的消息」
- openspec/changes/ai-audit-trail-and-outbound-gate/tasks.md 5.4（幂等键 `{content_hash}:{allowed}` 的字面规定）
- app/outbound/queue.py 的 approve()、app/graph/nodes.py 的 effect_record_outbound_audit（只读，⛔ 不改）
- app/audit/assertions.py 6.5 拦截统计（TD-9 让"一直发不出去的那批信"在这里系统性缺席）

【二、用 openspec-propose 立变更包】
变更名：outbound-retry-audit-trace
- proposal.md：问题＝放行后再被总开关拦下的那次尝试零留痕，违反"每一次外发尝试留痕"；影响＝可观测性，闸门本身完好；
  现网风险 0（TD-8：生产无调用方）。写明**为什么必须走正式变更**：要改已过审的 approve() 签名与 5.4 的字面公式
- specs delta（capability 沿用 outbound-approval-gate，⛔ 不新起一个）：ADDED/MODIFIED Scenario——
  从待审批队列 approve() 被总开关拦下时，SHALL 留痕、原因记为"外发总开关关闭"、且每次尝试各一条
- design.md 两条决策逐字对应 TD-9「怎么还」：① 幂等键改 `{content_hash}:{allowed}:{reason}`，同原因重放仍短路、
  不同原因另起一条；② approve() 加 recorder 依赖，被拦时也留痕。另写明：⛔ 不改 CANDIDATE_OUTBOUND_ENABLED 默认值与读取逻辑、
  ⛔ 不改 idempotent_effect 装饰器与 effect_log 表、⛔ 不在 effect_* 函数体内 append JSONL（跨单元约定 §4 第 2/4 条）。
  与 ai-audit-trail-and-outbound-gate 的关系：本包合入时同步订正那边 tasks.md 5.4 的字面规定（写成本包 tasks 的一条），⛔ 现在不改它
- tasks.md：按章节粒度（一章＝一份 plan＝一条 worktree），至少含：幂等键与偏离登记 / approve() 留痕接线 /
  回归（U4 gate.py 与 test_outbound_gate.py 四条锁定用例原样通过、AST 守护仍绿）/ 6.5 拦截统计能看见第二次拦截 / TD-9 销账
⛔ 不要在任何产物里写提问句；拿不准的写成「待 design 审时定」并列出选项。

【三、顺带提交 Cowork 侧 0903B 的真源改动（Shao Peishen 同日裁决）】
git status 里应有这些改动，连同新变更包一起提交：
- CLAUDE.md（引用式 opener 一行规则）
- .claude/skills/kickoff/SKILL.md（「引用式 Opener」节）
- .claude/skills/lane-dispatch/SKILL.md（③ 第 4 条 data/ 产物去向；④ 看护者改引用式）
- .claude/skills/run-build/SKILL.md（收口第 2 步：先搬回 data/ 产物再合并）
- docs/session接力.md、docs/openers/OP-0820-全量编排.md（号池登记 K/L、状态对齐）
- docs/openers/0903K-TD9立变更包.md（本文件）

【四、提交（只 add 列出的路径）】
1. index.lock 规则：等 5 秒重试最多 5 次，⛔ 默认不删；孤儿锁三判据（0 字节 + mtime>10 分钟 + pgrep -x git 无进程）同时成立才可删
2. git add openspec/changes/outbound-retry-audit-trace CLAUDE.md .claude/skills/kickoff/SKILL.md .claude/skills/lane-dispatch/SKILL.md .claude/skills/run-build/SKILL.md docs/session接力.md docs/openers/OP-0820-全量编排.md docs/openers/0903K-TD9立变更包.md
   ⛔ 禁止 git add -A / git add . / commit -a
   git status 里出现别人的改动（0903L 在并行改 docs/audit-and-outbound-ops.md、intake 的 tasks.md、docs/openers/0903L-*.md）不要停、不要问、不要顺手提交。
   若 docs/session接力.md / OP-0820-全量编排.md 里混有 0903L 的未提交改动，用 git add -p 只拿你这份对应的块；拿不准就整文件提交并在汇报里说明
3. git commit -m "spec(outbound): 立 outbound-retry-audit-trace 变更包（TD-9）＋ 真源：引用式 opener、worktree 收口先搬回 data/ 产物"
4. git push origin main；被拦 → 点放行一次；仍失败 → 登记「未推」即停，⛔ 不改权限配置

【五、汇报】变更包四个文件路径 / tasks.md 章节清单 / commit hash / 是否已推
