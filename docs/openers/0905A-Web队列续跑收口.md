[Mac]0905A-Web队列续跑收口
【设置】执行环境: CC Desktop ｜ Session: 利旧（有进度台账——worktree 内 Task 1-5 均已 complete 且 review clean，只差全分支终审+合并，不是从零开始）｜ 分支: worktree-unit8-web-list-detail-queue（已存在，不新建）｜ worktree: ❌ 不勾 Desktop 的"新建 worktree"选项（勾选会另建一个新分支名的 worktree；本任务要复用已存在的这一个，直接把工作区指过去即可）｜ 工作区: /Users/paulshao/Projects/HumanResource/.claude/worktrees/unit8-web-list-detail-queue

> 本文件是 `[Mac]0905A` 的正文（引用式 opener）。上接第十批 `0904M`——该任务跑到"全分支终审→派发 fix wave"这一步时预算耗尽（$25.01/$25）被打断，未确认修复完整、未合并进 main。本轮续跑，不重新起 worktree。

【一、开工前自检 · 任一条不过就停下来告诉我，不要绕过】

1. `pwd` 应落在上面写的工作区路径；`git branch --show-current` 应为 `worktree-unit8-web-list-detail-queue`；`git status` 应干净。
2. `cat .superpowers/sdd/2026-09-04-m1-job-profile-intake-unit8-web-list-detail-queue/progress.md`，确认 Task 1 到 Task 5 都标着 `complete` 且各自 `review clean`（spec ✅ + quality Approved）。对不上就停下报我——⛔ 不要假设台账没变就跳过对应 Task 的 review。
3. `git log --oneline -5`：最新一条应是 `ec73b3a fix(web): 拆分 switchView/showView，队列点详情不再顺带刷新岗位列表`。这是上一轮全分支终审的 fix wave 里唯一已提交的一条，`progress.md` 里没登记它（预算耗尽打断在这之后）。⚠️ 不要把这一条当成 fix wave 的全部——终审当时具体发现了几条问题、还有几条没落地，这一轮什么记录都没留下，必须重新完整走一遍全分支终审来确认，不能只凭这一条 commit 就直接判定"已经修完"。
4. 确认 `Skill` 工具能否调到 `superpowers:subagent-driven-development`。调不到就按磁盘 `~/.claude/plugins/cache/claude-plugins-official/superpowers/6.2.0/skills/subagent-driven-development/SKILL.md` 手工走"全分支 final review"这一步的协议，与上一轮（0904H/I/J/K/L/M）一致，报告里如实标注"未真正调到 skill"。
5. `./venv/bin/python -m pytest -q 2>&1 | tail -3` 记基线——上一轮中断前全量是 1074 passed, 1 skipped，此刻应不低于这个数、且 0 failed。

【二、任务】

本 worktree 实现 `openspec/changes/m1-job-profile-intake/tasks.md` 的 8.1/8.2/8.4（岗位列表、画像详情含版本历史、`needs_manual` 转人工队列，三个只读视图）。计划见 `docs/superpowers/plans/2026-09-04-m1-job-profile-intake-unit8-web-list-detail-queue.md`。Task 1-5 已全部完成并逐个 review 通过，上一轮卡在全分支终审的 fix wave 阶段。

本轮要做：

1. **重新完整跑一次全分支 final review**，对照该 plan、`openspec/changes/m1-job-profile-intake/design.md`、以及 `CLAUDE.md` 的 Global Constraints（工程铁律 1-7 条、合规红线）。因为上一轮终审具体发现了什么、fix wave 是否已全部覆盖，都没有留下任何文字记录（唯一线索是已提交的 `ec73b3a`，但如上所述不能假设它就是全部）。
2. 终审发现的问题走一轮 fix wave → 复审 ADDRESSED，标准与前 5 个 Task 一致——两阶段 review（spec + quality）、变异验证优先用真断言，不写迎合验证的同义反复测试（参照 progress.md 里 Task 3/Task 4 已经落成机器判据的两条断言写法）。
3. 全分支终审通过后：
   a. **先处理与 main 的落差**：本分支落后 main 15 个 commit（含 0904G/0904I/0904K 三条独立合并）。其中 0904G 的账目对齐把 `tasks.md` 第 250 行 `8.3 JD 查看与复制` 标成了 `[x]`，与本分支要改的 8.1/8.2/8.4 在同一段落（现在的第 248-253 行附近）——`git rebase main`（或按项目惯例的合并方式）时 `tasks.md` 大概率冲突，**必须逐行核对，不能整段 accept ours 或 theirs**。变基/合并前先跑一次 pytest 记基线，变基/合并后再跑一次确认无回归。
   b. 回勾 `tasks.md` 的 8.1/8.2/8.4 三条（`- [ ]` → `- [x]`），把 `progress.md` 里已经登记的落地偏离——6 条 minor deferred（job_queries.py 解析重复、状态字面量未走枚举、stage_label 分支覆盖缺口、O(n) 扫表两处、payload 裸值约定、注释剥离与 SVG href 两条边界）以及 Task 5 的 plan-mandated 偏离 I1（队列页点"查看详情"改成了会跳转，偏离计划原文的"静默无反应"写法，理由已在 progress.md 记录，需 Shao Peishen 事后复核）——都写进 `tasks.md` 对应条目下，不要漏。
   c. 合并进 main，参照 0904I/0904K 两条先例的合并方式。
   d. worktree 用完即清理（`git worktree remove`），分支删除——本轮是收尾，不是"批次看护、留着不清理"那种场景。

【三、并发注意（非并行任务，但仍要防误伤）】

本任务与其他 session 不并行，但 main 分支仍可能有别人推送：

1. 只 `git add` 本任务改动的路径。⛔ 禁止 `git add -A` / `git add .` / `git commit -a`。
2. `git status` 里出现别人的改动是正常的，不要停下、不要问、不要顺手提交。
3. push 被拒 → `git pull --rebase --autostash origin main` 后重试，最多 3 次。⛔ 不要在 commit 前 pull。
4. 报 `.git/index.lock` 已存在 → 等 5 秒重试最多 5 次，⛔ 默认绝不删除该锁（孤儿锁判据见 `CLAUDE.md`「多指令并行的硬规则」第 4 条，三项同时成立才可删）。

【四、完成标准】

- 全分支终审 Approved：无 Critical/Important 悬而未决，minor 可 deferred 但要登记。
- pytest 全量 ≥ 1074 passed, 1 skipped，且 0 failed。
- `tasks.md` 8.1/8.2/8.4 三条 `[x]`，落地偏离登记完整（含上述 6 条 minor + Task 5 的 I1 偏离）。
- `git rev-list --count main..<分支>` = 0，`git cherry -v main <分支>` 为空（真已合并，不是"看起来合了"）。
- worktree 与分支已清理。

【五、不要做】

- 不要跳过全分支终审直接合并——上一轮就是卡在这一步，"看起来已经修完"不能替代终审。
- 不要因为找不到上一轮终审的具体发现记录就假设"没问题"——空白不是绿灯。
- 不要在 `tasks.md` 冲突时整段用某一侧覆盖，逐条核对后再解决。
- 不要顺手清理其它无关的历史遗留 worktree（`intake-unit6-approval` / `td9-outbound-retry-audit-trace` / `unit7-jd-grounding-export`），那三个已完全合并（`rev-list=0 真未合=0`），不属于本任务范围。
- 不要修改预算或绕过预算限制。

【六、收工】

列出本次全部新增/修改/删除文件（仓库相对路径，逐个列，不要写"等若干文件"），并给出建议的 commit message（如需多条，逐条列出，参照 0904I/0904K 的收尾提交粒度：一条修复提交 + 一条回勾/落地偏离登记提交）。
