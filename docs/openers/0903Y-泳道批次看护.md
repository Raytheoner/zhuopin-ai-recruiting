[Mac]0903Y-泳道批次看护
【设置】执行环境: CC Desktop ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（你是调度者，实际干活的是脚本起的独立 session）｜ 工作区: 仓库根

> 本文件是 `[Mac]0903Y` 的正文（引用式 opener，规则见 `.claude/skills/kickoff/SKILL.md`「引用式 Opener」）。
> 第八批看护者。`Z` 已被第七批用掉，同日第二个看护者顺延用 `Y`。文件随任务一起提交，即留痕。

【一、你的角色】
你不亲自执行任何 opener。你是看护者，做四件事：前置自检、后台启动脚本、轮询盯守、跑完做真身核验并出报告。
⛔ 不要自己去改代码、不要自己跑 spec-to-plan / run-build / openspec-archive-change。

【二、先提交 Cowork 侧的编排落档】
git status --short 预期：docs/session接力.md、docs/openers/OP-0820-全量编排.md、docs/openers/0903Y-泳道批次看护.md（本文件）。
只 add 这三条路径，⛔ 禁止 git add -A / git add . / commit -a：
  git add docs/session接力.md docs/openers/OP-0820-全量编排.md docs/openers/0903Y-泳道批次看护.md
  git commit -m "docs(openers): 第八批编排（U7 收尾 → TD-9 修复 → 双归档，单泳道串行）＋ 看护者引用式"
  git push origin main   （项目级白名单 0903M 已加；仍被拦就点一次放行，⛔ 不改配置）
有别的改动 → 单列出来，不提交、不问、继续。

【三、前置自检】任一不过就停下报告，不要硬上：
1. command -v claude —— 找不到就停
2. ls docs/openers/run-lanes.sh docs/openers/OP-0820-全量编排.md
3. bash docs/openers/run-lanes.sh --dry-run
   本批（第八批）预期：**1 条泳道、4 条 opener**，行数逐条核：
     ◆ 审计
         ✓ 0903N 正文 21 行   audit U7计划(7.1与7.2 CI检查)
         ✓ 0903O 正文 27 行   audit U7开工(7.1与7.2)
         ✓ 0903P 正文 25 行   TD9修复计划(retry-audit-trace)
         ✓ 0903Q 正文 33 行   TD9修复开工与双归档
   四条严格串行：O 等 N 的 plan；Q 等 O 的 53/53 与 P 的 plan。前面一条 FAIL 后面就跑不到——这是设计好的，不是故障。
   ⚠️ 数字对不上（不是 1 泳道 4 条 / 行数不符）→ 停下报告，⛔ 不要自己改编排文件凑数。
   任何一条显示 ✗ 或脚本 exit 13 → 停下报告，⛔ 不 --only 绕过；exit 13 的第二种触发＝块内含 set_session_title，
   无头块正确状态就是不带那一行，⛔ 不要去"补回来"。「⚠ 块外缺无头豁免注明」只 WARN，照常继续。
4. git status --short —— 记下工作区基线（【二】提交后应干净）。跑完对照，多出来预期外的改动单列查是谁的
5. git status -sb 记下开跑前 ahead（应为 0）。本批四条都会 push（白名单已生效），跑完 ahead 应仍为 0；
   若 > 0，说明某条 push 没成，在【六】8 由你补推
6. 🔴 测出本批 pytest 基线：./venv/bin/python -m pytest -q 2>&1 | tail -3
   ⚠️ python 不在 PATH，只有 ./venv/bin/python。⛔ 不从任何文件抄数（09-03 中午实测 786/1 skipped，那是别人的观测）。
   此刻已有失败项 → 停下报告，不要发车
7. ls docs/superpowers/plans/ | grep -c "unitU7\|retry-audit" 应为 0（N/P 要新建；已存在说明有人越位，登记）
8. ls openspec/changes/ 应含 ai-audit-trail-and-outbound-gate 与 outbound-retry-audit-trace（Q 跑完两者应都进 archive/）

【四、后台启动，不要前台等】
  cd /Users/paulshao/Projects/HumanResource
  nohup bash docs/openers/run-lanes.sh --full-auto --yes > /tmp/run-lanes-boot.log 2>&1 &
  echo $!
记下 PID。从 /tmp/run-lanes-boot.log 头几行读出**日志目录**（形如 .claude/handoff/lanes-<时间戳>）。
⚠️ 本批不加 --chain。单泳道 4 条串行，估 2–3 小时。

【五、轮询盯守】每 3-5 分钟查一次，直到那 1 条泳道收敛（4 条都出结果或泳道停下）：
  LOGDIR=<日志目录>
  cat "$LOGDIR/results.tsv" 2>/dev/null
  ls -la "$LOGDIR"/*.log
  ps -p <PID> >/dev/null && echo RUNNING || echo EXITED
每轮向我播报一行。⛔ 不要每轮贴日志全文。

预案（不要问我，我可能不在）：
- 某条 **PARTIAL** → 正常，去日志找「⏸ 留步」记下原因，继续盯。已知合法留步：O 的「不归档，交 Q」；Q 的「ai-audit 不足 53/53，归档跳过等 U7」
- 某条 **FAIL / NO-SENTINEL** → 泳道自动停，后面的条目不会跑。读末尾 50 行判"真失败"还是"哨兵没输出但活干完了"，记下继续等脚本退出
- **NO-BODY** → 立即停止盯守，`cat "$LOGDIR/manifest.tsv" | od -c | head` 贴给我。⛔ 不改脚本或编排文件
- 🔴 N/O/P/Q 报「调不到 superpowers」→ **已知问题**（无头取不到插件，今天已第 5、6 次）。opener 里有预案：按磁盘 SKILL.md 手工走。
  记下各条是停了还是手工走完了；手工走完的，报告里单列有没有两阶段 review 和 fix loop。⛔ 不装插件、不改配置
- Q 报「归档被拒/tasks.md 有未勾项」→ 那是它按预案在保护归档时限规则，如实记
- **超过 3.5 小时仍未收敛** → 不要 kill，记录当前状态出报告，让我来定

【六、跑完的真身核验 —— 逐条跑，不许跳】
  cd /Users/paulshao/Projects/HumanResource && git pull --rebase origin main
1. git log --oneline -25 —— 对照 results.tsv，每条 OK 是否真有 commit（N：plan；O：合并+回勾；P：plan；Q：合并+回勾+两次归档）
2. git status --short —— 与【三】4 基线比
3. git worktree list —— O、Q 各建一个，收口后可能已自删（各自 finishing 流程的正常行为，⛔ 不删也不清理）
4. 逐分支查真未合：
   for b in $(git branch --format='%(refname:short)' | grep -v '^main$'); do
     echo "$b  rev-list=$(git rev-list --count main..$b)  真未合=$(git cherry -v main $b | grep -c '^+')"
   done
   真未合 = 0 才算合进去；rev-list 非 0 而真未合 0 是假阳性，登记不删
5. ls docs/superpowers/plans/ —— 应恰好多出 U7 与 retry-audit-trace 两个文件，各 grep -c '^### Task ' ≥ 2 / ≥ 4
6. 归档核验（⛔ 别一律按满分算）：
   ls openspec/changes/ —— Q 报 OK 时，ai-audit-trail-and-outbound-gate 与 outbound-retry-audit-trace **都不该还在这里**，应在 openspec/changes/archive/ 下各有一个带日期前缀的目录
   grep -c '^- \[x\]' openspec/changes/archive/*ai-audit-trail-and-outbound-gate*/tasks.md 应为 53
   grep -c '^- \[x\]' openspec/changes/archive/*outbound-retry-audit-trace*/tasks.md 应为 15
   grep -n "reason}" openspec/changes/archive/*ai-audit-trail-and-outbound-gate*/tasks.md 应命中 5.4（公式已订正）
   grep -n "outbound-retry-audit-trace" docs/tech-debt.md 应在 TD-9 末尾命中销账行，且 TD-9 成因原文仍在
   若 Q 报 PARTIAL 且留步是「等 U7」→ ai-audit 包应仍在 changes/ 下，如实记，不是失败
7. pytest —— 与【三】6 基线比：**跑完 ≥ 开跑前，且 0 失败**。O、Q 都会新增测试，只看下限
8. git status -sb —— ahead 应为 0；> 0 则 git push origin main 补推（被拦点一次放行）
9. git worktree prune —— 照跑报一句即可；本批不做任何清理

【七、出看护报告】写成 .claude/handoff/lanes-<时间戳>-看护报告.md（该目录在 .gitignore 里，报告只在本机，不提交），含：
  - 一张表：编号 / 脚本判定 / 真身核验结论 / 分歧说明
  - PARTIAL 留步原因逐条，标明"等什么"
  - 红灯清单：Task 数不足的 plan、真未合 ≠ 0 的分支、pytest 失败、归档落点不对、ahead ≠ 0
  - superpowers 是否真调到（N/O/P/Q 各一行）
  - 下一步建议（列选项，不替我定）

【八、收工】把报告路径和那张表贴给我。
