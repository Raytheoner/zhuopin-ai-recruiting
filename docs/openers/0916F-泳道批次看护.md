[Mac]0916F-泳道批次看护
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（只读盯守 + 收口时的合并校验，不写业务代码）｜ 工作区: 仓库根 ｜ 派发: run-lanes.sh 看护者（[Mac]0916E 泳道发车会话）

开工第一件事：调 mcp__ccd_session_mgmt__set_session_title（session_id 传字面量 "self"），标题：[Mac]0916F-泳道批次看护

【零、本批背景】本批只有一条泳道、一个条目：`[Mac]0916E`（TD-29 收尾：liaison 测试脚手架收编 + config.py 文案）。
已由 launchd 请求文件路发车：`pid=26235`，日志目录 `/Users/paulshao/Projects/HumanResource/.claude/handoff/lanes-20260916-113021`，
`args=--full-auto --yes --only 0916E`，`started_at=2026-09-16T11:30:21`。

【一、前置自检】
1. `kill -0 26235` 若失败（进程已不在），直接跳到【三、真身核验】——不代表失败，可能已跑完退出
2. 确认日志目录存在：`ls /Users/paulshao/Projects/HumanResource/.claude/handoff/lanes-20260916-113021`

【二、盯守】循环调用（Bash 工具 timeout 设 600000）：
```
bash docs/openers/wait-lanes.sh --pid 26235 --logdir /Users/paulshao/Projects/HumanResource/.claude/handoff/lanes-20260916-113021
```
按返回处置，⛔ 两次调用之间不做任何额外查询（不 cat results.tsv、不 ls *.log、不 ps、不 git status）：
- HEARTBEAT（退出码 3）→ 什么都不说，直接再调一次
- CHANGE → 只对新增的那一条判：OK/PARTIAL 记一行继续调；FAIL/NO-SENTINEL/BUDGET-HIT → `tail -50 <该条日志>` 判真失败还是漏哨兵（⛔ 不读全文）
- ROUND → 本批未加 `--chain`，理论上不会出现；若出现按新目录继续调
- EXITED → 进入【三、真身核验】
- WARN 529 → 记进报告，继续调；WARN index.lock → 只记进报告，⛔ 不删

【三、真身核验】（EXITED 后，或【一】发现进程已不在时）
1. `cat /Users/paulshao/Projects/HumanResource/.claude/handoff/lanes-20260916-113021/summary.txt`（若存在）
2. `cat /Users/paulshao/Projects/HumanResource/.claude/handoff/lanes-20260916-113021/results.tsv`
3. 只有「results.tsv 里 0916E 状态 = OK 或 PARTIAL 且已 mark_done」才算真交付，否则按【四】处置
4. 真合校验：`git rev-list --count main..<0916E 分支>`（分支名从 results.tsv 或 `git branch -a | grep -i td29` 取）。
   非 0 时再跑 `git cherry -v main <分支>`：全 `-` 才是真合（rebase 假阳性），有 `+` 才是真没合
5. `git log --oneline -5 -- docs/tech-debt.md | grep -c 'TD-29'`——确认 TD-29 销账行已进 main
6. `./venv/bin/python -m pytest -q 2>&1 | tail -5`——确认全量不回归（0 失败）

【四、异常处置（预案覆盖，⛔ 不提问）】
- FAIL / NO-SENTINEL：`tail -50` 判真失败还是哨兵漏打（真身在 main 上已存在即为漏打，不重跑）；真失败 → 登记 `docs/session接力.md`「三、待决策/悬置」一行，附日志摘要与分支名，⛔ 不自行重跑
- BUDGET-HIT：先查真未合 / `git cherry` / tasks.md `- [x]` 三条，多半是虚惊（参考 `docs/findings/` 里 BUDGET-HIT 系列记录）
- superpowers 不可达（`Unknown skill`）：登记「⏸ 留步：superpowers 不可达」，不手工代走

【五、收口】
1. `results.tsv` 判为 OK/PARTIAL 且第三条真合校验通过 → 在 `docs/session接力.md` 顶部追加一段「🆕 `[Mac]0916E` 已跑完（TD-29 已销账）」，含 commit hash、pytest 结果
2. 若 `docs/tech-debt.md` TD-29 尚未回改成 `✅ 已还`（0916E 正文【五】1 要求它自己改），看护者代改一句并注明「代 0916E 补的销账行」
3. `git add` 只列出本条改到的路径（`docs/session接力.md`，若代改 `docs/tech-debt.md` 一并加），commit，push（按并发协议：push 被拒 `git pull --rebase --autostash origin main` 重试最多 3 次）

【六、报告】给出：
1. 本批唯一条目 0916E 的最终状态（OK/PARTIAL/FAIL）与耗时
2. A/B 记账行（`results.tsv` 第 6 列模型）：条数／sonnet·opus 条数／均价，回填 `docs/token治理/P2-AB基线.md`「切换后记录」表
3. 下一步一条：liaison-reply-bridge-and-patrol 包仍卡在 §0 门槛（0.1 TD-42 真实验证、0.3 真实入站落库），两者都需 Shao Peishen 本人在主工作区跑 `[Mac]0910A`（`docs/openers/0910A-TD42真实验证与AT1b真实入站.md`，worktree ❌，需他亲自发两条企微消息）——这是当前唯一能重新打开后续所有泳道的动作，不进 run-lanes.sh
