[Mac]0903L-51二次发版与U6巡检与8.4取证
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（在仓库根跑 sync-to-server.sh，改文档提交 main）｜ 工作区: 仓库根

> 本文件是 `[Mac]0903L` 的正文（引用式 opener，规则见 `.claude/skills/kickoff/SKILL.md`「引用式 Opener」）。
> 聊天里贴的 4 行引用块指向这里。文件随任务一起提交，即留痕。

【零、授权留痕】
本 opener 含 CLAUDE.md 不可代项：.51 生产发版。依据：Shao Peishen 2026-09-03 14:05 在 Cowork 会话 HR业务线-接力0903B
对「8.4 + U6 巡检都要上 .51」回复「都要上」。留痕写进 docs/audit-and-outbound-ops.md §五（巡检实跑记录）。
本 session 是交互式、他在场——8.4 那一步需要他本人在页面操作，到那步**可以等他回帖**（这不是无人值守 prompt）。

【一、发版前置核对（任一不过即停）】
1. git status -sb：main 应与 origin 同步或仅 ahead；记下 git log -1 --format=%h main（发版的就是它，应 ≥ b86db65）
2. ssh zp51 "echo ok" 回 ok
3. git diff --stat d104249..main -- requirements.txt 必须为空（pyproject.toml 有 6 行改动是 pytest markers，不影响运行时，⛔ 不用重跑 deploy-server.ps1）
4. ssh zp51 "powershell -NoProfile -Command \"Test-Path 'C:\apps\zhuopin-recruit-agent\.venv\Scripts\python.exe'\"" 必须 True

【二、快照 → 发版 → 冒烟（与 0903D 同一套，逐字）】
1. 快照（在 .51 上）：
   ssh zp51 "powershell -NoProfile -Command \"\$ts=Get-Date -Format yyyyMMdd-HHmm; \$b='C:\apps\backups\'+\$ts; New-Item -ItemType Directory -Force \$b | Out-Null; Copy-Item 'C:\apps\zhuopin-recruit-agent\app' \$b\app -Recurse; Copy-Item 'C:\apps\zhuopin-recruit-agent\data' \$b\data -Recurse; Write-Output \$b\""
   记下目录。引号嵌套报错 → 改 -EncodedCommand（0903A/D 已用）
2. ./sync-to-server.sh，末尾必须 HTTP 200 + 「发版完成」。⛔ 不加参数、不改白名单
3. 冒烟：curl 首页 200；抓 src=/href= 相对资源逐个 curl 无 404；app.log 尾 60 行无 Traceback/OperationalError；
   ssh zp51 "powershell -NoProfile -Command \"Test-Path 'C:\apps\zhuopin-recruit-agent\app\audit\assertions.py'\"" 必须 True（这是本次发版的目标文件）
4. 任一不过 → 回滚：Remove-Item app -Recurse -Force → Copy-Item <快照>\app 回去 → schtasks /end + /run → curl 200；
   写 docs/findings/2026-09-03-51二次发版回滚.md，停。⛔ data\ 不回滚

【三、U6 巡检 CLI 对真实库跑（在 .51 上）】
ssh zp51 "powershell -NoProfile -Command \"cd C:\apps\zhuopin-recruit-agent; .venv\Scripts\python.exe -m app.audit.assertions --db data\demo.db --mirror data\audit\decisions.jsonl; Write-Output ('EXIT=' + \$LASTEXITCODE)\""
退出码契约：0 全绿 · 1 有违例 · 2 路径不存在。
预案：decisions.jsonl 在 .51 上**大概率不存在**（0903D 链校验时 exists: False，现网至今无外发调用方）→ 预期 EXIT=2，
这是"尚无审计记录"不是失败，原样记录；⛔ 不要为了凑 0 去创建空文件。EXIT=1 → 把违例原文贴进记录，⛔ 不修数据、不改断言，停下报我。
输出原文（含 EXIT）写进 docs/audit-and-outbound-ops.md §五新增「U6 巡检 CLI 首次实跑（2026-09-03）」小节。

【四、8.4 取证——这一步要 Shao Peishen 本人在页面操作】
发版与冒烟都过后，把下面这段原样贴给他，然后等他回「跑完了」：
  ┌ 请在浏览器打开 http://192.168.100.51:8095/hr/recruit-agent/ ，新建一个 job，
  │ 按顺序走完：给一个模糊回复 → 系统给出候选档位 → 点选一个档位 → 走到"带缺口确认"。
  │ 走完回我「跑完了」并告诉我 job 名。
  └ 期间请别用旧 job。
他回复后从 .51 取证（只读）：
ssh zp51 "powershell -NoProfile -Command \"cd C:\apps\zhuopin-recruit-agent; .venv\Scripts\python.exe -c \\\"import sqlite3,json; c=sqlite3.connect('data/demo.db'); r=c.execute('select j.id,j.title,p.version,p.asked_questions,p.derived_unspecified_fields,p.written_fields,p.llm_response_model,p.llm_latency_ms from job j join job_profile p on p.job_id=j.id order by p.created_at desc limit 8').fetchall(); print(json.dumps(r,ensure_ascii=False,indent=1))\\\"\""
判据：最新 job 的多轮 job_profile 里 asked_questions 非空（追问发生）、某轮 written_fields 含点选产生的字段、末轮 derived_unspecified_fields 非空（带缺口确认）。
三条都有 → 勾 8.4，依据写「Shao Peishen 2026-09-03 页面实跑，job=<名>，取证行摘要」；缺一条 → 不勾，把取证原文贴回，等我。
⛔ 不替他跑、不用 curl 模拟点选充数——8.4 要的就是人眼走一遍。

【五、落档与提交（只 add 列出的路径）】
1. docs/audit-and-outbound-ops.md §五：发版记录（commit、快照目录、冒烟 4 项）+ 巡检小节；第 1 项备份任务仍 ⏸
2. openspec/changes/m1-intake-quality-fixes/tasks.md：8.4 按【四】结果处理，勾了则顶部进度改 68/69
3. docs/session接力.md 【一】.51 代码行改「已发版 <commit>（含 U6）」；docs/openers/OP-0820-全量编排.md 号池 0903L 行补结果
4. git add docs/audit-and-outbound-ops.md openspec/changes/m1-intake-quality-fixes/tasks.md docs/session接力.md docs/openers/OP-0820-全量编排.md docs/openers/0903L-51二次发版与U6巡检与8.4取证.md
   ⛔ 禁止 git add -A / git add . / commit -a；0903K 在并行改 CLAUDE.md、.claude/skills/、openspec/changes/outbound-retry-audit-trace/、docs/openers/0903K-*.md——
   git status 里有它的改动不要停、不要问、不要顺手提交。
   接力与编排文件若混有 0903K 的未提交改动，用 git add -p 只拿你这份对应的块；拿不准就只提交前两个文件 + 本文件，并在汇报里说明
5. git commit -m "ops(.51): 二次发版 <commit>（含 U6）＋ 巡检 CLI 首跑 ＋ 8.4 取证"
6. git push origin main；被拦 → 点放行一次；仍失败 → 登记「未推」即停
7. index.lock：等 5 秒重试最多 5 次，⛔ 默认不删

【六、汇报】发版 commit / 快照目录 / 冒烟 4 项 / 巡检 EXIT 与原文 / 8.4 取证三条判据各自结果与是否回勾 / commit hash / 是否已推
