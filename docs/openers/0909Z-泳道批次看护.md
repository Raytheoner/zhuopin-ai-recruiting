[Mac]0909Z-泳道批次看护
【设置】执行环境: CC Desktop ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（你是调度者，实际干活的是脚本起的独立 session）｜ 工作区: 仓库根 ｜ 派发: Cowork·HR业务线-接力0903B

> 本文件是 `[Mac]0909Z` 的正文（引用式 opener）。第十三批看护者（09-08 编排，编号按发车日 0909）。文件随任务一起提交，即留痕。

【一、你的角色】
你不亲自执行任何 opener。做四件事：前置自检、**把启动命令交给 Shao Peishen 点 Run**、轮询盯守、跑完真身核验并出报告。
⛔ 不改代码、不跑 spec-to-plan / run-build / openspec-archive-change。本批不发版。

【二、先提交 Cowork 侧的编排落档】
git status --short 预期：docs/session接力.md、docs/openers/OP-0820-全量编排.md、docs/openers/0909Z-泳道批次看护.md（0908U 若已跑完会把前两个带走一部分，⛔ 不要惊讶）。
只 add 这三条路径，⛔ 禁止 git add -A / git add . / commit -a：
  git add docs/session接力.md docs/openers/OP-0820-全量编排.md docs/openers/0909Z-泳道批次看护.md
  git commit -m "docs(openers): 第十三批编排（值守第 4→5 章 ∥ 第 7 章）"
  git push origin main
有别的改动 → 单列出来，不提交、不问、继续。

【三、前置自检】任一不过就停下报告，不要硬上：
0. 🔴 `grep -c "RUN_LANES_COPY" docs/openers/run-lanes.sh` 必须 ≥ 1（`0908U` 的自拷贝执行已落地）。为 0 → 停下报告「0908U 未跑」，⛔ 不发车——第十二批的自我损坏事故就是这条护栏要挡的
1. command -v claude；pgrep -f run-lanes.sh 应无进程（第十二批已收）
2. bash docs/openers/run-lanes.sh --dry-run
   本批（第十三批）预期：**2 条泳道、6 条 opener**，末尾一行「条目总数自检：Σ=N=M=6」：
     ◆ 归档队列
         ✓ 0909A 正文 20 行   值守第4章消息归档计划
         ✓ 0909B 正文 23 行   值守第4章消息归档开工
         ✓ 0909C 正文 19 行   值守第5章任务队列计划
         ✓ 0909D 正文 23 行   值守第5章任务队列开工
     ◆ 连接
         ✓ 0909E 正文 20 行   值守第7章连接生命周期计划
         ✓ 0909F 正文 23 行   值守第7章连接生命周期开工
   归档队列四条串行（第 4、5 章同写 storage/effects.py），连接泳道并行。数字对不上 / 任一 ✗ / exit 13 / exit 15 → 停下报告，⛔ 不改编排文件、不 --only 绕
3. git status --short 记基线；git status -sb 记 ahead（应 0）
4. 🔴 pytest 基线：./venv/bin/python -m pytest -q 2>&1 | tail -3（python 不在 PATH；⛔ 不抄——第十二批收口 1378 那是别人的观测）。有失败项 → 停下报告
5. ls docs/superpowers/plans/ | grep -c "unit4-message-archive\|unit5-task-queue\|unit7-connection" 应为 0
6. grep -c '^- \[x\]' openspec/changes/hr-wecom-aibot-liaison/tasks.md 应为 18
7. git worktree list：`/private/tmp/wt-0908M` 若仍在（prunable，分支真未合 0）→ 记一句，⛔ 不清理
8. 看一眼 /usage：两泳道并行、归档队列连跑四条，余量不够就停下报告

【四、启动——交给 Shao Peishen 点 Run，⛔ 你不要自己调 Bash 起脚本】
先 `rm -f /tmp/run-lanes-boot.log`，然后把下面这段**原样**作为 bash 代码块发给他，请他点 Run：

```bash
cd /Users/paulshao/Projects/HumanResource && nohup bash docs/openers/run-lanes.sh --full-auto --yes > /tmp/run-lanes-boot.log 2>&1 & echo "PID=$!"
```

等他回 PID。从 /tmp/run-lanes-boot.log 头几行读日志目录（只认半角冒号那行；自拷贝执行后第一行会打印副本路径，那不是日志目录）。本批不加 --chain，归档队列估 3–4 小时。

【五、轮询盯守】每 5 分钟：
  LOGDIR=<日志目录>; cat "$LOGDIR/results.tsv" 2>/dev/null; ls -la "$LOGDIR"/*.log; ps -p <PID> >/dev/null && echo RUNNING || echo EXITED
每轮播报一行。预案（不要问我）：
- PARTIAL → 正常，找「⏸ 留步」记原因继续盯。已知合法留步：F 的「真实建连留步：等 BotID/Secret」
- FAIL / NO-SENTINEL → 该泳道停、别的不受影响。读末尾 50 行；`API Error: 529` → 查 status.claude.com，解除后把
  `cd /Users/paulshao/Projects/HumanResource && nohup bash docs/openers/run-lanes.sh --full-auto --yes --only <剩余编号逗号分隔> > /tmp/run-lanes-boot.log 2>&1 & echo "PID=$!"`
  作为 bash 块发给 Shao Peishen 点 Run，新日志目录另记
- `Budget limit reached ($25)` 中断在终审/合并前 → ⛔ 不重开；报告里写 worktree 名与未合 commit 数，推荐「利旧 worktree 续跑收口」
- 🔴 results.tsv 里同一编号出现两行 → 第十二批那种"被派两遍"复发，立即停止盯守，贴 `cat /tmp/run-lanes-boot.log | head -5`（看是不是副本在跑）给我
- NO-BODY → 停止盯守，`cat "$LOGDIR/manifest.tsv" | od -c | head` 贴给我
- 「调不到 superpowers」→ 已知，按预案手工走；记下各条是停了还是走完了
- 超过 4.5 小时仍未收敛 → 不 kill，记录现状出报告

【六、跑完的真身核验 —— 逐条跑，不许跳】
  cd /Users/paulshao/Projects/HumanResource && git pull --rebase origin main
1. git log --oneline -50 对照 results.tsv（A/C/E：各 1 个 plan；B/D/F：各 合并 + 回勾）
2. git status --short 与基线比；git status -sb ahead 应 0（> 0 补推）
3. git worktree list；逐分支真未合：
   for b in $(git branch --format='%(refname:short)' | grep -v '^main$'); do echo "$b rev-list=$(git rev-list --count main..$b) 真未合=$(git cherry -v main $b | grep -c '^+')"; done
4. ls docs/superpowers/plans/ 应恰好多出 unit4 / unit5 / unit7 三个，`### Task` 数各 ≥ 4
5. checkbox：grep -c '^- \[x\]' openspec/changes/hr-wecom-aibot-liaison/tasks.md —— 满额 47（18 + 10 + 10 + 9）；按各条 OK/PARTIAL 分别判；核 `<<<<<<<` 计数为 0
6. 🔴 落点守卫：git diff --stat <开跑前 main>..main -- app/ scripts/ requirements.txt 必须为空（本批三章全在 tools/）
7. 🔴 密钥守卫：git grep -nE "HR_LIAISON_BOT_SECRET\s*=\s*\S{8,}" -- ':!*.md' 应为空
8. 🔴 边界守卫：storage/effects.py 只被归档队列泳道改过；session*/alerts*/__main__.py 只被连接泳道改过（git log --format=%s -- <路径> 看 commit 前缀）
9. pytest 与【三】4 基线比：≥ 开跑前且 0 失败；单测离线
10. git status 里不得出现 data/ 下任何文件
11. ⛔ 本批不做 worktree 清理

【七、出看护报告】写成 .claude/handoff/lanes-<时间戳>-第十三批看护报告.md（gitignore 内，不提交），含：
  - 编号 / 脚本判定 / 真身核验结论 / 分歧
  - PARTIAL 留步逐条；红灯清单（Task 数不足、真未合 ≠ 0、pytest 失败、tasks.md 冲突标记、落点/密钥/边界守卫、data/ 进 git、ahead ≠ 0、同编号双行）
  - superpowers 是否真调到（六条各一行）
  - 下一步：**一条推荐动作 + 理由**（同结果只写一种；只有结果不同且属 Shao Peishen 必须拍的才列选项并标推荐）

【八、收工】把报告路径和那张表贴给我。
