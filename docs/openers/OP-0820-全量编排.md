# OP-0820 · 全量 Opener 编排

> 2026-08-20。**唯一权威 opener 清单**，`OP-0820-A_单元BC开工.md` 已作废。
>
> **编排原则**：不依赖人工控制执行节奏。有依赖就彻底串行，能并发的全部并行。
> 并发安全靠《并发协议》做到机器自愈，**不靠"你记得别同时开"**。
>
> **格式**：每个代码块头两行是标题行 + 【设置】行，整块复制即可，不要拆。

---

## 🔢 号池台账 → 已迁至 `docs/openers/号池台账.md`

> 〔归档工具〕 2026-09-16：出号前查、给出后登记，一律改在该文件；历史月份的行在 `docs/openers/归档/号池台账-归档.md`。

## 二、并发协议（**每条 opener 内已逐字内嵌，此处备查**）

1. **只 `git add` 本条 opener 明确列出的路径。** ⛔ 禁止 `git add -A` / `git add .` / `git commit -a`。这是整套并行编排成立的唯一前提。
2. **`git status` 里出现别人的改动是正常的**——那是并行的另一条 opener 在干活。不要停下、不要问、不要顺手提交。
3. ⛔ 不要在 commit 前 pull（并发下会被别人的未提交改动挡住）；push 被拒才 `git pull --rebase --autostash origin main` 重试。
4. push 被拒（non-fast-forward）→ 再 `git pull --rebase origin main` 后重试，**最多 3 次**，仍失败停下贴错误。
5. 报 `.git/index.lock: File exists` → **等 5 秒重试，最多 5 次**。⛔ **绝不删除该锁文件**——那是另一个 session 正在用，删了会毁掉它的提交。

---

## 待执行区

> 〔归档工具〕 2026-09-16：`lane-dispatch` 新写的无头 opener 放在本节之后（`> 泳道：<名>` ＋ 代码块）。
> 〔归档工具〕 跑完被 `mark_done` 摘标注的批次，下次 `python3 scripts/archive_docs.py --apply` 会整段搬去归档。

> ✅ 已完成 2026-09-16（OK）· 原泳道 liaison测试脚手架收尾（脚本自动摘，不再进 run-lanes）

```
[Mac]0916E-TD29收尾liaison测试脚手架收编与config文案
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ✅ 勾（写代码）｜ 工作区: worktree（`.claude/worktrees/td29-liaison-test-scaffolding-0916E`）｜ 派发: run-lanes.sh

【零、背景与授权】TD-29 剩余两条，Shao Peishen 2026-09-10 已答 `4a`：**下一批泳道单独派一条**，独占
`tools/liaison/tests/` 与 `tools/liaison/config.py`，⛔ 不挂 8.6 灰度前置、⛔ 不留着不做。全文见
`docs/tech-debt.md` TD-29 条目「仍欠两条」。这是**纯重构 + 文案修正，不改变外部可观察行为**，按
CLAUDE.md「轻量通道」走：⛔ 不用 `spec-to-plan`/`run-build`，直接 TDD 单条改完提交
（`superpowers:test-driven-development`；调不到 ⇒ 登记「⏸ 留步：superpowers 不可达」并停，⛔ 不手工按磁盘 SKILL.md 走）。

【一、前置自检】
1. `pwd` 必须含 `.claude/worktrees/`，否则停下报
2. `git log --oneline -5 -- tools/liaison/tests/test_notify_store.py | grep -c 'assert_group_notify_identity'` 之类的旧顾虑已随
   `5ca1f50` 处置，本条不需要再验；只需确认 `grep -n 'assert_group_notify_identity' tools/liaison/tests/test_notify_store.py` 命中 ≥2 处（存在即可，⛔ 不是本条任务）

【二、任务 A · conftest 收编（三处复制粘贴）】
现状（已核实的确切行号，实现前重新 grep 一遍确认未漂移）：
- `FakeClock`：`tools/liaison/tests/test_notify_ratelimit.py:14` 与 `tools/liaison/tests/test_notify_webhook.py:63` 两份定义，逐字节比对是否相同
- `RecordingSink`：`tools/liaison/tests/test_notify_store.py:29` 与 `tools/liaison/tests/test_notify_webhook.py:76` 两份
- `FAKE_WEBHOOK`：`tools/liaison/tests/test_notify_transport.py:25` 与 `tools/liaison/tests/test_notify_webhook.py:15` 两份（值＝`https://example.invalid/cgi-bin/webhook/send?key=fake-key-for-tests`，⛔ 改动中不得把这个假值换成真实域名）

步骤：
1. 若两份定义逐字节不同，先登记差异、以更完整/更新的那份为准，⛔ 不擅自改逻辑
2. 三者收进 `tools/liaison/tests/conftest.py`，以 pytest fixture 或模块级常量形式导出（`FakeClock`/`RecordingSink` 用 `@pytest.fixture` 更符合 pytest 惯例，但若两处原调用点是直接实例化类而非注入，保持调用点最小改动优先——不确定就选影响面小的那种）
3. 四个原文件改为从 `conftest.py`（同目录自动可见，无需显式 import fixture；若是模块级常量/类则需要 `from .conftest import ...` 或按 pytest 惯例处理）里取用，删除本地重复定义
4. 跑 `tools/liaison/tests/` 全量，四份原文件涉及的用例必须逐条仍过，且总数不减少（收编不是删测试）

【三、任务 B · config.py:75 异常文案错位】
现状：`tools/liaison/config.py:70-83` 的 `load_group_webhook` 复用 `tools/liaison/errors.py` 的
`MissingCredentialsError`，其消息固定「HR 值守通道**拒绝启动**：…」（`errors.py:19`）。但
`load_group_webhook` 是**发送期**校验（docstring 已明写"⛔ 刻意不加进 REQUIRED_CREDENTIAL_ENV_NAMES"，
理由是收消息与发群通知独立、不能连坐），服务本身仍在正常运行，只是这一次群通知发不出去。
运维看到"拒绝启动"字样会误判成服务整体宕了。

步骤：
1. 在 `tools/liaison/errors.py` 新增一个独立异常（例如 `GroupWebhookMissingError(RuntimeError)`），
   构造签名与 `MissingCredentialsError` 一致（`missing_names: Iterable[str]`，消息同样⛔ 只出现变量名不出现取值），
   但文案改为准确描述"群通知发送地址缺失，本次跳过发送"一类措辞，**不含"拒绝启动"字样**
2. `config.py:83` 的 `raise MissingCredentialsError([GROUP_WEBHOOK_ENV])` 改成 `raise GroupWebhookMissingError([GROUP_WEBHOOK_ENV])`
3. 调用方核对并同步：`tools/liaison/followup.py:216-217`（`except MissingCredentialsError` 那块，
   若捕获的正是 `load_group_webhook` 抛出的这个异常则要改成捕获新类型；若那里捕获的是别的调用点的
   `MissingCredentialsError` 则不动，只改群通知这条路径）、`tools/liaison/notify/webhook.py`（若有捕获此异常的逻辑同样核）
4. `tools/liaison/tests/test_notify_transport.py:333-366` 四处 `config.load_group_webhook(...)` 相关用例，
   涉及"抛异常"的断言改成断言新异常类型 `GroupWebhookMissingError`（⛔ 不要只改字符串匹配，要改类型）
5. 先红后绿：新增/改动的用例在改代码前跑必须红，改完再绿

【四、全量回归】`./venv/bin/python -m pytest tools/liaison/tests/ -q` 与仓库根 `./venv/bin/python -m pytest -q` 均需
0 失败，且总用例数 ≥ 改动前（收编与改名不得导致用例被误删）。

【五、收口】
1. `git add tools/liaison/tests/conftest.py tools/liaison/tests/test_notify_ratelimit.py tools/liaison/tests/test_notify_store.py tools/liaison/tests/test_notify_webhook.py tools/liaison/tests/test_notify_transport.py tools/liaison/errors.py tools/liaison/config.py tools/liaison/followup.py tools/liaison/notify/webhook.py docs/tech-debt.md`
   （最后一项：把 TD-29「仍欠两条」改成 `✅ 已还`，附本次 commit hash）
2. commit（一条即可），走 `finishing-a-development-branch` 合回 `main`
3. 合并后 `git rev-list --count main..<分支>`；非 0 时再跑 `git cherry -v main <分支>` 确认全 `-`（真合）
4. 确认内容真在 main 上之前 ⛔ 不许输出 `OPENER_DONE`

【六、并发协议（已逐字内嵌，本批只有一条泳道，仍照抄）】
1. 只 `git add` 本条明确列出的路径。⛔ 禁止 `git add -A` / `git add .` / `git commit -a` / `git stash`
2. `git status` 里出现别人的改动是正常的，不要停下、不要问、不要顺手提交
3. push 被拒 → `git pull --rebase --autostash origin main` 后重试，最多 3 次
4. 报 `.git/index.lock` 已存在 → 等 5 秒重试最多 5 次，⛔ 默认绝不删除该锁
```

> ✅ 已完成 2026-09-16（OK，commit 555592a；脚本判 NO-SENTINEL 系哨兵被加粗成 `**OPENER_DONE**`，Cowork 核验后手工摘）· 原泳道 token治理0916K
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Paul 在手机上，2026-09-16 由 Cowork 经 launchd 请求文件发车），名字另由 -n 给。🔴 若改为手工贴进 CC Desktop，必须自行补上第 3 行。

```
[Mac]0916K-Token治理-泳道开工整读大文件与提醒误停根治
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（改项目 hook、执行器与共享文档，要在主检出一次改完一次提交）｜ 工作区: 仓库根 ｜ 派发: Cowork·Token治理-0916

读 docs/openers/0916K-Token治理-泳道开工整读大文件与提醒误停根治.md 全文并逐节执行（该文件按分段读，每次 Read 带 offset＋limit ≤ 400 行），以该文件为准。文件不存在 → 输出 OPENER_PARTIAL: 正文文件不存在 并停。
无头补充（覆盖正文里对应说法）：
1. 正文【一】3 的 git status 预期里另有 `M docs/openers/OP-0820-全量编排.md`（本块自身），属正常；【七】2 的 git add 列表追加 `docs/openers/OP-0820-全量编排.md`。
2. 本 session 就是由 run-lanes.sh 起的泳道：正文【一】2「pgrep run-lanes 为空否则停」**跳过**（会命中自己的父进程）。
3. ⛔ 不修改 `docs/openers/run-lanes.sh`（它正在执行本泳道）。若【二】2 的失败只能靠改 run-lanes.sh 解决 → 不改，登记「⏸ 留步：run-lanes.sh 需改，待非泳道会话处理」，其余段照做，最后 OPENER_PARTIAL。
4. 凡正文写「停下报我」，一律改为：登记原因、跳过该段后面依赖它的步骤、最终输出 OPENER_PARTIAL: <原因>，⛔ 不提问。
5. 收工报告写进 `.claude/handoff/0916K-收工报告.md`（正文【八】各项），并在终端输出里同样给出。
```

> ✅ 已完成 2026-09-16（OK）· 原泳道 token治理0916L（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Cowork 经 launchd 请求文件发车），名字另由 -n 给。

```
[Mac]0916L-哨兵加粗容错与0916K摘标注提交
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（只提交 Cowork 已写好的两处小改动）｜ 工作区: 仓库根 ｜ 派发: Cowork·Token治理-0916

背景：0916K 活已干完并提交（555592a），但哨兵输出成 `**OPENER_DONE**` 被判 NO-SENTINEL、泳道标注没摘，下一批会重跑它。Cowork 已改好（未提交）：run-lanes.sh 哨兵正则容忍加粗／反引号；tests/test_run_lanes_model.py 加一条用例；OP-0820 把 0916K 标注换成已完成行，并追加本块。
1. `git pull --rebase --autostash origin main`；`git status --short` 本件相关应只有：M docs/openers/run-lanes.sh ｜ M tests/test_run_lanes_model.py ｜ M docs/openers/OP-0820-全量编排.md ｜ M docs/openers/号池台账.md
2. `bash -n docs/openers/run-lanes.sh && ./venv/bin/python -m pytest -q tests/test_run_lanes_model.py 2>&1 | tail -2` → 0 失败；失败 → 不提交，输出 OPENER_PARTIAL: 测试失败 <末行>
3. ⛔ 不修改 run-lanes.sh（它正在执行本泳道），只提交
4. git add docs/openers/run-lanes.sh tests/test_run_lanes_model.py docs/openers/OP-0820-全量编排.md docs/openers/号池台账.md
5. git commit -m "fix(run-lanes): 哨兵容忍加粗/反引号 ＋ 摘 0916K 标注（0916L）"，按并发协议 push
6. 最后一行顶格输出 OPENER_DONE（不加粗、不加任何符号）
```

## 路线图落档 ＋ 收尾归档 ＋ 自动化缺口 G1/G2（2026-09-17 12:2x Cowork·HR业务线-接力0917D 远程发车，Shao Peishen「能开尽开」）

> 全景见 `docs/roadmap/HR项目实施路线图与构建自动化流程.md`。三条泳道触碰区零重叠：甲＝文档与两个 openspec 包（主工作区串行）；乙＝`tools/liaison/` 附件链路（worktree）；丙＝新增提交请求通道脚本（worktree）。

> 泳道：收尾落档
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Cowork 经 launchd 请求文件发车），名字另由 -n 给。

```
[Mac]0917T-路线图与Cowork文档落档提交
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: main ｜ worktree: ❌ 不勾（只提交 Cowork 已写好的文档）｜ 工作区: 仓库根 ｜ 派发: Cowork·HR业务线-接力0917D

无人值守：⛔ 不提问。
1. `git status --short`；本条只提交这些（存在即 add，不存在跳过）：`docs/roadmap/HR项目实施路线图与构建自动化流程.md`、`docs/跟进信/人事部-汤丽萍-答复单-2026-09-17-人事部#1决策点a.md`、`docs/session接力.md`、`docs/openers/OP-0820-全量编排.md`、`docs/openers/归档/OP-0820-历史批次.md`、`docs/openers/号池台账.md`。⚠️ 若 `docs/跟进信/README-跟进信清单.md` 或 `docs/跟进信/回件/**` 有改动、或 `data/liaison/unpack-session.lock` 的 pid 存活（`ps -p`）⇒ 汤丽萍真实回件正被拆件会话处理：⛔ 不碰那些文件
2. `./venv/bin/python -m pytest -q tests/test_doc_size_budget.py tests/test_caretaker_openers.py 2>&1 | tail -1` 0 失败，否则 OPENER_PARTIAL: 体积闸
3. 逐个 `git add` 上述路径（⛔ 禁 -A／./commit -a／stash；⛔ 不 add `_to_delete/`）→ `git commit -m "docs(roadmap): HR 实施路线图与构建自动化流程 ＋ 答复单审核通过 ＋ 派 0917T–X（0917T）"` → push（被拒 pull --rebase --autostash 重试≤3；index.lock 等 5 秒重试≤5，⛔ 不删锁）
4. `git rev-list --left-right --count HEAD...@{u}` ＝ `0 0`；最后一行顶格 OPENER_DONE
```

> 泳道：收尾落档
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Cowork 经 launchd 请求文件发车），名字另由 -n 给。

```
[Mac]0917U-回件包5点5落档与归档
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: main ｜ worktree: ❌ 不勾（openspec 归档是文档操作）｜ 工作区: 仓库根 ｜ 派发: Cowork·HR业务线-接力0917D

无人值守：⛔ 不提问；「停下报」＝OPENER_PARTIAL: <原因>。
【前置】`git log --oneline -n 10 | grep -c 0917T` ≥1；`head -1 openspec/changes/liaison-reply-bridge-and-patrol/tasks.md` 含 `32/33`；`grep -n '^- \[ \]' openspec/changes/liaison-reply-bridge-and-patrol/tasks.md` 只剩 5.5 一行。否则 PARTIAL
【一、5.5 判据核对】`docs/findings/2026-09-17-打标即开班真实起活实测.md` 须含 5.2、5.3、5.4 各自的命令、时刻、审计表 SELECT 输出、日志片段，以及「订正（Cowork·0917D）」节。缺哪项就从 `.claude/handoff/lanes-20260917-*` 对应日志与 `data/liaison.db`（python sqlite3 只读）补进 findings，⛔ 不编造
【二、勾 5.5 → 33/33】tasks.md 勾 5.5，抬头进度改 33/33
【三、归档】`Skill(openspec-archive-change)` 归档 `liaison-reply-bridge-and-patrol`（按 skill：同步 delta specs 进 `openspec/specs/`、移到 `openspec/changes/archive/2026-09-17-liaison-reply-bridge-and-patrol/`）。skill 不可达 ⇒ 登记留步并 PARTIAL，⛔ 不手工 mv。归档后 `openspec validate --specs --strict`（或 skill 规定的校验）通过
【四、全量回归】`./venv/bin/python -m pytest -q 2>&1 | tail -1` 0 失败（归档会移动 openspec 路径，若有测试引用旧路径而红 ⇒ 只改测试里的路径常量、在报告列出；涉及产品代码 ⇒ PARTIAL）
【五、落档】`docs/session接力.md` R-3 行改「✅ §5 全通过，已归档（`<commit>`）」；`docs/roadmap/HR项目实施路线图与构建自动化流程.md` 第二节「M0′ 回件自动化」行状态改「✅ 已归档 2026-09-17」、第五节 U 行加 ✅
【六、提交】add 仅：`openspec/`（本包相关移动与 specs 同步）、findings、`docs/session接力.md`、路线图文件、必要的测试路径改动 → commit `chore(openspec): 归档 liaison-reply-bridge-and-patrol（0917U）` → push（并发协议同 0917T）。最后一行顶格 OPENER_DONE
```

> 泳道：收尾落档
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Cowork 经 launchd 请求文件发车），名字另由 -n 给。

```
[Mac]0917V-值守包8点6与8点7核证及8点8开窗
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: main ｜ worktree: ❌ 不勾（读真实库与日志、改 tasks 与 findings）｜ 工作区: 仓库根 ｜ 派发: Cowork·HR业务线-接力0917D

无人值守：⛔ 不提问；⛔ 不发任何消息、⛔ 不改名单、⛔ 不重启服务。
【前置】`git log --oneline -n 10 | grep -c 0917U` ≥1，否则 PARTIAL: 0917U 未完成
【一、逐条对照 `openspec/changes/hr-wecom-aibot-liaison/tasks.md` 8.6、8.7 原文判据，用真实证据核】证据源（只读）：`data/liaison.db`（python sqlite3，`mode=ro`）的 `liaison_message`／`liaison_task`／群通知相关表、`data/liaison/archive/`、值守服务日志（`launchctl print gui/$UID/com.zhuopin.hr.liaison` 找路径）、`docs/findings/2026-09-17-打标即开班真实起活实测.md`、`tools/liaison/config/whitelist.yaml`。
- 8.6 拆三条：群里文字 → 入队；私信文档 → 归档（⚠️ 目前附件链路未通，归 `0917W`——若只有私信文字而无文档归档证据，这一半如实判未满足）；群通知 → 回推；外加恒等不变式（`consistency.py` 或既有断言函数跑一次只读核对）。
- 8.7：名单含汤丽萍且她已有真实入站（09-17 09:45/09:46）。「告知她照原样发」有 `人事部#1`（09-09 已推送）为证。
判据全满足才勾；8.6 若仅差附件一半 ⇒ ⛔ 不勾，在该行下追加一行「⏸ 仅差私信文档归档，归 `0917W`（附件链路）」
【二、8.8 开窗】在 `docs/findings/2026-09-17-值守通道一周观察窗.md` 新建观察记录：开窗 2026-09-17、到期 2026-09-24；三个观察项（漏消息告警误报／限流触发／归档重名冲突）各写「查法（命令）」与「当日基线值」；⛔ 不在观察期内改判据。tasks.md 8.8 行下追加「⏳ 观察窗 09-17 → 09-24，记录见上述文件」，⛔ 不勾
【三、落档】tasks.md 抬头进度按实勾数更新；`docs/session接力.md` 【下一步】加一行 R-6「8.8 观察窗 09-24 到期 → 收观察结论 → 勾 8.8 → 当场归档 hr-wecom-aibot-liaison」（四列：谁做=Cowork 派泳道／状态／判据／不做会怎样）
【四、提交】add 仅：该 tasks.md、两份 findings、`docs/session接力.md` → commit `docs(liaison): 8.6/8.7 实证核证 ＋ 8.8 观察窗开窗（0917V）` → push。最后一行顶格 OPENER_DONE（8.6 未勾也算 DONE，只要如实登记）
```

> 泳道：附件归档接通
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Cowork 经 launchd 请求文件发车），名字另由 -n 给。

```
[Mac]0917W-私信附件进归档与回件桥
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: lane-0917w-attachments ｜ worktree: ✅ 勾（改产品代码）｜ 工作区: .claude/worktrees/lane-0917w-attachments ｜ 派发: Cowork·HR业务线-接力0917D ｜ 模型: Opus

无人值守：⛔ 不提问；「停下报」＝落档后 OPENER_PARTIAL。拆分豁免：先实测后接线，一条链路。
【零、为什么】业务要专员回的是**文件**（判例批改表、评测集标注、历史岗位资料）。现状：`tools/liaison/unpack/bridge.py` 的 `resolve_reply_archive_relpath` 注释写明 `outcome.attachments` 在当前系统里**恒为空**——入站帧里的文件从未被取出落盘。本条补齐「私信文件 → 归档 → 回件桥用附件路径 → 拆件会话能读到」。属已批准 spec（`hr-wecom-aibot-liaison` 第 4 章消息归档：附件落盘）与代码之间的缺口 ⇒ 轻量通道 TDD；若必须改 spec 文本 ⇒ PARTIAL。
🔴 红线：⛔ 不真实建连、⛔ 不发消息、⛔ 不改名单、⛔ 不碰 `tools/liaison/unpack/dispatch.py` 与章程 SKILL.md、⛔ 不改 `openspec/changes/hr-wecom-aibot-liaison/tasks.md`（另一条泳道在改）。
【一、前置】pwd／分支一致；`grep -n 'attachments' tools/liaison/unpack/bridge.py` 命中「恒为空」注释；解释器 `PYTHONPATH=. /Users/paulshao/Projects/HumanResource/venv/bin/python -m pytest`；SDK 只在主工作区 `tools/liaison/.venv`，测试 ⛔ 不 import aibot
【二、字段实测（⛔ 不许猜）】
1. 只读查 SDK 表面：主工作区 `tools/liaison/.venv` 里 aibot 包对文件/图片消息的类型定义、下载接口（URL＋aeskey 解密？media_id？）、`docs/findings/2026-09-09-aibot-wsclient-表面实测.md`、`docs/findings/2026-09-09-入站带附件链路预演.md`、win 端实证 findings
2. 只读查真实帧日志：值守服务日志里 `frames` 打印的「帧键结构」行（`grep -rn` 日志目录找 msgtype 非 text 的帧）；有真实文件帧 ⇒ 按实际键写映射，注释写依据（文件:行）
3. 无真实文件帧依据 ⇒ **fail-closed 支**（TD-19/TD-43 同一处置）：映射表留空、遇文件帧只记一行无取值的帧键结构、不落盘、不报错；在 `docs/tech-debt.md` 新登 TD（号＝当前最大+1）「私信文件帧字段未经真实帧确认」，销账条件＝Shao Peishen／汤丽萍私信一个测试文件后按日志补映射。这种情况下仍把【三】的下游接线与测试做完
【三、接线（先红后绿）】
- 帧 → `attachment`（名称、类型、下载句柄）的映射是纯函数 `compute_*`；下载与落盘是副作用，放值守线程侧，复用 `archive.archive_message(... attachment=...)` → `attachments.store_attachment`（已有、带幂等）
- 下载失败 ⇒ 正文照常归档、附件记告警、⛔ 不吞成 0 字节文件（TD-41 同类风险，加一条测试：下载返回空 ⇒ 不写空文件、记告警）
- `bridge.resolve_reply_archive_relpath` 在有附件时用附件路径（已有分支），删掉「恒为空」注释改为实情；新增测试：带附件的入站 ⇒ 信号项 `archived_path` 指向附件文件
- 幂等：同 msgid 重投不重复下载/落盘（测试）
【四、验收】新测试先红后绿（报红时原文）；`tools/liaison/tests` 与全量 pytest 0 失败（报前后数）；`openspec validate hr-wecom-aibot-liaison --strict` 过
【五、收口】worktree 内提交（若走 fail-closed 支，TD 登记一并提交）→ ff 合 main（失败 ⇒ worktree 内 rebase main 重测再 ff，仍失败 PARTIAL）→ push → 真合校验。并发协议：只 add 本条路径；禁 -A／./commit -a／stash；不删锁。⚠️ 合入后值守服务要**重启**才生效——本条 ⛔ 不重启，报告写「待下一条主工作区泳道或 Shao Peishen 重启」。收工三行：映射走实测还是 fail-closed ｜ passed·failed ｜ commit。最后一行顶格 OPENER_DONE
```

> 泳道：提交请求通道
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Cowork 经 launchd 请求文件发车），名字另由 -n 给。

```
[Mac]0917X-Cowork文档提交请求通道
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: lane-0917x-commit-request ｜ worktree: ✅ 勾（新增脚本与测试）｜ 工作区: .claude/worktrees/lane-0917x-commit-request ｜ 派发: Cowork·HR业务线-接力0917D ｜ 模型: Opus

无人值守：⛔ 不提问。拆分豁免：脚本、安装器、测试三件一体。
【零、为什么】Cowork 碰 `.git/` 会留删不掉的锁，只能写文件；它写的文档必须等「下一条泳道顺带提交」，已多次造成未提交态滞留与 ff 合并被挡（09-17 `0917K`）。发车已有「请求文件 → launchd → lane-launcher.sh」这条不经分类器的路（`.claude/skills/lane-dispatch/SKILL.md`「发车怎么起」），本条仿它做**提交请求**通道。
🔴 ⛔ 不修改 `docs/openers/run-lanes.sh`、`docs/openers/lane-launcher.sh`、`scripts/install_lane_launcher.py`（它们在跑/在用）；⛔ 本条不安装 LaunchAgent（安全配置，Shao Peishen 本人在 Terminal 装）。
【一、设计（写进脚本头注释）】
- 请求文件 `.claude/handoff/commit/<ts>.request`，JSON：`{"message": "...", "paths": ["docs/...", ...], "push": true}`
- 新脚本 `docs/openers/commit-launcher.sh`（或 `scripts/commit_request.py` ＋ 薄壳，自选，写明理由）：认领（改名 `.claiming`）→ 校验 → `git add -- <逐个路径>` → `git commit -m` → 可选 push → 写 `<ts>.done`（含 commit hash）或 `<ts>.rejected`（含原因）
- **路径白名单**（机器闸）：只允许 `docs/**`（排除 `docs/openers/run-lanes.sh`、`docs/openers/lane-launcher.sh`、`docs/openers/commit-launcher.sh`）、`openspec/changes/**/tasks.md`；拒绝 `tools/`、`app/`、`scripts/`、`tests/`、`.claude/`、`CLAUDE.md`、`data/`、`.env*`、任何含 `..` 或绝对路径、任何不存在于工作区 diff 的路径；禁止 `-A`／`.`
- 提交前跑 `tests/test_doc_size_budget.py`（有 venv 用 venv，否则跳过并在 .done 注明），红则 rejected
- 有 `run-lanes` 在跑时仍可提交（只 add 列出路径），但 `.git/index.lock` 存在 ⇒ 等 5 秒重试≤5，仍在 ⇒ `.deferred`，⛔ 不删锁
- 安装器 `scripts/install_commit_launcher.py`：生成 plist（WatchPaths 指 `.claude/handoff/commit/`；**AbandonProcessGroup** 与 **EnvironmentVariables.PATH** 两项必带——09-09 lane-launcher 实证缺这两项会被连坐杀/找不到命令）；支持 `--print` 只打印不安装
【二、测试（先红后绿，`tests/test_commit_launcher.py`）】在 tmp git 仓里：合法请求 ⇒ commit 出现且只含列出路径；白名单外路径 ⇒ rejected 且无 commit；`..`／绝对路径 ⇒ rejected；未改动路径 ⇒ rejected；index.lock 存在 ⇒ deferred 且锁未删；push=false ⇒ 不 push；plist 生成含 AbandonProcessGroup 与 PATH
【三、文档】`.claude/skills/lane-dispatch/SKILL.md` ⛔ 不改（`.claude/` 在白名单外且属规则真源，改动需另议）；改在 `docs/roadmap/HR项目实施路线图与构建自动化流程.md`？⛔ 也不改（另一条泳道在改）。只在脚本头注释与 `docs/tech-debt.md` 之外新增 `docs/findings/2026-09-17-提交请求通道设计与安装说明.md`（含 Shao Peishen 本人要跑的**一行安装命令**）
【四、验收收口】全量 pytest 0 失败（报数）→ ff 合 main（失败 rebase 重测）→ push → 真合校验。并发协议同前。收工三行：设计要点 ｜ passed·failed ｜ commit ＋ 待 Shao Peishen 安装的一行命令原文。最后一行顶格 OPENER_DONE
```
