# OP-0820 · 全量 Opener 编排

> 2026-08-20。**唯一权威 opener 清单**，`OP-0820-A_单元BC开工.md` 已作废。
>
> **编排原则**：不依赖人工控制执行节奏。有依赖就彻底串行，能并发的全部并行。
> 并发安全靠《并发协议》做到机器自愈，**不靠"你记得别同时开"**。
>
> **格式**：每个代码块头两行是标题行 + 【设置】行，整块复制即可，不要拆。

---

## 🔢 号池台账 → 已迁至 `docs/openers/号池台账.md`

> 〔归档工具〕 2026-09-16：出号前查、给出后登记，一律改在该文件；历史月份的行在 `docs/openers/归档/号池台账-归档.md`。

## 二、并发协议（**每条 opener 内已逐字内嵌，此处备查**）

1. **只 `git add` 本条 opener 明确列出的路径。** ⛔ 禁止 `git add -A` / `git add .` / `git commit -a`。这是整套并行编排成立的唯一前提。
2. **`git status` 里出现别人的改动是正常的**——那是并行的另一条 opener 在干活。不要停下、不要问、不要顺手提交。
3. ⛔ 不要在 commit 前 pull（并发下会被别人的未提交改动挡住）；push 被拒才 `git pull --rebase --autostash origin main` 重试。
4. push 被拒（non-fast-forward）→ 再 `git pull --rebase origin main` 后重试，**最多 3 次**，仍失败停下贴错误。
5. 报 `.git/index.lock: File exists` → **等 5 秒重试，最多 5 次**。⛔ **绝不删除该锁文件**——那是另一个 session 正在用，删了会毁掉它的提交。

---

## 待执行区

> 〔归档工具〕 2026-09-16：`lane-dispatch` 新写的无头 opener 放在本节之后（`> 泳道：<名>` ＋ 代码块）。
> 〔归档工具〕 跑完被 `mark_done` 摘标注的批次，下次 `python3 scripts/archive_docs.py --apply` 会整段搬去归档。

> ✅ 已完成 2026-09-16（OK）· 原泳道 liaison测试脚手架收尾（脚本自动摘，不再进 run-lanes）

```
[Mac]0916E-TD29收尾liaison测试脚手架收编与config文案
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ✅ 勾（写代码）｜ 工作区: worktree（`.claude/worktrees/td29-liaison-test-scaffolding-0916E`）｜ 派发: run-lanes.sh

【零、背景与授权】TD-29 剩余两条，Shao Peishen 2026-09-10 已答 `4a`：**下一批泳道单独派一条**，独占
`tools/liaison/tests/` 与 `tools/liaison/config.py`，⛔ 不挂 8.6 灰度前置、⛔ 不留着不做。全文见
`docs/tech-debt.md` TD-29 条目「仍欠两条」。这是**纯重构 + 文案修正，不改变外部可观察行为**，按
CLAUDE.md「轻量通道」走：⛔ 不用 `spec-to-plan`/`run-build`，直接 TDD 单条改完提交
（`superpowers:test-driven-development`；调不到 ⇒ 登记「⏸ 留步：superpowers 不可达」并停，⛔ 不手工按磁盘 SKILL.md 走）。

【一、前置自检】
1. `pwd` 必须含 `.claude/worktrees/`，否则停下报
2. `git log --oneline -5 -- tools/liaison/tests/test_notify_store.py | grep -c 'assert_group_notify_identity'` 之类的旧顾虑已随
   `5ca1f50` 处置，本条不需要再验；只需确认 `grep -n 'assert_group_notify_identity' tools/liaison/tests/test_notify_store.py` 命中 ≥2 处（存在即可，⛔ 不是本条任务）

【二、任务 A · conftest 收编（三处复制粘贴）】
现状（已核实的确切行号，实现前重新 grep 一遍确认未漂移）：
- `FakeClock`：`tools/liaison/tests/test_notify_ratelimit.py:14` 与 `tools/liaison/tests/test_notify_webhook.py:63` 两份定义，逐字节比对是否相同
- `RecordingSink`：`tools/liaison/tests/test_notify_store.py:29` 与 `tools/liaison/tests/test_notify_webhook.py:76` 两份
- `FAKE_WEBHOOK`：`tools/liaison/tests/test_notify_transport.py:25` 与 `tools/liaison/tests/test_notify_webhook.py:15` 两份（值＝`https://example.invalid/cgi-bin/webhook/send?key=fake-key-for-tests`，⛔ 改动中不得把这个假值换成真实域名）

步骤：
1. 若两份定义逐字节不同，先登记差异、以更完整/更新的那份为准，⛔ 不擅自改逻辑
2. 三者收进 `tools/liaison/tests/conftest.py`，以 pytest fixture 或模块级常量形式导出（`FakeClock`/`RecordingSink` 用 `@pytest.fixture` 更符合 pytest 惯例，但若两处原调用点是直接实例化类而非注入，保持调用点最小改动优先——不确定就选影响面小的那种）
3. 四个原文件改为从 `conftest.py`（同目录自动可见，无需显式 import fixture；若是模块级常量/类则需要 `from .conftest import ...` 或按 pytest 惯例处理）里取用，删除本地重复定义
4. 跑 `tools/liaison/tests/` 全量，四份原文件涉及的用例必须逐条仍过，且总数不减少（收编不是删测试）

【三、任务 B · config.py:75 异常文案错位】
现状：`tools/liaison/config.py:70-83` 的 `load_group_webhook` 复用 `tools/liaison/errors.py` 的
`MissingCredentialsError`，其消息固定「HR 值守通道**拒绝启动**：…」（`errors.py:19`）。但
`load_group_webhook` 是**发送期**校验（docstring 已明写"⛔ 刻意不加进 REQUIRED_CREDENTIAL_ENV_NAMES"，
理由是收消息与发群通知独立、不能连坐），服务本身仍在正常运行，只是这一次群通知发不出去。
运维看到"拒绝启动"字样会误判成服务整体宕了。

步骤：
1. 在 `tools/liaison/errors.py` 新增一个独立异常（例如 `GroupWebhookMissingError(RuntimeError)`），
   构造签名与 `MissingCredentialsError` 一致（`missing_names: Iterable[str]`，消息同样⛔ 只出现变量名不出现取值），
   但文案改为准确描述"群通知发送地址缺失，本次跳过发送"一类措辞，**不含"拒绝启动"字样**
2. `config.py:83` 的 `raise MissingCredentialsError([GROUP_WEBHOOK_ENV])` 改成 `raise GroupWebhookMissingError([GROUP_WEBHOOK_ENV])`
3. 调用方核对并同步：`tools/liaison/followup.py:216-217`（`except MissingCredentialsError` 那块，
   若捕获的正是 `load_group_webhook` 抛出的这个异常则要改成捕获新类型；若那里捕获的是别的调用点的
   `MissingCredentialsError` 则不动，只改群通知这条路径）、`tools/liaison/notify/webhook.py`（若有捕获此异常的逻辑同样核）
4. `tools/liaison/tests/test_notify_transport.py:333-366` 四处 `config.load_group_webhook(...)` 相关用例，
   涉及"抛异常"的断言改成断言新异常类型 `GroupWebhookMissingError`（⛔ 不要只改字符串匹配，要改类型）
5. 先红后绿：新增/改动的用例在改代码前跑必须红，改完再绿

【四、全量回归】`./venv/bin/python -m pytest tools/liaison/tests/ -q` 与仓库根 `./venv/bin/python -m pytest -q` 均需
0 失败，且总用例数 ≥ 改动前（收编与改名不得导致用例被误删）。

【五、收口】
1. `git add tools/liaison/tests/conftest.py tools/liaison/tests/test_notify_ratelimit.py tools/liaison/tests/test_notify_store.py tools/liaison/tests/test_notify_webhook.py tools/liaison/tests/test_notify_transport.py tools/liaison/errors.py tools/liaison/config.py tools/liaison/followup.py tools/liaison/notify/webhook.py docs/tech-debt.md`
   （最后一项：把 TD-29「仍欠两条」改成 `✅ 已还`，附本次 commit hash）
2. commit（一条即可），走 `finishing-a-development-branch` 合回 `main`
3. 合并后 `git rev-list --count main..<分支>`；非 0 时再跑 `git cherry -v main <分支>` 确认全 `-`（真合）
4. 确认内容真在 main 上之前 ⛔ 不许输出 `OPENER_DONE`

【六、并发协议（已逐字内嵌，本批只有一条泳道，仍照抄）】
1. 只 `git add` 本条明确列出的路径。⛔ 禁止 `git add -A` / `git add .` / `git commit -a` / `git stash`
2. `git status` 里出现别人的改动是正常的，不要停下、不要问、不要顺手提交
3. push 被拒 → `git pull --rebase --autostash origin main` 后重试，最多 3 次
4. 报 `.git/index.lock` 已存在 → 等 5 秒重试最多 5 次，⛔ 默认绝不删除该锁
```

> ✅ 已完成 2026-09-16（OK，commit 555592a；脚本判 NO-SENTINEL 系哨兵被加粗成 `**OPENER_DONE**`，Cowork 核验后手工摘）· 原泳道 token治理0916K
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Paul 在手机上，2026-09-16 由 Cowork 经 launchd 请求文件发车），名字另由 -n 给。🔴 若改为手工贴进 CC Desktop，必须自行补上第 3 行。

```
[Mac]0916K-Token治理-泳道开工整读大文件与提醒误停根治
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（改项目 hook、执行器与共享文档，要在主检出一次改完一次提交）｜ 工作区: 仓库根 ｜ 派发: Cowork·Token治理-0916

读 docs/openers/0916K-Token治理-泳道开工整读大文件与提醒误停根治.md 全文并逐节执行（该文件按分段读，每次 Read 带 offset＋limit ≤ 400 行），以该文件为准。文件不存在 → 输出 OPENER_PARTIAL: 正文文件不存在 并停。
无头补充（覆盖正文里对应说法）：
1. 正文【一】3 的 git status 预期里另有 `M docs/openers/OP-0820-全量编排.md`（本块自身），属正常；【七】2 的 git add 列表追加 `docs/openers/OP-0820-全量编排.md`。
2. 本 session 就是由 run-lanes.sh 起的泳道：正文【一】2「pgrep run-lanes 为空否则停」**跳过**（会命中自己的父进程）。
3. ⛔ 不修改 `docs/openers/run-lanes.sh`（它正在执行本泳道）。若【二】2 的失败只能靠改 run-lanes.sh 解决 → 不改，登记「⏸ 留步：run-lanes.sh 需改，待非泳道会话处理」，其余段照做，最后 OPENER_PARTIAL。
4. 凡正文写「停下报我」，一律改为：登记原因、跳过该段后面依赖它的步骤、最终输出 OPENER_PARTIAL: <原因>，⛔ 不提问。
5. 收工报告写进 `.claude/handoff/0916K-收工报告.md`（正文【八】各项），并在终端输出里同样给出。
```

> ✅ 已完成 2026-09-16（OK）· 原泳道 token治理0916L（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Cowork 经 launchd 请求文件发车），名字另由 -n 给。

```
[Mac]0916L-哨兵加粗容错与0916K摘标注提交
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（只提交 Cowork 已写好的两处小改动）｜ 工作区: 仓库根 ｜ 派发: Cowork·Token治理-0916

背景：0916K 活已干完并提交（555592a），但哨兵输出成 `**OPENER_DONE**` 被判 NO-SENTINEL、泳道标注没摘，下一批会重跑它。Cowork 已改好（未提交）：run-lanes.sh 哨兵正则容忍加粗／反引号；tests/test_run_lanes_model.py 加一条用例；OP-0820 把 0916K 标注换成已完成行，并追加本块。
1. `git pull --rebase --autostash origin main`；`git status --short` 本件相关应只有：M docs/openers/run-lanes.sh ｜ M tests/test_run_lanes_model.py ｜ M docs/openers/OP-0820-全量编排.md ｜ M docs/openers/号池台账.md
2. `bash -n docs/openers/run-lanes.sh && ./venv/bin/python -m pytest -q tests/test_run_lanes_model.py 2>&1 | tail -2` → 0 失败；失败 → 不提交，输出 OPENER_PARTIAL: 测试失败 <末行>
3. ⛔ 不修改 run-lanes.sh（它正在执行本泳道），只提交
4. git add docs/openers/run-lanes.sh tests/test_run_lanes_model.py docs/openers/OP-0820-全量编排.md docs/openers/号池台账.md
5. git commit -m "fix(run-lanes): 哨兵容忍加粗/反引号 ＋ 摘 0916K 标注（0916L）"，按并发协议 push
6. 最后一行顶格输出 OPENER_DONE（不加粗、不加任何符号）
```

## 拆件检查点边界 ＋ M2·U0 建造三段（2026-09-17 14:1x Cowork·HR业务线-接力0917D 远程发车，「能开尽开」）

> 两条泳道零重叠：甲只动 `tools/liaison/`；乙只动 `app/`、`tests/`、`scripts/`（U0 新文件）、`requirements-m2-u0.txt`、U0 计划与 m2 包 tasks。U0「待裁决」#6（paddlepaddle 无 cp314）未答前按计划写的 D14 退路执行；#3 无 API key 的模型登记留步，⛔ 不采购、⛔ 不向任何人要 key。

> ✅ 已完成 2026-09-17（OK）· 原泳道 拆件检查点边界（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Cowork 经 launchd 请求文件发车），名字另由 -n 给。

```
[Mac]0917AG-拆件检查点边界与口径台账测试
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: lane-0917ag-checkpoint ｜ worktree: ✅ 勾（改产品代码与测试）｜ 工作区: .claude/worktrees/lane-0917ag-checkpoint ｜ 派发: Cowork·HR业务线-接力0917D

无人值守：⛔ 不提问；轻量通道 TDD（补已批准 spec 与代码缺口，⛔ 不改 spec 文本；必须改 ⇒ PARTIAL）。拆分豁免：两处小修同一包。
【一、检查点边界】事实（拆件会话 `95b4d15` 登记于 `docs/session接力.md`）：`unpack-signal --clear --before <检查点>` 在检查点与信号项 `at` **相等**时清不掉该项。spec `openspec/specs/liaison-unpack-dispatch/spec.md`「只清检查点之前」保持严格小于不变。先查根因：`tools/liaison/unpack/dispatch_wiring.py::bridge_dispatch` 传给 `compute_prompt` 的 `checkpoint_iso` 与 `bridge` 落信号项的 `at` 是否取自同一 `now`、格式口径是否一致（`session.format_instant` 的 `+08:00` 微秒定宽 vs `isoformat()`——`clear_signal_before` 按字符串比较，口径不一致本身就是缺陷）。修法：检查点取**信号落盘之后**的时刻且与 `at` 同口径（`format_instant`），保证本条信号 `at < checkpoint`；起活期间新到的项仍 `>=`。先红（复现：同一 now 生成的信号项清不掉）后绿；并加口径一致性测试。
【二、口径台账测试】`tools/liaison/tests/test_criteria_ledger_file.py::test_signed_off_row_has_no_evidence_yet` 断言**真实台账**里 HR-G-01 恒为「待专员且 evidence 空」，今天真实流转（已回复 → 已签认，Shao Peishen 答 1a）后必然红。改为不依赖流转进度的不变式：真实台账每行状态 ∈ 四态；状态为「已签认」的行 evidence 非空；状态为「待专员」的行 evidence 为空。⛔ 不改台账数据文件。
【三、验收收口】`tools/liaison/tests` 与全量 pytest 0 失败（报前后数）；提交 → ff 合 main（失败 rebase 重测）→ push → 真合校验。⛔ 不重启值守服务（报告写需重启生效）。并发协议：只 add 本条路径；禁 -A／./commit -a／stash；不删锁。最后一行顶格 OPENER_DONE
```

> ✅ 已完成 2026-09-17（OK）· 原泳道 M2U0建造（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Cowork 经 launchd 请求文件发车），名字另由 -n 给。

```
[Mac]0917AH-M2U0建造Task1至3
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: lane-m2-u0 ｜ worktree: ✅ 勾（写代码）｜ 工作区: .claude/worktrees/lane-m2-u0 ｜ 派发: Cowork·HR业务线-接力0917D ｜ 模型: Opus

【零、范围】`m2-resume-parse-and-rank` 单元 U0，计划 `docs/superpowers/plans/2026-09-17-m2-unit0-model-comparison.md` 的 **Task 1–3**（本单元 3 段中的第 1 段）。⛔ 只做这几个 Task；⛔ 不做 final review、不合 main、不删 worktree、不回勾 tasks.md。
【一、前置——任一不过即 OPENER_PARTIAL 并停，⛔ 不提问】
1. `grep -c '^### Task ' docs/superpowers/plans/2026-09-17-m2-unit0-model-comparison.md` = 8，Task 1–3 存在；`git log --oneline -1 -- docs/superpowers/plans/2026-09-17-m2-unit0-model-comparison.md` 有提交
2. `Skill(superpowers:subagent-driven-development)` 可达；不可达 ⇒ OPENER_PARTIAL: superpowers 不可达（⛔ 不手工按磁盘 SKILL.md 走）
3. `pwd` 含 `.claude/worktrees/lane-m2-u0`，分支 `lane-m2-u0`；解释器用主工作区 `/Users/paulshao/Projects/HumanResource/venv/bin/python`（`PYTHONPATH=.`），⛔ 不在 worktree 新建 venv
【二、执行】调 `Skill(run-build)`（即 subagent-driven-development）按计划执行 Task 1–3：每 Task 实现子代理＋两阶段 review，逐 Task 提交在 `lane-m2-u0`。计划 Global Constraints 原样交 reviewer。
🔴 红线：⛔ 真实在招简历、⛔ 真实个人信息进仓库或测试夹具；⛔ 发任何外部 API 请求（Task 1–3 是纯函数地基）；⛔ 改 `tools/liaison/`。
【三、收工】报告写各 Task commit hash 与 `git log lane-m2-u0 --oneline -n 5`；最后一行顶格 OPENER_DONE
```

> ✅ 已完成 2026-09-17（OK）· 原泳道 M2U0建造（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Cowork 经 launchd 请求文件发车），名字另由 -n 给。

```
[Mac]0917AI-M2U0建造Task4至6
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: lane-m2-u0 ｜ worktree: ✅ 勾（写代码）｜ 工作区: .claude/worktrees/lane-m2-u0 ｜ 派发: Cowork·HR业务线-接力0917D ｜ 模型: Opus

【零、范围】同单元第 2 段：计划 `docs/superpowers/plans/2026-09-17-m2-unit0-model-comparison.md` 的 **Task 4–6**。⛔ 不做 final review、不合 main、不回勾。
【一、前置】`git log lane-m2-u0 --oneline -n 10` 能看到 0917AH 的 Task 1–3 提交（按 superpowers 进度台账核），否则 PARTIAL；superpowers 可达；worktree/分支同上
【二、执行】`Skill(run-build)` 从进度台账续跑 Task 4–6。计划里依赖安装步骤：只装计划列出的轻依赖（numpy／pypdf／python-docx／reportlab／Pillow／pymupdf）到**主工作区 venv**前先 `pip download --only-binary` 核 cp314 wheel；PaddleOCR／FlagEmbedding 只探针不强装；「待裁决」#6 未答 ⇒ 按 D14 退路（扫描件进不可读队列＋登记技术债）。⛔ 真实简历、⛔ 外部 API
【三、收工】报告写 commit hash；最后一行顶格 OPENER_DONE
```

> ✅ 已完成 2026-09-17（PARTIAL）· 原泳道 M2U0建造（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Cowork 经 launchd 请求文件发车），名字另由 -n 给。

```
[Mac]0917AJ-M2U0建造Task7至8与收口
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: lane-m2-u0 ｜ worktree: ✅ 勾（写代码）｜ 工作区: .claude/worktrees/lane-m2-u0 ｜ 派发: Cowork·HR业务线-接力0917D ｜ 模型: Opus

【零、范围】同单元末段：计划 `docs/superpowers/plans/2026-09-17-m2-unit0-model-comparison.md` 的 **Task 7–8** ＋ 全分支 final review ＋ 合回 main ＋ 回勾 ＋ 清 worktree。
【一、前置】`git log lane-m2-u0` 能看到 Task 1–6 提交，否则 PARTIAL；superpowers 可达
【二、执行】`Skill(run-build)` 续跑 Task 7–8。实跑部分：只用**本机已配置**的模型 key（读既有配置文件，⛔ 不新建、⛔ 不打印 key、⛔ 不向任何人索取）；无 key 的模型登记「⏸ 留步：缺 key（外部采购，Shao Peishen）」；BGE-M3 本地 CPU 若权重未下载且下载超 20 分钟 ⇒ 登记留步不硬等。样本只用计划 Task 5 的合成样本。
【三、收口】全分支 final review（Opus）→ 修复波 → 全量 pytest 0 失败（报数）→ `git -C /Users/paulshao/Projects/HumanResource merge --ff-only lane-m2-u0`（失败 ⇒ worktree 内 rebase main 重测再 ff）→ push → 真合校验（rev-list/cherry）→ 在 worktree 里回勾 `openspec/changes/m2-resume-parse-and-rank/tasks.md` 中已闭环的 1.1–1.6（1.7 🔴 定型确认留给 Shao Peishen，⛔ 不勾）并提交合入 → 清 worktree。并发协议：只 add 本单元路径；禁 -A／./commit -a／stash；不删锁。
【四、收工】三行：合入 commit ｜ passed·failed ｜ 1.7 定型确认需要 Shao Peishen 看的结论文件路径与待答项。最后一行顶格 OPENER_DONE
```

## 任务驱动自转 R1–R5（2026-09-17 15:5x Cowork·HR业务线-接力0917D，Shao Peishen：拆巡逻、任务驱动自转、能开尽开）

> 设计真源 `docs/roadmap/任务驱动workflow设计.md`。甲只动 `docs/openers/lane-launcher.sh`、`docs/openers/commit-launcher.sh`、`scripts/`、`tools/liaison/owner_notify.py` 与测试、`docs/openers/run-lanes.sh` 末尾**一行**；乙只动 `.claude/skills/task-dispatcher/`、`docs/roadmap/`、`scripts/dispatcher_*`／`scripts/install_task_dispatcher.py` 与其测试。零重叠。

> ✅ 已完成 2026-09-17（OK）· 原泳道 发车排队与动作通道（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Cowork 经 launchd 请求文件发车），名字另由 -n 给。

```
[Mac]0917AK-发车排队与动作通道固化
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: lane-0917ak-launch-queue ｜ worktree: ✅ 勾（改脚本与测试）｜ 工作区: .claude/worktrees/lane-0917ak-launch-queue ｜ 派发: Cowork·HR业务线-接力0917D ｜ 模型: Opus

无人值守：⛔ 不提问；「停下报」＝落档后 OPENER_PARTIAL。拆分豁免：排队、事件、动作通道同一闭环。读 `docs/roadmap/任务驱动workflow设计.md` 第二、四节 R1。
【一、发车排队 R1】`lane-launcher.sh` 检测到 run-lanes 在跑时：请求移入 `.claude/handoff/launch/queue/<ts>.request`（⛔ 不再 .deferred）并写 `<ts>.queued` 回执。`run-lanes.sh` 收敛末尾（owner-notify 调用之后）**只加一行**：`mkdir -p "$REPO/.claude/handoff/events" && : > "$REPO/.claude/handoff/events/lanes-done-$STAMP"`——超出这一行 ⇒ PARTIAL。出队：`scripts/action_request.py` 新动作 `launch-queue-drain`（无参数）——无 run-lanes 在跑时把 queue 里最早一条移回 `.claude/handoff/launch/` 触发发车；并由 R2 调度器在处理 `lanes-done-*` 事件时调用（乙泳道接）。⚠️ 本泳道由 run-lanes 驱动，改 run-lanes.sh 只加那一行；运行实例是自拷贝的副本，不受影响。
【二、动作通道固化】`scripts/action_request.py` 与 `docs/openers/commit-launcher.sh` 是 Cowork 直写、未测：补 `tests/test_action_request.py`（白名单、🆕 待发闸、路径闸含 `..`、key 打码、claiming 残留在完成后清理为 `.processed`、未知动作拒绝、kickstart 调用形状用替身）；新增动作 `install-agent`（参数 `script` 只允许 `scripts/install_[a-z_]+\.py` 且文件存在，执行 `venv/bin/python <script>`）、`launch-lanes`（参数 `args` 过 lane-launcher 同一白名单，写入 launch 目录或 queue）。
【三、私信口径】`tools/liaison/owner_notify.py` 已被 Cowork 直改：批次 results 全 OK 不入队、有 PARTIAL/FAIL 才入队——补测试（全 OK 不入队；含 PARTIAL/FAIL 入队；空 results 行为不变）。
【四、验收收口】新测试先红后绿（Cowork 直改部分以「删改动即红」证明）；全量 pytest 0 失败（报数）；提交（含 Cowork 直写的 `scripts/action_request.py`、`commit-launcher.sh`、`owner_notify.py` 改动）→ ff 合 main（失败 rebase 重测）→ push → 真合校验。并发协议：只 add 本条路径；禁 -A／./commit -a／stash；不删锁。最后一行顶格 OPENER_DONE
```

> ✅ 已完成 2026-09-17（OK）· 原泳道 任务驱动调度器（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Cowork 经 launchd 请求文件发车），名字另由 -n 给。

```
[Mac]0917AL-任务台账与定夺队列与阶段规则
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: lane-0917al-dispatcher ｜ worktree: ✅ 勾（新增文档、脚本与测试）｜ 工作区: .claude/worktrees/lane-0917al-dispatcher ｜ 派发: Cowork·HR业务线-接力0917D ｜ 模型: Opus

无人值守：⛔ 不提问。本单元 2 段中的第 1 段：⛔ 不合 main、不删 worktree。读 `docs/roadmap/任务驱动workflow设计.md` 全文。
【一、R3 任务台账】写 `scripts/dispatcher_backlog.py`：从 `docs/roadmap/HR项目实施路线图与构建自动化流程.md`、`openspec/changes/*/tasks.md`（未勾项、🔴 不可代标记）、`docs/superpowers/plans/*.md`（Task 与拆段建议）、`docs/session接力.md` 待办表、`docs/openers/号池台账.md` 生成 `docs/roadmap/任务台账.yaml`（字段按设计第四节：id／场景／阶段／依赖／触碰区／状态／阻塞类型／产出判据／来源）；生成是**合并**不是覆盖：已有条目的状态以台账为准，真身冲突写 `conflicts:`。跑一次产出首版并提交在分支上。测试：给定小样本仓库生成稳定、幂等、不可代项标为阻塞类型「决策」。
【二、R5 定夺队列】`docs/roadmap/定夺队列.md`（表：编号／场景／问题／选项与代价／推荐／来源／阻塞的任务 id／状态 待答|已答|作废／答复）；迁入现存待答项：`docs/session接力.md` 与路线图第六节里仍未答的、m2 包 tasks 中 🔴 待 Shao Peishen 项（如 1.7 定型确认、试运行岗位 D10）、TD 中「需 Shao Peishen」的（如群 webhook 配置、TD-51 真实文件帧）。每条标阻塞类型（决策／外部输入）。
【三、R4 阶段推进规则】写进 `.claude/skills/task-dispatcher/rules.md`（被下一段的 skill 引用）：设计第四节 R4 逐条，外加：ready 判据（依赖全完成、非阻塞、触碰区不与在跑泳道重叠、不属 `.51` 发版等不可代）；能开尽开上限（max-parallel 3、单批 ≤6 条、按场景优先级 M0>M1>M2>M3、同场景按阶段先后）；每阶段的完成判据取自真身（合 main 用 rev-list/cherry、包 validate、plan Task 数）。**新场景发现（intent 前一棒）**：三个来源各一条规则——① 路线图里状态为「未启动」且前置场景已达启动条件的场景（如 M2 各单元依次、M3 在 M2 上线后、「明确不在 MVP」清单在主链路验证后）⇒ 定夺队列追加「是否启动场景 X 的需求收敛（grill）」；② 拆件会话回灌结论判为「新需求」或台账口径点提出新能力 ⇒ 同上并附来源回件；③ Shao Peishen 在本线直接提出 ⇒ Cowork 写 intent 草稿进 `docs/roadmap/intents/`。启动获批 ⇒ 生成 grill 任务（阻塞类型＝决策，由 `requirement-grill` skill 在本线执行）⇒ intent 落档后进 propose。
【四、收工】提交在 `lane-0917al-dispatcher`；报告写台账条目数、ready 数、定夺队列待答数。最后一行顶格 OPENER_DONE
```

> ✅ 已完成 2026-09-17（OK）· 原泳道 任务驱动调度器（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Cowork 经 launchd 请求文件发车），名字另由 -n 给。

```
[Mac]0917AM-调度器skill与事件唤醒及收口
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: lane-0917al-dispatcher ｜ worktree: ✅ 勾（新增 skill、脚本与测试）｜ 工作区: .claude/worktrees/lane-0917al-dispatcher ｜ 派发: Cowork·HR业务线-接力0917D ｜ 模型: Opus

无人值守：⛔ 不提问。同单元第 2 段：做完合 main。前置：分支上有 0917AL 的提交与 `docs/roadmap/任务台账.yaml`，否则 PARTIAL。
【一、R2 调度器 skill】`.claude/skills/task-dispatcher/SKILL.md`：被事件唤醒的无头会话逐步执行——① 单实例锁 `.claude/handoff/dispatcher.lock`（pid 存活即退出）② 刷新台账（`scripts/dispatcher_backlog.py`）③ 按 `rules.md` 算 ready 集 ④ 对 ready 集按 `lane-dispatch` skill 规则写无头块进 OP-0820、登记号池（先 grep）、dry-run、opener_split_check、体积闸 ⑤ 经动作通道 `launch-lanes`（或写 launch request；发车器占用自动入队）发车 ⑥ 需人项追加 `docs/roadmap/定夺队列.md`（去重）⑦ 文档改动经提交请求通道提交 ⑧ 处理 `lanes-done-*` 事件时先调 `launch-queue-drain` 动作 ⑨ 事件文件处理完移到 `events/processed/`。红线：⛔ 发版 `.51`、⛔ 对外发信（除非台账行已是 🆕 待发）、⛔ 改 `.claude/skills/` 其它 skill、⛔ 改 CLAUDE.md、⛔ 真实简历；遇不确定 ⇒ 写定夺队列，不猜。
【二、事件唤醒】`scripts/dispatcher_event.sh`（薄壳：cd 仓库、取锁、`claude -p` 以 Sonnet 执行 skill、预算上限 $10、日志 `.claude/handoff/dispatcher/<ts>.log`、PATH 含 `~/.local/bin`）＋ `scripts/install_task_dispatcher.py`（LaunchAgent：WatchPaths `.claude/handoff/events/`＋StartCalendarInterval 每日 09:00 兜底；AbandonProcessGroup；PATH；`--print`）。事件来源接线：提交请求通道完成一次含 `docs/roadmap/定夺队列.md` 的提交后写 `events/decision-<ts>`（改 `scripts/commit_request.py` 末尾一处）；拆件会话收口不接（Shao Peishen：拆件完成不需第一时间知道；由每日兜底与下一事件带上）。
【三、测试】锁互斥；事件文件移入 processed；安装器 plist 两项必带键；commit_request 写 decision 事件仅在含定夺队列路径时；skill 文档含全部红线字样（机器闸）。
【四、收口】全分支 final review（Opus）→ 全量 pytest 0 失败（报数）→ ff 合 main（失败 rebase 重测）→ push → 真合校验 → 清 worktree。⛔ 本条不安装 LaunchAgent（合入后由 Cowork 经动作通道 `install-agent` 安装）。收工三行：合入 commit ｜ passed·failed ｜ 台账 ready 数与定夺待答数。最后一行顶格 OPENER_DONE
```

> ✅ 已完成 2026-09-17（OK）· 原泳道 需求收敛skill移植（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Cowork 经 launchd 请求文件发车），名字另由 -n 给。

```
[Mac]0917AN-移植需求收敛grill与跟进信收敛到HR
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: lane-0917an-grill ｜ worktree: ✅ 勾（新增 skill 与测试）｜ 工作区: .claude/worktrees/lane-0917an-grill ｜ 派发: Cowork·HR业务线-接力0917D ｜ 模型: Opus

无人值守：⛔ 不提问；「停下报」＝落档后 OPENER_PARTIAL。拆分豁免：单个 skill 移植。
【零、为什么】Shao Peishen 2026-09-17：任务驱动 workflow 的第一棒 intent 采用 Win 端（卓品 AI 赋能项目）的需求收敛机制——grill 追问＋跟进信判例批改让需求快速收敛。HR 仓库目前没有这条 skill（`.claude/skills/` 无 grill），M2 十题是 Cowork 凭方法跑的。按 memory「企业AI转型只读参考、HR 侧自建实现、不跨仓库耦合」口径移植。
【一、取正本（⛔ 凭印象写＝违反原 skill 的 fail-loud 规则）】Win 端 skill 是引用式，规则正文在 zhuopin-ai 仓库 `0-学习与工具/skills源码/zhuopin-需求grill/SKILL.md`。在本机找只读来源：`gh repo list Raytheoner --limit 100` 与 `gh search code "zhuopin-需求grill" --owner Raytheoner`（或该仓库的 GitHub 远端）→ `gh api repos/<owner>/<repo>/contents/<URL编码路径>` 取全文；同时取它引用的判例批改相关正本（跟进信 skill 的判例批改法段）。取不到 ⇒ 在 `docs/roadmap/定夺队列.md`（不存在则新建最小表头）追加一条「外部输入：请提供 zhuopin-ai 仓库地址或授权」，OPENER_PARTIAL 并停，⛔ 不凭摘要仿写。
【二、HR 侧自建】`.claude/skills/requirement-grill/SKILL.md`：保留 M1 设计树与前沿／M2 可查事实自己查（HR 版事实源清单：`CLAUDE.md`、`docs/session接力.md`、`docs/roadmap/*`、`openspec/`（含 archive 与 specs）、`docs/findings/`、`docs/tech-debt.md`、`docs/跟进信/`（台账、回件、口径点台账）、git 历史、值守库只读）／M3 前沿为空才算完；受访者只有 Shao Peishen 或场景发起人；专员部分走跟进信判例批改（接 `docs/跟进信/` 口径与回件桥：信件决策点 ⇒ 口径点台账 ⇒ 签认进定夺队列）；提问格式＝本线定夺模板（选项＋代价＋推荐＋一行答复模板）；产出落 `docs/roadmap/intents/<场景>-intent.md`（格式对齐 `docs/roadmap/M2-intent.md`：决策 D1…Dn、建议交付单元、不做、不可代项），落档经提交通道提交即产生事件交调度器进 propose。改动之处在 SKILL.md 末尾「与 Win 端正本差异」逐条列出。
【三、测试】`tests/test_requirement_grill_skill.py`：SKILL.md 含三机制名、受访者限制、事实源清单中每个路径在仓库真实存在、产出路径与 intent 必备小节字样（机器闸）。
【四、收口】全量 pytest 0 失败（报数）→ 提交 → ff 合 main（失败 rebase 重测）→ push → 真合校验。并发协议：只 add 本条路径；禁 -A／./commit -a／stash；不删锁。最后一行顶格 OPENER_DONE
```

## 在环闸门 G1–G5 接线（2026-09-17 17:0x Cowork·HR业务线-接力0917D，Shao Peishen：intent、design、spec 定稿与发布、发信须他在环确认才推进）

> ✅ 已完成 2026-09-17（OK）· 原泳道 在环闸门接线（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Cowork 经 launchd 请求文件发车），名字另由 -n 给。

```
[Mac]0917AO-在环闸门G1至G5接线进调度器与动作通道
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: lane-0917ao-gates ｜ worktree: ✅ 勾（改 skill 规则、脚本与测试）｜ 工作区: .claude/worktrees/lane-0917ao-gates ｜ 派发: Cowork·HR业务线-接力0917D ｜ 模型: Opus

无人值守：⛔ 不提问。前置：main 上已有 0917AK、0917AM、0917AN 的合入（`.claude/skills/task-dispatcher/rules.md`、`SKILL.md`、`.claude/skills/requirement-grill/SKILL.md`、`scripts/action_request.py` 均存在），缺任一 ⇒ OPENER_PARTIAL 并停。拆分豁免：单一机制接线。
【零、为什么】读 `docs/roadmap/任务驱动workflow设计.md` 末节「在环闸门」全文——这是 Shao Peishen 的硬口径：G1 intent 定稿、G2 design／spec 定稿、G3 发布、G4 发信、G5 口径签认，放行只认 `docs/roadmap/定夺队列.md` 对应行「状态＝已答」且答复含放行字样（定／发／签）。
【一、规则】`rules.md`：R4 阶段迁移 intent→propose、包 validate→plan、全单元合 main→发布、跟进信起草→发送、拆件口径→签认，全部插入闸门：到闸 ⇒ 定夺队列追加（去重，字段含闸号与产出路径）并把该场景标阻塞类型「决策」；放行判据函数化（放 `scripts/dispatcher_backlog.py` 或新 `scripts/gates.py`：`gate_open(gate, subject) -> bool`，只读定夺队列）。`task-dispatcher/SKILL.md` 红线改为：⛔ 越闸；⛔ 调用 `send-followup` 动作或把台账改为 🆕 待发（发信只由 Cowork 在本线收到「发」后执行）；删去「除非台账行已是 🆕 待发」例外。`requirement-grill/SKILL.md`：intent 落档状态字样为「草稿·待 G1」，⛔ 不再写「落档即交调度器进 propose」，改为「落档 ⇒ 定夺队列 G1」。
【二、动作通道】`scripts/action_request.py` 的 `send-followup`：除原「🆕 待发」闸外，再要求定夺队列存在含该信件编号的 G4 行且已答含「发」，否则 rejected（原因写明缺 G4 放行）；`deploy-51` 若已存在同样接 G3。
【三、测试】`gate_open` 各闸真值表（待答／已答非放行字样／已答放行／作废）；调度器规则在 intent 草稿存在但 G1 未放行时 ready 集不含 propose；send-followup 无 G4 放行被拒、有则放行（用临时仓库夹具，⛔ 不真发）；三份 SKILL.md 含闸门红线字样（机器闸）。
【四、收口】全分支 final review（Opus）→ 全量 pytest 0 失败（报数）→ ff 合 main（失败 rebase 重测）→ push → 真合校验 → 清 worktree。并发协议：只 add 本条路径；禁 -A／./commit -a／stash；不删锁。收工三行：合入 commit ｜ passed·failed ｜ 定夺队列现有闸门行数。最后一行顶格 OPENER_DONE
```

## M2 试运行岗位 intent 补行（2026-09-17 18:1x task-dispatcher·decision-20260917-181604，Q-03 已答 b：底层软件工程师）

> ✅ 已完成 2026-09-17（OK）· 原泳道 M2试运行岗位intent补行（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动，print 模式下那个 MCP 工具未必挂载，名字另由 -n 给。🔴 若改为手工贴进 CC Desktop，必须自行补上第 3 行。

```
[Mac]0917AP-M2试运行岗位intent补行
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: lane-0917ap-jobline ｜ worktree: ✅ 勾（只改 intent.md／tasks.md 两行）｜ 工作区: .claude/worktrees/lane-0917ap-jobline ｜ 派发: task-dispatcher·decision-20260917-181604

无人值守：⛔ 不提问；查不到或有歧义 ⇒ 定夺队列追加一条后 OPENER_PARTIAL 并停，⛔ 不猜。并发协议四条（照抄）：① 只 `git add` 本条明确列出的路径，⛔ `git add -A`／`git add .`／`git commit -a`／`git stash`；② `git status` 里出现别人的改动是正常的，不要停、不要问、不要顺手提交；③ push 被拒 → `git pull --rebase --autostash origin main` 重试最多 3 次；④ `.git/index.lock` 已存在 → 等 5 秒重试最多 5 次，⛔ 不删锁（孤儿锁判据见 CLAUDE.md，本条不判定，直接留步）。
【零、为什么】`openspec/changes/m2-resume-parse-and-rank/tasks.md` 0.3 判据：`intent.md` 末尾补一行「试运行岗位：<岗位> job_id=<…> profile_version=<n>」。Shao Peishen 已在定夺队列 Q-03 答 b：底层软件工程师（仍在招）；其冻结画像版本作 U6 评测集与 U7 试运行 `job_id`（2026-09-17 18:0x）。
【一、查 job_id 与 profile_version】读 `app/storage/job_queries.py` 与 `app/web/server.py` 里 `job_profile` 相关查询写法，去实际数据源（`app/config.py` 里配的 DB，⛔ 不是 `data/demo.db`——那是演示夹具，`title` 全是「待确定」）里按标题含"底层软件工程师"查 `job` 与 `job_profile` 表，取该岗位当前 `status='approved'` 的最新 `version`。⚠️ 若同标题出现多条不同 `job_id`（如试点期误建重复记录），或找不到真实数据源，⛔ 不猜、不用测试夹具凑数——在 `docs/roadmap/定夺队列.md` 追加一条「外部输入：请指认底层软件工程师岗位的真实 job_id」，OPENER_PARTIAL 并停。
【二、补行】确认唯一后：① 在 `intent.md` 末尾追加一行「试运行岗位：底层软件工程师 job_id=<实际值> profile_version=<实际值>」；② `tasks.md` 0.3 复选框改 `- [x]`。⛔ 不改其它行。
【三、收口】跑一遍与 intent/tasks 相关的既有测试确认未破坏（如有）；提交 → ff 合 main（失败则 rebase 重测）→ push → 真合校验（`git cherry -v main <分支>` 无 `+`）→ 清 worktree。收工两行：合入 commit（或 PARTIAL 原因）｜ job_id／version 取值来源。最后一行顶格 OPENER_DONE
```

## 已答定夺转任务（2026-09-17 18:4x Cowork·HR业务线-接力0917D 首轮核验发现：调度器 3 轮 ready=0，已答 Q-02a/Q-20a/Q-21a 等没有生成可发车任务）

> ✅ 已完成 2026-09-17（OK）· 原泳道 已答定夺转任务（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Cowork 经 launchd 请求文件发车），名字另由 -n 给。

```
[Mac]0917AQ-已答定夺自动转任务并补派首批
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: lane-0917aq-answered ｜ worktree: ✅ 勾（改脚本、规则与测试）｜ 工作区: .claude/worktrees/lane-0917aq-answered ｜ 派发: Cowork·HR业务线-接力0917D ｜ 模型: Opus

无人值守：⛔ 不提问。拆分豁免：单一机制缺口。
【零、缺口实证】`.claude/handoff/dispatcher/20260917-181634.log` 三轮均 ready=0；第 3 轮自述「Q-02 已答但文本规则会永久重判决策——保留台账待开原值」。`docs/roadmap/定夺队列.md` 状态＝已答的行（Q-02a、Q-12a、Q-13b、Q-14a、Q-15a、Q-17a、Q-18b、Q-19①、Q-20a、Q-21a、Q-03b、Q-05b、Q-06a、Q-07a、Q-09a）没有转成台账任务，也没有解除对应阻塞。
【一、机制】`scripts/dispatcher_backlog.py`：读定夺队列「已答」行 ⇒ ① 解除「阻塞的任务 id」列所列条目的决策阻塞（以队列为准覆盖文本启发式，修掉 Q-02 永久重判）② 答复蕴含新动作的，按 `rules.md` 新增一节「答复→任务映射」生成任务条目（字段齐全、来源＝队列编号、幂等：同编号不重复生成）。映射首版逐条写死并测试：Q-02a ⇒ 起草 `docs/compliance/` PIA／候选人同意条款（单列 AI 评估）／留存与删除策略三份草稿（对接人谷雨，产出后进定夺队列待 Shao Peishen 签，属 G5 类签认）；Q-20a ⇒ TD-13 方案 C 小泳道（`app/web/server.py` 调用点 catch、ERROR 日志、照常返回，TDD）；Q-21a ⇒ `requirement-grill` SKILL.md 移植 15 分钟超时默认＋测试；Q-12a／Q-14a ⇒ 挂「下次 `.51` 发版」清单（阻塞类型＝决策 G3，不单独发车）；Q-15a ⇒ 2026-09-20 22:00–23:00 `.51` 重启任务（阻塞类型＝外部输入：内网＋执行前本线确认）；Q-17a ⇒ 06 清单 3.3／9.1–9.3 标作废；Q-19① ⇒ 写进 M2 U4 plan 约束；Q-18b／Q-13b ⇒ 关闭对应条目；Q-07a ⇒ 1.4 阻塞改外部输入（等 key）。未来新答复无映射 ⇒ 调度器写定夺队列「答复 Q-xx 缺任务映射」而非静默。
【二、测试】已答行解阻塞；映射幂等；无映射项进队列；Q-02 不再被文本规则重判；现有闸门测试全绿。
【三、收口】全分支 final review（Opus）→ 全量 pytest 0 失败（报数）→ ff 合 main → push → 真合校验 → 清 worktree → 写 `.claude/handoff/events/answers-mapped-<ts>` 触发调度器按新台账派发（⛔ 本条自己不派业务泳道）。并发协议：只 add 本条路径；禁 -A／./commit -a／stash；不删锁。收工三行：合入 commit ｜ passed·failed ｜ 新生成任务数与解阻塞数。最后一行顶格 OPENER_DONE
```

## 06清单标作废与合规草稿与TD13与grill超时默认（2026-09-17 19:0x task-dispatcher·answers-mapped-20260917-190659，Q-17a／Q-20a／Q-02a／Q-21a 四条已答任务四触碰区互不重叠，各自独立泳道并行）

> ✅ 已完成 2026-09-17（OK）· 原泳道 06清单标作废（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动，print 模式下那个 MCP 工具未必挂载，名字另由 -n 给。🔴 若改为手工贴进 CC Desktop，必须自行补上第 3 行。

```
[Mac]0917AR-06清单3.3与9.1至9.3标作废
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: main ｜ worktree: ❌ 不勾（只改一份文档，无代码）｜ 工作区: 仓库根 ｜ 派发: task-dispatcher·answers-mapped-20260917-190659

无人值守：⛔ 不提问；查不到对应章节 ⇒ 定夺队列追加一条后 OPENER_PARTIAL 并停，⛔ 不猜。并发协议四条（照抄）：① 只 `git add` 本条明确列出的路径，⛔ `git add -A`／`git add .`／`git commit -a`／`git stash`；② `git status` 里出现别人的改动是正常的，不要停、不要问、不要顺手提交；③ push 被拒 → `git pull --rebase --autostash origin main` 重试最多 3 次；④ `.git/index.lock` 已存在 → 等 5 秒重试最多 5 次，⛔ 不删锁（孤儿锁判据见 CLAUDE.md，本条不判定，直接留步）。
【零、为什么】`docs/roadmap/定夺队列.md:33` Q-17 已答 a：`06-企业AI转型资产借鉴清单.md` 的 §3.3、§9.1–9.3 标作废——原沟通线内容已并入《人事部#2》之后的跟进信，不再单列。
【一、改动】只改 `06-企业AI转型资产借鉴清单.md`：定位 §3.3、§9.1、§9.2、§9.3 的标题与文末对应清单项，在标题末尾追加「⚰️ 作废（Q-17a）」标记，并在该条下补一行去向说明「已并入《人事部#2》之后的跟进信」。⛔ 不删原文、不改其余章节、不动其它文件。
【二、收口】`git add 06-企业AI转型资产借鉴清单.md` → commit → push（被拒按上方并发协议③重试）→ 确认远端含本次提交（`git log origin/main -1`）。收工两行：合入 commit ｜ 标作废的 4 处小节列表。最后一行顶格 OPENER_DONE
```

> ✅ 已完成 2026-09-17（OK）· 原泳道 TD13方案C（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动，print 模式下那个 MCP 工具未必挂载，名字另由 -n 给。🔴 若改为手工贴进 CC Desktop，必须自行补上第 3 行。

```
[Mac]0917AS-TD13方案C服务端异常兜底
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: lane-0917as-td13 ｜ worktree: ✅ 勾（改 `app/web/server.py` 与测试）｜ 工作区: .claude/worktrees/lane-0917as-td13 ｜ 派发: task-dispatcher·answers-mapped-20260917-190659

无人值守：⛔ 不提问；`discard_thread_checkpoints` 调用点找不到或既有测试已覆盖同等断言 ⇒ 登记「⏸ 留步：<原因>」后 OPENER_PARTIAL 并停，⛔ 不猜。并发协议四条（照抄）：① 只 `git add` 本条明确列出的路径，⛔ `git add -A`／`git add .`／`git commit -a`／`git stash`；② `git status` 里出现别人的改动是正常的，不要停、不要问、不要顺手提交；③ push 被拒 → `git pull --rebase --autostash origin main` 重试最多 3 次；④ `.git/index.lock` 已存在 → 等 5 秒重试最多 5 次，⛔ 不删锁（孤儿锁判据见 CLAUDE.md，本条不判定，直接留步）。
【零、为什么】`docs/tech-debt.md` TD-13、`定夺队列.md:36` Q-20 已答 a：方案 C——`app/web/server.py` 里调用 `discard_thread_checkpoints` 的地方 catch 异常、记 ERROR 级日志、照常返回既有引导语，不让 checkpointer 清理失败拖垮接口响应（工程铁律 1 的幂等/清理动作不应影响用户可见路径）。
【一、TDD】`superpowers:test-driven-development` 起手（调不到 ⇒ 登记「⏸ 留步：superpowers 不可达」并停，⛔ 不手工按磁盘 SKILL.md 走）。先在 `tests/test_web_api.py` 写失败用例：mock/monkeypatch 让 `discard_thread_checkpoints` 抛异常，断言该接口仍返回既有引导语（HTTP 200、响应结构不变）且产生一条 ERROR 级日志记录；再改 `app/web/server.py` 对应调用点加 try/except 使测试通过。
【二、销账】`docs/tech-debt.md` TD-13 行改标「✅ 已还」，附本次分支合入后的 commit hash（final review 通过、真拿到 hash 后再补这一行，⛔ 不先写占位再忘记回填——回填也在本条分支上做，不再开新分支）。
【三、收口】`venv/bin/python -m pytest tests/test_web_api.py -q` 全绿 → 全量 `venv/bin/python -m pytest -q`（报 passed/failed 数，非 0 failed 不许合并）→ ff 合 main（失败则 `git pull --rebase --autostash origin main` 后重测）→ push → 真合校验 `git cherry -v main lane-0917as-td13`（要求无 `+`）→ 清 worktree。收工三行：合入 commit ｜ passed·failed ｜ TD-13 销账状态。最后一行顶格 OPENER_DONE
```

> ✅ 已完成 2026-09-17（OK）· 原泳道 合规验收1号三份草稿（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动，print 模式下那个 MCP 工具未必挂载，名字另由 -n 给。🔴 若改为手工贴进 CC Desktop，必须自行补上第 3 行。

```
[Mac]0917AT-合规验收1号三份草稿
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: main ｜ worktree: ❌ 不勾（只落三份文档草稿＋登记一条定夺队列行，无代码）｜ 工作区: 仓库根 ｜ 派发: task-dispatcher·answers-mapped-20260917-190659

无人值守：⛔ 不提问，⛔ 不代签、不对外发送、不改 `m2-resume-parse-and-rank/0.2` 的阻塞状态。并发协议四条（照抄）：① 只 `git add` 本条明确列出的路径，⛔ `git add -A`／`git add .`／`git commit -a`／`git stash`；② `git status` 里出现别人的改动是正常的，不要停、不要问、不要顺手提交；③ push 被拒 → `git pull --rebase --autostash origin main` 重试最多 3 次；④ `.git/index.lock` 已存在 → 等 5 秒重试最多 5 次，⛔ 不删锁（孤儿锁判据见 CLAUDE.md，本条不判定，直接留步）。
【零、为什么】`定夺队列.md:18` Q-02 已答 a：现在启动合规验收 #1，法务对接人＝谷雨。M2 门槛（CLAUDE.md 部署约束 5、合规红线）要求处理真实简历前须有 PIA 报告、候选人同意条款（单列 AI 评估）、留存与删除策略三份文档，且须 Shao Peishen 本人在 G5 签认——本条只起草，⛔ 不代签。
【一、起草三份】`docs/compliance/` 下（目录不存在先建）新建三份 md，顶部统一标注「草稿·待签认（Q-02a，对接人：谷雨，起草日期 2026-09-17）」：
  - `PIA报告-合规验收1号.md`：处理目的、处理范围（简历数据不出境）、AI 评分用途、风险点与缓解措施，对齐工程铁律 3/4/5 与合规红线七条逐条自查
  - `候选人同意条款-AI评估单列.md`：单列告知候选人简历将经 AI 辅助评分排序、AI 只做排序推荐不做自动淘汰（淘汰须人工确认留痕）、候选人可行使的权利
  - `留存与删除策略.md`：简历与评分数据留存期限、删除触发条件与执行方式，对齐 PIPL 与工程铁律 6（企微回调先落库）的数据边界
【二、登记 G5 定夺队列行】`python3 scripts/gates.py request G5 合规验收#1 --scene M2 --artifact docs/compliance/ --tasks m2-resume-parse-and-rank/0.2`；命令报「已有行」则跳过，不重复登记。
【三、收口】`git add docs/compliance/ docs/roadmap/定夺队列.md` → commit → push（被拒按并发协议③重试）→ 确认远端含本次提交。收工两行：合入 commit ｜ 三份草稿是否齐全＋G5 行是否已登记。最后一行顶格 OPENER_DONE
```

> ✅ 已完成 2026-09-17（OK）· 原泳道 grill十五分钟超时默认移植（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动，print 模式下那个 MCP 工具未必挂载，名字另由 -n 给。🔴 若改为手工贴进 CC Desktop，必须自行补上第 3 行。

```
[Mac]0917AU-grill十五分钟超时默认移植
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: lane-0917au-grilltimeout ｜ worktree: ✅ 勾（改 skill 文档与测试）｜ 工作区: .claude/worktrees/lane-0917au-grilltimeout ｜ 派发: task-dispatcher·answers-mapped-20260917-190659

无人值守：⛔ 不提问。并发协议四条（照抄）：① 只 `git add` 本条明确列出的路径，⛔ `git add -A`／`git add .`／`git commit -a`／`git stash`；② `git status` 里出现别人的改动是正常的，不要停、不要问、不要顺手提交；③ push 被拒 → `git pull --rebase --autostash origin main` 重试最多 3 次；④ `.git/index.lock` 已存在 → 等 5 秒重试最多 5 次，⛔ 不删锁（孤儿锁判据见 CLAUDE.md，本条不判定，直接留步）。
【零、为什么】`定夺队列.md:37` Q-21 已答 a：`requirement-grill` SKILL.md 移植 Win 端判例「答复模板发出 15 分钟无答复 ⇒ 按推荐生效」默认，同时保留 G1 intent 定稿人闸与「判据／口径／阈值类永不默认生效」的既有铁律不变。
【一、TDD】`superpowers:test-driven-development` 起手（调不到 ⇒ 登记「⏸ 留步：superpowers 不可达」并停，⛔ 不手工按磁盘 SKILL.md 走）。先在 `tests/test_requirement_grill_skill.py` 补用例（grep 断言）：SKILL.md 含「⏸️ 默认项」行与「15 分钟」规则字样；含落档措辞模板「超时按默认项生效（未获明确答复，<会话> <时刻>）」；同时仍含「G1 intent 定稿人闸保留」与「判据／口径／阈值类永不默认生效」字样。再改 `.claude/skills/requirement-grill/SKILL.md` 补对应段落使测试通过。
【二、收口】`venv/bin/python -m pytest tests/test_requirement_grill_skill.py -q` 全绿 → 全量 `venv/bin/python -m pytest -q`（报 passed/failed 数）→ ff 合 main（失败则 `git pull --rebase --autostash origin main` 后重测）→ push → 真合校验 `git cherry -v main lane-0917au-grilltimeout`（要求无 `+`）→ 清 worktree。收工三行：合入 commit ｜ passed·failed ｜ SKILL.md 新增段落摘要。最后一行顶格 OPENER_DONE
```

## 无头会话 `.51` 可达性只读探测（2026-09-17 19:4x Cowork·HR业务线-接力0917D，Shao Peishen 答「探」：验证 iPad 远程指挥 Mac 无头任务做内网部署是否可行）

> ✅ 已完成 2026-09-17（OK）· 原泳道 51可达性探测（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Cowork 经 launchd 请求文件发车），名字另由 -n 给。

```
[Mac]0917AV-无头会话51可达性只读探测
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: main（只写 findings 一份） ｜ worktree: ❌ 不勾（只读探测＋一份文档） ｜ 派发: Cowork·HR业务线-接力0917D ｜ 模型: Sonnet

无人值守：⛔ 不提问。⛔ 红线：对 `.51` 只允许只读命令；⛔ 不 scp、不 pip、不重启服务、不改计划任务、不跑 `sync-to-server.sh`、不写服务器任何文件；⛔ 不打印密钥、私钥路径内容、`.env`。每条 ssh 带 `-o BatchMode=yes -o ConnectTimeout=8`（免交互，卡认证即判失败而非挂起）。
【一、探测】逐项记录命令、退出码、耗时、stderr 摘要：① 本机网络：`route -n get 192.168.100.51` 与 `nc -z -G 5 192.168.100.51 22`（区分「不在内网」与「22 口不通」）② SSH 配置：`ssh -G zp51 | grep -E '^(hostname|user|identityfile|identitiesonly)'`（只记字段名与是否存在，不贴私钥路径以外内容）③ 免交互认证：`ssh -o BatchMode=yes -o ConnectTimeout=8 zp51 hostname` ④ 只读远端：`ssh … zp51 "powershell -NoProfile -Command \"Get-ScheduledTask | Where-Object TaskName -like '*recruit*' | Select TaskName,State\""` ⑤ 服务健康：从本机 `curl -s -o /dev/null -w '%{http_code}' --max-time 8 http://192.168.100.51:8095/hr/recruit-agent/`（及 `docs/deploy-51-server.md` 第 5b 步的健康地址）⑥ 环境差异：记录本会话 `SSH_AUTH_SOCK` 是否存在、`ssh-add -l` 退出码（1＝agent 无 key，2＝无 agent），对照判断是否因 launchd 无钥匙串 agent 导致认证失败。
【二、结论落档】写 `docs/findings/2026-09-17-无头会话51可达性探测.md`：逐项结果表＋总判定（可达／不在内网／认证需修）＋若认证失败给出一次性修复方案候选（如 `~/.ssh/config` 为 zp51 加 `UseKeychain yes`＋`AddKeysToAgent yes`、或专用无口令部署 key 限定命令），⛔ 本条不实施修复。经提交请求通道提交（`docs/**`）。
【三、收工】三行：总判定 ｜ 失败项编号 ｜ findings 路径。若判定「可达」，报告写「1.1 冒烟、Q-12、Q-14 可由无头发版任务执行（仍过 G3）」。最后一行顶格 OPENER_DONE（任何一项因红线未做 ⇒ OPENER_PARTIAL）
```
