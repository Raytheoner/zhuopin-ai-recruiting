# OP-0820 · 全量 Opener 编排

> 2026-08-20。**唯一权威 opener 清单**，`OP-0820-A_单元BC开工.md` 已作废。
>
> **编排原则**：不依赖人工控制执行节奏。有依赖就彻底串行，能并发的全部并行。
> 并发安全靠《并发协议》做到机器自愈，**不靠"你记得别同时开"**。
>
> **格式**：每个代码块头两行是标题行 + 【设置】行，整块复制即可，不要拆。

---

## 🔢 号池台账 → 已迁至 `docs/openers/号池台账.md`

> 〔归档工具〕 2026-09-16：出号前查、给出后登记，一律改在该文件；历史月份的行在 `docs/openers/归档/号池台账-归档.md`。

## 二、并发协议（**每条 opener 内已逐字内嵌，此处备查**）

1. **只 `git add` 本条 opener 明确列出的路径。** ⛔ 禁止 `git add -A` / `git add .` / `git commit -a`。这是整套并行编排成立的唯一前提。
2. **`git status` 里出现别人的改动是正常的**——那是并行的另一条 opener 在干活。不要停下、不要问、不要顺手提交。
3. ⛔ 不要在 commit 前 pull（并发下会被别人的未提交改动挡住）；push 被拒才 `git pull --rebase --autostash origin main` 重试。
4. push 被拒（non-fast-forward）→ 再 `git pull --rebase origin main` 后重试，**最多 3 次**，仍失败停下贴错误。
5. 报 `.git/index.lock: File exists` → **等 5 秒重试，最多 5 次**。⛔ **绝不删除该锁文件**——那是另一个 session 正在用，删了会毁掉它的提交。

---

## 待执行区

> 〔归档工具〕 2026-09-16：`lane-dispatch` 新写的无头 opener 放在本节之后（`> 泳道：<名>` ＋ 代码块）。
> 〔归档工具〕 跑完被 `mark_done` 摘标注的批次，下次 `python3 scripts/archive_docs.py --apply` 会整段搬去归档。

> ✅ 已完成 2026-09-16（OK）· 原泳道 liaison测试脚手架收尾（脚本自动摘，不再进 run-lanes）

```
[Mac]0916E-TD29收尾liaison测试脚手架收编与config文案
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ✅ 勾（写代码）｜ 工作区: worktree（`.claude/worktrees/td29-liaison-test-scaffolding-0916E`）｜ 派发: run-lanes.sh

【零、背景与授权】TD-29 剩余两条，Shao Peishen 2026-09-10 已答 `4a`：**下一批泳道单独派一条**，独占
`tools/liaison/tests/` 与 `tools/liaison/config.py`，⛔ 不挂 8.6 灰度前置、⛔ 不留着不做。全文见
`docs/tech-debt.md` TD-29 条目「仍欠两条」。这是**纯重构 + 文案修正，不改变外部可观察行为**，按
CLAUDE.md「轻量通道」走：⛔ 不用 `spec-to-plan`/`run-build`，直接 TDD 单条改完提交
（`superpowers:test-driven-development`；调不到 ⇒ 登记「⏸ 留步：superpowers 不可达」并停，⛔ 不手工按磁盘 SKILL.md 走）。

【一、前置自检】
1. `pwd` 必须含 `.claude/worktrees/`，否则停下报
2. `git log --oneline -5 -- tools/liaison/tests/test_notify_store.py | grep -c 'assert_group_notify_identity'` 之类的旧顾虑已随
   `5ca1f50` 处置，本条不需要再验；只需确认 `grep -n 'assert_group_notify_identity' tools/liaison/tests/test_notify_store.py` 命中 ≥2 处（存在即可，⛔ 不是本条任务）

【二、任务 A · conftest 收编（三处复制粘贴）】
现状（已核实的确切行号，实现前重新 grep 一遍确认未漂移）：
- `FakeClock`：`tools/liaison/tests/test_notify_ratelimit.py:14` 与 `tools/liaison/tests/test_notify_webhook.py:63` 两份定义，逐字节比对是否相同
- `RecordingSink`：`tools/liaison/tests/test_notify_store.py:29` 与 `tools/liaison/tests/test_notify_webhook.py:76` 两份
- `FAKE_WEBHOOK`：`tools/liaison/tests/test_notify_transport.py:25` 与 `tools/liaison/tests/test_notify_webhook.py:15` 两份（值＝`https://example.invalid/cgi-bin/webhook/send?key=fake-key-for-tests`，⛔ 改动中不得把这个假值换成真实域名）

步骤：
1. 若两份定义逐字节不同，先登记差异、以更完整/更新的那份为准，⛔ 不擅自改逻辑
2. 三者收进 `tools/liaison/tests/conftest.py`，以 pytest fixture 或模块级常量形式导出（`FakeClock`/`RecordingSink` 用 `@pytest.fixture` 更符合 pytest 惯例，但若两处原调用点是直接实例化类而非注入，保持调用点最小改动优先——不确定就选影响面小的那种）
3. 四个原文件改为从 `conftest.py`（同目录自动可见，无需显式 import fixture；若是模块级常量/类则需要 `from .conftest import ...` 或按 pytest 惯例处理）里取用，删除本地重复定义
4. 跑 `tools/liaison/tests/` 全量，四份原文件涉及的用例必须逐条仍过，且总数不减少（收编不是删测试）

【三、任务 B · config.py:75 异常文案错位】
现状：`tools/liaison/config.py:70-83` 的 `load_group_webhook` 复用 `tools/liaison/errors.py` 的
`MissingCredentialsError`，其消息固定「HR 值守通道**拒绝启动**：…」（`errors.py:19`）。但
`load_group_webhook` 是**发送期**校验（docstring 已明写"⛔ 刻意不加进 REQUIRED_CREDENTIAL_ENV_NAMES"，
理由是收消息与发群通知独立、不能连坐），服务本身仍在正常运行，只是这一次群通知发不出去。
运维看到"拒绝启动"字样会误判成服务整体宕了。

步骤：
1. 在 `tools/liaison/errors.py` 新增一个独立异常（例如 `GroupWebhookMissingError(RuntimeError)`），
   构造签名与 `MissingCredentialsError` 一致（`missing_names: Iterable[str]`，消息同样⛔ 只出现变量名不出现取值），
   但文案改为准确描述"群通知发送地址缺失，本次跳过发送"一类措辞，**不含"拒绝启动"字样**
2. `config.py:83` 的 `raise MissingCredentialsError([GROUP_WEBHOOK_ENV])` 改成 `raise GroupWebhookMissingError([GROUP_WEBHOOK_ENV])`
3. 调用方核对并同步：`tools/liaison/followup.py:216-217`（`except MissingCredentialsError` 那块，
   若捕获的正是 `load_group_webhook` 抛出的这个异常则要改成捕获新类型；若那里捕获的是别的调用点的
   `MissingCredentialsError` 则不动，只改群通知这条路径）、`tools/liaison/notify/webhook.py`（若有捕获此异常的逻辑同样核）
4. `tools/liaison/tests/test_notify_transport.py:333-366` 四处 `config.load_group_webhook(...)` 相关用例，
   涉及"抛异常"的断言改成断言新异常类型 `GroupWebhookMissingError`（⛔ 不要只改字符串匹配，要改类型）
5. 先红后绿：新增/改动的用例在改代码前跑必须红，改完再绿

【四、全量回归】`./venv/bin/python -m pytest tools/liaison/tests/ -q` 与仓库根 `./venv/bin/python -m pytest -q` 均需
0 失败，且总用例数 ≥ 改动前（收编与改名不得导致用例被误删）。

【五、收口】
1. `git add tools/liaison/tests/conftest.py tools/liaison/tests/test_notify_ratelimit.py tools/liaison/tests/test_notify_store.py tools/liaison/tests/test_notify_webhook.py tools/liaison/tests/test_notify_transport.py tools/liaison/errors.py tools/liaison/config.py tools/liaison/followup.py tools/liaison/notify/webhook.py docs/tech-debt.md`
   （最后一项：把 TD-29「仍欠两条」改成 `✅ 已还`，附本次 commit hash）
2. commit（一条即可），走 `finishing-a-development-branch` 合回 `main`
3. 合并后 `git rev-list --count main..<分支>`；非 0 时再跑 `git cherry -v main <分支>` 确认全 `-`（真合）
4. 确认内容真在 main 上之前 ⛔ 不许输出 `OPENER_DONE`

【六、并发协议（已逐字内嵌，本批只有一条泳道，仍照抄）】
1. 只 `git add` 本条明确列出的路径。⛔ 禁止 `git add -A` / `git add .` / `git commit -a` / `git stash`
2. `git status` 里出现别人的改动是正常的，不要停下、不要问、不要顺手提交
3. push 被拒 → `git pull --rebase --autostash origin main` 后重试，最多 3 次
4. 报 `.git/index.lock` 已存在 → 等 5 秒重试最多 5 次，⛔ 默认绝不删除该锁
```

> ✅ 已完成 2026-09-16（OK，commit 555592a；脚本判 NO-SENTINEL 系哨兵被加粗成 `**OPENER_DONE**`，Cowork 核验后手工摘）· 原泳道 token治理0916K
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Paul 在手机上，2026-09-16 由 Cowork 经 launchd 请求文件发车），名字另由 -n 给。🔴 若改为手工贴进 CC Desktop，必须自行补上第 3 行。

```
[Mac]0916K-Token治理-泳道开工整读大文件与提醒误停根治
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（改项目 hook、执行器与共享文档，要在主检出一次改完一次提交）｜ 工作区: 仓库根 ｜ 派发: Cowork·Token治理-0916

读 docs/openers/0916K-Token治理-泳道开工整读大文件与提醒误停根治.md 全文并逐节执行（该文件按分段读，每次 Read 带 offset＋limit ≤ 400 行），以该文件为准。文件不存在 → 输出 OPENER_PARTIAL: 正文文件不存在 并停。
无头补充（覆盖正文里对应说法）：
1. 正文【一】3 的 git status 预期里另有 `M docs/openers/OP-0820-全量编排.md`（本块自身），属正常；【七】2 的 git add 列表追加 `docs/openers/OP-0820-全量编排.md`。
2. 本 session 就是由 run-lanes.sh 起的泳道：正文【一】2「pgrep run-lanes 为空否则停」**跳过**（会命中自己的父进程）。
3. ⛔ 不修改 `docs/openers/run-lanes.sh`（它正在执行本泳道）。若【二】2 的失败只能靠改 run-lanes.sh 解决 → 不改，登记「⏸ 留步：run-lanes.sh 需改，待非泳道会话处理」，其余段照做，最后 OPENER_PARTIAL。
4. 凡正文写「停下报我」，一律改为：登记原因、跳过该段后面依赖它的步骤、最终输出 OPENER_PARTIAL: <原因>，⛔ 不提问。
5. 收工报告写进 `.claude/handoff/0916K-收工报告.md`（正文【八】各项），并在终端输出里同样给出。
```

> ✅ 已完成 2026-09-16（OK）· 原泳道 token治理0916L（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Cowork 经 launchd 请求文件发车），名字另由 -n 给。

```
[Mac]0916L-哨兵加粗容错与0916K摘标注提交
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（只提交 Cowork 已写好的两处小改动）｜ 工作区: 仓库根 ｜ 派发: Cowork·Token治理-0916

背景：0916K 活已干完并提交（555592a），但哨兵输出成 `**OPENER_DONE**` 被判 NO-SENTINEL、泳道标注没摘，下一批会重跑它。Cowork 已改好（未提交）：run-lanes.sh 哨兵正则容忍加粗／反引号；tests/test_run_lanes_model.py 加一条用例；OP-0820 把 0916K 标注换成已完成行，并追加本块。
1. `git pull --rebase --autostash origin main`；`git status --short` 本件相关应只有：M docs/openers/run-lanes.sh ｜ M tests/test_run_lanes_model.py ｜ M docs/openers/OP-0820-全量编排.md ｜ M docs/openers/号池台账.md
2. `bash -n docs/openers/run-lanes.sh && ./venv/bin/python -m pytest -q tests/test_run_lanes_model.py 2>&1 | tail -2` → 0 失败；失败 → 不提交，输出 OPENER_PARTIAL: 测试失败 <末行>
3. ⛔ 不修改 run-lanes.sh（它正在执行本泳道），只提交
4. git add docs/openers/run-lanes.sh tests/test_run_lanes_model.py docs/openers/OP-0820-全量编排.md docs/openers/号池台账.md
5. git commit -m "fix(run-lanes): 哨兵容忍加粗/反引号 ＋ 摘 0916K 标注（0916L）"，按并发协议 push
6. 最后一行顶格输出 OPENER_DONE（不加粗、不加任何符号）
```

## 语音面试包 U2 prep 出题引擎计划已就绪（0918AH），启动 run-build（2026-09-18 task-dispatcher·lanes-done-20260918-180916；`### Task` 计 9 个（Task 0–8）≥5 且计划无「## 建议拆段点」节，按 rules.md §2 兜底每 3 个一段：seg1=位置1–3=文档序 Task 0/1/2，seg2=位置4–6=Task 3/4/5，seg3=位置7–9=Task 6/7/8——⚠️ 本计划 Task 编号从 0 起，台账自动生成的段标题「Task 1–3」等是**位置序**不是字面 Task 编号，三条块正文按文档实际 Task 编号写死，⛔ 不要按台账标题字面理解；触碰区 `app/llm/gateway.py`／`app/audit/hook.py`／`app/schemas/job_profile.py`／`app/storage/db.py`／`app/agents/interview_prep.py`／`app/graph/interview_prep_nodes.py`／`app/web/server.py`／`app/web/static/interview_prep_review.html`／对应测试文件，仓库此刻无其它泳道在跑（`pgrep run-lanes` 为空），零冲突)

> ✅ 已完成 2026-09-18（OK）· 原泳道 语音面试包U2建造（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动，print 模式下那个 MCP 工具未必挂载，名字另由 -n 给。🔴 若改为手工贴进 CC Desktop，必须自行补上第 3 行。

```
[Mac]0918AJ-语音面试包U2建造Task0-2
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: lane-voice-interview-u2-prep-question-engine ｜ worktree: ✅ 勾 ｜ 工作区: .claude/worktrees/lane-voice-interview-u2-prep-question-engine ｜ 派发: task-dispatcher·lanes-done-20260918-180916

无人值守：⛔ 不提问。计划：`docs/superpowers/plans/2026-09-18-voice-structured-interview-unit2-prep-question-engine.md`（含 Global Constraints 段，逐条已判适用性）。本条只做文档里的 **Task 0、Task 1、Task 2**（`### Task 0: 扩展 AuditHook 返回值，让调用方拿到写入的 analysis_run.id` → `### Task 1: rubric 维度推导——derive_rubric_dimensions()` → `### Task 2: 岗位级配置——新表 job_prep_config`），⛔ 不做 final review、⛔ 不合 main、⛔ 不删 worktree、⛔ 不回勾 tasks.md。

用 `superpowers:subagent-driven-development` 跑该计划，只推进到文档 Task 2 完成即停（进度台账 `.superpowers/sdd/2026-09-18-voice-structured-interview-unit2-prep-question-engine/progress.md` 会记录已完成 Task，下一条从这里续）。`Skill(superpowers:…)` 回 `Unknown skill` ⇒ 登记「⏸ 留步：superpowers 不可达」并停，⛔ 不手工按磁盘 SKILL.md 走。

worktree 建好后按 run-build 惯例补齐 `.env`（gitignore 未带过去）与本地 venv（`ModuleNotFoundError` 即缺这步）。每个 Task 独立提交在本分支，⛔ 不跨 Task 合并提交。涉及文件：Task 0 `app/llm/gateway.py`／`app/audit/hook.py`／`tests/test_audit_hook.py`／`tests/test_llm_gateway.py`；Task 1 `app/schemas/job_profile.py`／`tests/test_job_profile_schema.py`；Task 2 `app/storage/db.py`／`tests/test_db_m3_schema.py`。

【并发协议】
1. 只 `git add` 本条产出的文件；⛔ 禁止 `git add -A` / `git add .` / `git commit -a` / `git stash`
2. `git status` 里出现别人的改动是正常的，不要停下、不要问、不要顺手提交
3. push 被拒 → `git pull --rebase --autostash origin main` 后重试，最多 3 次（本分支只推自己，正常不会被拒）
4. 报 `.git/index.lock` 已存在 → 等 5 秒重试最多 5 次，⛔ 默认绝不删除该锁

【收口】本条不合并，只提交在 `lane-voice-interview-u2-prep-question-engine` 分支上。收工两行：Task 0/1/2 各自 commit｜本分支范围内 pytest passed/failed 数。最后一行顶格输出 `OPENER_DONE` 或 `OPENER_PARTIAL: <原因>`。
```

> ✅ 已完成 2026-09-18（OK）· 原泳道 语音面试包U2建造（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动，print 模式下那个 MCP 工具未必挂载，名字另由 -n 给。🔴 若改为手工贴进 CC Desktop，必须自行补上第 3 行。

```
[Mac]0918AK-语音面试包U2建造Task3-5
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: lane-voice-interview-u2-prep-question-engine ｜ worktree: ✅ 勾 ｜ 工作区: .claude/worktrees/lane-voice-interview-u2-prep-question-engine ｜ 派发: task-dispatcher·lanes-done-20260918-180916

无人值守：⛔ 不提问。前置：0918AJ 已在 `lane-voice-interview-u2-prep-question-engine` 分支提交 Task 0/1/2（核验＝`git log lane-voice-interview-u2-prep-question-engine --oneline` 能看到对应提交；看不到 ⇒ 登记「⏸ 留步：前置 0918AJ 未完成」并停，⛔ 不代跑 Task 0–2）。

计划同上（`docs/superpowers/plans/2026-09-18-voice-structured-interview-unit2-prep-question-engine.md`）。本条只做文档里的 **Task 3、Task 4、Task 5**（`### Task 3: L3 Agent——app/agents/interview_prep.py::generate()/regenerate_one()` → `### Task 4: L4 编排层——app/graph/interview_prep_nodes.py` → `### Task 5: 登记 5 个新 effect 节点——EFFECT_NODE_MANIFEST + 崩溃-恢复配方`），⛔ 不做 final review、⛔ 不合 main、⛔ 不删 worktree、⛔ 不回勾 tasks.md。

用 `superpowers:subagent-driven-development` 从进度台账续跑到文档 Task 5 完成即停。`Skill(superpowers:…)` 回 `Unknown skill` ⇒ 登记「⏸ 留步：superpowers 不可达」并停。涉及文件：Task 3 `app/agents/interview_prep.py`／`tests/test_interview_prep_agent.py`；Task 4 `app/graph/interview_prep_nodes.py`／`tests/test_interview_prep_nodes.py`；Task 5 `tests/test_effect_idempotency_suite.py`（登记进 `EFFECT_NODE_MANIFEST`，在 `app/graph/build.py`）。

【并发协议】同 0918AJ 四条（只 add 本条文件；别人改动不停手；push 被拒重试 3 次；index.lock 等重试 5 次不删）。

【收口】本条不合并，只提交在本分支上。收工两行：Task 3/4/5 各自 commit｜本分支范围内 pytest passed/failed 数。最后一行顶格输出 `OPENER_DONE` 或 `OPENER_PARTIAL: <原因>`。
```

> ✅ 已完成 2026-09-18（OK）· 原泳道 语音面试包U2建造（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动，print 模式下那个 MCP 工具未必挂载，名字另由 -n 给。🔴 若改为手工贴进 CC Desktop，必须自行补上第 3 行。

```
[Mac]0918AL-语音面试包U2建造Task6-8与收口
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: lane-voice-interview-u2-prep-question-engine ｜ worktree: ✅ 勾 ｜ 工作区: .claude/worktrees/lane-voice-interview-u2-prep-question-engine ｜ 派发: task-dispatcher·lanes-done-20260918-180916

无人值守：⛔ 不提问。前置：0918AK 已在 `lane-voice-interview-u2-prep-question-engine` 分支提交 Task 3/4/5（核验＝`git log lane-voice-interview-u2-prep-question-engine --oneline` 能看到对应提交；看不到 ⇒ 登记「⏸ 留步：前置 0918AK 未完成」并停）。

计划同上。本条做文档里的 **Task 6、Task 7、Task 8**（`### Task 6: Web 端点——生成/查看/改/删/重生成/冻结` → `### Task 7: 确认页静态 HTML——interview_prep_review.html` → `### Task 8: prep e2e 测试（tasks 3.8）`，涉及文件 `app/web/server.py`／`tests/test_prep_endpoints.py`／`app/web/static/interview_prep_review.html`／`tests/test_prep_e2e.py`）＋ 全分支收口（run-build 原收口动作全在本条）：

1. `superpowers:subagent-driven-development` 跑完 Task 8
2. 全分支 final review 通过后，回到 `openspec/changes/voice-structured-interview/tasks.md`，把第 3 章 3.1–3.8 的 `- [ ]` 改成 `- [x]`（只有 final review 通过才勾）
3. 本单元不产生需搬回主工作区的 `data/` 一次性产物，跳过搬运步骤
4. 调用 `superpowers:finishing-a-development-branch` 合并
5. 验证合并真的发生：`git rev-list --count main..lane-voice-interview-u2-prep-question-engine` 必须是 `0`；不是 0 ⇒ 再跑 `git cherry -v main lane-voice-interview-u2-prep-question-engine`，全 `-` 是 rebase 假阳性（删旧分支即可），有 `+` 才是真没合（回去补第 4 步）
6. 清理 worktree

【并发协议】同 0918AJ 四条。

【收口】收工四行：合入 commit｜全量 pytest passed/failed 数｜`rev-list --count main..lane-voice-interview-u2-prep-question-engine` 结果｜3.1–3.8 回勾状态。最后一行顶格输出 `OPENER_DONE` 或 `OPENER_PARTIAL: <原因>`。
```

## M2 可见薄片发版到 `.51`（2026-09-18 19:3x Cowork·HR业务线-接力0918G 派；G3 已放行 `Q-44`「发」，Shao Peishen 切审批模式后授权执行）

> 泳道：M2发版
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动，名字另由 -n 给。

```
[Mac]0918AM-M2可见薄片发版51
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: main ｜ worktree: ❌ 不勾（发版操作在主检出，不写业务代码）｜ 工作区: 仓库根 ｜ 派发: Cowork·HR业务线-接力0918G ｜ 模型: Sonnet

无人值守：⛔ 不提问。凡遇需 Shao Peishen 裁决的分支，⛔ 不猜、不代答，按正文的判据停并如实登记。

🔴 铁律：⛔ 不得把任务交给后台子代理之后结束回合——子代理一律前台等待到返回；哨兵必须由本会话自己顶格打印（`0918O` 正是栽在这里）。

正文见 `docs/openers/0918AM-M2可见薄片发版51.md`，**逐节照做**（零、授权 → 一、自核①②④ → 二、锁定发版 commit 并申报携带范围 → 三、按发版清单执行 → 四、红线 → 五、收口）。

🔴 三条最重要的：① 开工先自核预授权条件，任一不成立立刻停、⛔ 不发版、⛔ 不顺手修；② 任一阶段不过即按清单回滚到快照点并停链，⛔ 不重试、⛔ 不跳阶段；③ 真实简历入库闸发版前后各核验一次，全程保持关闭，⛔ 任何情况下不得在本条里打开。

最后一行由本会话自己顶格输出 `OPENER_DONE` 或 `OPENER_PARTIAL: <原因>`。
```

## 语音面试包 U2 已合 main，启动 U3 出实现计划（2026-09-18 task-dispatcher·lanes-done-20260918-182246；U2 全 8 条（3.1–3.8）真身核验完成（tasks.md 已勾 `[x]`）＋已合 main（`67772ba`），G2 已放行，前置齐；触碰区 `docs/superpowers/plans/*-voice-structured-interview-unit3-*.md`，与当前在跑批次（M2发版 0918AM：`.51` 服务器发版操作）零重叠，可并行）

> 泳道：语音面试包U3出计划
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动，print 模式下那个 MCP 工具未必挂载，名字另由 -n 给。🔴 若改为手工贴进 CC Desktop，必须自行补上第 3 行。

```
[Mac]0918AN-语音面试包U3邀约同意实现计划
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: main ｜ worktree: ❌ 不勾（spec-to-plan 只产出一份 markdown 计划，不写代码不建分支）｜ 工作区: 仓库根 ｜ 派发: task-dispatcher·lanes-done-20260918-182246

无人值守：⛔ 不提问。凡遇需 Shao Peishen 裁决的分支，按预案处理并在计划里如实记录待定点，⛔ 不停下等答复。

用 spec-to-plan 技能，为变更包 voice-structured-interview 的交付单元「第 4 章 U3 邀约与同意流程（一次性链接 ＋ 门禁接线 ＋ 双同意 ＋ 验证码）」出一份实现计划，并提交。

先读这四份：
- openspec/changes/voice-structured-interview/tasks.md 第 4 章（4.1–4.11，U3 范围）
- openspec/changes/voice-structured-interview/specs/interview-invite-and-consent/spec.md
- openspec/changes/voice-structured-interview/design.md 决策 D5（一次性链接＋双同意）、D6（邀约只经 `deliver_candidate_message()`）、D13（一期身份核验＝手机号验证码，`identity_check.result` 恒 `skipped`）
- app/storage/db.py 已有的 `interview_session`／`interview_consent`／`identity_check` 建表（U1 已交付，本单元只接线不改表结构）

单元范围 = tasks.md 第 4 章（4.1–4.11）：令牌签发与校验（`effect_issue_invite`）／续入令牌／邀约投递（`effect_deliver_invitation`，只经 `deliver_candidate_message()`，反证测试代码里不得 `import channel.deliver`）／同意条款文件与同意页（两个独立勾选、两条独立 `interview_consent` 记录）／验证码（6 位、5 分钟、错 5 次锁场次）／验证码送达（短信通道未配置时 HR 人工转告）／真实候选人开闸（`Settings.live_interview_enabled` 默认 `False`）／手机号来源接线（`live` 场次经 `candidate-contact-vault`）／HR 签发页与 e2e。

⚠️ 4.5「同意条款文件占位文本」、4.8「短信通道未配置」、4.9「真实候选人开闸默认 `False`」均已在 tasks.md 原文写明降级路径——出计划按降级路径（占位文本／HR 人工转告／开关默认关）规划，⛔ 不要因 OQ-6（法务合规验收 #2）与 OQ-10（短信通道采购）未决而认为本单元不能出计划；这两项是**运行时启用开关**，不阻塞写代码与测试。

Global Constraints：从 CLAUDE.md「工程铁律」与「合规红线」两节逐字复制与本单元相关的条目（尤其铁律 1 幂等键与同事务、铁律 6 企微回调落库即返回、合规红线「候选人入口一律一次性邀请链接」「禁止人脸/表情分析」「AI 生成的邀约须带标识」），原样写进计划的 Global Constraints 段；某条本单元不适用也要在段内写明「不适用，理由」，⛔ 不能整段省略。

`superpowers:writing-plans` 不可达 ⇒ 登记「⏸ 留步：superpowers 不可达」并停，⛔ 不手工按磁盘 SKILL.md 走。

【并发协议】
1. 只 `git add` 本条产出的计划文件本身；⛔ 禁止 `git add -A` / `git add .` / `git commit -a` / `git stash`
2. `git status` 里出现别人的改动是正常的，不要停下、不要问、不要顺手提交
3. push 被拒 → `git pull --rebase --autostash origin main` 后重试，最多 3 次
4. 报 `.git/index.lock` 已存在 → 等 5 秒重试最多 5 次，⛔ 默认绝不删除该锁

【收口】本条不合并，只提交计划文件本身。收工两行：计划文件路径｜`grep -c '^### Task '` 结果与 Task 数是否 ≥5（含拆段点判断）。最后一行顶格输出 `OPENER_DONE` 或 `OPENER_PARTIAL: <原因>`。
```
