# OP-0820 · 全量 Opener 编排

> 2026-08-20。**唯一权威 opener 清单**，`OP-0820-A_单元BC开工.md` 已作废。
>
> **编排原则**：不依赖人工控制执行节奏。有依赖就彻底串行，能并发的全部并行。
> 并发安全靠《并发协议》做到机器自愈，**不靠"你记得别同时开"**。
>
> **格式**：每个代码块头两行是标题行 + 【设置】行，整块复制即可，不要拆。

---

## 🔢 号池台账 → 已迁至 `docs/openers/号池台账.md`

> 〔归档工具〕 2026-09-16：出号前查、给出后登记，一律改在该文件；历史月份的行在 `docs/openers/归档/号池台账-归档.md`。

## 二、并发协议（**每条 opener 内已逐字内嵌，此处备查**）

1. **只 `git add` 本条 opener 明确列出的路径。** ⛔ 禁止 `git add -A` / `git add .` / `git commit -a`。这是整套并行编排成立的唯一前提。
2. **`git status` 里出现别人的改动是正常的**——那是并行的另一条 opener 在干活。不要停下、不要问、不要顺手提交。
3. ⛔ 不要在 commit 前 pull（并发下会被别人的未提交改动挡住）；push 被拒才 `git pull --rebase --autostash origin main` 重试。
4. push 被拒（non-fast-forward）→ 再 `git pull --rebase origin main` 后重试，**最多 3 次**，仍失败停下贴错误。
5. 报 `.git/index.lock: File exists` → **等 5 秒重试，最多 5 次**。⛔ **绝不删除该锁文件**——那是另一个 session 正在用，删了会毁掉它的提交。

---

## 待执行区

> 〔归档工具〕 2026-09-16：`lane-dispatch` 新写的无头 opener 放在本节之后（`> 泳道：<名>` ＋ 代码块）。
> 〔归档工具〕 跑完被 `mark_done` 摘标注的批次，下次 `python3 scripts/archive_docs.py --apply` 会整段搬去归档。

> ✅ 已完成 2026-09-16（OK）· 原泳道 liaison测试脚手架收尾（脚本自动摘，不再进 run-lanes）

```
[Mac]0916E-TD29收尾liaison测试脚手架收编与config文案
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ✅ 勾（写代码）｜ 工作区: worktree（`.claude/worktrees/td29-liaison-test-scaffolding-0916E`）｜ 派发: run-lanes.sh

【零、背景与授权】TD-29 剩余两条，Shao Peishen 2026-09-10 已答 `4a`：**下一批泳道单独派一条**，独占
`tools/liaison/tests/` 与 `tools/liaison/config.py`，⛔ 不挂 8.6 灰度前置、⛔ 不留着不做。全文见
`docs/tech-debt.md` TD-29 条目「仍欠两条」。这是**纯重构 + 文案修正，不改变外部可观察行为**，按
CLAUDE.md「轻量通道」走：⛔ 不用 `spec-to-plan`/`run-build`，直接 TDD 单条改完提交
（`superpowers:test-driven-development`；调不到 ⇒ 登记「⏸ 留步：superpowers 不可达」并停，⛔ 不手工按磁盘 SKILL.md 走）。

【一、前置自检】
1. `pwd` 必须含 `.claude/worktrees/`，否则停下报
2. `git log --oneline -5 -- tools/liaison/tests/test_notify_store.py | grep -c 'assert_group_notify_identity'` 之类的旧顾虑已随
   `5ca1f50` 处置，本条不需要再验；只需确认 `grep -n 'assert_group_notify_identity' tools/liaison/tests/test_notify_store.py` 命中 ≥2 处（存在即可，⛔ 不是本条任务）

【二、任务 A · conftest 收编（三处复制粘贴）】
现状（已核实的确切行号，实现前重新 grep 一遍确认未漂移）：
- `FakeClock`：`tools/liaison/tests/test_notify_ratelimit.py:14` 与 `tools/liaison/tests/test_notify_webhook.py:63` 两份定义，逐字节比对是否相同
- `RecordingSink`：`tools/liaison/tests/test_notify_store.py:29` 与 `tools/liaison/tests/test_notify_webhook.py:76` 两份
- `FAKE_WEBHOOK`：`tools/liaison/tests/test_notify_transport.py:25` 与 `tools/liaison/tests/test_notify_webhook.py:15` 两份（值＝`https://example.invalid/cgi-bin/webhook/send?key=fake-key-for-tests`，⛔ 改动中不得把这个假值换成真实域名）

步骤：
1. 若两份定义逐字节不同，先登记差异、以更完整/更新的那份为准，⛔ 不擅自改逻辑
2. 三者收进 `tools/liaison/tests/conftest.py`，以 pytest fixture 或模块级常量形式导出（`FakeClock`/`RecordingSink` 用 `@pytest.fixture` 更符合 pytest 惯例，但若两处原调用点是直接实例化类而非注入，保持调用点最小改动优先——不确定就选影响面小的那种）
3. 四个原文件改为从 `conftest.py`（同目录自动可见，无需显式 import fixture；若是模块级常量/类则需要 `from .conftest import ...` 或按 pytest 惯例处理）里取用，删除本地重复定义
4. 跑 `tools/liaison/tests/` 全量，四份原文件涉及的用例必须逐条仍过，且总数不减少（收编不是删测试）

【三、任务 B · config.py:75 异常文案错位】
现状：`tools/liaison/config.py:70-83` 的 `load_group_webhook` 复用 `tools/liaison/errors.py` 的
`MissingCredentialsError`，其消息固定「HR 值守通道**拒绝启动**：…」（`errors.py:19`）。但
`load_group_webhook` 是**发送期**校验（docstring 已明写"⛔ 刻意不加进 REQUIRED_CREDENTIAL_ENV_NAMES"，
理由是收消息与发群通知独立、不能连坐），服务本身仍在正常运行，只是这一次群通知发不出去。
运维看到"拒绝启动"字样会误判成服务整体宕了。

步骤：
1. 在 `tools/liaison/errors.py` 新增一个独立异常（例如 `GroupWebhookMissingError(RuntimeError)`），
   构造签名与 `MissingCredentialsError` 一致（`missing_names: Iterable[str]`，消息同样⛔ 只出现变量名不出现取值），
   但文案改为准确描述"群通知发送地址缺失，本次跳过发送"一类措辞，**不含"拒绝启动"字样**
2. `config.py:83` 的 `raise MissingCredentialsError([GROUP_WEBHOOK_ENV])` 改成 `raise GroupWebhookMissingError([GROUP_WEBHOOK_ENV])`
3. 调用方核对并同步：`tools/liaison/followup.py:216-217`（`except MissingCredentialsError` 那块，
   若捕获的正是 `load_group_webhook` 抛出的这个异常则要改成捕获新类型；若那里捕获的是别的调用点的
   `MissingCredentialsError` 则不动，只改群通知这条路径）、`tools/liaison/notify/webhook.py`（若有捕获此异常的逻辑同样核）
4. `tools/liaison/tests/test_notify_transport.py:333-366` 四处 `config.load_group_webhook(...)` 相关用例，
   涉及"抛异常"的断言改成断言新异常类型 `GroupWebhookMissingError`（⛔ 不要只改字符串匹配，要改类型）
5. 先红后绿：新增/改动的用例在改代码前跑必须红，改完再绿

【四、全量回归】`./venv/bin/python -m pytest tools/liaison/tests/ -q` 与仓库根 `./venv/bin/python -m pytest -q` 均需
0 失败，且总用例数 ≥ 改动前（收编与改名不得导致用例被误删）。

【五、收口】
1. `git add tools/liaison/tests/conftest.py tools/liaison/tests/test_notify_ratelimit.py tools/liaison/tests/test_notify_store.py tools/liaison/tests/test_notify_webhook.py tools/liaison/tests/test_notify_transport.py tools/liaison/errors.py tools/liaison/config.py tools/liaison/followup.py tools/liaison/notify/webhook.py docs/tech-debt.md`
   （最后一项：把 TD-29「仍欠两条」改成 `✅ 已还`，附本次 commit hash）
2. commit（一条即可），走 `finishing-a-development-branch` 合回 `main`
3. 合并后 `git rev-list --count main..<分支>`；非 0 时再跑 `git cherry -v main <分支>` 确认全 `-`（真合）
4. 确认内容真在 main 上之前 ⛔ 不许输出 `OPENER_DONE`

【六、并发协议（已逐字内嵌，本批只有一条泳道，仍照抄）】
1. 只 `git add` 本条明确列出的路径。⛔ 禁止 `git add -A` / `git add .` / `git commit -a` / `git stash`
2. `git status` 里出现别人的改动是正常的，不要停下、不要问、不要顺手提交
3. push 被拒 → `git pull --rebase --autostash origin main` 后重试，最多 3 次
4. 报 `.git/index.lock` 已存在 → 等 5 秒重试最多 5 次，⛔ 默认绝不删除该锁
```

> ✅ 已完成 2026-09-16（OK，commit 555592a；脚本判 NO-SENTINEL 系哨兵被加粗成 `**OPENER_DONE**`，Cowork 核验后手工摘）· 原泳道 token治理0916K
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Paul 在手机上，2026-09-16 由 Cowork 经 launchd 请求文件发车），名字另由 -n 给。🔴 若改为手工贴进 CC Desktop，必须自行补上第 3 行。

```
[Mac]0916K-Token治理-泳道开工整读大文件与提醒误停根治
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（改项目 hook、执行器与共享文档，要在主检出一次改完一次提交）｜ 工作区: 仓库根 ｜ 派发: Cowork·Token治理-0916

读 docs/openers/0916K-Token治理-泳道开工整读大文件与提醒误停根治.md 全文并逐节执行（该文件按分段读，每次 Read 带 offset＋limit ≤ 400 行），以该文件为准。文件不存在 → 输出 OPENER_PARTIAL: 正文文件不存在 并停。
无头补充（覆盖正文里对应说法）：
1. 正文【一】3 的 git status 预期里另有 `M docs/openers/OP-0820-全量编排.md`（本块自身），属正常；【七】2 的 git add 列表追加 `docs/openers/OP-0820-全量编排.md`。
2. 本 session 就是由 run-lanes.sh 起的泳道：正文【一】2「pgrep run-lanes 为空否则停」**跳过**（会命中自己的父进程）。
3. ⛔ 不修改 `docs/openers/run-lanes.sh`（它正在执行本泳道）。若【二】2 的失败只能靠改 run-lanes.sh 解决 → 不改，登记「⏸ 留步：run-lanes.sh 需改，待非泳道会话处理」，其余段照做，最后 OPENER_PARTIAL。
4. 凡正文写「停下报我」，一律改为：登记原因、跳过该段后面依赖它的步骤、最终输出 OPENER_PARTIAL: <原因>，⛔ 不提问。
5. 收工报告写进 `.claude/handoff/0916K-收工报告.md`（正文【八】各项），并在终端输出里同样给出。
```

> ✅ 已完成 2026-09-16（OK）· 原泳道 token治理0916L（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Cowork 经 launchd 请求文件发车），名字另由 -n 给。

```
[Mac]0916L-哨兵加粗容错与0916K摘标注提交
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（只提交 Cowork 已写好的两处小改动）｜ 工作区: 仓库根 ｜ 派发: Cowork·Token治理-0916

背景：0916K 活已干完并提交（555592a），但哨兵输出成 `**OPENER_DONE**` 被判 NO-SENTINEL、泳道标注没摘，下一批会重跑它。Cowork 已改好（未提交）：run-lanes.sh 哨兵正则容忍加粗／反引号；tests/test_run_lanes_model.py 加一条用例；OP-0820 把 0916K 标注换成已完成行，并追加本块。
1. `git pull --rebase --autostash origin main`；`git status --short` 本件相关应只有：M docs/openers/run-lanes.sh ｜ M tests/test_run_lanes_model.py ｜ M docs/openers/OP-0820-全量编排.md ｜ M docs/openers/号池台账.md
2. `bash -n docs/openers/run-lanes.sh && ./venv/bin/python -m pytest -q tests/test_run_lanes_model.py 2>&1 | tail -2` → 0 失败；失败 → 不提交，输出 OPENER_PARTIAL: 测试失败 <末行>
3. ⛔ 不修改 run-lanes.sh（它正在执行本泳道），只提交
4. git add docs/openers/run-lanes.sh tests/test_run_lanes_model.py docs/openers/OP-0820-全量编排.md docs/openers/号池台账.md
5. git commit -m "fix(run-lanes): 哨兵容忍加粗/反引号 ＋ 摘 0916K 标注（0916L）"，按并发协议 push
6. 最后一行顶格输出 OPENER_DONE（不加粗、不加任何符号）
```

## 第十七批·收尾（2026-09-17 Cowork·HR业务线-接力0917D 发车）

> 一条泳道两条串行：`0917E` 先把 Cowork 已写好的落档（体积闸归档 ＋ 本节 ＋ 号池两行 ＋ 接力 R-1 销账）提交到 main，`0917F` 才能从含这些提交的 main 建 worktree。⛔ 不可并行：`0917F` 的 worktree 基线必须含 `0917E` 的提交，否则全量 pytest 的 `test_doc_size_budget` 在旧基线上是红的（接力 61 KB > 58）。

> ✅ 已完成 2026-09-17（PARTIAL）· 原泳道 第十七批收尾（脚本自动摘，不再进 run-lanes）｜ ⚰️ 作废：E 撞 `docs/tech-debt.md` 体积闸（51.5 KB > 50，Cowork 起草时漏核这一项）未提交；F 因前置②（无 E 提交）正确停下、零改动。由下方 `0917G`→`0917H` 重发
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Cowork 经 launchd 请求文件发车），名字另由 -n 给。

```
[Mac]0917E-体积闸归档与收尾编排落档提交
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: main ｜ worktree: ❌ 不勾（只提交 Cowork 已写好的文档改动）｜ 工作区: 仓库根 ｜ 派发: Cowork·HR业务线-接力0917D

背景：Cowork·0917D 发现 `docs/session接力.md` 61 KB 超体积闸（58）、`OP-0820-全量编排.md` 65 KB 超闸（60）⇒ `tests/test_doc_size_budget.py` 红、看护者不发车。已按闸的唯一处置做完：给 5 段已闭环标题加「【已闭环】」后跑 `python3 scripts/archive_docs.py --apply`（守恒校验通过：接力 61→41 KB、编排 65→12 KB）；另追加本节两块、号池两行、接力【下一步】R-1 销账与 R-2 指向 `0917F`。全部未提交。
1. `git status --short` 本件相关应只有（多出别的不管、⛔ 不 add）：M docs/session接力.md ｜ M docs/archive/session接力-归档.md ｜ M docs/openers/OP-0820-全量编排.md ｜ M docs/openers/归档/OP-0820-历史批次.md ｜ M docs/openers/号池台账.md。缺任何一个 ⇒ 输出 OPENER_PARTIAL: 落档文件缺 <名> 并停
2. `./venv/bin/python -m pytest -q tests/test_doc_size_budget.py tests/test_caretaker_openers.py 2>&1 | tail -2` → 0 失败；失败 ⇒ 不提交，OPENER_PARTIAL: 体积闸仍红 <末行>
3. `grep -c '【已闭环】' docs/session接力.md` 应为 0（已被搬走的段不应残留标记）；非 0 ⇒ OPENER_PARTIAL
4. `git add docs/session接力.md docs/archive/session接力-归档.md docs/openers/OP-0820-全量编排.md docs/openers/归档/OP-0820-历史批次.md docs/openers/号池台账.md`（⛔ 禁 `git add -A`／`.`／`commit -a`／`stash`；⛔ 不 add `_to_delete/`）
5. `git commit -m "docs(relay): 体积闸归档 ＋ 第十七批收尾编排 0917E/0917F 落档（0917E）"`；push 被拒 ⇒ `git pull --rebase --autostash origin main` 后重试最多 3 次；`.git/index.lock` 已存在 ⇒ 等 5 秒重试最多 5 次，⛔ 不删锁
6. `git rev-list --left-right --count HEAD...@{u}` 应为 `0 0`
7. 最后一行顶格输出 OPENER_DONE（不加粗、不加任何符号）
```

> ✅ 已完成 2026-09-17（PARTIAL）· 原泳道 第十七批收尾（脚本自动摘，不再进 run-lanes）｜ ⚰️ 作废：E 撞 `docs/tech-debt.md` 体积闸（51.5 KB > 50，Cowork 起草时漏核这一项）未提交；F 因前置②（无 E 提交）正确停下、零改动。由下方 `0917G`→`0917H` 重发
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Cowork 经 launchd 请求文件发车），名字另由 -n 给。

```
[Mac]0917F-P2章程接入生产路径3点2收口
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: lane-0917f-charter-wiring ｜ worktree: ✅ 勾（改产品代码）｜ 工作区: .claude/worktrees/lane-0917f-charter-wiring ｜ 派发: Cowork·HR业务线-接力0917D

【零、范围与流程口径】`liaison-reply-bridge-and-patrol` tasks.md **3.2** 的最后一公里：`unpack/charter.py` 代码已交付、两轮 review 过，但**没接进生产路径**——`__main__.py` 持有 `CHARTER_RELPATH` 字面量、`dispatch_wiring.py` 持有 `_read_charter_text`／`_build_minimal_prompt` 副本（两处都带 `P2-TODO` 注释），spec `liaison-unpack-charter`「该路径只在一处常量定义」现为假。本条补已批准 spec 与代码之间的缺口，⛔ 不动任何 spec 文本 ⇒ 按 `03-工具链协作规则.md`「轻量通道」第 4 条走**单条直接 TDD**，⛔ 不走 spec-to-plan／run-build。拆分豁免：单条 TDD、无 plan 文件、改动集中在两个模块的一处调用链，拆开反而要跨会话传中间态。
开工后若发现**必须改 spec 文本** ⇒ 输出 OPENER_PARTIAL: 需改 spec <哪条> 并停，⛔ 不自行扩大范围。

【一、前置——任一不过即输出 OPENER_PARTIAL: <原因> 并停，⛔ 不提问】
1. `pwd` 含 `.claude/worktrees/lane-0917f-charter-wiring`；`git branch --show-current` ＝ `lane-0917f-charter-wiring`
2. `git log --oneline -n 30 | grep -c '0917E'` ≥ 1（基线含体积闸归档提交）
3. `grep -n '^- \[ \] 3\.2' openspec/changes/liaison-reply-bridge-and-patrol/tasks.md` 命中（未勾）；`grep -c '^- \[x\]' openspec/changes/liaison-reply-bridge-and-patrol/tasks.md` ＝ 27
4. `grep -rn 'P2-TODO' tools/liaison/__main__.py tools/liaison/unpack/dispatch_wiring.py | wc -l` ≥ 2（已有人先做掉 ⇒ OPENER_PARTIAL: 前提失效）
5. `Skill(superpowers:test-driven-development)` 可达；回 `Unknown skill` ⇒ 登记「⏸ 留步：superpowers 不可达」并 OPENER_PARTIAL，⛔ 不按磁盘 SKILL.md 手工走、⛔ 不自己装插件
6. 测试解释器：worktree 里没有 venv 与 `tools/liaison/.venv`（gitignore），⛔ 不新建。一律 `PYTHONPATH=. /Users/paulshao/Projects/HumanResource/venv/bin/python -m pytest <路径> -q`；⛔ 测试里不许 `import aibot`
7. 改前基线：`tools/liaison/tests` 与全量 `pytest -q` 各跑一次，记下 passed 数

【二、要做什么（先红后绿）】
A. **`dispatch_wiring.bridge_dispatch` 改用 `charter` 模块**
   - 删 `_read_charter_text`、`_build_minimal_prompt` 与模块 docstring 里的 P2-TODO 段；`from tools.liaison.unpack import charter`
   - 读章程：`charter.read_charter(REPO_ROOT)`，捕获 `charter.CharterMissing` ⇒ 沿用现有降级语义（`charter_text=None` 交给 `dispatch_headless_unpack`，由它判 `failed(charter_missing)`），⛔ 不改三态审计口径
   - 拼 prompt：`charter.compute_prompt(letter_number=…, msgid=…, signal_relpath=…, checkpoint_iso=…, charter_text=…)`
     · `signal_relpath`：信号文件相对仓库根的路径，取 `unpack_cli.py` 的 `DEFAULT_SIGNAL_PATH`（尊重 `HR_LIAISON_SIGNAL_PATH` 环境变量的那套解析，照 unpack_cli 现有函数取，⛔ 不另写字面量）再 `relative_to(REPO_ROOT)`；不在仓库根下 ⇒ 用绝对路径字符串
     · `checkpoint_iso`：`now` 的 ISO 8601（带时区）
     · `letter_number`：`__main__.py` 绑的是 `None`（P0 零参 `dispatch()` 契约拿不到，已知缺口）。`bridge._emit_signal_and_dispatch` 在调 `dispatch()` **之前**已 `append_signal`，信号项带 `letter_number` ⇒ 当入参为 `None` 时，从信号文件里按 `msgid` 取该项的 `letter_number`（读逻辑复用 `unpack/signal.py`，必要时加一个纯读函数 `find_pending(path, msgid)`，⛔ 不改写入逻辑）；仍取不到 ⇒ 写 `"（未匹配）"`，⛔ 不抛异常、⛔ 不阻断起活
   - 签名：删除 `charter_relpath`／`charter_root` 两个参数（唯一来源改为 `charter.CHARTER_RELATIVE_PATH`）；测试需要替换章程时改注入 `repo_root: Path = REPO_ROOT` 一个参数
B. **`__main__.py`**：删 `CHARTER_RELPATH` 常量及其 P2-TODO 注释；`functools.partial(dispatch_wiring.bridge_dispatch, …)` 去掉那两个关键字。`tools/liaison/tests/test_inbound_wiring.py:656-657` 两条断言改为：partial 的 kwargs **不含** `charter_relpath`／`charter_root`
C. **单点常量机器闸（新测试）**：扫 `tools/liaison/**/*.py`（排除 `tests/` 与 `unpack/charter.py` 本身），`.claude/skills/liaison-unpack/SKILL.md` 这个字面量**零命中**。先红（改前应命中 `__main__.py`）后绿
D. **prompt 字段测试**：`bridge_dispatch` 起活时传给 `dispatch_headless_unpack` 的 `prompt` 以 `# 拆件会话起活` 开头、含 msgid／信号文件路径／检查点时刻，信号文件里有该 msgid 时含其 `letter_number`，且以章程全文逐字结尾（替身 `dispatch_headless_unpack`，⛔ 不起真进程）
E. **顺手还 TD-45**（`docs/tech-debt.md`，触发条件正是「碰 SKILL.md 或 dispatch 白名单/接线时顺手改」）：`.claude/skills/liaison-unpack/SKILL.md` §二.6 与 `unpack/dispatch.py` 白名单里的裸 `python -m tools.liaison criteria` 一律改成 `PYTHONPATH=. tools/liaison/.venv/bin/python -m tools.liaison criteria` 前缀形式（与 `unpack-signal` 同口径）；`test_unpack_charter_allowlist.py` 必须仍绿；TD-45 标销账并写 commit
F. **顺手一个词**：`openspec/changes/liaison-reply-bridge-and-patrol/design.md` D4 的 `HEADLESS_ARGV_TEMPLATE` → `HEADLESS_ARGV_FIXED_PART`（代码真名）。这是文档对齐代码，不是改 spec 行为

🔴 工程铁律 2：`compute_*` 纯函数不记日志、不读时钟、不读文件；读信号文件属副作用，放在 `bridge_dispatch` 里、⛔ 不塞进 `compute_prompt`。

【三、验收】
1. C、D 先红后绿，报告写红时的报错原文
2. `tools/liaison/tests` 全绿且 passed 数 ≥ 基线；全量 `pytest -q` 0 失败（报前后两个数）
3. `grep -rn 'P2-TODO\|CHARTER_RELPATH\|_build_minimal_prompt\|_read_charter_text' tools/liaison --include=*.py | grep -v '/tests/'` 零命中
4. `openspec validate liaison-reply-bridge-and-patrol --strict` 通过
5. ⛔ 不做真实起活、⛔ 不重启／kickstart 值守服务 `com.zhuopin.hr.liaison`、⛔ 不碰 `data/liaison.db`／`.env`／`_to_delete/`。报告明写「未做真实起活，归 §5，Shao Peishen 本人」

【四、落档（都在 worktree 里改并提交在分支上）】
- `tasks.md`：勾 3.2，删掉其行尾「不勾——…」括注，抬头 `27/33` → `28/33`、「⚠️ 3.2 未做」改为「3.2 已接入生产路径（`0917F`）」
- `docs/session接力.md`【下一步】R-2 行：状态改「✅ 已完成（`<commit>`）」；「🆕 2026-09-16 `0916U`」段三条遗留待办的第 1、2、3 行状态改「✅ 已还（`0917F`）」
- `docs/tech-debt.md` TD-45 标销账

【五、收口】
1. 分支上全部提交后：`git -C /Users/paulshao/Projects/HumanResource merge --ff-only lane-0917f-charter-wiring`；ff 失败（main 已前进）⇒ 在 worktree 里 `git rebase main` 后重跑【三】2 再 ff，仍失败 ⇒ OPENER_PARTIAL: ff 合并失败
2. `git -C /Users/paulshao/Projects/HumanResource push`（被拒 ⇒ 主工作区 `pull --rebase --autostash` 后重试最多 3 次）
3. 真合判据：`git -C /Users/paulshao/Projects/HumanResource rev-list --count main..lane-0917f-charter-wiring` ＝ 0；非 0 再跑 `git cherry -v main lane-0917f-charter-wiring`，有 `+` 即真未合 ⇒ ⛔ 不许输出 OPENER_DONE
4. 并发协议：只 add 本条列出的路径；⛔ 禁 `git add -A`／`.`／`commit -a`／`stash`；`index.lock` 存在等 5 秒重试最多 5 次，⛔ 不删锁；`git status` 出现别人的改动正常，不动
5. 收工三行：改前/改后 passed 数 ｜ 合入 commit ｜ ⏸ 下一步：§5 真实起活实测（Shao Peishen 本人，先 `launchctl kickstart -k gui/$UID/com.zhuopin.hr.liaison` 让新代码生效）
6. 最后一行顶格输出 OPENER_DONE（不加粗、不加任何符号）
```

## 第十七批·收尾·重发（2026-09-17 06:3x Cowork·HR业务线-接力0917D）

> 一条泳道两条串行：`0917G` 先把 Cowork 已写好的落档（体积闸归档 ＋ 本节 ＋ 号池两行 ＋ 接力 R-1 销账）提交到 main，`0917H` 才能从含这些提交的 main 建 worktree。⛔ 不可并行：`0917H` 的 worktree 基线必须含 `0917G` 的提交，否则全量 pytest 的 `test_doc_size_budget` 在旧基线上是红的（接力 61 KB > 58）。

> 泳道：第十七批收尾重发
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Cowork 经 launchd 请求文件发车），名字另由 -n 给。

```
[Mac]0917G-体积闸归档与收尾编排落档提交
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: main ｜ worktree: ❌ 不勾（只提交 Cowork 已写好的文档改动）｜ 工作区: 仓库根 ｜ 派发: Cowork·HR业务线-接力0917D

背景：Cowork·0917D 发现 `docs/session接力.md` 61 KB 超体积闸（58）、`OP-0820-全量编排.md` 65 KB 超闸（60）⇒ `tests/test_doc_size_budget.py` 红、看护者不发车。已按闸的唯一处置做完：给 5 段已闭环标题加「【已闭环】」后跑 `python3 scripts/archive_docs.py --apply`（守恒校验通过：接力 61→41 KB、编排 65→12 KB）；另把已还的 TD-29 标题划掉后跑 `--scope techdebt --apply`（`docs/tech-debt.md` 51.5→46 KB；首发那条正是撞这道闸停下的）；追加本节、号池四行、接力【下一步】R-1 销账与 R-2 指向 `0917H`。全部未提交。
1. `git status --short` 本件相关应只有（多出别的不管、⛔ 不 add）：M docs/session接力.md ｜ M docs/archive/session接力-归档.md ｜ M docs/openers/OP-0820-全量编排.md ｜ M docs/openers/归档/OP-0820-历史批次.md ｜ M docs/openers/号池台账.md ｜ M docs/tech-debt.md ｜ M docs/archive/tech-debt-已还.md。缺任何一个 ⇒ 输出 OPENER_PARTIAL: 落档文件缺 <名> 并停
1b. 清首发残留：`git log --oneline main..lane-0917f-charter-wiring | wc -l` 为 0 ⇒ `git worktree remove .claude/worktrees/lane-0917f-charter-wiring && git branch -D lane-0917f-charter-wiring`；非 0 ⇒ ⛔ 不删，报告里记一行
2. `./venv/bin/python -m pytest -q tests/test_doc_size_budget.py tests/test_caretaker_openers.py 2>&1 | tail -2` → 0 失败；失败 ⇒ 不提交，OPENER_PARTIAL: 体积闸仍红 <末行>
3. `grep -c '【已闭环】' docs/session接力.md` 应为 0（已被搬走的段不应残留标记）；非 0 ⇒ OPENER_PARTIAL
4. `git add docs/session接力.md docs/archive/session接力-归档.md docs/openers/OP-0820-全量编排.md docs/openers/归档/OP-0820-历史批次.md docs/openers/号池台账.md docs/tech-debt.md docs/archive/tech-debt-已还.md`（⛔ 禁 `git add -A`／`.`／`commit -a`／`stash`；⛔ 不 add `_to_delete/`）
5. `git commit -m "docs(relay): 体积闸归档 ＋ 第十七批收尾编排 0917G/0917H 落档（0917G）"`；push 被拒 ⇒ `git pull --rebase --autostash origin main` 后重试最多 3 次；`.git/index.lock` 已存在 ⇒ 等 5 秒重试最多 5 次，⛔ 不删锁
6. `git rev-list --left-right --count HEAD...@{u}` 应为 `0 0`
7. 最后一行顶格输出 OPENER_DONE（不加粗、不加任何符号）
```

> 泳道：第十七批收尾重发
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Cowork 经 launchd 请求文件发车），名字另由 -n 给。

```
[Mac]0917H-P2章程接入生产路径3点2收口
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: lane-0917h-charter-wiring ｜ worktree: ✅ 勾（改产品代码）｜ 工作区: .claude/worktrees/lane-0917h-charter-wiring ｜ 派发: Cowork·HR业务线-接力0917D

【零、范围与流程口径】`liaison-reply-bridge-and-patrol` tasks.md **3.2** 的最后一公里：`unpack/charter.py` 代码已交付、两轮 review 过，但**没接进生产路径**——`__main__.py` 持有 `CHARTER_RELPATH` 字面量、`dispatch_wiring.py` 持有 `_read_charter_text`／`_build_minimal_prompt` 副本（两处都带 `P2-TODO` 注释），spec `liaison-unpack-charter`「该路径只在一处常量定义」现为假。本条补已批准 spec 与代码之间的缺口，⛔ 不动任何 spec 文本 ⇒ 按 `03-工具链协作规则.md`「轻量通道」第 4 条走**单条直接 TDD**，⛔ 不走 spec-to-plan／run-build。拆分豁免：单条 TDD、无 plan 文件、改动集中在两个模块的一处调用链，拆开反而要跨会话传中间态。
开工后若发现**必须改 spec 文本** ⇒ 输出 OPENER_PARTIAL: 需改 spec <哪条> 并停，⛔ 不自行扩大范围。

【一、前置——任一不过即输出 OPENER_PARTIAL: <原因> 并停，⛔ 不提问】
1. `pwd` 含 `.claude/worktrees/lane-0917h-charter-wiring`；`git branch --show-current` ＝ `lane-0917h-charter-wiring`
2. `git log --oneline -n 30 | grep -c '0917G'` ≥ 1（基线含体积闸归档提交）
3. `grep -n '^- \[ \] 3\.2' openspec/changes/liaison-reply-bridge-and-patrol/tasks.md` 命中（未勾）；`grep -c '^- \[x\]' openspec/changes/liaison-reply-bridge-and-patrol/tasks.md` ＝ 27
4. `grep -rn 'P2-TODO' tools/liaison/__main__.py tools/liaison/unpack/dispatch_wiring.py | wc -l` ≥ 2（已有人先做掉 ⇒ OPENER_PARTIAL: 前提失效）
5. `Skill(superpowers:test-driven-development)` 可达；回 `Unknown skill` ⇒ 登记「⏸ 留步：superpowers 不可达」并 OPENER_PARTIAL，⛔ 不按磁盘 SKILL.md 手工走、⛔ 不自己装插件
6. 测试解释器：worktree 里没有 venv 与 `tools/liaison/.venv`（gitignore），⛔ 不新建。一律 `PYTHONPATH=. /Users/paulshao/Projects/HumanResource/venv/bin/python -m pytest <路径> -q`；⛔ 测试里不许 `import aibot`
7. 改前基线：`tools/liaison/tests` 与全量 `pytest -q` 各跑一次，记下 passed 数

【二、要做什么（先红后绿）】
A. **`dispatch_wiring.bridge_dispatch` 改用 `charter` 模块**
   - 删 `_read_charter_text`、`_build_minimal_prompt` 与模块 docstring 里的 P2-TODO 段；`from tools.liaison.unpack import charter`
   - 读章程：`charter.read_charter(REPO_ROOT)`，捕获 `charter.CharterMissing` ⇒ 沿用现有降级语义（`charter_text=None` 交给 `dispatch_headless_unpack`，由它判 `failed(charter_missing)`），⛔ 不改三态审计口径
   - 拼 prompt：`charter.compute_prompt(letter_number=…, msgid=…, signal_relpath=…, checkpoint_iso=…, charter_text=…)`
     · `signal_relpath`：信号文件相对仓库根的路径，取 `unpack_cli.py` 的 `DEFAULT_SIGNAL_PATH`（尊重 `HR_LIAISON_SIGNAL_PATH` 环境变量的那套解析，照 unpack_cli 现有函数取，⛔ 不另写字面量）再 `relative_to(REPO_ROOT)`；不在仓库根下 ⇒ 用绝对路径字符串
     · `checkpoint_iso`：`now` 的 ISO 8601（带时区）
     · `letter_number`：`__main__.py` 绑的是 `None`（P0 零参 `dispatch()` 契约拿不到，已知缺口）。`bridge._emit_signal_and_dispatch` 在调 `dispatch()` **之前**已 `append_signal`，信号项带 `letter_number` ⇒ 当入参为 `None` 时，从信号文件里按 `msgid` 取该项的 `letter_number`（读逻辑复用 `unpack/signal.py`，必要时加一个纯读函数 `find_pending(path, msgid)`，⛔ 不改写入逻辑）；仍取不到 ⇒ 写 `"（未匹配）"`，⛔ 不抛异常、⛔ 不阻断起活
   - 签名：删除 `charter_relpath`／`charter_root` 两个参数（唯一来源改为 `charter.CHARTER_RELATIVE_PATH`）；测试需要替换章程时改注入 `repo_root: Path = REPO_ROOT` 一个参数
B. **`__main__.py`**：删 `CHARTER_RELPATH` 常量及其 P2-TODO 注释；`functools.partial(dispatch_wiring.bridge_dispatch, …)` 去掉那两个关键字。`tools/liaison/tests/test_inbound_wiring.py:656-657` 两条断言改为：partial 的 kwargs **不含** `charter_relpath`／`charter_root`
C. **单点常量机器闸（新测试）**：扫 `tools/liaison/**/*.py`（排除 `tests/` 与 `unpack/charter.py` 本身），`.claude/skills/liaison-unpack/SKILL.md` 这个字面量**零命中**。先红（改前应命中 `__main__.py`）后绿
D. **prompt 字段测试**：`bridge_dispatch` 起活时传给 `dispatch_headless_unpack` 的 `prompt` 以 `# 拆件会话起活` 开头、含 msgid／信号文件路径／检查点时刻，信号文件里有该 msgid 时含其 `letter_number`，且以章程全文逐字结尾（替身 `dispatch_headless_unpack`，⛔ 不起真进程）
E. **顺手还 TD-45**（`docs/tech-debt.md`，触发条件正是「碰 SKILL.md 或 dispatch 白名单/接线时顺手改」）：`.claude/skills/liaison-unpack/SKILL.md` §二.6 与 `unpack/dispatch.py` 白名单里的裸 `python -m tools.liaison criteria` 一律改成 `PYTHONPATH=. tools/liaison/.venv/bin/python -m tools.liaison criteria` 前缀形式（与 `unpack-signal` 同口径）；`test_unpack_charter_allowlist.py` 必须仍绿；TD-45 标销账并写 commit
F. **顺手一个词**：`openspec/changes/liaison-reply-bridge-and-patrol/design.md` D4 的 `HEADLESS_ARGV_TEMPLATE` → `HEADLESS_ARGV_FIXED_PART`（代码真名）。这是文档对齐代码，不是改 spec 行为

🔴 工程铁律 2：`compute_*` 纯函数不记日志、不读时钟、不读文件；读信号文件属副作用，放在 `bridge_dispatch` 里、⛔ 不塞进 `compute_prompt`。

【三、验收】
1. C、D 先红后绿，报告写红时的报错原文
2. `tools/liaison/tests` 全绿且 passed 数 ≥ 基线；全量 `pytest -q` 0 失败（报前后两个数）
3. `grep -rn 'P2-TODO\|CHARTER_RELPATH\|_build_minimal_prompt\|_read_charter_text' tools/liaison --include=*.py | grep -v '/tests/'` 零命中
4. `openspec validate liaison-reply-bridge-and-patrol --strict` 通过
5. ⛔ 不做真实起活、⛔ 不重启／kickstart 值守服务 `com.zhuopin.hr.liaison`、⛔ 不碰 `data/liaison.db`／`.env`／`_to_delete/`。报告明写「未做真实起活，归 §5，Shao Peishen 本人」

【四、落档（都在 worktree 里改并提交在分支上）】
- `tasks.md`：勾 3.2，删掉其行尾「不勾——…」括注，抬头 `27/33` → `28/33`、「⚠️ 3.2 未做」改为「3.2 已接入生产路径（`0917H`）」
- `docs/session接力.md`【下一步】R-2 行：状态改「✅ 已完成（`<commit>`）」；「🆕 2026-09-16 `0916U`」段三条遗留待办的第 1、2、3 行状态改「✅ 已还（`0917H`）」
- `docs/tech-debt.md` TD-45 标销账

【五、收口】
1. 分支上全部提交后：`git -C /Users/paulshao/Projects/HumanResource merge --ff-only lane-0917h-charter-wiring`；ff 失败（main 已前进）⇒ 在 worktree 里 `git rebase main` 后重跑【三】2 再 ff，仍失败 ⇒ OPENER_PARTIAL: ff 合并失败
2. `git -C /Users/paulshao/Projects/HumanResource push`（被拒 ⇒ 主工作区 `pull --rebase --autostash` 后重试最多 3 次）
3. 真合判据：`git -C /Users/paulshao/Projects/HumanResource rev-list --count main..lane-0917h-charter-wiring` ＝ 0；非 0 再跑 `git cherry -v main lane-0917h-charter-wiring`，有 `+` 即真未合 ⇒ ⛔ 不许输出 OPENER_DONE
4. 并发协议：只 add 本条列出的路径；⛔ 禁 `git add -A`／`.`／`commit -a`／`stash`；`index.lock` 存在等 5 秒重试最多 5 次，⛔ 不删锁；`git status` 出现别人的改动正常，不动
5. 收工三行：改前/改后 passed 数 ｜ 合入 commit ｜ ⏸ 下一步：§5 真实起活实测（Shao Peishen 本人，先 `launchctl kickstart -k gui/$UID/com.zhuopin.hr.liaison` 让新代码生效）
6. 最后一行顶格输出 OPENER_DONE（不加粗、不加任何符号）
```
