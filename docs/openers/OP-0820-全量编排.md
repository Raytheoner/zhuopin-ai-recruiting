# OP-0820 · 全量 Opener 编排

> 2026-08-20。**唯一权威 opener 清单**，`OP-0820-A_单元BC开工.md` 已作废。
>
> **编排原则**：不依赖人工控制执行节奏。有依赖就彻底串行，能并发的全部并行。
> 并发安全靠《并发协议》做到机器自愈，**不靠"你记得别同时开"**。
>
> **格式**：每个代码块头两行是标题行 + 【设置】行，整块复制即可，不要拆。

---

## 🔢 号池台账 → 已迁至 `docs/openers/号池台账.md`

> 〔归档工具〕 2026-09-16：出号前查、给出后登记，一律改在该文件；历史月份的行在 `docs/openers/归档/号池台账-归档.md`。

## 二、并发协议（**每条 opener 内已逐字内嵌，此处备查**）

1. **只 `git add` 本条 opener 明确列出的路径。** ⛔ 禁止 `git add -A` / `git add .` / `git commit -a`。这是整套并行编排成立的唯一前提。
2. **`git status` 里出现别人的改动是正常的**——那是并行的另一条 opener 在干活。不要停下、不要问、不要顺手提交。
3. ⛔ 不要在 commit 前 pull（并发下会被别人的未提交改动挡住）；push 被拒才 `git pull --rebase --autostash origin main` 重试。
4. push 被拒（non-fast-forward）→ 再 `git pull --rebase origin main` 后重试，**最多 3 次**，仍失败停下贴错误。
5. 报 `.git/index.lock: File exists` → **等 5 秒重试，最多 5 次**。⛔ **绝不删除该锁文件**——那是另一个 session 正在用，删了会毁掉它的提交。

---

## 待执行区

> 〔归档工具〕 2026-09-16：`lane-dispatch` 新写的无头 opener 放在本节之后（`> 泳道：<名>` ＋ 代码块）。
> 〔归档工具〕 跑完被 `mark_done` 摘标注的批次，下次 `python3 scripts/archive_docs.py --apply` 会整段搬去归档。

> ✅ 已完成 2026-09-16（OK）· 原泳道 liaison测试脚手架收尾（脚本自动摘，不再进 run-lanes）

```
[Mac]0916E-TD29收尾liaison测试脚手架收编与config文案
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ✅ 勾（写代码）｜ 工作区: worktree（`.claude/worktrees/td29-liaison-test-scaffolding-0916E`）｜ 派发: run-lanes.sh

【零、背景与授权】TD-29 剩余两条，Shao Peishen 2026-09-10 已答 `4a`：**下一批泳道单独派一条**，独占
`tools/liaison/tests/` 与 `tools/liaison/config.py`，⛔ 不挂 8.6 灰度前置、⛔ 不留着不做。全文见
`docs/tech-debt.md` TD-29 条目「仍欠两条」。这是**纯重构 + 文案修正，不改变外部可观察行为**，按
CLAUDE.md「轻量通道」走：⛔ 不用 `spec-to-plan`/`run-build`，直接 TDD 单条改完提交
（`superpowers:test-driven-development`；调不到 ⇒ 登记「⏸ 留步：superpowers 不可达」并停，⛔ 不手工按磁盘 SKILL.md 走）。

【一、前置自检】
1. `pwd` 必须含 `.claude/worktrees/`，否则停下报
2. `git log --oneline -5 -- tools/liaison/tests/test_notify_store.py | grep -c 'assert_group_notify_identity'` 之类的旧顾虑已随
   `5ca1f50` 处置，本条不需要再验；只需确认 `grep -n 'assert_group_notify_identity' tools/liaison/tests/test_notify_store.py` 命中 ≥2 处（存在即可，⛔ 不是本条任务）

【二、任务 A · conftest 收编（三处复制粘贴）】
现状（已核实的确切行号，实现前重新 grep 一遍确认未漂移）：
- `FakeClock`：`tools/liaison/tests/test_notify_ratelimit.py:14` 与 `tools/liaison/tests/test_notify_webhook.py:63` 两份定义，逐字节比对是否相同
- `RecordingSink`：`tools/liaison/tests/test_notify_store.py:29` 与 `tools/liaison/tests/test_notify_webhook.py:76` 两份
- `FAKE_WEBHOOK`：`tools/liaison/tests/test_notify_transport.py:25` 与 `tools/liaison/tests/test_notify_webhook.py:15` 两份（值＝`https://example.invalid/cgi-bin/webhook/send?key=fake-key-for-tests`，⛔ 改动中不得把这个假值换成真实域名）

步骤：
1. 若两份定义逐字节不同，先登记差异、以更完整/更新的那份为准，⛔ 不擅自改逻辑
2. 三者收进 `tools/liaison/tests/conftest.py`，以 pytest fixture 或模块级常量形式导出（`FakeClock`/`RecordingSink` 用 `@pytest.fixture` 更符合 pytest 惯例，但若两处原调用点是直接实例化类而非注入，保持调用点最小改动优先——不确定就选影响面小的那种）
3. 四个原文件改为从 `conftest.py`（同目录自动可见，无需显式 import fixture；若是模块级常量/类则需要 `from .conftest import ...` 或按 pytest 惯例处理）里取用，删除本地重复定义
4. 跑 `tools/liaison/tests/` 全量，四份原文件涉及的用例必须逐条仍过，且总数不减少（收编不是删测试）

【三、任务 B · config.py:75 异常文案错位】
现状：`tools/liaison/config.py:70-83` 的 `load_group_webhook` 复用 `tools/liaison/errors.py` 的
`MissingCredentialsError`，其消息固定「HR 值守通道**拒绝启动**：…」（`errors.py:19`）。但
`load_group_webhook` 是**发送期**校验（docstring 已明写"⛔ 刻意不加进 REQUIRED_CREDENTIAL_ENV_NAMES"，
理由是收消息与发群通知独立、不能连坐），服务本身仍在正常运行，只是这一次群通知发不出去。
运维看到"拒绝启动"字样会误判成服务整体宕了。

步骤：
1. 在 `tools/liaison/errors.py` 新增一个独立异常（例如 `GroupWebhookMissingError(RuntimeError)`），
   构造签名与 `MissingCredentialsError` 一致（`missing_names: Iterable[str]`，消息同样⛔ 只出现变量名不出现取值），
   但文案改为准确描述"群通知发送地址缺失，本次跳过发送"一类措辞，**不含"拒绝启动"字样**
2. `config.py:83` 的 `raise MissingCredentialsError([GROUP_WEBHOOK_ENV])` 改成 `raise GroupWebhookMissingError([GROUP_WEBHOOK_ENV])`
3. 调用方核对并同步：`tools/liaison/followup.py:216-217`（`except MissingCredentialsError` 那块，
   若捕获的正是 `load_group_webhook` 抛出的这个异常则要改成捕获新类型；若那里捕获的是别的调用点的
   `MissingCredentialsError` 则不动，只改群通知这条路径）、`tools/liaison/notify/webhook.py`（若有捕获此异常的逻辑同样核）
4. `tools/liaison/tests/test_notify_transport.py:333-366` 四处 `config.load_group_webhook(...)` 相关用例，
   涉及"抛异常"的断言改成断言新异常类型 `GroupWebhookMissingError`（⛔ 不要只改字符串匹配，要改类型）
5. 先红后绿：新增/改动的用例在改代码前跑必须红，改完再绿

【四、全量回归】`./venv/bin/python -m pytest tools/liaison/tests/ -q` 与仓库根 `./venv/bin/python -m pytest -q` 均需
0 失败，且总用例数 ≥ 改动前（收编与改名不得导致用例被误删）。

【五、收口】
1. `git add tools/liaison/tests/conftest.py tools/liaison/tests/test_notify_ratelimit.py tools/liaison/tests/test_notify_store.py tools/liaison/tests/test_notify_webhook.py tools/liaison/tests/test_notify_transport.py tools/liaison/errors.py tools/liaison/config.py tools/liaison/followup.py tools/liaison/notify/webhook.py docs/tech-debt.md`
   （最后一项：把 TD-29「仍欠两条」改成 `✅ 已还`，附本次 commit hash）
2. commit（一条即可），走 `finishing-a-development-branch` 合回 `main`
3. 合并后 `git rev-list --count main..<分支>`；非 0 时再跑 `git cherry -v main <分支>` 确认全 `-`（真合）
4. 确认内容真在 main 上之前 ⛔ 不许输出 `OPENER_DONE`

【六、并发协议（已逐字内嵌，本批只有一条泳道，仍照抄）】
1. 只 `git add` 本条明确列出的路径。⛔ 禁止 `git add -A` / `git add .` / `git commit -a` / `git stash`
2. `git status` 里出现别人的改动是正常的，不要停下、不要问、不要顺手提交
3. push 被拒 → `git pull --rebase --autostash origin main` 后重试，最多 3 次
4. 报 `.git/index.lock` 已存在 → 等 5 秒重试最多 5 次，⛔ 默认绝不删除该锁
```

> ✅ 已完成 2026-09-16（OK，commit 555592a；脚本判 NO-SENTINEL 系哨兵被加粗成 `**OPENER_DONE**`，Cowork 核验后手工摘）· 原泳道 token治理0916K
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Paul 在手机上，2026-09-16 由 Cowork 经 launchd 请求文件发车），名字另由 -n 给。🔴 若改为手工贴进 CC Desktop，必须自行补上第 3 行。

```
[Mac]0916K-Token治理-泳道开工整读大文件与提醒误停根治
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（改项目 hook、执行器与共享文档，要在主检出一次改完一次提交）｜ 工作区: 仓库根 ｜ 派发: Cowork·Token治理-0916

读 docs/openers/0916K-Token治理-泳道开工整读大文件与提醒误停根治.md 全文并逐节执行（该文件按分段读，每次 Read 带 offset＋limit ≤ 400 行），以该文件为准。文件不存在 → 输出 OPENER_PARTIAL: 正文文件不存在 并停。
无头补充（覆盖正文里对应说法）：
1. 正文【一】3 的 git status 预期里另有 `M docs/openers/OP-0820-全量编排.md`（本块自身），属正常；【七】2 的 git add 列表追加 `docs/openers/OP-0820-全量编排.md`。
2. 本 session 就是由 run-lanes.sh 起的泳道：正文【一】2「pgrep run-lanes 为空否则停」**跳过**（会命中自己的父进程）。
3. ⛔ 不修改 `docs/openers/run-lanes.sh`（它正在执行本泳道）。若【二】2 的失败只能靠改 run-lanes.sh 解决 → 不改，登记「⏸ 留步：run-lanes.sh 需改，待非泳道会话处理」，其余段照做，最后 OPENER_PARTIAL。
4. 凡正文写「停下报我」，一律改为：登记原因、跳过该段后面依赖它的步骤、最终输出 OPENER_PARTIAL: <原因>，⛔ 不提问。
5. 收工报告写进 `.claude/handoff/0916K-收工报告.md`（正文【八】各项），并在终端输出里同样给出。
```

> ✅ 已完成 2026-09-16（OK）· 原泳道 token治理0916L（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Cowork 经 launchd 请求文件发车），名字另由 -n 给。

```
[Mac]0916L-哨兵加粗容错与0916K摘标注提交
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（只提交 Cowork 已写好的两处小改动）｜ 工作区: 仓库根 ｜ 派发: Cowork·Token治理-0916

背景：0916K 活已干完并提交（555592a），但哨兵输出成 `**OPENER_DONE**` 被判 NO-SENTINEL、泳道标注没摘，下一批会重跑它。Cowork 已改好（未提交）：run-lanes.sh 哨兵正则容忍加粗／反引号；tests/test_run_lanes_model.py 加一条用例；OP-0820 把 0916K 标注换成已完成行，并追加本块。
1. `git pull --rebase --autostash origin main`；`git status --short` 本件相关应只有：M docs/openers/run-lanes.sh ｜ M tests/test_run_lanes_model.py ｜ M docs/openers/OP-0820-全量编排.md ｜ M docs/openers/号池台账.md
2. `bash -n docs/openers/run-lanes.sh && ./venv/bin/python -m pytest -q tests/test_run_lanes_model.py 2>&1 | tail -2` → 0 失败；失败 → 不提交，输出 OPENER_PARTIAL: 测试失败 <末行>
3. ⛔ 不修改 run-lanes.sh（它正在执行本泳道），只提交
4. git add docs/openers/run-lanes.sh tests/test_run_lanes_model.py docs/openers/OP-0820-全量编排.md docs/openers/号池台账.md
5. git commit -m "fix(run-lanes): 哨兵容忍加粗/反引号 ＋ 摘 0916K 标注（0916L）"，按并发协议 push
6. 最后一行顶格输出 OPENER_DONE（不加粗、不加任何符号）
```

## TD-47 合入 ＋ §5 重跑·三（2026-09-17 08:2x Cowork·HR业务线-接力0917D 远程发车）

> `0917M`：文档已提交（`e0d1d46`）、TD-48 已登记（`65f87cb`），但前置「K 分支顶＝`8ce57b3`」写得过严——K worktree 里游离的无头拆件子会话 08:08 又自发提交了 `fe64bc1`，M 按字面预案停下。本块改判据为「修复提交在分支上」，并先清掉游离子会话。

> ✅ 已完成 2026-09-17（OK）· 原泳道 TD47合入与§5重跑三（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Cowork 经 launchd 请求文件发车），名字另由 -n 给。

```
[Mac]0917N-TD47合入与§5重跑三
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: main ｜ worktree: ❌ 不勾（提交主工作区未提交文档、cherry-pick 修复、再跑真实服务实测）｜ 工作区: 仓库根 ｜ 派发: Cowork·HR业务线-接力0917D

无人值守：⛔ 不提问；「停下报」＝落档已得结果后输出 OPENER_PARTIAL: <原因> 结束。

【零、先提交 Cowork 文档】
`git status --short` 若有 `M docs/openers/OP-0820-全量编排.md`／`M docs/openers/号池台账.md`／`M docs/session接力.md` ⇒ add 这些 → `git commit -m "docs(openers): 派 0917N（0917N）"`；没有就跳过

【一、只取修复提交合入 main】
0. 🔴 先清游离子会话：K 的 `--force` 实测起过的无头拆件会话在 K worktree 里持续自发提交（`a644d45`→`f575c5e`→`fe64bc1`，最后一条 08:08）。跑 `ps -axo pid,etime,command | grep -E 'claude .*(-p|--print)' | grep -v grep`，对其中 **cwd 在 `.claude/worktrees/lane-0917k-td47-child-auth`**（用 `lsof -a -p <pid> -d cwd -Fn` 核）的进程 `kill -TERM`，10 秒后仍在 ⇒ `kill -KILL`；⛔ 不杀其它 claude 进程（含本 session 与值守服务）。报告里列出杀掉的 pid
1. `git merge-base --is-ancestor 8ce57b3 lane-0917k-td47-child-auth` 退出码 0（修复提交在分支上即可，⛔ 不再要求它是分支顶；分支顶多出的 `a644d45`/`f575c5e`/`fe64bc1` 等子会话噪声一律不取）；不在 ⇒ PARTIAL: 修复提交丢失
2. `git cherry-pick 8ce57b3`（⛔ 不 merge 整个分支、⛔ 不取 `a644d45`/`f575c5e`）。冲突 ⇒ 只可能在 `docs/tech-debt.md`，以分支版本的 TD-47 段为准手工合并后 `git cherry-pick --continue`；其它文件冲突 ⇒ `git cherry-pick --abort` 并 PARTIAL
3. `PYTHONPATH=. ./venv/bin/python -m pytest -q tools/liaison/tests/test_unpack_dispatch.py 2>&1 | tail -2` 0 失败；全量 `./venv/bin/python -m pytest -q 2>&1 | tail -2` 0 失败（报数）
4. `grep -n '"USER"' tools/liaison/unpack/dispatch.py` 命中；`grep -n '^## ~~TD-47~~' docs/tech-debt.md` 命中
5. push（被拒 ⇒ `pull --rebase --autostash` 重试最多 3 次；`index.lock` 等 5 秒重试最多 5 次，⛔ 不删锁）
6. 清 K 残留：`git worktree remove --force .claude/worktrees/lane-0917k-td47-child-auth && git branch -D lane-0917k-td47-child-auth`（有意丢弃子会话自发的噪声提交 `a644d45`/`f575c5e`/`fe64bc1` 及之后任何同类提交）

【二、TD-48 已由 `0917M` 登记（`65f87cb`）】补一句「0917M 后又复现：`fe64bc1`（08:08）」进 TD-48 事实段，随【四】一起提交

【三、§5 重跑（沿用 `docs/openers/0917J-打标即开班真实起活实测.md`，跳过其【零】，其余逐节照做）】差异：
1. **5.1 必须重做**（服务要重启才加载新代码）；`launchctl print` 的 pid 与 0917J 记录的 41080 不同
2. **5.2**：判据加一条——最新无头日志 ⛔ 不含 `Not logged in`。🔴 子会话 10 分钟内未自行结束 ⇒ 按 `unpack-session.lock` 的 pid `kill -TERM`（10 秒后仍在 `-KILL`），findings 记「超时终止」，这不单独判 5.2 失败（判据看日志是否已登录并跑起来）。🔴 **子会话结束后立刻** `git log --oneline -n 5` 与 `git status --short`：若出现本 session 之外的新 commit（子会话自己提交的），记下 hash 与 `git show --stat`，逐条 `git revert --no-edit <hash>`（⛔ 不 reset、⛔ 不改写历史），写进 findings 并补进 TD-48「又复现一次」；子会话留下的未提交改动 ⇒ 只记录、⛔ 不 add
3. **5.4**：进度信号照原文写 `.claude/handoff/0917J-progress.txt`（Cowork 在盯它，会手机通知 Shao Peishen）；等待窗口 30 分钟
4. **落档**：findings 追加到 `docs/findings/2026-09-17-打标即开班真实起活实测.md` 末尾一节「重跑三（0917N）」，⛔ 不覆盖前文；`docs/session接力.md` R-3、R-3b 两行按实际结果改（R-3b 写 TD-47 已还 `<cherry-pick 后的 hash>`）
5. 【四】提交：`git add` findings、`tasks.md`、`docs/session接力.md`、`docs/tech-debt.md`（及 revert 已自成 commit）→ commit `docs(liaison): §5 重跑三 5.1/5.2/5.4 ＋ TD-48 登记（0917N）` → push
6. 收工三行：5.1/5.2/5.4 各自 ✅/❌ ｜ commits ｜ ⏸ 下一步
7. 全部 5.1/5.2/5.4 通过 ⇒ 最后一行顶格输出 OPENER_DONE；否则 OPENER_PARTIAL: <哪步>。不加粗、不加任何符号
```

## TD-48 还债（两层）＋ 5.2 复验（2026-09-17 08:5x Cowork·HR业务线-接力0917D 远程发车，Shao Peishen 答 `1a`）

> Cowork 核到的事实（写给执行者，⛔ 当假设再核一遍）：① 章程 §三／§四 本来就允许会话写并提交 `docs/session接力.md`（登记待人栏），所以 `07537fe` 那类提交**可能是按章程行事**，真正的问题是权限层没有路径边界（design「Risks」第一条早已承认 `acceptEdits` 不按路径限制编辑）。② 真实信号文件 `data/liaison/unpack-signal.json` 里挂着一条**测试假数据**：`人事部#1 / MSGID0001 / data/liaison/archive/threadA/20260910/MSGID0001__正文.txt`（该归档路径不存在），`MSGID0001` 只出现在 `tools/liaison/tests/test_inbound_wiring.py` ⇒ 疑似该测试没隔离信号路径、写进了真实文件。每次起活会话都探到这条假信号 ⇒ 走 §四 待人登记 ⇒ 提交接力。
> 一条泳道两条串行：`0917O` worktree 改代码合回 main → `0917P` 主工作区清假信号、重启服务、复验 5.2。

> ✅ 已完成 2026-09-17（OK）· 原泳道 TD48两层与5点2复验（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Cowork 经 launchd 请求文件发车），名字另由 -n 给。

```
[Mac]0917O-TD48权限路径边界与force带真章程
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: lane-0917o-td48-path-guard ｜ worktree: ✅ 勾（改产品代码与测试）｜ 工作区: .claude/worktrees/lane-0917o-td48-path-guard ｜ 派发: Cowork·HR业务线-接力0917D ｜ 模型: Opus

无人值守：⛔ 不提问；「停下报」＝落档已得结果后输出 OPENER_PARTIAL: <原因> 结束。

【零、授权与流程口径】Shao Peishen 2026-09-17 答 `1a`：TD-48 两层一起做——(1) 机器层把拆件会话的编辑与 `git add` 收窄到章程 §三 列出的路径；(2) `unpack-dispatch --force` 也带真章程。这一裁决**改变 design D4 的权限形状**，因此本条被授权同步修改 `openspec/changes/liaison-reply-bridge-and-patrol/design.md`（D4 与 Risks 第一条）及必要时 `specs/liaison-unpack-dispatch/spec.md` 的对应措辞，改完 `openspec validate liaison-reply-bridge-and-patrol --strict` 必须过；⛔ 不动其它 spec 条款。实现走 TDD（`superpowers:test-driven-development`；不可达 ⇒ 登记留步并 PARTIAL）。拆分豁免：权限常量、CLI 与交叉核对测试共用同一常量，拆开会跨会话传半成品。
🔴 ⛔ 不用 `--dangerously-skip-permissions`；⛔ 不透传整份环境；⛔ 章程 SKILL.md 的红线九项与 §三 路径清单**不改语义**（红线 ⑧ 同样约束本条：只允许为对齐权限写法而做的措辞改动，且报告里逐条列出）。

【一、前置——任一不过即 PARTIAL】
1. `pwd` 含 `.claude/worktrees/lane-0917o-td48-path-guard`，分支一致
2. `grep -n '^## TD-48' docs/tech-debt.md`、`grep -n '"USER"' tools/liaison/unpack/dispatch.py` 均命中
3. 解释器：`PYTHONPATH=. /Users/paulshao/Projects/HumanResource/venv/bin/python -m pytest …`；claude 路径取主工作区 `tools/liaison/.venv/bin/python -m tools.liaison unpack-dispatch --dry-run` 的输出

【二、先做实验，定权限写法（⛔ 凭文档猜写法不算）】
在 `mktemp -d` 建一个一次性 git 仓（含 `tools/x.py`、`docs/跟进信/README-跟进信清单.md`、`docs/session接力.md` 三个空文件并首提交），在其中用候选 argv 跑极小无头会话（`env -i PATH HOME USER` ＋ claude 路径，`--max-budget-usd 0.2` 若支持），prompt 让它依次尝试：a 编辑 `tools/x.py`；b 编辑 `docs/session接力.md`；c `git add -A`；d `git add docs/session接力.md`；e `git commit -a -m t`；f `git commit -m t`。逐项记「放行/被拒」原文。
候选写法按顺序试，取**第一个满足「a/c/e 被拒、b/d/f 放行」的**：
- 甲：保留 `--permission-mode acceptEdits`，`--allowedTools` 里 `Edit`/`Write` 改为路径限定（如 `Edit(docs/跟进信/**)`、`Edit(docs/session接力.md)`），`Bash(git add:*)` 改为逐路径前缀（`Bash(git add docs/跟进信/:*)`、`Bash(git add docs/session接力.md:*)`），`Bash(git commit:*)` 改为 `Bash(git commit -m:*)`
- 乙：甲 ＋ `--disallowedTools` 显式拒 `Edit(tools/**)`、`Edit(app/**)`、`Edit(scripts/**)`、`Edit(tests/**)`、`Edit(openspec/**)`、`Edit(.claude/**)`、`Edit(CLAUDE.md)`、`Edit(data/**)`、`Edit(.env*)`、`Edit(docs/openers/**)` 及同形 `Write(...)`、`Bash(git add -A:*)`、`Bash(git add .:*)`、`Bash(git commit -a:*)`、`Bash(git stash:*)`、`Bash(git push:*)`
- 丙：`--permission-mode default` ＋ 甲的路径限定 allowedTools（非交互下未放行即拒）
都不满足 ⇒ 实验记录写进 TD-48，PARTIAL: 权限层无法按路径收窄 <最接近的写法与缺口>
⚠️ 路径规则的相对基准（仓库根 vs 绝对路径 `//`）以实验为准；拆件会话 cwd＝仓库根（`dispatch.py` 的 `cwd=str(REPO_ROOT)`）

【三、改代码（先红后绿）】
A. `dispatch.HEADLESS_ARGV_FIXED_PART` 按【二】选中写法重写；路径清单与章程 §三 逐字一致（`docs/跟进信/回件/`、`docs/跟进信/README-跟进信清单.md`、`docs/跟进信/口径点台账.md`、`docs/session接力.md`）
B. 新测试（先红）：① argv 里不再有裸 `Edit`/`Write`/`Bash(git add:*)`/`Bash(git commit:*)`；② 章程 §三 列出的每个路径都在 argv 的 add 与 Edit/Write 放行里（从 SKILL.md 解析，不另抄字面量）；③ 若选乙，红线目录都在 disallowed 里；④ `send-followup`、`git push` 仍不在放行里
C. `tools/liaison/tests/test_unpack_charter_allowlist.py`：扫描器适配新写法（`Bash(<前缀>:*)` 正则可能要放宽到带路径的前缀），两条既有核对仍绿
D. `unpack_cli.unpack_dispatch_main` 的 `--force`：删占位 `charter_text`/`prompt`，改 `charter.read_charter(REPO_ROOT)` ＋ `charter.compute_prompt(letter_number="（验收起活·无真实回件）", msgid=f"FORCE-{now:%Y%m%dT%H%M%SZ}", signal_relpath=<与 bridge_dispatch 同一解析>, checkpoint_iso=now.isoformat(), charter_text=…)`；缺章程 ⇒ 打印一行错误、退出码非 0、⛔ 不起进程。测试：替身 `dispatch_headless_unpack` 收到的 prompt 以章程全文结尾
E. 测试隔离：查 `MSGID0001` 进入真实 `data/liaison/unpack-signal.json` 的路径（疑 `tools/liaison/tests/test_inbound_wiring.py` 未隔离 `DEFAULT_SIGNAL_PATH`／`HR_LIAISON_SIGNAL_PATH`）。证实 ⇒ 在 `tools/liaison/tests/conftest.py` 加 autouse fixture 把信号路径与锁/日志默认路径指到 `tmp_path`，并加回归测试：跑完该测试文件后真实 `data/liaison/unpack-signal.json` 的 mtime 不变（用 monkeypatch 过的 REPO 常量判，⛔ 不读真实文件内容）。⛔ 本条**不清**真实信号文件（归 `0917P`）
F. 真实验证一次（不经服务、不在主工作区）：在【二】的一次性仓里用新 argv ＋ 真章程 prompt 跑一轮，确认 a/c/e 仍被拒；报告附原文

【四、验收】
1. B/D/E 新测试先红后绿（报红时原文）；`tools/liaison/tests` 全绿；全量 `pytest -q` 0 失败（报前后数）
2. `openspec validate liaison-reply-bridge-and-patrol --strict` 通过
3. design D4 argv 块与 `HEADLESS_ARGV_FIXED_PART` 一致；Risks 第一条改为「已由权限层按路径收窄（0917O，写法 甲/乙/丙）」并保留残余风险说明

【五、落档与收口】
- worktree 内：`docs/tech-debt.md` TD-48 改 `## ~~TD-48~~ … ✅ 已还（0917O）`，补实验记录、选中写法、测试隔离根因；若 E 证实泄漏另登 `## ~~TD-49~~ … ✅ 已还（0917O）`（号以当时最大号 +1 为准）。⛔ 不改 `docs/session接力.md`、`tasks.md`
- 分支提交 → `git -C /Users/paulshao/Projects/HumanResource merge --ff-only lane-0917o-td48-path-guard`；ff 失败 ⇒ worktree 内 `git rebase main`，重跑【四】1 再 ff，仍失败 ⇒ PARTIAL
- push（被拒 ⇒ 主工作区 `pull --rebase --autostash` 重试最多 3 次）；真合 `rev-list --count main..<分支>`＝0，否则看 `git cherry -v`，有 `+` ⇒ ⛔ 不许 OPENER_DONE
- 并发协议：只 add 本条改的路径；⛔ 禁 `git add -A`／`.`／`commit -a`／`stash`；`index.lock` 等 5 秒重试最多 5 次、⛔ 不删锁
- 收工三行：选中写法与实验结论 ｜ 改前/改后 passed·failed ｜ 合入 commit
- 最后一行顶格输出 OPENER_DONE（不加粗、不加任何符号）
```

> ✅ 已完成 2026-09-17（OK）· 原泳道 TD48两层与5点2复验（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Cowork 经 launchd 请求文件发车），名字另由 -n 给。

```
[Mac]0917P-清测试假信号与5点2复验
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: main ｜ worktree: ❌ 不勾（要真实服务、真实信号文件、`tools/liaison/.venv`）｜ 工作区: 仓库根 ｜ 派发: Cowork·HR业务线-接力0917D

无人值守：⛔ 不提问；「停下报」＝落档后输出 OPENER_PARTIAL: <原因> 结束。⛔ 不发任何企微消息。

【零、提交 Cowork 文档】`git status --short` 若有 `M docs/openers/OP-0820-全量编排.md`／`M docs/openers/号池台账.md`／`M docs/session接力.md` ⇒ add 这些 → `git commit -m "docs(openers): 派 0917O/0917P（0917P）"`；没有就跳过

【一、前置】`git log --oneline -n 30 | grep -c 0917O` ≥ 1 且 `grep -n '^## ~~TD-48~~' docs/tech-debt.md` 命中；否则 PARTIAL: 0917O 未合入

【二、清测试假信号（数据操作，只动这一条）】
1. `cat data/liaison/unpack-signal.json`；判假数据的机器判据三条全中才清：该项 `msgid` ＝ `MSGID0001`；`archived_path` 指向的文件不存在（`test -e` 为假）；`grep -rn MSGID0001 tools/liaison/tests` 命中而 `sqlite3 data/liaison.db "SELECT COUNT(*) FROM liaison_message WHERE msgid='MSGID0001'"` 为 0
2. 三条全中 ⇒ 先 `cp data/liaison/unpack-signal.json data/liaison/unpack-signal.json.bak-0917P`，再只用 CLI 清：`PYTHONPATH=. tools/liaison/.venv/bin/python -m tools.liaison unpack-signal --clear --before 2026-09-11T00:00:00+08:00`；然后 `--probe` 应输出 `[NO-SIGNAL]`。任何一条不中 ⇒ ⛔ 不清，PARTIAL: 信号项非测试数据 <原文>
3. 若文件里还有 `at` ≥ 2026-09-11 的项 ⇒ ⛔ 不清它们，报告原文

【三、重启服务 ＋ 5.2 复验（真章程、收窄权限）】
1. `launchctl kickstart -k gui/$UID/com.zhuopin.hr.liaison`；30 秒后 `launchctl print` 为 running 且 pid 变化；stderr 日志新增行无新 ERROR（冷启动两条已知噪声除外）
2. 记 `git rev-parse HEAD` 为 H0；`PYTHONPATH=. tools/liaison/.venv/bin/python -m tools.liaison unpack-dispatch --dry-run` 抄 argv（应含路径限定写法）
3. `… unpack-dispatch --force`；按锁文件 pid 等结束，最多 10 分钟，超时 `kill -TERM`（10 秒后 `-KILL`）
4. `head -20` 最新 `data/liaison/logs/unpack-headless/*.log`：判据＝无 `Not logged in`；会话读到真章程（日志或行为可见探测了 `unpack-signal --probe`）；信号已清则以 `[NO-SIGNAL]` 结束
5. `git log --oneline H0..HEAD` 与 `git status --porcelain`：子会话若有提交，`git show --name-only` 的每个路径都必须在章程 §三 四类路径内；有任一越界路径 ⇒ `git revert --no-edit` 该提交，TD-48 重新打开（标题去掉删除线并追加复现），PARTIAL。全在清单内 ⇒ 记录即可，不 revert
6. 通过 ⇒ `tasks.md` 5.2 保持已勾（findings 注明复验）

【四、落档】
- `docs/findings/2026-09-17-打标即开班真实起活实测.md` 末尾加「复验（0917P）」一节：假信号三条判据原文、清理前后 probe 输出、kickstart 前后 pid、dry-run argv、日志前 20 行、H0..HEAD 提交清单与路径核对
- `docs/session接力.md` R-3、R-3b 两行按结果改；R-3 注明「5.3 仍待汤丽萍真实入站；5.5 与归档随 5.3」
- add 仅：findings、`docs/session接力.md`、（若重开 TD-48）`docs/tech-debt.md` → commit `docs(liaison): 清测试假信号 ＋ 5.2 真章程复验（0917P）` → push（子会话若有合规提交，此时会一并推上去——⚠️ 这违反 5.3 判据里「会话提交不 push」只针对 5.3 端到端那一轮，本轮是验收起活，报告里写明被一并推送的 hash）
- 收工三行：假信号是否清 ｜ 5.2 复验 ✅/❌ 与子会话提交路径核对 ｜ commits
- 最后一行顶格输出 OPENER_DONE（不加粗、不加任何符号）；否则 OPENER_PARTIAL: <哪步>
```

## 5.3 首次真实回件卡在 git add（2026-09-17 09:5x Cowork·HR业务线-接力0917D 远程发车）

> 事实（Cowork 已核）：汤丽萍 09:45 群 @「人力保障组测试」、09:46 私信「测试短信」。桥两次打标（审计 `bridge_marked` ×2）、第一条 `dispatch_started`、第二条 `dispatch_skipped_busy`；信号文件两项。拆件会话（日志 `data/liaison/logs/unpack-headless/20260917T014544528242Z.log`）读真章程、判第一条**非实质**、写了 `docs/跟进信/回件/人事部#1-20260917.md`、还原台账、在接力追加一行——然后 `git add` 三次都报「This command requires approval」（合并／加引号／单文件），按章程没 commit、没清信号就结束。之后第二条的打标又把台账写回第九态。⇒ 主工作区现有三处会话产物未提交（`README-跟进信清单.md` 当前＝第二条打标后的第九态；`回件/人事部#1-20260917.md`；`session接力.md` 末尾一行）。另：日志开头多条「`Write(...)` 规则不被文件权限检查匹配，只认 `Edit(...)`」告警，说明 TD-48 里 `Write(...)` allow/deny 规则是无效噪声。
> 泳道两条串行：`0917Q`（worktree）修 `git add` 放行 → `0917R`（主工作区）清理会话半成品、重跑一轮验 5.3。

> ✅ 已完成 2026-09-17（OK）· 原泳道 5点3回件git-add放行（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Cowork 经 launchd 请求文件发车），名字另由 -n 给。

```
[Mac]0917Q-拆件会话git-add放行修复TD50
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: lane-0917q-td50-git-add ｜ worktree: ✅ 勾（改产品代码与测试）｜ 工作区: .claude/worktrees/lane-0917q-td50-git-add ｜ 派发: Cowork·HR业务线-接力0917D ｜ 模型: Opus

无人值守：⛔ 不提问；「停下报」＝落档后输出 OPENER_PARTIAL: <原因> 结束。授权：属 TD-48 已授权范围（Shao Peishen 答 `1a`）的缺陷修复；⛔ 不放宽路径边界、⛔ 不用跳过权限参数。允许同步改 design D4 的 argv 块。TDD。拆分豁免：同一常量与测试。

【一、前置】pwd 含本 worktree、分支一致；`grep -n 'CHARTER_WRITABLE_PATHS' tools/liaison/unpack/dispatch.py` 命中；解释器同 0917O

【二、实验复现（一次性 git 仓，照 0917O【二】做法）】仓内建 `docs/跟进信/回件/a.md`、`docs/跟进信/README-跟进信清单.md`、`docs/session接力.md`，用**当前** `HEADLESS_ARGV_FIXED_PART`（`env -i PATH HOME USER`）让会话依次跑：
g1 `git add docs/跟进信/README-跟进信清单.md`；g2 `git add "docs/跟进信/README-跟进信清单.md"`；g3 `git add docs/跟进信/回件/a.md docs/session接力.md`；g4 `git add -- docs/session接力.md`；g5 `git add docs/session接力.md`（纯 ASCII 目录对照：另建 `notes/x.md` 并临时加 `Bash(git add notes/:*)` 试）。逐项记放行/需审批原文。
定因：非 ASCII 路径？引号？多参数？`git -c core.quotepath` 无关（那是输出）。
【三、修（先红后绿）】按实验结论选**最小**放宽，候选依次试，取第一个使 g1–g4 全放行且 `git add -A`/`git add .`/`git add tools/x.py`/`git add docs/openers/x.md` 仍被拒的：
- 甲：每个路径再加引号变体 `Bash(git add "<path>:*)`
- 乙：若非 ASCII 前缀根本不匹配 ⇒ 改为放行 `Bash(git add:*)` ＋ deny 追加 `Bash(git add tools:*)`、`Bash(git add app:*)`、`Bash(git add scripts:*)`、`Bash(git add tests:*)`、`Bash(git add openspec:*)`、`Bash(git add .claude:*)`、`Bash(git add CLAUDE.md:*)`、`Bash(git add data:*)`、`Bash(git add docs/openers:*)` 及各自加引号变体 —— 且在一次性仓实测 `git add tools/x.py` 被拒
- 丙：章程侧无法约束写法时，改由前言明示「git add 每次只加一个路径、不加引号」——⛔ 只能作为甲/乙之上的补充，不能单独作为修法（红线仍须在权限层）
同时：删掉无效的 `Write(...)` allow/deny 规则（告警原文附报告），确认 `Edit(...)` 规则覆盖 Write 工具（一次性仓实测 Write 越界文件被拒）。
测试：新增断言覆盖实验定下的写法；`test_unpack_path_guard.py`、`test_unpack_charter_allowlist.py` 同步；argv 里不再有 `Write(`。
【四、验收】新测试先红后绿；`tools/liaison/tests` 与全量 pytest 0 失败（报数）；`openspec validate liaison-reply-bridge-and-patrol --strict` 过；一次性仓用新 argv ＋ 真章程 prompt 再跑 g1–g4 与越界项，附原文
【五、落档收口】worktree 内 `docs/tech-debt.md` 新增 `## ~~TD-50~~ · 拆件会话 git add 章程路径被判需审批 ✅ 已还（0917Q）`（号取当前最大号+1；含首次真实回件日志证据、实验表、选中写法）；⛔ 不改 `docs/session接力.md`、`docs/跟进信/**`。ff 合并 main（失败 ⇒ worktree 内 rebase main 重测再 ff，仍失败 PARTIAL）→ push → 真合校验（rev-list/cherry）。并发协议同前（只 add 本条路径；禁 -A/./commit -a/stash；不删锁）。收工三行：根因与写法 ｜ passed·failed ｜ commit。最后一行顶格 OPENER_DONE
```

> ✅ 已完成 2026-09-17（PARTIAL）· 原泳道 5点3回件git-add放行（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Cowork 经 launchd 请求文件发车），名字另由 -n 给。

```
[Mac]0917R-清会话半成品并重跑一轮验5点3
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: main ｜ worktree: ❌ 不勾（真实服务、真实信号与台账）｜ 工作区: 仓库根 ｜ 派发: Cowork·HR业务线-接力0917D

无人值守：⛔ 不提问；⛔ 不发任何企微消息；⛔ 不请任何人发消息。「停下报」＝落档后 OPENER_PARTIAL: <原因>。

【一、前置】`git log --oneline -n 20 | grep -c 0917Q` ≥1 且 `grep -n '~~TD-50~~' docs/tech-debt.md docs/archive/tech-debt-已还.md` 命中；否则 PARTIAL: 0917Q 未合入

【二、先提交 Cowork 文档，再清会话半成品】
1. `git status --short`；只 add `docs/openers/OP-0820-全量编排.md`、`docs/openers/号池台账.md`（若 M）→ commit `docs(openers): 派 0917Q/0917R（0917R）` → push。⛔ 这一步不 add `docs/session接力.md` 与 `docs/跟进信/**`
2. 备份会话半成品：`mkdir -p .claude/handoff/0917R-会话半成品 && cp "docs/跟进信/回件/人事部#1-20260917.md" docs/跟进信/README-跟进信清单.md docs/session接力.md .claude/handoff/0917R-会话半成品/`
3. 把 `docs/session接力.md` 还原到 HEAD 后，只重新应用 Cowork 的 R-3b/号池改动（`git diff HEAD -- docs/session接力.md` 看清：会话追加的是末尾「2026-09-17 拆件会话（自动）处理 人事部#1…」那一行；Cowork 改的是【下一步】表内行）。做法：用 python 读当前文件，删除以 `- 2026-09-17 拆件会话（自动）处理` 开头的那一行（及其前面紧邻的空行，若是会话加的），其余保持
4. 删会话写的回件文件：`git rm --cached` 不适用（未跟踪）⇒ `mv "docs/跟进信/回件/人事部#1-20260917.md" .claude/handoff/0917R-会话半成品/moved-回件.md`；目录空则 `rmdir docs/跟进信/回件`
5. 台账 `README-跟进信清单.md`：**保持当前第九态不动**（这是第二条回件的桥打标，合法状态）。核 `grep '^| \`人事部#1\`'` 那行以 `📨 回件已到，待拆件` 开头且含 `━━━ 原状态 ━━━ ✅ 已推送 2026-09-09`；不符 ⇒ PARTIAL 并附原文
6. 把第九态台账作为测试基线提交：add `docs/跟进信/README-跟进信清单.md` `docs/session接力.md` → commit `docs(liaison): 人事部#1 第九态基线（桥打标，重跑前）（0917R）` → push。记 H0=`git rev-parse HEAD`
7. `cat data/liaison/unpack-signal.json` 应仍有两项（b8b523c7…、a932acca…）；不是 ⇒ 记原文继续

【三、重启 ＋ 重跑一轮】
1. `launchctl kickstart -k gui/$UID/com.zhuopin.hr.liaison`，30 秒后 running、pid 变、无新 ERROR
2. `PYTHONPATH=. tools/liaison/.venv/bin/python -m tools.liaison unpack-dispatch --dry-run` 抄 argv（应无 `Write(`）
3. `… unpack-dispatch --force`（带真章程，会话会自己 probe 到这两项信号）；按锁 pid 等结束，最多 15 分钟，超时 TERM/KILL
4. 抄日志全文到 findings（≤200 行则全抄，否则首尾各 40 行）
【四、判 5.3（⛔ 本轮之后不 push 会话提交）】
- 台账：两条都是测试消息 ⇒ 预期最终还原为 `✅ 已推送 2026-09-09`（非实质）；若会话把第二条判待人（台账已不在第九态）也可接受，记原文
- 审计：`python3 - <<'PY'` 用 sqlite3 读 `liaison_unpack_audit`，应有两次 `bridge_marked` 与至少一次 `dispatch_started`（09:45 那次）
- 信号：`--probe` ＝ `[NO-SIGNAL]`（或剩余项与会话「待人」登记一致）
- git：`git log --oneline H0..HEAD` 有会话 commit，每条 `git show --name-only` 路径全在章程 §三 清单；`git status` 无越界改动；**⛔ 不 push 这些会话 commit**
- 全部满足 ⇒ 勾 tasks.md 5.3
【五、落档（⚠️ 会话 commit 未 push 的前提下，本 session 的落档也只 commit 不 push，并在报告写明 main 领先 origin 的 commit 列表，交 Shao Peishen/下一条决定何时推）】
- `docs/findings/2026-09-17-打标即开班真实起活实测.md` 追加「5.3 真实回件（0917R）」：两条原文、审计四行、日志、H0..HEAD 提交与路径核对、台账前后
- 若 5.3 过：tasks.md 勾 5.3、抬头 32/33；`docs/session接力.md` R-3 改为「5.3 ✅，只剩 5.5 落档＋归档」
- add findings、tasks.md、`docs/session接力.md` → commit `docs(liaison): 5.3 真实回件端到端（0917R）`，⛔ 不 push
- 收工三行：5.3 ✅/❌ ｜ 本地领先 origin 的 commit 列表 ｜ ⏸ 下一步：push 与 5.5 归档
- 最后一行顶格 OPENER_DONE；否则 OPENER_PARTIAL: <哪步>
```

## 5.3 订正落档与推送（2026-09-17 10:2x Cowork·HR业务线-接力0917D）

> 泳道：5点3订正落档
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Cowork 经 launchd 请求文件发车），名字另由 -n 给。

```
[Mac]0917S-5点3订正落档与推送
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: main ｜ worktree: ❌ 不勾（只提交 Cowork 已写好的文档并推送）｜ 工作区: 仓库根 ｜ 派发: Cowork·HR业务线-接力0917D

无人值守：⛔ 不提问。背景：`0917R` 把 Cowork 出的《人事部#1 答复单》两份文件误判为子会话越界，5.3 未勾。Cowork 已在 findings 追加订正节、tasks 勾 5.3（32/33）、接力 R-3 改写、号池登记本条。
1. `git status --short` 应含：M `docs/findings/2026-09-17-打标即开班真实起活实测.md` ｜ M `openspec/changes/liaison-reply-bridge-and-patrol/tasks.md` ｜ M `docs/session接力.md` ｜ M `docs/openers/OP-0820-全量编排.md` ｜ M `docs/openers/号池台账.md` ｜ ?? `docs/跟进信/人事部-汤丽萍-答复单-2026-09-17-人事部#1决策点a.md` 与同名 `.docx`。⚠️ 若此刻 `docs/跟进信/README-跟进信清单.md`、`docs/跟进信/回件/**` 有改动或 `data/liaison/unpack-session.lock` 的 pid 存活（`ps -p`）⇒ 汤丽萍的真实回件正在被拆件会话处理：⛔ 不 add 那些文件，只提交本条列出的七个路径
2. `head -1 openspec/changes/liaison-reply-bridge-and-patrol/tasks.md` 含 `32/33`；`grep -n '^- \[x\] 5\.3'` 命中；`openspec validate liaison-reply-bridge-and-patrol --strict` 过；`./venv/bin/python -m pytest -q tests/test_doc_size_budget.py 2>&1 | tail -1` 0 失败。任一不过 ⇒ OPENER_PARTIAL: <原因>
3. `git add` 上面七个路径（逐个写出，⛔ 禁 -A／./commit -a／stash；⛔ 不 add `_to_delete/`）→ `git commit -m "docs(liaison): 5.3 判通过（订正 0917R 越界误判）＋ 人事部#1 答复单（0917S）"`
4. push（带上 `b2ec31d`/`d703cd3`/`f280d27`：5.3 的「会话提交未 push」判据已在 `0917R` 核验留证，验证期已过）。被拒 ⇒ `pull --rebase --autostash` 重试最多 3 次；`index.lock` 等 5 秒重试最多 5 次，⛔ 不删锁
5. `git rev-list --left-right --count HEAD...@{u}` ＝ `0 0`
6. 最后一行顶格输出 OPENER_DONE（不加粗、不加任何符号）
```
