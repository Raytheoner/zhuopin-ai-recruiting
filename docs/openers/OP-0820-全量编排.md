# OP-0820 · 全量 Opener 编排

> 2026-08-20。**唯一权威 opener 清单**，`OP-0820-A_单元BC开工.md` 已作废。
>
> **编排原则**：不依赖人工控制执行节奏。有依赖就彻底串行，能并发的全部并行。
> 并发安全靠《并发协议》做到机器自愈，**不靠"你记得别同时开"**。
>
> **格式**：每个代码块头两行是标题行 + 【设置】行，整块复制即可，不要拆。

---

## 🔢 号池台账 → 已迁至 `docs/openers/号池台账.md`

> 〔归档工具〕 2026-09-16：出号前查、给出后登记，一律改在该文件；历史月份的行在 `docs/openers/归档/号池台账-归档.md`。

## 二、并发协议（**每条 opener 内已逐字内嵌，此处备查**）

1. **只 `git add` 本条 opener 明确列出的路径。** ⛔ 禁止 `git add -A` / `git add .` / `git commit -a`。这是整套并行编排成立的唯一前提。
2. **`git status` 里出现别人的改动是正常的**——那是并行的另一条 opener 在干活。不要停下、不要问、不要顺手提交。
3. ⛔ 不要在 commit 前 pull（并发下会被别人的未提交改动挡住）；push 被拒才 `git pull --rebase --autostash origin main` 重试。
4. push 被拒（non-fast-forward）→ 再 `git pull --rebase origin main` 后重试，**最多 3 次**，仍失败停下贴错误。
5. 报 `.git/index.lock: File exists` → **等 5 秒重试，最多 5 次**。⛔ **绝不删除该锁文件**——那是另一个 session 正在用，删了会毁掉它的提交。

---

## 待执行区

> 〔归档工具〕 2026-09-16：`lane-dispatch` 新写的无头 opener 放在本节之后（`> 泳道：<名>` ＋ 代码块）。
> 〔归档工具〕 跑完被 `mark_done` 摘标注的批次，下次 `python3 scripts/archive_docs.py --apply` 会整段搬去归档。

> ✅ 已完成 2026-09-16（OK）· 原泳道 liaison测试脚手架收尾（脚本自动摘，不再进 run-lanes）

```
[Mac]0916E-TD29收尾liaison测试脚手架收编与config文案
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ✅ 勾（写代码）｜ 工作区: worktree（`.claude/worktrees/td29-liaison-test-scaffolding-0916E`）｜ 派发: run-lanes.sh

【零、背景与授权】TD-29 剩余两条，Shao Peishen 2026-09-10 已答 `4a`：**下一批泳道单独派一条**，独占
`tools/liaison/tests/` 与 `tools/liaison/config.py`，⛔ 不挂 8.6 灰度前置、⛔ 不留着不做。全文见
`docs/tech-debt.md` TD-29 条目「仍欠两条」。这是**纯重构 + 文案修正，不改变外部可观察行为**，按
CLAUDE.md「轻量通道」走：⛔ 不用 `spec-to-plan`/`run-build`，直接 TDD 单条改完提交
（`superpowers:test-driven-development`；调不到 ⇒ 登记「⏸ 留步：superpowers 不可达」并停，⛔ 不手工按磁盘 SKILL.md 走）。

【一、前置自检】
1. `pwd` 必须含 `.claude/worktrees/`，否则停下报
2. `git log --oneline -5 -- tools/liaison/tests/test_notify_store.py | grep -c 'assert_group_notify_identity'` 之类的旧顾虑已随
   `5ca1f50` 处置，本条不需要再验；只需确认 `grep -n 'assert_group_notify_identity' tools/liaison/tests/test_notify_store.py` 命中 ≥2 处（存在即可，⛔ 不是本条任务）

【二、任务 A · conftest 收编（三处复制粘贴）】
现状（已核实的确切行号，实现前重新 grep 一遍确认未漂移）：
- `FakeClock`：`tools/liaison/tests/test_notify_ratelimit.py:14` 与 `tools/liaison/tests/test_notify_webhook.py:63` 两份定义，逐字节比对是否相同
- `RecordingSink`：`tools/liaison/tests/test_notify_store.py:29` 与 `tools/liaison/tests/test_notify_webhook.py:76` 两份
- `FAKE_WEBHOOK`：`tools/liaison/tests/test_notify_transport.py:25` 与 `tools/liaison/tests/test_notify_webhook.py:15` 两份（值＝`https://example.invalid/cgi-bin/webhook/send?key=fake-key-for-tests`，⛔ 改动中不得把这个假值换成真实域名）

步骤：
1. 若两份定义逐字节不同，先登记差异、以更完整/更新的那份为准，⛔ 不擅自改逻辑
2. 三者收进 `tools/liaison/tests/conftest.py`，以 pytest fixture 或模块级常量形式导出（`FakeClock`/`RecordingSink` 用 `@pytest.fixture` 更符合 pytest 惯例，但若两处原调用点是直接实例化类而非注入，保持调用点最小改动优先——不确定就选影响面小的那种）
3. 四个原文件改为从 `conftest.py`（同目录自动可见，无需显式 import fixture；若是模块级常量/类则需要 `from .conftest import ...` 或按 pytest 惯例处理）里取用，删除本地重复定义
4. 跑 `tools/liaison/tests/` 全量，四份原文件涉及的用例必须逐条仍过，且总数不减少（收编不是删测试）

【三、任务 B · config.py:75 异常文案错位】
现状：`tools/liaison/config.py:70-83` 的 `load_group_webhook` 复用 `tools/liaison/errors.py` 的
`MissingCredentialsError`，其消息固定「HR 值守通道**拒绝启动**：…」（`errors.py:19`）。但
`load_group_webhook` 是**发送期**校验（docstring 已明写"⛔ 刻意不加进 REQUIRED_CREDENTIAL_ENV_NAMES"，
理由是收消息与发群通知独立、不能连坐），服务本身仍在正常运行，只是这一次群通知发不出去。
运维看到"拒绝启动"字样会误判成服务整体宕了。

步骤：
1. 在 `tools/liaison/errors.py` 新增一个独立异常（例如 `GroupWebhookMissingError(RuntimeError)`），
   构造签名与 `MissingCredentialsError` 一致（`missing_names: Iterable[str]`，消息同样⛔ 只出现变量名不出现取值），
   但文案改为准确描述"群通知发送地址缺失，本次跳过发送"一类措辞，**不含"拒绝启动"字样**
2. `config.py:83` 的 `raise MissingCredentialsError([GROUP_WEBHOOK_ENV])` 改成 `raise GroupWebhookMissingError([GROUP_WEBHOOK_ENV])`
3. 调用方核对并同步：`tools/liaison/followup.py:216-217`（`except MissingCredentialsError` 那块，
   若捕获的正是 `load_group_webhook` 抛出的这个异常则要改成捕获新类型；若那里捕获的是别的调用点的
   `MissingCredentialsError` 则不动，只改群通知这条路径）、`tools/liaison/notify/webhook.py`（若有捕获此异常的逻辑同样核）
4. `tools/liaison/tests/test_notify_transport.py:333-366` 四处 `config.load_group_webhook(...)` 相关用例，
   涉及"抛异常"的断言改成断言新异常类型 `GroupWebhookMissingError`（⛔ 不要只改字符串匹配，要改类型）
5. 先红后绿：新增/改动的用例在改代码前跑必须红，改完再绿

【四、全量回归】`./venv/bin/python -m pytest tools/liaison/tests/ -q` 与仓库根 `./venv/bin/python -m pytest -q` 均需
0 失败，且总用例数 ≥ 改动前（收编与改名不得导致用例被误删）。

【五、收口】
1. `git add tools/liaison/tests/conftest.py tools/liaison/tests/test_notify_ratelimit.py tools/liaison/tests/test_notify_store.py tools/liaison/tests/test_notify_webhook.py tools/liaison/tests/test_notify_transport.py tools/liaison/errors.py tools/liaison/config.py tools/liaison/followup.py tools/liaison/notify/webhook.py docs/tech-debt.md`
   （最后一项：把 TD-29「仍欠两条」改成 `✅ 已还`，附本次 commit hash）
2. commit（一条即可），走 `finishing-a-development-branch` 合回 `main`
3. 合并后 `git rev-list --count main..<分支>`；非 0 时再跑 `git cherry -v main <分支>` 确认全 `-`（真合）
4. 确认内容真在 main 上之前 ⛔ 不许输出 `OPENER_DONE`

【六、并发协议（已逐字内嵌，本批只有一条泳道，仍照抄）】
1. 只 `git add` 本条明确列出的路径。⛔ 禁止 `git add -A` / `git add .` / `git commit -a` / `git stash`
2. `git status` 里出现别人的改动是正常的，不要停下、不要问、不要顺手提交
3. push 被拒 → `git pull --rebase --autostash origin main` 后重试，最多 3 次
4. 报 `.git/index.lock` 已存在 → 等 5 秒重试最多 5 次，⛔ 默认绝不删除该锁
```

> ✅ 已完成 2026-09-16（OK，commit 555592a；脚本判 NO-SENTINEL 系哨兵被加粗成 `**OPENER_DONE**`，Cowork 核验后手工摘）· 原泳道 token治理0916K
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Paul 在手机上，2026-09-16 由 Cowork 经 launchd 请求文件发车），名字另由 -n 给。🔴 若改为手工贴进 CC Desktop，必须自行补上第 3 行。

```
[Mac]0916K-Token治理-泳道开工整读大文件与提醒误停根治
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（改项目 hook、执行器与共享文档，要在主检出一次改完一次提交）｜ 工作区: 仓库根 ｜ 派发: Cowork·Token治理-0916

读 docs/openers/0916K-Token治理-泳道开工整读大文件与提醒误停根治.md 全文并逐节执行（该文件按分段读，每次 Read 带 offset＋limit ≤ 400 行），以该文件为准。文件不存在 → 输出 OPENER_PARTIAL: 正文文件不存在 并停。
无头补充（覆盖正文里对应说法）：
1. 正文【一】3 的 git status 预期里另有 `M docs/openers/OP-0820-全量编排.md`（本块自身），属正常；【七】2 的 git add 列表追加 `docs/openers/OP-0820-全量编排.md`。
2. 本 session 就是由 run-lanes.sh 起的泳道：正文【一】2「pgrep run-lanes 为空否则停」**跳过**（会命中自己的父进程）。
3. ⛔ 不修改 `docs/openers/run-lanes.sh`（它正在执行本泳道）。若【二】2 的失败只能靠改 run-lanes.sh 解决 → 不改，登记「⏸ 留步：run-lanes.sh 需改，待非泳道会话处理」，其余段照做，最后 OPENER_PARTIAL。
4. 凡正文写「停下报我」，一律改为：登记原因、跳过该段后面依赖它的步骤、最终输出 OPENER_PARTIAL: <原因>，⛔ 不提问。
5. 收工报告写进 `.claude/handoff/0916K-收工报告.md`（正文【八】各项），并在终端输出里同样给出。
```

> ✅ 已完成 2026-09-16（OK）· 原泳道 token治理0916L（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动（Cowork 经 launchd 请求文件发车），名字另由 -n 给。

```
[Mac]0916L-哨兵加粗容错与0916K摘标注提交
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（只提交 Cowork 已写好的两处小改动）｜ 工作区: 仓库根 ｜ 派发: Cowork·Token治理-0916

背景：0916K 活已干完并提交（555592a），但哨兵输出成 `**OPENER_DONE**` 被判 NO-SENTINEL、泳道标注没摘，下一批会重跑它。Cowork 已改好（未提交）：run-lanes.sh 哨兵正则容忍加粗／反引号；tests/test_run_lanes_model.py 加一条用例；OP-0820 把 0916K 标注换成已完成行，并追加本块。
1. `git pull --rebase --autostash origin main`；`git status --short` 本件相关应只有：M docs/openers/run-lanes.sh ｜ M tests/test_run_lanes_model.py ｜ M docs/openers/OP-0820-全量编排.md ｜ M docs/openers/号池台账.md
2. `bash -n docs/openers/run-lanes.sh && ./venv/bin/python -m pytest -q tests/test_run_lanes_model.py 2>&1 | tail -2` → 0 失败；失败 → 不提交，输出 OPENER_PARTIAL: 测试失败 <末行>
3. ⛔ 不修改 run-lanes.sh（它正在执行本泳道），只提交
4. git add docs/openers/run-lanes.sh tests/test_run_lanes_model.py docs/openers/OP-0820-全量编排.md docs/openers/号池台账.md
5. git commit -m "fix(run-lanes): 哨兵容忍加粗/反引号 ＋ 摘 0916K 标注（0916L）"，按并发协议 push
6. 最后一行顶格输出 OPENER_DONE（不加粗、不加任何符号）
```

## 第十七批·波次 1（2026-09-16 Cowork 远程发车，Paul 在手机上）

> ✅ 已完成 2026-09-16（OK）· 原泳道 P0回件桥计划（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动，名字另由 -n 给。

```
[Mac]0910G-P0回件桥实现计划
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（spec-to-plan 只产出一份 markdown 计划，不写代码不建分支——skill 明写「在这一步建 worktree 只会多一层目录切换的混乱」）｜ 工作区: 仓库根（/Users/paulshao/Projects/HumanResource）｜ 派发: Cowork·HR业务线-接力0909AU

读 docs/openers/0910G-P0回件桥实现计划.md 全文并逐节执行（分段读，每次 Read 带 offset＋limit ≤ 400 行），以该文件为准。文件不存在 → 输出 OPENER_PARTIAL: 正文文件不存在 并停。
无头补充（覆盖正文里对应说法）：
1. 凡正文写「停下报」「停下报我」，一律改为：登记原因、不做依赖它的后续步骤、最终顶格输出 OPENER_PARTIAL: <原因>；⛔ 不提问。
2. 【零】1 的 §0 三门槛已于 2026-09-16 全勾（0910A，commit 53f56f8）；帧字段映射走的是**实际字段**（fa4c17a，FIELD_PATHS 已按真实帧填、thread_id 按 chattype 分叉）——写计划时以 `tools/liaison/frames.py` 现状为准。
3. `Skill(superpowers:writing-plans)` 调不到 ⇒ OPENER_PARTIAL: superpowers 不可达（⛔ 不手工按磁盘 SKILL.md 走）。
4. 本批四条同时在跑，只 git add 本条计划文件；OP-0820-全量编排.md 不归本条提交。
5. 收工报告（正文【六】各项）在终端输出里给全，最后一行顶格输出 OPENER_DONE 或 OPENER_PARTIAL: <原因>（不加粗、不加符号）。
```

> ✅ 已完成 2026-09-16（OK）· 原泳道 P1开班计划（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动，名字另由 -n 给。

```
[Mac]0910H-P1打标即开班实现计划
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（spec-to-plan 只产出一份 markdown 计划，不写代码不建分支——skill 明写「在这一步建 worktree 只会多一层目录切换的混乱」）｜ 工作区: 仓库根（/Users/paulshao/Projects/HumanResource）｜ 派发: Cowork·HR业务线-接力0909AU

读 docs/openers/0910H-P1打标即开班实现计划.md 全文并逐节执行（分段读，每次 Read 带 offset＋limit ≤ 400 行），以该文件为准。文件不存在 → 输出 OPENER_PARTIAL: 正文文件不存在 并停。
无头补充（覆盖正文里对应说法）：
1. 凡正文写「停下报」「停下报我」，一律改为：登记原因、不做依赖它的后续步骤、最终顶格输出 OPENER_PARTIAL: <原因>；⛔ 不提问。
2. 【零】1 的 §0 三门槛已于 2026-09-16 全勾（0910A，commit 53f56f8）；帧字段映射走的是**实际字段**（fa4c17a，FIELD_PATHS 已按真实帧填、thread_id 按 chattype 分叉）——写计划时以 `tools/liaison/frames.py` 现状为准。
3. `Skill(superpowers:writing-plans)` 调不到 ⇒ OPENER_PARTIAL: superpowers 不可达（⛔ 不手工按磁盘 SKILL.md 走）。
4. 本批四条同时在跑，只 git add 本条计划文件；OP-0820-全量编排.md 不归本条提交。
5. 收工报告（正文【六】各项）在终端输出里给全，最后一行顶格输出 OPENER_DONE 或 OPENER_PARTIAL: <原因>（不加粗、不加符号）。
```

> ✅ 已完成 2026-09-16（OK）· 原泳道 P2章程计划（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动，名字另由 -n 给。

```
[Mac]0910I-P2拆件章程正本实现计划
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（spec-to-plan 只产出一份 markdown 计划，不写代码不建分支——skill 明写「在这一步建 worktree 只会多一层目录切换的混乱」）｜ 工作区: 仓库根（/Users/paulshao/Projects/HumanResource）｜ 派发: Cowork·HR业务线-接力0909AU

读 docs/openers/0910I-P2拆件章程正本实现计划.md 全文并逐节执行（分段读，每次 Read 带 offset＋limit ≤ 400 行），以该文件为准。文件不存在 → 输出 OPENER_PARTIAL: 正文文件不存在 并停。
无头补充（覆盖正文里对应说法）：
1. 凡正文写「停下报」「停下报我」，一律改为：登记原因、不做依赖它的后续步骤、最终顶格输出 OPENER_PARTIAL: <原因>；⛔ 不提问。
2. 【零】1 的 §0 三门槛已于 2026-09-16 全勾（0910A，commit 53f56f8）；帧字段映射走的是**实际字段**（fa4c17a，FIELD_PATHS 已按真实帧填、thread_id 按 chattype 分叉）——写计划时以 `tools/liaison/frames.py` 现状为准。
3. `Skill(superpowers:writing-plans)` 调不到 ⇒ OPENER_PARTIAL: superpowers 不可达（⛔ 不手工按磁盘 SKILL.md 走）。
4. 本批四条同时在跑，只 git add 本条计划文件；OP-0820-全量编排.md 不归本条提交。
5. 收工报告（正文【六】各项）在终端输出里给全，最后一行顶格输出 OPENER_DONE 或 OPENER_PARTIAL: <原因>（不加粗、不加符号）。
```

> ✅ 已完成 2026-09-16（OK）· 原泳道 P3台账计划（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动，名字另由 -n 给。

```
[Mac]0910J-P3口径点台账实现计划
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（spec-to-plan 只产出一份 markdown 计划，不写代码不建分支——skill 明写「在这一步建 worktree 只会多一层目录切换的混乱」）｜ 工作区: 仓库根（/Users/paulshao/Projects/HumanResource）｜ 派发: Cowork·HR业务线-接力0909AU

读 docs/openers/0910J-P3口径点台账实现计划.md 全文并逐节执行（分段读，每次 Read 带 offset＋limit ≤ 400 行），以该文件为准。文件不存在 → 输出 OPENER_PARTIAL: 正文文件不存在 并停。
无头补充（覆盖正文里对应说法）：
1. 凡正文写「停下报」「停下报我」，一律改为：登记原因、不做依赖它的后续步骤、最终顶格输出 OPENER_PARTIAL: <原因>；⛔ 不提问。
2. 【零】1 的 §0 三门槛已于 2026-09-16 全勾（0910A，commit 53f56f8）；帧字段映射走的是**实际字段**（fa4c17a，FIELD_PATHS 已按真实帧填、thread_id 按 chattype 分叉）——写计划时以 `tools/liaison/frames.py` 现状为准。
3. `Skill(superpowers:writing-plans)` 调不到 ⇒ OPENER_PARTIAL: superpowers 不可达（⛔ 不手工按磁盘 SKILL.md 走）。
4. 本批四条同时在跑，只 git add 本条计划文件；OP-0820-全量编排.md 不归本条提交。
5. 收工报告（正文【六】各项）在终端输出里给全，最后一行顶格输出 OPENER_DONE 或 OPENER_PARTIAL: <原因>（不加粗、不加符号）。
```

> ✅ 已完成 2026-09-16（OK）· 原泳道 收口提交0916M（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 无头启动（Cowork 经 launchd 发车）。

```
[Mac]0916M-波次1收口与P7A路规则提交
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（只提交 Cowork 已写好的文档与脚本）｜ 工作区: 仓库根 ｜ 派发: Cowork·Token治理-0916

背景：第十七批波次 1 四条计划泳道已 OK 并各自提交；Cowork 另写好（未提交）：lane-dispatch skill「长 run-build 按 Task 拆成同泳道串行多条」规则、scripts/opener_split_check.py ＋ tests/test_opener_split_check.py、路线图 Phase 7 状态行、P2-AB基线 A/B 记账两行、编排文件里波次 1 四个块与本块（及 run-lanes 自动摘的标注）、号池台账 0916M 行。
1. `git pull --rebase --autostash origin main`；`git status --short` 本件相关应只有：M .claude/skills/lane-dispatch/SKILL.md ｜ M docs/openers/OP-0820-全量编排.md ｜ M docs/token治理/路线图.md ｜ M docs/token治理/P2-AB基线.md ｜ M docs/openers/号池台账.md ｜ ?? scripts/opener_split_check.py ｜ ?? tests/test_opener_split_check.py。出现别的改动照常存在、不提交。
2. `./venv/bin/python -m pytest -q tests/test_opener_split_check.py tests/test_doc_size_budget.py 2>&1 | tail -2` → 0 失败；失败 → 不提交，输出 OPENER_PARTIAL: 测试失败 <末行>
3. `python3 scripts/opener_split_check.py; echo rc=$?` → rc=0
4. `python3 scripts/token_ledger.py --since 2026-09-16 --out /tmp/ledger-0916m`；从 /tmp/ledger-0916m.md 的成本 Top 表里取 0910G／0910H／0910I／0910J／0916K／0916L 六条的「$」列，把 docs/token治理/P2-AB基线.md 里两处「待账本」替换成对应合计（写成「$x.x（均 $y.y）」）；某条账本未命中就写「账本未命中」
5. git add .claude/skills/lane-dispatch/SKILL.md scripts/opener_split_check.py tests/test_opener_split_check.py docs/token治理/路线图.md docs/token治理/P2-AB基线.md docs/openers/OP-0820-全量编排.md docs/openers/号池台账.md
6. git commit -m "chore(token治理): P7·A 路拆分规则与机器闸 ＋ 波次 1 收口记账（0916M）"，按并发协议 push
7. 终端输出六条泳道成本明细一行一条，最后一行顶格输出 OPENER_DONE（不加粗、不加符号）
```

## 第十七批·波次 2（2026-09-16 Cowork 远程发车，Paul 答「发波次 2」）

> 一条泳道全串行（P0→P1→P2→P3 真依赖；`__main__.py` 被 P0/P1/P3 共同触碰）。按 P7·A 路规则每单元拆成 ≤3 Task 的连续几条，末条才 final review／合 main／回勾。

> ✅ 已完成 2026-09-16（OK）· 原泳道 第十七批波次2（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动，名字另由 -n 给。

```
[Mac]0916N-P0建造Task1至3
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: wave2-p0-bridge ｜ worktree: ✅ 勾（写代码）｜ 工作区: .claude/worktrees/wave2-p0-bridge ｜ 派发: Cowork·Token治理-0916

【零、范围】`liaison-reply-bridge-and-patrol` 单元 P0，计划 `docs/superpowers/plans/2026-09-10-liaison-reply-bridge.md` 的 **Task 1–3**（本单元共 3 条中的第 1 条）。⛔ 只做这几个 Task，⛔ 不越到下一段。

【一、前置——任一不过即输出 OPENER_PARTIAL: <原因> 并停，⛔ 不提问】
1. `grep -n '^- \[.\] 0\.[123]' openspec/changes/liaison-reply-bridge-and-patrol/tasks.md` 三条都是 `[x]`
2. `grep -c '^### Task ' docs/superpowers/plans/2026-09-10-liaison-reply-bridge.md` 与计划 Task 总数一致，且 Task 1–3 都存在
3. `Skill(superpowers:subagent-driven-development)` 可达；调不到 ⇒ OPENER_PARTIAL: superpowers 不可达（⛔ 不手工按磁盘 SKILL.md 走）
4. `git log --oneline -1 -- docs/superpowers/plans/2026-09-10-liaison-reply-bridge.md` 有提交（计划已在 main）

【二、建工作区（本单元第一条才做）】
1. 主工作区 `git pull --rebase --autostash origin main`；`git worktree add .claude/worktrees/wave2-p0-bridge -b wave2-p0-bridge main`（已存在同名分支/目录 ⇒ OPENER_PARTIAL: 工作区已存在，⛔ 不删不覆盖）
2. 按 `run-build` skill「建完 worktree 后必须手工补三样」补齐：venv（软链或在 worktree 内按 requirements 建，含 `tools/liaison/.venv`）、`.env` 拷贝（确认 `git check-ignore .env` 命中后才拷）、其余照 skill 表
3. `cd .claude/worktrees/wave2-p0-bridge` 后跑一次测试基线：`./venv/bin/python -m pytest -q tools/liaison/tests 2>&1 | tail -3` 记下

【三、执行】
调 `Skill(run-build)`（即 `superpowers:subagent-driven-development`）执行计划的 **Task 1–3**：每 Task 派实现子代理＋两阶段 review，逐 Task 提交在 `wave2-p0-bridge` 分支上。计划里有 Global Constraints 段，照原样交给 reviewer。
⛔ 本条**不做**：全分支 final review、合 main、删 worktree、回勾 tasks.md——这些留给本单元最后一条。做完 Task 3 即收工。

【守住】
- ⛔ 不重启、不 kickstart 值守服务（`com.zhuopin.hr.liaison`）；⛔ 不碰 `data/liaison.db`、`.env` 内容进 git、`_to_delete/`
- ⛔ 不做 §5 真实起活实测（不可代，Paul 本人）；⛔ 不改 `openspec/` 下 spec/design；⛔ 不改 `docs/openers/run-lanes.sh`
- 读大文件分段（Read 带 offset＋limit ≤ 400 行），计划文件同样分段读
- 并发协议：⛔ 禁止 git add -A / . / -a；index.lock 等 5 秒重试 5 次不删；push 被拒才 pull --rebase --autostash

【收工】终端输出：做了哪几个 Task、各 Task 两阶段 review 结论、pytest 基线→收工、commit hash 列表。最后一行顶格输出 OPENER_DONE 或 OPENER_PARTIAL: <原因>（不加粗、不加符号）。
```

> ✅ 已完成 2026-09-16（OK）· 原泳道 第十七批波次2（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动，名字另由 -n 给。

```
[Mac]0916O-P0建造Task4至6
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: wave2-p0-bridge ｜ worktree: ✅ 勾（写代码）｜ 工作区: .claude/worktrees/wave2-p0-bridge ｜ 派发: Cowork·Token治理-0916

【零、范围】`liaison-reply-bridge-and-patrol` 单元 P0，计划 `docs/superpowers/plans/2026-09-10-liaison-reply-bridge.md` 的 **Task 4–6**（本单元共 3 条中的第 2 条）。⛔ 只做这几个 Task，⛔ 不越到下一段。

【一、前置——任一不过即输出 OPENER_PARTIAL: <原因> 并停，⛔ 不提问】
1. `grep -n '^- \[.\] 0\.[123]' openspec/changes/liaison-reply-bridge-and-patrol/tasks.md` 三条都是 `[x]`
2. `grep -c '^### Task ' docs/superpowers/plans/2026-09-10-liaison-reply-bridge.md` 与计划 Task 总数一致，且 Task 4–6 都存在
3. `Skill(superpowers:subagent-driven-development)` 可达；调不到 ⇒ OPENER_PARTIAL: superpowers 不可达（⛔ 不手工按磁盘 SKILL.md 走）
4. `git -C .claude/worktrees/wave2-p0-bridge status --short` 能执行（worktree 在）；`git log wave2-p0-bridge --oneline -15` 能看到本单元前一条（Task 1–3）的提交；superpowers 进度台账（worktree 内 `.superpowers/sdd/2026-09-10-liaison-reply-bridge/progress.md`，路径以 skill 实际为准）显示 Task 1–3 已完成。任一不符 ⇒ OPENER_PARTIAL: 前一条未完成

【二、进入工作区】
`cd .claude/worktrees/wave2-p0-bridge`；`git rebase main`（有冲突 ⇒ `git rebase --abort`，OPENER_PARTIAL: rebase 冲突）；测试基线 `./venv/bin/python -m pytest -q tools/liaison/tests 2>&1 | tail -3` 记下

【三、执行】
调 `Skill(run-build)`（即 `superpowers:subagent-driven-development`）执行计划的 **Task 4–6**：每 Task 派实现子代理＋两阶段 review，逐 Task 提交在 `wave2-p0-bridge` 分支上。计划里有 Global Constraints 段，照原样交给 reviewer。
⛔ 本条**不做**：全分支 final review、合 main、删 worktree、回勾 tasks.md——这些留给本单元最后一条。做完 Task 6 即收工。

【守住】
- ⛔ 不重启、不 kickstart 值守服务（`com.zhuopin.hr.liaison`）；⛔ 不碰 `data/liaison.db`、`.env` 内容进 git、`_to_delete/`
- ⛔ 不做 §5 真实起活实测（不可代，Paul 本人）；⛔ 不改 `openspec/` 下 spec/design；⛔ 不改 `docs/openers/run-lanes.sh`
- 读大文件分段（Read 带 offset＋limit ≤ 400 行），计划文件同样分段读
- 并发协议：⛔ 禁止 git add -A / . / -a；index.lock 等 5 秒重试 5 次不删；push 被拒才 pull --rebase --autostash

【收工】终端输出：做了哪几个 Task、各 Task 两阶段 review 结论、pytest 基线→收工、commit hash 列表。最后一行顶格输出 OPENER_DONE 或 OPENER_PARTIAL: <原因>（不加粗、不加符号）。
```

> ✅ 已完成 2026-09-16（OK）· 原泳道 第十七批波次2（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动，名字另由 -n 给。

```
[Mac]0916P-P0建造Task7至9
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: wave2-p0-bridge ｜ worktree: ✅ 勾（写代码）｜ 工作区: .claude/worktrees/wave2-p0-bridge ｜ 派发: Cowork·Token治理-0916

【零、范围】`liaison-reply-bridge-and-patrol` 单元 P0，计划 `docs/superpowers/plans/2026-09-10-liaison-reply-bridge.md` 的 **Task 7–9**（本单元共 3 条中的第 3 条）。⛔ 只做这几个 Task，⛔ 不越到下一段。

【一、前置——任一不过即输出 OPENER_PARTIAL: <原因> 并停，⛔ 不提问】
1. `grep -n '^- \[.\] 0\.[123]' openspec/changes/liaison-reply-bridge-and-patrol/tasks.md` 三条都是 `[x]`
2. `grep -c '^### Task ' docs/superpowers/plans/2026-09-10-liaison-reply-bridge.md` 与计划 Task 总数一致，且 Task 7–9 都存在
3. `Skill(superpowers:subagent-driven-development)` 可达；调不到 ⇒ OPENER_PARTIAL: superpowers 不可达（⛔ 不手工按磁盘 SKILL.md 走）
4. `git -C .claude/worktrees/wave2-p0-bridge status --short` 能执行（worktree 在）；`git log wave2-p0-bridge --oneline -15` 能看到本单元前一条（Task 1–6）的提交；superpowers 进度台账（worktree 内 `.superpowers/sdd/2026-09-10-liaison-reply-bridge/progress.md`，路径以 skill 实际为准）显示 Task 1–6 已完成。任一不符 ⇒ OPENER_PARTIAL: 前一条未完成

【二、进入工作区】
`cd .claude/worktrees/wave2-p0-bridge`；`git rebase main`（有冲突 ⇒ `git rebase --abort`，OPENER_PARTIAL: rebase 冲突）；测试基线 `./venv/bin/python -m pytest -q tools/liaison/tests 2>&1 | tail -3` 记下

【三、执行】
调 `Skill(run-build)`（即 `superpowers:subagent-driven-development`）执行计划的 **Task 7–9**：每 Task 派实现子代理＋两阶段 review，逐 Task 提交在 `wave2-p0-bridge` 分支上。计划里有 Global Constraints 段，照原样交给 reviewer。

【四、单元收口（本条是本单元最后一条）】
1. 全分支 final review（superpowers finishing-a-development-branch）；有 Critical/Important 未解 ⇒ 不合，OPENER_PARTIAL: final review 未过 <摘要>
2. 合回 main：`git rebase main` → 主工作区 `git merge --ff-only wave2-p0-bridge`（非 ff ⇒ 先在 worktree rebase 再试一次，仍不行 ⇒ OPENER_PARTIAL）→ push；`git rev-list --count main..wave2-p0-bridge` 与 `git cherry -v main wave2-p0-bridge | grep -c '^+'` 都为 0
3. 回勾 `openspec/changes/liaison-reply-bridge-and-patrol/tasks.md` 的 §1（1.1–1.9）中**已被实现且有测试覆盖**的项；进度行照实改。补一行落地偏离：design D11 字面缺 `liaison_unpack_audit.thread_id`，计划已加该列（0910G 发现）；⛔ 不改 design.md
4. worktree 内 `data/` 测试产物如需保留拷回主工作区；`git worktree remove .claude/worktrees/wave2-p0-bridge`（删不掉就留着并在报告写明）
5. 只 git add 本单元代码与测试路径＋`openspec/changes/liaison-reply-bridge-and-patrol/tasks.md`（及 `docs/tech-debt.md` 若有登记）

【守住】
- ⛔ 不重启、不 kickstart 值守服务（`com.zhuopin.hr.liaison`）；⛔ 不碰 `data/liaison.db`、`.env` 内容进 git、`_to_delete/`
- ⛔ 不做 §5 真实起活实测（不可代，Paul 本人）；⛔ 不改 `openspec/` 下 spec/design；⛔ 不改 `docs/openers/run-lanes.sh`
- 读大文件分段（Read 带 offset＋limit ≤ 400 行），计划文件同样分段读
- 并发协议：⛔ 禁止 git add -A / . / -a；index.lock 等 5 秒重试 5 次不删；push 被拒才 pull --rebase --autostash

【收工】终端输出：做了哪几个 Task、各 Task 两阶段 review 结论、pytest 基线→收工、commit hash 列表、合入与回勾的计数。最后一行顶格输出 OPENER_DONE 或 OPENER_PARTIAL: <原因>（不加粗、不加符号）。
```

> ✅ 已完成 2026-09-16（OK）· 原泳道 第十七批波次2（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动，名字另由 -n 给。

```
[Mac]0916Q-P1建造Task1至3
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: wave2-p1-dispatch ｜ worktree: ✅ 勾（写代码）｜ 工作区: .claude/worktrees/wave2-p1-dispatch ｜ 派发: Cowork·Token治理-0916

【零、范围】`liaison-reply-bridge-and-patrol` 单元 P1，计划 `docs/superpowers/plans/2026-09-10-liaison-unpack-dispatch.md` 的 **Task 1–3**（本单元共 3 条中的第 1 条）。⛔ 只做这几个 Task，⛔ 不越到下一段。

【一、前置——任一不过即输出 OPENER_PARTIAL: <原因> 并停，⛔ 不提问】
1. `grep -n '^- \[.\] 0\.[123]' openspec/changes/liaison-reply-bridge-and-patrol/tasks.md` 三条都是 `[x]`
2. `grep -c '^### Task ' docs/superpowers/plans/2026-09-10-liaison-unpack-dispatch.md` 与计划 Task 总数一致，且 Task 1–3 都存在
3. `Skill(superpowers:subagent-driven-development)` 可达；调不到 ⇒ OPENER_PARTIAL: superpowers 不可达（⛔ 不手工按磁盘 SKILL.md 走）
4. `git log --oneline -1 -- docs/superpowers/plans/2026-09-10-liaison-unpack-dispatch.md` 有提交（计划已在 main）
5. 上一单元已合入 main：`git log main --oneline -30 | grep -c '0916'` 能看到上一单元末条的合入提交，且 `git worktree list | grep -c wave2-` 中不含上一单元的 worktree（已清）；否则停

【二、建工作区（本单元第一条才做）】
1. 主工作区 `git pull --rebase --autostash origin main`；`git worktree add .claude/worktrees/wave2-p1-dispatch -b wave2-p1-dispatch main`（已存在同名分支/目录 ⇒ OPENER_PARTIAL: 工作区已存在，⛔ 不删不覆盖）
2. 按 `run-build` skill「建完 worktree 后必须手工补三样」补齐：venv（软链或在 worktree 内按 requirements 建，含 `tools/liaison/.venv`）、`.env` 拷贝（确认 `git check-ignore .env` 命中后才拷）、其余照 skill 表
3. `cd .claude/worktrees/wave2-p1-dispatch` 后跑一次测试基线：`./venv/bin/python -m pytest -q tools/liaison/tests 2>&1 | tail -3` 记下

【三、执行】
调 `Skill(run-build)`（即 `superpowers:subagent-driven-development`）执行计划的 **Task 1–3**：每 Task 派实现子代理＋两阶段 review，逐 Task 提交在 `wave2-p1-dispatch` 分支上。计划里有 Global Constraints 段，照原样交给 reviewer。
⛔ 本条**不做**：全分支 final review、合 main、删 worktree、回勾 tasks.md——这些留给本单元最后一条。做完 Task 3 即收工。

【守住】
- ⛔ 不重启、不 kickstart 值守服务（`com.zhuopin.hr.liaison`）；⛔ 不碰 `data/liaison.db`、`.env` 内容进 git、`_to_delete/`
- ⛔ 不做 §5 真实起活实测（不可代，Paul 本人）；⛔ 不改 `openspec/` 下 spec/design；⛔ 不改 `docs/openers/run-lanes.sh`
- 读大文件分段（Read 带 offset＋limit ≤ 400 行），计划文件同样分段读
- 并发协议：⛔ 禁止 git add -A / . / -a；index.lock 等 5 秒重试 5 次不删；push 被拒才 pull --rebase --autostash

【收工】终端输出：做了哪几个 Task、各 Task 两阶段 review 结论、pytest 基线→收工、commit hash 列表。最后一行顶格输出 OPENER_DONE 或 OPENER_PARTIAL: <原因>（不加粗、不加符号）。
```

> ✅ 已完成 2026-09-16（OK）· 原泳道 第十七批波次2（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动，名字另由 -n 给。

```
[Mac]0916R-P1建造Task4至6
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: wave2-p1-dispatch ｜ worktree: ✅ 勾（写代码）｜ 工作区: .claude/worktrees/wave2-p1-dispatch ｜ 派发: Cowork·Token治理-0916

【零、范围】`liaison-reply-bridge-and-patrol` 单元 P1，计划 `docs/superpowers/plans/2026-09-10-liaison-unpack-dispatch.md` 的 **Task 4–6**（本单元共 3 条中的第 2 条）。⛔ 只做这几个 Task，⛔ 不越到下一段。

【一、前置——任一不过即输出 OPENER_PARTIAL: <原因> 并停，⛔ 不提问】
1. `grep -n '^- \[.\] 0\.[123]' openspec/changes/liaison-reply-bridge-and-patrol/tasks.md` 三条都是 `[x]`
2. `grep -c '^### Task ' docs/superpowers/plans/2026-09-10-liaison-unpack-dispatch.md` 与计划 Task 总数一致，且 Task 4–6 都存在
3. `Skill(superpowers:subagent-driven-development)` 可达；调不到 ⇒ OPENER_PARTIAL: superpowers 不可达（⛔ 不手工按磁盘 SKILL.md 走）
4. `git -C .claude/worktrees/wave2-p1-dispatch status --short` 能执行（worktree 在）；`git log wave2-p1-dispatch --oneline -15` 能看到本单元前一条（Task 1–3）的提交；superpowers 进度台账（worktree 内 `.superpowers/sdd/2026-09-10-liaison-unpack-dispatch/progress.md`，路径以 skill 实际为准）显示 Task 1–3 已完成。任一不符 ⇒ OPENER_PARTIAL: 前一条未完成

【二、进入工作区】
`cd .claude/worktrees/wave2-p1-dispatch`；`git rebase main`（有冲突 ⇒ `git rebase --abort`，OPENER_PARTIAL: rebase 冲突）；测试基线 `./venv/bin/python -m pytest -q tools/liaison/tests 2>&1 | tail -3` 记下

【三、执行】
调 `Skill(run-build)`（即 `superpowers:subagent-driven-development`）执行计划的 **Task 4–6**：每 Task 派实现子代理＋两阶段 review，逐 Task 提交在 `wave2-p1-dispatch` 分支上。计划里有 Global Constraints 段，照原样交给 reviewer。
⛔ 本条**不做**：全分支 final review、合 main、删 worktree、回勾 tasks.md——这些留给本单元最后一条。做完 Task 6 即收工。

【守住】
- ⛔ 不重启、不 kickstart 值守服务（`com.zhuopin.hr.liaison`）；⛔ 不碰 `data/liaison.db`、`.env` 内容进 git、`_to_delete/`
- ⛔ 不做 §5 真实起活实测（不可代，Paul 本人）；⛔ 不改 `openspec/` 下 spec/design；⛔ 不改 `docs/openers/run-lanes.sh`
- 读大文件分段（Read 带 offset＋limit ≤ 400 行），计划文件同样分段读
- 并发协议：⛔ 禁止 git add -A / . / -a；index.lock 等 5 秒重试 5 次不删；push 被拒才 pull --rebase --autostash

【收工】终端输出：做了哪几个 Task、各 Task 两阶段 review 结论、pytest 基线→收工、commit hash 列表。最后一行顶格输出 OPENER_DONE 或 OPENER_PARTIAL: <原因>（不加粗、不加符号）。
```

> ✅ 已完成 2026-09-16（OK）· 原泳道 第十七批波次2（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动，名字另由 -n 给。

```
[Mac]0916S-P1建造Task7至7
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: wave2-p1-dispatch ｜ worktree: ✅ 勾（写代码）｜ 工作区: .claude/worktrees/wave2-p1-dispatch ｜ 派发: Cowork·Token治理-0916

【零、范围】`liaison-reply-bridge-and-patrol` 单元 P1，计划 `docs/superpowers/plans/2026-09-10-liaison-unpack-dispatch.md` 的 **Task 7–7**（本单元共 3 条中的第 3 条）。⛔ 只做这几个 Task，⛔ 不越到下一段。

【零·续跑（2026-09-16 23:1x Cowork 加）】上一轮本条判 NO-SENTINEL，原因是 `claude -p` 只等后台子任务 600 秒就强杀（脚本已放宽到 60 分钟）。上一轮已做到：Task 7 完成（78e2bd4）；全分支 final review 发现 C1＋I2–I7，其中 C1（c50c6fc）、I2＋I3（8d39aaa）、I4＋I5＋I6（cb225c7）已修复提交；**I7 修复被中途杀掉**，worktree 里 `tools/liaison/unpack/bridge.py` 与 `tools/liaison/tests/test_unpack_bridge.py` 有未提交半成品。
续跑顺序：① `git diff` 读半成品，对照 review 记录判断 I7 修复是否可补完——能补完就补完、跑测试、提交；判断不了就先 `git diff > /tmp/0916S-I7-partial.patch` 留底再 `git checkout -- <这两个文件>` 丢弃，重做 I7；② 对 C1＋I2–I7 做一次**聚焦复审**（不必重新全量 review）；③ 通过后按下面【四】收口。⛔ 不重复做 Task 7。

【一、前置——任一不过即输出 OPENER_PARTIAL: <原因> 并停，⛔ 不提问】
1. `grep -n '^- \[.\] 0\.[123]' openspec/changes/liaison-reply-bridge-and-patrol/tasks.md` 三条都是 `[x]`
2. `grep -c '^### Task ' docs/superpowers/plans/2026-09-10-liaison-unpack-dispatch.md` 与计划 Task 总数一致，且 Task 7–7 都存在
3. `Skill(superpowers:subagent-driven-development)` 可达；调不到 ⇒ OPENER_PARTIAL: superpowers 不可达（⛔ 不手工按磁盘 SKILL.md 走）
4. `git -C .claude/worktrees/wave2-p1-dispatch status --short` 能执行（worktree 在）；`git log wave2-p1-dispatch --oneline -15` 能看到本单元前一条（Task 1–6）的提交；superpowers 进度台账（worktree 内 `.superpowers/sdd/2026-09-10-liaison-unpack-dispatch/progress.md`，路径以 skill 实际为准）显示 Task 1–6 已完成。任一不符 ⇒ OPENER_PARTIAL: 前一条未完成

【二、进入工作区】
`cd .claude/worktrees/wave2-p1-dispatch`；`git rebase main`（有冲突 ⇒ `git rebase --abort`，OPENER_PARTIAL: rebase 冲突）；测试基线 `./venv/bin/python -m pytest -q tools/liaison/tests 2>&1 | tail -3` 记下

【三、执行】
调 `Skill(run-build)`（即 `superpowers:subagent-driven-development`）执行计划的 **Task 7–7**：每 Task 派实现子代理＋两阶段 review，逐 Task 提交在 `wave2-p1-dispatch` 分支上。计划里有 Global Constraints 段，照原样交给 reviewer。

【四、单元收口（本条是本单元最后一条）——⛔ 全程在 worktree 里改文件与提交，主工作区只做 merge/push】
1. 全分支 final review（superpowers finishing-a-development-branch）；有 Critical/Important 未解 ⇒ 不合，OPENER_PARTIAL: final review 未过 <摘要>
2. **在 worktree 内**回勾 `openspec/changes/liaison-reply-bridge-and-patrol/tasks.md` 的 §2（2.1–2.9）中已被实现且有测试覆盖的项、进度行照实改，提交在 `wave2-p1-dispatch` 上
3. worktree 内 `git rebase main` → `git -C /Users/paulshao/Projects/HumanResource merge --ff-only wave2-p1-dispatch` → `git -C /Users/paulshao/Projects/HumanResource push origin main`；非 ff ⇒ 再 rebase 一次仍不行 ⇒ OPENER_PARTIAL；核 `git rev-list --count main..wave2-p1-dispatch` 与 `git cherry -v main wave2-p1-dispatch | grep -c '^+'` 都为 0
4. `git -C /Users/paulshao/Projects/HumanResource worktree remove <本 worktree 绝对路径>`（删不掉就留着并在报告写明）
5. ⛔ 不在主工作区 git add/commit（hook 会拦）；worktree 内只 add 本单元代码与测试路径＋tasks.md（及 docs/tech-debt.md 若有登记）

【守住】
- ⛔ 不重启、不 kickstart 值守服务（`com.zhuopin.hr.liaison`）；⛔ 不碰 `data/liaison.db`、`.env` 内容进 git、`_to_delete/`
- ⛔ 不做 §5 真实起活实测（不可代，Paul 本人）；⛔ 不改 `openspec/` 下 spec/design；⛔ 不改 `docs/openers/run-lanes.sh`
- 读大文件分段（Read 带 offset＋limit ≤ 400 行），计划文件同样分段读
- 并发协议：⛔ 禁止 git add -A / . / -a；index.lock 等 5 秒重试 5 次不删；push 被拒才 pull --rebase --autostash

【收工】终端输出：做了哪几个 Task、各 Task 两阶段 review 结论、pytest 基线→收工、commit hash 列表、合入与回勾的计数。最后一行顶格输出 OPENER_DONE 或 OPENER_PARTIAL: <原因>（不加粗、不加符号）。
```

> ✅ 已完成 2026-09-16（OK）· 原泳道 第十七批波次2（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动，名字另由 -n 给。

```
[Mac]0916T-P2建造Task1至3
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: wave2-p2-charter ｜ worktree: ✅ 勾（写代码）｜ 工作区: .claude/worktrees/wave2-p2-charter ｜ 派发: Cowork·Token治理-0916

【零、范围】`liaison-reply-bridge-and-patrol` 单元 P2，计划 `docs/superpowers/plans/2026-09-10-liaison-unpack-charter.md` 的 **Task 1–3**（本单元共 2 条中的第 1 条）。⛔ 只做这几个 Task，⛔ 不越到下一段。

【一、前置——任一不过即输出 OPENER_PARTIAL: <原因> 并停，⛔ 不提问】
1. `grep -n '^- \[.\] 0\.[123]' openspec/changes/liaison-reply-bridge-and-patrol/tasks.md` 三条都是 `[x]`
2. `grep -c '^### Task ' docs/superpowers/plans/2026-09-10-liaison-unpack-charter.md` 与计划 Task 总数一致，且 Task 1–3 都存在
3. `Skill(superpowers:subagent-driven-development)` 可达；调不到 ⇒ OPENER_PARTIAL: superpowers 不可达（⛔ 不手工按磁盘 SKILL.md 走）
4. `git log --oneline -1 -- docs/superpowers/plans/2026-09-10-liaison-unpack-charter.md` 有提交（计划已在 main）
5. 上一单元已合入 main：`git log main --oneline -30 | grep -c '0916'` 能看到上一单元末条的合入提交，且 `git worktree list | grep -c wave2-` 中不含上一单元的 worktree（已清）；否则停

【二、建工作区（本单元第一条才做）】
1. 主工作区 `git pull --rebase --autostash origin main`；`git worktree add .claude/worktrees/wave2-p2-charter -b wave2-p2-charter main`（2026-09-16 起 run-lanes.sh 可能已替你建好：目录已存在且 `git -C <路径> rev-parse --abbrev-ref HEAD` 等于本分支 ⇒ 直接用；存在但分支不符 ⇒ OPENER_PARTIAL: 工作区分支不符，⛔ 不删不覆盖）
2. 按 `run-build` skill「建完 worktree 后必须手工补三样」补齐：venv（软链或在 worktree 内按 requirements 建，含 `tools/liaison/.venv`）、`.env` 拷贝（确认 `git check-ignore .env` 命中后才拷）、其余照 skill 表
3. `cd .claude/worktrees/wave2-p2-charter` 后跑一次测试基线：`./venv/bin/python -m pytest -q tools/liaison/tests 2>&1 | tail -3` 记下

【三、执行】
调 `Skill(run-build)`（即 `superpowers:subagent-driven-development`）执行计划的 **Task 1–3**：每 Task 派实现子代理＋两阶段 review，逐 Task 提交在 `wave2-p2-charter` 分支上。计划里有 Global Constraints 段，照原样交给 reviewer。
⛔ 本条**不做**：全分支 final review、合 main、删 worktree、回勾 tasks.md——这些留给本单元最后一条。做完 Task 3 即收工。

【守住】
- ⛔ 不重启、不 kickstart 值守服务（`com.zhuopin.hr.liaison`）；⛔ 不碰 `data/liaison.db`、`.env` 内容进 git、`_to_delete/`
- ⛔ 不做 §5 真实起活实测（不可代，Paul 本人）；⛔ 不改 `openspec/` 下 spec/design；⛔ 不改 `docs/openers/run-lanes.sh`
- 读大文件分段（Read 带 offset＋limit ≤ 400 行），计划文件同样分段读
- 并发协议：⛔ 禁止 git add -A / . / -a；index.lock 等 5 秒重试 5 次不删；push 被拒才 pull --rebase --autostash

【收工】终端输出：做了哪几个 Task、各 Task 两阶段 review 结论、pytest 基线→收工、commit hash 列表。最后一行顶格输出 OPENER_DONE 或 OPENER_PARTIAL: <原因>（不加粗、不加符号）。
```

> ✅ 已完成 2026-09-17（OK）· 原泳道 第十七批波次2（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动，名字另由 -n 给。

```
[Mac]0916U-P2建造Task4至5
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: wave2-p2-charter ｜ worktree: ✅ 勾（写代码）｜ 工作区: .claude/worktrees/wave2-p2-charter ｜ 派发: Cowork·Token治理-0916

【零、范围】`liaison-reply-bridge-and-patrol` 单元 P2，计划 `docs/superpowers/plans/2026-09-10-liaison-unpack-charter.md` 的 **Task 4–5**（本单元共 2 条中的第 2 条）。⛔ 只做这几个 Task，⛔ 不越到下一段。

【一、前置——任一不过即输出 OPENER_PARTIAL: <原因> 并停，⛔ 不提问】
1. `grep -n '^- \[.\] 0\.[123]' openspec/changes/liaison-reply-bridge-and-patrol/tasks.md` 三条都是 `[x]`
2. `grep -c '^### Task ' docs/superpowers/plans/2026-09-10-liaison-unpack-charter.md` 与计划 Task 总数一致，且 Task 4–5 都存在
3. `Skill(superpowers:subagent-driven-development)` 可达；调不到 ⇒ OPENER_PARTIAL: superpowers 不可达（⛔ 不手工按磁盘 SKILL.md 走）
4. `git -C .claude/worktrees/wave2-p2-charter status --short` 能执行（worktree 在）；`git log wave2-p2-charter --oneline -15` 能看到本单元前一条（Task 1–3）的提交；superpowers 进度台账（worktree 内 `.superpowers/sdd/2026-09-10-liaison-unpack-charter/progress.md`，路径以 skill 实际为准）显示 Task 1–3 已完成。任一不符 ⇒ OPENER_PARTIAL: 前一条未完成

【二、进入工作区】
`cd .claude/worktrees/wave2-p2-charter`；`git rebase main`（有冲突 ⇒ `git rebase --abort`，OPENER_PARTIAL: rebase 冲突）；测试基线 `./venv/bin/python -m pytest -q tools/liaison/tests 2>&1 | tail -3` 记下

【三、执行】
调 `Skill(run-build)`（即 `superpowers:subagent-driven-development`）执行计划的 **Task 4–5**：每 Task 派实现子代理＋两阶段 review，逐 Task 提交在 `wave2-p2-charter` 分支上。计划里有 Global Constraints 段，照原样交给 reviewer。

【四、单元收口（本条是本单元最后一条）——⛔ 全程在 worktree 里改文件与提交，主工作区只做 merge/push】
1. 全分支 final review（superpowers finishing-a-development-branch）；有 Critical/Important 未解 ⇒ 不合，OPENER_PARTIAL: final review 未过 <摘要>
2. **在 worktree 内**回勾 `openspec/changes/liaison-reply-bridge-and-patrol/tasks.md` 的 §3（§3 全部）中已被实现且有测试覆盖的项、进度行照实改，提交在 `wave2-p2-charter` 上
3. worktree 内 `git rebase main` → `git -C /Users/paulshao/Projects/HumanResource merge --ff-only wave2-p2-charter` → `git -C /Users/paulshao/Projects/HumanResource push origin main`；非 ff ⇒ 再 rebase 一次仍不行 ⇒ OPENER_PARTIAL；核 `git rev-list --count main..wave2-p2-charter` 与 `git cherry -v main wave2-p2-charter | grep -c '^+'` 都为 0
4. `git -C /Users/paulshao/Projects/HumanResource worktree remove <本 worktree 绝对路径>`（删不掉就留着并在报告写明）
5. ⛔ 不在主工作区 git add/commit（hook 会拦）；worktree 内只 add 本单元代码与测试路径＋tasks.md（及 docs/tech-debt.md 若有登记）

【守住】
- ⛔ 不重启、不 kickstart 值守服务（`com.zhuopin.hr.liaison`）；⛔ 不碰 `data/liaison.db`、`.env` 内容进 git、`_to_delete/`
- ⛔ 不做 §5 真实起活实测（不可代，Paul 本人）；⛔ 不改 `openspec/` 下 spec/design；⛔ 不改 `docs/openers/run-lanes.sh`
- 读大文件分段（Read 带 offset＋limit ≤ 400 行），计划文件同样分段读
- 并发协议：⛔ 禁止 git add -A / . / -a；index.lock 等 5 秒重试 5 次不删；push 被拒才 pull --rebase --autostash

【收工】终端输出：做了哪几个 Task、各 Task 两阶段 review 结论、pytest 基线→收工、commit hash 列表、合入与回勾的计数。最后一行顶格输出 OPENER_DONE 或 OPENER_PARTIAL: <原因>（不加粗、不加符号）。
```

> ✅ 已完成 2026-09-17（OK）· 原泳道 第十七批波次2（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动，名字另由 -n 给。

```
[Mac]0916V-P3建造Task1至4
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: wave2-p3-ledger ｜ worktree: ✅ 勾（写代码）｜ 工作区: .claude/worktrees/wave2-p3-ledger ｜ 派发: Cowork·Token治理-0916

【零、范围】`liaison-reply-bridge-and-patrol` 单元 P3，计划 `docs/superpowers/plans/2026-09-10-liaison-criteria-ledger.md` 的 **Task 1–4**（本单元共 1 条中的第 1 条）。⛔ 只做这几个 Task，⛔ 不越到下一段。

【一、前置——任一不过即输出 OPENER_PARTIAL: <原因> 并停，⛔ 不提问】
1. `grep -n '^- \[.\] 0\.[123]' openspec/changes/liaison-reply-bridge-and-patrol/tasks.md` 三条都是 `[x]`
2. `grep -c '^### Task ' docs/superpowers/plans/2026-09-10-liaison-criteria-ledger.md` 与计划 Task 总数一致，且 Task 1–4 都存在
3. `Skill(superpowers:subagent-driven-development)` 可达；调不到 ⇒ OPENER_PARTIAL: superpowers 不可达（⛔ 不手工按磁盘 SKILL.md 走）
4. `git log --oneline -1 -- docs/superpowers/plans/2026-09-10-liaison-criteria-ledger.md` 有提交（计划已在 main）
5. 上一单元已合入 main：`git log main --oneline -30 | grep -c '0916'` 能看到上一单元末条的合入提交，且 `git worktree list | grep -c wave2-` 中不含上一单元的 worktree（已清）；否则停

【二、建工作区（本单元第一条才做）】
1. 主工作区 `git pull --rebase --autostash origin main`；`git worktree add .claude/worktrees/wave2-p3-ledger -b wave2-p3-ledger main`（2026-09-16 起 run-lanes.sh 可能已替你建好：目录已存在且 `git -C <路径> rev-parse --abbrev-ref HEAD` 等于本分支 ⇒ 直接用；存在但分支不符 ⇒ OPENER_PARTIAL: 工作区分支不符，⛔ 不删不覆盖）
2. 按 `run-build` skill「建完 worktree 后必须手工补三样」补齐：venv（软链或在 worktree 内按 requirements 建，含 `tools/liaison/.venv`）、`.env` 拷贝（确认 `git check-ignore .env` 命中后才拷）、其余照 skill 表
3. `cd .claude/worktrees/wave2-p3-ledger` 后跑一次测试基线：`./venv/bin/python -m pytest -q tools/liaison/tests 2>&1 | tail -3` 记下

【三、执行】
调 `Skill(run-build)`（即 `superpowers:subagent-driven-development`）执行计划的 **Task 1–4**：每 Task 派实现子代理＋两阶段 review，逐 Task 提交在 `wave2-p3-ledger` 分支上。计划里有 Global Constraints 段，照原样交给 reviewer。

【四、单元收口（本条是本单元最后一条）——⛔ 全程在 worktree 里改文件与提交，主工作区只做 merge/push】
1. 全分支 final review（superpowers finishing-a-development-branch）；有 Critical/Important 未解 ⇒ 不合，OPENER_PARTIAL: final review 未过 <摘要>
2. **在 worktree 内**回勾 `openspec/changes/liaison-reply-bridge-and-patrol/tasks.md` 的 §4（§4 全部）中已被实现且有测试覆盖的项、进度行照实改，提交在 `wave2-p3-ledger` 上
3. worktree 内 `git rebase main` → `git -C /Users/paulshao/Projects/HumanResource merge --ff-only wave2-p3-ledger` → `git -C /Users/paulshao/Projects/HumanResource push origin main`；非 ff ⇒ 再 rebase 一次仍不行 ⇒ OPENER_PARTIAL；核 `git rev-list --count main..wave2-p3-ledger` 与 `git cherry -v main wave2-p3-ledger | grep -c '^+'` 都为 0
4. `git -C /Users/paulshao/Projects/HumanResource worktree remove <本 worktree 绝对路径>`（删不掉就留着并在报告写明）
5. ⛔ 不在主工作区 git add/commit（hook 会拦）；worktree 内只 add 本单元代码与测试路径＋tasks.md（及 docs/tech-debt.md 若有登记）

【守住】
- ⛔ 不重启、不 kickstart 值守服务（`com.zhuopin.hr.liaison`）；⛔ 不碰 `data/liaison.db`、`.env` 内容进 git、`_to_delete/`
- ⛔ 不做 §5 真实起活实测（不可代，Paul 本人）；⛔ 不改 `openspec/` 下 spec/design；⛔ 不改 `docs/openers/run-lanes.sh`
- 读大文件分段（Read 带 offset＋limit ≤ 400 行），计划文件同样分段读
- 并发协议：⛔ 禁止 git add -A / . / -a；index.lock 等 5 秒重试 5 次不删；push 被拒才 pull --rebase --autostash

【收工】终端输出：做了哪几个 Task、各 Task 两阶段 review 结论、pytest 基线→收工、commit hash 列表、合入与回勾的计数。最后一行顶格输出 OPENER_DONE 或 OPENER_PARTIAL: <原因>（不加粗、不加符号）。
```

> ✅ 已完成 2026-09-17（OK）· 原泳道 收口提交0916W（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 无头启动（Cowork 经 launchd 发车）。

```
[Mac]0916W-波次2收口与worktree强制机制提交及P2判定
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（只提交 Cowork 已写好的机制与文档，并修 TD 撞号）｜ 工作区: 仓库根 ｜ 派发: Cowork·Token治理-0916

背景：第十七批波次 2 九条全部 OK、四单元已合 main。Cowork 已写好（未提交）：run-lanes.sh「worktree 由脚本强制建并在其中启动 claude」＋「-p 后台等待上限 60 分钟」、scripts/hooks/worktree-guard.py ＋ tests/test_worktree_guard_hook.py、tests/test_run_lanes_model.py（worktree 用例）、tests/test_hook_commands_fail_open.py、.claude/settings.json（新 hook ＋ 所有 hook 命令脚本缺失即放行）、lane-dispatch SKILL（worktree 强制说明）、编排文件波次 2 块与本块、号池台账、P2-AB基线波次 2 行。

一、测试（失败 → 不提交，输出 OPENER_PARTIAL: 测试失败 <末行>）
`./venv/bin/python -m pytest -q tests/test_worktree_guard_hook.py tests/test_run_lanes_model.py tests/test_hook_commands_fail_open.py tests/test_limit_output_hook.py tests/test_context_guard_hook.py tests/test_doc_size_budget.py tests/test_opener_split_check.py 2>&1 | tail -3`
`bash -n docs/openers/run-lanes.sh`

二、修 TD 撞号（0916V 登记的「TD-44 criteria 子命令 python 二进制名」与已归档的 ~~TD-44~~ SdkLogObserver 撞号——根因是 0916K 把已还条目搬到 docs/archive/tech-debt-已还.md 后，新登记只看了 tech-debt.md 取号）
1. `grep -hoE 'TD-[0-9]+' docs/tech-debt.md docs/archive/tech-debt-已还.md | sort -t- -k2 -n | tail -1` 取当前最大号 N，新号 = N+1（预期 TD-45）
2. 把 9724339 引入的那条 TD-44 改成新号：`git show 9724339 --name-only` 列出的文件里，只改指向 criteria/python 二进制名那一条的 TD-44 字样（`grep -n 'TD-44' <文件>` 逐处看，⛔ 不改指向 SdkLogObserver 的旧 TD-44）
3. 在 docs/tech-debt.md 标题下第一行追加一行：`> 新登记取号：\`grep -hoE 'TD-[0-9]+' docs/tech-debt.md docs/archive/tech-debt-已还.md | sort -t- -k2 -n | tail -1\` 再 +1（已还条目归档后本文件里看不到最大号，2026-09-16 0916V 撞号实证）`
4. `.claude/skills/run-build/SKILL.md` 与 `.claude/skills/spec-to-plan/SKILL.md` 里若有「登记 docs/tech-debt.md 新 TD」的说明，同处补一句「取号查两个文件」；没有就不改

三、Phase 2 判定（写入 docs/token治理/P2-AB基线.md 末尾「## 判定（2026-09-17）」）
1. `python3 scripts/token_ledger.py --since 2026-09-16 --out /tmp/ledger-0916w`；从成本 Top 表取 0916N–0916V 九条的 $，把 P2-AB基线 波次 2 行的「待账本」替换成「$x.x（均 $y.y）」（未命中的写明）；同时取「子代理」类在今天的合计成本写进判定
2. 按文件里原判据逐条判：①失败类占比（质量类）＝0/16 ≤ 21% ⇒ 质量过；②泳道类单会话均价：用今天全部 [Mac] 泳道会话（账本「泳道/opener」类）均价，≤ $3.1 过；>$3.1 写实际值并注明「建造类含 final review，与基线口径差异」
3. 结论一句：Phase 2 通过／部分通过／不通过，及依据数字

四、提交
git add docs/openers/run-lanes.sh scripts/hooks/worktree-guard.py tests/test_worktree_guard_hook.py tests/test_run_lanes_model.py tests/test_hook_commands_fail_open.py .claude/settings.json .claude/skills/lane-dispatch/SKILL.md docs/openers/OP-0820-全量编排.md docs/openers/号池台账.md docs/token治理/P2-AB基线.md docs/tech-debt.md 以及【二】实际改过的文件
git commit -m "feat(lanes): 脚本强制 worktree ＋ worktree-guard hook ＋ hook 缺文件放行 ＋ 后台等待上限；修 TD 撞号；Phase 2 判定（0916W）"，按并发协议 push
五、终端输出：测试末行、TD 新号、Phase 2 判定原文，最后一行顶格 OPENER_DONE（不加粗、不加符号）
```

> 泳道：Token治理总验收
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 无头启动（Cowork 经 launchd 发车）。

```
[Mac]0917A-Token治理总验收
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（只读本机日志、写验收文档）｜ 工作区: 仓库根 ｜ 派发: Cowork·Token治理-0916

目标：用同一把量尺对比治理前（09-01～09-15 基线）与治理后（09-16 起），判定 Token 治理能否结项。写 `docs/token治理/总验收.md`。⛔ 不改任何脚本、hook、skill、配置。

一、出账
1. `python3 scripts/token_ledger.py --since 2026-09-16 --out docs/token治理/after-0916 --compare docs/token治理/baseline-0901-0915.json`
2. 另出一份「仅泳道」口径：从 after-0916.json 的 top 与 by_kind 取数即可，⛔ 不改脚本

二、五项硬指标（基线值已由 Cowork 从 baseline-0901-0915.json 算好，照抄进表，治理后值从 after-0916.json 算）
| # | 指标 | 基线 | 达标线 | 算法 |
|---|---|---|---|---|
| 1 | 泳道/opener 类单会话均价 | $5.17（102 会话 $527.6） | ≤ $3.10 | by_kind["泳道/opener"].cost / sessions |
| 2 | 看护类单会话均价 | $5.47（11 会话 $60.2） | ≤ $2.74；或 0 会话（已改由 Cowork 远程看护）记「达标·机制替代」 | by_kind["看护"] |
| 3 | 单次调用上下文 ≥150k 占比 | 18.5% | ≤ 9.3% | ctx_band_calls 中 150–250k 与 ≥250k 之和 / 总 |
| 4 | Opus 调用占比 | 54.5% | ≤ 15% | by_model.opus.calls / 总调用 |
| 5 | 子代理单会话均价 | $0.60（296 会话 $176.6） | ≤ $0.60 | by_kind["子代理"] |
每项另列一行「仅 [Mac] 无头泳道口径」（排除标题含 Token治理 / 0916A–D、0916G–K 的 Desktop 手工治理会话——它们是治理工作本身，不是日常负载），两种口径都写，**判定以无头泳道口径为准**。

三、判定规则（照判，⛔ 不问）
- 五项全部达标 ⇒ 「结项」
- 有未达标项 ⇒ 「部分达标」，对每个未达标项写：实际值、差距、最可能的原因（查 after-0916.md 成本 Top 表定位到具体会话）、应回到路线图哪个 Phase
- 必须写明局限：治理后样本只有约 1 天、以 run-build 建造与治理本身为主，与基线期工作构成不同；账本不含 Cowork 会话成本

四、文档结构（总验收.md，普通文字与表格，⛔ 不贴原始日志）
1. 结论一句 2. 五项指标对比表（两种口径） 3. 各 Phase 落地清单（路线图各状态行 + 提交 hash：23e47b8、e28ed1a、b74012b、96bfafc、8832368、2473d1c、e21d90f、d064744、555592a、9a69695、c491230、aa3d8f6） 4. 未达标项与回归方向（若有） 5. 局限 6. 常态化：每批看护报告附 A/B 行与账本一行（已写进 lane-dispatch）；建议两周后用同命令复测一次（`--since` 取两周窗口）
另：`docs/token治理/P2-AB基线.md` 的「## 判定（2026-09-17）」段误嵌在表格行末尾（行尾多了 ` |`、判定段在降级清单前被表格吞掉）——把判定段移到表格之后、降级清单之前，独立成节，⛔ 不改文字内容。
路线图 `docs/token治理/路线图.md` 顶部追加一行状态：「2026-09-17 总验收：<结项/部分达标>，见 总验收.md」。

五、提交
git add docs/token治理/总验收.md docs/token治理/after-0916.md docs/token治理/after-0916.json docs/token治理/P2-AB基线.md docs/token治理/路线图.md docs/openers/OP-0820-全量编排.md docs/openers/号池台账.md
git commit -m "docs(token治理): 总验收——治理前后五项指标对比与结项判定（0917A）"，按并发协议 push
终端输出：五项指标表（无头泳道口径）、判定结论一句、commit hash；最后一行顶格 OPENER_DONE（不加粗、不加符号）
```
