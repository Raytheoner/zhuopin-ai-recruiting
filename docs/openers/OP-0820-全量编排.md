# OP-0820 · 全量 Opener 编排

> 2026-08-20。**唯一权威 opener 清单**，`OP-0820-A_单元BC开工.md` 已作废。
>
> **编排原则**：不依赖人工控制执行节奏。有依赖就彻底串行，能并发的全部并行。
> 并发安全靠《并发协议》做到机器自愈，**不靠"你记得别同时开"**。
>
> **格式**：每个代码块头两行是标题行 + 【设置】行，整块复制即可，不要拆。

---

## 🔢 号池台账 —— 出号前必查，给出后当场登记

🔴 **2026-08-27 血的教训：一天之内撞号 5 次**——`0827E` 派给三个不同 session，
`0827D` 撞两次，`0827B` 同时给了 CC 的单元 F 泳道和 Cowork 的接力 session。

根因不是记性差，是**没有台账**：提交类、清理类这些一次性 opener 只出现在聊天记录里，
从不写进任何文件 ⇒ 下一轮 `grep` 仓库只查得到编排文件里那几个（`A/B/C/Z`），
`D/E` 查不到 ⇒ 每轮都以为是空号 ⇒ 必然撞。
撞号的代价正好抵消 `set_session_title` 那条规则的全部价值——侧边栏三条同名，一样分不清。

**两条硬规则**：

1. **出号前**：`grep -rho "\[Mac\]MMDD\([A-Z]\)" --include=*.md --include=*.sh . | sed 's/.*MMDD//' | sort -u`
   ——⚠️ 这条只查得到**已登记的**，所以第 2 条才是关键
2. **给出后当场登记到下表**，哪怕那个 opener 不写进本文件正文。⛔ 不登记等于没给过

⚠️ **本表只记已知的，`claude.ai` 侧边栏才是真身。** 拿不准就让 Shao Peishen 看一眼侧边栏。

⚠️ **「号」列必须写成完整的 `[Mac]MMDDX` 形式**，不要只写字母——上面那条 grep 靠它命中。
（初版就写成了 `` `D` ``，结果查重命令查不到自己的台账，当场作废了一次。）

| 日期 | 号 | 主题 | 去向 |
|---|---|---|---|
| 08-27 | `[Mac]0827A` | （模板占位，未实际派发） | — |
| 08-27 | ~~`[Mac]0827B`~~ | ⚠️ **撞号 2 次**：① 单元 F 开工（第 7 章）② Cowork 接力 `HR业务线-接力0827B` | ① 编排文件·第四批·溯源泳道；② 本轮 Cowork session。⛔ 该号作废不再派 |
| 08-27 | `[Mac]0827C` | audit U2 开工（`app/audit`） | 编排文件·第四批·审计泳道 |
| 08-27 | `[Mac]0827D` | 提交链式执行器与第四批 | 聊天里派发，已跑完（`a171cef` 那批） |
| 08-27 | ~~`[Mac]0827E`~~ | ⚠️ **撞号 3 次**：提交 session 命名根治与 lane-dispatch／提交 opener 补丁／提交命名实测与自检 | 三条都已跑完（`a171cef`／`528c634`／`ef2c850`），⛔ 该号作废不再派 |
| 08-27 | `[Mac]0827F` | 提交号池台账与查重规则 | 聊天里派发 |
| 08-27 | `[Mac]0827G` | Cowork 接力（下一轮） | `docs/session接力.md` 开场词首行 |
| 08-27 | `[Mac]0827Z` | 泳道批次看护 | 编排文件·看护者 opener（`Z` 固定留给看护者） |
| 08-28 | `[Mac]0828A` | 账目对齐（第 4 章回勾、4.6/4.7 订正、2.x/4.x 偏离登记、7.5/7.6 登记） | 编排文件·第五批·账目泳道（串行第 1 条） |
| 08-28 | `[Mac]0828B` | ai-audit U5 开工（第 5 章·待审批队列与图节点接线） | 编排文件·第五批·账目泳道（串行第 3 条） |
| 08-28 | `[Mac]0828C` | 运维口径页 7.3 ＋ 06 清单 7.4 | 编排文件·第五批·账目泳道（串行第 2 条） |
| 08-28 | `[Mac]0828D` | U2 全分支 final review ＋ 11 条 deferred minor 分诊建议 | 编排文件·第五批·账目泳道（串行第 3 条） |
| 08-28 | `[Mac]0828E` | 提交看护者块补位（0827Z→0828Z、预期值改第五批） | 聊天里派发（Cowork 侧），已提交 `ba565d7`，**push 被权限模式拦、未推** |
| 08-28 | `[Mac]0828F` | 提交 pytest 基线实测化 + superpowers 预案；给 settings.json 加 push 白名单 | 聊天里派发（Cowork 侧） |
| 08-28 | `[Mac]0828Z` | 泳道批次看护（第五批） | 编排文件·看护者 opener（`Z` 固定留给看护者） |
| 08-31 | `[Mac]0831A` | `.51` §5-3 开关链路验证（开→验→关→再验四步）＋ 回填闭合 | 聊天里派发。**不可代项，Shao Peishen 08-31 明确指示后执行** |
| 08-31 | `[Mac]0831A` | `.51` §5-3 四步验证 | **⏸ 未闭合**：报 `CommandNotFoundException`，误判为「.51 缺 venv」。真因＝文档路径少一个点（`venv` 应为 `.venv`），09-03 查明 |
| 08-31 | `[Mac]0831B` | U5 续跑·走「已闭合」支：合并 + 回勾第 5 章 9/9 + 归档判断 | 聊天里派发。前置＝§5-3 已闭合，**尚未满足**，等 `0903A` |
| 09-03 | `[Mac]0903A` | `.51` §5-3 四步验证**重跑**（路径订正为 `.venv`）＋ 顺带 §2.2 链校验 | 聊天里派发。**不可代项，Shao Peishen 已指示** |
| 09-03 | `[Mac]0903A` | `.51` §5-3 四步验证重跑结果 | **⏸ 仍未闭合**：路径订正有效（`.venv` 确认存在），但真因是 `.51` 部署代码落后于功能开发进度——`app\config.py` 停在 08-19（34 行），功能 08-27 才落地（`4890fab`/`eb338b0`/`49c5e4c`），`app\audit\` 整包缺失。产线开关已独立确认为 `false`（安全）。U5 接线前需 Shao Peishen 决定是否执行 `.51` 重新部署——发版决定不可代，本 opener 到此为止 |
| 09-03 | `[Mac]0903B` | Cowork 接力（转场） | `docs/session接力.md` 开场词首行 |
| 09-03 | `[Mac]0903C` | 提交转场落档（接力文档重写 + `.venv` 订正 + 号池补登） | 聊天里派发（Cowork 侧），已跑完（`3101c99`） |
| 09-03 | `[Mac]0903D` | `.51` 阶段 D 日常发版（快照 → `sync-to-server.sh` → 验收 1/2）＋ 发版后 §5-3 四步与 §2.2 链校验补跑 ＋ 提交接力落档 | 聊天里派发（Cowork 侧 0903B 出）。🔴 **发版＋开关四步均为不可代项**。**Shao Peishen 2026-09-03 09:55 已回「发」**（Cowork 会话 0903B），授权生效 |
| 09-03 | `[Mac]0903D` | `.51` 阶段 D 发版 ＋ 补跑结果 | ✅ 已完成，**已闭合**：发版至 `d104249`（快照 `C:\apps\backups\20260903-1003`，冒烟 4 项全过），§5-3 四步与 §2.2 链校验全过（`ok=True, total=0`，属"尚无审计记录"而非失败），详见 `docs/audit-and-outbound-ops.md` §五第 2/3 项。授权依据同上一行 |
| 09-03 | `[Mac]0903E` | U5 收口：从 `worktree-audit-u5-queue-and-wiring` 分支合并回 main ＋ 回勾第 5 章 9/9 ＋ 不归档（第 6、7 章未完） | 聊天里派发（Cowork 侧 0903B 出）。前置＝`0903D` 报 §5-3 已闭合。**替代原 `0831B`**（其 worktree 前提已失效） |
| 09-03 | `[Mac]0903E` | U5 收口结果 | ✅ 已完成：`finishing-a-development-branch` 合并 `worktree-audit-u5-queue-and-wiring` → `main`（`--no-ff`，merge commit `06a55d2c`），`git rev-list --count main..<分支>` = 0 确认真合非假阳性；合并后全量 pytest **720 passed / 0 failed**（合并前基线 675）；第 5 章 9/9 已回勾；**未归档**（第 6 章 0/7、第 7 章 4/6 未完，见 `tasks.md` 顶部进度行）；分支已 `git branch -d` 删除 |
| 09-03 | `[Mac]0903F` | ai-audit U6 实现计划（第 6 章·合规断言、对账与 CI） | 编排文件·第七批·审计泳道（串行第 1 条）。✅ OK 16m，`18f85ad`，plan 5 Task；superpowers 没调到、手工走 |
| 09-03 | `[Mac]0903G` | ai-audit U6 开工（6.1–6.7 run-build ＋ 合并 ＋ 回勾第 6 章） | 编排文件·第七批·审计泳道（串行第 2 条）。✅ 7/7 合入 `e5e8e33`，PARTIAL 仅因两项不可代（`.51` 巡检、TD-9）；superpowers 没调到、手工走 |
| 09-03 | `[Mac]0903H` | intake 第 8 章·数据与回放（8.2 副本加列 / 8.1 三段真实会话回放 / 8.5 延迟核对） | 编排文件·第七批·回放泳道（串行第 1 条）。✅ 合入 `e56462f`，18 轮全通；PARTIAL＝8.4 留步 + 8.5 无基线不可比；⚠️ `data/replay/` 快照随 worktree 自删 |
| 09-03 | `[Mac]0903I` | intake 第 8 章·文档回填（8.6 / 8.7 首个编造率 / 8.8 经理指南） | 编排文件·第七批·回放泳道（串行第 2 条）。✅ `d19625f`；PARTIAL＝`m1-fabrication-rate.md` 首测表不在白名单未填（交 0903J） |
| 09-03 | `[Mac]0903Z` | 泳道批次看护（第七批）＋ 推送 main（Shao Peishen 09-03 同意） | 编排文件·看护者 opener（`Z` 固定留给看护者）。✅ 4/4 合入，push 发车前被拒、收尾成功（ahead 0），报告 `lanes-20260903-115028-看护报告.md` |
| 09-03 | `[Mac]0903J` | 第七批收尾：填 `m1-fabrication-rate.md` 首测表 ＋ 提交看护报告/编排标注/接力 ＋ 推送 | 聊天里派发（Cowork 侧 0903B 出） |
| 09-03 | `[Mac]0903K` | TD-9 立正式变更包（`openspec-propose`，只出 planning 产物）＋ 提交 skill 真源改动（data/ 产物搬回规矩） | 聊天里派发（Cowork 侧 0903B 出）。Shao Peishen 09-03 裁决「按推荐」 |
| 09-03 | `[Mac]0903L` | `.51` 二次发版（含 U6）＋ U6 巡检 CLI 对真实库 ＋ 8.4 页面跑通取证回勾 | 聊天里派发（Cowork 侧 0903B 出）。🔴 发版不可代，**Shao Peishen 09-03 14:05 回「都要上」即授权** |
| 09-03 | `[Mac]0903L` | `.51` 二次发版 ＋ 巡检 ＋ 8.4 取证结果 | ✅ 已完成：发版至 `b86db65`（快照 `C:\apps\backups\20260903-1428`，冒烟 4 项全过，未回滚）；U6 巡检 CLI `EXIT=2`（JSONL 镜像不存在，尚无审计记录，非失败）；8.4 三条判据全部核验通过（job `51b225f1-1ede-49a1-8c6f-06b6ce401058`，`derived_unspecified_fields` 6 项与页面红框逐项对应），已回勾，`tasks.md` 第 8 章 68/69。授权依据同上一行 |
| 09-03 | `[Mac]0903M` | `.claude/settings.json` 加 `Bash(git push:*)` 白名单 ＋ 提交 ＋ 推 main（Shao Peishen 09-03 拍板 (b)） | 聊天里派发（Cowork 侧 0903B 出，≤500 字直接贴）。✅ `365e5fa`，ahead 0 |
| 09-03 | `[Mac]0903N` | ai-audit U7 实现计划（7.1/7.2 CI 检查） | 编排文件·第八批·审计泳道（串行 1） |
| 09-03 | `[Mac]0903O` | ai-audit U7 run-build ＋ 回勾 53/53（不归档，等 Q） | 编排文件·第八批·审计泳道（串行 2） |
| 09-03 | `[Mac]0903P` | outbound-retry-audit-trace 实现计划（1+2 章合一单元） | 编排文件·第八批·审计泳道（串行 3） |
| 09-03 | `[Mac]0903Q` | outbound-retry-audit-trace run-build ＋ 归档本包 ＋ 归档 ai-audit 包 | 编排文件·第八批·审计泳道（串行 4） |
| 09-03 | `[Mac]0903Y` | 泳道批次看护（第八批）。⚠️ `Z` 已被第七批用掉，同日第二个看护者顺延用 `Y` | `docs/openers/0903Y-泳道批次看护.md`（引用式） |
| 09-04 | `[Mac]0904A` | 双归档（先 ai-audit 后 retry-audit-trace，顺序按 spec 依赖）＋ TD-10 登记 ＋ wt-U7 遗留清理 | `docs/openers/0904A-双归档与TD10登记与wt清理.md`（引用式）。依据 0903Y 报告选项 A+C+E。✅ `7faf682`，两包已归档 |
| 09-04 | `[Mac]0904B` | intake 第 6 章·确认断点 实现计划（6.x D 类 8 条 + 1.4 + 9.3） | 编排文件·第九批·确认泳道（串行 1） |
| 09-04 | `[Mac]0904C` | intake 第 6 章 run-build ＋ 合并 ＋ 回勾（→42/71） | 编排文件·第九批·确认泳道（串行 2） |
| 09-04 | `[Mac]0904D` | intake 第 7 章·JD 溯源与导出 实现计划（7.3/7.5/7.7） | 编排文件·第九批·确认泳道（串行 3） |
| 09-04 | `[Mac]0904E` | intake 第 7 章 run-build ＋ 合并 ＋ 回勾（→45/71） | 编排文件·第九批·确认泳道（串行 4） |
| 09-04 | `[Mac]0904Z` | 泳道批次看护（第九批）＋ 前置 `.51` 三次发版（TD-9 修复 `7faf682`，Shao Peishen 09-04 回「发」） | `docs/openers/0904Z-泳道批次看护.md`（引用式） |
| 08-30 | `[Mac]0830A` | ai-audit U5 **续跑**（第 5 章·Task 5 端到端四条 ＋ 终审 ＋ 回勾 ＋ 收口） | 编排文件·第六批·账目泳道（本批唯一一条） |
| 08-30 | `[Mac]0830Z` | 泳道批次看护（第六批） | 编排文件·看护者 opener（`Z` 固定留给看护者） |

**08-27 当日撞号 5 次**（`B`×2、`D`×2、`E`×3 中的重复计 3 次）。⇒ 本表存在的理由。

⚠️ Cowork 侧接力号与 CC 共用本号池（同日同号在两个界面指不同东西会读岔）。
本轮 Cowork 接力号原定 `0827D`，**与上表 `D` 撞**，已改判：见 `docs/session接力.md` 顶部。

---

## 〇、执行进度（2026-08-20 12:30 实地核对 git 真身）

| Opener | 状态 | 证据 |
|---|---|---|
| OP-0820-1 提交契约改动 | ✅ 完成 | `2c2cc34` `3899757` `fdc34f9` |
| OP-0820-2 单元 B 的 plan | ✅ 完成 | `3a061e6`，7 个 Task |
| OP-0820-3 单元 C 的 plan | ✅ 完成 | `1d9e2c7` |
| ~~OP-0820-4 统一提交闸门~~ | ⚰️ **已取消，不是漏跑** | 原为"两份计划一起提交"的闸门。改成每条 opener 只 `git add` 自己产出的路径后，闸门不再需要——它本质是人工节奏控制。职责已分散进 OP-0820-2 / -3 各自的收工段。**编号保留空洞以免后续误认** |
| OP-0820-7 .51 ACL + 06 清单 1.1 | ✅ 完成 | `2d5eaf0` |
| OP-0820-8 工具链纪律四条 | ✅ 完成 | `f4f2c8f` |
| OP-0820-6 单元 C 开工 | ✅ **完整闭环** | `4c010a5` Merge + `c78f954` 回勾，`main..分支 = 0`，测试 169→176 |
| **OP-0820-5 单元 B 开工** | 🔴 **做到一半，未合并** | 见下方 |
| OP-0820-9 .51 整机重启 | 🟡 **重启未执行**，前置采集已完成 | `4377d59`，四条发现 |
| OP-0820-W 门户挂载 | ⬜ 未做 | —— |

**单元 B 的实际停点**：计划 7 个 Task 只做完 Task 1-3，5 个 commit 全卡在分支
`claude/delivery-unit-b-run-build-dd9e53`（`main..分支 = 5`），第 3 章 11 项 checkbox 一项未勾。
未做的是这一章存在的理由：Task 4（`is_productive` 判定 / 3.9）、Task 5（兜底档位强制注入 +
`prompt_version` 升 v4 / 3.4·3.5）、Task 6（预算双口径 `MAX_TOTAL_ROUNDS` / 3.10）、Task 7（回勾）。
worktree 目录仍在，TDD 台账（git-ignored、存在 worktree 内部）没丢，可接着跑 → **OP-0820-5R**。

**当前待执行队列**（2026-08-20 12:50 后）：

| 现在能开 | 说明 | 冲突 |
|---|---|---|
| ~~OP-0820-5R~~ → **OP-0820-5S** | 原 session 活着，改用贴补丁形态 | 独占该 worktree |
| **OP-0820-10** 清文档失真 | Shao Peishen 定：先清，清完三份 plan 并行 | ⚠️ 与 9R 都写 `07`，串行 |
| **OP-0820-9R** .51 重启 | 等 08-22 周六 08:00 CST 窗口 | ⚠️ 与 10 都写 `07`，串行 |
| **OP-0820-W** 门户挂载 | 不在本仓库 | 无 |
| 10 之后三条并行 | **OP-0820-11**（D 的 plan）｜ **-12**（F 的 plan）｜ **-13**（ai-audit-trail 拆分方案） | 互相零重叠，与 5S 也零重叠 |

**已发现的两处文档失真**（OP-0820-10 处理）：
1. `m1-job-profile-intake` 变更包 58/69 未勾，未勾内容（Postgres / 企微 / LangGraph 图骨架）
   写于选型前，与实际实现（SQLite / Web）脱节，**按现状永远归不了档**。
   Shao Peishen 定：**重写 tasks 对齐现实**
2. `07-开发环境现状与优化待办.md` 的 P0 段说"81 个测试""`.github/workflows/` 不存在"，
   实际测试 176、CI 已有且撞过额度；P2#9 的"差 0.11 就归档"也是错的

---

## 一、执行秩序（历史，供追溯）

### 波 1 —— 六条，全部立即开工，无先后

**OP-0820-1** ｜ **OP-0820-2** ｜ **OP-0820-3** ｜ **OP-0820-7** ｜ **OP-0820-8** ｜ **OP-0820-W**

六条之间零依赖、零触碰区重叠。**开哪几条、什么顺序、隔多久开，都不影响正确性。**

### 波 2 —— 三条，各自前置满足即开工，三条互相可并行

| Opener | 前置（必须已 push 完成） |
|---|---|
| **OP-0820-5** run-build 单元 B | OP-0820-1 **且** OP-0820-2 |
| **OP-0820-6** run-build 单元 C | OP-0820-1 **且** OP-0820-3 |
| **OP-0820-9** .51 整机重启 | OP-0820-7 ＋ **人工放行**（见 §四） |

### 全部真依赖及其理由

| 依赖 | 为什么绕不开 |
|---|---|
| 5 ← 1 | worktree 从 main 的 commit 检出。窄化后的 spec 还在工作区没进 main，worktree 里就是旧契约。且 1 的 `git worktree prune` 必须在任何人开 worktree 之前跑完 |
| 5 ← 2 | 同上——计划文件没提交到 main，worktree 里就没有它 |
| 6 ← 1 | 同 5←1 的 prune 那半条 |
| 6 ← 3 | 同 5←2 |
| 9 ← 7 | 两条都写 `07-开发环境现状与优化待办.md`，同时改会互相覆盖 |
| 9 ← 人工放行 | **授权门，非节奏门**：重启中断 .51 上另外 5 个服务，属不可代项 |

### 已核实**不存在**的依赖（曾经排错过，别再排回去）

| 曾以为 | 实际 |
|---|---|
| 2/3 要等 1 提交完 | ❌ `spec-to-plan` 读的是**工作区**里的文件，磁盘上已经有了，提不提交都读得到 |
| 8 会和 1 抢 skill 文件 | ❌ 8 只碰 `kickoff/SKILL.md` 与 `CLAUDE.md`；1 提交的是 `run-build/SKILL.md`。零重叠 |
| 需要一个"统一提交"闸门 | ❌ 闸门本质是人工节奏控制。改成各自只提交自己的路径后就不需要了 |
| 并行时 `git status` 会互相污染 | ❌ 只要没人 `git add -A`。协议已写死禁止 |

---

## 二、并发协议（**每条 opener 内已逐字内嵌，此处备查**）

1. **只 `git add` 本条 opener 明确列出的路径。** ⛔ 禁止 `git add -A` / `git add .` / `git commit -a`。这是整套并行编排成立的唯一前提。
2. **`git status` 里出现别人的改动是正常的**——那是并行的另一条 opener 在干活。不要停下、不要问、不要顺手提交。
3. ⛔ 不要在 commit 前 pull（并发下会被别人的未提交改动挡住）；push 被拒才 `git pull --rebase --autostash origin main` 重试。
4. push 被拒（non-fast-forward）→ 再 `git pull --rebase origin main` 后重试，**最多 3 次**，仍失败停下贴错误。
5. 报 `.git/index.lock: File exists` → **等 5 秒重试，最多 5 次**。⛔ **绝不删除该锁文件**——那是另一个 session 正在用，删了会毁掉它的提交。

---

## 三、Opener 全文

```
[Mac] 0820-1 提交契约改动
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（提交 main 上的未提交改动，勾了改动就不在工作区）｜ 工作区: 仓库根

【一、任务】
在 HumanResource 仓库，提交 m1-intake-quality-fixes 三个决策的落地改动，并做三项仓库维护。

三个决策由 Shao Peishen 于 2026-08-19 拍板：
  1. Coverage Gap「整轮失败记累计耗时」→ 走窄化路（改 spec 原文），归档阻塞解除
  2. F（第 7 章·字段溯源）排位 → 最后
  3. D（第 6 章）先于 E（第 5 章）
最终执行顺序：B∥C → D → E → F → G

【二、并发协议】本仓库此刻可能有多条 opener 在并行跑，逐条遵守：
  - 只 git add 本条明确列出的路径，禁止 git add -A / git add . / git commit -a
  - git status 里出现列表之外的文件是正常的，不要停下、不要问、不要顺手提交
  - ⛔ 不要在 commit 前 pull——并发下别的泳道有未提交改动，rebase 会被挡（实测 error: cannot pull with rebase）。
    正确顺序：git add <明确路径> → commit → push；**push 被拒才** git pull --rebase --autostash origin main 再重试，最多 3 次
  - 报 .git/index.lock 已存在，等 5 秒重试最多 5 次，绝不删除该锁文件

【三、步骤】
1. git diff 通读这两个文件，核对两处契约改动是否互相自洽：
   - openspec/changes/m1-intake-quality-fixes/specs/intake-turn-observability/spec.md
     「逐轮时序留痕」那条 SHALL 已窄化为只覆盖「重试后成功」，并新增「整轮失败不留痕」
     Scenario，把"该轮视为未发生"写成正面契约
   - openspec/changes/m1-intake-quality-fixes/design.md
     Coverage Gaps 段已销号，明确写了「归档阻塞解除」
   两处口径若对不上，停下告诉我，不要自己改。

2. openspec validate m1-intake-quality-fixes --strict，必须通过。报错停下贴原文。

3. pytest，确认 169 全绿。这批只改文档，测试数与结果都不该变。

4. 分 3 个 commit 提交并推送，每个只 add 下列精确路径：

   commit 1（变更包契约）：
     openspec/changes/m1-intake-quality-fixes/specs/intake-turn-observability/spec.md
     openspec/changes/m1-intake-quality-fixes/design.md
     openspec/changes/m1-intake-quality-fixes/delivery-units.md
     openspec/changes/m1-intake-quality-fixes/tasks.md
   信息写明：决策人 Shao Peishen、日期 2026-08-19、理由（另一条路会让"系统炸了"的轮次
   伪装成"用户交互过的一轮"，污染 job_profile 行数这个唯一的轮次口径，而第 8 章的回放
   对比正是按它算修复效果）、接受的代价（整轮失败的耗时不可从持久层度量，只能从运行
   日志侧观察）

   commit 2（项目文档）：
     docs/session接力.md
     docs/openers/

   commit 3（工具链）：
     .claude/skills/run-build/SKILL.md
     CLAUDE.md（给 Paul 任务指令的强制格式已改为标题行 +【设置】单行）

5. 仓库维护三项（必须做完，OP-0820-5 / -6 开 worktree 依赖它）：
   a. git worktree prune，然后 git worktree list 贴结果
   b. 这三个分支的 git rev-list --count main..<分支> 我这边看全是 0（都已合并）。
      你自己逐个再验一次，确认是 0 才删本地分支，不是 0 就跳过那一个并告诉我：
        claude/m1-intake-quality-fixes-unit-a-017ce2
        claude/server-runtime-logging-run-caf1c9
        claude/sqlite-transaction-ownership-ch1-c1fc25
   c. ls -la .openspec-test-* 确认无残留（07 文档 P1 第 6 条），还在就删掉

【四、收工】列出本次新增/修改文件清单，贴 3 个 commit 的 hash。
```

---

```
[Mac] 0820-2 单元B实现计划
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（只产出一份 markdown 计划，不写代码不建分支）｜ 工作区: 仓库根

【一、任务】
用 spec-to-plan 技能，为变更包 m1-intake-quality-fixes 的交付单元 B 出一份实现计划，并提交。

先读这三份，是本次的全部上下文：
- openspec/changes/m1-intake-quality-fixes/delivery-units.md
  （拆分方案。§2.B 是单元 B 详情，§5 是跨单元接口约定）
- openspec/changes/m1-intake-quality-fixes/specs/intake-guided-options/spec.md
  （真正的输入。本单元对应其中「模糊回复与反问的兜底档位」「候选档位不得代替用户做
    决定」「零产出轮不消耗追问预算」三条要求）
- openspec/changes/m1-intake-quality-fixes/design.md（决策 3、4、5、10）

单元 B 范围 = tasks.md 第 3 章（3.1–3.11）＋ 两个安置项，共 13 项，预估 6-7 个 plan Task。

【二、并发协议】本仓库此刻可能有多条 opener 在并行跑：
  - 只 git add 你自己产出的那一个计划文件，禁止 git add -A / git add . / git commit -a
  - git status 里出现别的文件是正常的，不要停下、不要问、不要顺手提交
  - ⛔ 不要在 commit 前 pull——并发下别的泳道有未提交改动，rebase 会被挡（实测 error: cannot pull with rebase）。
    正确顺序：git add <明确路径> → commit → push；**push 被拒才** git pull --rebase --autostash origin main 再重试，最多 3 次
  - 报 .git/index.lock 已存在，等 5 秒重试最多 5 次，绝不删除该锁文件

【三、这四条必须体现在计划里】
1. Coverage Gap「整轮失败记累计耗时」已于 2026-08-19 走窄化路关闭、spec 原文已改，
   对单元 B 零影响：3.9 的 is_productive 判定不需要多加分支，3.10 的预算口径不需要多加
   规则。若读到任何还说"这是 B 的开工前置"的旧文字，一律以 spec.md 现行原文为准。

2. 有一个 minor 项落在 B，而且它是 B 的前置条件、不是顺手修：
   derive_question_id 未校验 field 是否属于 JobProfile.model_fields。
   理由：3.9 让 question_id 第一次从"只用来渲染"变成"参与判定"。模型给一个野 field
   （拼错，或幻觉出不存在的字段名）时，derive_question_id 今天原样接受，于是每轮都
   产出一个"新"的 question_id、每一轮都被判成有产出——MAX_ROUNDS 的有产出轮计数当场
   失效，正是这一章要修的那个故障换了个形式回来。
   落地形态：对不在 model_fields 里的 field 按"无 field"降级（走既有 free: 哈希分支，
   不抛异常——降级而非报错是单元 A 已确立的基调），并把"降级次数"与"null-field 次数"
   打点，供第 8 章 8.1 回放时看比例。

3. 一个待你在 design 阶段定的技术方案（属 CLAUDE.md 决策代理表的可代范围，不必回头找
   Shao Peishen，但必须在计划里写明选了哪个及理由）：已问台账落哪儿——
   (a) 新增一列，走 1.1 已建立的 init_schema 幂等加列路径（design 决策 10）
   (b) 放 profile_json 的下划线前缀内部键，与 _jd_text 同位置（design 决策 8 的做法）
   注意：1.1 的加列清单里没有这一项，是新决定；这份台账是单元 E 的 5.1 的雏形，E 会在
   其上扩"已答 / 重问次数"，选型要把 E 的扩展算进去。

4. 顺带必须做：prompt_version 从 intake-v3 升到 intake-v4（3.5 要改 SYSTEM_PROMPT。
   铁律 5：提示词改了不升版本，input_hash 与历史评分就对不上）。

Global Constraints 段必须从 CLAUDE.md 的「工程铁律」与「合规红线」两节逐字复制相关
条目，另外逐字加上 delivery-units.md §5 的第 3、4、5 条。

【四、交付前自查】
grep -c '^### Task ' <计划文件> 必须等于实际任务数、不能是 0。
（二级标题 ## Task N: 会让 scripts/task-brief 静默返回空 brief，看不出哪里错了）

【五、收工】
只提交你产出的那一个计划文件（docs/superpowers/plans/ 下），一个 commit，推送。
信息写明这是单元 B 的实现计划、执行顺序 B∥C → D → E → F → G。
列出文件清单、贴 commit hash 与任务清单。不要进 run-build。
```

---

```
[Mac] 0820-3 单元C实现计划
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（只产出一份 markdown 计划，不写代码不建分支）｜ 工作区: 仓库根

【一、任务】
用 spec-to-plan 技能，为变更包 m1-intake-quality-fixes 的交付单元 C 出一份实现计划，并提交。

先读：
- openspec/changes/m1-intake-quality-fixes/delivery-units.md（§2.C 是单元 C 详情，§5 是
  跨单元接口约定）
- openspec/changes/m1-intake-quality-fixes/specs/intake-guided-options/spec.md
  （本单元对应其中「结构化追问与可选项作答」在 Web 通道的落地）

单元 C 范围 = tasks.md 第 4 章（4.1–4.5），共 5 项，预估 2-3 个 plan Task。
触碰文件只有 app/web/static/index.html 一个——这是 C 能与单元 B 并行开工的全部前提，
计划里不得扩大触碰面。

【二、并发协议】本仓库此刻可能有多条 opener 在并行跑：
  - 只 git add 你自己产出的那一个计划文件，禁止 git add -A / git add . / git commit -a
  - git status 里出现别的文件是正常的，不要停下、不要问、不要顺手提交
  - ⛔ 不要在 commit 前 pull——并发下别的泳道有未提交改动，rebase 会被挡（实测 error: cannot pull with rebase）。
    正确顺序：git add <明确路径> → commit → push；**push 被拒才** git pull --rebase --autostash origin main 再重试，最多 3 次
  - 报 .git/index.lock 已存在，等 5 秒重试最多 5 次，绝不删除该锁文件

【三、这三条必须体现在计划里】
1. 点选提交不改 API 契约（delivery-units.md §5 约定 2，硬约束）：
   把选中的档位文本原样拼成该轮的回复文本，POST 到既有的 /api/jobs/{id}/reply。
   不许给 ReplyRequest 加 selected_options 字段——那会碰 app/web/server.py，单元 C 与
   B/D 立刻从并行变串行。
   附带好处：第 7 章 7.4 的"点选来源例外"大概率不必单独实现，因为被选中的档位文本
   本来就逐字出现在该轮用户原话里，7.3 的子串判定天然命中。

2. 不依赖单元 B 的代码：B 没合并之前 options 基本为空，渲染分支要自然退化成今天的纯
   文本，不能崩、不能渲染空控件。还要兼容 .51 上的历史裸字符串 payload——
   normalize_question_payload() 归一化后 options 恒为 []。

3. 如实写明这个单元的验收弱点，不要粉饰：本仓库没有 JS 测试运行器，
   tests/test_static_frontend.py 只能做字符串弱断言。C 是六个交付单元里"可独立测试"
   成色最弱的一个，真实验收落在第 8 章 8.4 的手工路径跑通上。所以计划里要列一份明确的
   手工验证步骤清单——点哪里、看到什么算通过、什么情况算失败——不要只写"手工验证"
   四个字了事。

Global Constraints 段从 CLAUDE.md 的「工程铁律」与「合规红线」逐字复制相关条目，
另外逐字加上 delivery-units.md §5 的第 2、5 条。

【四、交付前自查】
grep -c '^### Task ' <计划文件> 必须等于实际任务数、不能是 0。

【五、收工】
只提交你产出的那一个计划文件（docs/superpowers/plans/ 下），一个 commit，推送。
列出文件清单、贴 commit hash 与任务清单。不要进 run-build。
```

---

```
[Mac] 0820-7 .51 ACL排查
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（只读排查 + 改本地脚本，不动应用代码）｜ 工作区: 仓库根

【一、任务】
查清 deploy-server.ps1 里的 Set-Acl 段到底有没有生效，把结论落进文档，并顺带落地
06 清单第 1.1 条。

背景（这是一个"我们以为跑了、其实从没跑过"的缺口）：
2026-08-19 那次发版，.51 上的日志目录是应用自己 mkdir 出来的，deploy-server.ps1 里
第 68-73 行那段给 SYSTEM 账户加 Modify 权限的 Set-Acl 没有执行过。CI 也不跑 PowerShell，
所以这段代码至今零次真实执行。

【二、并发协议】本仓库此刻可能有多条 opener 在并行跑：
  - 只 git add 本条明确列出的路径，禁止 git add -A / git add . / git commit -a
  - git status 里出现别的文件是正常的，不要停下、不要问、不要顺手提交
  - ⛔ 不要在 commit 前 pull——并发下别的泳道有未提交改动，rebase 会被挡（实测 error: cannot pull with rebase）。
    正确顺序：git add <明确路径> → commit → push；**push 被拒才** git pull --rebase --autostash origin main 再重试，最多 3 次
  - 报 .git/index.lock 已存在，等 5 秒重试最多 5 次，绝不删除该锁文件

【三、步骤】
1. 读 deploy-server.ps1 第 60-90 行，把执行前提搞清楚：Set-Acl 是无条件跑，还是挂在
   "目录不存在才创建"那个分支里？（第 65 行有一句"已存在，跳过创建"）
   若它在 if 分支外、本该无条件执行，那问题就不是"没跑"而是"跑了但没效果"——这两种
   结论的修法完全不同，先定性再动手。

2. 在 .51 上查真实 ACL 现状。命令写成 ssh 包装形式，不要给裸命令，例如：
   ssh <你配置的 .51 主机> "powershell -Command \"(Get-Acl 'C:\apps\zhuopin-recruit-agent\logs').Access | Format-List\""
   看 SYSTEM 到底有没有 Modify。

3. 结合 1、2 给结论：
   - 若 ACL 其实已经对（Windows 从父目录继承），那这段 Set-Acl 是冗余的保险，记一笔
     "已验证冗余但保留"即可，不要删——它在别的机器上未必冗余
   - 若 ACL 不对，那是一个潜伏故障：应用现在能写日志只是因为跑在 SYSTEM 下、且目录是
     它自己建的。换个路径或换个账户就会静默失去日志，而失去日志本身没有告警。要修

4. 落地 06 清单第 1.1 条（🟢 直接照搬）：防火墙规则补 -Profile Any。
   见 06-企业AI转型资产借鉴清单.md 第 17 行起。同属 deploy-server.ps1，一起改。

【四、两条不要碰的】（07 文档第五节）
- deploy-server.ps1 的 UTF-8 BOM —— 去掉会让服务器上的 PowerShell 5.1 直接语法错误
- 服务器上的 .env —— 独立维护的生产凭据，不从开发机同步
另：不要重启 .51 上的任何服务。本条只做只读排查 + 改本地脚本，重启是 OP-0820-9 的事。

【五、收工】只提交这几个路径，一个 commit，推送：
  deploy-server.ps1
  05-发布运行手册.md
  07-开发环境现状与优化待办.md
  06-企业AI转型资产借鉴清单.md（把 1.1 标成已落地）
列出文件清单、贴 commit hash。
```

---

```
[Mac] 0820-8 06清单工具链四条
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（只改 skill 与 CLAUDE.md，不写代码）｜ 工作区: 仓库根

【一、任务】
把 06-企业AI转型资产借鉴清单.md 里已判定 🟢 且属于"改 skill 就能落地"的四条落地：
- 6.1（第 237 行）kickoff skill 补"收工列文件清单 + commit message"字段
- 8.1（第 369 行）标题行硬规则：每次交付/重发 opener，代码块前必须有一行标题
- 8.2（第 382 行）多 opener 并行/顺序矩阵：一次给出 ≥2 个 opener 时必须附顺序/并行说明
- 8.3（第 394 行）定时任务 prompt 绝对禁止提问

前三条改 .claude/skills/kickoff/SKILL.md；8.3 改 CLAUDE.md 或 kickoff skill，你判断哪个
更合适——若放 CLAUDE.md，先读该文件末尾的「CLAUDE.md 收录规则（防膨胀）」三关判据，
过不了关就放 skill。

【二、并发协议】本仓库此刻可能有多条 opener 在并行跑：
  - 只 git add 本条明确列出的路径，禁止 git add -A / git add . / git commit -a
  - git status 里出现别的文件是正常的，不要停下、不要问、不要顺手提交
  - ⛔ 不要在 commit 前 pull——并发下别的泳道有未提交改动，rebase 会被挡（实测 error: cannot pull with rebase）。
    正确顺序：git add <明确路径> → commit → push；**push 被拒才** git pull --rebase --autostash origin main 再重试，最多 3 次
  - 报 .git/index.lock 已存在，等 5 秒重试最多 5 次，绝不删除该锁文件
  - 不要碰 .claude/skills/run-build/SKILL.md 与 .claude/skills/spec-to-plan/SKILL.md
    ——另一条 opener 正在提交前者，且可能有 run-build 会话正在按它执行

【三、做法】
逐条读 06 清单里那一节的原文，再去 GitHub 读对应的原始做法，然后在本仓库自建实现。

⚠️ **原始做法的位置 2026-08-26 变了**：该项目已迁出 OneDrive，本地路径
~/Library/CloudStorage/OneDrive-Personal/Projects/企业AI转型/ **已经不存在**，别去那儿找。
改用 WebFetch 读 GitHub 公开仓库：

  https://raw.githubusercontent.com/Raytheoner/zhuopin-ai-transformation/master/<路径>

  ⚠️ 分支是 master 不是 main；中文路径要 percent-encode。
  本批四条的原始做法都在那边的 `0-学习与工具/` 与 `1-转型规划/0-全景路线图/专线opener模板库.md`。
  ⚠️ **没有本地副本就 grep 不了**——先用 GitHub 网页搜索或按目录逐层 WebFetch 定位到具体文件，
  找不到就登记「⏸ 留步：<哪一条> 的原始做法定位不到」并跳过那一条，**不要凭印象自己编一套**。

⚠️ 引入方式是「读取参考 + 在本仓库自建」，⛔ 不 clone 进来、不跨仓库引用或拷贝文件形成耦合。
⚠️ 规则真源在 .claude/skills/，不在 .claude/commands/。改规则改 skill。

注意 8.1 已有既定形态，照它落地、不要另创一套：CLAUDE.md「给 Paul 任务指令时的强制
格式」一节现行版本就是标准答案（标题行【OP-MMDD-X】+【设置】单行，两行都在代码块
内部）。kickoff skill 要与它一致，不一致就以 CLAUDE.md 为准。

落地后在 06 清单里把这四条标成已落地，注明落到了哪个文件。

【四、不要顺手做的 6 条 🟢】
- 3.3 webhook 发送封装 —— 属企微通道那一批的代码，现在做没有挂载点
- 7.1 审计留存年限、7.4 人工确认独立性 —— 是合规条款，应该进
  ai-audit-trail-and-outbound-gate 变更包的 spec，走 openspec-propose，不是改文档
- 9.1 判例批改法、9.2 一信≤3决策点、9.3 开放点计数 —— 属对外沟通/跟进信那条线

【五、收工】只提交这几个路径，一个 commit，推送：
  .claude/skills/kickoff/SKILL.md
  CLAUDE.md（若 8.3 落这里）
  06-企业AI转型资产借鉴清单.md
列出文件清单、贴 commit hash。
```

---

```
[Mac] 0820-W 门户挂载
【设置】执行环境: Win 笔记本手工（不在本仓库）｜ Session: 不适用 ｜ 分支: 不适用 ｜ worktree: 不适用 ｜ 工作区: 统一门户的 HTML

在统一门户的 HTML 里加一个板块：
  板块名：HR·招聘智能体
  外链：http://192.168.100.51:8095/hr/recruit-agent/

⚠️ 8095 是过渡端口，已登记为技术债，触发迁移的条件 = 统一门户网关上线。这个外链是
临时形态，网关上线时要一并改掉。

做完在 docs/session接力.md 的待决策表里销掉第 2 条。
```

---

```
[Mac] 0820-5 单元B开工
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: 不指定（勾 worktree 后 CC 自己生成 claude/<任务名>-dev-<hash>）｜ worktree: ✅ 勾选（写代码，需隔离工作区）｜ 工作区: worktree

前置：OP-0820-1 与 OP-0820-2 都已 push 完成。

【一、开工前确认】任一不满足就停下告诉我：
1. git log --oneline -10 origin/main 里能看到「交付单元 B 的实现计划」那个 commit
2. 同上，能看到 m1-intake-quality-fixes 契约窄化那个 commit
   （spec.md 的「整轮失败不留痕」Scenario 必须已经在 main 上）

【二、任务】
用 run-build 技能执行交付单元 B 的实现计划。
计划文件：docs/superpowers/plans/ 下单元 B 那一份（文件名含 unitB 或 chapter3）。
找到多份或找不到就停下问我。

开工前 rebase 到最新 main —— app/agents/intake_agent.py 与 app/graph/nodes.py 会被
B/D/E/F 四个单元连续改动，是这批最热的两个文件。

⚠️ 此刻可能有另一条 run-build（单元 C）在并行跑，它在自己的 worktree 里、只改
app/web/static/index.html。你不要碰那个文件，碰了两条分支合并时会冲突。

【三、收口要求】（run-build 的 SKILL.md 里有，这里再强调一遍，因为它失败时毫无症状）
合并后单独验一次 git rev-list --count main..<分支> 是否为 0。
finishing-a-development-branch 被跳过时唯一的表现就是 main 上什么都没有。

【四、收工】
两阶段 review 全过、tasks.md 第 3 章 checkbox 勾完之后停下，不要顺手做第 4 章。
列出本次新增/修改文件清单 + commit hash。
```

---

```
[Mac] 0820-6 单元C开工
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: 不指定（勾 worktree 后 CC 自己生成 claude/<任务名>-dev-<hash>）｜ worktree: ✅ 勾选（写代码，需隔离工作区）｜ 工作区: worktree

前置：OP-0820-1 与 OP-0820-3 都已 push 完成。

【一、开工前确认】
git log --oneline -10 origin/main 里能看到「交付单元 C 的实现计划」那个 commit。
看不到就停下告诉我。

【二、任务】
用 run-build 技能执行交付单元 C 的实现计划。
计划文件：docs/superpowers/plans/ 下单元 C 那一份（文件名含 unitC 或 chapter4）。

⚠️ 硬约束（违反了 B ∥ C 的并行立刻失效，而且是静默失效——两条分支合并时才炸）：
只许改 app/web/static/index.html 一个文件，外加它对应的 tests/test_static_frontend.py。
若实现过程中发现非改 app/web/server.py 不可，立刻停下告诉我，不要自己扩大触碰面。

⚠️ 此刻可能有另一条 run-build（单元 B）在并行跑，它在自己的 worktree 里改后端。
你不要碰任何后端文件。

【三、收口要求】
合并后单独验一次 git rev-list --count main..<分支> 是否为 0。

【四、收工】
tasks.md 第 4 章 checkbox 勾完之后停下。
列出本次新增/修改文件清单 + commit hash。
```

---

```
[Mac] 0820-9 .51整机重启
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（服务器运维 + 改文档，不动代码）｜ 工作区: 仓库根

前置（两条都必须已满足）：
1. OP-0820-7 已 push 完成 —— 两条都要写 07-开发环境现状与优化待办.md
2. Shao Peishen 本人已选定低峰窗口并知会相关方 —— .51 上还跑着另外 5 个服务，重启会
   全部中断。按 CLAUDE.md 决策代理表，生产服务器 .51 的重启决定属不可代项，代理人至今
   未指定

【一、任务】
验证 .51 上招聘智能体的计划任务能在整机重启后自动拉起。
背景（07 文档 P1 第 5 条）：计划任务配置看着是对的（SYSTEM 账户 + AtStartup + 失败重启
3 次），但从来没有真的验证过重启后会自动起来。

【二、并发协议】本仓库此刻可能有别的 opener 在并行跑：
  - 只 git add 本条明确列出的路径，禁止 git add -A / git add . / git commit -a
  - git status 里出现别的文件是正常的，不要停下、不要问、不要顺手提交
  - ⛔ 不要在 commit 前 pull——并发下别的泳道有未提交改动，rebase 会被挡（实测 error: cannot pull with rebase）。
    正确顺序：git add <明确路径> → commit → push；**push 被拒才** git pull --rebase --autostash origin main 再重试，最多 3 次
  - 报 .git/index.lock 已存在，等 5 秒重试最多 5 次，绝不删除该锁文件

【三、步骤】
1. 重启前：记录当前服务状态、进程 PID、日志文件末尾位置，全部落进 .claude/handoff/
   下一个 log 文件
2. 重启 .51（命令写 ssh 包装形式，不要裸命令）
3. 重启后按两个梯度验：
   - 计划任务是否被触发（查任务计划程序的历史记录，不要只看进程在不在）
   - http://192.168.100.51:8095/hr/recruit-agent/ 是否可访问
4. 专门验一个已知的耦合点：log_file 配的是相对路径 logs\app.log，靠计划任务的
   WorkingDirectory=C:\apps\zhuopin-recruit-agent 解析。重启后确认日志确实还写在那个
   目录里，没有被静默挪到别处
5. 结论写进 07 文档，销掉 P1 第 5 条

【四、收工】只提交这几个路径，一个 commit，推送：
  07-开发环境现状与优化待办.md
  .claude/handoff/ 下你新建的 log 文件
列出文件清单、贴 commit hash。
```

---

## 三·续、接续 Opener（2026-08-20 新增）

> **5S 与 5R 二选一，判据是客观的**：单元 B 的原 run-build session 还开着 → 用 **5S**（贴进去）；
> 已经关掉或失去响应 → 用 **5R**（新开 session 接管同一个 worktree）。
> 2026-08-20 12:50 核对：原 session **活着**（刚产出 `213f651`），当前该用 **5S**。

```
[Mac] 0820-5S 单元B收口补丁
【设置】执行环境: CC ｜ Session: 利旧（单元 B 的 run-build 原 session，TDD 台账在其 worktree 内）｜ 分支: claude/delivery-unit-b-run-build-dd9e53 ｜ worktree: 已在其中，不要新建 ｜ 工作区: 该 worktree

补充三条约束。你的上下文可能已经被压缩，这三条是最容易丢、丢了又不报错的：

【一、收口验证——这是上一次单元 B 卡住的确切原因，逐字执行】
合并回 main 之后，单独跑一次：
  git rev-list --count main..claude/delivery-unit-b-run-build-dd9e53
**必须输出 0。** 不是 0 就说明 finishing-a-development-branch 没跑到位，当场补上。
⛔ 在这条输出 0 之前，不许报告"单元 B 已完成"。
这个失败模式毫无症状：分支上 commit 好好的、测试全绿、你会觉得做完了，
但 main 上什么都没有。上一轮就是这样卡了 6 个 commit。

【二、Task 5 必须升 prompt_version】
Task 5 会改 SYSTEM_PROMPT，prompt_version 必须从 intake-v3 升到 intake-v4。
CLAUDE.md 铁律 5：提示词改了不升版本，input_hash 与历史评分就对不上，
而 PIPL 的说明权要求能回答"这条评分是哪个版本打的"。

【三、Task 7 必须回勾 checkbox】
openspec/changes/m1-intake-quality-fixes/tasks.md 第 3 章 11 项（3.1–3.11）
现在是 0 勾。Task 7 的职责包含把它们全部勾上。
勾不满 11 项就说明有 task 实际没做完，不要凑数勾。

【四、剩余范围提醒】
计划 7 个 Task，目前 6 个 commit 覆盖到 Task 3（含其修复波次）。还剩：
  Task 4  已问台账列与 is_productive 判定（tasks 3.9）
  Task 5  兜底档位强制注入、候选档位不入画像、prompt_version 升 v4（3.4 / 3.5 / 3.7）
  Task 6  追问预算改为双口径 MAX_TOTAL_ROUNDS（3.10）
  Task 7  老库迁移演练、文档回填与 WBS 回勾
Task 6 改预算口径时，business_key 继续用总行数，幂等语义不变（design.md 决策 5）。

【五、收工】
列出本次新增/修改文件清单 + commit hash + 第【一】条那个计数的实际输出。
```

---

```
[Mac] 0820-5R 单元B接管(备用)
⚠️ 仅当单元 B 的原 run-build session 已关闭或失去响应时才用这条；原 session 还活着就用 5S。
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: claude/delivery-unit-b-run-build-dd9e53 ｜ worktree: ❌ 不勾（复用已存在的 worktree，勾了会另建一个新的）｜ 工作区: /Users/paulshao/Projects/HumanResource/.claude/worktrees/delivery-unit-b-run-build-dd9e53

【一、为什么是接着做，不是重做】
交付单元 B 此前跑到一半停了，5 个 commit 卡在分支上没合并，main 上看不到任何 B 的东西
——这正是「finishing-a-development-branch 被跳过时毫无症状」那个静默失败模式。

已完成（对应计划的 Task 1-3，5 个 commit）：
  026d115 derive_question_id 校验 field 归属并打降级指标        → Task 1
  c65d39f 野字段指标明细加上限、补 LangGraph 重跑重复计数说明   → Task 1 修复波次
  c3e2d6b 领域选项库对象化，补齐采购侧词条与通用兜底档位        → Task 2
  9b721d5 领域档位取数改为按命中术语限定，堵住跨域串档          → Task 2 修复波次
  4054c5a 模糊回复与反问的确定性判定 is_vague_reply             → Task 3

未做（Task 4-7），且这四个才是第 3 章存在的理由：
  Task 4  已问台账列与 is_productive 判定（tasks 3.9）
  Task 5  兜底档位强制注入、候选档位不入画像、prompt_version 升 v4（tasks 3.4 / 3.5 / 3.7）
  Task 6  追问预算改为双口径 MAX_TOTAL_ROUNDS（tasks 3.10）
  Task 7  老库迁移演练、文档回填与 WBS 回勾

【二、开工前确认】任一不满足就停下告诉我：
1. worktree 目录仍在：
   ls /Users/paulshao/Projects/HumanResource/.claude/worktrees/delivery-unit-b-run-build-dd9e53
2. TDD 台账仍在（git-ignored，存在 worktree 内部，删了 worktree 就没了）
3. git rev-list --count main..claude/delivery-unit-b-run-build-dd9e53 == 5

⛔ 不要勾 worktree、不要新建分支。勾了 CC 会另建一个空的，5 个 commit 和台账都不在里面。

【三、任务】
用 run-build 技能，从 Task 4 开始跑完计划剩余部分。
计划文件：docs/superpowers/plans/2026-08-19-m1-intake-quality-fixes-unitB-fallback-options-and-budget.md

开工前 rebase 到最新 main —— 单元 C 已于本轮合并（4c010a5），它改了
app/web/static/index.html，与你无重叠，但 main 已经前进了。

⚠️ Task 5 会改 SYSTEM_PROMPT，prompt_version 必须从 intake-v3 升到 intake-v4。
   铁律 5：提示词改了不升版本，input_hash 与历史评分就对不上。
⚠️ Task 6 改预算口径时，business_key 继续用总行数，幂等语义不变（design.md 决策 5）。

【四、收口要求】（这一条就是上次栽的地方，逐字执行）
合并回 main 之后，单独跑一次：
  git rev-list --count main..claude/delivery-unit-b-run-build-dd9e53
**必须是 0**。不是 0 就说明 finishing-a-development-branch 没跑到位，当场补上，
不要报告"已完成"。

【五、收工】
tasks.md 第 3 章 11 项 checkbox 全部勾完（Task 7 的职责），停下。
列出本次新增/修改文件清单 + commit hash + 上面那条计数的实际输出。
```

---

```
[Mac] 0820-9R .51整机重启
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（服务器运维 + 改文档，不动代码）｜ 工作区: 仓库根

⛔ 前置：Shao Peishen 本人已选定窗口并按 7 个服务的范围知会到位。
   推荐窗口 2026-08-22（周六）08:00 CST，次选 08-21（周五）07:00 CST。
   ⚠️ .51 在 CST，开发机在 EDT，差 12 小时——按服务器本地时间判断是不是低峰，
   不要按你这边的时间。上一轮就是因为放行时刻服务器侧是周四上午 11:31 上班高峰而中止。

【一、上一轮已经做完的，不要重做】
2026-08-20 已完成重启前全量采集，落档
docs/findings/2026-08-20-51整机重启验证-重启前采集.md，过程台账
.claude/handoff/server-reboot-verify.log。开工先读这两份。

四条发现，其中三条改变了这次的执行方式：
A【阻断，已清除】任务计划程序 Microsoft-Windows-TaskScheduler/Operational 通道出厂即
  IsEnabled=False。不开就重启，100/129/102 事件根本不会产生，"任务是否被触发"那一梯度
  的证据白丢**且不报错**。已 wevtutil sl 启用并验证 IsEnabled=True。
  → 本轮开工先复验它仍是 True，被组策略刷回去就白跑。
B【订正】爆炸半径是**另外 7 个服务不是 5 个**，且含 UnifiedPortalGateway 门户网关本体。
  → 知会范围按 7 算。网关一停，其他部门的门户入口一起没。
C【风险兼收益】.51 上次开机 2026-05-26，8 个服务的 LastRunTime 全部晚于该时刻——它们的
  Boot 触发器**从未在真实重启中被验证过**。这次一次验 8 个，但坏一个就是那个部门下线。
  配置层面已逐条核过：账户全 SYSTEM、触发全 Boot、Enabled 全 True。
  → 重启后要逐个验这 8 个，不是只验招聘智能体。哪个没起来，当场记录并尝试手动拉起。
D【风险】CBS RebootPending=True + PendingFileRenameOperations=True + 85 天未重启。
  → **停机时长不可按普通重启预估**，可能要装一批挂起的更新。知会时把这条说在前面。

【二、并发协议】本仓库此刻可能有别的 opener 在并行跑：
  - 只 git add 本条明确列出的路径，禁止 git add -A / git add . / git commit -a
  - git status 里出现别的文件是正常的，不要停下、不要问、不要顺手提交
  - ⛔ 不要在 commit 前 pull——并发下别的泳道有未提交改动，rebase 会被挡（实测 error: cannot pull with rebase）。
    正确顺序：git add <明确路径> → commit → push；**push 被拒才** git pull --rebase --autostash origin main 再重试，最多 3 次
  - 报 .git/index.lock 已存在，等 5 秒重试最多 5 次，绝不删除该锁文件

【三、步骤】命令一律写成 ssh 包装形式，不要给裸命令
1. 复验 A：TaskScheduler/Operational 的 IsEnabled 仍为 True
2. 记录重启前基线：8 个服务的状态与 PID、招聘智能体日志文件末尾位置，落进
   .claude/handoff/server-reboot-verify.log（追加，不要覆盖上一轮的采集）
3. 重启 .51
4. 重启后按三个梯度验：
   a. 8 个服务的计划任务**是否被触发**——查任务计划程序的历史记录（100/129/102 事件），
      不要只看进程在不在
   b. http://192.168.100.51:8095/hr/recruit-agent/ 是否可访问
   c. 统一门户网关是否恢复（它是 B 项里爆炸半径最大的那个）
5. 专门验一个已知的耦合点：log_file 配的是相对路径 logs\app.log，靠计划任务的
   WorkingDirectory=C:\apps\zhuopin-recruit-agent 解析。重启后确认日志确实还写在那个
   目录里，**没有被静默挪到别处**
6. 结论写进 07 文档，销掉 P1 第 5 条；同时回填
   docs/findings/2026-08-20-51整机重启验证-重启前采集.md 的验证结果段

【四、收工】只提交这几个路径，一个 commit，推送：
  07-开发环境现状与优化待办.md
  docs/findings/2026-08-20-51整机重启验证-重启前采集.md
列出文件清单、贴 commit hash，以及 8 个服务各自的拉起结果。
```

---

## 第四批（2026-08-27）· 当前待执行

> ⚠️ **跑完即摘泳道标注。** 本文件是累积的，已完成条目若还带 `> 泳道：`，
> `run-lanes.sh` 下次会把它们连同新条目一起抓走**重跑**。摘法：改成
> `> ✅ 已完成 <日期> · 原泳道 <名>`（正则要求 `>` 后紧跟「泳道：」，改了就抓不到）。

| 泳道 | 编号 | 做什么 | 触碰区 |
|---|---|---|---|
| **溯源** | `0827B` | 交付单元 F 开工（第 7 章·字段溯源与编造率度量） | `intake_agent.py`、`graph/nodes.py`、`graph/state.py`（在 worktree 里） |
| **审计** | `0827C` | ai-audit U2 开工（`app/audit` 模块） | **全新目录 `app/audit/`，与整个仓库零文件重叠** |

**两条真并行**——U2 是 delivery-units §2.U2 写明的"本批里最干净的一个单元，可与 M1 的任何单元并行"，
不是勉强凑的并行。各自开 worktree，天然隔离。

前置均已满足：F 的 plan 6 Task、D/E 已合；U2 的 plan 6 Task、U1 已合。

---

> ✅ 已完成 2026-08-27（OK）· 原泳道 溯源（脚本自动摘，不再进 run-lanes）
>
> ⚠️ 本块**不带** `set_session_title` 那一行，这是豁免不是漏写：由 `run-lanes.sh` 以
> `printf | claude -p` 无头启动，print 模式下那个 MCP 工具未必挂载，且名字另由 `-n` 给。
> 🔴 若改为手工贴进 CC Desktop，**必须自行补上第 3 行**，否则侧边栏丢编号。

```
[Mac]0827B-单元F开工(第7章)
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: 不指定（勾 worktree 后 CC 自己生成 claude/<任务名>-dev-<hash>）｜ worktree: ✅ 勾选（写代码，需隔离工作区）｜ 工作区: worktree

【一、开工前确认】任一不满足就登记「⏸ 留步」并停下，不要硬上：
1. git log --oneline -20 origin/main 里能看到第 5、6 章的回勾 commit
   （单元 D 与 E 都已合并——F 依赖它们）
2. 计划文件在：docs/superpowers/plans/2026-08-25-m1-intake-quality-fixes-unitF-field-grounding.md
   grep -c '^### Task ' 该文件 应为 6

【二、任务】
用 run-build 技能执行交付单元 F 的实现计划，对应 tasks.md 第 7 章（7.1–7.11，11 项）。
开工前 rebase 到最新 main。

【三、这三条必须守住，第 1 条是本单元最容易炸的地方】
1. 🔴 7.1 把 profile_patch 的字段从裸值升级为 {value, source_quote, source_turn}，
   **这个结构升级不得穿透到落库层**。合进 profile_patch_accumulated 与
   job_profile.profile_json 的**必须仍然是裸值**，结构升级只允许存在于「模型返回 → 校验」
   这一段，落库前拍平。不拍平会同时炸三处：
     a. 单元 D 的 derive_unspecified_fields 会把 {"value": null,...} 当成"这个字段有值"，
        漏报回到修复前的故障
     b. POST /confirm 里的 JobProfile.model_validate 直接 422（headcount 收到一个 dict）
     c. effect_generate_and_persist_jd / jd_agent 全部按裸值读 profile_dict
   这条要逐字进 plan 的 Global Constraints，reviewer 直接查落库调用点。
2. 7.2 改 SYSTEM_PROMPT → prompt_version 升到 intake-v5。
   ⚠️ v4 是单元 B 占用的，别重号。铁律 5：提示词改了不升版本，input_hash 与历史评分对不上。
3. 本章**只观测不拦截**（design 决策 12），对三位业务经理零可见。
   ⛔ 不要加任何拦截/降级逻辑。未溯源率到多少才值得拦，等 ≥20 场真实会话的分布出来再单开变更。
   另：7.4 的「点选来源例外」先核还需不需要单独实现——单元 C 已按「点选文本原样拼进回复」
   落地，被选中的档位文本逐字出现在用户原话里，7.3 的子串判定大概率天然命中；
   若确认不必单独实现，在 tasks 里写明依据再勾。

⚠️ 此刻有另一条 run-build（ai-audit U2）在并行跑，它只碰全新目录 app/audit/。
你不要碰那个目录。

【四、收口要求】（单元 B 就栽在这，逐字执行）
合并回 main 后单独跑：git rev-list --count main..<你的分支>
不是 0 时**先别下结论**，再跑 git cherry -v main <你的分支>：
  - 全部行以 '-' 开头 → 假阳性（main 被 rebase 过，内容已在 main），删掉陈旧分支即可
  - 有任何一行以 '+' 开头 → 真没合，去补 finishing-a-development-branch
⛔ 在确认内容真的在 main 上之前，不许输出 OPENER_DONE。

【五、收工】
tasks.md 第 7 章 11 项 checkbox 勾完后停下，不要顺手做第 8 章。
列出新增/修改文件清单 + commit hash + 上面那两条计数的实际输出。
```

---

> ✅ 已完成 2026-08-27（PARTIAL）· 原泳道 审计（脚本自动摘，不再进 run-lanes）
>
> ⚠️ 本块**不带** `set_session_title` 那一行，这是豁免不是漏写：由 `run-lanes.sh` 以
> `printf | claude -p` 无头启动，print 模式下那个 MCP 工具未必挂载，且名字另由 `-n` 给。
> 🔴 若改为手工贴进 CC Desktop，**必须自行补上第 3 行**，否则侧边栏丢编号。

```
[Mac]0827C-audit U2开工(app/audit)
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: 不指定（勾 worktree 后 CC 自己生成 claude/<任务名>-dev-<hash>）｜ worktree: ✅ 勾选（写代码，需隔离工作区）｜ 工作区: worktree

【一、开工前确认】任一不满足就登记「⏸ 留步」并停下：
1. ai-audit-trail-and-outbound-gate 的第 1 章（U1）已全勾 6/6，且已合并进 main
2. 计划文件在：docs/superpowers/plans/2026-08-26-ai-audit-trail-unitU2-audit-module.md
   grep -c '^### Task ' 该文件 应为 6

【二、任务】
用 run-build 技能执行 ai-audit-trail-and-outbound-gate 交付单元 U2 的实现计划，
对应 tasks.md 第 2 章（app/audit：事件、双 sink、统一入口，9 项）。
开工前 rebase 到最新 main。

本单元是**全新目录、此时尚未接线**，现有行为完全不变——与整个仓库零文件重叠。
⚠️ 正因如此，⛔ **不要顺手去改 app/llm/gateway.py 或 app/graph/nodes.py**——
接线是 U3/U5 的事。碰了就与并行的单元 F 撞车（它正在改 nodes.py）。

【三、这三条逐字进 plan 的 Global Constraints】
1. 🔴 **AuditRecorder 的对外 API 必须是两段式**——写 SQLite 与 append JSONL 是两次
   可分别调用的动作，⛔ 不得打包在一个 record() 里同步完成。
   tasks 2.8 的「先 SQLite 后 JSONL」是**顺序**要求，两段式满足它；打包成一次调用会在
   事务回滚时产生「JSONL 有、SQLite 无」——design D1 明令这是更糟的偏差方向
   （审计查不到记录）。这条必须在 U2 定死，否则 U3/U5 接线时返工。
2. 🔴 ⛔ **禁止在 effect_* 函数体内 append JSONL**。允许的偏差只有单向：
   「SQLite 有、JSONL 缺行」（真身完整、镜像缺证据）；反向禁止。
   落地形态＝写 SQLite 那段进 effect_* 函数体，append JSONL 那段在 effect_* **返回之后**
   由调用点触发（此时幂等装饰器已 commit）。**这不需要改 idempotent_effect 装饰器。**
3. 🔴 **2.5 的第四个场景是这条防线的分水岭，不是"多写一个用例"**：
   「删光全部 prev_hash 字段后重写」必须在**第 2 行**就判断链断裂。
   平台侧已经踩过这个绕过，本仓库一次做对。2.5 与 2.6 是本单元含金量最高的两条。

另：SqliteSink.write ⛔ 不自行 commit，与 effect_persist_draft 同一约定（见 nodes.py:61 注释）。

⚠️ 此刻有另一条 run-build（单元 F）在并行跑，它改 intake_agent.py / graph/nodes.py。
你只碰 app/audit/ 新目录即可。

【四、收口要求】
合并回 main 后单独跑：git rev-list --count main..<你的分支>
不是 0 时再跑 git cherry -v main <你的分支>：全 '-' 是 rebase 假阳性（删陈旧分支即可），
有 '+' 才是真没合。⛔ 确认内容真在 main 上之前，不许输出 OPENER_DONE。

【五、收工】
tasks.md 第 2 章 9 项 checkbox 勾完后停下，不要顺手做第 3 章（那是 U3 接线）。
列出新增/修改文件清单 + commit hash + 上面那两条计数的实际输出。
```

---

## 第五批（2026-08-28）· 当前待执行

> ⚠️ **跑完即摘泳道标注。** 规则同第四批，此处不重复。

| 泳道 | 编号 | 做什么 | 触碰区 |
|---|---|---|---|
| **账目** | `0828A` | 账目对齐：回勾第 4 章、订正 4.6/4.7、补 `2.x` 与 `4.x` 偏离登记、订正 U5 计划三处 stale、落 TD-6/TD-7 并回勾 7.5/7.6 | `openspec/…/tasks.md`、`docs/tech-debt.md`、`plans/…unitU5-*.md` |
| **账目** | `0828C` | 7.3 运维口径页（含开关文件编码约束）＋ 7.4 追加 `06` 清单，回勾 7.3/7.4 | `docs/audit-and-outbound-ops.md`（新建）、`06-企业AI转型资产借鉴清单.md`、`openspec/…/tasks.md` |
| **账目** | `0828B` | ai-audit U5 开工（第 5 章·待审批队列与图节点接线） | `app/outbound/`、`app/graph/nodes.py`、`tests/`（在 worktree 里） |

**本批并行度是 1，这是故意的，不是没拆开。** 三条都要写同一个
`openspec/changes/ai-audit-trail-and-outbound-gate/tasks.md`——并发协议解的是 `.git/index.lock`，
解不了两条 session 同写一个文件（后写的整段覆盖先写的，**没有冲突提示**）。
所以三条同泳道串行，用「一次只有一个 `tasks.md` 写者」来保证账实一致，而不是靠自觉。
第 4 章 0/9 挂了两轮，病根就是写锁在三条 session 之间转手时没人认领。

顺序还有第二重理由：`0828C` 的 7.3 是 `0828B` 的**硬前置**——`tasks.md` 7.3 子条目逐字
「**U5 接线前必须确认本条已落地**，否则总开关在 `.51` 上不具备可操作性」。

**下一批候选（本批故意不带泳道标注，`run-lanes.sh` 抓不到）**：
- U6 spec-to-plan（第 6 章·合规断言与对账）——只新建一个 plan 文件、与本批零重叠，`0828A` 收工后随时可单开一条泳道真并行
- m1 第 8 章的 8.1 回放测试——⛔ 先别排。`build_question_ledger()` 每轮把整部问题史重数一遍，
  `question_id_metrics()` 的计数按 `O(q·R²/2)` 膨胀**且按轮次位置加权**（详见
  `app/agents/intake_question.py:38-64`）。这条至今只活在源码注释里，没进 `tech-debt.md`、
  也没进第 8 章任何 checkbox，登记去向要 Shao Peishen 定。不定就排，8.1 会拿一份被加权的比例
  当"修复前后对比"写进 `docs/m1-demo-pilot-feedback.md`——不报错、不红灯，直接产出一个错结论。

---

> ✅ 已完成 2026-08-28（OK）· 原泳道 账目（脚本自动摘，不再进 run-lanes）
>
> ⚠️ 本块**不带** `set_session_title` 那一行，这是豁免不是漏写：由 `run-lanes.sh` 以
> `printf | claude -p` 无头启动，print 模式下那个 MCP 工具未必挂载，且名字另由 `-n` 给。
> 🔴 若改为手工贴进 CC Desktop，**必须自行补上第 3 行**，否则侧边栏丢编号。

```
[Mac]0828A-账目对齐(第4章回勾与偏离登记)
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（只改文档，不写代码）｜ 工作区: 仓库根

【零、这条 opener 存在的理由】
U4（第 4 章·外发门禁）的代码早已全部合进 main 且两轮 review 完毕，但 tasks.md 第 4 章至今 0/9。
三方各写了一句"不是我"：
  · U4 计划完成判据 5：「由**当时持有 tasks.md 写锁的那条 session**回勾」——甩给别人
  · U3 那条 session 拿到写锁，只回勾了自己的第 3 章、只搬了 3.x 登记
  · U5 计划「需拍板/需协调」第 1 条：把"第 4 章已回勾"当成自己的**开工前置**
于是形成纯文档死锁：U5 等回勾 → 回勾等"某条持锁 session" → 一条 session 都没在跑。
**你就是那条持锁 session。** 本批后面两条排在你后面，别把锁带走，也别越界替它们勾。

【一、开工前确认】任一不满足就登记「⏸ 留步」并停下：
1. git rev-parse --abbrev-ref HEAD 为 main
2. git status --porcelain 里没有 openspec/ 与 docs/tech-debt.md 的改动
3. grep -c '^- \[x\]' openspec/changes/ai-audit-trail-and-outbound-gate/tasks.md 应为 22
   （不是 22 说明第 4 章已被别人动过，先查清再说）
4. git log -3 --format='%h %ad %s' --date=format:'%H:%M' -- app/outbound/
   最近一笔若在 30 分钟内 → 停下，可能还有 session 在动那个目录
5. ./venv/bin/python -m pytest -q 末行应为 675 passed
   （本条 opener 一行代码都不改，收工时这个数必须一模一样）

【二、六件事，全部只改文档】

1. 回勾第 4 章 4.1–4.9 共 9 个 checkbox。
   依据（逐条核过再勾，⛔ 别照抄本段就打钩）：
     5e3fe79 契约与词表 ｜ b3f8c46 证据采集三态 ｜ 32ccb14 六条判定与裸对象主防线
     534d310 两道闸 ｜ 9475543 异常按拦截与纯函数性 ｜ de0eb62 review round 1 七条
     121713f D-6 取 (b) ｜ b83f081 + adcee86 第七条 fail-closed 与 spec 同步
   ./venv/bin/python -m pytest tests/test_outbound_gate.py tests/test_outbound_gate_structure.py -q
   应为 124 passed。

2. 🔴 订正 4.6 / 4.7 的措辞——**这两条现在与代码和 spec 都对不上，直接勾就是把错的钉死。**
   现文是旧口径：4.6 把「requires_confirmation 为真」「severity 最高级」当成六条 fail-closed
   之一的终局拦截；4.7 说放行唯一路径要「显式为假 + 已知非最高级」。
   真值（D-6 取 (b)，2026-08-28 Shao Peishen 拍板，见 121713f / bcc41a1，spec 已同步）：
     · **五条消息畸形 ＝ 终局拦截，人也清不掉**：未登记类型、确认标志读不出布尔、
       风险等级不在词表、缺 AI 标识、收件对象读不出非空字符串
       （理由：签字的前提是知道自己在签什么；允许 confirmed_by 清掉畸形，
       人工确认就成了"随便谁点一下就能发任何东西"的橡皮图章）
     · **两类已知高风险 ＝ 风险分级，不是终局**：确认标志显式为真、风险等级为已登记最高级，
       由第一道闸的人以 confirmed_by 清关
     · 两道闸仍**串联**：人签了字、总开关关着照样不发；总开关仍每次求值
   按上面改写 4.6 / 4.7，并在改动处注明「2026-08-28 D-6 取 (b) 后订正」。
   ⛔ 不要改 specs/outbound-approval-gate/spec.md（它已经是对的），只改 tasks.md 这份 WBS。

3. 插入 `### 2.x 落地偏离登记（U2 实施，2026-08-27）`。
   逐字文本**已经写好**，在
   docs/superpowers/plans/2026-08-28-ai-audit-trail-unitU2-plan-reconciliation.md
   的 §七 Task 1 Step 3（约 216-235 行）那个 ```markdown 代码块里。**原样搬运，⛔ 不要改写措辞**
   ——判据列的测试名是机器可核的。位置：2.9 之后、`## 3.` 之前。
   搬完照该 plan 的 Step 4 跑一次测试名存在性核对（七个 def 名逐个 grep），
   任何一个 MISSING 都要改到对为止再提交。
   由来：这一节 2026-08-28 被 U5 计划 :883 引用过，但 tasks.md 里根本没有它，是条悬空引用。
   当初停手的理由是"另一条 session 正在写 tasks.md"，那条早收工了。

4. 插入 `### 4.x 落地偏离登记（U4 实施，2026-08-28）`，体例照 1.x / 3.x。
   材料在 docs/superpowers/plans/2026-08-28-ai-audit-trail-unitU4-outbound-gate-pure-functions.md：
     · :1676-1689 的**八条偏离表**（原样搬）
     · 「五项口径 —— ✅ 已拍板」D-1..D-5 的结论表，含批准人/时间/依据那三行留痕
       （CLAUDE.md「决策代理」要求：无留痕的代批视为未决）
     · 「D-6 —— ✅ 已拍板取 (b)」的两类划分表与四条锁定用例名
   位置：4.9 之后、`## 5.` 之前。

5. 🔴 订正 U5 计划的三处 stale——**本条 opener 里唯一一处不做就会让下游把已经对的代码改错的。**
   文件：docs/superpowers/plans/2026-08-28-ai-audit-trail-unitU5-queue-and-wiring.md
   它头尾自相矛盾：尾部 :1942 已写明「D-6 已闭环，无需再决」，但
     · :29 「落地在本单元 Task 1（改 app/outbound/gate.py、**翻转 U4 的锁定用例**、
       同步 tasks.md 4.6/4.7）」
     · File Structure 表 :117-119 三行把 gate.py / test_outbound_gate.py / tasks.md 派给 Task 1
   而实际 Task 1（:132）的 Files 块**根本不含这些文件**。
   照 :29 字面执行会把 test_confirmed_by_clears_a_known_high_risk_block_per_d6_option_b 与
   test_confirmed_by_cannot_clear_a_malformed_message 翻回 (a)——那正是 D-6 修掉的 bug：
   queue.approve() 带 confirmed_by 重走门禁仍被拦，**待审批队列里的候选人信件永远发不出去**。
   处置：把 :29 那段改写成「D-6 已于 2026-08-28 落码（121713f）并同步 spec（bcc41a1），
   本单元 ⛔ 不再改 gate.py 与 tests/test_outbound_gate.py；tasks.md 4.6/4.7 的措辞由 0828A 订正」，
   并**删掉 File Structure 表里那三行**。

6. 7.5 / 7.6 两条技术债登记 ＋ 回勾。
   docs/tech-debt.md 现有编号到 TD-5，按该文件开篇的体例（**每条必须写明触发条件与不还的后果**）追加：
     · TD-6 · operator_id 现阶段不可信（鉴权空壳）——材料见 tasks.md 7.5 与 design.md D7
     · TD-7 · JSONL 写入侧仅进程内锁，假设单进程部署——材料见 tasks.md 7.6
   ⚠️ 触发条件必须能从**已有白纸黑字**推出来（例如 7.5 的「M2 处理真实简历前」）。
   推不出来的，写「⏳ 待 Shao Peishen 定触发条件」并把该条留在那儿，⛔ 不要自己发明一个
   ——没有触发条件的条目会永远悬着，那是这份清单开篇明令禁止的。
   然后回勾 7.5 / 7.6。
   ⚠️ 顺带一处号码撞车：U5 计划 :1858 与 :1933 把「生产里没有调用方」写死成 `## TD-6`，
   与你刚占的号撞。把 U5 计划里那两处改成 `TD-8`。

7. 🔴 **抢救一份随时会被误删的清单——这一步请最先做，别排在最后。**
   U2 的 11 条 deferred minor 清单躺在
   .claude/worktrees/audit-module-u2/.superpowers/sdd/2026-08-26-ai-audit-trail-unitU2-audit-module/progress.md
   那是 **git-ignored 的 worktree 内部文件**：谁哪天清一次 worktree，它就永久没了，
   而 U2 的全分支 final review 至今没跑（2026-08-27 那轮预算耗尽断在这儿，
   见 .claude/handoff/lanes-20260827-160205-看护报告.md §二）。
   动作：整份复制到 docs/findings/2026-08-26-U2-deferred-minors-与final-review缺口.md，
   顶部加一段说明：来源路径、为什么要搬（原件 git-ignored）、搬运日期。
   ⚠️ **文件里那句 `Deferred minors awaiting triage (9)` 是错的，真值 11**——
   T6 的两条（backfill() 不检查 missing_id 是否真缺失就写、
   test_reconcile_is_clean_when_both_sides_match 没单独断言 missing_in_store）
   在正文里计了数、没进编号列表。搬运时**原文照搬、⛔ 不要改它**，
   在你加的说明段里注明这处计数不符，并写明分诊时以 **11** 为准。
   ⛔ 不要在这条 opener 里做分诊、不要改任何 app/audit/ 代码——那是 0828D 的活。

【三、边界】
⛔ 不改任何 .py。⛔ 不改 specs/ 下任何文件。⛔ 不碰 app/、tests/。
⛔ 不勾第 5、6 章任何一条（第 5 章是 0828B 的活）。
⛔ 不勾 7.1 / 7.2（CI 检查，属 U7 的另一半）、不勾 7.3 / 7.4（那是 0828C 的活）。

【四、并发协议】本仓库此刻可能有别的 session 在跑：
  - 只 git add 这三个路径：
      openspec/changes/ai-audit-trail-and-outbound-gate/tasks.md
      docs/tech-debt.md
      docs/superpowers/plans/2026-08-28-ai-audit-trail-unitU5-queue-and-wiring.md
      docs/findings/2026-08-26-U2-deferred-minors-与final-review缺口.md（新建）
    ⛔ 禁止 git add -A / git add . / git commit -a
  - git status 里出现别人的改动是正常的，不要停下、不要问、不要顺手提交
  - ⛔ 不要在 commit 前 pull（并发下必被别人的未提交改动挡住）。顺序：add 明确路径 → commit → push；
    push 被拒才 git pull --rebase --autostash origin main 再重试，最多 3 次
  - 报 .git/index.lock 已存在，等 5 秒重试最多 5 次，⛔ 绝不删除该锁（另一个 session 正在用）

【五、收工判据】四条全要贴**实际输出**，不许写"应该是"：
1. grep -c '^- \[x\]' openspec/changes/ai-audit-trail-and-outbound-gate/tasks.md → 33
   （22 ＋ 第 4 章 9 ＋ 7.5/7.6 两条）
2. openspec validate --all --strict → 5 passed, 0 failed
3. ./venv/bin/python -m pytest -q 末行 → 仍是 675 passed（一个字都不该变）
4. grep -n "落地偏离登记" openspec/changes/ai-audit-trail-and-outbound-gate/tasks.md → **四行**：1.x / 2.x / 3.x / 4.x
列出修改文件清单 ＋ 实际 commit hash，并反查 git log --oneline -3 与 git status，
确认你以为提交的东西真的在 main 上。
```

---

> ✅ 已完成 2026-08-28（PARTIAL）· 原泳道 账目（脚本自动摘，不再进 run-lanes）
>
> ⚠️ 本块**不带** `set_session_title` 那一行，这是豁免不是漏写：由 `run-lanes.sh` 以
> `printf | claude -p` 无头启动，print 模式下那个 MCP 工具未必挂载，且名字另由 `-n` 给。
> 🔴 若改为手工贴进 CC Desktop，**必须自行补上第 3 行**，否则侧边栏丢编号。

```
[Mac]0828C-运维口径页(7.3/7.4)
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（只写文档）｜ 工作区: 仓库根

【零、为什么它排在 U5 开工之前】
tasks.md 7.3 的子条目逐字：「**U5 接线前必须确认本条已落地**，否则总开关在 .51 上
不具备可操作性」。而 delivery-units.md 把整个第 7 章排在最后——**排期与前置条件互相打架**。
7.3 / 7.4 是纯文档，拆出来先做就解，不必等 U7 整章。

【一、开工前确认】任一不满足就登记「⏸ 留步」并停下：
1. 0828A 已收工：grep -c '^- \[x\]' openspec/changes/ai-audit-trail-and-outbound-gate/tasks.md 应为 33
   （不是 33 就是 0828A 没跑完或没跑对——同泳道串行，你不该比它早）
2. git rev-parse --abbrev-ref HEAD 为 main；git status --porcelain 里没有 openspec/ 的改动

【二、三件事】

1. 7.3 新建 docs/audit-and-outbound-ops.md，写清四件运维口径：
   a. 审计 JSONL 的路径与备份——**路径去 app/config.py 查真值**，⛔ 别照抄本段
   b. 链校验 verify_chain() 怎么手动跑——给一条可直接复制的命令，自己跑一遍贴输出
   c. CANDIDATE_OUTBOUND_ENABLED 的开关流程，以及**为什么不提供"一键放行全部"**
      （design.md 迁移计划回滚策略逐字：关掉开关是"更安全"的方向＝全拦；
      真要恢复无门禁投递必须**显式移除门禁节点**）
   d. 🔴 **开关文件的编码约束**——这是本页存在的首要理由。
      2026-08-27 Shao Peishen 拍板取方案 (b)：**不改代码**，写进文档。
      （改代码剥 BOM 属"在合规开关上放松"，是不可代项，他明确选择不改。）
        · 唯一允许的写法：[System.IO.File]::WriteAllText($path, 'true')
        · ⛔ 禁止 PowerShell 的 Out-File / > / >>（默认写 UTF-16LE）
        · ⛔ 禁止记事本的"UTF-8"另存（带 BOM）
        · 症状：_read_switch_file() 不剥 BOM、不认 UTF-16，用错写法
          **开关静默不生效且不报错**（方向 fail-closed，拦住了但打不开）
      写之前先读 app/config.py 里 _read_switch_file() 的真实实现核一遍。
   ⚠️ .51 是 Windows，但这条 opener **不需要 .51 访问**，⛔ 不要去连服务器。
      确实需要 .51 才能确认的点，如实登记「⏸ 留步：<原因>」，⛔ 不得假装闭合。

2. 7.4 在**仓库根**的 06-企业AI转型资产借鉴清单.md 追加本次借鉴记录：
   · 借的四条做法（哨兵双指标 / 无头引导头 / 泳道 / 错峰启动 ＋ 失败只停本泳道），
     源头与逐条说明见 docs/openers/run-lanes.sh 文件头
   · 自建的对应模块（本仓库自建实现，不跨仓库引用）
   · **明确未引入依赖、未拷贝代码**——两条判据各跑一遍贴实际输出：
       git diff origin/main -- requirements.txt pyproject.toml   （应为空）
       grep -rn "zhuopin_platform" app/                          （应零命中）

3. 回勾 tasks.md 的 7.3 / 7.4。⛔ 只勾这两条。

【三、边界】
⛔ 不改任何 .py。⛔ 不碰 app/、tests/、specs/。
⛔ 不碰 docs/tech-debt.md（0828A 刚写过，TD-6/TD-7 已占号）。
⛔ 不勾 7.1 / 7.2（CI 检查，属 U7 另一半，与 U6 串行）、不勾 7.5 / 7.6（0828A 已勾）。

【四、并发协议】
  - 只 git add 这三个路径：
      docs/audit-and-outbound-ops.md
      06-企业AI转型资产借鉴清单.md
      openspec/changes/ai-audit-trail-and-outbound-gate/tasks.md
    ⛔ 禁止 git add -A / git add . / git commit -a
  - git status 里出现别人的改动是正常的，不要停下、不要问、不要顺手提交
  - ⛔ 不要在 commit 前 pull；顺序：add 明确路径 → commit → push；push 被拒才
    git pull --rebase --autostash origin main 再重试，最多 3 次
  - 报 .git/index.lock 已存在，等 5 秒重试最多 5 次，⛔ 绝不删除该锁

【五、收工判据】贴实际输出：
1. grep -c '^- \[x\]' openspec/changes/ai-audit-trail-and-outbound-gate/tasks.md → 35
2. openspec validate --all --strict → 5 passed, 0 failed
3. ./venv/bin/python -m pytest -q 末行 → 仍是 675 passed（本条不改代码）
列出新增/修改文件清单 ＋ 实际 commit hash，并反查 git log --oneline -3 与 git status。
```

---

> ✅ 已完成 2026-08-28（OK）· 原泳道 账目（脚本自动摘，不再进 run-lanes）
>
> ⚠️ 本块**不带** `set_session_title` 那一行，这是豁免不是漏写：由 `run-lanes.sh` 以
> `printf | claude -p` 无头启动，print 模式下那个 MCP 工具未必挂载，且名字另由 `-n` 给。
> 🔴 若改为手工贴进 CC Desktop，**必须自行补上第 3 行**，否则侧边栏丢编号。

```
[Mac]0828D-U2补跑final review与minor分诊
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（只读核查 + 写一份报告，不改代码）｜ 工作区: 仓库根

【零、这条 opener 存在的理由】
交付单元 U2（app/audit，第 2 章 9/9，已合并已推）**从来没跑过全分支 final review**。
2026-08-27 那轮无头执行在 Task 6 修复轮结束时预算耗尽（$23.3 / $25），
final review、Task 6 的 scoped re-review、以及 11 条 deferred minor 的分诊**三件一起落空**。
当时留的三个选项（补跑 / 只分诊 / 接受现状挂到 U3 前做）**至今没人选**，
而 U3 已经在没有分诊的情况下做完了——这是第二次跳过。
现在 U5（第 5 章）又要建在 U2 的 AuditRecorder 之上。**这条 opener 就是不让它跳第三次。**
完整背景：.claude/handoff/lanes-20260827-160205-看护报告.md 的 §二 与 §三-①。

【一、开工前确认】任一不满足就登记「⏸ 留步」并停下：
1. 0828A 与 0828C 已收工：
   grep -c '^- \[x\]' openspec/changes/ai-audit-trail-and-outbound-gate/tasks.md 应为 35
2. 0828A 已把清单捞进仓库：
   ls docs/findings/2026-08-26-U2-deferred-minors-与final-review缺口.md
   ⚠️ 不在就去原址读，并**当场先把它复制到那个路径再往下做**：
   .claude/worktrees/audit-module-u2/.superpowers/sdd/2026-08-26-ai-audit-trail-unitU2-audit-module/progress.md
   （原件在 git-ignored 的 worktree 里，谁清一次 worktree 它就没了）
3. ./venv/bin/python -m pytest -q 末行应为 675 passed

【二、你要交付什么】
**一份报告，不是一批修改。** 写到
docs/findings/2026-08-28-U2-final-review与分诊结论.md
⛔ **不改一行 app/audit/**、⛔ 不改 tests/、⛔ 不勾任何 checkbox。
理由：11 条里哪几条值得返工，属「两轮 review 之间的 Triage：bug 定级、返工与否」——
CLAUDE.md 决策代理表把它列在**可代**，但**代理人至今未指定**，
而「代理人未指定期间的默认：可代项同样一律挂起等本人」。
所以你的产出是**带证据的建议**，处置由 Shao Peishen 定。

【三、两件事】

1. **补跑 U2 的全分支 final review。**
   审查对象 = U2 那 8 个新文件（app/audit/ 下 2091 行，纯新增、零修改既有文件）。
   分支 diff 的范围可用：git log --oneline c1ff33c..0fa4d54
   审查透镜逐字取自 CLAUDE.md「工程铁律」与 docs/superpowers/plans/2026-08-26-ai-audit-trail-unitU2-audit-module.md
   的 Global Constraints，重点四条：
     · 铁律 1：幂等记录与业务写必须同一事务、同一连接、同一个 BEGIN
     · 铁律 3/4：AI 评分留痕字段齐全；criterion_score 的 evidence_ref 为空不允许写入
     · design D1：SQLite 与 JSONL 两段式，⛔ 无打包方法（回滚时不得留下「JSONL 有、SQLite 无」）
     · 守护测试有没有阳性对照——本单元历史上栽过两次「守护恒真」
   ⚠️ **每条发现必须给可复现证据**（file:line ＋ 一条能让它变红的变异），
   ⛔ 没有变异证据的一律记作"疑似"，不许升格成"确认"。

2. **11 条 deferred minor 逐条分诊。** 出一张表：
   | # | 条目（原文一句） | 位置 file:line | 现在还成立吗 | 严重度 | 建议 | 依据 |
   ⚠️ 以 **11 条**为准，⛔ 不要用清单标题里那个 `(9)`——
   T6 的两条（backfill() 不检查 missing_id 是否真缺失、
   test_reconcile_is_clean_when_both_sides_match 没单独断言 missing_in_store）
   在正文计了数、没进编号列表。
   ⚠️ **逐条现场复核，⛔ 不许照抄清单描述**：U3 之后 app/audit/ 已经变过
   （RecorderAuditHook、criterion_key 白名单构造期强制），有些条目可能已经消失或改了形状。
   "还成立吗"这一列必须是你跑去看过源码得出的，不是推测的。
   执行者当时点名最值得先看的两条，优先复核：
     a. verify_chain() 不取写锁（写方持锁时并发读；当时未能复现撕裂读）
     b. broken_at 与报错文案里的「第 N 行」是**记录序号不是物理行号**——审计查证时会翻错行
   建议列只写三档之一：**本批修 / 挂技术债（给触发条件）/ 判定不成立可关闭**，并说明理由。

【四、边界与无人值守预案】
⛔ 不改 app/、tests/、openspec/、specs/ 下任何文件。本条只新增 docs/findings/ 一个文件。
⚠️ 若 superpowers 的 skill 在本 session 里**调不到**（无头 session 取不到项目作用域插件，
2026-08-27 实测，见看护报告 §五-①）：⛔ 不要停，也⛔ 不要假装调过——
照磁盘上的 .claude/skills/ 与 superpowers SKILL.md **手工执行同一套协议**，
并在报告开头如实登记「skill 未注册，按 SKILL.md 手工执行」。
凡属 CLAUDE.md 不可代项（合规红线七条、候选人对外通道开关、.51 发版、预算采购）——
登记后停在该点，⛔ 绝不替他拍，⛔ 不要输出问句等人回答。

【五、并发协议】
  - 只 git add 这两个路径：
      docs/findings/2026-08-28-U2-final-review与分诊结论.md
      docs/findings/2026-08-26-U2-deferred-minors-与final-review缺口.md（仅当第【一】节第 2 条由你补建时）
    ⛔ 禁止 git add -A / git add . / git commit -a
  - git status 里出现别人的改动是正常的，不要停下、不要问、不要顺手提交
  - ⛔ 不要在 commit 前 pull；顺序：add 明确路径 → commit → push；push 被拒才
    git pull --rebase --autostash origin main 再重试，最多 3 次
  - 报 .git/index.lock 已存在，等 5 秒重试最多 5 次，⛔ 绝不删除该锁

【六、收工判据】贴实际输出：
1. git status --short —— 只应有 docs/findings/ 下的文件
2. ./venv/bin/python -m pytest -q 末行 → **仍是 675 passed**（本条不改代码，变了就是越界了）
3. grep -c '^- \[x\]' openspec/changes/ai-audit-trail-and-outbound-gate/tasks.md → **仍是 35**（本条不勾任何 checkbox）
4. 分诊表行数 → 应为 11
报告里最后单列一节「需 Shao Peishen 定的」：哪几条建议本批修、修的话要不要单开一条 opener。
⛔ 不要自己去修。
```

---

> ✅ 已完成 2026-08-28（PARTIAL）· 原泳道 账目（脚本自动摘，不再进 run-lanes）
>
> ⚠️ 本块**不带** `set_session_title` 那一行，这是豁免不是漏写：由 `run-lanes.sh` 以
> `printf | claude -p` 无头启动，print 模式下那个 MCP 工具未必挂载，且名字另由 `-n` 给。
> 🔴 若改为手工贴进 CC Desktop，**必须自行补上第 3 行**，否则侧边栏丢编号。

```
[Mac]0828B-audit U5开工(队列与接线)
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: 不指定（勾 worktree 后 CC 自己生成 claude/<任务名>-dev-<hash>）｜ worktree: ✅ 勾选（写代码，需隔离工作区）｜ 工作区: worktree

【一、开工前确认】任一不满足就登记「⏸ 留步」并停下，⛔ 不要硬上：
1. grep -c '^- \[x\]' openspec/changes/ai-audit-trail-and-outbound-gate/tasks.md 应为 35
   （＝ 0828A 与 0828C 都已收工。U5 计划自己写着：「U5 开工前先确认第 4 章已回勾，
   否则门禁的行为可能还会再变一次」）
2. 计划文件在 docs/superpowers/plans/2026-08-28-ai-audit-trail-unitU5-queue-and-wiring.md
   grep -c '^### Task ' 该文件 应为 5
3. 🔴 打开该计划第 29 行附近，确认 0828A 已把 D-6 那段 stale 订正掉：
   **不该再出现「翻转 U4 的锁定用例」这样的字眼**，File Structure 表里也不该再有
   gate.py / test_outbound_gate.py 那两行。
   若还在 → 停下登记，⛔ **绝不照它执行**。照做会把
   test_confirmed_by_clears_a_known_high_risk_block_per_d6_option_b 与
   test_confirmed_by_cannot_clear_a_malformed_message 翻回 (a)，
   那正是 D-6 修掉的 bug：待审批队列里的候选人信件永远发不出去。
4. ./venv/bin/python -m pytest -q 末行应为 675 passed

【二、任务】
用 run-build 技能执行交付单元 U5 的实现计划，对应 tasks.md 第 5 章（5.1–5.9，9 项）。
开工前 rebase 到最新 main。

【三、这四条必须守住】
1. 🔴 **合并时 CANDIDATE_OUTBOUND_ENABLED 保持默认关闭（全拦）。**
   这是合规红线范围，design 迁移计划第 4 步要的就是那段"全关"观察期。
   ⛔ 不得改默认值；⛔ 不得加任何"跳过门禁"的开关、环境变量或参数
   （design.md 迁移计划回滚策略逐字：**不提供"一键放行全部"的配置项**）。
   reviewer 判据：本单元 diff 里不出现任何绕过门禁的口子。
2. 🔴 **判定只求值一次。** compute_outbound_gate( 在 deliver_candidate_message() 里
   只出现一次；留痕的 evidence 必须是那次调用返回的 decision.evidence **对象本身**，
   ⛔ 不重新求值（design D4——重新求值会制造"判定时未知、留痕时又变成已知"的不一致）。
3. 🔴 **AuditRecorder.record() 对外发事件恒为 False，⛔ 不得当成"写失败"、
   ⛔ 不得从 False 反推原因。** 外发事件在 SQLite sink 里没有真身（真身是 pending_approval），
   JSONL 镜像那一行是它唯一的留痕。这是 2026-08-28 对「残留 B」的拍板（取 (a)：
   调用点按自己写的 event_type 决定行为）。0828A 已把这条搬进 tasks.md 的
   「2.x 落地偏离登记」，去那儿核一眼再动手。
4. 沿用现有 idempotent_effect 装饰器：⛔ 不改装饰器、不改 effect_log、
   ⛔ 不改 effect_deliver_message 内部逻辑、不改 Channel Protocol。
   ⛔ 禁止在 effect_* 函数体内 append JSONL——U2 已落 AST 守护
   tests/test_audit_recorder.py::test_no_effect_function_appends_jsonl，
   **本单元是它第一次真正有活干**，跑完必须仍绿。

【四、遇到拍板点怎么办】
无人在场。凡属 CLAUDE.md 不可代项（合规红线七条的变更或单次例外、候选人对外通道开关、
真实简历处理范围、.51 发版决定、预算与外部采购）——**登记后停在该点**，
⛔ 绝不默认生效、⛔ 绝不替他拍、⛔ 不要输出问句等人回答，没人会回答。
本单元已知还开着的一条：**开关文件路径按进程 CWD 解析**
（tasks.md 第 1 章「遗留二」明写"未裁决，留给 U5 接线时定"，两条路是
"部署脚本里锁定工作目录" vs "改用绝对路径配置"，属 .51 生产运维部署决定）。
→ 处置：**按 fail-closed 一侧实现（读不到就当关）**，把两个选项与各自后果写进偏离登记，
   停在该点，⛔ 不要自己选部署方案。

【五、收口要求】（单元 B 就栽在这，逐字执行）
合并回 main 后单独跑：git rev-list --count main..<你的分支>
不是 0 时**先别下结论**，再跑 git cherry -v main <你的分支>：
  - 全部行以 '-' 开头 → 假阳性（main 被 rebase 过，内容已在 main），删掉陈旧分支即可
  - 有任何一行以 '+' 开头 → 真没合，去补 finishing-a-development-branch
⛔ 在确认内容真的在 main 上之前，不许输出 OPENER_DONE。

【六、收工】
tasks.md 第 5 章 9 项 checkbox 勾完、并补上 `### 5.x 落地偏离登记` 之后**停下**，
⛔ 不要顺手做第 6、7 章。
偏离登记的材料 U5 计划已自带三条（新增 messages.py / delivery.py、⛔ 不改 graph/build.py、
CandidateOutboundMessage 的两个默认值站在红线一侧），原样搬进去，再加上你自己新产生的。
⚠️ 技术债「生产里没有调用方」登记为 **TD-8**——TD-6 / TD-7 已被 0828A 的 7.5 / 7.6 占用，
U5 计划 :1858 / :1933 里那个写死的 TD-6 由 0828A 改过了，若还是 TD-6 说明 0828A 没做全，登记一笔。
列出新增/修改文件清单 ＋ 实际 commit hash ＋ 第【五】节那两条计数的实际输出。
```

---

## 第六批（2026-08-30）· 当前待执行

> ⚠️ **跑完即摘泳道标注。** 规则同第四批，此处不重复。

| 泳道 | 编号 | 做什么 | 触碰区 |
|---|---|---|---|
| **账目** | `0830A` | ai-audit U5 **续跑**（第 5 章·Task 5 端到端四条 ＋ 全分支终审 ＋ 回勾 ＋ 收口） | `app/outbound/`、`app/graph/nodes.py`、`tests/`、`openspec/…/tasks.md`、`docs/tech-debt.md`（**在既有 worktree 里**） |

**本批只有 1 条，是故意的。** U5 是唯一在推进交付的一条，且它要写
`openspec/changes/ai-audit-trail-and-outbound-gate/tasks.md`——只要它在跑，就不能有第二条写者
（并发协议解的是 `.git/index.lock`，解不了两条 session 同写一个文件：后写的整段覆盖先写的，
**没有冲突提示**）。第 4 章 0/9 挂了两轮，病根就是写锁在三条 session 之间转手时没人认领。

🔴 **本批与前五批有一处根本不同：`0830A` 利旧既有 worktree，不新建。**
U5 上一轮（`0828B`）判定 PARTIAL，Tasks 1–4 已完成并各自过审，9 笔提交挂在分支
`worktree-audit-u5-queue-and-wiring` 上真未合；**TDD 台账是 git-ignored 的，只存在于那个
worktree 内部**。新建 worktree ＝ 台账取不到 ＝ 从头再来，2026-08-20 单元 B 就是这么报废掉一轮的。

**下一批候选（本批故意不带泳道标注，`run-lanes.sh` 抓不到）**：
- U6 spec-to-plan（第 6 章·合规断言与对账）——只新建一个 plan 文件、与本批零重叠，`0830A` 收工后可单开一条泳道真并行
- U2 的 5 条 deferred minor（`0828D` 分诊，最小集 `#10 + #6`）——**待 Shao Peishen 定修不修**，未定不排

---

> ✅ 已完成 2026-08-30（PARTIAL）· 原泳道 账目（脚本自动摘，不再进 run-lanes）
>
> ⚠️ 本块**不带** `set_session_title` 那一行，这是豁免不是漏写：由 `run-lanes.sh` 以
> `printf | claude -p` 无头启动，print 模式下那个 MCP 工具未必挂载，且名字另由 `-n` 给。
> 🔴 若改为手工贴进 CC Desktop，**必须自行补上第 3 行**，否则侧边栏丢编号。

```
[Mac]0830A-audit U5续跑(Task5与收口)
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: worktree-audit-u5-queue-and-wiring（**已存在，⛔ 不新建**）｜ worktree: ⛔ **不勾** —— 利旧现有 .claude/worktrees/audit-u5-queue-and-wiring ｜ 工作区: 该 worktree

【零、这是续跑，不是开工 —— 先读这一段】
U5 已跑过一轮（`[Mac]0828B`），判定 PARTIAL：Tasks 1-4 做完并各自过审，**Task 5 未开工**、
全分支终审未跑、未合并回 main。成果 ＝ 分支 worktree-audit-u5-queue-and-wiring 上 9 笔真未合的提交。
🔴 **TDD 台账是 git-ignored 的，只存在于那个 worktree 内部**：
  .claude/worktrees/audit-u5-queue-and-wiring/.superpowers/sdd/2026-08-28-ai-audit-trail-unitU5-queue-and-wiring/
含 progress.md、4 份 task brief、4 份 task report、6 份 review diff。
⛔ **绝不 `git clean -fdx` 那个 worktree、绝不新建 worktree、绝不重做 Tasks 1-4。**
2026-08-20 单元 B 就是靠「新起 session 从头再来」把一轮工作报废掉的。

【一、开工前确认】任一不满足就登记「⏸ 留步」并停下，⛔ 不要硬上：
  W=.claude/worktrees/audit-u5-queue-and-wiring
  B=worktree-audit-u5-queue-and-wiring
1. ls $W/.superpowers/sdd/2026-08-28-ai-audit-trail-unitU5-queue-and-wiring/progress.md
   —— 台账必须在。不在就停，⛔ 不要凭计划重做 Tasks 1-4
2. git rev-list --count main..$B          应为 9
   git cherry -v main $B | grep -c '^+'   应为 9
   （两条都是 9 ＝ 9 笔真未合，与上轮记录一致）
3. git -C $W status --short 应为空（上轮收干净了）
4. grep -c '^### Task ' docs/superpowers/plans/2026-08-28-ai-audit-trail-unitU5-queue-and-wiring.md 应为 5
5. cd $W && ./venv/bin/python -m pytest -q 末行应为 715 passed
   （main 侧是 675，差的 40 条是 Tasks 1-4 的增量）
   ⚠️ `python` 不在 PATH，本机只有 ./venv/bin/python
6. 第 5 章 checkbox 应为 0：
   sed -n '/^## 5\./,/^## 6\./p' openspec/changes/ai-audit-trail-and-outbound-gate/tasks.md | grep -c '^- \[x\]'

【二、任务】只做 Task 5，别的一律不碰
计划 docs/superpowers/plans/2026-08-28-ai-audit-trail-unitU5-queue-and-wiring.md 的
`### Task 5`（约 :1593）＝ tasks.md 的 5.6 / 5.7 / 5.8 / 5.9 四条端到端测试。
做完再依次做：全分支终审 → 补 `### 5.x 落地偏离登记` → 回勾第 5 章 9 项 → 收口双验证。
⛔ 不做第 6、7 章；⛔ 不重做 Tasks 1-4；⛔ 不改已过审的 Tasks 1-4 代码，除非终审判它有真缺陷。

开工先 rebase 到最新 main：分支落后 2 笔（`e6f8fe6` / `07b3a45`），**纯文档、与本分支触碰区零交集**，
预期无冲突。真出冲突 → 停下登记，⛔ 不要 `-X ours/theirs` 强行压过去。

【三、这条最容易栽，逐字执行】
🔴 **计划里 Task 5 的重放测试会假绿。** 上一轮 reviewer 已咬住这点，逐字记在 task-4-report.md 里：
那条测试**只断言 effect_log 计数**。而 `idempotent_effect` 重放时返回 None，业务写被跳过、
`mirror()` 却照样 append；外发事件在 SQLite sink 里**没有真身**（真身是 pending_approval），
JSONL 那一行是它唯一的留痕，而 `reconcile()` 比的是 id **集合**，重复行对它完全隐形。
上一轮已在 `07fccec` 修了代码，但**测试必须改成断言 JSONL 镜像行数**，否则守不住这个修复。
⇒ 5.8 必须断言：三次重放后，同一 `event.id` 在 JSONL 里**只有一行**。
⇒ 写完必须做变异验证：把 `07fccec` 的那个返回值判断改回无条件 append，**这条必须变红**。
   不变红 ＝ 测试没咬住 ＝ 重写。⛔ 不许留着当「已覆盖」。
⚠️ 这条同时是「不写自我实现的测试」的判据：断言的量（JSONL 行数）必须与构造的量
（重放次数）来自不同的源，⛔ 不许用同一个常量既造数据又当判据。

【四、其余必须守住的六条】
1. 🔴 `AuditRecorder.record()` 对外发事件**恒为 False**，⛔ 不当成「写失败」、⛔ 不从 False 反推原因。
   调用点按自己写的 event_type 决定行为（2026-08-28 对「残留 B」的拍板，取 (a)）。
   已搬进 tasks.md 的「2.x 落地偏离登记」，动手前去那儿核一眼。
2. 🔴 `CANDIDATE_OUTBOUND_ENABLED` 合并时保持**默认关闭**（全拦）。
   ⛔ 不改开关读取代码、⛔ 不加任何跳过门禁的开关／环境变量／参数
   （design 迁移计划回滚策略逐字：**不提供「一键放行全部」的配置项**）。属不可代项。
   reviewer 判据：本单元 diff 里不出现任何绕过门禁的口子。
3. ⛔ 不改 app/outbound/gate.py 与 tests/test_outbound_gate.py —— D-6 那四条锁定用例必须原样通过。
4. ⛔ 不改 `idempotent_effect` 装饰器、不改 `effect_log`、不改 `effect_deliver_message` 内部逻辑、
   不改 `Channel` Protocol。⛔ 禁止在 `effect_*` 函数体内 append JSONL
   （AST 守护 tests/test_audit_recorder.py::test_no_effect_function_appends_jsonl 跑完必须仍绿）。
5. 开关文件路径口径**已拍板**（Shao Peishen 2026-08-28）：取「部署脚本里锁定工作目录」。
   ⇒ 把这句写进接线处说明与运维页，⛔ 不在代码里做路径兜底（兜底＝在合规开关上放松，属不可代项）。
   ⛔ 不要重开这个决定 —— 第 1 章「遗留二」里那句「未裁决，留给 U5 接线时定」**已过期**。
6. 技术债「生产里没有调用方」登记为 **TD-8**（TD-6 / TD-7 已被 7.5 / 7.6 占用）。

【五、🔴 合并前的硬闸门 —— 这条决定你是 DONE 还是 PARTIAL】
tasks.md 第 5 章前言把 docs/audit-and-outbound-ops.md 第五节**第 3 项**
（在 .51 上按 4.1 实际创建一次开关文件 ＋ 跑 3.3 验证）列为本章前置，属**不可代项**，
只能 Shao Peishen 本人上机。开跑时它可能已闭合、也可能没有。判据：
  sed -n '/^## 五/,/^## 六/p' docs/audit-and-outbound-ops.md
读第 3 项是否已标注闭合（有实跑输出／日期／「已闭合」字样）。
- **已闭合** → 正常合并回 main，跑第【六】节收口，输出 OPENER_DONE
- **未闭合** → Task 5 照做、终审照跑、提交照提交、偏离登记照写，
  **停在合并前**：⛔ 不合并、⛔ 不勾第 5 章 checkbox（回勾判据是 final review 通过**且已合并**），
  输出 OPENER_PARTIAL 并写明「等 .51 §5-3」。
  ⛔ **不要模拟、不要跳过、不要自己在本机创建那个文件充数。**

【六、收口】（单元 B 就栽在这，逐字执行）
只有走到「已闭合 → 合并」那一支才跑这节。合并回 main 后单独跑：
  git rev-list --count main..worktree-audit-u5-queue-and-wiring
  git cherry -v main worktree-audit-u5-queue-and-wiring | grep -c '^+'
**两条都必须是 0。** rev-list 非 0 而 cherry 计数为 0 ＝ rebase 造成的假阳性（陈旧分支，登记即可，
⛔ 不要自己删）；cherry 计数非 0 ＝ 真没合，去补 finishing-a-development-branch。
⛔ 在两条都出 0 之前，不许输出 OPENER_DONE。

【七、⛔ 不 push】
本条只提交到本地 main，⛔ 不 `git push`。推送由 Shao Peishen 另行决定。

【八、遇到拍板点怎么办】
无人在场。凡属 CLAUDE.md 不可代项（合规红线七条的变更或单次例外、候选人对外通道开关、
真实简历处理范围、.51 发版决定、预算与外部采购）——**登记后停在该点**，
⛔ 绝不默认生效、⛔ 绝不替他拍、⛔ 不要输出问句等人回答，没人会回答。
已知开着的一条：**TD-5**（`raw_response` 逐字存 vs spec 只约束输入），属合规红线、M1 不触发
⇒ 原样留在 docs/tech-debt.md，⛔ 不要顺手「解决」它。

【九、并发四条】
1. 只 `git add` 本条列出的路径：app/outbound/、app/graph/nodes.py、tests/、
   openspec/changes/ai-audit-trail-and-outbound-gate/tasks.md、docs/tech-debt.md。
   ⛔ 禁止 `git add -A` / `git add .` / `git commit -a`
2. `git status` 里出现别人的改动是正常的，不要停下、不要问、不要顺手提交
3. push 不适用（见第【七】节）
4. 报 `.git/index.lock` 已存在 → 等 5 秒重试最多 5 次，⛔ **默认绝不删除该锁**。
   唯一例外＝孤儿锁三项同时成立：① 文件 0 字节；② mtime 距今 > 10 分钟；
   ③ `pgrep -x git` 查无 git 进程（⛔ **不要用 `-f`**，`DigitalGoodsApi` 之类含 git 三字母会误报）

【十、收工】
报告里必须含：
  - Task 5 四条测试各自的红 → 绿证据，以及第【三】节那条变异验证的**实际输出**
  - 变异检查里活下来的变异体、各自补了什么守卫、补完是否重验转红
  - 全分支终审结论：发现了什么、改了什么、驳回了什么（驳回的要写理由）
  - 第 5 章 checkbox 实跑值（sed ＋ grep -c，目标 9；未合并则仍为 0 并说明原因）
  - 第【五】节闸门判定：.51 §5-3 闭合与否、你走的是哪一支
  - 第【六】节两条计数的**原始输出**（走了合并那一支才有）
  - pytest 前后数字（开跑前 715 ／ 跑完实测），⛔ 别用 grep -c "def test_" 数
  - 新增／修改文件清单 ＋ 实际 commit hash
```

---

## 第七批（2026-09-03）· 当前待执行

> ⚠️ **跑完即摘泳道标注。** 规则同第四批，此处不重复。

| 泳道 | 编号 | 做什么 | 触碰区 | 为什么这样排 |
|---|---|---|---|---|
| **审计** | `0903F` | ai-audit **U6 实现计划**（第 6 章·合规断言、对账与 CI） | `docs/superpowers/plans/`（新建一个文件） | 只产出一个新文件，零重叠 |
| **审计** | `0903G` | ai-audit **U6 run-build**（6.1–6.7）＋ 合并 ＋ 回勾第 6 章 | `app/audit/assertions.py`（新）｜`tests/test_audit_assertions*.py`（新）｜`.github/workflows/ci.yml`｜`openspec/…/ai-audit…/tasks.md`｜`docs/tech-debt.md` | 依赖 `0903F` 的 plan ⇒ 同泳道串行 |
| **回放** | `0903H` | intake **第 8 章·数据与回放**（8.2 副本加列 → 8.1 三段真实会话回放 → 8.5 延迟核对，产出对比数据） | `scripts/replay_pilot_sessions.py`（新）｜`tests/test_replay_pilot_sessions.py`（新）｜`docs/findings/2026-09-03-pilot会话回放对比.md`（新）｜`openspec/…/m1-intake-quality-fixes/tasks.md`｜`data/replay/`（git-ignored） | 与审计泳道**零文件重叠**（`tests/` 下是不同的新文件，各自只 add 自己的路径）⇒ 可并行 |
| **回放** | `0903I` | intake **第 8 章·文档回填**（8.6 / 8.7 / 8.8） | `docs/m1-demo-pilot-feedback.md`｜`docs/m1-demo-manager-guide.md`｜`openspec/…/m1-intake-quality-fixes/tasks.md` | 8.7 的数字来自 `0903H` ⇒ 同泳道串行 |

**两条泳道为什么能并行**：审计泳道写 `app/audit/` + `.github/`，回放泳道写 `scripts/` + `docs/` + 各自的新测试文件，
`delivery-units.md` §3.2 列的重叠区（`intake_agent.py` / `nodes.py`）**本批两边都不碰**。唯一共同的写者是各自的 `tasks.md`（两个不同变更包，两个不同文件）。

**本批故意不排的**（登记，不进泳道）：
- **push**：由看护者 `0903Z` 在 CC Desktop（有人能点放行）里做，⛔ 泳道内一律不 push
- **TD-9**（同一草稿二次拦截零留痕）：要改 `approve()` 已过审签名，属契约层变更需过审，Shao Peishen 尚未定修不修 ⇒ `0903G` ⛔ 不碰
- **8.4**（`.51` 上用新 job 手工跑通整条路径）：要人在页面上操作，留给 Shao Peishen
- **8.9**（归档顺序）：`m1-job-profile-intake` 先归档，本批不动
- **§5-1 备份任务**：`.51` 常态化备份任务的确认/新增是运维决定，不进无头泳道

---

> ✅ 已完成 2026-09-03（OK）· 原泳道 审计（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh
> 以 printf | claude -p 无头启动，print 模式下那个 MCP 工具未必挂载，
> 名字另由 -n 给。🔴 若改为手工贴进 CC Desktop，必须自行补上第 3 行。

```
[Mac]0903F-audit U6计划(断言与CI)
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: main ｜ worktree: ❌ 不勾（只产出一份 markdown 计划，不写代码不建分支）｜ 工作区: 仓库根

【一、任务】
用 spec-to-plan 技能，为变更包 ai-audit-trail-and-outbound-gate 的交付单元 U6 出实现计划并提交。
范围 = tasks.md 第 6 章（6.1–6.7，7 项：三条合规断言 + 对账查询 + 拦截统计 + CI 接入 + 断言有效性反证）。
计划文件名：docs/superpowers/plans/2026-09-03-ai-audit-trail-unitU6-assertions-and-ci.md

先读（顺序不能反，⛔ tasks.md 只用来划范围，不是 writing-plans 的输入）：
- openspec/changes/ai-audit-trail-and-outbound-gate/delivery-units.md（§2.U6 与 §4 跨单元约定）
- openspec/changes/ai-audit-trail-and-outbound-gate/specs/（真正的输入）
- openspec/changes/ai-audit-trail-and-outbound-gate/design.md（D1 对账方向、迁移计划）
- app/audit/（U2 已落地的 AuditRecorder / JsonlChainSink.verify_chain / reconcile）与 app/storage/db.py 的三张表 DDL
- .github/workflows/ci.yml 现有 job 结构（6.6 要接进去，不另起一套 CI）

【二、这五条必须体现在计划里，1–3 条逐字进 Global Constraints】
1. **6.7 是本单元的价值所在，⛔ 不许写成"可选"**：三条断言在空表上全部恒真，"0 命中"同时兼容
   "红线守住了"和"断言没生效"两种解释。计划里 6.7 必须是独立 Task，判据＝故意插入一条
   `reason_type='ai_score'` 的拒绝记录 / 一条白名单外的 `criterion_key` → 对应断言**必须失败**；
   不失败 ＝ 断言写成了恒真 ＝ 重写。
2. **6.4 对账查询与 U2 的 `verify_chain()` 是两条不同的断言，不可互相替代**：`verify_chain()` 只证"链没被改"，
   证不了"该留的痕都留了"。两条各有独立测试。⛔ 不改 `verify_chain()`、⛔ 不改 `app/audit/sinks.py`。
3. **6.1 的 `rejection_record` 表在 M1 尚不存在**：断言实现为「表不存在即通过、表存在则计数必须为 0」，
   M2 建表后自动生效。⛔ 不在本单元建这张表。
4. 跨单元接口约定 §4 第 2 条（AuditRecorder 两段式、⛔ 禁止在 effect_* 函数体内 append JSONL）、
   第 7 条（不新增 zhuopin_platform 依赖、不跨仓库 import）、第 8 条（开工前 rebase 到最新 main）逐字抄进 Global Constraints。
5. CANDIDATE_OUTBOUND_ENABLED 默认关闭是不可代项，本单元只读、⛔ 不改开关读取代码。

【三、superpowers 取不到时的预案（无头场景已实测两次取不到，不是新故障）】
调不到 superpowers:writing-plans → 按磁盘上该 skill 的 SKILL.md 手工走完协议，产物必须带三级 `### Task N:` 标题
（二级会让 scripts/task-brief 静默返回空）。⛔ 不要因为调不到就停，⛔ 也不要退化成一段散文式清单。
在收工报告里单列一行「writing-plans 是否真调到 / 手工走的」。

【四、交付前自查】
grep -c '^### Task ' <计划文件> 必须 ≥ 3 且等于实际任务数；grep -c 'Global Constraints' 必须 ≥ 1。

【五、收工】
只 git add 那一个计划文件，一个 commit，⛔ 不 push（推送由看护者在 Desktop 里做）。
⛔ 不进 run-build（那是下一条 0903G 的事）。列出文件路径 + commit hash + Task 清单。
```

---

> ✅ 已完成 2026-09-03（PARTIAL）· 原泳道 审计（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh
> 以 printf | claude -p 无头启动，print 模式下那个 MCP 工具未必挂载，
> 名字另由 -n 给。🔴 若改为手工贴进 CC Desktop，必须自行补上第 3 行。

```
[Mac]0903G-audit U6开工(断言对账与CI)
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: 不指定（建 worktree 后 git 自己生成）｜ worktree: ✅ 勾选（写代码，需隔离工作区）｜ 工作区: worktree

【一、开工前确认】任一不满足就登记「⏸ 留步」并停下，⛔ 不要硬上：
1. grep -c '^### Task ' docs/superpowers/plans/2026-09-03-ai-audit-trail-unitU6-assertions-and-ci.md 应 ≥ 3
   （文件不在 ＝ 0903F 没跑成，停）
2. sed -n '/^## 5\./,/^## 6\./p' openspec/changes/ai-audit-trail-and-outbound-gate/tasks.md | grep -c '^- \[x\]' 应为 9
   （U5 已合 main，本单元的 6.5 拦截统计依赖它的 pending_approval 留痕）
3. sed -n '/^## 6\./,/^## 7\./p' 同文件 | grep -c '^- \[x\]' 应为 0
4. ./venv/bin/python -m pytest -q 末行记下 passed 数当本次基线（09-03 11:11 实测 720，⛔ 别抄，自己测）
   ⚠️ python 不在 PATH，本机只有 ./venv/bin/python

【二、任务】
用 run-build 技能执行 U6 的实现计划，对应 tasks.md 第 6 章（6.1–6.7，7 项）。
建 worktree 后按 run-build 前置检查 4 补三样：拷 .env、按需拷 data/、在 worktree 内建 venv 装依赖。
开工先 rebase 到最新 main。

【三、这六条必须守住】
1. 🔴 **6.7 反证不通过就不许合并。** 三条断言各自的"故意违例 → 断言必须失败"要有实际红→绿证据；
   同时做变异验证：把断言的比较条件改成恒真，对应测试必须变红。不变红 ＝ 重写。
2. ⛔ 不改 app/audit/sinks.py、app/audit/recorder.py、app/outbound/、app/graph/、app/storage/idempotency.py。
   本单元只新增 app/audit/assertions.py 与测试，以及在 .github/workflows/ci.yml 里**追加**一个 step，
   ⛔ 不重写现有 CI job、不改现有 pytest step。
3. 🔴 CANDIDATE_OUTBOUND_ENABLED 保持默认关闭；⛔ 不加任何跳过门禁的口子（不可代项）。
4. 6.1 的 rejection_record 表不存在即通过，⛔ 不建表；6.3 白名单取 app/audit 里已有的 criterion_key 来源，⛔ 不另造一份。
5. ⛔ 不碰 TD-9（同一草稿二次拦截零留痕）——它要改已过审的 approve() 签名，Shao Peishen 尚未定修不修。
   若本单元的 6.5 拦截统计撞上这个洞（第二次拦截统计不到），**如实写进偏离登记**，⛔ 不顺手修。
6. 并行同伴：回放泳道 0903H 正在改 scripts/replay_pilot_sessions.py、tests/test_replay_pilot_sessions.py、
   docs/findings/、docs/m1-demo-*、m1-intake-quality-fixes 的 tasks.md。**你一个都不要碰。**

【四、遇到拍板点怎么办】
无人在场。凡属 CLAUDE.md 不可代项——登记后停在该点，⛔ 绝不默认生效、⛔ 不输出问句。
其余歧义取保守方向并登记，继续跑。
superpowers 取不到 → 按磁盘上 SKILL.md 手工走完 subagent-driven-development 协议（每 Task 新鲜子代理、
两阶段 review、fix loop、进度台账），⛔ 不退化成自己逐个任务写代码；报告里单列「是否真调到」。

【五、收口】（逐字执行）
走 finishing-a-development-branch 合并回 main 后单独跑：
  git rev-list --count main..<你的分支>
  git cherry -v main <你的分支> | grep -c '^+'
两条都必须是 0。rev-list 非 0 而 cherry 为 0 ＝ 假阳性，登记即可；cherry 非 0 ＝ 真没合，回去补。
⛔ 两条都出 0 之前不许输出 OPENER_DONE。
合并后在 main 上：回勾第 6 章 7 项（依据写合并 commit）、补 `### 6.x 落地偏离登记`、
tasks.md 顶部进度行改成 51/53（第 7 章 7.1/7.2 未完，⛔ 不归档，写明依据）。⛔ 不做第 7 章。

【六、并发四条】
1. 只 git add：app/audit/assertions.py、tests/test_audit_assertions*.py、.github/workflows/ci.yml、
   openspec/changes/ai-audit-trail-and-outbound-gate/tasks.md、docs/tech-debt.md（若有登记）。
   ⛔ 禁止 git add -A / git add . / git commit -a
2. git status 里出现别人的改动是正常的，不要停下、不要问、不要顺手提交
3. ⛔ 不 push（推送由看护者在 Desktop 里做）
4. 报 .git/index.lock 已存在 → 等 5 秒重试最多 5 次，⛔ 默认绝不删除。唯一例外＝孤儿锁三项同时成立：
   0 字节 + mtime > 10 分钟 + `pgrep -x git` 无进程（⛔ 不用 -f）

【七、收工】报告含：每 Task 红→绿证据与 6.7 变异验证实际输出；两阶段 review 结论；
第 6 章 checkbox 实跑值（目标 7）；第【五】节两条计数原始输出；pytest 前后数字；文件清单 + commit hash。
```

---

> ✅ 已完成 2026-09-03（PARTIAL）· 原泳道 回放（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh
> 以 printf | claude -p 无头启动，print 模式下那个 MCP 工具未必挂载，
> 名字另由 -n 给。🔴 若改为手工贴进 CC Desktop，必须自行补上第 3 行。

```
[Mac]0903H-intake第8章数据与回放(8.2/8.1/8.5)
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: 不指定（建 worktree 后 git 自己生成）｜ worktree: ✅ 勾选（要写回放脚本与测试）｜ 工作区: worktree

【零、这条为什么现在能跑】
.51 于 2026-09-03 已发版至 d104249（0903D），第 1–7 章代码全部在生产上。三段真实会话
（反馈 1 供应链 a478499c 08-13 / 反馈 2 软件 19b6ec6d 08-18 / 反馈 3 采购 2494103e 08-18）
**只在 .51 的 data/demo.db 里**，本机 data/demo.db 是 08-10 的旧库（5 个 job，不含这三段）。
delivery-units.md §2.G 明写第 8 章不走 plan、按清单执行——本条不用 run-build，直接做。

【一、开工前确认】任一不满足就登记「⏸ 留步」并停下：
1. ssh zp51 "echo ok" 回 ok（不通 ＝ 不在内网，整条留步，⛔ 不要用本机旧库冒充）
2. ls .env 存在（回放要真调 LLM）
3. ./venv/bin/python -m pytest -q 记下 passed 数当基线
4. sed -n '/^## 8\./,$p' openspec/changes/m1-intake-quality-fixes/tasks.md | grep -c '^- \[x\]' 应为 0

【二、取数（在 .51 上做一致性快照，⛔ 不直接 scp 正在写的库文件）】
在 .51 上用 sqlite 的 backup API 出一份一致快照，再拉回来：
  ssh zp51 "powershell -NoProfile -Command \"cd C:\apps\zhuopin-recruit-agent; .venv\Scripts\python.exe -c \\\"import sqlite3; s=sqlite3.connect('data/demo.db'); d=sqlite3.connect('data/demo-snapshot-20260903.db'); s.backup(d); d.close(); print('ok')\\\"\""
  mkdir -p data/replay && scp zp51:C:/apps/zhuopin-recruit-agent/data/demo-snapshot-20260903.db data/replay/demo-51-20260903.db
  ssh zp51 "powershell -NoProfile -Command \"Remove-Item 'C:\apps\zhuopin-recruit-agent\data\demo-snapshot-20260903.db'\""
引号嵌套在 shell 层报语法错 → 改用 powershell -EncodedCommand（0903A/0903D 已用此法）。
data/ 是 git-ignored，⛔ 副本绝不进 git。⛔ 只拉这一个文件，不拉 logs/、不拉 .env。

【三、8.2 副本加列】
用 app.storage.db 的 init_schema / apply_column_migrations 对 data/replay/demo-51-20260903.db 跑一遍，记录：
加了哪些列、job 表计数（预期 15）、每个 job 的 job_profile 能否正常读。
⚠️ .51 今天已启动过新代码，列可能已加好 → 返回空列表也是正常结果，如实记。

【四、8.1 三段真实会话回放】
写 scripts/replay_pilot_sessions.py：从副本的 conversation.history_json 取三个 thread 的**用户输入序列**
（thread_id 前缀 19b6ec6d / 2494103e / a478499c），逐轮喂给当前 intake 流程（走 app 现有入口，
⛔ 不另写一套调用），temperature=0，落到一个**临时本地库**（⛔ 不写副本、⛔ 不写 data/demo.db）。
每段输出四个数：空转轮数、最终未指定字段数、总轮数、单轮 LLM 延迟（均值/最大），
并从副本里算出**修复前**同名四个数做对比表。
🔴 读数前必须先读 app/agents/intake_question.py:30-60 那段注释：降级计数上界有已知重复计数问题，
且 8.1 **不得跨单元 E 直接比较 total / null_field**——对比表里把这两列标成"不可直接比"，⛔ 不硬比。
写 tests/test_replay_pilot_sessions.py：只测**解析与统计**（从 history_json 抽输入序列、四个数的计算），
⛔ 测试里不调真 LLM；真回放那段加 `@pytest.mark.skipif(not os.environ.get("REPLAY_LIVE"))`，
默认 pytest 必须不联网。
结果写 docs/findings/2026-09-03-pilot会话回放对比.md：对比表 + 原始输出 + 本次 llm_response_model +
按 llm_response_model 分组的 ungrounded_fields / written_fields 计数（这是 8.7 编造率的分子分母，
⛔ 本条只出原始数，不下"编造率是 X%"的结论，留给 0903I）。

【五、8.5 延迟核对】
用回放落库的 llm_latency_ms 与副本里修复前三段的 llm_latency_ms 对比，判据＝均值没有明显变差（>30% 记红）。
写进同一份 findings。

【六、回勾】
合并回 main 后在 m1-intake-quality-fixes/tasks.md 勾 8.1 / 8.2 / 8.5；
8.3 勾上并注明「0903D 发版前已快照 C:\apps\backups\20260903-1003（含 data\）；本条另有 backup API 一致快照」；
8.4 ⛔ 不勾，登记「留步：需 Shao Peishen 在页面手工跑通」。顶部进度行改 64/69。

【七、并发】
并行同伴：审计泳道 0903G 正在改 app/audit/、tests/test_audit_assertions*.py、.github/workflows/ci.yml。**不要碰。**
只 git add：scripts/replay_pilot_sessions.py、tests/test_replay_pilot_sessions.py、
docs/findings/2026-09-03-pilot会话回放对比.md、openspec/changes/m1-intake-quality-fixes/tasks.md。
⛔ 禁止 git add -A / git add . / commit -a；⛔ 不 push；index.lock 等 5 秒重试 5 次不删（孤儿锁三判据除外）。
收口：finishing-a-development-branch 合回 main → git rev-list --count main..<分支> 与 git cherry -v main <分支> | grep -c '^+' 都为 0 才输出 OPENER_DONE。

【八、拍板点】无人在场。回放数据是业务经理的岗位需求对话，不含候选人个人信息；若副本里发现任何简历/候选人字段，
**停止拉取、删除本地副本、登记留步**（真实简历处理范围属不可代项）。

【九、收工】报告含：取数是否成功、8.2 加列清单与 15 job 读取结果、三段回放的对比表、8.5 判定、
pytest 前后、文件清单 + commit hash + 两条收口计数原始输出。
```

---

> ✅ 已完成 2026-09-03（PARTIAL）· 原泳道 回放（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh
> 以 printf | claude -p 无头启动，print 模式下那个 MCP 工具未必挂载，
> 名字另由 -n 给。🔴 若改为手工贴进 CC Desktop，必须自行补上第 3 行。

```
[Mac]0903I-intake第8章文档回填(8.6/8.7/8.8)
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: main ｜ worktree: ❌ 不勾（只改文档）｜ 工作区: 仓库根

【一、开工前确认】
1. ls docs/findings/2026-09-03-pilot会话回放对比.md 必须在（不在 ＝ 0903H 没跑成，整条留步停下）
2. git log --oneline -5 里能看到 0903H 的合并 commit

【二、任务】
1. 8.6 回填 docs/m1-demo-pilot-feedback.md：把「反馈 2、3 的调查」三条发现标注为已修复，附回放对比数据
   （从 findings 抄表，⛔ 不改数字）
2. 8.7 用第 7 章口径算**首个真实编造率数字**：分子＝ungrounded_fields 计数、分母＝written_fields 计数，
   按 llm_response_model 分组，写进 docs/m1-demo-pilot-feedback.md。样本只有三段会话——
   **必须写明样本量与"不可外推"**。findings 里没给分母的模型不算，⛔ 不用 profile_json 键数反推分母。
3. 8.8 改 docs/m1-demo-manager-guide.md 第 2 题：把"确认画像"整页截图的提示挪到追问对话最后一步旁边；
   截图对象说明改得更难跳过（两位经理都贴成了 JD 截图）。**只改文档，⛔ 不改代码。**
4. 回勾 8.6 / 8.7 / 8.8，顶部进度行改 67/69。8.9 ⛔ 不做（归档顺序：m1-job-profile-intake 先归档）。

【三、并发】
只 git add：docs/m1-demo-pilot-feedback.md、docs/m1-demo-manager-guide.md、openspec/changes/m1-intake-quality-fixes/tasks.md。
⛔ 禁止 git add -A / git add . / commit -a；⛔ 不 push；index.lock 等 5 秒重试 5 次不删。
并行同伴 0903G 可能仍在改 app/audit/ 与 ai-audit 的 tasks.md，不要碰。

【四、收工】列出改动清单 + commit hash + 编造率那一段的原文。全部完成输出 OPENER_DONE。
```

---

## 第八批（2026-09-03 下午）· 当前待执行

> ⚠️ **跑完即摘泳道标注。** 规则同第四批，此处不重复。

| 泳道 | 编号 | 做什么 | 触碰区 | 为什么这样排 |
|---|---|---|---|---|
| **审计** | `0903N` | ai-audit **U7 实现计划**（7.1/7.2 两条 CI 检查） | `docs/superpowers/plans/`（新建） | 串行第 1 |
| **审计** | `0903O` | ai-audit **U7 run-build** ＋ 回勾 7.1/7.2 → 53/53，**不归档**（等 2.5 改完同一份 tasks.md） | `.github/workflows/ci.yml`｜`scripts/`（若加检查脚本）｜`tests/`｜audit `tasks.md` | 依赖 N |
| **审计** | `0903P` | `outbound-retry-audit-trace` **实现计划**（第 1+2 章合一个交付单元） | `docs/superpowers/plans/`（新建） | 与 O 零重叠，但 Q 要改 audit `tasks.md`（2.5），为免两条泳道同写一文件，整批串行 |
| **审计** | `0903Q` | `outbound-retry-audit-trace` **run-build**（1.1–2.6）＋ 2.7 归档本包 ＋ **归档 ai-audit 包**（此时 53/53 且 5.4 已订正） | `app/outbound/delivery.py` `queue.py`｜`app/graph/nodes.py`（docstring）｜`tests/test_outbound_*.py` `test_audit_*.py`｜`docs/tech-debt.md`｜两个包的 `tasks.md`｜`openspec/specs/`（归档） | 依赖 O（U7 53/53 才能归档）与 P |

**为什么只有一条泳道**：U7（O）与 TD-9 包（Q）都要写 `openspec/changes/ai-audit-trail-and-outbound-gate/tasks.md`
（O 回勾 7.1/7.2，Q 的 2.5 订正 5.4 公式），并发协议解不了两条 session 同写一个文件（第 4 章 0/9 挂两轮的病根）。
四条串行，估 2–3 小时。`--chain` 不开，看护者盯完一轮出报告。

**明令不进泳道**：8.9（先归档 `m1-job-profile-intake`——33/71 的包归档要人工核对两处矛盾表述，交互式做）；§5-1 备份任务；`.51` 发版（TD-9 修复合入后是否再发版，等 Shao Peishen）。

---

> ✅ 已完成 2026-09-03（OK）· 原泳道 审计（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh
> 以 printf | claude -p 无头启动，print 模式下那个 MCP 工具未必挂载，
> 名字另由 -n 给。🔴 若改为手工贴进 CC Desktop，必须自行补上第 3 行。

```
[Mac]0903N-audit U7计划(7.1与7.2 CI检查)
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: main ｜ worktree: ❌ 不勾（只产出一份 markdown 计划）｜ 工作区: 仓库根

【一、任务】
用 spec-to-plan 技能，为变更包 ai-audit-trail-and-outbound-gate 的交付单元 U7 出实现计划并提交。
范围 = tasks.md 第 7 章剩余两条：7.1（CI 禁止 `from/import zhuopin_platform`、禁止 `sys.path` 指向 OneDrive 路径的注入）、
7.2（CI 检查 `requirements.txt` 与 `pyproject.toml` 不含 `zhuopin_platform`；本变更依赖文件 diff 为空）。
文件名：docs/superpowers/plans/2026-09-03-ai-audit-trail-unitU7-boundary-ci.md
先读：delivery-units.md §2.U7 与 §4 第 7 条；specs/；.github/workflows/ci.yml 现有结构（U6 刚加了一个 compliance step，⛔ 不动它）；
06-企业AI转型资产借鉴清单.md §10（7.4 已实跑过 `grep zhuopin_platform app/` 零命中，这就是 7.1 要机器化的那条）。

【二、必须进 Global Constraints】
1. 只**追加** CI step，⛔ 不重写现有 job、不改 pytest/compliance step
2. 检查用仓库内脚本（scripts/ 下新建）或纯 shell，⛔ 不引第三方 action、不加依赖
3. 「本变更的依赖文件 diff 必须为空」以 `git diff --stat <U1 起点>..HEAD -- requirements.txt` 判，起点 commit 由你从 git log 找出并写死进 plan
4. 7.1/7.2 的检查必须有**反证**：故意塞一行 `import zhuopin_platform` 进临时文件，检查必须失败（不失败＝恒真）
superpowers 取不到 → 按磁盘 SKILL.md 手工走，产物必须带三级 `### Task N:`；报告里单列「是否真调到」。

【三、自查与收工】
grep -c '^### Task ' <计划文件> ≥ 2；只 git add 那一个文件，一个 commit；push 已有白名单，⛔ 不要因为被拦而反复重试，登记即可。
列出文件路径 + commit hash + Task 清单。⛔ 不进 run-build。
```

---

> ✅ 已完成 2026-09-04（OK）· 原泳道 审计（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh
> 以 printf | claude -p 无头启动，print 模式下那个 MCP 工具未必挂载，
> 名字另由 -n 给。🔴 若改为手工贴进 CC Desktop，必须自行补上第 3 行。

```
[Mac]0903O-audit U7开工(7.1与7.2)
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: 不指定（建 worktree 后 git 自己生成）｜ worktree: ✅ 勾选（写 CI 与脚本，需隔离工作区）｜ 工作区: worktree

【一、开工前确认】任一不满足即登记「⏸ 留步」并停：
1. grep -c '^### Task ' docs/superpowers/plans/2026-09-03-ai-audit-trail-unitU7-boundary-ci.md ≥ 2（文件不在＝0903N 没跑成）
2. grep -c '^- \[x\]' openspec/changes/ai-audit-trail-and-outbound-gate/tasks.md 应为 51
3. ./venv/bin/python -m pytest -q 记下 passed 数当基线（⛔ 不抄任何历史数字；python 不在 PATH）

【二、任务】用 run-build 执行 U7 计划（7.1/7.2）。建 worktree 后按 run-build 前置检查 4 补 .env / venv。开工先 rebase 到最新 main。
superpowers 取不到 → 按磁盘 SKILL.md 手工走完协议（每 Task 子代理、两阶段 review、fix loop、台账），⛔ 不退化成自己写；报告单列「是否真调到」。

【三、守住】
1. 🔴 反证必须真跑：塞 `import zhuopin_platform` 的临时文件让检查变红，再删掉变绿，两次输出都贴进报告
2. ⛔ 不动 U6 的 compliance step、不动 pytest step、不加依赖
3. ⛔ 不碰 app/outbound/、app/audit/、docs/tech-debt.md——后面 0903Q 要改
4. worktree 内若产生 data/ 下的东西（本条不该有），收口前拷回主工作区（run-build 收口第 2 步）

【四、收口】合并回 main → git rev-list --count main..<分支> 与 git cherry -v main <分支> | grep -c '^+' 都为 0 才算合。
合并后在 main 上回勾 7.1/7.2 → 53/53，补 `### 7.x 落地偏离登记`，顶部进度行改 53/53。
🔴 **⛔ 本条不跑 openspec-archive-change**——后面 0903Q 的 2.5 还要订正本 tasks.md 的 5.4 公式，归档由 0903Q 在 2.5 之后做。
在进度行写明「53/53，归档交 0903Q（等 5.4 公式订正）」。

【五、并发四条】只 git add：.github/workflows/ci.yml、scripts/<你新建的检查脚本>、tests/<你新建的测试>、
openspec/changes/ai-audit-trail-and-outbound-gate/tasks.md。⛔ 禁止 -A / . / -a；别人的改动不停不问不顺手提交；
push 已有白名单，被拒才 pull --rebase --autostash 重试 ≤3；index.lock 等 5 秒重试 5 次不删（孤儿锁三判据除外）。

【六、收工】报告含：反证两次输出、两阶段 review 结论、第 7 章 checkbox 实跑值（目标 6/6 即整包 53/53）、收口两条计数原文、pytest 前后、文件清单 + commit hash。
```

---

> ✅ 已完成 2026-09-04（OK）· 原泳道 审计（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh
> 以 printf | claude -p 无头启动，print 模式下那个 MCP 工具未必挂载，
> 名字另由 -n 给。🔴 若改为手工贴进 CC Desktop，必须自行补上第 3 行。

```
[Mac]0903P-TD9修复计划(retry-audit-trace)
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: main ｜ worktree: ❌ 不勾（只产出一份 markdown 计划）｜ 工作区: 仓库根

【一、任务】
用 spec-to-plan 技能，为变更包 outbound-retry-audit-trace（TD-9，09-03 由 0903K 立）出实现计划并提交。
🔴 范围 = tasks.md **第 1 章 + 第 2 章合为一个交付单元**（15 项，一条分支）。这是对「一章一 plan」的明示偏离，
理由：第 2 章是第 1 章的回归与契约同步，拆开合并会让 main 上出现"幂等键已改、5.4 字面规定还是旧的"的中间态。把这条理由写进 plan 头部。
文件名：docs/superpowers/plans/2026-09-03-outbound-retry-audit-trace.md
先读（⛔ tasks.md 只用来划范围，不是 writing-plans 的输入）：
- openspec/changes/outbound-retry-audit-trace/{proposal,design}.md 与 specs/**/spec.md（真正的输入）
- docs/tech-debt.md TD-9（成因两条叠加，缺一条修不好）
- app/outbound/delivery.py、app/outbound/queue.py、app/graph/nodes.py 的 effect_record_outbound_audit（只读）

【二、必须逐字进 Global Constraints】
1. design 决策 1：幂等键与 DecisionEvent.id 统一为 `{content_hash}:{allowed}:{reason}`，reason 以空串归一化 None；同原因重放键不变照样短路
2. design 决策 2：approve() 追加关键字参数 recorder: AuditRecorder；留痕提炼为公共函数，函数体内延迟 import 规避循环依赖
3. ⛔ 不改 CANDIDATE_OUTBOUND_ENABLED 默认值与读取逻辑（不可代项）；⛔ 不改 idempotent_effect 装饰器、effect_log 表、Channel Protocol、effect_deliver_message 函数体
4. ⛔ 禁止在 effect_* 函数体内 append JSONL（AST 守护 test_no_effect_function_appends_jsonl 必须仍绿）
5. ⛔ 不改 app/outbound/gate.py 与 tests/test_outbound_gate.py 的四条锁定用例；死锁防线两条测试原样通过（tasks 2.1/2.2）
6. 1.8 的重放测试判据＝JSONL 镜像行数，⛔ 不许只断言 effect_log 计数（U5 终审踩过的假绿）
7. 2.4 要让 6.5 outbound_block_stats 能看见第二次拦截——这是 TD-9 的可观测性目标本身，必须有测试
8. 2.5 改 ai-audit 包 tasks.md 5.4 字面公式；2.6 TD-9 销账保留成因原文；2.7 归档本包
superpowers 取不到 → 按磁盘 SKILL.md 手工走，产物带三级 `### Task N:`；报告单列「是否真调到」。

【三、自查与收工】grep -c '^### Task ' ≥ 4；只 add 那一个文件，一个 commit；push 已有白名单。列出文件路径 + commit hash + Task 清单。⛔ 不进 run-build。
```

---

> ✅ 已完成 2026-09-04（PARTIAL）· 原泳道 审计（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh
> 以 printf | claude -p 无头启动，print 模式下那个 MCP 工具未必挂载，
> 名字另由 -n 给。🔴 若改为手工贴进 CC Desktop，必须自行补上第 3 行。

```
[Mac]0903Q-TD9修复开工与双归档
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: 不指定（建 worktree 后 git 自己生成）｜ worktree: ✅ 勾选（写代码）｜ 工作区: worktree

【一、开工前确认】任一不满足即登记「⏸ 留步」并停：
1. grep -c '^### Task ' docs/superpowers/plans/2026-09-03-outbound-retry-audit-trace.md ≥ 4
2. grep -c '^- \[x\]' openspec/changes/ai-audit-trail-and-outbound-gate/tasks.md 应为 53（0903O 收工）。不是 53 → 本条照做 1.1–2.4 与 2.6，
   但 2.5 仍可改（改字面公式不依赖 7.x），**归档 ai-audit 包那一步跳过**并登记「等 U7」
3. ./venv/bin/python -m pytest -q 记下基线

【二、任务】用 run-build 执行 outbound-retry-audit-trace 计划（1.1–2.6）。建 worktree 后补 .env / venv；开工先 rebase 到最新 main。
superpowers 取不到 → 按磁盘 SKILL.md 手工走完协议；报告单列「是否真调到」。

【三、守住（与 plan 的 Global Constraints 同源，此处只列最易栽的）】
1. 🔴 1.8 重放测试必须断言 JSONL 镜像行数，并做变异验证：把去重条件改成无条件 append，测试必须变红
2. 🔴 1.7 "放行被总开关拦下"必须真留一条痕、原因＝外发总开关关闭；且 2.4 的 outbound_block_stats 能数到它
3. ⛔ 不改 gate.py / test_outbound_gate.py；⛔ 不改开关读取；⛔ 不在 effect_* 里 append JSONL
4. 2.5 只改 ai-audit 包 tasks.md 5.4 那一条的字面公式并追加一句指向本包，⛔ 不动其它勾选
5. 2.6 在 TD-9 末尾追加销账行，⛔ 不删成因原文
6. worktree 内 data/ 下若有产物，收口前拷回主工作区（run-build 收口第 2 步）

【四、收口与双归档】
合并回 main → rev-list 与 cherry 两条计数都为 0 → 回勾本包 1.1–2.6 → 
2.7：openspec-archive-change outbound-retry-audit-trace；
然后若 ai-audit-trail-and-outbound-gate 为 53/53（【一】2 成立）→ 也跑 openspec-archive-change 归档它，
归档前人工核一眼 specs 折进 openspec/specs/ 后 outbound-approval-gate 的「每一次外发尝试留痕」条款含本包 delta。
⛔ 两次归档都不得在 tasks.md 有未勾项时强行跑。

【五、并发四条】只 git add：app/outbound/、app/graph/nodes.py、tests/、docs/tech-debt.md、
openspec/changes/outbound-retry-audit-trace/、openspec/changes/ai-audit-trail-and-outbound-gate/、openspec/specs/、openspec/changes/archive/。
⛔ 禁止 -A / . / -a；别人的改动不停不问不顺手提交；push 已有白名单，被拒才 pull --rebase --autostash ≤3；index.lock 等 5 秒重试 5 次不删。

【六、收工】报告含：1.7/1.8 红→绿与变异验证输出、2.1–2.3 全量回归结果、2.4 统计输出、两阶段 review 结论、两个包最终 checkbox 数、
两次归档的实际落点（archive 目录名）、pytest 前后、文件清单 + commit hash。
```

---

## 第九批（2026-09-04）· 当前待执行

> ⚠️ **跑完即摘泳道标注。** 规则同第四批，此处不重复。

| 泳道 | 编号 | 做什么 | 触碰区 | 为什么这样排 |
|---|---|---|---|---|
| **确认** | `0904B` | `m1-job-profile-intake` **第 6 章·确认断点 实现计划**（6.1/6.3–6.9 ＋ 1.4 `human_review` 表 ＋ 9.3 留痕断言，共 10 项） | `docs/superpowers/plans/`（新建） | 串行 1 |
| **确认** | `0904C` | 第 6 章 **run-build** ＋ 合并 ＋ 回勾 | `app/web/static/index.html`｜`app/web/server.py`｜`app/graph/build.py` `nodes.py`｜`app/storage/db.py`｜`app/audit/assertions.py`（9.3）｜`tests/`｜intake 包 `tasks.md` | 依赖 B |
| **确认** | `0904D` | 第 7 章·JD 溯源与导出 **实现计划**（7.3 / 7.5 / 7.7，3 项） | `docs/superpowers/plans/`（新建） | 与 C 零重叠，但 E 与 C 同写 `index.html`/`server.py`/`tasks.md` ⇒ 整批串行 |
| **确认** | `0904E` | 第 7 章 **run-build** ＋ 合并 ＋ 回勾 | `app/agents/jd_agent.py`｜`app/web/static/index.html`｜`app/web/server.py`｜`tests/`｜intake 包 `tasks.md` | 依赖 D，且要等 C 合完 |

**为什么单泳道**：C 与 E 都写 `index.html` / `server.py` 和同一份 `tasks.md`，并发协议解不了同文件同写。四条串行，估 3–4 小时。
**看护者 `0904Z` 前置里先发一次 `.51`**（Shao Peishen 09-04 回「发」，把 TD-9 修复 `7faf682` 推上生产）——发版与泳道无文件重叠，发完再发车。

**明令不进泳道**：6.8 挂起提醒（无定时基础设施，plan 里明写留步）；5.6 超时置 abandoned（同上）；1.2b/5.8 硬门槛表（另一单元）；§5-1 备份任务。

---

> ✅ 已完成 2026-09-04（PARTIAL）· 原泳道 确认（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh
> 以 printf | claude -p 无头启动，print 模式下那个 MCP 工具未必挂载，
> 名字另由 -n 给。🔴 若改为手工贴进 CC Desktop，必须自行补上第 3 行。

```
[Mac]0904B-intake第6章确认断点计划
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: main ｜ worktree: ❌ 不勾（只产出一份 markdown 计划）｜ 工作区: 仓库根

【一、任务】
用 spec-to-plan 技能，为变更包 m1-job-profile-intake 的交付单元「第 6 章·确认断点」出实现计划并提交。
范围 = tasks.md 第 6 章 D 类 8 条（6.1 / 6.3 / 6.4 / 6.5 / 6.6 / 6.7 / 6.8 / 6.9）＋ 1.4（建表 human_review）＋ 9.3（审计断言：每次人工决策都有 human_review 记录）。
后两条并入的理由：6.4「记 human_review」没有表就落不了，9.3 是它的守护——tasks.md 顶部把三条归在同一组「人工决策留痕缺失」。
文件名：docs/superpowers/plans/2026-09-04-m1-job-profile-intake-unit6-approval-checkpoint.md
先读（⛔ tasks.md 只用来划范围）：
- openspec/changes/m1-job-profile-intake/specs/job-profile-approval/spec.md（真正的输入：画像确认断点 / 修改与重新生成 / 副作用幂等 / 决策留痕 四条 Requirement）
- openspec/changes/m1-job-profile-intake/design.md；tasks.md 顶部「2026-08-20 对齐现实」那段（6.1 是现网真实缺陷的描述在那里）
- 现状代码：app/graph/build.py:58-97 `_deliver_node`（confirmation_prompt 的构造，payload 已带 profile_patch_accumulated）、
  app/web/server.py:140-200（GET /api/jobs 与确认接口）、app/web/static/index.html:240 附近 confirmation_prompt 分支（只渲染一句话＋未指定字段，**从不渲染画像本身**）、
  app/storage/db.py（job_profile.version 已有；_ADDED_COLUMNS 机制）、app/storage/idempotency.py（idempotent_effect）

【二、必须逐字进 Global Constraints】
1. CLAUDE.md 工程铁律 1、2（副作用独占 effect_* 节点 + 幂等键 `{thread_id}:{node_name}:{business_key}` 落 effect_log；幂等记录与业务写**同一事务**；L3 纯函数 compute_*，副作用只在 effect_*）逐字抄
2. 确认 / 修改 / 放弃三个分支各自是独立的 effect_* 节点，⛔ 不合并成一个"处理确认"节点
3. human_review 走 CREATE TABLE IF NOT EXISTS（新表），⛔ 不进 _ADDED_COLUMNS；列＝决策人 / 决策类型 / 时间 / 关联画像版本 / 预留 batch_id。.51 上 17 个真实 job 的既有表一行不改
4. 6.1 只渲染中文字段名（index.html:162 那条既有约束），画像从 payload 的 profile_patch_accumulated 取，⛔ 不另加接口；前端资源与接口一律相对路径（部署约束 1）
5. 6.5 每一版草案都保留（job_profile 新 version，不覆盖）；6.6 上限 5 次，超限提示转人工编辑；6.7 abandoned 保留内容
6. 6.3 / 6.9 的"重启后可恢复"用 LangGraph SQLite checkpointer 的真实重开来验（新进程/新 graph 实例按 thread_id 恢复），⛔ 不用 mock 冒充重启
7. 6.8 挂起提醒：无定时基础设施，plan 里明写「⏸ 留步：等定时基础设施（与 5.6 同源）」，⛔ 不要自造 sleep 循环或后台线程充数
8. ⛔ 不碰 app/outbound/、app/audit/ 除 assertions.py 追加 9.3 一条断言外；⛔ 不改 effect_deliver_message、idempotent_effect、effect_log
9. 合规：确认是人工节点，必须留痕；⛔ 不得把 AI 评分或任何自动判定写成决策人
superpowers 取不到 → 按磁盘 SKILL.md 手工走，产物带三级 `### Task N:`；报告单列「是否真调到」。

【三、自查与收工】grep -c '^### Task ' ≥ 5；grep -c 'Global Constraints' ≥ 1。只 add 那一个文件，一个 commit，push（白名单已在）。⛔ 不进 run-build。
```

---

> ✅ 已完成 2026-09-04（PARTIAL）· 原泳道 确认（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh
> 以 printf | claude -p 无头启动，print 模式下那个 MCP 工具未必挂载，
> 名字另由 -n 给。🔴 若改为手工贴进 CC Desktop，必须自行补上第 3 行。

```
[Mac]0904C-intake第6章确认断点开工
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: 不指定（建 worktree 后 git 自己生成）｜ worktree: ✅ 勾选（写代码）｜ 工作区: worktree

【一、开工前确认】任一不满足即登记「⏸ 留步」并停：
1. grep -c '^### Task ' docs/superpowers/plans/2026-09-04-m1-job-profile-intake-unit6-approval-checkpoint.md ≥ 5（文件不在＝0904B 没跑成）
2. grep -c '^- \[x\]' openspec/changes/m1-job-profile-intake/tasks.md 应为 33
3. ./venv/bin/python -m pytest -q 记下 passed 数当基线（⛔ 不抄；python 不在 PATH）

【二、任务】用 run-build 执行第 6 章计划。建 worktree 后补 .env / venv / data（本机 data/demo.db 是 08-10 旧库，5 个 job，够测）。开工先 rebase 到最新 main。
superpowers 取不到 → 按磁盘 SKILL.md 手工走完协议；报告单列「是否真调到」。

【三、守住（与 plan 的 Global Constraints 同源，此处只列最易栽的）】
1. 🔴 6.1 的判据是**人眼可读的画像卡片**：测试要断言渲染结果里出现中文字段名与值，⛔ 不是断言"payload 非空"
2. 🔴 三个 effect_* 分支各自有幂等重放测试：同一 thread 同一 business_key 重跑，effect_log 与业务表行数恒等（铁律 1 的不变式）
3. 🔴 6.9 重启恢复测试必须真的新建 graph/checkpointer 实例再恢复，做变异验证：把恢复逻辑改成新建空 state，测试必须变红
4. ⛔ 不改 app/outbound/、app/audit/（除 assertions.py 追加 9.3）、effect_deliver_message、idempotent_effect
5. 6.8 按 plan 留步登记，⛔ 不自造定时器
6. worktree 内 data/ 下若有产物（临时库），收口前拷回主工作区（run-build 收口第 2 步）

【四、收口】finishing-a-development-branch 合回 main → git rev-list --count main..<分支> 与 git cherry -v main <分支> | grep -c '^+' 都为 0 才输出 OPENER_DONE。
合并后在 main 上回勾 6.1/6.3/6.4/6.5/6.6/6.7/6.9 + 1.4 + 9.3（6.8 不勾，条目后注明留步），补 `### 6.x 落地偏离登记`，顶部进度改 42/71。⛔ 不归档（D 类还剩 18 条）。

【五、并发四条】只 git add：app/web/、app/graph/、app/storage/db.py、app/audit/assertions.py、tests/、openspec/changes/m1-job-profile-intake/tasks.md、docs/tech-debt.md（若有登记）。
⛔ 禁止 -A / . / -a；别人的改动不停不问不顺手提交；push 白名单已在，被拒才 pull --rebase --autostash ≤3；index.lock 等 5 秒重试 5 次不删。

【六、收工】报告含：每 Task 红→绿、6.1 渲染断言样例、三个分支的幂等重放输出、6.9 变异验证输出、两阶段 review 结论、checkbox 实跑值、收口两条计数、pytest 前后、文件清单 + commit hash。
```

---

> ✅ 已完成 2026-09-04（OK）· 原泳道 确认（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh
> 以 printf | claude -p 无头启动，print 模式下那个 MCP 工具未必挂载，
> 名字另由 -n 给。🔴 若改为手工贴进 CC Desktop，必须自行补上第 3 行。

```
[Mac]0904D-intake第7章JD溯源与导出计划
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: main ｜ worktree: ❌ 不勾（只产出一份 markdown 计划）｜ 工作区: 仓库根

【一、任务】
用 spec-to-plan 技能，为变更包 m1-job-profile-intake 的交付单元「第 7 章·JD 溯源与导出」出实现计划并提交。
范围 = 7.3（溯源校验：文案里的技术要求都能追溯到画像字段）、7.5（AI 标识保护：常规编辑不可删；"标记为人工撰写"显式操作并留痕）、7.7（纯文本一键复制导出）。
文件名：docs/superpowers/plans/2026-09-04-m1-job-profile-intake-unit7-jd-grounding-and-export.md
先读：openspec/changes/m1-job-profile-intake/specs/job-description/spec.md（真正的输入）；design.md；
app/agents/jd_agent.py（AI_LABEL_TEMPLATE 与 JD 生成）；app/agents/field_grounding.py:112 `verify_field_grounding`（intake-fixes 第 7 章的溯源纯函数，7.3 复用其归一化+子串判定，⛔ 不另造一套）；
app/web/static/index.html 与 server.py 的 JD 展示/编辑路径（0904C 刚合入的确认断点改动也在里面，先 rebase 再读）。

【二、必须逐字进 Global Constraints】
1. 合规红线：AI 生成的 JD 须带标识（《AI 生成合成内容标识办法》）。7.5 的"标记为人工撰写"是唯一能去掉标识的路径，且必须留痕（谁、何时）；⛔ 常规编辑不得删标识
2. 7.3 溯源校验是纯函数、确定性、不调模型（同 design 决策 11 的形状）；未溯源只观测不拦截（同决策 12）
3. 铁律 3/4 不适用于 JD 文案本身，但 7.5 的留痕走 effect_* 节点 + 幂等键，同一事务
4. 前端相对路径；7.7 导出为纯文本，⛔ 不引第三方剪贴板库
5. ⛔ 不碰 app/outbound/、app/audit/、app/graph/nodes.py 的既有节点
superpowers 取不到 → 按磁盘 SKILL.md 手工走，产物带三级 `### Task N:`；报告单列「是否真调到」。

【三、自查与收工】grep -c '^### Task ' ≥ 3。只 add 那一个文件，一个 commit，push。⛔ 不进 run-build。
```

---

> ✅ 已完成 2026-09-04（PARTIAL）· 原泳道 确认（脚本自动摘，不再进 run-lanes）
> ⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh
> 以 printf | claude -p 无头启动，print 模式下那个 MCP 工具未必挂载，
> 名字另由 -n 给。🔴 若改为手工贴进 CC Desktop，必须自行补上第 3 行。

```
[Mac]0904E-intake第7章JD溯源与导出开工
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: 不指定（建 worktree 后 git 自己生成）｜ worktree: ✅ 勾选（写代码）｜ 工作区: worktree

【一、开工前确认】任一不满足即登记「⏸ 留步」并停：
1. grep -c '^### Task ' docs/superpowers/plans/2026-09-04-m1-job-profile-intake-unit7-jd-grounding-and-export.md ≥ 3
2. grep -c '^- \[x\]' openspec/changes/m1-job-profile-intake/tasks.md 应为 42（0904C 收工）。不是 42 → 照做但报告里说明，⛔ 不要去补第 6 章
3. ./venv/bin/python -m pytest -q 记下基线

【二、任务】用 run-build 执行第 7 章计划。建 worktree 后补 .env / venv / data。开工先 rebase 到最新 main（0904C 的改动在 index.html / server.py 里，必须先拿到）。
superpowers 取不到 → 按磁盘 SKILL.md 手工走完协议；报告单列「是否真调到」。

【三、守住】
1. 🔴 7.5 反证：常规编辑接口尝试删标识 → 必须被拒且标识仍在；"标记为人工撰写"后 human 留痕行存在。做变异验证：把拒绝逻辑去掉，测试必须变红
2. 🔴 7.3 用一段画像里没有的技术要求做反例，必须被判为未溯源；用画像有、措辞不同的做正例，必须判已溯源
3. ⛔ 不动 0904C 刚合入的确认断点代码，除非终审判它有真缺陷（登记后改，⛔ 不顺手重构）
4. worktree 内 data/ 产物收口前拷回主工作区

【四、收口】合回 main → rev-list 与 cherry 都 0 → 回勾 7.3/7.5/7.7，补 `### 7.x 落地偏离登记`，顶部进度改 45/71。⛔ 不归档（D 类还剩 15 条）。

【五、并发四条】只 git add：app/agents/、app/web/、app/graph/、tests/、openspec/changes/m1-job-profile-intake/tasks.md、docs/tech-debt.md（若有登记）。
⛔ 禁止 -A / . / -a；别人的改动不停不问不顺手提交；push 白名单已在；index.lock 等 5 秒重试 5 次不删。

【六、收工】报告含：7.3 正反例输出、7.5 反证与变异验证输出、两阶段 review 结论、checkbox 实跑值、收口两条计数、pytest 前后、文件清单 + commit hash。
```

---

## 看护者 Opener（贴进 CC Desktop，一次跑完全量）

⚠️ **本节的代码块故意不带 `> 泳道：` 标注** —— 带了 `run-lanes.sh` 会把看护者自己也解析成
一条待执行 opener，变成自己调度自己。改本文件时不要给它补标注。

这份贴进 CC Desktop 后，由那个 CC 充当看护者：前置自检 → 后台起脚本 → 轮询看护 →
**跑完做真身核验**（不是只读汇总表）→ 出报告。你只需要粘一次。

> 🔁 **2026-09-03 起看护者 opener 走引用式**：正文在 `docs/openers/<MMDDY|Z>-泳道批次看护.md`，本节只留指针。
> 上一份（第七批 `[Mac]0903Z`）正文已随 `b86db65` 入库，见 git 历史；第八批＝`docs/openers/0903Y-泳道批次看护.md`；第九批＝`docs/openers/0904Z-泳道批次看护.md`。


---

## 三·续三、第三批（2026-08-26）· 泳道编排

**执行方式**：`bash docs/openers/run-lanes.sh --dry-run` 先看编排，确认后去掉 `--dry-run`。
无人值守加 `--full-auto --yes`。

| 泳道 | 顺序 | 触碰区 | 为什么这么分 |
|---|---|---|---|
| **主线** | A → B | `openspec/`＋`.claude/`＋`CLAUDE.md`（A）；`intake_agent.py`/`nodes.py`/`server.py`/`index.html`（B，在 worktree 里） | B 要开 worktree，worktree 从 commit 检出——A 的决策回填不进 main，B 的 worktree 里就是旧的。**这是唯一一条真依赖** |
| **计划** | C | `docs/superpowers/plans/<E>.md` | 只写一个新文件，与谁都不重叠。读的 `delivery-units.md` 工作区已是新版，不必等 A 提交 |
| **审计** | D → E | `docs/superpowers/plans/<U1>.md`、`<U2>.md` | 同上。U2 的 plan 要引用 U1 定下的表结构，故泳道内串行 |

三条泳道触碰区实测零重叠，可真并行。

---

> ✅ 已完成 2026-08-27 · 原泳道 主线（标注已摘，不再进 run-lanes）
```
[Mac]0826A-提交决策回填与hook
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（提交 main 上的未提交改动，勾了改动就不在工作区）｜ 工作区: 仓库根

【一、任务】
提交工作区里的两批未提交改动，并清掉一个陈旧分支。

【二、并发协议】本仓库此刻可能有多条 opener 在并行跑：
  - 只 git add 本条明确列出的路径，禁止 git add -A / git add . / git commit -a
  - git status 里出现别的文件是正常的，不要停下、不要问、不要顺手提交
  - ⛔ 不要在 commit 前 pull——并发下别的泳道有未提交改动，rebase 会被挡（实测 error: cannot pull with rebase）。
    正确顺序：git add <明确路径> → commit → push；**push 被拒才** git pull --rebase --autostash origin main 再重试，最多 3 次
  - 报 .git/index.lock 已存在，等 5 秒重试最多 5 次，绝不删除该锁文件

【三、分 3 个 commit 提交并推送】

commit 1（Shao Peishen 2026-08-26 的三项决策回填）：
  openspec/changes/m1-job-profile-intake/tasks.md
  openspec/changes/ai-audit-trail-and-outbound-gate/delivery-units.md
  docs/openers/OP-0820-全量编排.md
内容：
  - m1-job-profile-intake 的两条存疑归类均判为 A 类并回勾：4.3（interrupt 闭环，
    Web 通道下由 HTTP + SQLite 状态 + 独立 /confirm 端点达成，判为行为等价）、
    7.4（AI 标识注入，对外标识层已达成）。7.4 括号里"元数据记录模型"半条**移出**到
    ai-audit-trail-and-outbound-gate，不随判定消失
  - CANDIDATE_OUTBOUND_ENABLED 三项拍板：默认关（.env 显式开）、只有 Shao Peishen
    本人可开、**允许热改不重启生效**。U4 无阻塞可开工

commit 2（opener 标题行的机器校验）：
  scripts/hooks/check-opener-header.py
  .claude/settings.json
  .claude/skills/kickoff/SKILL.md
  CLAUDE.md
提交前先做三件事：
  a. 读一遍 hook 源码，确认它异常时 exit 0（fail open）——hook 自己坏掉不该挡住干活
  b. 手工验一次：造一个含【设置】但缺【OP-...】标题行的代码块，确认 exit 2；
     再造一个两行齐全的，确认 exit 0
  c. 核对 .claude/settings.json 的 hooks.Stop 配置路径与文件实际位置一致

commit 3（收口判据补强）：
  .claude/skills/run-build/SKILL.md
内容：`git rev-list --count main..<分支>` 不是 0 时不要直接下结论，再跑
`git cherry -v main <分支>`——全 `-` 是假阳性（main 被 rebase 重写过历史，内容已在
main 上，只是 commit 换了 hash），有 `+` 才是真没合。rev-list 只比 hash，穿不透 rebase。

【四、清陈旧分支】
claude/delivery-unit-c-run-build-22abbe 的 rev-list --count 报 5，但
git cherry -v main 该分支 五行全是 `-`，第 4 章 checkbox 也是 5/5 全勾 —— 单元 C 早已
合并（新 hash 是 846ae16 / f3930ff）。
你自己复跑一次 git cherry 确认全是 `-`，确认后删掉这个本地分支；有任何一行 `+` 就停下
告诉我，不要删。
顺带 git worktree prune 并贴 git worktree list 结果。

【五、收工】列出本次新增/修改文件清单，贴 3 个 commit 的 hash 与 worktree list 输出。
```

---

> ✅ 已完成 2026-08-27 · 原泳道 主线（标注已摘，不再进 run-lanes）
```
[Mac]0826B-单元D开工(第6章)
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: 不指定（勾 worktree 后 CC 自己生成 claude/<任务名>-dev-<hash>）｜ worktree: ✅ 勾选（写代码，需隔离工作区）｜ 工作区: worktree

【一、开工前确认】任一不满足就登记「⏸ 留步」并停下，不要硬上：
1. git log --oneline -10 origin/main 里能看到 OP-0826-A 的三个 commit
   （决策回填 / opener 校验 hook / 收口判据补强）
2. 计划文件在：docs/superpowers/plans/2026-08-25-m1-intake-quality-fixes-unitD-unspecified-fields-and-gap-warning.md
   找不到就停

【二、任务】
用 run-build 技能执行交付单元 D 的实现计划（7 个 Task，对应 tasks.md 第 6 章 6.1–6.10）。
开工前 rebase 到最新 main。

【三、这一条是本单元的分水岭，reviewer 要专门查】
tasks 6.2 是 loggable_summary() 在本仓库的**首个生产上岗点**。那条 debug 日志必须走
app.observability.redaction.loggable_summary()，⛔ 不得 logger.debug("...%s", parsed.xxx)
直接打。**并且要有一条测试断言这条路径确实调用了它**——不能只测"日志里没泄漏"：
在没有调用点的情况下"0 命中"同时兼容"脱敏有效"和"脱敏根本没上岗"两种解释，那不叫验证。
依据 docs/findings/2026-08-13-sqlite-事务归属冲突.md §8.3.1。

【四、收口要求】（上一轮单元 B 就栽在这，逐字执行）
合并回 main 后单独跑：git rev-list --count main..<你的分支>
不是 0 时**先别下结论**，再跑 git cherry -v main <你的分支>：
  - 全部行以 - 开头 → 假阳性（main 被 rebase 过，内容已在 main 上），删掉陈旧分支即可
  - 有任何一行以 + 开头 → 真没合，去补 finishing-a-development-branch
⛔ 在确认内容真的在 main 上之前，不许输出 OPENER_DONE。

【五、收工】
tasks.md 第 6 章 10 项 checkbox 勾完后停下，不要顺手做第 5 章。
列出新增/修改文件清单 + commit hash + 上面那两条计数的实际输出。
```

---

> ✅ 已完成 2026-08-27 · 原泳道 计划（标注已摘，不再进 run-lanes）
```
[Mac]0826C-单元E实现计划(第5章)
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（只产出一份 markdown 计划，不写代码不建分支）｜ 工作区: 仓库根

【一、任务】
用 spec-to-plan 技能，为 m1-intake-quality-fixes 的交付单元 E 出实现计划，并提交。
范围 = tasks.md 第 5 章（5.1–5.8），8 项，预估 4-5 个 plan Task。

⚠️ 只出计划，不开工。E 的**执行**要等单元 D 合并（5.5 用 D 的 derive_unspecified_fields）。
执行顺序 B∥C → D → E → F → G 不变，提前出计划不改这个顺序。

先读：
- openspec/changes/m1-intake-quality-fixes/delivery-units.md（§2.E 是单元 E 详情，§5 跨单元约定）
- openspec/changes/m1-intake-quality-fixes/specs/intake-question-tracking/spec.md（真正的输入）
- docs/superpowers/plans/2026-08-25-m1-intake-quality-fixes-unitD-*.md
  （D 的计划。5.5 要接 D 定的 derive_unspecified_fields 签名，照它写，不要自己另定一个）

【二、这四条必须体现在计划里】
1. **前置已就位，别再当未知**：单元 B 已合并，已问台账的落库载体已经定了。
   开工先读 app/agents/intake_question.py 与 app/graph/state.py 确认实际形态，
   5.1 是在它之上扩「已答 / 重问次数」，**不是另起一套**。
2. **5.5 的「重问超限 → 计入未指定字段」不要写平行逻辑**：D 合并后这条自动成立——
   字段没值，derive_unspecified_fields 自然把它列进去。计划里要写明这一点，
   ⛔ 不要再写一套标记逻辑等 D 落地后再拆。
3. **5.5 的轮次口径要和 3.10 对齐**：单元 B 已落地 MAX_ROUNDS(有产出轮) 与
   MAX_TOTAL_ROUNDS(总行数) 双口径。「超限即不再消耗追问轮次」必须和这两个口径对上，
   否则会出现"这个子问题不再问了，但轮次照扣"。开工先读 B 的实现确认实际常量名。
4. **5.8 要对 _repeats_earlier_assistant_turn 的去留给结论**：保留作为最后一道逐字防线，
   还是由 question_id 追踪取代。单元 A 已在该函数 docstring 里把这个待办明确挂给 5.8，
   是留给本单元的交接。计划里要给出结论与理由，不许含糊带过。
另：5.4 要求重问条目与新问题在界面上可区分，光靠文本前缀不够，会碰 index.html。

Global Constraints 段从 CLAUDE.md 的「工程铁律」与「合规红线」逐字复制相关条目，
另外逐字加上 delivery-units.md §5 的第 5 条。

【三、交付前自查】
grep -c '^### Task ' <计划文件> 必须等于实际任务数、不能是 0。

【四、收工】
只提交你产出的那一个计划文件，一个 commit，推送。列出文件清单 + commit hash + 任务清单。
不要进 run-build。
```

---

> ✅ 已完成 2026-08-27 · 原泳道 审计（标注已摘，不再进 run-lanes）
```
[Mac]0826D-audit U1计划(数据层)
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（只产出一份 markdown 计划，不写代码不建分支）｜ 工作区: 仓库根

【一、任务】
用 spec-to-plan 技能，为变更包 ai-audit-trail-and-outbound-gate 的交付单元 U1 出实现计划，
并提交。范围 = tasks.md 第 1 章（数据层三张表与存储层约束）＋ 搬进来的两个 config 键。

先读：
- openspec/changes/ai-audit-trail-and-outbound-gate/delivery-units.md（§2.U1 与 §4 跨单元约定）
- openspec/changes/ai-audit-trail-and-outbound-gate/specs/（真正的输入）
- openspec/changes/ai-audit-trail-and-outbound-gate/design.md

【二、这三条必须体现在计划里，第 1、2 条要逐字进 Global Constraints】
1. **analysis_run 的业务关联列与 rubric 列必须全部可空**：application_id / job_id /
   rubric_snapshot / system_fingerprint / token_usage 一律允许 NULL。
   理由不是"以防万一"：U3 一旦把 RecorderAuditHook 接到 _gateway_factory() 上，
   **M1 现有的岗位画像采集调用会立刻开始写 analysis_run**，而采集期没有投递、没有 rubric。
   任何一列 NOT NULL 都会在 U3 合并当天把 M1 的采集流程打挂。
   只有 id / configured_model / prompt_version / temperature / input_hash / raw_response /
   created_at 是 NOT NULL。
2. **新表全部走 CREATE TABLE IF NOT EXISTS，⛔ 不碰 _ADDED_COLUMNS 那条加列路径**
   （app/storage/db.py:71-108）。三张表都是新的，.51 上 15 个真实 job 的既有表一行不改，
   无数据迁移。tasks 1.6 的回归测试就是这条的守护。
3. **两个 config 键的形态（Shao Peishen 2026-08-26 已拍板）**：
   审计 JSONL 路径 + CANDIDATE_OUTBOUND_ENABLED，一次加齐（U3/U4 才不必共写 config.py）。
   CANDIDATE_OUTBOUND_ENABLED 默认值取「关闭」，且 **必须每次外发时求值，⛔ 禁止在模块
   导入期 / __init__ / 任何单例上读成常量**——他明确定了"允许热改、不重启生效"，
   理由是出事时要能立刻全拦，而 .51 上还有 7 个别的服务，为改一个开关重启整机不成比例。
   tasks 4.5 已写"支持传 callable"，走那条路。守护测试：运行中改值不重启，下一次求值
   立刻按新值走。

【三、交付前自查】grep -c '^### Task ' <计划文件> 必须等于实际任务数、不能是 0。

【四、收工】只提交你产出的那一个计划文件，一个 commit，推送。
列出文件清单 + commit hash + 任务清单。不要进 run-build。
```

---

> ✅ 已完成 2026-08-27 · 原泳道 审计（标注已摘，不再进 run-lanes）
```
[Mac]0826E-audit U2计划(app/audit)
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（只产出一份 markdown 计划，不写代码不建分支）｜ 工作区: 仓库根

【一、开工前确认】
docs/superpowers/plans/ 下已有 U1 的计划文件（同批次刚出的）。没有就登记「⏸ 留步：U1 计划
未就绪」并停下——U2 要引用 U1 定下的三张表结构，凭空写会对不上。

【二、任务】
用 spec-to-plan 技能，为 ai-audit-trail-and-outbound-gate 的交付单元 U2 出实现计划，并提交。
范围 = tasks.md 第 2 章（app/audit：事件、双 sink、统一入口）。

先读：delivery-units.md 的 §2.U2 与 §3.4、§4；specs/ai-decision-audit/spec.md；design.md。

【三、这三条必须体现在计划里，全部逐字进 Global Constraints】
1. **AuditRecorder 的对外 API 必须是两段式**——写 SQLite 与 append JSONL 是两次可分别
   调用的动作，⛔ 不得打包在一个 record() 里同步完成。tasks 2.8 的"先 SQLite 后 JSONL"
   是**顺序**要求，两段式满足它；打包成一次调用会在事务回滚时产生「JSONL 有、SQLite 无」
   ——design D1 明令这是更糟的偏差方向（审计查不到记录）。这条在 U2 定死，否则 U3/U5 返工。
2. **⛔ 禁止在 effect_* 函数体内 append JSONL**。允许的偏差只有单向：
   「SQLite 有、JSONL 缺行」（真身完整、镜像缺证据）；反向禁止。
   落地形态＝写 SQLite 那段进 effect_* 函数体，append JSONL 那段在 effect_* **返回之后**
   由调用点触发（此时幂等装饰器已 commit）。**这不需要改 idempotent_effect 装饰器**。
3. **2.5 的第四个场景是这条防线的分水岭，不是"多写一个用例"**：
   「删光全部 prev_hash 字段后重写」必须在**第 2 行**就判断链断裂。平台侧已经踩过这个
   绕过，本仓库一次做对。2.5 与 2.6 是本单元含金量最高的两条。

另：SqliteSink.write ⛔ 不自行 commit，与 effect_persist_draft 同一约定（见 nodes.py:61 注释）。
本单元是全新目录、此时尚未接线，现有行为完全不变——与整个仓库零文件重叠。

【四、交付前自查】grep -c '^### Task ' <计划文件> 必须等于实际任务数、不能是 0。

【五、收工】只提交你产出的那一个计划文件，一个 commit，推送。
列出文件清单 + commit hash + 任务清单。不要进 run-build。
```

---

## 三·续二、第二批：清失真 → 三份 plan 并行（2026-08-20 定）

**Shao Peishen 已定**：先清文档失真，清完 D / F / ai-audit-trail 三份 plan 并行全开；
`m1-job-profile-intake` 走「重写 tasks 对齐现实」。

| 步 | Opener | 触碰区 | 冲突 |
|---|---|---|---|
| 先 | **OP-0820-10** 清两处文档失真 | `m1-job-profile-intake/tasks.md`、`07-…待办.md` | ⚠️ **与 OP-0820-9R 都写 `07`，两条必须串行**，谁先都行 |
| 后（三条并行） | **OP-0820-11** 单元 D 的 plan | `docs/superpowers/plans/<D>.md` | 无 |
| | **OP-0820-12** 单元 F 的 plan | `docs/superpowers/plans/<F>.md` | 无 |
| | **OP-0820-13** ai-audit-trail 拆分方案 | `ai-audit-trail-…/delivery-units.md` | 无 |

三条与正在跑的 OP-0820-5S（单元 B）也零重叠——5S 在 worktree 里改后端代码，这三条在 main 写文档。

---

```
[Mac] 0820-10 清文档口径失真
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（只改文档，不写代码）｜ 工作区: 仓库根

【一、任务】
两份文档的口径已经和现实脱节，每次对账都会被它们带偏一次。按下面两节分别处理。

【二、并发协议】本仓库此刻可能有多条 opener 在并行跑：
  - 只 git add 本条明确列出的路径，禁止 git add -A / git add . / git commit -a
  - git status 里出现别的文件是正常的，不要停下、不要问、不要顺手提交
  - ⛔ 不要在 commit 前 pull——并发下别的泳道有未提交改动，rebase 会被挡（实测 error: cannot pull with rebase）。
    正确顺序：git add <明确路径> → commit → push；**push 被拒才** git pull --rebase --autostash origin main 再重试，最多 3 次
  - 报 .git/index.lock 已存在，等 5 秒重试最多 5 次，绝不删除该锁文件
⚠️ OP-0820-9R（.51 整机重启）也写 07-开发环境现状与优化待办.md。若它正在跑，
   等它 push 完再动 07 那一节；先做第【三】节即可。

【三、重写 m1-job-profile-intake 的 tasks，对齐现实】（Shao Peishen 2026-08-20 定）
现状：69 项里 58 项未勾，而未勾的内容——Postgres 建库、pgvector、企微自建应用与回调、
LangGraph 图骨架、JD 生成 Agent——是写于选型前的 M1 全量 WBS，与实际走的实现路径
（SQLite + Web 通道）已经脱节。按现状这个包永远归不了档。

请把 69 项逐条过一遍，分成四类处理，**每一条都要给出归类理由，不许批量拍**：
  A【已用其他方式完成】→ 勾上，并在条目后注明实际落在哪（如"1.2 建表 → 已用 SQLite
    实现，见 app/storage/"）。判据是**行为等价**，不是名字相同
  B【被选型变更作废】→ 原位划掉留墓碑（~~原文~~ ⚰️ 已作废，原因…），⛔ 不许直接删。
    如 Postgres/pgvector 相关项被 SQLite 方案取代
  C【确实还要做，但属别的阶段】→ 划出去，在 tasks.md 末尾新开一节「已移出，另开变更包」
    逐条列明移到哪（如企微通道 3.1-3.6 → 阶段二；审计留痕 → 已在
    ai-audit-trail-and-outbound-gate 覆盖）。**不要真去新建变更包**，本条只做登记
  D【仍属本包、仍未做】→ 保持未勾

处理完在文件顶部加一段「2026-08-20 对齐现实」说明：改了什么、四类各多少条、
归档还差哪些（即 D 类剩余项）。

⚠️ 不要动 specs/ 与 design.md ——那两份是行为契约，本条只理 WBS。

⚠️ **归类拿不准时的固定处置（预案，不要停下来问）**：本条可能在无人值守下运行，
问了没人答。凡"行为等价"判断存疑的，一律按以下规则处理，**不得中止、不得提问**：
  - 存疑一律归 **D 类（仍属本包、仍未做，保持未勾）**——保守方向是"当它还没做"，
    错判成 D 的代价是多留一条待办，错判成 A 的代价是把没做的事标成做完了
  - 同时在 tasks.md 末尾新开一节「⏳ 待 Shao Peishen 判定的归类」，逐条列出：
    条目号、原文、你倾向哪一类、存疑的具体点是什么
  - 列完继续往下跑，把剩余条目处理完

【四、修 07-开发环境现状与优化待办.md 的 P0 与 P2】
P0 段现在写着"81 个测试""`.github/workflows/` 不存在"，都过时了：
  - 实际测试数：跑一次 pytest 取准数（我这边看是 176，以你跑出来的为准）
  - CI 已存在：.github/workflows/ 下有 1 个 windows-latest + 2 个 ubuntu-latest。
    另有一条本仓库特有的环境事实要补进去：private 仓库 Free 计划 2000 分钟/月，
    **Windows runner 按 2x 扣**，2026-08-19 撞过一次额度。折中方案（尚未实施）是
    「feature 分支只跑 Linux，PR 与 main 推送才跑 Windows」
  - P0#1 无 pre-commit 钩子、P0#3 Python 版本偏斜：逐条核实还成不成立，成立就留、
    改掉了就销号，**不要凭印象**
P2#9 写着"m1-job-profile-intake 的 tasks 还差 0.11，archive 要等它勾完"——这句是错的，
按第【三】节的实际结论改写。

【五、收工】只提交这两个路径，一个 commit，推送：
  openspec/changes/m1-job-profile-intake/tasks.md
  07-开发环境现状与优化待办.md
列出文件清单、贴 commit hash，并把四类各自的条数报给我。
```

---

```
[Mac] 0820-11 单元D实现计划
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（只产出一份 markdown 计划，不写代码不建分支）｜ 工作区: 仓库根

【一、任务】
用 spec-to-plan 技能，为变更包 m1-intake-quality-fixes 的交付单元 D 出实现计划，并提交。

⚠️ 只出计划，不开工。单元 D 的**执行**要等单元 B 合并之后——D 与 B 在
app/agents/intake_agent.py / app/graph/nodes.py / app/web/server.py / index.html
四处文件重叠，必须串行。提前出计划不改这个执行顺序。

先读：
- openspec/changes/m1-intake-quality-fixes/delivery-units.md（§2.D 是单元 D 详情，
  §3.3 是 loggable_summary 的硬约束，§5 是跨单元接口约定）
- openspec/changes/m1-intake-quality-fixes/specs/intake-completeness-warning/spec.md（真正的输入）
- openspec/changes/m1-intake-quality-fixes/design.md（决策 6、7、8）

单元 D 范围 = tasks.md 第 6 章（6.1–6.10），共 10 项，预估 5-6 个 plan Task。

【二、并发协议】本仓库此刻可能有多条 opener 在并行跑：
  - 只 git add 你自己产出的那一个计划文件，禁止 git add -A / git add . / git commit -a
  - git status 里出现别的文件是正常的，不要停下、不要问、不要顺手提交
  - ⛔ 不要在 commit 前 pull——并发下别的泳道有未提交改动，rebase 会被挡（实测 error: cannot pull with rebase）。
    正确顺序：git add <明确路径> → commit → push；**push 被拒才** git pull --rebase --autostash origin main 再重试，最多 3 次
  - 报 .git/index.lock 已存在，等 5 秒重试最多 5 次，绝不删除该锁文件

【三、这三条必须体现在计划里】
1. **tasks 6.2 是 loggable_summary() 在本仓库的首个生产上岗点**（delivery-units.md §3.3）。
   那条 debug 日志（模型给的 unspecified_fields 与系统推导结果对照）是这个变更包里第一次
   把业务对象内容送进 logging，**必须走 app.observability.redaction.loggable_summary()**，
   不得 logger.debug("...%s", parsed.unspecified_fields) 直接打。
   ⚠️ 验收要求：要有一条测试断言这条日志路径**确实调用了** loggable_summary()。
   不能只测"日志里没泄漏"——在没有调用点的情况下"0 命中"同时兼容"脱敏有效"和"脱敏
   根本没上岗"两种解释，那不叫验证。依据见
   docs/findings/2026-08-13-sqlite-事务归属冲突.md §8.3.1 的更正段。

2. **6.3 的测试要用真实回放数据反证**，不要造假数据：
   用 a478499c 收尾时的已累积画像断言推导结果**不为空**（模型当时给的是空数组，漏报）；
   用 19b6ec6d 的断言 functional_safety / sop_projects **不在**结果里（模型当时虚报了
   用户已答的字段）。

3. **6.9 的知情确认留痕写 profile_json 的下划线前缀内部键**，与 _jd_text 同位置，
   **不新建表**（design.md 决策 8）。6.4 的 field→中文名映射放
   app/schemas/job_profile.py 紧邻字段定义，**不放前端**（决策 7），并补一条完整性测试：
   JobProfile 每个字段都必须有中文名，加字段漏改即失败。

Global Constraints 段从 CLAUDE.md 的「工程铁律」与「合规红线」逐字复制相关条目，
另外逐字加上 delivery-units.md §5 的第 5 条。

【四、交付前自查】
grep -c '^### Task ' <计划文件> 必须等于实际任务数、不能是 0。

【五、收工】
只提交你产出的那一个计划文件，一个 commit，推送。列出文件清单、贴 commit hash 与
任务清单。不要进 run-build。
```

---

```
[Mac] 0820-12 单元F实现计划
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（只产出一份 markdown 计划，不写代码不建分支）｜ 工作区: 仓库根

【一、任务】
用 spec-to-plan 技能，为变更包 m1-intake-quality-fixes 的交付单元 F 出实现计划，并提交。

⚠️ 只出计划，不开工。Shao Peishen 已定 F **排在最后**（执行顺序 B∥C → D → E → F → G），
提前出计划不改这个顺序。F 的执行前提是 B、D、E 都已合并。

先读：
- openspec/changes/m1-intake-quality-fixes/delivery-units.md（§2.F 是单元 F 详情，
  §5 是跨单元接口约定）
- openspec/changes/m1-intake-quality-fixes/specs/intake-field-grounding/spec.md（真正的输入）
- openspec/changes/m1-intake-quality-fixes/design.md（决策 11、决策 12）

单元 F 范围 = tasks.md 第 7 章（7.1–7.11），共 11 项，预估 5-6 个 plan Task。

【二、并发协议】本仓库此刻可能有多条 opener 在并行跑：
  - 只 git add 你自己产出的那一个计划文件，禁止 git add -A / git add . / git commit -a
  - git status 里出现别的文件是正常的，不要停下、不要问、不要顺手提交
  - ⛔ 不要在 commit 前 pull——并发下别的泳道有未提交改动，rebase 会被挡（实测 error: cannot pull with rebase）。
    正确顺序：git add <明确路径> → commit → push；**push 被拒才** git pull --rebase --autostash origin main 再重试，最多 3 次
  - 报 .git/index.lock 已存在，等 5 秒重试最多 5 次，绝不删除该锁文件

【三、这三条必须体现在计划里，第 1 条要逐字进 Global Constraints】
1. **7.1 把 profile_patch 的字段从裸值升级为 {value, source_quote, source_turn}，
   这个结构升级不得穿透到落库层。** 合进 profile_patch_accumulated 与
   job_profile.profile_json 的**必须仍然是裸值**，结构升级只允许存在于"模型返回 → 校验"
   这一段，落库前拍平。不拍平会同时炸三处：
     a. 单元 D 的 derive_unspecified_fields 会把 {"value": null, ...} 当成"这个字段有值"，
        漏报回到今天的故障
     b. POST /confirm 里的 JobProfile.model_validate 直接 422（headcount 收到一个 dict）
     c. effect_generate_and_persist_jd / jd_agent 全部按裸值读 profile_dict

2. **7.2 再改 SYSTEM_PROMPT → prompt_version 升到 intake-v5**（铁律 5）。
   注意 v4 是单元 B 占用的，F 是 v5，不要重号。

3. **本章只观测不拦截**（design.md 决策 12），对三位业务经理**零可见**。计划里不要加
   任何拦截/降级逻辑。未溯源率达到多少才值得拦截，等 ≥20 场真实会话的分布出来再单独开
   变更。另：7.4 的"点选来源例外"先核一下还需不需要单独实现——单元 C 已按
   「点选文本原样拼进回复」落地，被选中的档位文本逐字出现在用户原话里，7.3 的子串判定
   大概率天然命中；若确认不必单独实现，在计划里写明并说明依据。

Global Constraints 段从 CLAUDE.md 的「工程铁律」与「合规红线」逐字复制相关条目，
另外逐字加上上面第 1 条全文，以及 delivery-units.md §5 的第 1、3、5 条。

【四、交付前自查】
grep -c '^### Task ' <计划文件> 必须等于实际任务数、不能是 0。

【五、收工】
只提交你产出的那一个计划文件，一个 commit，推送。列出文件清单、贴 commit hash 与
任务清单。不要进 run-build。
```

---

```
[Mac] 0820-13 audit交付单元拆分
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（只出分析与方案，不写代码）｜ 工作区: 仓库根

【一、任务】
读 openspec/changes/ai-audit-trail-and-outbound-gate/ 的 proposal.md / design.md /
specs/ / tasks.md，把 7 章 53 项拆成交付单元，产出 delivery-units.md。

⛔ 只出拆分方案，**不跑 spec-to-plan、不开工**，出完停下等 Shao Peishen 确认。
（这一步的形态照抄 m1-intake-quality-fixes 的 delivery-units.md，那份是本项目的既有范式）

粒度约定（CLAUDE.md）：一个交付单元 = 一份 superpowers plan = 一条 worktree 分支
= 一个可独立测试并合并的交付物。

【二、这个变更包为什么要紧，写进方案的开头】
两条合规底线目前在本仓库里都只有壳：
- AI 评分留痕：gateway.py 的 AuditHook 只有 NoopAuditHook，工程铁律 3、4 **一条都不成立**。
  模型标识、prompt 版本、输入哈希、rubric 快照、原始响应都没落盘，criterion_score 与
  evidence_ref 连表都不存在
- 外发人工确认门禁：effect_deliver_message 是无条件投递，合规红线「AI 只做排序推荐，
  不做自动淘汰」目前靠调用方自觉
**M2 开始处理真实简历前这两条必须就位**——PIPL 的说明权要求能回答"这条评分是哪个模型、
哪个版本、按哪份 rubric 打的，依据是简历里哪一段"。

【三、拆分必须回答的五件事】
1. **两条能力（ai-decision-audit / outbound-approval-gate）能不能各自独立交付？**
   看它们是否共用 AuditRecorder——门禁的每次拦截也要留痕，若共用则留痕必须先做。
   给出明确的依赖方向，不要含糊说"有些关联"。
2. **与 m1-intake-quality-fixes 剩余单元（B/D/E/F/G）的触碰区重叠情况。**
   本包会碰 app/llm/gateway.py（接 AuditHook）与 app/graph/nodes.py
   （effect_deliver_message 前插门禁）。nodes.py 是那批最热的文件之一。
   **逐条列出哪些单元能并行、哪些必须串行，判据＝触碰文件是否重叠**，不要只说"注意冲突"。
3. **fail-closed 语义怎么测。** proposal 写明门禁是 fail-closed（消息类型未知、风险等级
   未知、缺 requires_confirmation 字段时一律拦截）。这类"缺字段就拦"的语义最容易被后来的
   重构悄悄改成 fail-open，指出哪个单元要为它建一条守护测试。
4. **hash-chain 的 verify_chain() 归哪个单元**，以及它和 SQLite 表的一致性怎么保证——
   注意工程铁律 1：幂等记录与业务写必须同一事务，同一连接、同一个 BEGIN。
   JSONL 镜像不在事务里，这个不一致窗口要在方案里给出结论（是接受还是消除）。
5. **CANDIDATE_OUTBOUND_ENABLED 总开关的默认值。** 这属合规红线范围，
   按 CLAUDE.md 决策代理表是**不可代**项——不要替 Shao Peishen 定，列出选项与后果让他拍。

【四、并发协议】本仓库此刻可能有多条 opener 在并行跑：
  - 只 git add openspec/changes/ai-audit-trail-and-outbound-gate/delivery-units.md，
    禁止 git add -A / git add . / git commit -a
  - git status 里出现别的文件是正常的，不要停下、不要问、不要顺手提交
  - ⛔ 不要在 commit 前 pull——并发下别的泳道有未提交改动，rebase 会被挡（实测 error: cannot pull with rebase）。
    正确顺序：git add <明确路径> → commit → push；**push 被拒才** git pull --rebase --autostash origin main 再重试，最多 3 次
  - 报 .git/index.lock 已存在，等 5 秒重试最多 5 次，绝不删除该锁文件

【五、收工】
只提交那一个 delivery-units.md，一个 commit，推送。
列出文件清单、贴 commit hash、单元划分表，以及第【三】节第 5 条要 Shao Peishen 拍的那项。
```

---

## 四、OP-0820-9 的人工放行，为什么保留

这不是执行节奏控制，是**授权门**。两者的区别：节奏控制是"为了让并行不出错而要求人掐时间"，授权门是"这件事的后果需要人承担"。前者已经全部消掉，后者不该消。

OP-0820-9 重启 `.51`，会中断另外 5 个服务。CLAUDE.md 决策代理表把「生产服务器 `.51` 的发版决定」列为**不可代**项，且代理人至今未指定——留空不等于谁都可以。

所以它的开工条件是「你选定窗口并知会相关方」，不是「等某条 opener 跑完」。

---

## 五、不是 opener、但悬着的两件事

| 事项 | 性质 | 现状 |
|---|---|---|
| **决策代理人人选** | 纯决策，只有 Shao Peishen 能定 | CLAUDE.md 里框架已建、可代/不可代两张清单已列，**人选空着**。空着期间"可代"项同样一律挂起等本人。这也是 OP-0820-9 至今只能等本人放行的直接原因 |
| **脱敏层从未被真实数据检验** | 已知缺口，有明确解决时点 | `loggable_summary()` 至今无生产调用点，发版验证的"0 命中"同时兼容"脱敏有效"和"脱敏根本没上岗"两种解释。**首个上岗点是单元 D 的 tasks 6.2**，那时要有一条测试断言"确实调用了"，不能只测"日志里没泄漏" |
