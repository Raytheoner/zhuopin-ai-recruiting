[Mac]0909AR-TD29收尾与口径订正
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: 不指定（建 worktree 后 git 自己生成）｜ worktree: ✅ 勾选（改测试与配置文案）｜ 工作区: worktree ｜ 派发: Cowork·HR业务线-接力0909Q

> 本文件是 `[Mac]0909AR` 的正文（引用式 opener）。三件事一次收干净：TD-29 剩的 2 条、
> `0909AP` 留步的「名单外归档」口径订正、`OP-0820` 的未提交尾巴。

【一、TD-29 剩 2 条】依据＝`docs/tech-debt.md` TD-29 原文（🟡 部分已还，`5ca1f50` 还了三条半）。
⚠️ **先读原文确认剩的到底是哪两条**（登记里写的是 `conftest` 收编、`config.py` 文案），⛔ 不要照本 opener 的转述做。
做完把 TD-29 从 🟡 改成 `~~TD-29~~ ✅ 已还（<commit>）`。若发现剩的不止 2 条 ⇒ 如实登记、⛔ 不硬销。

【二、名单外归档的口径订正】🔴 **Shao Peishen 2026-09-10 裁决：名单外也归档，学 AI 赋能项目的做法。**
依据（本会话读 win 端源码所得，逐字）：`工具-跟进闸查询.py` docstring「回件到达时机器人自动追 §一 行、
拆件回灌由人写队列行」；`aibot_service/intake.py` 的 `archive_inbound_message` **无任何白名单概念**，回件到达即归档。
与本项目 spec 与 `tools/liaison/inbound.py` docstring 逐字的「SHALL 仍然归档」一致。
1. 把这条裁决落进 `docs/findings/2026-09-10-名单外归档口径裁决.md`：写清裁决、win 端依据、以及**被作废的错误口径**
   （`0909AP` opener 里「名单外 ⇒ ⛔ 不归档」那句是起草者写错，⛔ 今后不得再据它改代码或改测试）
2. 核 `tools/liaison/inbound.py` 与既有用例**当前行为就是归档** ⇒ ✅ 代码无需改动，只补一条**把口径钉死的断言**
   （名单外 ＋ 带附件 ⇒ 归档发生、⛔ 不入队、走礼貌回复三者同时成立），用例名与注释里点名本裁决
3. ⚠️ 若核出当前行为**不是**归档 ⇒ 🔴 停下报告，⛔ 不要自行改产品代码——那说明 spec 与实现不一致，属需要他再拍的口径冲突

【三、提交 `OP-0820` 的尾巴】主工作区 `docs/openers/OP-0820-全量编排.md` 有未提交改动，含两部分：
① run-lanes.sh 自己摘的第十六批完成标注（脚本正常行为）；② 号池台账新增的**规则第 4 条**
（「写进本表还不够，必须 commit——未提交的登记行 grep 得到、别的 session `git show` 不到」，2026-09-09 `0909AP` 撞号的根治）。
两部分**一并提交是对的**，⛔ 不要试图拆开。
🔴 ⛔ **绝不 add** `_to_delete/`（Shao Peishen 自己删）、`.env`、`tools/liaison/.env`、`data/`。commit 前核 `git status --short` 的暂存区。

【四、守住】⛔ 只动：TD-29 那两条各自的落点（以原文为准）、`tools/liaison/tests/`（本条只加断言）、
`docs/findings/`（新增一份）、`docs/tech-debt.md` 的 TD-29 一段、`docs/openers/OP-0820-全量编排.md`（只提交，不改内容）。
⛔ 不碰 `session*`、`notify/`、`queue.py`、`logsetup*`、`followup.py`、`retention*`、`storage/*`、`docs/跟进信/`、`tasks.md`、`app/`、`scripts/`。
⛔ 不发任何真实消息、⛔ 不跑 `send-followup`、⛔ 不碰真实服务（它正在 launchd 下常驻运行）。

【五、收口】合回 main（先 rebase）→ `git rev-list --count main..<分支>` 与 `git cherry -v main <分支>` 都判真合 0；
pytest ≥ 基线且 0 失败；单测离线。⛔ 不归档。

【六、收工】报告含：TD-29 剩的两条**原文**与各自落点、口径订正 findings 的路径、那条钉死口径的断言红→绿输出
（或「当前行为已符合、断言一次即绿」的说明）、`OP-0820` 的 commit hash、暂存区守卫复核结论、pytest 前后。
