# [Mac]0920B-诊断岗位列表读取失败

【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: main ｜ worktree: ❌ 不勾（纯诊断，SSH到.51只读排查，不改代码不改现网）｜ 工作区: 仓库根 ｜ 派发: Cowork·HR业务线-接力0918AO ｜ 模型: Sonnet

⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动，print 模式下那个 MCP 工具未必挂载，名字另由 -n 给。🔴 若改为手工贴进 CC Desktop，必须自行补上第 3 行。

## 零、紧急情况

Shao Peishen 刚在浏览器实测：`http://192.168.100.51:8095/hr/recruit-agent/` 首页正常，但点「岗位列表」标签页显示"岗位列表读取失败，请稍后重试"，他确认"昨天晚上都好的，应该是新发布出问题了"——即 `0920A`（本轮 M2·U3 发版，commit `8596829`）疑似引入回归，尽管 `0920A` 自己的冒烟测试里 `curl :8095/api/jobs` 在 Windows 本机测得 200+JSON。

已排除的方向（Cowork 侧只读核对过）：`app/web/static/index.html` 里 `loadJobList()` 调用 `getJson("api/jobs")` 是相对路径，没问题；`app/middleware/auth.py::PROTECTED_PATH_PREFIXES` 不含 `/api/jobs`，且该文件在 `67772ba..8596829` 之间**完全没有改动**（`git diff` 空）；`app/web/static/index.html` 本身在这两个 commit 之间也**没有改动**。真正改动的是 `app/storage/db.py`（+99 行，大概率是 M3 语音面试相关的新表 `job_prep_config`/`interview_session`/`appeal_event` 的 `SCHEMA`/迁移）与 `app/web/server.py`（+252 行）。怀疑方向：现网 `data/demo.db` 跑新迁移时可能没完全成功，或 `GET /api/jobs` 路径上间接触发了某个新迁移/新表相关的异常。

## 一、只读诊断（本条不做任何写操作、不改代码、不重启服务、不回滚）

1. `ssh zp51`：`Get-Content app.log -Tail 300`，用 `Select-String -Pattern 'Traceback|ERROR|Exception'` 找 `0920A` 发版时间点（`08:44` 左右 CST）之后、以及刚才浏览器点击「岗位列表」那次请求（大约现在）前后的报错，全文摘出来，不要只看 EXIT code
2. 在 `.51` 上直接 `curl -s -w '\nHTTP:%{http_code}\n' http://localhost:8095/hr/recruit-agent/api/jobs`（注意带 `root_path` 前缀，这是浏览器实际请求的路径，冒烟测试之前测的是不带 `root_path` 前缀的 `:8095/api/jobs`——**这本身就是一处可疑差异，优先验证**）
3. 若第 2 步复现 404/500，用返回体和 `app.log` 对应时间点的报错定位具体异常（哪个查询、哪张表/哪个字段）
4. 检查现网 `data/demo.db` 的 schema 实际状态：新表 `job_prep_config`/`interview_session` 是否真的建成、`jobs` 表本身的列是否完整（`sqlite3 data\demo.db ".schema jobs"` 之类，用现网实际的 python/sqlite3 可执行方式）
5. 对照 main 分支上 `GET /api/jobs`（`app/web/server.py` 里那个 handler，约行 912）的查询逻辑，看它是否读取了本次新增的任何表/字段

## 二、红线

- ⛔ 纯诊断，不改现网任何文件、不重启服务、不回滚、不 sync
- ⛔ 不碰 `.51` 上的数据文件
- 无人值守：诊断完就停，把找到的根因原样报告，**不要自己决定要不要回滚或热修——这是生产发版决定，必须回来问 Shao Peishen**

## 三、收口

顶格输出 `OPENER_DONE`，正文里给：① 第 2 步 curl 的实际返回（状态码+响应体前 500 字符）；② app.log 里对应的报错全文（如有）；③ 一句话根因判断（如果证据够）；④ 明确说这是诊断，等待下一步授权，不要自己下结论说"已经修好了"或做任何改动。
