# [Mac]0920A-M2U3硬门槛引擎发版51Q51放行

【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: main ｜ worktree: ❌ 不勾（发版操作在主检出，不写业务代码）｜ 工作区: 仓库根 ｜ 派发: Cowork·HR业务线-接力0918AO ｜ 模型: Sonnet

⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动，print 模式下那个 MCP 工具未必挂载，名字另由 -n 给。🔴 若改为手工贴进 CC Desktop，必须自行补上第 3 行。

## 零、授权

`定夺队列.md` `Q-51`（取代失真的 `Q-49`）：Shao Peishen 2026-09-20 答「发」。前情：`0919O` 首次尝试发版时自核拦停（pytest 4 failed，与 `Q-49` 引用的旧名单不符）；`0919T` 已修复全部四条（`merge --no-ff` 回 main，commit `a6df909`），全量 pytest 重新核实 **3716 passed／0 failed／10 skipped**；`Q-51` 据此重新走闸，本次已放行。

## 一、🔴 开工第一步自己核，不许信任何转述

1. `git log --oneline -5 main`，确认 `a6df909`（`0919T` 合并提交）在祖先链上，`tasks.md`（`openspec/changes/m2-resume-parse-and-rank/tasks.md`）4.1–4.6 仍全部 `[x]`
2. **重新跑一次全量 `pytest -q`**（`venv` 内），不得直接引用 `Q-51`/`0919T` 的历史数字——若与 3716 passed／0 failed／10 skipped 不一致（哪怕只多 1 条 failed），立即停，不得发版，原样报告新的失败清单，`OPENER_PARTIAL`，不擅自扩大解决范围
3. 重新读一遍 `app/storage/rejection.py::write_rejection`（`reason_type` 只认 `hard_rule`/`human_decision`）与 `app/audit/assertions.py::COMPLIANCE_ASSERTIONS`（4 条断言都在）
4. 确认 `live_resume_intake_enabled` 默认仍是 `False` 且无覆盖（M2 门槛：真实简历入库闸保持关闭）
5. 任一条不通过 ⇒ 立即停，不发版，报告实测

## 二、锁定发版 commit 并申报携带范围

1. 当前 main HEAD 即发版 commit，记录 hash
2. 申报本次相对 `.51` 现网旧 commit（上次发版 `0918AM`）的携带范围：至少包含 M2·U3（`182383d`）＋本次 M3 语音面试遗留缺口修复（`0919T`/`a6df909`）＋期间的调度器脚本修复（`0919P`/`0919S` 等）＋若干 docs-only 提交。逐项确认均不改变 `.51` 现网既有业务逻辑（除 U3 本身要接线的申诉/标记流程外）
3. 确认无新增 pip 依赖（`git log 182383d..<HEAD> -- requirements.txt` 应为空或仅无关变更）
4. 确认 `sync-to-server.sh` 的目录级白名单（`app`/`scripts`/`requirements.txt`/...）覆盖所有新文件，无需改白名单本身

## 三、执行（按 `docs/releases/2026-09-发版清单-51.md`「四、发版步骤」标准五阶段）

快照 → [装依赖，若有] → sync → G-e → 冒烟（标准四条）＋两条额外：⑤ 单独跑 `python -m app.audit.assertions`，隔离报告 `assert_no_ai_score_rejections` 的具体结果（不止看整体 EXIT=0）；⑥ 确认 `appeal_event` 表已建且为空。不过即回滚停链。新建执行记录 `docs/releases/2026-09-20-发版四-M2U3硬门槛引擎执行记录.md`。

## 四、红线

- 入库闸（`live_resume_intake_enabled`）全程保持关闭，执行前后各核验一次
- 不动 `.51` 现网既有系统组件、不碰真实简历数据
- 不改 `scripts/gates.py`
- 不对外发送（企微/邮件/跟进信一律不碰）
- 无人值守：任何一步与预案不符（自核不过、冒烟不过、push/sync 失败重试 3 次仍失败）一律停+原样报告，不追问、不代拍、不扩大范围
- 结束回合前不得留后台子代理

## 五、收口

1. 执行记录文件落地（含四步实测结果）
2. `docs/session接力.md`「二、下一步」追加一行：M2·U3 已发版到 `.51`（或未过冒烟已回滚），附 commit hash 与执行记录链接
3. `定夺队列.md` `Q-51` 答复列追加执行结果引用
4. 用本条自身的 CC 原生环境直接 `git add`/`git commit`/`git push`（不需要 commit-request 通道）
5. 顶格输出 `OPENER_DONE` 或 `OPENER_PARTIAL: <原因>`
