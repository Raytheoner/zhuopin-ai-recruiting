[Mac]0908Y-泳道批次看护
【设置】执行环境: CC Desktop ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（你是调度者，实际干活的是脚本起的独立 session）｜ 工作区: 仓库根 ｜ 派发: Cowork·HR业务线-接力0903B

> 本文件是 `[Mac]0908Y` 的正文（引用式 opener）。第十二批看护者，`Z` 已被第十一批用掉、顺延 `Y`。文件随任务一起提交，即留痕。

【一、你的角色】
你不亲自执行任何 opener。做四件事：前置自检、**把启动命令交给 Shao Peishen 点 Run**、轮询盯守、跑完真身核验并出报告。
⛔ 不改代码、不跑 spec-to-plan / run-build / openspec-archive-change。本批不发版。

【二、先提交 Cowork 侧的编排落档】
git status --short 预期：docs/session接力.md、docs/openers/OP-0820-全量编排.md、docs/openers/0908Y-泳道批次看护.md。
只 add 这三条路径，⛔ 禁止 git add -A / git add . / commit -a：
  git add docs/session接力.md docs/openers/OP-0820-全量编排.md docs/openers/0908Y-泳道批次看护.md
  git commit -m "docs(openers): 第十二批编排（值守第 1–3 章串行 ∥ run-lanes 自检 ∥ 幂等装饰器撞键修复）"
  git push origin main
有别的改动 → 单列出来，不提交、不问、继续。

【三、前置自检】任一不过就停下报告，不要硬上：
1. command -v claude；ls docs/openers/run-lanes.sh
2. bash docs/openers/run-lanes.sh --dry-run
   本批（第十二批）预期：**3 条泳道、9 条 opener**：
     ◆ 值守
         ✓ 0908L 正文 20 行   值守第1章骨架与SDK可行性计划
         ✓ 0908M 正文 23 行   值守第1章骨架与SDK可行性开工
         ✓ 0908N 正文 18 行   值守第2章存储基座计划
         ✓ 0908O 正文 23 行   值守第2章存储基座开工
         ✓ 0908P 正文 19 行   值守第3章准入名单计划
         ✓ 0908Q 正文 22 行   值守第3章准入名单开工
     ◆ 机制
         ✓ 0908R 正文 23 行   run-lanes开跑前条目总数自检
     ◆ 修复
         ✓ 0908S 正文 23 行   幂等装饰器撞唯一键改短路计划
         ✓ 0908T 正文 22 行   幂等装饰器撞唯一键改短路开工
   值守六条严格串行（第 2/3 章要第 1 章的骨架），三泳道并行。数字对不上 / 任一 ✗ / exit 13 → 停下报告，⛔ 不改编排文件、不 --only 绕
3. git status --short 记基线（【二】后应干净）；git status -sb 记 ahead（应 0）
4. 🔴 pytest 基线：./venv/bin/python -m pytest -q 2>&1 | tail -3（python 不在 PATH；⛔ 不抄——第十一批收口 1227 passed 那是别人的观测）。有失败项 → 停下报告，不发车（0908B 线已把边界守卫换成登记制，此刻应全绿）
5. ls docs/superpowers/plans/ | grep -c "liaison-unit\|unique-race" 应为 0；ls tools/liaison 应不存在
6. grep -c '^- \[x\]' openspec/changes/hr-wecom-aibot-liaison/tasks.md 记下（09-08 下午应为 0）
7. 看一眼 /usage：三泳道并行、值守连跑六条，余量不够就停下报告

【四、启动——交给 Shao Peishen 点 Run，⛔ 你不要自己调 Bash 起脚本】
（Auto Mode 分类器在白名单齐全时仍拦 `nohup run-lanes.sh`，09-04 两批实证。）先 `rm -f /tmp/run-lanes-boot.log`，然后把下面这段**原样**作为 bash 代码块发给他，请他点 Run：

```bash
cd /Users/paulshao/Projects/HumanResource && nohup bash docs/openers/run-lanes.sh --full-auto --yes > /tmp/run-lanes-boot.log 2>&1 & echo "PID=$!"
```

等他回 PID。从 /tmp/run-lanes-boot.log 头几行读日志目录（只认半角冒号那行）。本批不加 --chain，值守泳道估 4–5 小时，是最长的一条。

【五、轮询盯守】每 5 分钟：
  LOGDIR=<日志目录>; cat "$LOGDIR/results.tsv" 2>/dev/null; ls -la "$LOGDIR"/*.log; ps -p <PID> >/dev/null && echo RUNNING || echo EXITED
每轮播报一行。预案（不要问我）：
- PARTIAL → 正常，找「⏸ 留步」记原因继续盯。已知合法留步：M 的「SDK 在 3.14 装不上，走 D8 退路」（这是设计内分支，不是失败）
- FAIL / NO-SENTINEL → 该泳道停、别的不受影响。读末尾 50 行；`API Error: 529` → 查 status.claude.com，解除后把
  `cd /Users/paulshao/Projects/HumanResource && nohup bash docs/openers/run-lanes.sh --full-auto --yes --only <剩余编号逗号分隔> > /tmp/run-lanes-boot.log 2>&1 & echo "PID=$!"`
  作为 bash 块发给 Shao Peishen 点 Run，新日志目录另记
- `Budget limit reached ($25)` 中断在终审/合并前 → ⛔ 不重开；报告里写 worktree 名与未合 commit 数，推荐「利旧 worktree 续跑收口」
- NO-BODY → 停止盯守，`cat "$LOGDIR/manifest.tsv" | od -c | head` 贴给我
- 「调不到 superpowers」→ 已知，按预案手工走；记下各条是停了还是走完了
- 值守泳道 M 若报 `pip install` 联网失败 → 记下，让它按 D8 退路继续；⛔ 不代它装
- 超过 5.5 小时仍未收敛 → 不 kill，记录现状出报告

【六、跑完的真身核验 —— 逐条跑，不许跳】
  cd /Users/paulshao/Projects/HumanResource && git pull --rebase origin main
1. git log --oneline -50 对照 results.tsv（L/N/P/S：各 1 个 plan；M/O/Q/T：各 合并 + 回勾；R：1 个脚本 commit）
2. git status --short 与基线比；git status -sb ahead 应 0（> 0 补推）
3. git worktree list；逐分支真未合：
   for b in $(git branch --format='%(refname:short)' | grep -v '^main$'); do echo "$b rev-list=$(git rev-list --count main..$b) 真未合=$(git cherry -v main $b | grep -c '^+')"; done
4. ls docs/superpowers/plans/ 应恰好多出 liaison-unit1 / unit2 / unit3 与 unique-race 四个，`### Task` 数 ≥ 3 / ≥ 3 / ≥ 2 / ≥ 2
5. checkbox：grep -c '^- \[x\]' openspec/changes/hr-wecom-aibot-liaison/tasks.md —— 满额 18（1.1–1.6 + 2.1–2.6 + 3.1–3.6）；按各条 OK/PARTIAL 分别判；核 `<<<<<<<` 计数为 0
6. 🔴 落点守卫：git diff --stat <开跑前 main>..main -- app/ scripts/ requirements.txt 里**不得**出现值守泳道的改动（D10：liaison 代码只能在 tools/）；修复泳道的 app/storage/idempotency.py、app/web/server.py 除外
7. 🔴 密钥守卫：git grep -nE "HR_LIAISON_BOT_SECRET\s*=\s*\S{8,}" -- ':!*.md' 应为空（.env.example 只许占位）
8. pytest 与【三】4 基线比：≥ 开跑前且 0 失败；单测离线
9. bash docs/openers/run-lanes.sh --dry-run 应输出「条目总数自检」一行且 0 条泳道（R 的产物 + 六条已摘）
10. git status 里不得出现 data/ 下任何文件（liaison.db 测试库应在 data/ 且被 ignore）
11. ⛔ 本批不做 worktree 清理

【七、出看护报告】写成 .claude/handoff/lanes-<时间戳>-看护报告.md（gitignore 内，不提交），含：
  - 编号 / 脚本判定 / 真身核验结论 / 分歧
  - PARTIAL 留步逐条；红灯清单（Task 数不足、真未合 ≠ 0、pytest 失败、tasks.md 冲突标记、落点守卫、密钥守卫、data/ 进 git、ahead ≠ 0）
  - M 的 SDK 实测结论（装得上 / 走退路）单列一行——这决定第 4–7 章怎么排
  - superpowers 是否真调到（九条各一行）
  - 下一步：**一条推荐动作 + 理由**（同结果只写一种；只有结果不同且属 Shao Peishen 必须拍的才列选项并标推荐）

【八、收工】把报告路径和那张表贴给我。
