# [Mac]0919S-泳道收敛合并0919P0919Q回main精确版

【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: main ｜ worktree: ❌ 不勾（合并操作在主检出，不新建分支）｜ 工作区: 仓库根 ｜ 派发: Cowork·HR业务线-接力0918AO ｜ 模型: Sonnet

⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动，print 模式下那个 MCP 工具未必挂载，名字另由 -n 给。🔴 若改为手工贴进 CC Desktop，必须自行补上第 3 行。

## 零、为什么（取代 `0919R`，不是它的重跑）

`0919R` 已实测确认：`ff-only` 对 `lane-0919p-tag4-regex`／`lane-0919q-queue-digest` 均不可用（main 已分叉），按红线正确停车、未强合。本条基于 `0919R` 实测结果，已提前用只读 diff 精确定位冲突范围（见下），改用 `--no-ff` 真合并，且把唯一预期冲突的解法写死，执行者不需要判断，只需核对预判与实际是否一致。

**已定位的精确冲突范围**（`git diff --unified=0 <merge-base> <ref> -- <file>` 逐文件核对得出，`merge-base` 对两条分支均为 `8bfe582`）：

- `lane-0919q-queue-digest`：与 main 无任何共同改动文件重叠（只新增 `scripts/queue_pending.py`／`scripts/handoff_digest.py`／两个新 test 文件），**预期零冲突**
- `lane-0919p-tag4-regex`：与 main 只在 `docs/roadmap/定夺队列.md` 一处重叠——**只有 `Q-47` 那一行（表格里以 `| Q-47 |` 开头的行）双方都改了，是唯一预期冲突**；`docs/roadmap/定夺队列.md` 里 `Q-50` 那行 main 改了但分支没碰，`docs/session接力.md` 两边各自改了不同区域（main 在第 66 行后插入 7 行，分支改第 86/94/100 行），行号不重叠，git 三方合并应能自动合，不应出现冲突标记

## 一、开工先核

1. `git fetch origin`，确认两条远端分支 HEAD 仍是 `origin/lane-0919p-tag4-regex` 含 `53ed69a`/`bc8b0d3`、`origin/lane-0919q-queue-digest`＝`41222b3`（若已变化，说明有新提交推上去，停止报告，不要用本文预判的冲突范围继续）
2. 确认当前 `main` 仍是 `695170d`（`0919R` 收工时的状态）；若已前进，重新跑零节同款 `git diff --unified=0` 精确定位命令，核对预判是否仍成立，不成立则停止报告，不擅自套用旧预判

## 二、依次合并

**Step 1 — 先合 `lane-0919q-queue-digest`（预期零冲突）**

```
git merge --no-ff origin/lane-0919q-queue-digest -m "merge(0919Q): 定夺队列过滤+handoff摘要工具 收敛回main"
```

若报冲突 ⇒ 与零节预判不符，立即停，`git merge --abort`，原样报告冲突文件清单，不要自行解决。

若无冲突（预期路径）：跑 `python -m pytest tests/test_queue_pending.py tests/test_handoff_digest.py -q`，确认 0 failed；`tests/test_doc_size_budget.py -q` 确认 0 failed；通过后 `git push origin main`。

**Step 2 — 再合 `lane-0919p-tag4-regex`（预期恰好一处冲突）**

```
git merge --no-ff origin/lane-0919p-tag4-regex -m "merge(0919P): TAG4正则双格式兼容+session接力人工核对 收敛回main"
```

预期结果：只有 `docs/roadmap/定夺队列.md` 报冲突，且冲突块只有一处，就是 `Q-47` 那一行。

- 若冲突文件不止 `docs/roadmap/定夺队列.md`，或该文件里冲突块不止一处，或冲突块对应的不是 `Q-47` 行 ⇒ 与预判不符，立即 `git merge --abort`，原样报告实际冲突内容，不要自行判断怎么解
- 若与预判完全一致（唯一冲突块＝`Q-47` 那一行）：打开 `docs/roadmap/定夺队列.md`，找到形如
  ```
  <<<<<<< HEAD
  | Q-47 | ...（main 侧内容，即 0919R 之前追加的占位提示「尚未合入 main」那版）... |
  =======
  | Q-47 | ...（分支侧内容，即 0919P 的完整核实报告，含 relay:R-11 到 R-19）... |
  >>>>>>> origin/lane-0919p-tag4-regex
  ```
  的冲突块，**整体替换为 `=======` 与 `>>>>>>>` 之间的那一份**（即取分支侧／incoming 那版——它是完整审计报告，信息量是 main 占位提示的超集，不丢信息），删除三行冲突标记本身。核对文件里不再有任何 `<<<<<<<`／`=======`／`>>>>>>>` 残留后 `git add docs/roadmap/定夺队列.md`
- 跑 `python -m pytest tests/test_dispatcher_backlog.py tests/test_dispatcher_answers.py -q`（按实际存在的文件名核对，覆盖 `TAG4_RE` 改动），确认 0 failed；`tests/test_doc_size_budget.py -q` 确认 0 failed
- `git commit`（合并提交信息用上面 `-m` 给的那条，冲突解决后 `git merge --continue` 或直接 `git commit` 均可，用你环境里能正常收尾的那种）
- 确认通过后 `git push origin main`

## 三、红线

- ⛔ 除了解决零节已精确定位的那一处预期冲突，不改动任何其他文件、不顺手改代码、不扩大范围
- ⛔ 不删除已合并的 worktree（`.claude/worktrees/lane-0919p-tag4-regex`／`lane-0919q-queue-digest`）或远端分支——留给台账例行清理
- ⛔ 不碰 `.51`、不发版、不改 `app/` 下任何应用代码
- ⛔ 无人值守：任何一步与本文预判不符（多余冲突、测试不过、push 被拒且 3 次 `pull --rebase --autostash` 重试后仍失败）一律停+原样报告，不追问、不代拍、不自行扩大解决范围

## 四、收口

1. `docs/session接力.md`「二、下一步」追加一行：两分支已收敛回 main，两次 merge commit hash + push 结果
2. 定夺队列 `Q-47` 行此时应已是分支侧（0919P）的完整内容，若还需要补一句"已合入 main"的确认可以补，不强制
3. 上述 docs 改动可以直接 `git add`/`git commit`/`git push`（本条本身在 CC 原生环境，不需要走 commit-request 通道；若这一步单独产生新的冲突，同样按红线原样报告，不猜）
4. 顶格输出 `OPENER_DONE` 或 `OPENER_PARTIAL: <原因>`
