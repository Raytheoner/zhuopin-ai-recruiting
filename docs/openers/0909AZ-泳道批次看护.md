[Mac]0909AZ-泳道批次看护
【设置】执行环境: CC Desktop ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（你是调度者，实际干活的是脚本起的独立 session）｜ 工作区: 仓库根 ｜ 派发: Cowork·HR业务线-接力0909Q

> 本文件是 `[Mac]0909AZ` 的正文（引用式 opener）。第十六批看护者（编号 0909AK–AP，六条泳道（五条技术债 ＋ 一条入站预演））。
> ✅ **原互斥条件已解除（`[Mac]0909AJ` 订正，2026-09-09 23:10 CST）**：AJ 已于 23:02–23:10 跑完
> （断网 245 秒，TD-39 六项判据全过，**TD-39 已销账**，commit `ec5df34`／`b2700dd`），
> 本批发车时它**已收敛**，⛔ 不再互斥。⚠️ 第三节自检 2 的 `pgrep` 仍要跑——那是防止有人又手动起了
> 值守进程，⛔ 不是防 AJ。
> 🔴 六条泳道都 ⛔ 不碰 `session*`（那是 AJ 的地盘）、⛔ 不碰 `tasks.md`、⛔ 不碰 `docs/跟进信/`。

【一、你的角色】不亲自执行任何 opener。做五件事：提交编排落档 → 前置自检 → 发车 → 轮询盯守 → 真身核验并出报告。
⛔ 不改代码、不跑 spec-to-plan / run-build / openspec-archive-change、⛔ 不跑 `send-followup`、⛔ 不发任何真实消息、⛔ 不装 launchd、⛔ 不发版。

【二、先提交 Cowork 侧的编排落档】git status --short 预期含：
  M docs/openers/OP-0820-全量编排.md      （第十六批一节 ＋ 号池 AK–AO/AZ 六行）
  ?? docs/openers/0909AZ-泳道批次看护.md   （本文件）
另有 `_to_delete/` —— 🔴 **绝不 add**。其它未跟踪文件单列报告、不提交。
  🔴 **提交前先补登一行号池**（`[Mac]0909AJ` 2026-09-09 追加）：在 `OP-0820` 顶部号池台账里、
  紧跟 `0909AZ` 那行之后插入一行，内容＝日期 `09-09`、编号 `[Mac]0909AP`、
  做什么「N-0 兜底层（存活戳看门狗）端到端覆盖，⛔ 不再断 Wi-Fi」、
  出处 `docs/openers/0909AQ-N0看门狗端到端覆盖.md`（引用式，`[Mac]0909AJ` 出；Shao Peishen 答 `2a` 授权）。
  ✅ **本步你已于 2026-09-09 完成（`5efb0e2`），且做得比原指令对**：你查出 `0909AP` 早被
  第十六批·入站预演泳道占用（`0909AJ` 取号时它还在 `OP-0820` 未提交段里、grep 不到），
  改判 N-0 用 `0909AQ`。⛔ 不必再做一遍。
  ⚠️ **`0909AJ` 当时落不了这一行**：那会儿 `OP-0820` 带着你这 161 行未提交改动，`git add` 它会把你的活一并提交。
  ⛔ **不补登下一轮必重派 `0909AP`**（08-27 一天撞过 4 次）。补完下一个可用号是 **`0909AQ`**。
  ⛔ 本批**不发** `0909AP`（Shao Peishen 2026-09-09 答 `2b`：等本批收敛后单独发），只补台账。

  git add docs/openers/OP-0820-全量编排.md docs/openers/0909AZ-泳道批次看护.md
  git status --short 复核：暂存区不得出现 `_to_delete/`、`.env`、`tools/liaison/.env`、`data/`。出现即停下报
  git commit -m "docs(openers): 第十六批编排（队列债 ∥ 群通知债 ∥ 日志脱敏债 ∥ 守卫债 ∥ 回填复核）"
  git push origin main
push 被拒 → git pull --rebase --autostash origin main 后重试最多 3 次；index.lock 等 5 秒重试 5 次不删。

【三、前置自检】任一不过就停下报告，不要硬上：
1. 🔴 `ps -eo pid,command | grep -v grep | awk '$0 ~ /run-lanes/'` 必须为空。⚠️ ⛔ 不要用 `pgrep -f 'run-lanes.*\.sh'` 判——**你自己的命令行会被它匹配到，恒真**（2026-09-09 实测两次误判）
2. 🔴 `pgrep -f 'python.*tools\.liaison'`。⚠️ **二次订正**（`0909AJ`，2026-09-09 23:29 CST）：
   **原判据「应为空」已作废** —— Shao Peishen 已于 23:27 装好 launchd（G-4 已闭合，
   `com.zhuopin.hr.liaison`，`KeepAlive=true`），值守进程从此**常驻**，pgrep **必然非空**。
   ⛔ **非空不再是停车理由**。改判如下：
   ```
   PG=$(pgrep -f 'python.*tools\.liaison')
   LD=$(launchctl print gui/$(id -u)/com.zhuopin.hr.liaison 2>/dev/null | awk '/^\tpid =/{print $3}')
   echo "pgrep=$PG launchd_pid=$LD"
   ```
   - **两者一致（或 `$PG` 全部包含在 launchd 那个 pid 里）⇒ 正常，继续发车。**
     ⛔ **绝不 kill 它、⛔ 不 bootout**——那是他刚装好的生产值守通道，杀了他收不到企微消息
   - `$PG` 里有 launchd pid **之外**的进程 ⇒ 有人手动又起了一个，**停下报告**，⛔ 不要自己 kill
   - `$LD` 取不到而 `$PG` 非空 ⇒ 同上，停下报告
   ⚠️ 五条泳道都在 worktree 里跑单测（离线），**不碰这个常驻进程**，两者互不干扰
3. `grep -c "RUN_LANES_COPY" docs/openers/run-lanes.sh` ≥ 1；`command -v claude`；`grep -c "run-lanes.sh" .claude/settings.json` ≥ 2
4. `git pull --rebase --autostash origin main`；`git status -sb` 记 ahead（应 0）；`git log -1 --format=%h` 记作 <开跑前 main>
5. `ls .claude/handoff/launch/` —— 不应有 `*.request`
6. `bash docs/openers/run-lanes.sh --dry-run --only 0909AK,0909AL,0909AM,0909AN,0909AO,0909AP`
   预期 **6 条泳道、6 条 opener**，末尾「条目总数自检：Σ=N=6（--only 生效，M=7 不参与判据）」：
     ◆ 队列债      ✓ 0909AK 正文 16 行
     ◆ 群通知债    ✓ 0909AL 正文 17 行
     ◆ 日志脱敏债  ✓ 0909AM 正文 17 行
     ◆ 守卫债      ✓ 0909AN 正文 16 行
     ◆ 回填复核    ✓ 0909AO 正文 16 行
     ◆ 入站预演    ✓ 0909AP 正文 <见 dry-run 实际输出> 行
   数字对不上 / 任一 ✗ / exit 13 / exit 15 → 停下报告，⛔ 不改编排文件
7. 🔴 pytest 基线：`./venv/bin/python -m pytest -q 2>&1 | tail -3`（⛔ 不抄别人的数，也⛔ 不写死进任何文件）。有失败项 → 停下报告
8. 记基线实数（不作闸）：`grep -c '^## TD-' docs/tech-debt.md`、`grep -c '✅ 已还' docs/tech-debt.md`

【四、发车】
先判 launchd：`launchctl print gui/$UID/com.zhuopin.hr.lane-launcher >/dev/null 2>&1; echo $?`
- 为 0 → 用 **Write 工具**（⛔ 不用 Bash heredoc）写 `.claude/handoff/launch/<TS>.request`，内容一行：
  `--full-auto --yes --only 0909AK,0909AL,0909AM,0909AN,0909AO,0909AP --max-parallel 5`
  轮询 60 秒等 `<TS>.started`；出现后再等 60 秒确认 `ps` 里 run-lanes 真在跑**且**日志目录已建出来。两者任一不成立 ＝ 已知的连坐杀复现 → 切退路，⛔ 不重试第二次
- 非 0 或 launchd 没触发 → 退路：把下面这段原样作为 bash 代码块发给 Shao Peishen 点 Run（⛔ 不带 `echo "PID=$!"`，粘进 zsh 会卡在 dquote>）：
```bash
cd /Users/paulshao/Projects/HumanResource && nohup bash docs/openers/run-lanes.sh --full-auto --yes --only 0909AK,0909AL,0909AM,0909AN,0909AO,0909AP --max-parallel 5 > /tmp/run-lanes-b16.log 2>&1 &
```
贴完等他点，⛔ 不要自己再用 Bash 起一次（分类器会拦）。

【五、轮询盯守】每 5 分钟：`cat "$LOGDIR/results.tsv"`；`ls -la "$LOGDIR"/*.log`；`ps` 判是否还在跑。每轮播报一行。预案（⛔ 不问）：
- PARTIAL → 正常。已知合法留步：`0909AL` 若核出 TD-29 已被 `0909T` 处置则只补销账行；`0909AO` 若查出「代码无误、真因是改了副本」则不改代码只加防呆
- FAIL / NO-SENTINEL → 该泳道停、别的不受影响。`API Error: 529` → 查 status.claude.com，解除后按同样方式重发剩余编号
- 🔴 `docs/tech-debt.md` 合并冲突（四条泳道各销自己的账）→ 泳道自己按「各保留自己那几段」解，你只记录；解错了真身核验第 4 条会抓
- 🔴 results.tsv 同一编号两行 → 立即停止盯守，贴 `head -5 <boot_log>`
- 超过 3 小时未收敛 → 不 kill，记录现状出报告

【六、真身核验 —— 逐条跑，不许跳】
  `cd /Users/paulshao/Projects/HumanResource && git pull --rebase --autostash origin main`
1. `git log --oneline -30` 对照 results.tsv（AK/AL/AM/AN/AO/AP 各 1 条合并）；`git status -sb` ahead 应 0
2. 逐分支真未合：`for b in $(git branch --format='%(refname:short)' | grep -v '^main$'); do echo "$b rev-list=$(git rev-list --count main..$b) 真未合=$(git cherry -v main $b | grep -c '^+')"; done`
   ⚠️ `rev-list` 非 0 而 `cherry` 全 `-` ＝ main 被 rebase 过的假阳性，⛔ 不要当成没合
3. 🔴 落点守卫（`git diff --stat <开跑前 main>..main -- <路径>` 必须为空）：`app/`、`scripts/`、`tools/liaison/session_client.py`、`tools/liaison/session.py`、`openspec/changes/hr-wecom-aibot-liaison/tasks.md`、`docs/跟进信/`、`requirements.txt`、`pyproject.toml`、`.env.example`
4. 🔴 边界守卫（`git log --format=%s <开跑前 main>..main -- <路径>` 看归属）：`queue.py` 只被队列债改过；`notify/` 与 `storage/schema.py` 只被群通知债改过；`logsetup.py` 只被日志脱敏债改过；`tests/test_liaison_effects.py` 只被守卫债改过；`followup.py` 只被回填复核改过；🔴 `0909AP` 的 diff 里**不得出现任何 `tools/liaison/*.py` 产品代码**，只许 `tests/` 与 `docs/findings/`
5. 🔴 销账核对：`grep -c '✅ 已还' docs/tech-debt.md` 比基线**至少 +6**（TD-23/24/26/28/31/32/35 里除 TD-29 外的都该销；TD-29 视 `0909AL` 的核实结论，销或不销都要在报告里给理由）。⛔ TD-21/22/37/39 仍**不得**被销
6. 🔴 咬住证据在位（本批的全部价值，逐条确认报告里有）：AK 两条先红后绿；AL 的 TD-29 核实结论 + TD-28 新 CHECK 原文；AM 的对抗用例清单（手机号四种、thread_id 四种渲染）；AN 说清「哪部分差额被豁免、哪部分仍红」；AO 的四种可能排除证据与真因结论；AP 的四组链路断言输出 ＋ TD-22 防呆用例的报错原文
7. `grep -rc '<<<<<<<' docs/tech-debt.md` 为 0；`git status` 里不得出现 `data/`、`.env`、`_to_delete/`
8. pytest 与【三】7 基线比：≥ 开跑前且 0 失败；单测离线（⛔ 不得有真实网络）
9. ⛔ 本批不做 worktree 清理；⛔ 不跑 `0909AJ`

【七、出报告】写成 `.claude/handoff/lanes-<时间戳>-第十六批看护报告.md`（gitignore 内，不提交），含：前置状态（dry-run 原文 + pytest 基线 + TD 基线数）／编号与脚本判定／真身核验逐条结论／分歧／PARTIAL 留步逐条／红灯清单／发车路径实证（走 launchd 还是点 Run）／**下一步：一条推荐动作 + 理由**（同结果只写一种）。

【八、收工】把报告路径和那张表贴给我，并单列一行
「⏸ `0909AQ`（G-12·N-0 看门狗端到端覆盖）可以发了——本批已收敛」。
⚠️ **编号订正**：原文写 `0909AP`，那是撞号前的旧号；`AP` 是**你这批里的入站预演泳道**，
N-0 已改判为 **`0909AQ`**（你自己 `5efb0e2` 改的）。⛔ 收工那行别写回 `AP`。
⚠️ **订正**（`[Mac]0909AJ`）：原文这里写的是「`0909AJ` 可以跑了」，**已过期**——AJ 在本批发车前就跑完了。
