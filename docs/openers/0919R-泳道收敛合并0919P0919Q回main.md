# [Mac]0919R-泳道收敛合并0919P0919Q回main

【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: main ｜ worktree: ❌ 不勾（合并操作在主检出，不新建分支）｜ 工作区: 仓库根 ｜ 派发: Cowork·HR业务线-接力0918AO ｜ 模型: Sonnet

⚠️ 本块不带 set_session_title 那一行，这是豁免不是漏写：由 run-lanes.sh 以 printf | claude -p 无头启动，print 模式下那个 MCP 工具未必挂载，名字另由 -n 给。🔴 若改为手工贴进 CC Desktop，必须自行补上第 3 行。

## 零、为什么

`0919P`（TAG4 正则双格式兼容修复，commit `53ed69a` 代码 + `bc8b0d3` docs，分支 `lane-0919p-tag4-regex`）与 `0919Q`（定夺队列过滤 + handoff 摘要工具，commit `41222b3`，分支 `lane-0919q-queue-digest`）均已 `OPENER_DONE` 并各自 push 到 origin 对应分支，但两者收工报告都明确留步未合回 main——`0919P` 原话：「main 已领先本分支 3 个提交，未做 ff-only 合并，留给后续收敛流程处理」。

按 `.claude/skills/lane-dispatch/SKILL.md`「在 worktree 里改并提交在分支上，再 `git -C <主工作区> merge --ff-only <分支>` 与 push」的既定流程，本条补做这一步收敛。两分支改动路径互不重叠（`0919P` 改 `scripts/dispatcher_backlog.py`／`docs/session接力.md`／`docs/roadmap/定夺队列.md`；`0919Q` 新增 `scripts/queue_pending.py`／`scripts/handoff_digest.py`／两个新 test 文件），预期 ff-only 顺利，若不顺利见二、三节预案。

## 一、开工先核（自己核，不许信本文转述）

1. `git fetch origin`
2. 确认 `origin/lane-0919p-tag4-regex` 的 HEAD 是 `53ed69a` 的后继（含 `bc8b0d3`）；确认 `origin/lane-0919q-queue-digest` 的 HEAD 是 `41222b3`
3. `git diff --name-only main origin/lane-0919p-tag4-regex` 与 `git diff --name-only main origin/lane-0919q-queue-digest` 逐一核对改动路径与零节描述一致、两者无交集
4. 任一条对不上 ⇒ 停，不猜着合，`OPENER_PARTIAL` 报告实测差异

## 二、依次合并（ff-only，不 rebase，不三方合并）

1. `git merge --ff-only origin/lane-0919p-tag4-regex`
   - 成功后跑 `python -m pytest tests/test_dispatcher_backlog.py tests/test_dispatcher_answers.py -q`（按实际文件名核对，覆盖 `TAG4_RE` 改动），确认 0 failed
   - `tests/test_doc_size_budget.py -q` 确认 0 failed（该分支改了两份滚动文档）
   - 确认通过后 `git push origin main`
2. `git merge --ff-only origin/lane-0919q-queue-digest`
   - 成功后跑 `python -m pytest tests/test_queue_pending.py tests/test_handoff_digest.py -q`（按实际文件名核对），确认 0 failed
   - 确认通过后 `git push origin main`
3. 任一步 `merge --ff-only` 报错「不是 fast-forward」⇒ 立即停，不改用 `--no-ff`、不 rebase、不删重来，原样报告 `git log --oneline main..origin/<分支>` 与 `git log --oneline origin/<分支>..main` 的差集
4. 任一步测试不是 0 failed ⇒ 停在该步（已 merge 的部分保留在本地，不 push，不 reset），报告具体失败测试名

## 三、红线

- ⛔ 只做这两个分支的 merge，不改动被合并内容之外的任何文件，不顺手改代码
- ⛔ 不删除已合并的 worktree（`.claude/worktrees/lane-0919p-tag4-regex`／`lane-0919q-queue-digest`）或分支——留给台账例行清理，本条不做
- ⛔ 不碰 `.51`、不发版、不改 `app/` 下任何应用代码
- ⛔ 无人值守：ff-only 失败或测试不过一律停+报告，不追问、不代拍、不扩大范围自行修复

## 四、收口

1. `docs/session接力.md`「二、下一步」追加一行：两分支已收敛回 main，附两次 merge 后的 commit hash 与 push 结果
2. 定夺队列 `Q-47` 行答复列（若尚未含 merge 后 commit hash）追加一句收敛结果引用
3. 用 commit-request 通道提交上述 docs 改动（`docs/**` 白名单内），不要用裸 `git commit` 走 device_bash 类比操作——本条本身就在 CC 原生环境，直接 `git add`/`git commit`/`git push` 即可，无需 commit-request
4. 顶格输出 `OPENER_DONE` 或 `OPENER_PARTIAL: <原因>`
