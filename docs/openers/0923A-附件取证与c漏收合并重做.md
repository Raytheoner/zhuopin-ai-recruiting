# [Mac]0923A 附件接收取证 ＋ c 漏收诊断（合并重做 `0921E`／`0921E续1`／`0921F`）

## 零、为什么

`0921E`／`0921E续1`／`0921F` 三条都自报 `OPENER_DONE`，但 2026-09-23 实测 **main 上什么都没有**：
`test_attachments_unknown_frame_dump.py` 全仓库不存在、`tools/liaison/frames.py` 里没有
`frame_key_types`／`frame_chattype`、`docs/roadmap/定夺队列.md` 无「只走私信」、`docs/session接力.md`
无「决策点 c」诊断。`0920K` 的机器判据闸把三条全拦下（`GATE-FAIL`），拦对了。
⇒ 本条**合并重做**，⛔ 不再拆成多条（口径：缩 WIP）。

业务背景：汤丽萍的 10 个岗位清单实体是群里那份 41.5K `.doc`，两次文本答复都只有标题。
Shao Peishen 口径：**附件只走机器人私信（单聊）**，群里的附件不收。
附件 fail-closed 在 `frames.py::compute_attachment_ref`（`ATTACHMENT_FIELD_PATHS_BY_MSGTYPE` 为空），
**刻意如此**——没有真实帧就不许猜字段。本条只把「下次来帧能取到证」做好。

## 一、开工自核（任一不过即顶格 `OPENER_PARTIAL` 并停）

1. `python3 -c "import tools.liaison.frames as f; print(len(f.ATTACHMENT_FIELD_PATHS_BY_MSGTYPE))"` 为 `0`。
2. 分支 `lane-0923a-attach-probe` 从最新 main 起（旧分支 `lane-0921e-attach-probe` 上的东西
   **不要 cherry-pick**，重做即可；它从未合回 main，可能是半成品）。

## 二、动作

① **口径落地**：附件接收只在**单聊（私信）**帧上尝试；群帧附件一律忽略并记一行 info。⛔ 不动文本帧既有路径。
② **未知附件帧取证**：捕获 `AttachmentFieldsUnverifiedError` 时，把该帧的**键路径与值的类型名**
   落到 `data/liaison/logs/unknown-attachment-frames/<ts>-<msgtype>.json`（如 `{"file.url":"str"}`）。
   🔴 **一个原值都不许落盘**——url／aeskey／filename／字节全禁。⛔ 不改 fail-closed 行为本身。
③ **诊断决策点 c 漏收**：截图证实 2026-09-20 17:04 汤丽萍发了三条 `@MAC机器人` 文本
   （决策点 a／b／c），归档只进了 a、b 两条。查入站日志判成因（时间窗／去重键／群帧过滤／服务存活）。
   结论写进 `docs/session接力.md` 一行，含四标签。**只诊断，⛔ 不改判定逻辑。**
④ **测试**：新增 `test_attachments_unknown_frame_dump.py`，**放在本仓库既有测试布局里**
   （`tools/liaison/tests/` 或 `tests/`，按现状放，⛔ 不新建目录）。断言：产出文件只含键名与类型名、
   不含任何值；群帧附件被忽略且不抛错；单聊未知附件帧仍 fail-closed 且留下结构文件。
   narrow 跑该文件，⛔ 不跑全量。
⑤ **队列**：把「决策点 c 漏收」与「附件只走机器人私信」并入既有 `Q-01` 行的问题列，
   ⛔ 不另立新行、⛔ 不改 `Q-01` 状态与答复列。
⑥ **合回 main**：`git -C <主工作区> merge --ff-only lane-0923a-attach-probe` 并 push，清 worktree。
   🔴 合并后**在主工作区**复跑一次 ④ 的测试，确认东西真在 main 上——三条前作就是败在「分支上有、main 上没有」。

## 三、机器判据

```bash
set -e
cd "$(git rev-parse --show-toplevel)"
T=$(ls tools/liaison/tests/test_attachments_unknown_frame_dump.py tests/test_attachments_unknown_frame_dump.py 2>/dev/null | head -1)
test -n "$T"
venv/bin/python -m pytest "$T" -q
grep -q 'frame_key_types' tools/liaison/frames.py
grep -qE '私信|单聊' tools/liaison/frames.py
grep -q '只走私信' docs/roadmap/定夺队列.md
grep -q '决策点 c' docs/session接力.md
python3 -c "import tools.liaison.frames as f; assert len(f.ATTACHMENT_FIELD_PATHS_BY_MSGTYPE)==0"
```

## 四、并发协议

泳道「回件通道」内串行：本条过了 `0923B` 才跑。两条都会写 `docs/session接力.md`，故必须串行。

## 五、红线

🔴 一个附件原值都不许落盘或进日志。⛔ 不放开 fail-closed、不猜字段、不填映射表（等真实私信附件帧）；
⛔ 不改 `attachments.py` 的两条既有禁令；⛔ 不对外发送；⛔ 不碰 `.51`；
⛔ 不动 `data/liaison/unpack-signal.json` 与 `archive/`（那是 `0923B` 的事）。

## 六、收口

七条机器判据全过 ＋ ③ 结论已入接力卡 ⇒ **顶格** `OPENER_DONE`。
🔴 判据未全过却打 `OPENER_DONE` 是本泳道已犯三次的错（`0921E`／`0921E续1`／`0921F`），
任一不过一律 **顶格** `OPENER_PARTIAL: <哪条没过、实测值多少>`。
