# [Mac]0920D 续棒泳道看护机制落档

## 零、为什么有这条

2026-09-20 Shao Peishen 定两条口径，本条把它们从「会话里说好」变成「仓库里落档」：

1. **本机只构建本地项目，GitHub 只是存储**——⛔ 永不直接调 GitHub 上的 skill，⛔ 与其他项目（「企业AI转型」线）的 skill 无关；只有他明确指示「参考 XX 项目做法」时才去只读参考，且**只模仿、在本仓库自建实现**。
   *触发实证*：本轮 Cowork 开场误取了「企业AI转型」线的 `zhuopin-lane-watch`（其正本在 Win 端 `C:\Dev\zhuopin-ai\...`，本机不可及），白跑一轮才回到本项目的 `lane-dispatch`。
2. **续棒免粘贴 Opener**——新会话只要说「续棒泳道看护」即接上，不再出转场 Opener 让他手工粘。
   会话标题沿用 Opener 前缀形状：端口（`[Mac]`／`[Win]`）＋日期＋字母编号，续棒固定 `R` 系列。

产物 `.claude/skills/lane-watch-relay/SKILL.md` 已由 Cowork·0918AO 写进工作区（未提交）。
本条只做**提交与核验**，⛔ 不改该文件内容、⛔ 不改任何业务代码。

## 一、开工自核（任一不过即停，登记后退出）

1. `cd /Users/paulshao/Projects/HumanResource && git status --short` —— 确认下列五个路径在改动列表里（`.claude/skills/lane-watch-relay/SKILL.md` 为新增）：
   - `.claude/skills/lane-watch-relay/SKILL.md`
   - `docs/openers/0920D-续棒泳道看护机制落档.md`
   - `docs/openers/OP-0820-全量编排.md`
   - `docs/openers/号池台账.md`
   - `docs/session接力.md`
2. `head -4 .claude/skills/lane-watch-relay/SKILL.md` —— frontmatter 里 `name: lane-watch-relay` 存在。
3. `python3 -c "import re,sys; ..."` 不需要；只需 `grep -c '续棒泳道看护' .claude/skills/lane-watch-relay/SKILL.md` ≥ 1。
4. 工作区**别的**未提交改动（`_to_delete/`、`.claude/local-preview.pid`、`Claude outputs/`、`0918V-补Q27Q38映射与键归一化.md` 草稿）**原样不动**——⛔ 不提交、⛔ 不删除。

## 二、执行

```
git add -- .claude/skills/lane-watch-relay/SKILL.md docs/openers/0920D-续棒泳道看护机制落档.md docs/openers/OP-0820-全量编排.md docs/openers/号池台账.md docs/session接力.md
git commit --only -m "feat(skill): lane-watch-relay 续棒泳道看护——免粘贴 Opener 接手机制落档" -- .claude/skills/lane-watch-relay/SKILL.md docs/openers/0920D-续棒泳道看护机制落档.md docs/openers/OP-0820-全量编排.md docs/openers/号池台账.md docs/session接力.md
git push origin main
```

- `tests/test_doc_size_budget.py` 跑一次（`venv/bin/python -m pytest tests/test_doc_size_budget.py -q`），不过即停、不提交。
- push 被拒 ⇒ `git pull --rebase --autostash origin main` 后重试，最多 3 次。

## 三、并发协议（逐字内嵌，本条必须遵守）

1. **只 `git add` 上面明确列出的五个路径。** ⛔ 禁止 `git add -A` / `git add .` / `git commit -a` / `git stash`（含 `-u`）。
2. `git status` 里出现别人的改动是正常的，不要停下、不要问、不要顺手提交。
3. push 被拒 → `git pull --rebase --autostash origin main` 后重试，最多 3 次。⛔ 不要在 commit 前 pull。
4. 报 `.git/index.lock` 已存在 → 等 5 秒重试最多 5 次，⛔ 默认绝不删除该锁。

## 四、红线

- ⛔ 不改 `.claude/skills/lane-watch-relay/SKILL.md` 的内容（它是本条的交付物，不是待编辑草稿）。
- ⛔ 不碰 `.51`、不发版、不对外发送任何消息。
- ⛔ `Skill(superpowers:…)` 回 `Unknown skill` ⇒ 登记「⏸ 留步：superpowers 不可达」并停（本条实际不需要 superpowers）。
- ⛔ 不删除、不移动工作区里任何未追踪文件。

## 五、收口

1. `git rev-list --count main..origin/main` 为 0 且 `git log --oneline -1` 是本次 commit ⇒ 确认已上 main。
2. 顶格打印哨兵 `OPENER_DONE`，并在收工报告里给出 commit hash。
3. ⛔ 确认内容真在 main 上之前不许输出 `OPENER_DONE`。
