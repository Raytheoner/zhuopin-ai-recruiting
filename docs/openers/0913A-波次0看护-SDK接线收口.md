[Mac]0913A-波次0看护-SDK接线收口
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（本件是**看护件**：自己在主检出做 git 落档与收口，写代码那一步由它**自建 worktree** 隔离，见【三】）｜ 工作区: 仓库根（/Users/paulshao/Projects/HumanResource）｜ 派发: Cowork·HR业务线-接力0909AU

> 本文件是 `[Mac]0913A` 的正文（引用式 opener）。
> **一次粘贴，内部串行编排**（2026-09-12 Shao Peishen 口径：多 opener 且有时序要求 ⇒ 出一份看护件，⛔ 不拆成多份让人挨个粘）。
> 目标＝把 `liaison-reply-bridge-and-patrol` §0 的**门槛 0.2**（SDK `message` 事件接线）从 `- [ ]` 变成 `- [x]`，
> 这是第十七批波次 1 四条 plan 泳道**唯一**由机器能解的阻断。
>
> 🔴 **本件到 0.2 为止就交回**。门槛 0.1（TD-42 真实验证）与 0.3（一条真实入站落库）要 `.env` ＋ 真实建连 ＋ 断网 13 分钟 ＋
> Shao Peishen 本人发一条消息 ⇒ 归 `[Mac]0910A`（主工作区，他在场）。⛔ 本件不碰那两条、不重启值守服务、不请任何人发消息。
>
> **背景**：波次 1 的看护者已于 2026-09-13 08:39 跑过并**正确停下**，报告
> `.claude/handoff/lanes-20260913-083904-第十七批波次1看护报告.md`（gitignore 内）。它的两条阻断本件各解一半：
> A＝§0 门槛（本件解 0.2）；B＝编排文件没有波次 1 泳道块且有两处旧标注残留（本件【二】摘掉旧的；波次 1 按接力原意**手动贴四个 session**，不补块）。

---

【一、开工前状态核对】逐条跑，与预期不符就**停下报**，⛔ 不要自行修正后继续。

1. `pwd` ＝ 仓库根、`git branch --show-current` ＝ `main`、`ls .git/*.lock 2>/dev/null || echo NO-LOCK` ＝ `NO-LOCK`。
2. `git status --short` 预期**恰好**这六行（顺序不论）：
   `M .claude/skills/kickoff/SKILL.md` ／ `M docs/openers/0910B-SDK-message事件接线.md` ／
   `M docs/openers/OP-0820-全量编排.md` ／ `M docs/session接力.md` ／
   `?? docs/openers/0913A-波次0看护-SDK接线收口.md`（本文件自己）／ `?? _to_delete/`
   多出别的 ⇒ 停下报（有别的 session 在动）。⛔ **不要 add `_to_delete/`**，也⛔ 不要去删它。
3. `git log --oneline -1` ＝ `e57ebb6`（或更新）；`git fetch origin && git rev-list --left-right --count origin/main...HEAD` 两侧都是 0
   （非 0 ⇒ 先 `git pull --rebase --autostash origin main`）。
4. 记 pytest 基线：`./venv/bin/python -m pytest -q 2>&1 | tail -3`。**现跑一次取基线**，
   ⛔ 不许从接力文档抄一个数字当基线（那个数字会过期）。

---

【二、落档提交 —— 必须先做完并 push，⛔ 不能和【三】并行】

**为什么必须先**：【三】在 **worktree** 里跑，worktree 的文件来自 **git**。主工作区未提交的订正（尤其
`docs/openers/0910B-SDK-message事件接线.md` 那份判据修复）**worktree 里看不见**。
2026-09-10 已经因为这个白跑过一轮（旧版自检 2 用 `git log --oneline -3` 找前置 commit，隔了几小时就被挤出前 3，判据假阴性、结论还是反的）。

1. **核验两处残留泳道标注已摘**（⚠️ 改写动作 Cowork 侧已于 2026-09-13 做完，本步**只验收，⛔ 不要再改**）：
   `grep -c '^> 泳道：' docs/openers/OP-0820-全量编排.md` 必须 ＝ **0**。
   不为 0 ⇒ **停下报**（说明主工作区被别的 session 改回去了），⛔ 不要自己动手补。
   *为什么要有这一步*：`run-lanes.sh` 靠 `^> 泳道：` 抓待执行条目，两处旧标注（第十三批 `0909B`、第十六批 `0909AK`）
   都已跑完，留着的话将来任何一次不带 `--only` 的发车会把它们**重跑一遍**。改写形状固定为 `> ✅ 已完成 … · 原泳道 …`
   （正则要求 `>` 后紧跟「泳道：」才抓得到，所以改成这个形状就抓不到了；本文件 876–878 行写明了这条约定）。

2. 只 `git add` 这五条，⛔ 不 `-A`／不 `.`／不 `-a`／不 `stash`（含 `-u`）：
   ```
   docs/openers/0913A-波次0看护-SDK接线收口.md      ← 新（本文件自己）
   docs/openers/0910B-SDK-message事件接线.md        ← 改（判据订正，🔴 就是它必须先进 git）
   docs/openers/OP-0820-全量编排.md                 ← 改（号池 0911A 改号与 0913A 登记 ＋ 上面两处摘标注）
   docs/session接力.md                              ← 改
   .claude/skills/kickoff/SKILL.md                  ← 改（「判据不许写成会随时间漂移的形状」真源）
   ```
3. 提交前自检四条，任一不过就停下报：
   - `grep -c "git log --oneline -3" docs/openers/0910B-SDK-message事件接线.md` ＝ **1**（只剩订正段里把它当反例引用的那句）
   - `grep -c "会随时间漂移" .claude/skills/kickoff/SKILL.md` ≥ **1**
   - `grep -c '^> 泳道：' docs/openers/OP-0820-全量编排.md` ＝ **0**
   - `git status --short` 里除这五条外只剩 `?? _to_delete/`
4. commit → `git push origin main`（被拒 ⇒ `git pull --rebase --autostash origin main` 后重试，最多 3 次；⛔ **不要在 commit 前 pull**）。
   **记下 commit hash，后面报告要用。**

---

【三、建 worktree 做 SDK `message` 接线（原 `[Mac]0910B` 的全部内容）】

🔴 **必须在 worktree 里做**：主工作区有 `.env`、真实库 `data/liaison.db`、以及 launchd 常驻的真实值守进程；
在主检出改 `session_client.py` 会让那个跑着的服务读到半成品。

1. 建：`git worktree add .claude/worktrees/sdk-message-wiring-0913A`
   然后 `cd` 进去。`git branch --show-current` **报出实际分支名**（`git worktree add` 会按目录名自动生成），
   ⛔ 不要 `-b` 指定分支名、⛔ 不要改名。
2. 在该 worktree 内，**读 `docs/openers/0910B-SDK-message事件接线.md` 全文并逐节执行**，
   本步骤的全部指令以该文件为准。⚠️ 它的【一】第 1 条要求 `pwd` 含 `.claude/worktrees/` —— 上一步已满足。
   ⚠️ 它的【一】第 2 条已在 09-11 订正为「问 8.5bis 在不在 `tasks.md` 里」，⛔ 不要再用 `git log -3` 去找前置 commit。
   文件不存在、或其首行编号不是 `[Mac]0910B-SDK-message事件接线` ⇒ **停下报**，⛔ 不要凭标题猜任务。
3. 🔴 **该文件里所有 ⛔ 原样生效**，尤其：⛔ 不做真实建连、不等真实入站、不发任何消息、不装 launchd、不改 `.env`。
4. 该文件的【九】要求逐项报告，**照它给**（尤其那一栏：**帧字段映射走的是「实际字段」还是「fail-closed」**，必须明说是哪一种——
   这个答案决定波次 1 四份 plan 里的字段名）。

---

【四、合回 main 与回勾】回到仓库根（`cd` 回主检出）。

1. 合回：先在 worktree 里 `git pull --rebase --autostash origin main`，再回主检出合并（`--no-ff`）。
2. 🔴 **真合验证**：`git rev-list --count main..<分支>` ＝ 0；非 0 时再跑 `git cherry -v main <分支>`——
   全 `-` 是 rebase 假阳性，有 `+` 才是真没合。**确认内容真在 main 上之前，⛔ 不许报成功**。
3. pytest：`./venv/bin/python -m pytest -q 2>&1 | tail -3` **≥【一】第 4 步的基线且 0 失败**。
4. 回勾两处（只 `git add` 这两个文件）：
   - `openspec/changes/hr-wecom-aibot-liaison/tasks.md` 的 **8.5bis** 改 `- [x]`，并把顶部进度行 62/67 → 63/67
   - `openspec/changes/liaison-reply-bridge-and-patrol/tasks.md` 的 **0.2** 改 `- [x]`
     🔴 **只勾 0.2**。⛔ **0.1 与 0.3 一律不动**——它们要真实建连与真实入站，归 `[Mac]0910A`。
     勾错等于对下游宣布「门槛已过」，波次 1 会带着一条根本收不到消息的通道开工。
5. `openspec validate --strict` 通过。
6. commit → push（重试规则同【二】第 4 步）。
7. 清理：`git worktree remove .claude/worktrees/sdk-message-wiring-0913A`（合干净后再删；删不掉就留着并在报告里说明，
   ⛔ 不要 `--force`）。⛔ 不要动别的 worktree、⛔ 不 prune、⛔ 不 clean。

---

【五、守住】

- ⛔ 不重启／不 `kickstart`／不 `bootout` 值守服务（它按 `KeepAlive=true` 常驻，`pgrep` 非空是**正常**）
- ⛔ 不碰 `.env`、`tools/liaison/.env`、`whitelist.yaml`、`data/`
- ⛔ 不请汤丽萍或任何人发消息（截至今日她已白发 4 条；解锁判据是 `liaison_message` 由 0 变 1，归 `0910A`）
- ⛔ 不碰 `.51`、不发版
- ⛔ 不删 `.git` 下任何锁（孤儿锁三项判据见 `docs/findings/2026-08-26-index-lock-孤儿锁判据.md`；等 5 秒重试最多 5 次）
- ⛔ 不发车波次 1、⛔ 不跑 `run-lanes.sh`（波次 1 还差 0.1/0.3，且按接力原意手动贴四个 session）
- ⛔ 不 `git add -A` / `.` / `-a` / `stash`

---

【六、收工报告】逐项给，⛔ 不许写「均已完成」：

1. 【一】四条的实际输出（含 pytest 基线数字）
2. 【二】：`grep -c '^> 泳道：'` ＝ 0 的输出、四条自检输出、commit hash、push 后 ahead 0
3. 【三】：worktree 路径与**实际分支名**；`0910B` 正文【九】要求的全部各项，
   **其中「帧字段映射走实际字段还是 fail-closed」必须单独成行明说**
4. 【四】：`git rev-list --count` 与（若需）`git cherry -v` 的输出、pytest 前后数字、
   8.5bis 与 0.2 的回勾 diff、`openspec validate --strict` 结果、commit hash、worktree 是否已删
5. 🔴 **门槛现状三行**（照抄实际 grep 输出，⛔ 不写结论）：
   `grep -n "^- \[.\] 0\.[123]" openspec/changes/liaison-reply-bridge-and-patrol/tasks.md`

**收工单列一行**：
`⏸ 下一步：§0 只剩 0.1（TD-42 真实验证）与 0.3（真实入站一条），两条都归 [Mac]0910A —— 主工作区、worktree ❌、Shao Peishen 在场，引用块在 docs/session接力.md 顶部。0910A 报出 liaison_message 由 0 变 1 之后，波次 1 四条 plan 泳道才解锁（手动贴四个 CC session，⛔ 不走 run-lanes）`
