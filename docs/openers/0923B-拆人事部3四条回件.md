# [Mac]0923B 按章程拆 `人事部#3` 四条 pending 回件

## 零、为什么

`data/liaison/unpack-signal.json` 现有 **4 条 pending**，`letter_number` 均为 `人事部#3`：
09-20 17:04:09、17:04:39，09-21 11:08:39、11:08:40。`0921C` 拆前两条时判「待人」后停，
新两条一直没人拆，`README-跟进信清单.md` 里 `人事部#3` 仍停在第九态「📨 回件已到，待拆件」。

已知内容概貌（Cowork 侧只读所见，供判「实质/非实质」参考，⛔ 不替代你自己按章程读归档件）：
两条是决策点 a／b 的答复，一条是「决策点a 答复中的清单」标题（**清单实体是群里的 .doc 附件，未进系统**），
一条为 **0 字节空消息**。

## 一、开工自核（任一不过即顶格 `OPENER_PARTIAL` 并停）

1. 🔴 **第一件事**：用 `Read` 打开 `.claude/skills/liaison-unpack/SKILL.md` **全文**，
   它是本会话**唯一**行为边界正本，逐条遵守。⛔ 本 opener 不复述其条文；两者有出入**以章程为准**。
2. `PYTHONPATH=. tools/liaison/.venv/bin/python -m tools.liaison unpack-signal --probe` 输出 `[SIGNAL]`。

## 二、动作

按章程 §二 逐条处理 **4 条** pending，再按 §一.5 清信号、§三 收口。逐条提示：

- **0 字节空消息** ⇒ 按章程判「非实质回件」，还原原状态，⛔ 不当成答复。
- **「决策点a 答复中的清单」但无清单实体** ⇒ 这是**实体缺失**，不是内容歧义：
  按章程登记「待人」，判据写明「等汤丽萍私信重发 `.doc`，或群附件通道修好后补收」。
- **决策点 b「选择：1」** ⇒ 若章程判为实质回件，照章程回灌并转闭环态；缺的时数／人名登记为待补。
- `人事部#3` 正题（A/B/C/D 四场景与路线图回收单）**一条都没回** ⇒ 这一事实必须写进回灌结论，
  否则下一个会话会以为 `#3` 已闭环。

🔴 命中章程 §四（含 §〇⑨ 个人信息）⇒ 按章程登记「待人」并顶格
`OPENER_PARTIAL: 转待人 <原因>`，⛔ 不自行下裁决。

## 三、机器判据

```bash
set -e
cd "$(git rev-parse --show-toplevel)"
grep -q '人事部#3' docs/session接力.md
ls docs/跟进信/回件/ | grep -q '人事部#3' || grep -q '待人' docs/session接力.md
PYTHONPATH=. tools/liaison/.venv/bin/python -c "import json;d=json.load(open('data/liaison/unpack-signal.json'));assert len(d.get('pending',[]))<4, '信号未减少'"
```

## 四、并发协议

泳道「回件通道」内串行在 `0923A` 之后（两条都写 `docs/session接力.md`）。

## 五、红线

以章程 §〇 九项为准（⛔ 本文件不复制其条文）。特别提示：**⛔ 不对外发送**、**⛔ 不 `git push`**（章程⑦）、
**⛔ 不修改 `.claude/skills/` 与 `CLAUDE.md`**、**⛔ 不改 `tools/` 下任何文件**。

## 六、收口

章程 §三 做完且三条机器判据全过 ⇒ **顶格** `OPENER_DONE`；转待人或判据不过 ⇒ 顶格 `OPENER_PARTIAL: <原因>`。
