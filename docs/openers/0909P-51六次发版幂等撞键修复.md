[Mac]0909P-51六次发版幂等撞键修复
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（在仓库根跑 sync-to-server.sh，改文档提交 main）｜ 工作区: 仓库根 ｜ 派发: Cowork·HR业务线-接力0903B

> 本文件是 `[Mac]0909P` 的正文（引用式 opener）。🔴 `.51` 发版是不可代项：**依据＝Shao Peishen 对本条回「发」**。
> 本文件 09-08 晚写就时他**尚未**回这个字——⛔ 没有这句授权一步都不要跑；跑之前先在母 session 记录里核到它。
> 与第十四批泳道（只改 `tools/liaison/`，永不同步到 `.51`）零重叠，可同时进行。

【零、这次发什么】
`git diff --stat 95b298c..main -- app/ scripts/ requirements.txt pyproject.toml .env.example deploy-server.ps1` 现算，写就时是：
- `app/storage/idempotency.py`（`8ea7bd2` + `2853116`）：🔴 **唯一键冲突翻译成幂等短路而非 500**——重复 `confirm` 时 `idempotent_effect` 先 rollback 本次业务写、确认键已在库里，按"已执行"返回；现网 2026-09-08 13:18 实发过一次 500（`0908K` 查实的遗留缺陷）
- `app/outbound/delivery.py`（`878aad8`，TD-17）：docstring 非法转义改 raw string，行为零变化
- `.env.example`：多 `LLM_FALLBACK_*` 四行变量名（只名字，无值）；`pyproject.toml`：`testpaths` 接入 `tools/liaison/tests`（无依赖变更）；`scripts/install_lane_launcher.py`：Mac launchd 安装脚本，在 Windows 上是惰性文件
⚠️ **纯 sync，⛔ 无装依赖步骤**——`requirements.txt` 自 `0908J` 起未再改动，⛔ 不重跑 `deploy-server.ps1`。
现算结果多出上面没列的 `app/` 文件 → 停下报 Shao Peishen，⛔ 不把没核过的改动带上线（`0908K` 的教训：实际范围远大于 opener 所述）。

【一、前置核对（任一不过即停）】
1. `git status -sb`：main 与 origin 同步或仅 ahead；`git log -1 --format=%h main` 记下
2. `ssh zp51 "echo ok"` 回 ok；不通 → 登记「⏸ 留步：不在内网」停
3. 现网健康基线：`curl -s -o /dev/null -w "%{http_code}\n" --max-time 15 http://192.168.100.51:8095/hr/recruit-agent/`
   不是 200 → 停下报 Shao Peishen，⛔ 不在已经不健康的现网上叠发版
4. `git diff --stat 95b298c..main -- requirements.txt` 必须为空（非空 → 「纯 sync」前提不成立 → 停，改走 `0908J` 那套先装依赖的顺序）
5. `./venv/bin/python -m pytest -q tests/test_transaction_ownership.py tests/test_outbound_effects.py 2>&1 | tail -2` 本机绿（python 不在 PATH）

【二、快照 → 发版 → 冒烟】
1. 快照（在 .51 上跑）：
   ssh zp51 "powershell -NoProfile -Command \"\$ts=Get-Date -Format yyyyMMdd-HHmm; \$b='C:\apps\backups\'+\$ts; New-Item -ItemType Directory -Force \$b | Out-Null; Copy-Item 'C:\apps\zhuopin-recruit-agent\app' \$b\app -Recurse; Write-Output \$b\""
   （只快照 `app\`；`data\` 上次因 `demo.db-shm` 被占用中断，回滚只用 `app\`，⛔ 不为 `data\` 停服）
2. 发版命令**由 Shao Peishen 点 Run**（Auto Mode 分类器拦 AI 直接调 `sync-to-server.sh`；人点一下本身就是发版授权留痕）：
   ```bash
   ./sync-to-server.sh
   ```
   末尾必须 HTTP 200 + 「发版完成」。
3. 冒烟 4 项（逐项记原文，任一不过即走 §二.4）：
   ① `curl` 首页 → 200
   ② `curl .../api/jobs` → JSON 列表（⚠️ 405 ＝ 没发上去，⛔ 不要当通过）
   ③ `logs\app.log` 尾 60 行无本次进程启动后的 `Traceback` / `OperationalError`，且有新进程的 `Application startup complete`（⚠️ 路径是 `logs\app.log`，不是 `data\logs\`）
   ④ 🔴 **本次发版的目标验收——修复真的到了现网**：
      ssh zp51 "powershell -NoProfile -Command \"cd C:\apps\zhuopin-recruit-agent; Select-String -Path app\storage\idempotency.py -Pattern 'short-circuiting duplicate effect_key' | Measure-Object | Select-Object -ExpandProperty Count\""
      期望 `1`（或更大）；`0` ＝ 文件没同步过去，走 §二.4
   ⑤ 顺带回归：不带 `PYTHONIOENCODING` 裸跑巡检必须仍 `EXIT=0`：
      ssh zp51 "powershell -NoProfile -Command \"cd C:\apps\zhuopin-recruit-agent; .venv\Scripts\python.exe -m app.audit.assertions --db data\demo.db --mirror data\audit\decisions.jsonl; Write-Output ('EXIT=' + \$LASTEXITCODE)\""
4. 任一不过 → 回滚：`Remove-Item app -Recurse -Force` → `Copy-Item <本次快照>\app` 回 → `schtasks /end` + `/run` → `curl` 200 → 写 `docs/findings/2026-09-09-51六次发版回滚.md`，停。⛔ `data\` 不回滚

【三、落档与提交（只 add 列出的路径）】
1. `docs/audit-and-outbound-ops.md` §五新增「2026-09-09 六次发版记录」：commit / 快照 / 冒烟 ①–⑤ 原文 / 「重复 confirm 500」缺陷标已修上线。⛔ 不覆盖前五次记录
2. `docs/session接力.md`【一】`.51` 代码行改「已发版 <commit>」，把那句「尚未上 `.51`」删掉
3. `docs/openers/OP-0820-全量编排.md` 号池 `0909P` 行补结果
4. git add docs/audit-and-outbound-ops.md docs/session接力.md docs/openers/OP-0820-全量编排.md
   ⛔ 禁止 `-A` / `.` / `-a`；第十四批泳道在改 `tools/liaison/`，别人的改动不停、不问、不顺手提交
5. git commit -m "ops(.51): 六次发版 <commit>（幂等撞键短路修复上线）"；git push origin main
6. push 被拒 → `git pull --rebase --autostash origin main` 后重试最多 3 次，⛔ 不在 commit 前 pull；index.lock 等 5 秒重试 5 次不删

【四、汇报】发版 commit / 快照目录 / 冒烟 ①–⑤ 逐项原文 / commit hash / ahead
