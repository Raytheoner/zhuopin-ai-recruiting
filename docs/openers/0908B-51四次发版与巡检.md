[Mac]0908B-51四次发版与巡检
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（在仓库根跑 sync-to-server.sh，改文档提交 main）｜ 工作区: 仓库根

> 本文件是 `[Mac]0908B` 的正文（引用式 opener）。🔴 `.51` 发版是不可代项：依据＝Shao Peishen 2026-09-08 在 Cowork 会话 HR业务线-接力0903B 回「发」。
> 没有这句授权 ⛔ 一步都不要跑。

【零、这次发什么】
现网 `3f59842`（09-04 08:37）之后 main 又合入了：第 6 章确认断点（含 6.1 现网缺陷修复：确认页终于渲染画像本身）、第 7 章 JD 溯源/标识保护/一键复制、9.6 断言四豁免线修复、硬门槛规则草案（新表 hard_requirement）、Web 列表/详情/转人工队列。
requirements.txt / pyproject.toml / .env.example 自 `3f59842` 起无改动（Cowork 侧 09-08 核过）⇒ 只 sync，⛔ 不重跑 deploy-server.ps1。新表走 CREATE TABLE IF NOT EXISTS，启动自建。

【一、前置核对（任一不过即停）】
1. git status -sb：main 与 origin 同步或仅 ahead；记下 git log -1 --format=%h main（发版的就是它）
2. ⚠️ 若 git status 里有 `openspec/changes/hr-wecom-aibot-liaison/` 未跟踪——那是另一条线（0908A）的在途产物，⛔ 不碰、不 add、不问
3. ssh zp51 "echo ok" 回 ok；不通 → 整条登记「⏸ 留步：不在内网」停
4. git diff --stat 3f59842..main -- requirements.txt pyproject.toml 必须为空
5. ssh zp51 "powershell -NoProfile -Command \"Test-Path 'C:\apps\zhuopin-recruit-agent\.venv\Scripts\python.exe'\"" 必须 True

【二、快照 → 发版 → 冒烟（与 0903L/0904Z 同一套，逐字）】
1. 快照（在 .51 上）：
   ssh zp51 "powershell -NoProfile -Command \"\$ts=Get-Date -Format yyyyMMdd-HHmm; \$b='C:\apps\backups\'+\$ts; New-Item -ItemType Directory -Force \$b | Out-Null; Copy-Item 'C:\apps\zhuopin-recruit-agent\app' \$b\app -Recurse; Copy-Item 'C:\apps\zhuopin-recruit-agent\data' \$b\data -Recurse; Write-Output \$b\""
   引号嵌套报错 → 改 -EncodedCommand
2. 🔴 发版命令**由 Shao Peishen 点 Run**（09-04 实证：Auto Mode 分类器拦 AI 直接调 sync-to-server.sh，白名单无效）。把下面原样作为 bash 块发给他，等他回结果：
   ```bash
   cd /Users/paulshao/Projects/HumanResource && ./sync-to-server.sh
   ```
   末尾必须 HTTP 200 + 「发版完成」
3. 冒烟：curl 首页 200；相对资源逐个 curl 无 404；app.log 尾 60 行无 Traceback/OperationalError；
   curl -s http://192.168.100.51:8095/hr/recruit-agent/api/jobs 应返回 JSON 列表（8.1 新端点，本次发版的目标之一）；
   ssh zp51 查 data\demo.db 里 hard_requirement 与 human_review 两张表存在（sqlite_master）
4. 任一不过 → 回滚：Remove-Item app -Recurse -Force → Copy-Item <快照>\app 回去 → schtasks /end + /run → curl 200；写 docs/findings/2026-09-08-51四次发版回滚.md，停。⛔ data\ 不回滚

【三、巡检 CLI（9.6 上线后的预期变化，⛔ 不是失败）】
ssh zp51 "powershell -NoProfile -Command \"cd C:\apps\zhuopin-recruit-agent; .venv\Scripts\python.exe -m app.audit.assertions --db data\demo.db --mirror data\audit\decisions.jsonl; Write-Output ('EXIT=' + \$LASTEXITCODE)\""
- 镜像不存在 → EXIT=2，同 09-03，原样记
- 🔴 EXIT=1 且违例来自断言四 → **这是 0904I 预告的预期结果**：留痕上线前就已确认/放弃、既无 effect_log 也无 human_review 的历史行，9.6 把它们从"被豁免"翻成 fail-closed 违例。正确处置是**人工逐条核实**，⛔ 不改数据、⛔ 不把豁免线往后挪。
  把违例行（job_id / version / status / created_at）逐条抄进 docs/audit-and-outbound-ops.md §五新增「2026-09-08 断言四历史行清单（待 Shao Peishen 核实）」，只读取证：
  ssh zp51 "powershell -NoProfile -Command \"cd C:\apps\zhuopin-recruit-agent; .venv\Scripts\python.exe -c \\\"import sqlite3,json; c=sqlite3.connect('data/demo.db'); print(json.dumps(c.execute('select p.job_id,p.version,p.status,p.created_at from job_profile p where p.status in (\\\\\\\"approved\\\\\\\",\\\\\\\"abandoned\\\\\\\") and not exists (select 1 from human_review h where h.profile_version=p.version and h.job_id=p.job_id)').fetchall(),ensure_ascii=False))\\\"\""
  （列名以 app/storage/db.py 的 human_review 建表为准，先 grep 再跑；引号层数多，直接用 -EncodedCommand）

【四、落档与提交（只 add 列出的路径）】
1. docs/audit-and-outbound-ops.md §五：「2026-09-08 四次发版记录」（commit / 快照 / 冒烟 5 项 / 巡检 EXIT 与原文 / 历史行清单）
2. docs/session接力.md【一】.51 代码行改「已发版 <commit>」；docs/openers/OP-0820-全量编排.md 号池 0908B 行补结果
3. git add docs/audit-and-outbound-ops.md docs/session接力.md docs/openers/OP-0820-全量编排.md docs/openers/0908B-51四次发版与巡检.md docs/openers/0908I-intake定时三条移出C类.md
   （0908I 文件是 Cowork 侧同批写的 opener 正文，未在看护者 0908Z 的提交清单里，本条顺手带上；若已被别人提交则跳过）
   ⛔ 禁止 -A / . / -a；泳道在并行改代码与 intake tasks.md，别人的改动不停不问；接力/编排文件若混有别人的块，git add -p 只拿自己的
4. git commit -m "ops(.51): 四次发版 <commit>（第 6/7 章 + 9.6 + 硬门槛 + Web）＋ 巡检首见断言四历史行"；git push origin main

【五、汇报】发版 commit / 快照目录 / 冒烟 5 项 / 巡检 EXIT 与历史行条数 / commit hash / ahead
