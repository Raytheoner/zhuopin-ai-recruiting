[Mac]0908Z-泳道批次看护
【设置】执行环境: CC Desktop ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（你是调度者，实际干活的是脚本起的独立 session）｜ 工作区: 仓库根

> 本文件是 `[Mac]0908Z` 的正文（引用式 opener）。第十一批看护者。文件随任务一起提交，即留痕。

【一、你的角色】
你不亲自执行任何 opener。做四件事：前置自检、**把启动命令交给 Shao Peishen 点 Run**、轮询盯守、跑完真身核验并出报告。
⛔ 不改代码、不跑 spec-to-plan / run-build / openspec-archive-change。本批不发版（发版走 `0908B`，另一条 session）。
⚠️ 仓库里可能有 `openspec/changes/hr-wecom-aibot-liaison/`（未跟踪或已提交）——那是另一条线（`0908A`）的产物，⛔ 不碰、不 add、不问、不写进报告当红灯。

【二、先提交 Cowork 侧的编排落档】
git status --short 预期：docs/session接力.md、docs/openers/OP-0820-全量编排.md、docs/openers/0908Z-泳道批次看护.md、docs/openers/0908B-51四次发版与巡检.md（外加可能的 hr-wecom-aibot-liaison/，不碰）。
只 add 这四条路径，⛔ 禁止 git add -A / git add . / commit -a：
  git add docs/session接力.md docs/openers/OP-0820-全量编排.md docs/openers/0908Z-泳道批次看护.md docs/openers/0908B-51四次发版与巡检.md
  git commit -m "docs(openers): 第十一批编排（网关 ∥ 需求识别 ∥ 幂等专项测试）＋ 0904M 手工摘标注 ＋ 0908B 发版 opener"
  git push origin main

【三、前置自检】任一不过就停下报告，不要硬上：
1. command -v claude；ls docs/openers/run-lanes.sh
2. bash docs/openers/run-lanes.sh --dry-run
   本批（第十一批）预期：**3 条泳道、6 条 opener**：
     ◆ 网关
         ✓ 0908C 正文 21 行   网关双供应商与重试转人工计划(2.3与2.5)
         ✓ 0908D 正文 22 行   网关双供应商与重试转人工开工
     ◆ 识别
         ✓ 0908E 正文 19 行   需求识别计划(5.3)
         ✓ 0908F 正文 21 行   需求识别开工(5.3)
     ◆ 幂等
         ✓ 0908G 正文 20 行   幂等专项测试计划(4.4)
         ✓ 0908H 正文 21 行   幂等专项测试开工(4.4)
   ⚠️ 若多出一条 `Web 0904M` → 说明 Cowork 侧手工摘的标注没保存，停下报告，⛔ 不 --only 绕
   数字对不上 / 任一 ✗ / exit 13 → 停下报告，⛔ 不改编排文件
3. git status --short 记基线（【二】后应只剩 hr-wecom-aibot-liaison/ 或干净）；git status -sb 记 ahead（应 0）
4. 🔴 pytest 基线：./venv/bin/python -m pytest -q 2>&1 | tail -3（python 不在 PATH；⛔ 不抄——09-07 收口时约 1100+，那是别人的观测）。有失败项 → 停下报告
5. ls docs/superpowers/plans/ | grep -c "gateway-fallback\|intent-recognition\|idempotency-suite" 应为 0
6. grep -c '^- \[x\]' openspec/changes/m1-job-profile-intake/tasks.md 记下（09-08 上午是 56）
7. 看一眼 /usage：三条 run-build 并行；余量不够就停下报告

【四、启动——交给 Shao Peishen 点 Run，⛔ 你不要自己调 Bash 起脚本】
（Auto Mode 分类器在白名单齐全时仍拦 `nohup run-lanes.sh`，09-04 两批实证；看护者自己重试无效。）
先 `rm -f /tmp/run-lanes-boot.log`（上一批的 stale 日志会误判已启动），然后把下面这段**原样**作为 bash 代码块发给他，请他点 Run：

```bash
cd /Users/paulshao/Projects/HumanResource && nohup bash docs/openers/run-lanes.sh --full-auto --yes > /tmp/run-lanes-boot.log 2>&1 & echo "PID=$!"
```

等他回 PID。从 /tmp/run-lanes-boot.log 头几行读日志目录（只认半角冒号那行）。本批不加 --chain，估 2–3 小时。

【五、轮询盯守】每 3-5 分钟：
  LOGDIR=<日志目录>; cat "$LOGDIR/results.tsv" 2>/dev/null; ls -la "$LOGDIR"/*.log; ps -p <PID> >/dev/null && echo RUNNING || echo EXITED
每轮播报一行。预案（不要问我）：
- PARTIAL → 正常，找「⏸ 留步」记原因继续盯。已知合法留步：H 的「某节点真不幂等，红灯登记未修」
- FAIL / NO-SENTINEL → 该泳道停、别的不受影响。读末尾 50 行；`API Error: 529` → 查 status.claude.com，解除后把
  `cd /Users/paulshao/Projects/HumanResource && nohup bash docs/openers/run-lanes.sh --full-auto --yes --only <剩余编号逗号分隔> > /tmp/run-lanes-boot.log 2>&1 & echo "PID=$!"`
  作为 bash 块发给 Shao Peishen 点 Run，新日志目录另记
- `Budget limit reached ($25)` 中断在终审/合并前 → **不要**重开该条；报告里写明 worktree 名与未合 commit 数，下一步推荐「利旧 worktree 续跑收口」（0905A 就是这么做的）
- NO-BODY → 停止盯守，`cat "$LOGDIR/manifest.tsv" | od -c | head` 贴给我
- 「调不到 superpowers」→ 已知，按预案手工走；记下各条是停了还是走完了
- 超过 4 小时仍未收敛 → 不 kill，记录现状出报告

【六、跑完的真身核验 —— 逐条跑，不许跳】
  cd /Users/paulshao/Projects/HumanResource && git pull --rebase origin main
1. git log --oneline -40 对照 results.tsv（C/E/G：各 1 个 plan；D/F/H：各 合并 + 回勾）
2. git status --short 与基线比；git status -sb ahead 应 0（> 0 补推）
3. git worktree list；逐分支真未合：
   for b in $(git branch --format='%(refname:short)' | grep -v '^main$'); do echo "$b rev-list=$(git rev-list --count main..$b) 真未合=$(git cherry -v main $b | grep -c '^+')"; done
   （三个历史遗留分支 intake-unit6-approval / unit7-jd-grounding-export / td9-… 真未合应仍为 0，不属本批）
4. ls docs/superpowers/plans/ 应恰好多出 gateway-fallback / intent-recognition / idempotency-suite 三个，`### Task` 数 ≥ 3 / ≥ 2 / ≥ 2
5. checkbox：grep -c '^- \[x\]' openspec/changes/m1-job-profile-intake/tasks.md —— 满额＝【三】6 基数 + 2（2.3/2.5）+ 1（5.3）+ 1（4.4）；按各条分别判；核 `<<<<<<<` 计数为 0
6. pytest 与【三】4 基线比：≥ 开跑前且 0 失败；🔴 单测必须离线——若变慢且日志有真实 LLM 调用，红灯单列
7. git status 里不得出现 data/ 下任何文件
8. ⛔ 本批不做 worktree 清理

【七、出看护报告】写成 .claude/handoff/lanes-<时间戳>-看护报告.md（gitignore 内，不提交），含：
  - 编号 / 脚本判定 / 真身核验结论 / 分歧
  - PARTIAL 留步逐条；红灯清单（Task 数不足、真未合 ≠ 0、pytest 失败、tasks.md 冲突标记、data/ 进 git、ahead ≠ 0、H 报的不幂等节点）
  - superpowers 是否真调到（六条各一行）
  - 下一步：**一条推荐动作 + 理由**（同结果只写一种；只有结果不同且属 Shao Peishen 必须拍的才列选项并标推荐）

【八、收工】把报告路径和那张表贴给我。
