[Mac]0904Y-泳道批次看护
【设置】执行环境: CC Desktop ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（你是调度者，实际干活的是脚本起的独立 session）｜ 工作区: 仓库根

> 本文件是 `[Mac]0904Y` 的正文（引用式 opener）。第十批看护者，`Z` 已被第九批用掉、顺延 `Y`。文件随任务一起提交，即留痕。

【一、你的角色】
你不亲自执行任何 opener。做四件事：前置自检、**把启动命令交给 Shao Peishen 点 Run**、轮询盯守、跑完真身核验并出报告。
⛔ 不改代码、不跑 spec-to-plan / run-build / openspec-archive-change。本批不发版。

【二、先提交 Cowork 侧的编排落档】
git status --short 预期：docs/session接力.md、docs/openers/OP-0820-全量编排.md、docs/openers/0904Y-泳道批次看护.md。
只 add 这三条路径，⛔ 禁止 git add -A / git add . / commit -a：
  git add docs/session接力.md docs/openers/OP-0820-全量编排.md docs/openers/0904Y-泳道批次看护.md
  git commit -m "docs(openers): 第十批编排（账目对齐 → 9.6 ∥ 硬门槛 ∥ Web 列表详情队列，三泳道并行）"
  git push origin main
有别的改动 → 单列出来，不提交、不问、继续。

【三、前置自检】任一不过就停下报告，不要硬上：
1. command -v claude；ls docs/openers/run-lanes.sh
2. bash docs/openers/run-lanes.sh --dry-run
   本批（第十批）预期：**3 条泳道、7 条 opener**：
     ◆ 断言
         ✓ 0904G 正文 16 行   intake账目对齐(1.3与2.6与1.6b与8.3)
         ✓ 0904H 正文 20 行   断言四豁免线修复计划(9.6)
         ✓ 0904I 正文 21 行   断言四豁免线修复开工(9.6)
     ◆ 硬门槛
         ✓ 0904J 正文 20 行   硬门槛规则提取计划(1.2b与5.8与5.9)
         ✓ 0904K 正文 21 行   硬门槛规则提取开工
     ◆ Web
         ✓ 0904L 正文 19 行   Web列表详情与转人工队列计划(8.1与8.2与8.4)
         ✓ 0904M 正文 23 行   Web列表详情与转人工队列开工
   泳道内串行、泳道间并行（并行上限 3，错峰 90 s）。数字对不上 / 任一 ✗ / exit 13 → 停下报告，⛔ 不改编排文件、不 --only 绕
3. git status --short 记基线（【二】后应干净）；git status -sb 记 ahead（应 0）
4. 🔴 pytest 基线：./venv/bin/python -m pytest -q 2>&1 | tail -3（python 不在 PATH；⛔ 不抄——09-04 中午实测 1008，那是别人的观测）。有失败项 → 停下报告
5. ls docs/superpowers/plans/ | grep -c "unit9-6\|hard-requirement\|unit8-web" 应为 0
6. grep -c '^- \[x\]' openspec/changes/m1-job-profile-intake/tasks.md 记下（09-04 中午是 45；G 会回勾最多 4 条）
7. 看一眼 /usage：三条 run-build 并行，用量是单条的三倍；窗口余量不够就停下报告，⛔ 不硬发

【四、启动——交给 Shao Peishen 点 Run，⛔ 你不要自己调 Bash 起脚本】
09-04 上午实证（`0904Z` 报告 §五）：Auto Mode 分类器在白名单齐全时仍拦 `nohup run-lanes.sh`，看护者自己重试无效，⛔ 不要空耗轮次。
唯一验证有效的路径：把下面这段**原样**作为 bash 代码块发给他，请他在对话里点 Run 按钮：

```bash
cd /Users/paulshao/Projects/HumanResource && nohup bash docs/openers/run-lanes.sh --full-auto --yes > /tmp/run-lanes-boot.log 2>&1 & echo "PID=$!"
```

发出去后就等他回 PID。⚠️ 先 `rm -f /tmp/run-lanes-boot.log`（上一批的 stale 日志会让你误判已启动——09-04 上午踩过）。
拿到 PID 后从 /tmp/run-lanes-boot.log 头几行读日志目录（形如 .claude/handoff/lanes-<时间戳>；⚠️ 解析时只认半角冒号，全角冒号那行是描述不是路径——09-04 上午踩过）。
本批不加 --chain。三泳道并行，估 2–3 小时（最长的一条决定）。

【五、轮询盯守】每 3-5 分钟：
  LOGDIR=<日志目录>; cat "$LOGDIR/results.tsv" 2>/dev/null; ls -la "$LOGDIR"/*.log; ps -p <PID> >/dev/null && echo RUNNING || echo EXITED
每轮播报一行。预案（不要问我）：
- PARTIAL → 正常，找「⏸ 留步」记原因继续盯。已知合法留步：G 的「某条证据不足未勾」；M 的「浏览器交互未记作手工验证」
- FAIL / NO-SENTINEL → 该泳道停、别的泳道不受影响。读末尾 50 行判真失败还是哨兵漏输出。日志是 `API Error: 529 Overloaded` → 查 status.claude.com，事故解除后把重跑命令
  `cd /Users/paulshao/Projects/HumanResource && nohup bash docs/openers/run-lanes.sh --full-auto --yes --only <剩余编号逗号分隔> > /tmp/run-lanes-boot.log 2>&1 & echo "PID=$!"`
  作为 bash 块发给 Shao Peishen 点 Run（同【四】），新日志目录另记
- NO-BODY → 停止盯守，`cat "$LOGDIR/manifest.tsv" | od -c | head` 贴给我
- 「调不到 superpowers」→ 已知（无头取不到插件），按预案手工走；记下各条是停了还是走完了
- 某条报「预算耗尽（$25）终审未跑」→ 09-04 上午 C/E 都撞过，属预期内 PARTIAL，记下"缺全分支终审"，⛔ 不改预算重跑
- 超过 4 小时仍未收敛 → 不 kill，记录现状出报告

【六、跑完的真身核验 —— 逐条跑，不许跳】
  cd /Users/paulshao/Projects/HumanResource && git pull --rebase origin main
1. git log --oneline -40 对照 results.tsv（G：1 个回勾 commit；H/J/L：各 1 个 plan；I/K/M：各 合并 + 回勾）
2. git status --short 与基线比；git status -sb ahead 应 0（> 0 补推）
3. git worktree list；逐分支真未合：
   for b in $(git branch --format='%(refname:short)' | grep -v '^main$'); do echo "$b rev-list=$(git rev-list --count main..$b) 真未合=$(git cherry -v main $b | grep -c '^+')"; done
4. ls docs/superpowers/plans/ 应恰好多出 unit9-6 / hard-requirement / unit8-web 三个，`### Task` 数 ≥ 2 / ≥ 3 / ≥ 3
5. checkbox：grep -c '^- \[x\]' openspec/changes/m1-job-profile-intake/tasks.md —— 满额＝【三】6 的基数 + G 的实际勾数 + 1（9.6）+ 3（1.2b/5.8/5.9）+ 3（8.1/8.2/8.4）；按各条 OK/PARTIAL 分别判，⛔ 不一律按满分
   🔴 三条泳道都回勾同一份 tasks.md：核一眼文件里没有 `<<<<<<<` 冲突标记，且三章的勾都在（合并时若哪条把别人的勾覆盖掉了，这里能看出来）
6. pytest 与【三】4 基线比：≥ 开跑前且 0 失败
7. git status 里不得出现 data/ 下任何文件；ls data/ 记一下 I/K/M 拷回来的产物
8. ⛔ 本批不做 worktree 清理

【七、出看护报告】写成 .claude/handoff/lanes-<时间戳>-看护报告.md（gitignore 内，不提交），含：
  - 编号 / 脚本判定 / 真身核验结论 / 分歧
  - PARTIAL 留步原因逐条；红灯清单（Task 数不足、真未合 ≠ 0、pytest 失败、tasks.md 冲突标记、data/ 进 git、ahead ≠ 0）
  - superpowers 是否真调到（七条各一行）
  - 下一步：**一条推荐动作 + 理由**（同结果只写一种；只有结果不同且属 Shao Peishen 必须拍的才列选项并标推荐）

【八、收工】把报告路径和那张表贴给我。
