[Mac]0910A-TD42真实验证与AT1b真实入站
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（要用主工作区的 `.env`、`tools/liaison/.venv` 与真实库 `data/liaison.db`，还要重启真实值守服务——worktree 里三样都没有）｜ 工作区: 仓库根（/Users/paulshao/Projects/HumanResource）｜ 派发: Cowork·HR业务线-接力0909AU

> 本文件是 `[Mac]0910A` 的正文（引用式 opener）。**一次重启把两件事一起验完**：
> ① **AT-1b**——真实企微消息落进 `liaison_message` 一条（`docs/session接力.md` AT-1b）；
> ② **TD-42 真实验证**——假死 ⇒ 进程自行终止 ⇒ launchd 拉起（`docs/tech-debt.md` TD-42 末段，
> 亦即接力 P-3；⛔ P-3 不是另一件事，跑完本条即销）。
> 🔴 **⛔ 不重启两次**：②的断网会把①刚建好的连接打断，顺序钉死 ①→②。
>
> 🔴 **原 `0910A` 正文（「解锁追平与 TD-42 真实验证」）已作废**，正文挪进
> `_to_delete/0910旧稿-20260910/`：其【一】删 `.git` 孤儿锁已交 `[Mac]0910F`，
> 其【二】主检出追平 `origin/main` **已经追平**（2026-09-10 13:4x 核：`main` 与 `origin/main`
> ahead/behind 均为 0）。⛔ 不要再按那份跑。
>
> 🔴 **全程只在 Shao Peishen 在场时跑**：③会断 Wi-Fi 约 9 分钟，且重启真实服务与请人发消息属不可代项。

【为什么现在能跑】2026-09-10 13:4x 实测：值守进程自 09:38:26 起 `state=connected`、
`stamp_at` 每 30 秒推进，但 `liveness.json` **没有 `last_event_at` 键** ⇒ 跑的是 `20a2848`
（TD-42 修复）**之前**的旧进程，新判据根本没上场；同时 `liaison_message` **＝ 0 行**。
⇒ 必须重启，才谈得上验。

---

【一、开工前门槛——三条，任一不过就停下报，⛔ 不许绕过】

1. `pwd` ＝ 仓库根、`git branch --show-current` ＝ `main`（⛔ 不在 worktree 里）；
   `ls .git/*.lock 2>/dev/null || echo NO-LOCK` 预期 `NO-LOCK`。
   还有锁 ⇒ **停下报**，说明 `[Mac]0910F` 没跑或没跑完，⛔ 本条不自己删锁。
2. 🔴 `grep -n "SUBSCRIBED_EVENTS = " tools/liaison/session_client.py` **必须含 `message` 事件**
   （`0910B` 接线后应是五个）。只有四个 ⇒ **停下报「`[Mac]0910B` 还没合回 main」**，
   ⛔ 不要自己接线（那是 `0910B` 的范围，重复做会撞车）。
3. `grep -n "8.5bis" openspec/changes/hr-wecom-aibot-liaison/tasks.md` 那条已是 `- [x]`。
   仍是 `- [ ]` ⇒ 同上，停下报。

---

【二、重启值守服务，先看新判据在不在推进（约 3 分钟）】

1. `launchctl kickstart -k gui/$UID/com.zhuopin.hr.liaison`
   `launchctl print gui/$UID/com.zhuopin.hr.liaison | grep -E "state|pid|last exit code"`
   → 预期 `state = running`、**pid 与重启前不同**。
   ⚠️ `launchd.err.log` 开头那两条 ERROR 是**已知冷启动噪声**（他答 `3b` 明确不修），⛔ 不要当故障查。
2. 等 90 秒后连看两次（间隔 35 秒）：`cat data/liaison/liveness.json; echo`
   预期：`state=connected` ＋ **多出 `last_event_at` 键** ＋ 两次之间 `last_event_at` **前进了**。
   - 没有 `last_event_at` 键 ⇒ 起来的仍是旧进程 ⇒ **停下报**，⛔ 不要往下跑（旧代码验不出任何东西）。
   - 键在但不推进 ⇒ 就是 TD-42 的形态，**直接跳到【四】**（不必断网，等看门狗自己动手）。
3. `tail -5 data/liaison/logs/liaison.log`：⛔ 不应有「需要关注」那种旧格式残留行。

---

【三、AT-1b：真实入站落库一条 —— 🔴 **两轮，⛔ 不是一轮**（2026-09-13 订正）】

🔴 **先读这一段，否则你会在完全正常的第一轮就误判成故障。**
`[Mac]0913A` 已把 SDK `message` 接线合进 main（`7636613`），但 `tools/liaison/frames.py` 的
**`FIELD_PATHS` 是空的**（第 72 行），`compute_inbound_frame` **一律抛** `InboundFrameUnverifiedError`
⇒ `__main__.py:216` 走 **fail-closed 分支：本帧不落库**，只把帧的**键结构**打进日志。
这是**刻意的终态**，不是缺陷——已登记 `docs/tech-debt.md` **TD-43**。
*为什么这样设计*：`msgid` / 发送人 userid / 会话 id 落在帧的哪个键上，SDK 源码里**一处都查不到**
（`body` 的类型就是 `Any`，SDK 自己只读 `body["msgtype"]` 与 `headers["req_id"]`）。猜错的后果是
`thread_id` 或 `msgid` 落成错值 ⇒ 幂等键错 ⇒ 归档互相覆盖、或同一条消息永远重复入队，而**没有任何症状**。

⇒ **轮 1 的 `liaison_message` 必然仍是 0，这是对的。** ⛔ 轮 1 看到 0 **不许**停下报故障。

🔴 **发信人只能是 Shao Peishen 本人**（`whitelist.yaml` 里的 `ShaoPeiShen`）。
⛔ **不得请汤丽萍发**——她已白发 4 条；她的解锁点在轮 2 之后。

**轮 1 · 取帧结构**

1. 记基线：`python3 -c "import sqlite3;print(sqlite3.connect('data/liaison.db').execute('select count(*) from liaison_message').fetchone()[0])"` → 预期 `0`。
2. 记日志位置：`wc -l data/liaison/logs/liaison.log`（记下行数，轮 1 只读这一行之后的新增）。
3. 请 Shao Peishen **各发一条，两条都要**：
   - ① 在「人力AI保障群」**@ 机器人**发一条纯文字（内容随意，例如「帧结构取样1」）
   - ② **私信机器人**一条纯文字（例如「帧结构取样2」）
   🔴 **两条都要的理由**：群消息与单聊消息的帧里，会话标识很可能落在**不同的键**上
   （群是 chatid、单聊是 userid）。只拿一种就填表，另一种会在真回灌时静默失配。
   ℹ️ 私信这条还有独立价值：单聊 chatid 只有在对方先私信过之后才存在（第 4 章附件归档链路只能在带附件的私信上验，TD-22）。
4. 各等 30 秒后，从日志里取两份结构图：
   `grep "帧字段映射未经真实帧确认" data/liaison/logs/liaison.log | tail -4`
   预期每条都带一段「帧结构（只有键名与类型，⛔ 无取值）：…」。
   - **一条都没有** ⇒ 🔴 **这才是真故障**，停下报，并贴三样：`tail -30 data/liaison/logs/liaison.log`、
     两次 `liveness.json` 原文、`grep -n "SUBSCRIBED_EVENTS" -A6 tools/liaison/session_client.py`。
   - 🔴 **报告里贴结构图原文即可**（它按设计只有键名与类型、**无任何取值**）。
     ⛔ 不许为了排障把帧原文／正文／姓名打进日志或报告——那是个人信息。

**填表（这是本条唯一允许改的产品代码）**

5. 照两份结构图把 `tools/liaison/frames.py` 的 **`FIELD_PATHS` 填上**，⛔ **不改别处**
   （`frames.py` 模块 docstring 第三节写死了这条收口路径）。
   - 群与单聊若落在不同键 ⇒ 按 `frames.py` 现有结构做**兼容取值**，并把「哪种来源走哪条路径」写进注释；
     两者无法统一 ⇒ **停下报**，⛔ 不许只填一种就往下走。
   - 补单测：用**两份结构图各自的形状**（自己造假帧，⛔ 不要把真实取值写进测试）断言
     `compute_inbound_frame` 能取出 `msgid` / `sender_userid` / `thread_id` / `msgtype` / `content`，
     且**缺任一键时仍抛** `InboundFrameUnverifiedError`（fail-closed 不许被填表削弱）。**先红后绿**，红的报错原文抄进报告。
   - `docs/tech-debt.md` 的 **TD-43** 标题改 `~~TD-43~~ ✅ 已还（<commit>）`。
   - ⛔ **附件仍恒为 `None`**（`__main__.py` 里那段注释写明：附件句柄要再走一次 SDK 下载，且句柄落在哪个键同样未经确认）。附件归属 TD-22，⛔ 本条不做。
6. `./venv/bin/python -m pytest -q` ≥ 基线且 0 失败 → 提交（只 add `tools/liaison/frames.py`、对应测试文件、`docs/tech-debt.md`）→ `launchctl kickstart -k gui/$UID/com.zhuopin.hr.liaison` 让服务吃到新代码。

**轮 2 · 真落库**

7. 请 Shao Peishen **再发一条**（群或私信均可，⛔ 但要在报告里写明是哪一种）。
8. 30 秒后重跑第 1 步的计数：
   - **＝ 1**（或更多）⇒ ✅ **AT-1b 成立**。取证一行：
     `python3 -c "import sqlite3;print(sqlite3.connect('data/liaison.db').execute('select msgid,from_userid,received_at from liaison_message').fetchall())"`
     🔴 报告里**只贴 `msgid` / `from_userid` / 时间三列**，⛔ 不要贴消息正文。
   - **仍 ＝ 0** ⇒ 🔴 **停下报**，贴：新的 fail-closed 日志行（说明表填得不对）或落库失败的 ERROR 行（说明是别的毛病）。⛔ 不要再改一版表硬试。
9. 顺带：`liaison_task` 是否也 +1？不 +1 **不算失败**，照实记进报告即可。

【四、TD-42 真实验证：断网 9 分钟（全自动脚本，⛔ 不手动关 Wi-Fi）】

设备名**先实测**：`networksetup -listallhardwareports`（`0909AJ` 实测 `en1`，⛔ 不照抄）。
无害预检：`networksetup -setairportpower <DEV> on` 须 rc=0 且不要密码；
要密码 ⇒ **退回人工**——报告写「需 Shao Peishen 手动关 Wi-Fi 9 分钟再开」，⛔ 不硬试。

脚本落 `/tmp/aa/outage.sh`（三件事在关网**前**写死：独立兜底 `sleep 900` 复网、
`trap restore EXIT INT TERM`、每 5 秒采样）：

```
#!/bin/bash
DEV=<实测设备名>; REPO=<仓库根绝对路径>; LOG=/tmp/aa/samples.log; mkdir -p /tmp/aa
( sleep 900; networksetup -setairportpower "$DEV" on ) >/dev/null 2>&1 & disown
restore() { networksetup -setairportpower "$DEV" on; echo "$(date '+%T') RESTORE-TRAP" >> "$LOG"; }
trap restore EXIT INT TERM
sample() { echo "=== $(date '+%T') phase=$1 pid=$(launchctl print gui/$UID/com.zhuopin.hr.liaison 2>/dev/null | awk '/pid =/{print $3}') ===" >> "$LOG";
           cat "$REPO/data/liaison/liveness.json" >> "$LOG" 2>&1; echo >> "$LOG";
           cat "$REPO/data/liaison/watchdog.json" >> "$LOG" 2>&1; echo >> "$LOG";
           grep -E "判定连接假死|看门狗终止进程|连接假死|拉起" "$REPO/data/liaison/logs/liaison.log" | tail -3 >> "$LOG"; }
for i in $(seq 1 3);   do sample BEFORE;  sleep 5; done
echo "$(date '+%T') >>> WIFI OFF" >> "$LOG"; networksetup -setairportpower "$DEV" off
for i in $(seq 1 108); do sample OUTAGE;  sleep 5; done          # 540 秒 = 9 分钟
echo "$(date '+%T') >>> WIFI ON" >> "$LOG";  networksetup -setairportpower "$DEV" on
for i in $(seq 1 36);  do sample RECOVER; sleep 5; done          # 复网后观察 3 分钟
echo "$(date '+%T') >>> DONE" >> "$LOG"
```

`chmod +x /tmp/aa/outage.sh && nohup /tmp/aa/outage.sh > /tmp/aa/run.out 2>&1 &`，
然后**等 13 分钟**再读 `/tmp/aa/samples.log`。
⛔ 断网期间 CC 自己也没网，脚本起了就别碰；`trap` 或独立兜底触发了要**如实写进报告**
（那说明脚本没正常走完）。

预期时间线（相对 `WIFI OFF`）：~60–90 秒 SDK 判死 → `state=disconnected`、存活戳定格；
+180 秒 看门狗「判定连接假死…先请求停掉事件循环」；再 +180 秒 宽限期满
「看门狗终止进程（退出码 5…）」＋ 一条 `【HR 值守通道·连接假死】` 告警行；
`watchdog.json` 出现 `consecutive: 1`；`pid=` 列**变了**（launchd 在 `ThrottleInterval` 30 秒内拉起新进程）；
复网后新进程 `state=connected`、`last_event_at` 推进。

**六项判读**（每项贴 `samples.log` 原文行）：① 判死 ② 看门狗判定假死 ③ 终止 ERROR
④ 告警行 ⑤ pid 变化 ⑥ 复网自愈。

---

【五、守住】

- ⛔ **除【三】第 5 步的 `frames.FIELD_PATHS` 与其单测外，不改任何产品代码**（本条是验证，不是修复）。发现别的缺陷 ⇒ 登记 `docs/tech-debt.md` 新 TD 号并停下报，⛔ 不顺手修
- ⛔ 不碰 `_to_delete/`、`.env`、`tools/liaison/.env`、`whitelist.yaml`
- ⛔ 不 `git add -A` / `git add .` / `git commit -a` / `git stash`（含 `-u`）；只 `git add` 本条明确列出的路径
- ⛔ 不删任何 `.git` 下的文件（锁归 `[Mac]0910F`）
- ⛔ 不请汤丽萍或任何其他人发消息（见【三】）
- ⛔ 不装/不重装 launchd、⛔ 不 `bootout`（值守进程按 `KeepAlive=true` 常驻，`pgrep` 非空是**正常**）
- ⛔ 不勾 8.6／8.7／8.8／8.9

【六、收工】报告逐项给：门槛三条的实际输出；重启前后 pid；两次 `liveness.json` 原文
（含 `last_event_at` 是否推进）；【三】的基线计数、事后计数、三列取证（⛔ 无正文）；
断网六项判读逐条 ✅/❌ ＋ `samples.log` 关键行；`watchdog.json` 原文；`trap`/兜底是否触发。

**回填规则（只 `git add` 下列路径）**：

- 【三】轮 2 ＝ 1 ⇒ `openspec/changes/liaison-reply-bridge-and-patrol/tasks.md` 的 **0.3 改 `- [x]`**
  （⚠️ 8.5bis 与门槛 **0.2** 已由 `[Mac]0913A` 于 2026-09-13 勾完，⛔ 不要重勾），
  `docs/tech-debt.md` 的 **TD-43** 销账，并在 `docs/session接力.md` 把 **AT-1b 标 ✅**
- 【四】六项全 ✅ ⇒ `docs/tech-debt.md` 的 `~~TD-42~~` 标题里把「⏸ 真实验证待 Shao Peishen」
  改成「✅ 真实验证 <当天日期> <时:分> CST」（**实跑 `TZ=Asia/Shanghai date` 取**，⛔ 不写死 09-10），
  同时把本包 `tasks.md` 的 **0.1 改 `- [x]`**，并在接力把 **P-3 标 ✅**
- 任一 ❌ ⇒ 🔴 **⛔ 不改销账行**，把现场写进 `docs/findings/2026-09-10-TD42真实验证.md`，
  TD-42 回到「未验」

提交后 `git push origin main`（被拒 ⇒ `git pull --rebase --autostash origin main` 后重试，最多 3 次；
⛔ 不要在 commit 前 pull）。

**收工单列一行**：`⏸ 下一步：本包 §0 三门槛已全勾 ⇒ liaison-reply-bridge-and-patrol 可开工（P0→P1→P2→P3 四条 worktree 泳道），交 Cowork 编排`
