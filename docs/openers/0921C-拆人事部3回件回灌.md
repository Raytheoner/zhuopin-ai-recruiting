# [Mac]0921C 按章程拆 `人事部#3` 两条回件并回灌

## 零、为什么

`0921B` 修好 liaison CLI 后，`人事部#3`（汤丽萍·需求讨论会结论回收单，已并入 `人事部#2` 的未答项）
仍有 2 条 pending 信号等拆。Shao Peishen 2026-09-21 明确要求一并处理。

## 一、开工自核（任一不过即顶格 `OPENER_PARTIAL` 并停）

1. 🔴 **第一件事**：用 `Read` 打开 `.claude/skills/liaison-unpack/SKILL.md` **全文**，
   它是本会话**唯一**的行为边界正本，逐条遵守（红线九项、拆件步骤、收口、待人栏）。
   ⛔ 本 opener 不复述其条文，以该文件为准；两者若有出入，**以章程为准**。
2. `PYTHONPATH=. tools/liaison/.venv/bin/python -m tools.liaison unpack-signal --probe` 输出 `[SIGNAL]`。
   输出 `[NO-SIGNAL]` ⇒ 说明已被别的会话拆完，顶格 `OPENER_PARTIAL: 无信号，未拆` 并停。

## 二、动作

按章程 §二 逐条处理每一条 pending 项（本轮预期 2 条，`letter_number` 均为 `人事部#3`），
再按章程 §一.5 清信号、§三 收口。两条归档正文都很小（68 B／55 B），
若内容为寒暄或与决策点无关 ⇒ 按章程判「非实质回件」并还原原状态，⛔ 不硬判成实质。

🔴 命中章程 §四（含 §〇⑨ 个人信息）⇒ 按章程登记「待人」，然后顶格
`OPENER_PARTIAL: 转待人 <原因>`，⛔ 不自行下裁决、不硬拆。

## 三、机器判据

```bash
set -e
cd "$(git rev-parse --show-toplevel)"
grep -q '人事部#3' docs/session接力.md
ls docs/跟进信/回件/ | grep -q '人事部#3' || grep -q '待人' docs/session接力.md
grep '人事部#3' docs/跟进信/README-跟进信清单.md | grep -qE '📥 已回件并回灌|✅ 无需回复|📨 已确认闭环|❌ 已作废|待人'
```

## 四、并发协议

同泳道串行在 `0921B` 之后；`0921B` 不过则本条自动不跑（run-lanes 既有语义）。

## 五、红线

以章程 §〇 九项为准（⛔ 本文件不复制其条文）。特别提示三条：**⛔ 不对外发送**、
**⛔ 不 `git push`**（章程⑦，提交后停）、**⛔ 不修改 `.claude/skills/` 与 `CLAUDE.md`**。

## 六、收口

章程 §三 做完且三条机器判据全过 ⇒ **顶格** `OPENER_DONE`；转待人或判据不过 ⇒ **顶格** `OPENER_PARTIAL: <原因／实测值>`。
