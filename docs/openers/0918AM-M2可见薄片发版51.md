# `[Mac]0918AM` M2 可见薄片发版到 `.51`（G3 已放行 `Q-44`「发」）

【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: main ｜ worktree: ❌ 不勾（发版操作在主检出，不写业务代码）｜ 工作区: 仓库根 ｜ 派发: Cowork·HR业务线-接力0918G ｜ 模型: Sonnet

## 零、授权

定夺队列 `Q-44`【G3 发布】`M2 可见薄片`：**状态＝已答，答复＝「发」**（Shao Peishen 2026-09-18 07:3x「三单元开跑，上线」）。预授权五条件：① U1／U2／U2.5 全部合 main 且回勾 ② 全量 pytest 0 失败 ③ 按 `docs/releases/2026-09-发版清单-51.md` 流程快照→依赖→sync→G-e→冒烟，不过即回滚 ④ 真实简历入库闸保持关闭 ⑤ 发版后在本线报。

## 一、🔴 开工第一步：自己核 ①②④，不许信任何转述

1. **①**：`git log` 实证 U1（`66e3b97`）、U2（`42b999c`）、U2.5（`413fc98`）三个合入点都在 main 历史里；`openspec/changes/m2-resume-parse-and-rank/tasks.md` 的 2.4–2.8＋8.1、3.1–3.10、9.1–9.5 逐条实读确认为 `[x]`。
2. **②**：在主检出实跑全量 `pytest`，**必须 0 failed**，报数写进记录。今天 main 上出现过两次环境/回归缺口（`0918AB`→`0918AF` 的 `EFFECT_NODE_MANIFEST` 漏登记、`0918AG` 的 M3 探针依赖），最近一次全量绿是 `a7e417a`（3415 passed），但此后仍有泳道合入，**必须重跑，不得引用历史报数**。
3. **④**：实读真实简历入库闸（`live_resume_intake_enabled`）配置，确认为关闭。

任一条不成立 ⇒ ⛔ 立即停，写 `OPENER_PARTIAL: 预授权条件 <编号> 不成立：<实证>`，**不得发版**、⛔ 不得顺手去修（修是另一条 opener 的事）。

## 二、锁定发版 commit 并申报携带范围

今天 main 上除 M2 三单元外还合入了语音面试包 U1 数据模型、U2 出题引擎、调度器修复等。发版发的是 main 快照，这些会一并上 `.51`。所以：

1. 取当前 `main` HEAD 作为**本次发版 commit 并写死**，后续所有阶段都针对这个 commit，⛔ 中途不得因为别的泳道合入而换 commit。
2. 在执行记录里列出：本次发版 commit、现网原 commit、以及**超出 M2 可见薄片范围的携带项**（逐个变更包点名）。
3. 🔴 对每个携带项核验「对现网无行为改变」：语音面试相关的表与端点必须是默认关闭／未接线状态，`.51` 现有行为不因它们改变。任一携带项会改变现网行为 ⇒ ⛔ 停，写 `OPENER_PARTIAL: 携带项 <名> 会改变现网行为`，交 Shao Peishen 定夺。

## 三、执行

严格按 `docs/releases/2026-09-发版清单-51.md` 逐阶段执行并逐阶段留证：**快照 → 依赖 → sync → G-e → 冒烟**。新开记录 `docs/releases/2026-09-18-发版三-M2可见薄片执行记录.md`，体例参照 `2026-09-17-发版一执行记录.md`／`发版二与R9执行记录.md`，每阶段写：做了什么、实际输出、判定（过／不过）。`.51` 经免交互 `ssh zp51` 可达（`docs/findings/2026-09-17-无头会话51可达性探测.md`；09-17 两次发版即此路径）。

**⛔ 不过即回滚停链**：任一阶段判定不过 ⇒ 立刻按清单回滚到快照点，核验回滚后现网可用，然后停，写 `OPENER_PARTIAL: <阶段> 不过：<原因>＋回滚结果`。⛔ 不得"再试一次"、不得跳过阶段、不得边修边发。

## 四、红线

⛔ 入库闸发版前后各核验一次，全程保持关闭，任何情况下不得在本条里打开它。⛔ 不动 `.51` 系统组件、不装或升级系统级运行库。⛔ 不碰真实候选人简历数据、不导出任何库内数据行。⛔ 不改 `scripts/gates.py` 判据、不改台账状态放行。⛔ 不对外发送任何消息。⛔ 不得把任务交给后台子代理后结束回合——子代理一律前台等到返回，哨兵由本会话自己顶格打印。

## 五、收口

执行记录与 `docs/session接力.md` 的一行发版结果，走提交通道或直接 `git add` 这两个路径提交并 push。

【并发协议】仓库此刻有别的泳道在跑（语音面试包 U2 建造 `0918AJ`／`0918AK`／`0918AL`）：只 `git add` 本条产出的路径；⛔ 禁 `git add -A`／`git add .`／`git commit -a`／`git stash`；push 被拒 → `git pull --rebase --autostash origin main` 重试 ≤3 次；报 `.git/index.lock` → 等 5 秒重试 ≤5 次，⛔ 绝不删锁。

收工五行：①②④ 三条前置的实测结论 ｜ 本次发版 commit（现网原 → 新）与超范围携带项清单 ｜ 五个阶段各一句判定 ｜ 冒烟结果 ｜ 入库闸发版后复验结论。最后一行由本会话自己顶格输出 `OPENER_DONE` 或 `OPENER_PARTIAL: <原因>`。⛔ 不提问（无人值守）。
