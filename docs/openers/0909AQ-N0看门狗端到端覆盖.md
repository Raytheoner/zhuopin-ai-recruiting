[Mac]0909AQ-N0看门狗端到端覆盖
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ✅ 勾（写测试代码，须隔离）｜ 工作区: worktree 根 ｜ 派发: [Mac]0909AJ

> 本文件是 `[Mac]0909AQ` 的正文（引用式 opener；⚠️ 原拟 `0909AP`，与第十六批·入站预演泳道撞号，`[Mac]0909AZ` 2026-09-09 改判为 `AQ`）。目标＝**给 N-0 兜底层（存活戳看门狗）
> 补端到端覆盖**。`0909AJ` 实测证明：TD-39 的两层修法里，**只有改法 1 被真实验证过**，
> 看门狗那层至今只有单测。
> 依据：`docs/findings/2026-09-09-TD39真实断网复验.md` 第四节 ⑦、
> `docs/tech-debt.md` TD-39「真实断网复验」节。
> Shao Peishen 2026-09-09 答 `2a` 授权本条开工。

【一、为什么普通断网测不到 —— 先读懂这条，否则你会重跑一遍 `0909AJ`】

🔴 **⛔ 不要再去断 Wi-Fi。** `0909AJ` 已经断过 245 秒（远超阈值 180 秒），
看门狗**依然没触发**，而且这是**正确行为**，不是 bug：

> 改法 1（`error` 监听器）生效后，SDK 每次重连失败都会触发 `error` 事件、刷新一次存活戳。
> SDK 退避上限是 30 秒 ⇒ 存活戳最大间隔 30 秒 ⇒ **永远达不到 `STALE_LIVENESS_SECONDS = 180.0`**。

⇒ 两层是「改法 1 修好则看门狗静默」的**串联兜底**关系，不是并联触发。
⇒ 要触发看门狗，必须构造**改法 1 覆盖不到**的形态：
**「SDK 事件完全静默，但 `loop` 还活着」** —— 即 `0909AG` 撞到的原始故障
（`_receive_loop` task 已死、`client.run()` 仍不返回、外层 `run_forever` 永远等不到那次返回）。

⚠️ 这个形态**只能在测试里构造**，⛔ 断网、拔网线、关代理都造不出来。

【二、前置闸门】任一条不过就停下报，⛔ 不许往下跑。

1. `git pull --rebase --autostash origin main`。
2. 确认被验代码在：
   ```
   grep -n "STALE_LIVENESS_SECONDS" tools/liaison/session_client.py     # 应见 96 行 = 180.0
   grep -n "run_liveness_watchdog" tools/liaison/__main__.py            # 应见已接进 main()
   ```
3. `PYTHONPATH=. venv/bin/python -m pytest tools/liaison/tests -q` 应绿（基线 807 passed / 4 skipped，
   ⚠️ 第十六批可能已加测试，**数字只会涨不会跌**；跌了说明有回归，停下报）。

【三、要补什么】🔴 **本条只补测试，⛔ 不改任何产品代码。**

现有覆盖（`tools/liaison/tests/test_session_client_reconnect.py`）验的是
`run_liveness_watchdog` **这个函数自己**的判定逻辑。缺的是**接线那一段**：
看门狗触发之后，「停事件循环 → `client.run()` 返回 → 外层 `run_forever` 接手重建」
这条链路**从未被端到端跑过**。

至少要钉住三条，每条**先红后绿**（⛔ 不许只写绿的）：

| # | 要钉住的事 | 先红怎么造 |
|---|---|---|
| E-1 | 存活戳陈旧 ⇒ 看门狗**真的停掉**事件循环，`client.run()` **真的返回** | 把 `__main__.py:196` 的 `watchdog=` 接线摘掉 ⇒ 必须红 |
| E-2 | `client.run()` 返回后，**外层 `run_forever` 真的重建了一次连接** | 同上；另可把 `run_forever` 的重建分支改成 `pass` ⇒ 必须红 |
| E-3 | 重建成功后**存活戳恢复刷新**、中断窗口被闭合 | 断言 `liveness.json` 与 `liaison_outage_window`，同 `0909AJ` 的 ⑤⑥ 口径 |

**怎么构造「事件静默但 loop 活着」**：用假 SDK（本仓已有 fake 的写法，照抄
`test_session_client_reconnect.py` 现有夹具）——让 fake client 的 `run()` 起一个
**永不完成、也永不触发任何事件**的 task，再把时钟推过 180 秒。
⛔ **不要用真 `sleep(180)`**，那会让这条测试跑 3 分钟；用可注入的时钟/`freezegun` 式假时间，
或把阈值做成可注入参数（`run_liveness_watchdog` 已有 `threshold_seconds` 形参，**直接用它**）。

🔴 **每条测试必须先证明它在修复缺席时是红的**，把红的报错原文贴进 commit message。
⛔ 不许写「跑了一下是绿的」就算——本条守的正是无症状故障，没先红过的测试没有判据力。

【四、⛔ 明确不做的】
- ⛔ **不改产品代码**（`session_client.py` / `__main__.py`）。若发现必须改才能测，
  **停下报**，⛔ 不要顺手改——那会让「测试证明了代码对」变成「代码被改成能过测试」
- ⛔ **不修「冷启动瞬间看门狗误报」那条**（`0909AJ` 发现、Shao Peishen 2026-09-09
  答 `3b` **明确不修**，只留在 TD-39 记录里）。⚠️ 你写的 fake 夹具可能会撞见它，
  **撞见就绕开，⛔ 不要顺手修**
- ⛔ **不做真实建连、不断网**（见第一节，测不到）
- ⛔ **不装 launchd**
- ⛔ **不动 `max_reconnect_attempts`**（已是 `-1`，原 N-1 早已作废）

【五、并发协议】第十六批（`0909AK`–`AO`）可能同时在跑，逐条照做：
1. 只 `git add` 本条明确列出的路径。⛔ 禁止 `git add -A` / `git add .` / `git commit -a` /
   `git stash`（含 `-u`）
2. `git status` 里出现别人的改动是正常的，不要停下、不要问、不要顺手提交
3. push 被拒 → `git pull --rebase --autostash origin main` 后重试，最多 3 次。
   ⛔ 不要在 commit 前 pull
4. 报 `.git/index.lock` 已存在 → 等 5 秒重试最多 5 次，⛔ 默认绝不删除该锁
5. ⚠️ **触碰区核对**：本条动 `test_session_client_reconnect.py`；第十六批分别动
   `queue.py`（AK）/ `notify/`+`schema.py`（AL）/ `logsetup.py`（AM）/
   `test_liaison_effects.py`（AN）/ `followup.py`（AO）——**不重叠** ✅。
   唯一共享点是 `docs/tech-debt.md`：🔴 **只许改 TD-39 那一段**，⛔ 不重排目录、不动他人条目，
   冲突时**合并双方**、⛔ 不许二选一

【六、提交】只 add：`tools/liaison/tests/test_session_client_reconnect.py`（或你新建的测试文件）、
`docs/tech-debt.md`（仅 TD-39 段）、本文件。
⛔ 绝不 add `.env`、`data/`、`tools/liaison/.venv/`。合并回 main 后 push。

【七、落档】TD-39 已销账（`0909AJ`），本条**不改它的销账状态**，只在「真实断网复验」节后
追加一小节写明 N-0 现已有端到端覆盖，并把
「N-0 兜底层本次未被触发、仍只有单测覆盖」那句**订正为「已由 `0909AQ` 端到端覆盖」**。
⛔ 不许删掉原句——保留它才看得出这条是分两次验完的。
同时更新 `docs/session接力.md` 对应行状态。

【八、收工】逐个列出新增/修改的文件（仓库相对路径，⛔ 不写"等若干文件"），报告含：
三条测试各自的**先红报错原文**与转绿后的结果、全量 pytest 前后计数、commit hash，
以及一句明确结论：「N-0 兜底层现已有端到端覆盖 / 或：未能构造出该形态，原因是……」。

---

## ✅ 收工（2026-09-10 CST 执行完毕）

- 交付：`tools/liaison/tests/test_session_client_reconnect.py` 文末「N-0 端到端」一节，
  E-1/E-2/E-3 三条，全部**先红后绿**；红的报错原文见 `docs/tech-debt.md` TD-39
  「N-0 端到端覆盖」一节的表格。
- 🔴 **未改任何产品代码**（`session_client.py` / `__main__.py` 一字节未动，第四节要求）。
- 全量 `tools/liaison/tests`：894 → **897 passed** / 5 skipped（+3）。
- 结论：**N-0 兜底层现已有端到端覆盖。**
