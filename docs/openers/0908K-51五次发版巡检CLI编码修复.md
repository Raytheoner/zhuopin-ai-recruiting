[Mac]0908K-51五次发版巡检CLI编码修复
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（在仓库根跑 sync-to-server.sh，改文档提交 main）｜ 工作区: 仓库根 ｜ 派发: [Mac]0908B

> 本文件是 `[Mac]0908K` 的正文（引用式 opener）。🔴 `.51` 发版是不可代项：依据＝Shao Peishen 2026-09-08 回「请确保及时修复」（对巡检 CLI 在 GBK 控制台误报 `EXIT=1` 的处置裁决）。
> 没有这句授权 ⛔ 一步都不要跑。

【零、这次发什么，和 0908J 差在哪】
`19b8937 fix(audit): 巡检 CLI 不再因控制台编码不了 ✅ 而误报 EXIT=1`（含 `app/audit/assertions.py`
的 `_print_safely()` 与边界守护依赖判据换成登记制）。

⚠️ **本次是纯 sync，⛔ 无装依赖步骤**——`requirements.txt` 自 `0908J` 起未再改动，
`.51` 上 `tzdata==2026.3` 已在位（已核）。⛔ 不重跑 `deploy-server.ps1`。

【一、前置核对（任一不过即停）】
1. `git status -sb`：main 与 origin 同步或仅 ahead；`git log -1 --format=%h main` 记下
2. `ssh zp51 "echo ok"` 回 ok；不通 → 登记「⏸ 留步：不在内网」停
3. 现网健康基线：`curl -s -o /dev/null -w "%{http_code}\n" --max-time 15 http://192.168.100.51:8095/hr/recruit-agent/`
   不是 200 → 停下报 Shao Peishen，⛔ 不在已经不健康的现网上叠发版
4. `git diff --stat 7a48a19..main -- requirements.txt` 必须为空（非空说明又动了依赖，
   本 opener 的「纯 sync」前提不成立 → 停，改走 `0908J` 那套先装依赖的顺序）

【二、快照 → 发版 → 冒烟】
1. 快照（在 .51 上跑）：
   ssh zp51 "powershell -NoProfile -Command \"\$ts=Get-Date -Format yyyyMMdd-HHmm; \$b='C:\apps\backups\'+\$ts; New-Item -ItemType Directory -Force \$b | Out-Null; Copy-Item 'C:\apps\zhuopin-recruit-agent\app' \$b\app -Recurse; Copy-Item 'C:\apps\zhuopin-recruit-agent\data' \$b\data -Recurse; Write-Output \$b\""
2. 发版命令**由 Shao Peishen 点 Run**（Auto Mode 分类器拦 AI 直接调 `sync-to-server.sh`）：
   ```bash
   ./sync-to-server.sh
   ```
   末尾必须 HTTP 200 + 「发版完成」。
   ℹ️ 本次起「远程重启计划任务」那两行**应当是可读中文**（`cf308cc` 修了 GBK 乱码）；
   若仍是 `?J?: ?2?????` → 说明脚本没同步过去，记下来，不阻断
3. 冒烟 4 项（逐项记原文，任一不过即走 §二.4）：
   ① `curl` 首页 → 200
   ② `curl .../api/jobs` → JSON 列表（⚠️ 405 ＝ 没发上去，⛔ 不要当通过）
   ③ `app.log` 尾 60 行无 `Traceback` / `OperationalError`，且有本次新进程的 `Application startup complete`
   ④ 🔴 **本次发版的目标验收——不带 `PYTHONIOENCODING` 直接跑巡检，必须 `EXIT=0`**：
      ssh zp51 "powershell -NoProfile -Command \"cd C:\apps\zhuopin-recruit-agent; .venv\Scripts\python.exe -m app.audit.assertions --db data\demo.db --mirror data\audit\decisions.jsonl; Write-Output ('EXIT=' + \$LASTEXITCODE)\""
      期望：`EXIT=0`、6 条断言全过、断言四 detail 报「豁免 7 条」。
      标记降级成 `[OK]` 是**预期**（GBK 编不了 ✅），⛔ 不是失败；只要 `[OK]` 与 `[FAIL]`
      能分辨即可。⚠️ 仍报 `UnicodeEncodeError` 或 `EXIT=1` → 修复没生效，走 §二.4
4. 任一不过 → 回滚：`Remove-Item app -Recurse -Force` → `Copy-Item <本次快照>\app` 回 →
   `schtasks /end` + `/run` → `curl` 200 → 写 `docs/findings/2026-09-08-51五次发版回滚.md`，停。
   ⛔ `data\` 不回滚

【三、落档与提交（只 add 列出的路径）】
1. `docs/audit-and-outbound-ops.md` §五新增「2026-09-08 五次发版记录」：commit / 快照 /
   冒烟 4 项 / **不带环境变量的巡检 EXIT 与报告原文**。⛔ 不覆盖已有的四次发版两条记录
2. `docs/session接力.md`【一】`.51` 代码行改「已发版 <commit>」，并删去那句
   「加 `PYTHONIOENCODING=utf-8` 才得真结果」的临时口径——⛔ 留着会让下一个人以为还要加
3. `docs/openers/OP-0820-全量编排.md` 号池 `0908K` 行补结果
4. git add docs/audit-and-outbound-ops.md docs/session接力.md docs/openers/OP-0820-全量编排.md
   ⛔ 禁止 `-A` / `.` / `-a`；别人的改动不停、不问、不顺手提交
5. git commit -m "ops(.51): 五次发版 <commit>（巡检 CLI 编码修复）＋ 裸跑巡检 EXIT=0"；git push origin main
6. push 被拒 → `git pull --rebase --autostash origin main` 后重试最多 3 次，⛔ 不在 commit 前 pull

【四、汇报】发版 commit / 快照目录 / 冒烟 4 项逐项 / **不带 PYTHONIOENCODING 的巡检 EXIT 与报告原文** / commit hash / ahead
