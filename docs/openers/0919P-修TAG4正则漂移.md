# `[Mac]0919P` 修 `TAG4_RE` 四标签待办正则漂移（`Q-47`；`Q-48` 已核实无需重复修）

【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: lane-0919p-tag4-regex ｜ worktree: ✅ 勾 ｜ 工作区: `.claude/worktrees/lane-0919p-tag4-regex` ｜ 派发: Cowork·HR业务线-接力0918AO ｜ 模型: Sonnet

## 零、为什么（含一条重要澄清：本条范围比 `Q-47`/`Q-48` 合并授权小）

Shao Peishen 2026-09-19 22:0x 对 `Q-47`＋`Q-48` 合并答「(a) 现在派」。但 Cowork·0918AO 巡检发版前逐条核实代码真身时发现：**`Q-48`（`_match_plans` 前缀误绑依赖）已经修过了**——`commit 5c8ed9c`（`fix(dispatcher): 收紧前缀回退匹配+拆段兜底改3个一段（0918AI relay R-13/R-14）`，2026-09-18 10:13）比 `Q-48` 写入定夺队列的那次台账刷新（`095d1df`，2026-09-18 09:21）晚约 52 分钟落地，`_match_plans` 现在用 `re.match` 加可选日期戳前缀锚定字符串开头，`Q-48` 原文引用的旧正则 `(?<![\w]){prefix}-unit{u}(?!\.?\d)`（`re.search` 风格、无开头锚定）已不在代码里。定夺队列 `Q-48` 只是没被回填「已修」，这是重复劳动，本条**不再碰** `_match_plans`，只处理真正还活着的 `Q-47`——这条本身就是「先核实别重复修」的一次真实示范，收工时把这段核实结果原样写回 `Q-48` 答复列。

`Q-47`：`scripts/dispatcher_backlog.py` 第 646 行 `TAG4_RE`：
```python
TAG4_RE = re.compile(r"【谁做[：:]\s*(.*?)】\s*【状态[：:]\s*(.*?)】\s*【判据[：:]\s*(.*?)】\s*【不做会怎样[：:]\s*(.*?)】", re.S)
```
只认标签内带冒号（全角/半角均可）的格式。`docs/session接力.md` 当前**全部 7 条**四标签待办行已漂移成「【谁做】x｜【状态】y｜【判据】z｜【不做会怎样】w」（标签内无冒号、字段用｜分隔），本条 opener 起笔前实测 `TAG4_RE.findall()` 命中数＝**0**——这不是理论风险，是当下就在发生的漏派。

## 一、Task 1：修正则，双格式兼容

改 `scripts/dispatcher_backlog.py` 的 `TAG4_RE`，使其同时认：
1. 旧格式（原样保留，⛔ 不破坏向后兼容）：`【谁做：x】【状态：y】【判据：z】【不做会怎样：w】`
2. 新格式（当前 session接力.md 实际在用）：`【谁做】x｜【状态】y｜【判据】z｜【不做会怎样】w`（标签内无冒号，字段间用全角｜分隔，字段值本身可能含中英文/反引号/标点但不含｜）

两种格式必须能同时被同一个解析路径吃下（写 `parse_relay` 或其调用点时用「先试旧格式，未命中再试新格式」或单条支持两者的合并正则均可，你判断哪种改动量小）。⛔ 不要求以后统一成哪种格式——`session接力.md` 里两种格式可能长期共存，正则要能各自识别，不追加「以后必须写成 XX 格式」的新约束。

**测试**：新增/扩展 `tests/test_dispatcher_backlog.py`（或既有对应测试文件）覆盖：旧格式 4 条全部字段、新格式 4 条全部字段、旧新混合文本、字段值本身包含全角/半角冒号或｜字符时不误裂。跑现有全部 `tests/test_dispatcher*.py`，⛔ 不得引入新失败。

## 二、Task 2：人工核对 `session接力.md` 现存 7 条四标签待办行

Task 1 修好后，实跑一次 `TAG4_RE`（或改后的解析函数）对 `docs/session接力.md` 全文抽取，应得到 7 条（本条起笔时实测原文有 7 条，若你执行时行数有变以实测为准）。逐条核对：

1. 是否已经被某条 `relay:R-xx` 登记过（`grep` 定夺队列与台账里的 `relay:` 前缀，避免重复登记）；
2. 是否描述的问题在 main 当前状态下依然成立（例如涉及某单元代码的条目，若该单元已合 main 且对应测试已转绿，判「已过时，可标注 ✅ 或整行删」；描述外部依赖/等他人回件的条目，判「仍待」）；
3. 对确认仍待、且未被登记过的条目，登记进定夺队列 `relay:R-<新编号>` 并按 `lane-dispatch` 规则判断是否可派车；对确认已过时的条目，直接在 `session接力.md` 里整行标「【已闭环】」或删除（按 `03` 归档规则）。

⛔ 本步骤不改判据、不改闸门逻辑，只做「解析工具修好后，把它现在能看见但过去看不见的东西过一遍人工判断」，属于文档整理，不是代码变更，改 `docs/session接力.md` 时按 commit-request 白名单走（不在本条 worktree 分支里改，另开一次到主检出的小改动，或收口时一并处理——你判断更省事的方式，但⛔ 不要把 `docs/session接力.md` 的改动和 `scripts/` 的改动混进同一次 `git add`）。

## 三、红线

⛔ 不碰 `_match_plans`（`Q-48` 已修，见零节）。⛔ 不改闸门 G1–G5 判据。⛔ 不给调度器加自动发版/自动放行能力。⛔ 不得把任务交给后台子代理后结束回合——子代理一律前台等到返回，哨兵由本会话自己顶格打印。

## 四、无头块铁律

同 `OP-0820`「三、无头块铁律」：⛔ 不得后台起子代理后结束回合。

## 五、验证与收口

1. 真身验证：跑一次台账生成器（`python3 scripts/dispatcher_backlog.py` 或既有测试入口）确认 `session接力.md` 里的四标签行能被正确解析，数量与人工核对一致。
2. 定夺队列回填：`Q-47` 答复列已是「(a) 现在派」（不改），在旁追加一句执行结果引用（commit hash）；`Q-48` 答复列追加：「核实为已修复——commit 5c8ed9c（2026-09-18 10:13）已收紧 `_match_plans` 前缀匹配，早于本条待答登记（095d1df，09:21），无需重复修，本条 2026-09-19 由 `0919P` 核实关闭」，状态列改「已答」。
3. 提交：`scripts/dispatcher_backlog.py`、对应测试文件走正常 `git add`（本分支）；`docs/roadmap/定夺队列.md` 与（如有）`docs/session接力.md` 的改动⛔ 不在本 worktree 分支提交（它们不是本条触碰区代码），改为收口时用 commit-request 通道提交到主分支，或如实登记「待下一次巡检一并提交」。

【并发协议】
1. 只 `git add` 本条产出的文件；⛔ 禁止 `git add -A`／`git add .`／`git commit -a`／`git stash`
2. `git status` 里出现别人的改动是正常的，不要停下、不要问、不要顺手提交
3. push 被拒 → `git pull --rebase --autostash origin main` 后重试，最多 3 次
4. 报 `.git/index.lock` 已存在 → 等 5 秒重试最多 5 次，⛔ 默认绝不删除该锁

收工三行：Task 1（正则改动 commit＋测试 passed/failed 数）｜ Task 2（7 条逐条核对结论：几条登记新 relay、几条标已闭环、几条维持原状）｜ 定夺队列 `Q-47`/`Q-48` 回填是否完成。最后一行顶格输出 `OPENER_DONE` 或 `OPENER_PARTIAL: <原因>`。⛔ 不提问（无人值守）。
