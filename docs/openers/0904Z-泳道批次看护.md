[Mac]0904Z-泳道批次看护
【设置】执行环境: CC Desktop ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（你是调度者，实际干活的是脚本起的独立 session）｜ 工作区: 仓库根

> 本文件是 `[Mac]0904Z` 的正文（引用式 opener，规则见 `.claude/skills/kickoff/SKILL.md`「引用式 Opener」）。
> 第九批看护者。文件随任务一起提交，即留痕。

【一、你的角色】
你不亲自执行任何 opener。你做五件事：先发一次 `.51`（Shao Peishen 已授权）、前置自检、后台启动脚本、轮询盯守、跑完真身核验并出报告。
⛔ 不要自己去改代码、不要自己跑 spec-to-plan / run-build / openspec-archive-change。

【二、先提交 Cowork 侧的编排落档 ＋ 把 settings.local.json 挡在 git 外】
1. echo ".claude/settings.local.json" >> .gitignore（CC 本机「总是允许」记录，不该进仓库）；git status 里它应从 `??` 消失
2. git status --short 预期只剩：docs/session接力.md、docs/openers/OP-0820-全量编排.md、docs/openers/0904Z-泳道批次看护.md、.gitignore。
   只 add 这四条路径，⛔ 禁止 git add -A / git add . / commit -a：
   git add .gitignore docs/session接力.md docs/openers/OP-0820-全量编排.md docs/openers/0904Z-泳道批次看护.md
   git commit -m "docs(openers): 第九批编排（intake 第 6 章确认断点 → 第 7 章 JD 溯源，单泳道串行）＋ settings.local.json 入 gitignore"
   git push origin main
   有别的改动 → 单列出来，不提交、不问、继续

【三、`.51` 三次发版（不可代项；依据：Shao Peishen 2026-09-04 在 Cowork 会话 HR业务线-接力0903B 回「发」，把 TD-9 修复推上生产）】
与 0903L 同一套，逐字：
1. 前置：git log -1 --format=%h main 记下（应 ≥ 7faf682，且是【二】提交后的 HEAD）；ssh zp51 "echo ok" 回 ok；
   git diff --stat b86db65..main -- requirements.txt 必须为空（为空才允许只 sync 不重跑 deploy-server.ps1；不为空即停，报我）
2. 快照（在 .51 上）：
   ssh zp51 "powershell -NoProfile -Command \"\$ts=Get-Date -Format yyyyMMdd-HHmm; \$b='C:\apps\backups\'+\$ts; New-Item -ItemType Directory -Force \$b | Out-Null; Copy-Item 'C:\apps\zhuopin-recruit-agent\app' \$b\app -Recurse; Copy-Item 'C:\apps\zhuopin-recruit-agent\data' \$b\data -Recurse; Write-Output \$b\""
   记下目录。引号嵌套报错 → 改 -EncodedCommand
3. ./sync-to-server.sh，末尾必须 HTTP 200 + 「发版完成」
4. 冒烟：curl 首页 200；页面相对资源逐个 curl 无 404；app.log 尾 60 行无 Traceback/OperationalError；
   ssh zp51 "powershell -NoProfile -Command \"Select-String -Path 'C:\apps\zhuopin-recruit-agent\app\outbound\delivery.py' -Pattern 'record_outbound_decision' | Measure-Object | Select -Expand Count\"" 应 ≥ 1（TD-9 修复的目标符号真到了生产）
5. 任一不过 → 回滚：Remove-Item app -Recurse -Force → Copy-Item <快照>\app 回去 → schtasks /end + /run → curl 200；写 docs/findings/2026-09-04-51三次发版回滚.md；**发版失败不影响发车**，登记后继续【四】
6. 成功 → docs/audit-and-outbound-ops.md §五追加「2026-09-04 三次发版记录」（commit / 快照目录 / 冒烟 4 项）；docs/session接力.md【一】.51 代码行改「已发版 <commit>（含 TD-9 修复）」。这两处在【七】最后一并提交
ssh 不通（不在内网）→ 整节登记「⏸ 留步：不在内网」，继续【四】，⛔ 不要等

【四、前置自检】任一不过就停下报告，不要硬上：
0. grep -c 'run-lanes.sh' .claude/settings.json 必须为 2（白名单两条）。为 0 → 停下报告，⛔ 不自改 settings.json、不换写法绕
1. command -v claude
2. bash docs/openers/run-lanes.sh --dry-run
   本批（第九批）预期：**1 条泳道、4 条 opener**：
     ◆ 确认
         ✓ 0904B 正文 28 行   intake第6章确认断点计划
         ✓ 0904C 正文 26 行   intake第6章确认断点开工
         ✓ 0904D 正文 20 行   intake第7章JD溯源与导出计划
         ✓ 0904E 正文 23 行   intake第7章JD溯源与导出开工
   四条严格串行（C 等 B 的 plan；E 等 C 合入 + D 的 plan）。数字对不上 / 任一 ✗ / exit 13 → 停下报告，⛔ 不改编排文件、不 --only 绕
3. git status --short 记基线（【二】后应干净，或只有【三】6 的两处文档改动）；git status -sb 记 ahead
4. 🔴 pytest 基线：./venv/bin/python -m pytest -q 2>&1 | tail -3（python 不在 PATH；⛔ 不抄任何历史数字）。有失败项 → 停下报告，不发车
5. ls docs/superpowers/plans/ | grep -c "unit6-approval\|unit7-jd" 应为 0
6. grep -c '^- \[x\]' openspec/changes/m1-job-profile-intake/tasks.md 应为 33

【五、后台启动，不要前台等】
  nohup bash docs/openers/run-lanes.sh --full-auto --yes > /tmp/run-lanes-boot.log 2>&1 &
  echo $!
（在仓库根直接跑，⛔ 不加 cd 前缀。）记下 PID，从 /tmp/run-lanes-boot.log 读日志目录。本批不加 --chain，估 3–4 小时。

【六、轮询盯守】每 3-5 分钟查一次，直到泳道收敛：
  LOGDIR=<日志目录>; cat "$LOGDIR/results.tsv" 2>/dev/null; ls -la "$LOGDIR"/*.log; ps -p <PID> >/dev/null && echo RUNNING || echo EXITED
每轮播报一行。预案（不要问我）：
- PARTIAL → 正常，找「⏸ 留步」记原因继续盯。已知合法留步：C 的「6.8 无定时基础设施」
- FAIL / NO-SENTINEL → 泳道停、后面不跑。读末尾 50 行判真失败还是哨兵漏输出。**若日志里是 `API Error: 529 Overloaded`**（09-03 晚撞过两次）→ 查 status.claude.com，
  事故解除后用 `--only <剩余编号逗号分隔>` 重跑一次（这是 09-03 实证有效的路径，⛔ 不改编排文件），日志目录会换新的，两个都记进报告
- NO-BODY → 停止盯守，`cat "$LOGDIR/manifest.tsv" | od -c | head` 贴给我
- 「调不到 superpowers」→ 已知（无头取不到插件，昨天 10 次），按预案手工走；记下各条是停了还是走完了，报告单列有无两阶段 review
- 超过 4.5 小时仍未收敛 → 不 kill，记录现状出报告

【七、跑完的真身核验 —— 逐条跑，不许跳】
  cd /Users/paulshao/Projects/HumanResource && git pull --rebase origin main
1. git log --oneline -30 对照 results.tsv（B：plan；C：合并+回勾；D：plan；E：合并+回勾）
2. git status --short 与基线比
3. git worktree list；逐分支真未合：
   for b in $(git branch --format='%(refname:short)' | grep -v '^main$'); do echo "$b rev-list=$(git rev-list --count main..$b) 真未合=$(git cherry -v main $b | grep -c '^+')"; done
4. ls docs/superpowers/plans/ 应恰好多出 unit6-approval-checkpoint 与 unit7-jd-grounding-and-export 两个，`### Task` 数 ≥ 5 / ≥ 3
5. checkbox：grep -c '^- \[x\]' openspec/changes/m1-job-profile-intake/tasks.md —— C OK → 42；C、E 都 OK → 45；按各自 OK/PARTIAL 分别判，⛔ 不一律按满分
6. pytest 与【四】4 基线比：≥ 开跑前且 0 失败
7. ls data/ —— C/E 若拉过临时库应已拷回主工作区；git status 里不得出现 data/ 下任何文件
8. git status -sb ahead 应为 0；> 0 补推
9. 把【三】6 的两处文档改动提交：git add docs/audit-and-outbound-ops.md docs/session接力.md → commit -m "ops(.51): 三次发版 <commit>（含 TD-9 修复）" → push
10. ⛔ 本批不做任何 worktree 清理

【八、出看护报告】写成 .claude/handoff/lanes-<时间戳>-看护报告.md（gitignore 内，不提交），含：
  - 编号 / 脚本判定 / 真身核验结论 / 分歧
  - `.51` 发版结果一行（commit / 快照 / 冒烟 / 或留步原因）
  - PARTIAL 留步原因逐条；红灯清单（Task 数不足、真未合 ≠ 0、pytest 失败、data/ 进 git、ahead ≠ 0）
  - superpowers 是否真调到（B/C/D/E 各一行）
  - 下一步：**一条推荐动作 + 理由**（同结果的做法只写一种；只有结果不同且属 Shao Peishen 必须拍的才列选项并标推荐）

【九、收工】把报告路径和那张表贴给我。
