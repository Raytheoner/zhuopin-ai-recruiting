# `[Mac]0918U` 语音探针泳道续跑：核实 Task 4–6 真实状态 → 补完 → Task 7 → 收口

【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: lane-voice-u0-probe（**复用现有 worktree，⛔ 不新建**）｜ worktree: ✅ 勾 ｜ 工作区: .claude/worktrees/lane-voice-u0-probe ｜ 派发: Cowork·HR业务线-接力0918G ｜ 模型: Sonnet

## 零、为什么（`0918O` NO-SENTINEL 的根因，有 json 实证）

`0918O`（语音探针 Task 4–6）判 **NO-SENTINEL**，但它**不是崩了**：`语音探针建造-0918O.json` 里 `subtype=success`、`is_error=false`、`terminal_reason=completed`、`num_turns=2`、`subagent_stats.spawned=6 / started_in_background=6 / completed=5`，`result` 停在一句「Still running the probe process itself now. I'll wait for the implementer's next notification.」——**它把六个子代理全丢到后台，然后自己结束了回合去等通知**。`claude -p` 无头模式下没有下一回合，通知永远不来，于是哨兵没打印、泳道按判据停摆，`0918P`（Task 7 与收口）没发车。

实证补充（Cowork·`0918G` 只读核过）：`scripts/probe_m3_voice.py` 在 worktree 里**已有** `p3-cosyvoice`／`p4-llm-ttft`／`p5-sdk-compat` 三个注册子命令（文件 mtime 10:50:08，在 `0918O` 运行窗口内），说明后台子代理确实写出了东西。但**提交与否、测试有没有补、review 有没有跑，全部未知**。

## 一、🔴 本条最重要的铁律（`0918O` 的根因，违反即重演）

⛔ **不得把任务交给后台子代理之后结束回合。** 所有子代理一律前台等待到完成（拿到它们的返回再继续）；⛔ 不使用「先起后台、等通知」的模式；回合结束前**必须由本会话自己**顶格打印 `OPENER_DONE` 或 `OPENER_PARTIAL: <原因>`。⛔ 也不得把打印哨兵这件事交给子代理。

## 二、Task A：先核实真实状态（⛔ 不许假设 `0918O` 干完了，也不许假设它什么都没干）

在 worktree 里逐条查清并在收工报告里列出：

1. `git log --oneline main..HEAD`：本分支已有哪些提交（`0918N` 的 Task 1–3 六个提交是已知基线）。
2. `git status --porcelain`：有没有未提交的改动（后台子代理很可能写完没提交）。
3. `scripts/probe_m3_voice.py` 里 P3／P4／P5 三个探针各自：是否完整、有没有 `write_result` 路径、异常是否被兜住（`0918N` 的两轮 fix 立过这个规矩）。
4. `tests/test_probe_m3_voice.py`：P3／P4／P5 有没有对应用例；`PYTHONPATH=. venv/bin/pytest tests/test_probe_m3_voice.py -v` 实跑报数。

按核实结果分流：**已完成且质量达标的不要重做**；缺什么补什么。每个补完的 Task 照 `0918N` 的规矩走一轮 review → fix → re-review。

## 三、Task B：Task 7 探针总结

按 `openspec/changes/voice-structured-interview/tasks.md` 1.7 与计划 `docs/superpowers/plans/2026-09-18-voice-structured-interview-unit0-probe.md`，写 `docs/m3-voice-probe.md`：

- P1–P5 每项一个「通过／阻塞＋原因」结论（已知：P1 LiveKit 实机通过、P2 FunASR 因无真实 30 秒中文语音样本判阻塞，属预期内）；
- 「结论」节写 **live 段：可做／留步** 与**目标机规格建议**（CPU/GPU、内存、带宽），依 1.2/1.3 的资源占用实测数据；
- 🔴 语音主机采购与预算是**不可代项**（tasks 0.4，Shao Peishen 决策）：本条只给规格建议与依据，⛔ 不做采购结论、不写价格承诺。

## 四、Task C：收口

全量 `pytest` 0 失败（报数）→ final whole-branch review → ff 合 main → push → 真合校验（`git rev-list --count main..branch` = 0）→ 清 worktree 与分支 → 回勾 `tasks.md` 1.1–1.7 与 0.2（0.2 的判据是 `docs/m3-voice-probe.md` 对 P1–P5 各有结论），同步抬头进度行。

【并发协议】仓库此刻有别的泳道在跑（M2 U2.5 建造 `0918S`／`0918T`，触碰 `app/web/server.py`、`app/web/static/*.html`、`tests/test_resume_*.py`，与本条零重叠）：只 `git add` 本条产出的路径（`scripts/probe_m3_voice.py`、`tests/test_probe_m3_voice.py`、`requirements-m3-u0.txt`、`docs/m3-voice-probe.md`、该包 `tasks.md`）；⛔ 禁 `git add -A`／`git add .`／`git commit -a`／`git stash`；push 被拒 → `git pull --rebase --autostash origin main` 重试 ≤3 次；报 `.git/index.lock` → 等 5 秒重试 ≤5 次，⛔ 绝不删锁。

收工三行：Task A 的核实结论（`0918O` 实际留下了什么）｜ 合入 commit ＋ passed·failed ｜ `docs/m3-voice-probe.md` 里 P1–P5 的五个结论各一句 ＋ live 段判定。最后一行由**本会话自己**顶格输出 `OPENER_DONE` 或 `OPENER_PARTIAL: <原因>`。⛔ 不提问（无人值守）、不越闸、不自行改台账状态放行。
