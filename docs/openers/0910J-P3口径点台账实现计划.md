[Mac]0910J-P3口径点台账实现计划
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（spec-to-plan 只产出一份 markdown 计划，不写代码不建分支——skill 明写「在这一步建 worktree 只会多一层目录切换的混乱」）｜ 工作区: 仓库根（/Users/paulshao/Projects/HumanResource）｜ 派发: Cowork·HR业务线-接力0909AU

> 本文件是 `[Mac]0910J` 的正文（引用式 opener）。第十七批 · 波次 1 · **四条并行 plan 泳道之一**。
> 目标＝为 `liaison-reply-bridge-and-patrol` 的交付单元 **P3 · 口径点台账** 出一份 Superpowers 实现计划。
> ⛔ **本条不写任何产品代码**、⛔ 不建分支、⛔ 不勾 `tasks.md` 任何一项。

【零、开工前门槛 —— 两条，任一不过就停下报，⛔ 不许绕过】

1. 🔴 **§0 三门槛必须已全勾**：`grep -n "0\.[123]" openspec/changes/liaison-reply-bridge-and-patrol/tasks.md`
   三条都是 `- [x]`。**只要有一条还是 `- [ ]` ⇒ 停下报**，⛔ 不许"先出计划反正不写代码"——
   本包 tasks.md 抬头写死「§0 三条门槛全部勾完之前，§1–§5 ⛔ 不得开工」，出计划也在 §1–§5 里。
   （三门槛＝TD-42 真实验证 ／ SDK `message` 接线 ／ 一条真实入站落库，由 `[Mac]0910B`＋`[Mac]0910A` 清）
   🔴 **本条门槛 2026-09-10 由 Shao Peishen 答 `1a` 明确不放行，⛔ 不许再提「先出计划反正不写代码」**：
   `[Mac]0910B` 的收工报告要回答「帧字段映射走的是**实际字段**还是 **fail-closed**」，那个答案**直接决定**
   P0 第九态与 P1 dispatch 计划里的字段名 ⇒ 门槛没清就写计划＝在猜，返工的是整份 plan。
2. `Skill(superpowers:writing-plans)` 可达。回 `Unknown skill` ⇒ **登记「⏸ 留步：superpowers 不可达」并停**，
   ⛔ **不许按磁盘 SKILL.md 手工走**（那条旧预案 2026-09-09 已作废）、⛔ 不许自己装插件。

【一、按 `spec-to-plan` skill 走，⛔ 不要自己另发明流程】

调 `Skill(spec-to-plan)`，变更名 `liaison-reply-bridge-and-patrol`，交付单元 **P3 · 口径点台账**。
🔴 **输入只能是 spec**：`openspec/changes/liaison-reply-bridge-and-patrol/specs/liaison-criteria-ledger/spec.md`
＋ `design.md` 取技术决策。⛔ **绝不把 `tasks.md` 喂给 `writing-plans`**（粒度差一个数量级，`scripts/task-brief` 会解析失败）。
`tasks.md` 的 **§4（4.1–4.3）** 只用来划范围，⛔ 不作输入。

【二、Global Constraints —— 从 `CLAUDE.md` 逐字复制，本单元相关的是这些】

- 🔴 **合规红线同族纪律（本单元的核心约束）**：AI 只做排序推荐、不做自动淘汰，淘汰必须有**人工确认节点并留痕**。⇒ 口径点的 `已签认` 是一次人工确认，**必须带 evidence**，⛔ **绝不许有「超期自动签认」这种分支**。这条要用**形状断言**钉死：源码 AST 里不存在按时间改写状态为 `已签认` 的分支；argparse ⛔ 无 `--auto` / `--expire` / `--before` / `--older-than` 类选项。
- **铁律 4 同族**：每条 `criterion_score` 必须有 `evidence_ref`，为空不允许写入。⇒ 本单元：`已签认` 缺 evidence ⇒ **退出码 3 且文件逐字节不变**。
- **数据模型**：台账真身是 markdown 文件本身，⛔ 不 import `storage.db`（spec「子命令不碰库」）；状态转换用纯函数 `compute_criteria_transition`。
- **沟通口径**：对外正式文档一律用全名 **Shao Peishen**，⛔ 不用「Paul」。

⚠️ 逐字进计划的 Global Constraints 段。**这不是形式主义**——`subagent-driven-development`
会把这段原样交给 reviewer 当注意力透镜；铁律不进计划，reviewer 查不出缺陷，而那个失败是**静默的**。

【三、计划落位与自查】

计划存 `docs/superpowers/plans/2026-09-10-liaison-criteria-ledger.md`（⛔ 就这一个文件，别的一个字都不写）。

交付前逐条自查，⛔ 不许写「均已自查」：

- `grep -c '^### Task ' <计划文件>` **不为 0**，且等于实际任务数。🔴 **必须三级 `### Task N: `**，
  二级 `## Task N:` 会让 `scripts/task-brief` **静默返回空**（2026-08-19 实证：1900 行 7 任务，三级标题数 0）
- 有 **Global Constraints** 段，内容与【二】逐字一致
- `specs/liaison-criteria-ledger/spec.md` 里**每条 `### Requirement:`** 都能指到至少一个 Task（逐条列出对应关系）
- 每个 Task 有确切文件路径、完整代码、确切命令与预期输出；⛔ 无 TBD／TODO／「适当处理错误」类占位
- 前后 Task 的类型名、函数签名、字段名一致
- **每个有副作用的动作独占一个 Task 步骤**且带幂等键（第一铁律）
- 端到端提取验证：`scripts/task-brief docs/superpowers/plans/2026-09-10-liaison-criteria-ledger.md 1` 能取出非空正文

【四、并发协议 —— 本批四条 plan 泳道同时在跑，逐条照办】

1. **只 `git add` 本条明确列出的路径**：`docs/superpowers/plans/2026-09-10-liaison-criteria-ledger.md` 与本 opener 文件。
   ⛔ 禁止 `git add -A` ／ `git add .` ／ `git commit -a` ／ `git stash`（含 `-u`，会卷走别的泳道未提交的工作）
2. `git status` 里出现别人的改动是**正常的**，不要停下、不要问、不要顺手提交
3. push 被拒 → `git pull --rebase --autostash origin main` 后重试，最多 3 次。⛔ 不要在 commit 前 pull
4. 报 `.git/index.lock` 已存在 → 等 5 秒重试最多 5 次，⛔ **默认绝不删除该锁**（孤儿锁三项判据见
   `docs/findings/2026-08-26-index-lock-孤儿锁判据.md`，少一项都按「别人在用」处理）

**并行同伴的触碰区**（⛔ 一个字都不要碰）：
- `[Mac]0910G`（P0）在写 `docs/superpowers/plans/2026-09-10-liaison-reply-bridge.md`
- `[Mac]0910H`（P1）在写 `docs/superpowers/plans/2026-09-10-liaison-unpack-dispatch.md`
- `[Mac]0910I`（P2）在写 `docs/superpowers/plans/2026-09-10-liaison-unpack-charter.md`

【五、守住】

- ⛔ 不写任何产品代码（`tools/`、`app/` 一个字节都不动）
- ⛔ 不建 worktree、不建分支
- ⛔ 不勾 `tasks.md` 任何一项（回勾是 run-build 的收口动作）
- ⛔ 不改 `openspec/` 下任何文件（要动 spec 文本 ⇒ 停下报，回全流程）
- ⛔ 不碰 `.env`、`data/`、`_to_delete/`；⛔ 不重启值守服务；⛔ 不请任何人发消息

【六、收工】提交（只 add 上面两条路径）→ `git push origin main`。报告给出：
§0 三门槛的实际 grep 输出；`Skill(superpowers:writing-plans)` **是否真调到**（这一栏必须明说）；
计划文件路径与 `grep -c '^### Task '` 的值；spec 每条 Requirement → Task 的对应表；
`scripts/task-brief` 提取验证结果；commit hash。

**收工单列一行**：`⏸ 下一步：P3 · 口径点台账 的 run-build opener 由 Cowork 按本计划文件另出（波次 2，worktree ☑，四条串行）`
