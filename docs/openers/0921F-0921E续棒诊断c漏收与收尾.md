# [Mac]0921F（＝`0921E` 的续棒条）决策点 c 漏收诊断 ＋ 测试与队列收尾

## 零、为什么

`0921E` 跑到上下文 170,799（越 `0920H` 的 150k 硬闸）被注入转场哨兵，判 `CTX-RELAY` 收尾。
**已完成并已 commit＋push 在分支上**：① 附件只走私信口径；② 未知附件帧键名取证落盘
（`frames.py` 新增 `frame_chattype`／`frame_key_types`，`__main__.py` 接线，现有测试同步修复，
`tools/liaison/tests` 全量 1201 passed）。上一条 commit＝`dfcf8cd`。

⚠️ 本条是手工排的续棒：`run-lanes.sh` 的自动续棒这次**没有把续棒条写进编排文件**
（`--only 0921E` 单条批次，批次收工后编排里查无 `0921E续1`）——该缺口已登记，不在本条修。

**本条只做 `0921E` 未完的三件**：③ 决策点 c 漏收诊断、④ 新测试、⑤ 队列并入 `Q-01`。
⛔ 不重做①②、⛔ 不改已落盘的 `frames.py` 取证逻辑。

## 一、前置核验（不过即顶格 `OPENER_PARTIAL` 并停）

1. 进同一 worktree：`.claude/worktrees/lane-0921e-attach-probe`，分支 `lane-0921e-attach-probe`。
2. `git log lane-0921e-attach-probe --oneline | head -3` 能看到 `dfcf8cd`。看不到 ⇒ 停下报。

## 二、动作

③ **诊断决策点 c 漏收**：2026-09-20 17:04:09 与 17:04:39 两条 `@MAC机器人` 文本进了归档，
   截图证实还有第三条「`人事部#2` 决策点c 答复／选择：2」**未进**。查入站日志判成因
   （时间窗／去重键／群帧过滤／服务存活任一）。结论写进 `docs/session接力.md` 一行，
   含【谁做】【状态】【判据】【不做会怎样】四列。**只诊断，⛔ 不改判定逻辑**——改法等结论出来再单独派。
④ **补测试** `tests/test_attachments_unknown_frame_dump.py`（`0921E` 若已建则补齐断言）：
   产出文件只含键名与类型名、**不含任何值**；群帧附件被忽略且不抛错；单聊未知附件帧仍 fail-closed
   且留下结构文件。narrow 跑该文件，⛔ 不跑全量。
⑤ **队列**：把「决策点 c 漏收」与「附件只走机器人私信」**并入既有 `Q-01` 行**的问题列，
   ⛔ 不另立新行、⛔ 不改 `Q-01` 的状态与答复列。
⑥ 提交 ③④⑤，`merge --ff-only` 回 main 并 push，清 worktree。

## 三、机器判据

```bash
set -e
cd "$(git rev-parse --show-toplevel)"
venv/bin/python -m pytest tests/test_attachments_unknown_frame_dump.py -q
grep -q '只走私信' docs/roadmap/定夺队列.md
grep -q '决策点 c' docs/session接力.md
python3 -c "import tools.liaison.frames as f; assert len(f.ATTACHMENT_FIELD_PATHS_BY_MSGTYPE)==0"
```

## 四、并发协议

本批只此一条。

## 五、红线

🔴 一个附件原值都不许落盘或进日志（url／aeskey／filename／字节全禁）。
⛔ 不放开 fail-closed、不猜字段、不填 `ATTACHMENT_FIELD_PATHS_BY_MSGTYPE`（等真实私信附件帧）；
⛔ 不对外发送；⛔ 不碰 `.51`；⛔ 不动 `unpack-signal.json` 与 `archive/`。

## 六、收口

四条机器判据全过 ＋ ③ 结论已入接力卡 ⇒ **顶格** `OPENER_DONE`；否则顶格 `OPENER_PARTIAL: <实测>`。
