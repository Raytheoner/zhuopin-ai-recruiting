# [Mac]0920M `.51` 依赖冒烟（Q-08）——只读取数，⛔ 不发版

## 零、为什么

`Q-08`（`tasks.md` 1.1／U0 计划「待裁决」#4）等的是一次 `.51` 同款 Windows venv 的依赖探针：
`scripts/smoke_m2_deps.py` **只 import、不下载模型、不调用**，结果决定两件事——PaddleOCR 走不走 D14 退路、
M2 发版白名单要补哪些依赖。它一直挂在「需 `.51` 访问」，而 `0920R` 今天实证 Mac 一直在 LAN、`.51:8095` 可达
（此前的 off-LAN 判定来自挂载侧代理 403 假阴），前提已不成立，故派本条。

他答的是 (a)：用 CC opener 走 ssh 在 `.51` 上跑一次。**这不是发版。**

## 一、开工自核（任一不过即顶格 `OPENER_PARTIAL` 并停）

1. `git rev-parse --abbrev-ref HEAD` ＝ `main`；`worktree ❌`，在仓库根跑。
2. `ssh -o BatchMode=yes -o ConnectTimeout=8 zp51 "echo ok"` 回 `ok`。
   不通 ⇒ 顶格 `OPENER_PARTIAL: ssh zp51 不通（<实测报错>）` 并停，⛔ 不改 `~/.ssh/config`、⛔ 不试 RDP。
3. 远端存在被测脚本：
   `ssh zp51 "powershell -NoProfile -Command \"Test-Path C:\apps\zhuopin-recruit-agent\scripts\smoke_m2_deps.py\""` 为 `True`。
   为 `False` ⇒ 顶格 `OPENER_PARTIAL: .51 上无 smoke_m2_deps.py，需先随发版同步` 并停，
   🔴 **⛔ 不许自行跑 `sync-to-server.sh` 补文件——同步属发版类，不可代。**

## 二、动作

① **`.51` 侧跑冒烟**（远端唯一允许的写入＝它自己的 `data\eval\m2-pilot\smoke-win51.json`）：

```
ssh zp51 "powershell -NoProfile -Command \"cd C:\apps\zhuopin-recruit-agent; .venv\Scripts\python.exe -m scripts.smoke_m2_deps --json data\eval\m2-pilot\smoke-win51.json\""
```

② **取回产物**：`scp zp51:'C:/apps/zhuopin-recruit-agent/data/eval/m2-pilot/smoke-win51.json' data/eval/m2-pilot/`

③ **Mac 侧同跑一次做对照**：`venv/bin/python -m scripts.smoke_m2_deps --json data/eval/m2-pilot/smoke-mac.json`

④ **把两边输出贴进** `docs/m2-model-comparison.md` 的「环境」节（脚本 docstring 指定的落点），
   并在该节末尾写**两行结论**，逐字回答 `Q-08` 要的两问：
   - `PaddleOCR: 可装／不可装 ⇒ D14 退路 走／不走（依据：<实测字段>）`
   - `发版白名单需补：<依赖清单，或「无」>`

⑤ **回填定夺队列** `Q-08`：状态列 → `已答`，答复列 → `已提供：0920M 实跑，见 data/eval/m2-pilot/smoke-win51.json 与 docs/m2-model-comparison.md「环境」节`。

⑥ 提交 ④⑤（含 `data/eval/m2-pilot/smoke-*.json`，若被 `.gitignore` 挡则 `git add -f`）并 `push`。

## 三、机器判据

```bash
set -e
cd "$(git rev-parse --show-toplevel)"
test -f data/eval/m2-pilot/smoke-win51.json
python3 -c "import json;d=json.load(open('data/eval/m2-pilot/smoke-win51.json'));assert isinstance(d,(dict,list)) and d"
grep -q 'PaddleOCR' docs/m2-model-comparison.md
grep -q '发版白名单需补' docs/m2-model-comparison.md
grep -qE '^\| Q-08 .*已答' docs/roadmap/定夺队列.md
```

## 四、并发协议

本批只此一条，无并行同伴。⛔ 不要与任何 `.51` 部署类条目同批。

## 五、红线

🔴 **这是只读取数，不是发版。** ⛔ 不跑 `sync-to-server.sh`；⛔ 不在 `.51` 上 `pip install`／改 venv／重启服务；
⛔ 不改 `.51` 上除 `data\eval\m2-pilot\smoke-win51.json` 之外的任何文件；⛔ 不碰门户与现网配置；
⛔ 不发任何对外消息；⛔ 依赖缺失本身就是结论，照实记，不许"顺手装上让它过"。

## 六、收口

`git rev-list --count main..HEAD` ＝ 0；push 成功且机器判据全过 ⇒ **顶格** `OPENER_DONE`。
任一不过 ⇒ **顶格** `OPENER_PARTIAL: <哪条没过、实测值多少>`，⛔ 不许打 `OPENER_DONE`（`0920I` 的教训）。
