[Mac]0908J-51四次发版重试
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（在仓库根跑 sync-to-server.sh，改文档提交 main）｜ 工作区: 仓库根

> 本文件是 `[Mac]0908J` 的正文（引用式 opener）。🔴 `.51` 发版是不可代项：依据＝Shao Peishen 2026-09-08 回「补 tzdata」（对 `0908B` 失败回滚的处置裁决）。
> 没有这句授权 ⛔ 一步都不要跑。

【零、这次和 0908B 差在哪（先读，否则会重蹈覆辙）】
`0908B` 同样的代码（`6bb1d90`）发过一次，**服务起不来已回滚**：`.51` venv 缺 `tzdata`，
`app/storage/job_queries.py:62` 顶层 `ZoneInfo("UTC")` 在 import 期抛 `ZoneInfoNotFoundError`。
完整根因见 `docs/findings/2026-09-08-51四次发版回滚.md`。

本次唯一的新增动作＝**发版前先在 `.51` 装 `tzdata`**（§二.2）。
🔴 **顺序不能颠倒**：先装依赖、再 sync。装依赖对正在跑的旧代码是惰性的（没有任何代码 import 它），
服务不用停；⛔ 反过来先 sync 会重启到缺依赖的新代码上，白白再停摆一次。

⚠️ `requirements.txt` 本次**有改动**（加了 `tzdata==2026.3`，`cadb97d` 之后的提交），
所以 §一.4 那条「diff 必须为空」的闸门在本轮**不适用**，⛔ 不要照抄 `0908B` 的前置核对。
⛔ 仍然不重跑 `deploy-server.ps1`（它会连带重注册计划任务与防火墙，爆炸半径没必要）——
按 `05-发布运行手册.md` 阶段 D 的口径，依赖变动只需**重跑装包步骤**。

【一、前置核对（任一不过即停）】
1. `git status -sb`：main 与 origin 同步或仅 ahead；`git log -1 --format=%h main` 记下（发版的就是它）
2. `ssh zp51 "echo ok"` 回 ok；不通 → 整条登记「⏸ 留步：不在内网」停
3. `git show HEAD:requirements.txt | grep tzdata` 必须命中 `tzdata==2026.3`（没命中说明拿错分支，停）
4. 现网当前是健康的（发版前的基线，回滚后应为 200）：
   `curl -s -o /dev/null -w "%{http_code}\n" --max-time 15 http://192.168.100.51:8095/hr/recruit-agent/`
   不是 200 → 停下报 Shao Peishen，⛔ 不要在已经不健康的现网上叠发版

【二、快照 → 装依赖 → 发版 → 冒烟】
1. 快照（在 .51 上跑）：
   ssh zp51 "powershell -NoProfile -Command \"\$ts=Get-Date -Format yyyyMMdd-HHmm; \$b='C:\apps\backups\'+\$ts; New-Item -ItemType Directory -Force \$b | Out-Null; Copy-Item 'C:\apps\zhuopin-recruit-agent\app' \$b\app -Recurse; Copy-Item 'C:\apps\zhuopin-recruit-agent\data' \$b\data -Recurse; Write-Output \$b\""
2. 🔴 **先装依赖**（在 .51 上跑，服务不用停）：
   ssh zp51 "powershell -NoProfile -Command \"cd C:\apps\zhuopin-recruit-agent; .venv\Scripts\python.exe -m pip install tzdata==2026.3\""
   立刻验证，**这两条必须都过才准往下走**：
   ssh zp51 "powershell -NoProfile -Command \"cd C:\apps\zhuopin-recruit-agent; .venv\Scripts\python.exe -c 'from zoneinfo import ZoneInfo; print(ZoneInfo(\\\"UTC\\\"), ZoneInfo(\\\"Asia/Shanghai\\\"))'\""
   期望打出 `UTC Asia/Shanghai`。报 `ZoneInfoNotFoundError` → 装包没生效，停下报，⛔ 不要硬着头皮 sync
3. 发版命令**由 Shao Peishen 点 Run**（09-04 实证：Auto Mode 分类器拦 AI 直接调 `sync-to-server.sh`，白名单无效）。
   把下面原样作为 bash 块发给他，等他回结果：
   ```bash
   ./sync-to-server.sh
   ```
   末尾必须 HTTP 200 + 「发版完成」
4. 冒烟 5 项（逐项记原文，任一不过即走 §二.5）：
   ① `curl` 首页 → 200
   ② 页面相对资源逐个 `curl` → 无 404
   ③ `app.log` 尾 60 行无 `Traceback` / `OperationalError`，且能看到本次新进程的 `Application startup complete`
   ④ `curl -s http://192.168.100.51:8095/hr/recruit-agent/api/jobs` 返回 **JSON 列表**（8.1 新端点）
      ⚠️ 回滚态下这里是 **405**（旧代码只收 POST）——**405 就是没发上去**，⛔ 不要当成通过
   ⑤ `ssh zp51` 查 `data\demo.db` 的 `sqlite_master` 里 `hard_requirement` 与 `human_review` 两张表都在
5. 任一不过 → 回滚：`Remove-Item app -Recurse -Force` → `Copy-Item <本次快照>\app` 回去 →
   `schtasks /end` + `/run` → `curl` 200 → 追写进 `docs/findings/2026-09-08-51四次发版回滚.md` 新开一节，停。
   ⛔ `data\` 不回滚。⛔ `tzdata` 装了就不用卸（对旧代码惰性）

【三、巡检 CLI（9.6 上线后的预期变化，⛔ 不是失败）】
ssh zp51 "powershell -NoProfile -Command \"cd C:\apps\zhuopin-recruit-agent; .venv\Scripts\python.exe -m app.audit.assertions --db data\demo.db --mirror data\audit\decisions.jsonl; Write-Output ('EXIT=' + \$LASTEXITCODE)\""
- 镜像不存在 → EXIT=2，同 09-03，原样记
- 🔴 EXIT=1 且违例来自断言四 → **这是 0904I 预告的预期结果**：留痕上线前就已确认/放弃、既无 `effect_log`
  也无 `human_review` 的历史行，9.6 把它们从"被豁免"翻成 fail-closed 违例。正确处置是**人工逐条核实**，
  ⛔ 不改数据、⛔ 不把豁免线往后挪。
  把违例行（`job_id` / `version` / `status` / `created_at`）逐条抄进 `docs/audit-and-outbound-ops.md` §五
  新增「2026-09-08 断言四历史行清单（待 Shao Peishen 核实）」。只读取证（列名已于 0908B 核对：
  `human_review(job_id, profile_version, decision_type)`、`job_profile(job_id, version, status, created_at)`，
  引号层数多，建议 `-EncodedCommand`）：
  ssh zp51 "powershell -NoProfile -Command \"cd C:\apps\zhuopin-recruit-agent; .venv\Scripts\python.exe -c \\\"import sqlite3,json; c=sqlite3.connect('data/demo.db'); print(json.dumps(c.execute('select p.job_id,p.version,p.status,p.created_at from job_profile p where p.status in (\\\\\\\"approved\\\\\\\",\\\\\\\"abandoned\\\\\\\") and not exists (select 1 from human_review h where h.profile_version=p.version and h.job_id=p.job_id)').fetchall(),ensure_ascii=False))\\\"\""

【四、落档与提交（只 add 列出的路径）】
1. `docs/audit-and-outbound-ops.md` §五：「2026-09-08 四次发版记录（重试）」——commit / 快照 / 装依赖结果 /
   冒烟 5 项 / 巡检 EXIT 与原文 / 历史行清单。⛔ 不要覆盖已有的那条失败记录，**并列新增**
2. `docs/session接力.md`【一】`.51` 代码行改「已发版 <commit>」；`docs/openers/OP-0820-全量编排.md` 号池 `0908J` 行补结果
3. git add docs/audit-and-outbound-ops.md docs/session接力.md docs/openers/OP-0820-全量编排.md
   ⛔ 禁止 `-A` / `.` / `-a`；泳道在并行改代码，别人的改动不停、不问、不顺手提交；
   接力/编排文件若混有别人的块，`git add -p` 只拿自己的
4. git commit -m "ops(.51): 四次发版 <commit> 重试成功（补 tzdata 后）＋ 巡检首见断言四历史行"；git push origin main
5. push 被拒 → `git pull --rebase --autostash origin main` 后重试，最多 3 次。⛔ 不要在 commit 前 pull
6. 报 `.git/index.lock` 已存在 → 等 5 秒重试最多 5 次，⛔ 默认绝不删该锁（判据见 CLAUDE.md）

【五、汇报】发版 commit / 快照目录 / `tzdata` 装包与 ZoneInfo 验证结果 / 冒烟 5 项逐项 / 巡检 EXIT 与历史行条数 / commit hash / ahead
