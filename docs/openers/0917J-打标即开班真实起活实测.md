[Mac]0917J-打标即开班真实起活实测
【设置】执行环境: CC ｜ Session: 新开（run-lanes 无头起）｜ 分支: main ｜ worktree: ❌ 不勾（要真实服务、`.env`、`tools/liaison/.venv` 与真实库，worktree 里都没有）｜ 工作区: 仓库根 ｜ 派发: Cowork·HR业务线-接力0917D

> 本文件是 `[Mac]0917J` 的正文（引用式 opener）。目标＝`liaison-reply-bridge-and-patrol` tasks.md **§5 真实起活实测**。
> Shao Peishen 2026-09-17 答 `2a` 授权现在开跑；随后说明「不在 Mac 身边，请远程启动」⇒ 改由 Cowork·0917D 经 launchd 请求文件无头发车，重启服务、`--force` 起活均在他授权范围内。
> 🔴 **⛔ 单测全绿不算验收**（tasks.md 抬头写死）。本 session 是**无人值守**的（claude -p）：⛔ 不许提问、⛔ 不许等回复；凡写「停下报」的地方＝落档已得结果后输出 `OPENER_PARTIAL: <原因>` 结束。他本人要做的那一步（5.4 发消息）由 Cowork 在手机侧通知他，本 session 只按时限轮询。
> 🔴 红线：⛔ 不发任何企微消息（含测试消息）、⛔ 不请汤丽萍发消息、⛔ 不改 `.env`、⛔ 不用 `--dangerously-skip-permissions` 绕权限、⛔ 不 push 拆件会话产生的 commit（5.3 判据要求它**未 push**）。
> 解释器一律 `PYTHONPATH=. tools/liaison/.venv/bin/python`（本机没有裸 `python`，TD-45 同一教训）。

【零、先落档（Cowork 写好未提交的件）】
Cowork·0917D 无法在 `.git/` 操作，以下文件由本 session 先提交，再开测：
1. `git status --short` 应含：`?? docs/openers/0917J-打标即开班真实起活实测.md` ｜ `M docs/openers/号池台账.md` ｜ `M docs/openers/OP-0820-全量编排.md` ｜ `M docs/session接力.md`。缺本文件以外的某个 ⇒ 记一行继续；缺本文件 ⇒ 不可能（你正在读它）
2. `git add` 这四个路径（⛔ 禁 `-A`／`.`／`commit -a`／`stash`；⛔ 不 add `_to_delete/`）→ `git commit -m "docs(openers): 派 0917I 测试红灯泳道 ＋ 0917J §5 真实起活实测（0917J）"` → push（被拒 ⇒ `pull --rebase --autostash` 重试最多 3 次；`index.lock` 在 ⇒ 等 5 秒重试最多 5 次，⛔ 不删锁）
3. ⚠️ 此刻可能有 `[Mac]0917I` 泳道在 worktree 里跑、结束时会 ff 合并进 main——`git status` 里出现别人的改动正常，不动

【一、前置自检——任一不过即停下报】
1. `head -1 openspec/changes/liaison-reply-bridge-and-patrol/tasks.md` 含 `28/33`；`grep -n '^- \[ \] 5\.' …/tasks.md` 五条都未勾
2. `grep -rn 'P2-TODO\|CHARTER_RELPATH' tools/liaison --include=*.py | grep -v /tests/` 零命中（`0917H` 已合入）
3. `grep -n '人事部#1' docs/跟进信/README-跟进信清单.md` 那一行的状态列——记下原状态（5.3 要看它进第九态后接原状态）。不是在途态 ⇒ 5.3 前提不成立，报告里标出，5.2/5.4 照做
4. 记基线（报告要用）：
   `sqlite3 data/liaison.db "SELECT kind, COUNT(*) FROM liaison_unpack_audit GROUP BY kind; SELECT COUNT(*) FROM liaison_message;"`
   `ls -la data/liaison/unpack-signal.json data/liaison/unpack-session.lock 2>&1`

【二、5.1 重启值守服务】
1. `launchctl print gui/$UID/com.zhuopin.hr.liaison | grep -Ei 'state|pid|stderr|stdout'` 找到 stderr 日志路径，记下当前行数
2. `launchctl kickstart -k gui/$UID/com.zhuopin.hr.liaison`；等 30 秒
3. 再 `launchctl print …` 确认 `state = running` 且 pid 变了；stderr 日志新增行里 `grep -c ERROR`——冷启动那两条已知噪声除外，有新 ERROR ⇒ 抄原文、停下报，⛔ 不往下
4. 通过 ⇒ 勾 5.1

【三、5.2 P1 单独验】
1. `PYTHONPATH=. tools/liaison/.venv/bin/python -m tools.liaison unpack-dispatch --dry-run` → 抄 claude 路径与 argv。「找不到 claude 二进制」⇒ 阻断，停下报
2. `PYTHONPATH=. tools/liaison/.venv/bin/python -m tools.liaison unpack-dispatch --force`（⚠️ 会真起一个无头会话，这是本条验收要求）
0. 进度信号：`echo "5.2-running $(TZ=Asia/Shanghai date +%H:%M:%S)" > .claude/handoff/0917J-progress.txt`
3. 立即：`cat data/liaison/unpack-session.lock`，取 pid，`ps -p <pid>` 可见
4. 等它结束（每 30 秒 `ps -p <pid>` 一次，最多 10 分钟），然后 `ls -t data/liaison/logs/unpack-headless/ | head -1` 取最新日志，`head -20` 抄进报告
5. 判据：日志显示会话已起并以 `[NO-SIGNAL]` 结束。⚠️ `--force` 路径传的是**占位章程**（`unpack_cli.py` docstring 写明），所以「读到章程」这半在 5.2 里只能看到占位文本——如实记录，⛔ 不当成失败，也 ⛔ 不写成「已读到真章程」（真章程在 5.3 的桥路径上验）
6. 🔴 日志若是登录态／权限问题 ⇒ 在 `docs/tech-debt.md` 登记阻断项并停下报，⛔ 不改权限模式绕过
7. 通过 ⇒ 勾 5.2

【四、5.4 D1 反证（先于 5.3，因为 5.3 要等真实入站）】
1. 先写进度信号供 Cowork 读：`echo "5.4-waiting $(TZ=Asia/Shanghai date +%H:%M:%S) max_id=$(sqlite3 data/liaison.db 'SELECT COALESCE(MAX(rowid),0) FROM liaison_message')" > .claude/handoff/0917J-progress.txt`（Cowork 会据此在手机上通知 Shao Peishen 发一条消息）
2. 轮询：每 30 秒查一次 `liaison_message` 是否出现 rowid > 上面 max_id 的新行，**最多 30 分钟**。发送人判定：新行的 sender 对应邵培申（按 `tools/liaison` 白名单配置里的姓名映射取；取不到映射则记下 userid 如实写）。30 分钟无新行 ⇒ 5.4 记「⏸ 未收到反证消息」，⛔ 不判失败，直接进【五】【六】落档后输出 OPENER_PARTIAL: 5.4 窗口内未收到消息。有新行后再等 60 秒，查：
   `sqlite3 data/liaison.db "SELECT id, kind, sender_userid, letter_number, created_at FROM liaison_unpack_audit ORDER BY id DESC LIMIT 5; SELECT COUNT(*) FROM liaison_message;"`（列名以 `tools/liaison/storage/schema.py` 为准，对不上就先 `.schema liaison_unpack_audit`）
3. 判据：`liaison_message` 比基线 +1；新增一条 `bridge_skipped_no_inflight`；无 `dispatch_started`；`git diff --stat docs/跟进信/README-跟进信清单.md` 为空
4. `liaison_message` 没 +1 ⇒ 入站本身没到（TD-42 形态），抄 stderr 新增行、停下报，⛔ 不判 5.4
5. 通过 ⇒ 勾 5.4

【五、5.3 端到端——等汤丽萍真实入站，⛔ 不催不请】
- 本 session 不等待。在 `docs/session接力.md`【下一步】R-3 行写明：5.1/5.2/5.4 结果 ＋「5.3 待汤丽萍下一条真实入站；到达后查：台账 `人事部#1` 进第九态且原状态接后、审计 `bridge_marked`＋`dispatch_started` 各一、信号文件一项、无头日志走完一轮并 `--clear --before`、`git log` 有会话 commit 且未 push」
- 若开测期间她恰好已有入站：当场按上面判据核，通过即勾 5.3

【六、落档与提交】
1. `docs/findings/2026-09-17-打标即开班真实起活实测.md`：逐步写命令、时刻（`TZ=Asia/Shanghai date`）、SELECT 原文输出、日志前 20 行、判定；5.3 未到就写「⏸ 待真实入站」
2. `tasks.md`：勾通过的项，抬头 `28/33` 按实际勾数更新。⛔ 5.3 未过不勾 5.5、⛔ 不归档（归档在 5.3 通过后当场跑 `openspec-archive-change`）
3. `git add` 仅：findings 文件、`tasks.md`、`docs/session接力.md`（若登记了 TD 再加 `docs/tech-debt.md`）→ commit `docs(liaison): §5 真实起活实测 5.1/5.2/5.4（0917J）` → push
4. `echo "done $(TZ=Asia/Shanghai date +%H:%M:%S)" > .claude/handoff/0917J-progress.txt`
5. 收工三行：5.1/5.2/5.4 各自 ✅/❌ ｜ commit ｜ ⏸ 下一步：5.3 等汤丽萍真实入站
6. 全部 5.1/5.2/5.4 通过（5.3 待入站属预期）⇒ 最后一行顶格输出 OPENER_DONE；否则 OPENER_PARTIAL: <哪步>。不加粗、不加任何符号
