[Mac]0909AT-打标即开班需求收敛与立包
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（只产出 openspec 文档，不写代码）｜ 工作区: 仓库根 ｜ 派发: Cowork·HR业务线-接力0909Q

> 本文件是 `[Mac]0909AT` 的正文（引用式 opener）。目标＝把 win 端「打标即开班」机制**收敛成需求并立包**，
> ⛔ **一行代码都不写**。依据＝`docs/findings/2026-09-10-win端打标即开班机制核验与HR移植方案.md`（本会话核 win 端真身六处所得）。
> 🔴 主工作区有真实值守服务在跑，本 session 只产出 `openspec/changes/` 下的文档，⛔ 不碰 `tools/`、⛔ 不碰 `data/`。

【一、先跑需求收敛】用 `zhuopin-requirement-grill`（新场景开工前的第一道把关）。
🔴 **本场景至少有四条判据必须问清楚，⛔ 不许由起草者代拍**：
1. **第九态的语义边界**：`📨 回件已到，待拆件` 仍属在途、串行闸仍锁——这条照搬 win；
   但**我们的入站不只承接回件**（还承接岗位需求）。⇒ 「名单内且该发送人有在途信 ⇒ 标；**无在途信 ⇒ 怎么办**」
   这一分支 win 端没有，必须问出判据。⛔ 不许默认「一律标」——那会把新岗位需求误标成对某封信的回复
2. **回件归属的判据**：win 端靠**串行原则**（同一收信人只有一封在途）免去显式关联。
   ⇒ 我们的串行原则是否足够强？一人多封在途时怎么办？跨部门/多专员时呢？
3. **无头会话的红线清单**（对应 win 章程 §〇）：`--dangerously-skip-permissions` 意味着**没有工具确认闸**，
   全靠提示词层的红线兜。HR 侧至少要问：对外发送（`send-followup`）／建造／新裁决／改 spec 与 design／
   碰 `data/` 与 `.env`／改 `whitelist.yaml` —— 哪些一律不进池
4. **worktree 隔离**：值守服务由 launchd 常驻在**主工作区**并持有 `data/liaison.db`；
   拆件会话若也在主工作区跑会抢库。⇒ 在 worktree 内跑，还是限定「只碰 `docs/`」？谁来保证？

⚠️ grill 的纪律：**能查的事实自己查掉**（代码、findings、spec 里有的⛔ 不要拿去问他），
只把**只有他能定的**摆上去。产出＝可直接充当 `design.md` Decisions 的需求树。

【二、再立包】`openspec-propose`，产出 proposal / design / specs / tasks，⛔ 不写实现代码。
包名建议 `liaison-reply-bridge-and-patrol`（最终以 openspec 规范为准）。设计要点（依据 findings，⛔ 不要重新发明）：
- **P0 桥＋第九态**：入站落库后按串行原则定位在途信，写第九态到 `docs/跟进信/README-跟进信清单.md`，
  **原状态原样接在后面**（可溯源）。复用 `followup.py::compute_backfilled_ledger` 的行定位与回写代码
- **P1 打标即开班**：同一次调用里 ① 标第九态 ② 落信号 ③ **非阻塞**起无头会话；零轮询。
  并发守卫用 **pid 判活**（macOS ＝ `kill -0` / `ps -p`），🔴 **查询失败一律归到「不存活」一侧**（代价不对称）；
  坏锁按陈旧锁处理照常起活；**fail-open 只对「起没起来」负责、⛔ 从不读写信号文件**，四类失败只记审计不上抛
- **P2 章程正本**：落在仓库内、由**单点常量**解析路径，🔴 **代码里⛔ 不留副本**；运行时整段读出拼进 prompt；
  单测断言「章程原文逐字出现在 prompt 结尾且其前有前言」
- **P3 口径点台账**：`已签认` **必须带 evidence**、🔴 **⛔ 无任何超期自动签认路径**
- 🔴 **载体不换**：`liaison_task` 保持 DB，⛔ 不引入 markdown 队列＋编辑锁＋hook。
  **移植的是流程与红线，⛔ 不是载体**
- 🔴 **验收纪律写进 tasks**：起活类机制**必须有一次真实起活的实测记录**，⛔ 单测全绿不算验收

【三、守住】⛔ 只动 `openspec/changes/<新包>/`（新建）与 `docs/session接力.md`（回填一行进度）。
⛔ 不碰 `tools/`、`app/`、`scripts/`、`data/`、`docs/跟进信/`、`docs/tech-debt.md`、现有变更包的 `tasks.md`。
⛔ 不写任何实现代码、⛔ 不改任何测试、⛔ 不起任何进程。

【四、与 `0909AS` 的关系】AS 在还 TD-42（连接假死），触碰 `session_client.py`；本条只写文档 ⇒ **触碰区零重叠，可并行**。
⚠️ 但**依赖关系要写进 proposal**：TD-42 未还之前，「打标即开班」建在一条**会假死且看不出来**的通道上 ⇒
tasks 里把「TD-42 已还并真实验证」列为本包开工的**前置**。

【五、收工】报告含：grill 收敛出的需求树（四条判据各自的结论与理由）、立包路径、`openspec` 校验结果、
以及**哪些问题他还没答、卡在哪一条**——⛔ 不许替他填。
