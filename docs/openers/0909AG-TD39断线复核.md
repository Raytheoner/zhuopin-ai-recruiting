[Mac]0909AG-TD39断线复核
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（起真实进程、要读真实 .env 与真实库，必须在主工作区）｜ 工作区: 仓库根（/Users/paulshao/Projects/HumanResource）｜ 派发: [Mac]0909AE

> 本文件是 `[Mac]0909AG` 的正文（引用式 opener）。**两件事，一次跑完**：
> ① 验证 TD-38 修好之后心跳真的是 30 秒（不再被企微限流）；
> ② 复核 TD-39——SDK 判连接死亡时，断线事件到底有没有到 `LiaisonSession`。
> 依据：`docs/tech-debt.md` TD-38／TD-39、`docs/findings/2026-09-09-首次真实建连实测.md`。
> Shao Peishen 2026-09-09 答 `2a` 授权本条作为独立一条、排在 TD-38 之后、8.6 灰度之前。
>
> 🔴 **本条需要你本人在场**（要手动关/开 Wi-Fi 制造真实断线），⛔ 不是无人值守任务。
> ⛔ 全程不主动发任何消息、⛔ 不装 launchd（Shao Peishen 答 `3a`：本条验完再议）。

【网络节奏】🔴 **进程全程不停**——断网与复网都在服务活着的时候做，这才是在测"断线重连"。
⛔ **不是**「停掉进程 → 断网 → 再起」，那样测的是"起不来"，不是断线恢复。

| 阶段 | 网络 | 时长 | 你要做的 |
|---|---|---|---|
| 第一段：起进程、确认心跳＝30000ms | ✅ 联网 | ~90 秒 | 什么都不做，只看日志 |
| 第二段前半：制造断线 | 🔌 **你手动关 Wi-Fi** | 90 秒 | 关掉并记下时刻 |
| 第二段后半：验重连闭窗 | ✅ **你手动开回 Wi-Fi** | 120 秒 | 开回来并记下时刻 |

⚠️ 断网期间 CC 自己的网络也断了——**这是预期的**，⛔ 不要因此中止本条。断网那 90 秒里
只做本机操作（`cat liveness.json`、`sqlite3`、看日志），⛔ 不要试图 `git pull` / 联网搜索。
提交留到最后一段复网之后再做。

【一、前置闸门】🔴 任一条不过就**停下报，⛔ 不许往下跑**。
1. `git pull --rebase --autostash origin main`，然后
   `grep -n "DEFAULT_HEARTBEAT_MS" tools/liaison/session_client.py`
   —— **查不到就是 TD-38 还没合入，停。** 本条的全部前提是心跳已改对：没改对就去连，
   44 秒又会吃一次 `45009`，你复现的是限流不是断线，⛔ 白跑。
2. `grep -n "~~TD-38~~" docs/tech-debt.md` 应能看到销账行。
3. `ls -l data/liaison.db data/liaison/liveness.json` —— 上一轮（`0909AE`）的残留还在是正常的。
   **⛔ 不要删库**：`liaison_outage_window` 是否新增记录，正是本条的判据，需要对照。
   先记下当前行数：`sqlite3 data/liaison.db "select count(*) from liaison_outage_window;"`
4. 确认 Wi-Fi 现在是**连着的**，且你**人在机器前**（本条要你手动断网）。

【二、第一段·先确认心跳修好了】🔴 前台跑，别后台。
```
PYTHONPATH=. tools/liaison/.venv/bin/python -m tools.liaison
```
连上后**观察 90 秒**，其间什么都不要做（⛔ 不发消息、不 @ 机器人）。判读：
- 日志首行心跳应为 `Heartbeat timer started, interval: 30000ms`（⛔ 不是 `30ms`）
- 90 秒内 `Heartbeat sent` 计数应是**个位数**（约 3 次）。仍是几百上千 ⇒ TD-38 没修对，停下报
- ⛔ 出现 `errcode 45009` ⇒ 立刻停，说明仍在限流（或上一轮的限流还没过期），停下报，⛔ 不要反复重试撞
- ✅ 心跳正常、连接稳定 ⇒ 进入第二段。**进程保持不动，不要停。**

【三、第二段·制造真实断线（TD-39 的核心）】
**你手动关掉 Wi-Fi**，记下关闭时刻。保持断网 **90 秒**，其间观察并记录：
1. 日志有没有出现 `disconnected` / `reconnecting` / `connection considered dead`
2. `data/liaison/liveness.json` 的 `state` 有没有从 `connected` 翻成 `disconnected`，
   翻的时刻距断网多久（另开一个 CC 的 bash 反复 `cat` 即可，⛔ 不要动那个前台进程）
3. `sqlite3 data/liaison.db "select * from liaison_outage_window;"` 有没有**新开**一条窗口

然后**你手动开回 Wi-Fi**，记下恢复时刻，再观察 **120 秒**：
4. 有没有自动重连成功（日志出现 `Authentication successful`）
5. `liveness.json` 有没有翻回 `connected`
6. 那条中断窗口有没有被**闭合**（结束时间被写上）

观察完 Ctrl-C 停（⚠️ 按 TD-37，`client.run()` 吞 KeyboardInterrupt，**要按两次**，
第二次得按在退避窗口里；实在停不下就另开 bash `pkill -TERM -f "python -m tools.liaison"`，
实测 SIGTERM 有效，⛔ 不必上 SIGKILL）。

【四、判读与销账】
- **①②③④⑤⑥ 六项全对** ⇒ TD-39 销账为「虚惊」：`## ~~TD-39~~ · … ✅ 已还（实为虚惊，<commit>）`，
  段里写明当初的疑点来自 16 秒被强杀截断的观察窗，**不是缺陷**
- **任一项不对** ⇒ TD-39 **升级为已确认缺陷**，⛔ 不销账。改写条目：把"待复核"去掉，
  写清具体哪一项不对、实测数据是什么，并标 🔴 阻断 8.6
- ⚠️ **⛔ 不要因为"大体上工作"就销账**——本条守的正是"服务看起来在跑、其实早断了"
  这一类**无症状**故障，六项里任何一项含糊都按不对处理

【五、落证据】写 `docs/findings/2026-09-09-断线重连实测.md`（若已跨日按实际中国日期命名），含：
两段的起停时刻、命令原文、断网/恢复的准确时刻、日志（⛔ 脱敏：不得出现 BotID/Secret/webhook key；
企微报文里的出口公网 IP 打码成 `x.x` 形式）、`liveness.json` 每次变化的原文、
`liaison_outage_window` 前后对照、心跳计数对照（本次 vs `0909AE` 的 1399 次/44 秒）。

【六、并发协议】其它泳道可能同时在跑，逐条照做：
1. 只 `git add` 本条明确列出的路径。⛔ 禁止 `git add -A` / `git add .` / `git commit -a` / `git stash`（含 `-u`）
2. `git status` 里出现别人的改动是正常的，不要停下、不要问、不要顺手提交
3. push 被拒 → `git pull --rebase --autostash origin main` 后重试，最多 3 次。⛔ 不要在 commit 前 pull
4. 报 `.git/index.lock` 已存在 → 等 5 秒重试最多 5 次，⛔ 默认绝不删除该锁

【七、提交】只 add：`docs/findings/2026-09-09-断线重连实测.md`（或实际文件名）、`docs/tech-debt.md`、
本文件 `docs/openers/0909AG-TD39断线复核.md`。⛔ 绝不 add `.env`、`data/`（核 `git status` 里不出现它们）。commit + push。

【八、不要做的】
- ⛔ **不装 launchd**（`tools/liaison/scripts/install_launchd.py` 起 LaunchAgent 属安全配置，由 Shao Peishen 自己在 Terminal 跑；且是否装要等本条结果）
- ⛔ **不主动发任何消息**，包括群通知、私信、测试消息；⛔ 不请任何人发消息配合
- ⛔ 不删 `data/liaison.db`（对照基线要用）
- ⛔ 不改 `session_client.py` / `__main__.py` / `whitelist.yaml` / `.env`——本条只做验证。
  验出问题**只登记不修**，修归另一条 opener

【九、收工】逐个列出本次新增/修改/删除的文件（仓库相对路径，⛔ 不写"等若干文件"），报告含：
心跳是否已是 30000ms、六项判读逐项结论、TD-39 销账与否、commit hash，
并单列一行「⏸ 下一步：launchd 装机是否解禁 —— 需 Shao Peishen 拍板（TD-38 已还且本条六项全过才具备条件）」。
