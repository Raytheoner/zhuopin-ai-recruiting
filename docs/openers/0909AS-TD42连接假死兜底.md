[Mac]0909AS-TD42连接假死兜底
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: 不指定（建 worktree 后 git 自己生成）｜ worktree: ✅ 勾选（改产品代码，须与主工作区正在运行的真实服务隔离）｜ 工作区: worktree ｜ 派发: Cowork·HR业务线-接力0909Q

> 本文件是 `[Mac]0909AS` 的正文（引用式 opener）。目标＝还 **TD-42**：连接假死时看门狗**有判据无把手**，
> `liveness=connected` 会骗人。依据＝`docs/tech-debt.md` TD-42 原文（含 2026-09-10 的十小时实测与日志逐字）。
> 🔴 **主工作区有真实值守服务正在 launchd 下运行**（09-10 09:38 重启，正等汤丽萍回灌）。
> ⇒ 本 session **必须在 worktree 内**改代码与跑单测，⛔ 不碰主工作区、⛔ 不碰 `data/`、⛔ 不重启任何服务。

【一、任务】TDD。开工先 git pull --rebase origin main，`./venv/bin/python -m pytest -q` 记基线。
🔴 **两条都要做，缺一不可**（只做 1 会留下骗人的判据，只做 2 会探到了仍然停不下来）：

1. **不依赖 SDK 事件的兜底停机路径**
   - 现状：看门狗判定假死后要「拿事件循环把手去停」，而「本次连接从未触发过任何 SDK 事件」时**根本没有把手** ⇒ 停不下来
   - 改法方向（自己按代码实情定，把选择理由写进注释与报告）：判定假死后**总能**让进程终止，
     由 launchd `KeepAlive` 拉起重连。⛔ 不许把「能拿到把手」当前提
   - ⚠️ 终止前必须先落一条 ERROR 日志与一条中断告警（沿用 `alerts.py` 现有路径），⛔ 不许静默退出——
     静默退出会把「假死」换成另一种无症状
   - ⚠️ ⛔ 不许无限自杀循环：连续 N 次「起来就假死」要能被看出来（计数落日志或存活戳），
     N 与处置方式你定，写进报告

2. **修判据**：`liveness.json` 增加「距上次**真实 SDK 事件**」的信息（字段名你定，例如 `last_event_at`）
   - 看门狗改看它，⛔ 不再只看 `stamp_at`
   - 🔴 同时更新 `read_liveness_stamp` / `read_liveness_stamp_at` 的调用方，使**外部判据**（含人肉 `cat`）
     也能一眼看出「连着但十小时没收到东西」
   - ⚠️ 兼容：旧格式（无新字段）读到时按「未知」处理并当作**需要关注**，⛔ 不许当成健康

3. **单测**（全部离线、注入替身，⛔ 不建真连、⛔ 不发网络）：
   - 假死场景：存活戳陈旧 ＋ **从未触发过任何 SDK 事件** ⇒ 断言进程被终止（或等价的终止请求被发出），
     且 ERROR 日志与告警各一条。🔴 **这条在修复前必须红**，把红的输出抄进报告
   - `last_event_at` 推进：收到 SDK 事件后该字段前进；十分钟无事件后判据能识别
   - 旧格式 `liveness.json` 读入 ⇒ 判为「需要关注」而非健康
   - 连续假死计数的行为断言

4. `docs/tech-debt.md`：TD-42 打销账行 `~~TD-42~~ ✅ 已还（<commit>）`。
   🔴 ⛔ **不动 TD-39**——它已修好且已验证，本条是另一条路径

【二、守住】⛔ 只动 `tools/liaison/session_client.py`、`tools/liaison/session.py`（仅存活戳字段所需）、
`tools/liaison/tests/test_session_*.py`、`tools/liaison/tests/test_session_liveness.py`、`docs/tech-debt.md` 的 TD-42 一段。
⛔ 不碰 `inbound.py`、`archive.py`、`queue.py`、`notify/`、`retention*`、`logsetup*`、`followup.py`、`storage/*`、
`docs/跟进信/`、`tasks.md`、`app/`、`scripts/`、`.env`。

【三、🔴 不做真实验证】⛔ 不重启服务、⛔ 不 `pkill`、⛔ 不跑 `python -m tools.liaison`。
理由：主工作区那个进程正在等真实回灌，重启会打断；且真实起活验证属不可代项。
交付到**可发布态**即止，在报告末尾写明「⏸ 留步：真实验证由 Shao Peishen 做——重启服务后断网/静默 N 分钟，
确认进程能自己终止并被 launchd 拉起」。🔴 **⛔ 不许拿单测全绿当验收**（win 端同族纪律：起活类必须有一次真实实测）。

【四、收口】合回 main（先 rebase）→ rev-list 与 cherry 都判真合 0；pytest ≥ 基线且 0 失败。⛔ 不归档。

【五、收工】报告含：两条改法各自的选择理由、假死单测的红→绿输出、`liveness.json` 新旧格式各一份原文、
连续假死的处置方式与 N 值、销账行、pytest 前后、commit hash，以及那条留步。
