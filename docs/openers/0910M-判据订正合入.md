[Mac]0910M-判据订正合入
【设置】执行环境: CC ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（要动 main 的合入与主工作区的丢弃，worktree 里两件都做不了）｜ 工作区: 仓库根（/Users/paulshao/Projects/HumanResource）｜ 派发: [Mac]0910L

> 本文件是 `[Mac]0910M` 的正文（引用式 opener）。目标＝把云端分支
> `claude/criteria-correction-commit-3kqfgz`（commit `28499f1`，判据订正）合进 `main`，
> 并丢弃 Mac 主工作区那四条**同题旧改动**。
> 依据：Shao Peishen 2026-09-10 答 `3b`（Mac 侧那批改动丢弃重来，以云端那条为准）、
> 答 `1a`（本条合入由 `[Mac]` CC 做）。台账见 `docs/session接力.md`「判据订正」小节 L-1／L-2。
>
> 🔴 **本条独占 `main`，⛔ 不与任何泳道并行**——它要 merge 进 main。
> 开工前确认没有别的 session 正在往 main 上提交；有 ⇒ 等它收工再开。
> ⛔ 不改任何产品代码、⛔ 不跑 launchd、⛔ 不发任何消息、⛔ 不动 `.env`。

【为什么有两份订正】`[Mac]0910L` 的 opener 按 `[Mac]` 写、工作区写的是主检出路径，
但被投进了 claude.ai 云端容器（全新 clone、工作区干净）⇒ 主工作区那四条未提交改动在云端
根本不存在，三条自检当场不过。改判后订正在云端**重做了一遍**并已提交推送。
于是同一件事有两份：云端的（已进 git，`28499f1`）与 Mac 上的（未提交，内容不保证一致）。
`3b` 的裁决＝**留云端那份，丢 Mac 那份**，⛔ 不许试图合并两份或逐条比对择优——
两份都不长，比对的人力成本高于重做，且比错了不报错。

【一、开工前自检】任一条不过就**停下报，⛔ 不许绕过**。

1. `pwd` 是 `/Users/paulshao/Projects/HumanResource`，`git branch --show-current` ＝ `main`，
   路径**不含** `.claude/worktrees/`。不符 ⇒ 停下报（本条必须在主检出上跑）。
2. `git fetch origin claude/criteria-correction-commit-3kqfgz` 成功，且
   `git log --format=%s -1 origin/claude/criteria-correction-commit-3kqfgz` 的首行是
   `docs(0910L): 前置判据只落内容——订正 0910B 自检② 的历史窗口漂移`。
   不符 ⇒ 停下报，⛔ 不要合一个不认识的分支。
3. `git status --short` 里那四条应该都在（`.claude/skills/kickoff/SKILL.md`、
   `docs/openers/0910B-SDK-message事件接线.md`、`docs/openers/OP-0820-全量编排.md`、
   `docs/session接力.md`）。
   **只有零条**（工作区已干净）⇒ 说明有人先清过了，**跳过【二】直接做【三】**，在报告里写明。
   **有其它文件也是 M/A 状态** ⇒ 正常，别人的泳道在跑，⛔ 不要停、不要问、更不要顺手提交或 `stash`。
4. 判据落在**内容**上，⛔ 不看 `git log` 的第几条：
   `git show origin/claude/criteria-correction-commit-3kqfgz:.claude/skills/kickoff/SKILL.md | grep -c "会随时间漂移"` ≥ 1。
   为 0 ⇒ 停下报（拿到的不是那条订正）。

【二、丢弃 Mac 主工作区那四条旧改动（＝接力 L-2，答 `3b`）】

🔴 **逐条 checkout，⛔ 不许 `git checkout -- .`、⛔ 不许 `git reset --hard`、⛔ 不许 `git stash`**
——工作区里可能还有别的泳道的未提交改动，这三条会一并卷走（`git stash -u` 09-09 已虚惊过一次）。

```
git checkout -- ".claude/skills/kickoff/SKILL.md"
git checkout -- "docs/openers/0910B-SDK-message事件接线.md"
git checkout -- "docs/openers/OP-0820-全量编排.md"
git checkout -- "docs/session接力.md"
```

⚠️ 某条报 `error: pathspec ... did not match`（该文件本来就没改动）⇒ **跳过它继续下一条**，
不是错误，在报告里记一行即可。

自检：`git status --short` 里这四条**全部消失**；其它文件的状态**逐字不变**
（跑之前先 `git status --short > /tmp/before.txt`，跑完 `git status --short | diff /tmp/before.txt -`
应只显示这四行被删掉，⛔ 不应有任何别的差异）。有别的差异 ⇒ **立刻停下报**。

【三、合入 main】

```
git merge --no-ff origin/claude/criteria-correction-commit-3kqfgz -m "merge: [Mac]0910L 判据订正——前置判据只落内容，不落历史窗口"
```

- 用 `--no-ff` 留一条 merge 提交（⛔ 不 rebase、⛔ 不 squash——云端那条已推到远端分支，改写历史会让它对不上）
- 报**冲突**（这四个文件被别人在 main 上又改过）⇒ ⛔ **不要自己挑**，`git merge --abort` 后停下报，
  把冲突文件名与两侧各自的改动摘要写进报告

【四、推送】

```
git push -u origin main
```

被拒 ⇒ `git pull --rebase --autostash origin main` 后重试，**最多 3 次**。
⛔ 不要在 commit／merge 之前 pull——并发下别人的未提交改动会挡住 rebase，`--autostash` 才能带着它们跑完。
报 `.git/index.lock` 已存在 ⇒ 等 5 秒重试最多 5 次，⛔ **默认绝不删除该锁**
（三项孤儿锁判据见 `docs/findings/2026-08-26-index-lock-孤儿锁判据.md` §3，少一项都按「别人在用」处理）。

【五、收工自检】三条全过才算完，逐条把实际输出贴进报告：

1. `grep -c "会随时间漂移" .claude/skills/kickoff/SKILL.md` ＝ **1**（订正已在 `main` 上）
2. `grep -c "git log --oneline -3" docs/openers/0910B-SDK-message事件接线.md` ＝ **1**
   （只剩订正段里那句反例，⛔ 不应还有第二处把它当判据用）
3. `git rev-list --left-right --count origin/main...HEAD` ＝ `0	0`（ahead／behind 均 0）

【六、收工报告】必须含：
- 【一】四条自检的实际输出
- 【二】四条 `git checkout` 各自的结果（含跳过的），以及 `diff /tmp/before.txt -` 的输出
- merge 提交的 hash、`git show --stat` 的文件清单
- 【五】三条收工自检的实际输出

【收工·本条产出的文件清单】本条**不新增、不修改任何文件**——只做 merge 与 checkout，
合入内容全部来自 `28499f1`。⇒ ⛔ 无 `git add` 步骤，⛔ 不要为本条另起 commit。
唯一的新提交是【三】那条 merge 提交，其 message 已在【三】里写死。
