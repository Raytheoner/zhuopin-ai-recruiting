[Mac]0909X-泳道批次看护
【设置】执行环境: CC Desktop ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（你是调度者，实际干活的是脚本起的独立 session）｜ 工作区: 仓库根 ｜ 派发: Cowork·HR业务线-接力0909Q

> 本文件是 `[Mac]0909X` 的正文（引用式 opener）。第十五批看护者（2026-09-09 中午编排，编号 0909S–W）。文件随任务一起提交，即留痕。
> 🔴 本批全是**技术债与机制债**：五条泳道都不走 spec-to-plan / run-build，各自 TDD 直接做。⛔ 你不改代码、不发版、不归档、不装任何 LaunchAgent。
> 🔴 本批两条裁决已由 Shao Peishen 当场拍定（落档 `docs/findings/2026-09-09-Shao-Peishen-裁决-留存冲突B与TD20改法.md`）：冲突 B 取方案 ②、TD-20 取改法 ①。泳道按裁决直接做，⛔ 不要替他重新权衡。
> 🔴 `0909S` 修的正是 launchd 发车 plist 的两个缺陷——**修完要他在 Terminal 重装一次才生效**，所以本批自己大概率还得走「点 Run」退路（见【五】）。这不是故障。

【一、你的角色】
不亲自执行任何 opener。做五件事：提交编排落档 → 前置自检 → 发车 → 轮询盯守 → 跑完真身核验并出报告。
⛔ 不改代码、不跑 spec-to-plan / run-build / openspec-archive-change、⛔ 不跑 `sync-to-server.sh`（`.51` 六次发版是 `0909P`，另一条不可代路，等 Shao Peishen 回「发」）。

【二、先提交 Cowork 侧的编排落档】
git status --short 预期含这五条：docs/session接力.md、docs/openers/OP-0820-全量编排.md、docs/openers/0909X-泳道批次看护.md、docs/findings/2026-09-09-Shao-Peishen-裁决-留存冲突B与TD20改法.md、.claude/skills/kickoff/SKILL.md。
⚠️ 最后那条（kickoff skill 加「Cowork 接力开场词模板」）是上一个 Cowork session 留下的，**一并提交是对的**，⛔ 不要试图拆开。
只 add 这五条路径，⛔ 禁止 git add -A / git add . / commit -a：
  git add docs/session接力.md docs/openers/OP-0820-全量编排.md docs/openers/0909X-泳道批次看护.md docs/findings/2026-09-09-Shao-Peishen-裁决-留存冲突B与TD20改法.md .claude/skills/kickoff/SKILL.md
  git commit -m "docs(openers): 第十五批编排（发车机制 ∥ 留存 ∥ 会话 ∥ 群通知债 ∥ 幂等基座）＋ 两条裁决落档 ＋ kickoff 接力模板"
  git push origin main
push 被拒 → git pull --rebase --autostash origin main 后重试最多 3 次；index.lock 等 5 秒重试 5 次不删。有别的改动 → 单列出来，不提交、不问、继续。

【三、前置自检】任一不过就停下报告，不要硬上：
1. 🔴 pgrep -f 'run-lanes.*\.sh' 必须为空（第十四批 09-09 上午已收敛）。非空 → 停下报告，附 ps -o pid,etime,command
2. 🔴 grep -c "RUN_LANES_COPY" docs/openers/run-lanes.sh ≥ 1；command -v claude；grep -c "run-lanes.sh" .claude/settings.json ≥ 2（必要条件不是充分条件，见 lane-dispatch skill）
3. git pull --rebase --autostash origin main；git status -sb 记 ahead（应 0）；git log -1 --format=%h 记作 <开跑前 main>
4. ls .claude/handoff/launch/ —— 不应有 *.request（有 ＝ 上一份没被消费，先读 launchd.log，⛔ 不叠加写）
5. bash docs/openers/run-lanes.sh --dry-run --only 0909S,0909T,0909U,0909V,0909W
   预期 **5 条泳道、5 条 opener**，末尾「条目总数自检：Σ=N=5（--only 生效，M=6 不参与判据）」（M=6 里多出的那一处是第十三批 `0909B` 的残留标注，BUDGET-HIT 不自动摘、实为虚惊，随它）：
     ◆ 发车机制  ✓ 0909S 正文 18 行
     ◆ 留存      ✓ 0909T 正文 22 行
     ◆ 会话      ✓ 0909U 正文 19 行
     ◆ 群通知债  ✓ 0909V 正文 19 行
     ◆ 幂等基座  ✓ 0909W 正文 19 行
   数字对不上 / 任一 ✗ / exit 13 / exit 15 → 停下报告，⛔ 不改编排文件
6. 🔴 pytest 基线：./venv/bin/python -m pytest -q 2>&1 | tail -3（python 不在 PATH；⛔ 不抄别人的数，也⛔ 不把它写死进任何文件）。有失败项 → 停下报告
7. 记两个实数（不作闸，只记）：grep -c '^## TD-' docs/tech-debt.md；grep -c '^- \[x\]' openspec/changes/hr-wecom-aibot-liaison/tasks.md（本批不碰 tasks.md，跑完这个数应**一字不变**）
8. 看一眼 /usage：五条泳道并行，但全是轻量通道（不走 run-build），单条预算默认 $25 足够。余量不够就把 --max-parallel 降到能承受的数（仍全量 --only），并在报告里写明

【四、launchd 可用性判定】
launchctl print gui/$UID/com.zhuopin.hr.lane-launcher >/dev/null 2>&1; echo $?
- 非 0 → 直接走【五】B 退路
- 为 0 → 走【五】A，但 🔴 **本批对它的成功不抱预期**：09-09 `0909Y` 首跑证明 plist 缺 `AbandonProcessGroup` 会让 run-lanes 被连坐杀，而修它的正是本批的 `0909S`（且要他重装才生效）。按 A 试一次，60 秒内 `.started` 出现但随后 `pgrep` 为空、日志目录没建出来 ＝ 同一个已知缺陷复现，**立刻切 B**，⛔ 不重试第二次、⛔ 不在这上面空耗轮次

【五、发车】
A（launchd 路）：
1. TS=$(date +%Y%m%d-%H%M%S)；记下
2. 用 **Write 工具**（⛔ 不用 Bash heredoc——走 Bash 又回到分类器那条路）写 .claude/handoff/launch/<TS>.request，内容就一行：
   --full-auto --yes --only 0909S,0909T,0909U,0909V,0909W --max-parallel 5
3. 轮询最多 60 秒：ls .claude/handoff/launch/<TS>.*
   - <TS>.started → cat 它读 pid= 与 boot_log=；再等 60 秒确认 pgrep -f 'run-lanes.*\.sh' 非空**且**日志目录已建出来（只认半角冒号「日志目录：」那行）。两者任一不成立 ＝ 连坐杀复现 → 切 B
   - <TS>.rejected → cat 贴进报告，停下报告
   - <TS>.deferred → 与【三】1 矛盾，停下报告
   - 60 秒无任何文件 → cat .claude/handoff/launch/launchd.log 尾 30 行贴进报告 → 切 B
B（退路：点 Run）：把下面这段原样作为 bash 代码块发给 Shao Peishen，并附一句「`0909S` 正在修这个缺陷，他改完后在 Terminal 跑一次 `python3 /Users/paulshao/Projects/HumanResource/scripts/install_lane_launcher.py` 即根治」。🔴 命令⛔ 不带 echo "PID=$!"（粘进 zsh 会卡在 dquote>，09-09 实测）；日志路径带批次号，别冲掉上一批：
```bash
cd /Users/paulshao/Projects/HumanResource && nohup bash docs/openers/run-lanes.sh --full-auto --yes --only 0909S,0909T,0909U,0909V,0909W --max-parallel 5 > /tmp/run-lanes-b15.log 2>&1 &
```
贴完等他点，⛔ 不要自己再用 Bash 起一次（分类器会拦，09-04 已证白名单无效）。

【六、轮询盯守】每 5 分钟：
  LOGDIR=<日志目录>; cat "$LOGDIR/results.tsv" 2>/dev/null; ls -la "$LOGDIR"/*.log; pgrep -f 'run-lanes.*\.sh' >/dev/null && echo RUNNING || echo EXITED
每轮播报一行。预案（⛔ 不要问我）：
- PARTIAL → 正常，找「⏸ 留步」记原因继续盯。已知合法留步：`0909S` 的「等 Shao Peishen 在 Terminal 重装 launchd」；`0909W` 的「随 0909P 上 .51」；`0909V` 的「TD-26 未做，下一批」
- FAIL / NO-SENTINEL → 该泳道停、别的不受影响。读末尾 50 行；`API Error: 529` → 查 status.claude.com，解除后按同样方式重发剩余编号（新日志目录另记）
- `Budget limit reached` 中断在合并前 → ⛔ 不重开；报告里写 worktree 名与未合 commit 数，推荐「利旧 worktree 续跑收口」
- 🔴 results.tsv 里同一编号出现两行 → 立即停止盯守，贴 head -5 <boot_log> 给我
- NO-BODY → 停止盯守，cat "$LOGDIR/manifest.tsv" | od -c | head 贴给我
- 🔴 `docs/tech-debt.md` 合并冲突（T/U/V/W 四条都要销自己的账）→ 泳道自己按「各保留自己那几段」解；你只记录。解错了（吞掉别人的销账行）由真身核验第 5 条抓
- 「调不到 superpowers」→ 本批不走 run-build，本该用不到；真出现就记一行
- 超过 3 小时仍未收敛 → 不 kill，记录现状出报告（本批全是轻量通道，估 1–2 小时）

【七、跑完的真身核验 —— 逐条跑，不许跳】
  cd /Users/paulshao/Projects/HumanResource && git pull --rebase --autostash origin main
1. git log --oneline -40 对照 results.tsv（S/T/U/V/W 各 1 条合并）；git status --short 与基线比；git status -sb ahead 应 0（> 0 补推）
2. 逐分支真未合：for b in $(git branch --format='%(refname:short)' | grep -v '^main$'); do echo "$b rev-list=$(git rev-list --count main..$b) 真未合=$(git cherry -v main $b | grep -c '^+')"; done
3. 🔴 落点守卫（git diff --stat <开跑前 main>..main -- <路径>）：
   - `app/` 只有 `app/storage/idempotency.py` 一个文件被改（来自 `0909W`）；其它四条都写死 ⛔ 不碰 app/
   - `scripts/` 只有 `install_lane_launcher.py`（来自 `0909S`）
   - `docs/openers/run-lanes.sh` 与 `docs/openers/lane-launcher.sh` **零改动**
   - `openspec/changes/hr-wecom-aibot-liaison/tasks.md` **零改动**（本批不回勾）
   - `tools/liaison/storage/` 与 `requirements.txt` / `pyproject.toml` / `.env.example` **零改动**
4. 🔴 边界守卫（git log --format=%s <开跑前 main>..main -- <路径> 看归属）：`retention.py`+`logsetup.py` 只被留存泳道改过；`session.py` 只被会话泳道改过；`notify/` 只被群通知债泳道改过；`design.md` 只被留存泳道改过；两份 spec 各只被一条泳道改过（`liaison-message-archive` ← 留存；`liaison-channel-session` ← 会话）
5. 🔴 销账核对：grep -c '✅ 已还' docs/tech-debt.md 比基线多 **6** 条（TD-20 / TD-25 / TD-27 / TD-30 / TD-33 / TD-34），且 TD-23 / TD-24 / TD-26 仍**未**销账（它们不在本批）。少一条就点名是哪条泳道没销
6. 🔴 咬住证据在位（这四条是本批的全部价值，逐条确认报告里有）：
   - `0909T`：三个桶（含新桶）各自的单测；新桶要求"两个时间都过期"的边界用例
   - `0909U`：TD-20 复现场景（10:10/10:40/12:00）告警区间是 110 分钟、⛔ 不是 30 分钟
   - `0909V`：`93000`/`40001`/`NULL` 三类都不进自动重发口径
   - `0909W`：修复前第 ① 条用例的**红**输出（没有它就等于没验）
7. pytest 与【三】6 基线比：≥ 开跑前且 0 失败；另单独跑 `./venv/bin/python -m pytest -q tests/test_effect_idempotency_suite.py tests/test_graph_idempotency.py` 必须全绿（TD-33 影响所有 effect）
8. 核 `grep -rc '<<<<<<<' docs/tech-debt.md` 为 0；git status 里不得出现 data/ 下任何文件
9. ⛔ 本批不做 worktree 清理；⛔ 不跑 install_lane_launcher.py / install_launchd.py；⛔ 不发版

【八、出看护报告】写成 .claude/handoff/lanes-<时间戳>-第十五批看护报告.md（gitignore 内，不提交），含：
  - 前置状态（dry-run 原文 + pytest 基线 + TD 条目数基线）
  - 编号 / 脚本判定 / 真身核验结论 / 分歧
  - 🔴 发车路径实证一段：走的是 A 还是 B，A 失败的话失败在哪一步（本批是 `0909S` 修复前的最后一次取证，正反都写）
  - PARTIAL 留步逐条；红灯清单（真未合 ≠ 0、pytest 失败、落点/边界守卫、销账数 ≠ +6、同编号双行、tasks.md 被动过、data/ 进 git、ahead ≠ 0）
  - 下一步：**一条推荐动作 + 理由**（同结果只写一种；只有结果不同且属 Shao Peishen 必须拍的才列选项并标推荐）

【九、收工】把报告路径和那张表贴给我。
