# [Mac]0920J 补 `0920I` 漏做的两处 ＋ 修一处与实现不符的措辞

## 零、为什么

`0920I` 报 `OPENER_DONE` 并已合 main（`lane-watch-relay` §4 两行确已加上），但**四条完成判据里有一条实测未过**：

- 判据 2「`grep -c 'upeak' SKILL.md` ≥ 1」⇒ **实测 0**。§2 的「13 列／第 13 列是 `upeak`」注释与 §3 的「越线点名」两处**都没做**。
- 另有一处**与 `0920H` 实际实现不符**：新加的 `CTX-RELAY` 行写「已自动在同泳道**队尾**排了续棒」，而 `0920H` 收工报告明确记载实现为「本泳道**下一条先跑**续棒」（理由：泳道内串行正因触碰区重叠，字面队尾会让后一条跑在半成品上，该偏离已被采纳）。照现在的措辞写下去，下一个看护者会按错误的执行顺序去核对。

本条只补这三处，⛔ 不重做 `0920I` 已做对的部分。

## 一、做什么

只改 `.claude/skills/lane-watch-relay/SKILL.md`：

1. **§2 只读接手**：读 `results.tsv` 的那条命令后面补一句说明——
   `results.tsv 现为 13 列，第 13 列 upeak ＝ 该 session 上下文峰值（0920G 起）。`
2. **§3 逐条报**的「状态同步」项里补一句——
   `本批 upeak 若有任一条 ≥150k，必须点名是哪几条、峰值多少；run-lanes.sh 收工汇总已打 🔁 行与越线提示，照抄即可。`
3. **修措辞**：把 `CTX-RELAY` 那一行里的「同泳道队尾」改成「**本泳道下一条**（续棒条 `<原id>续<n>` 插在原条之后、其它条之前）」。其余文字不动。

## 二、开工自核

1. `grep -c 'upeak' .claude/skills/lane-watch-relay/SKILL.md` ＝ **0**（若已非 0，说明有人补过，**停下登记**）。
2. `grep -c 'CTX-RELAY' .claude/skills/lane-watch-relay/SKILL.md` ＝ **1**（`0920I` 的成果在）。
3. `git status --short` 里该文件不在改动列表。

## 三、完成判据 —— 🔴 逐条实跑并把命令与输出抄进收工报告，⛔ 不许凭印象判过

```
grep -c 'upeak' .claude/skills/lane-watch-relay/SKILL.md          # 须 ≥ 2
grep -c '同泳道队尾' .claude/skills/lane-watch-relay/SKILL.md      # 须 = 0
grep -c '本泳道下一条' .claude/skills/lane-watch-relay/SKILL.md    # 须 ≥ 1
grep -c 'CTX-RELAY' .claude/skills/lane-watch-relay/SKILL.md      # 须 = 1（不新增）
grep -c '^## ' .claude/skills/lane-watch-relay/SKILL.md           # 须 = 6
wc -c < .claude/skills/lane-watch-relay/SKILL.md                  # 须 ≤ 9216
```

🔴 **任一条不符 ⇒ 顶格输出 `OPENER_PARTIAL: <哪条没过、实测值多少>`，⛔ 不许输出 `OPENER_DONE`。**
（`0920I` 就是在一条 grep 判据为 0 的情况下打了 `OPENER_DONE`——本条把判据写成可直接粘贴执行的一段，就是为了不再复现。）

## 四、并发协议（逐字内嵌）

1. 只 `git add` `.claude/skills/lane-watch-relay/SKILL.md` 与 `docs/openers/0920J-*.md`。
   ⛔ 禁止 `git add -A` / `git add .` / `git commit -a` / `git stash`（含 `-u`）。
2. `git status` 里出现别人的改动是正常的，不要停下、不要问、不要顺手提交。
3. push 被拒 → `git pull --rebase --autostash origin main` 后重试，最多 3 次。⛔ 不要在 commit 前 pull。
4. 报 `.git/index.lock` 已存在 → 等 5 秒重试最多 5 次，⛔ 默认绝不删除该锁。

## 五、红线

⛔ 不改 `run-lanes.sh`、`context-guard.py`、任何测试；⛔ 不碰其他 skill；⛔ 不碰 `.51`、不对外发送；⛔ 不动未追踪文件。

## 六、收口

`venv/bin/python -m pytest tests/test_doc_size_budget.py -q` 通过后提交 + push，`main..origin/main` 为 0。
收工报告**必须逐条抄上面六条命令的实际输出**。顶格打印哨兵。
