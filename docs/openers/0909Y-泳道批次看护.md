[Mac]0909Y-泳道批次看护
【设置】执行环境: CC Desktop ｜ Session: 新开 ｜ 分支: main ｜ worktree: ❌ 不勾（你是调度者，实际干活的是脚本起的独立 session）｜ 工作区: 仓库根 ｜ 派发: Cowork·HR业务线-接力0903B

> 本文件是 `[Mac]0909Y` 的正文（引用式 opener）。第十四批看护者（09-08 晚编排，编号 0909H–O）。文件随任务一起提交，即留痕。
> 🔴 **本批是第一批走 launchd 请求文件发车的**（`0909G` 09-08 已装，`bootstrap rc=0`）：你写一个文件，起进程的是 launchd 不是你。⛔ 不再贴 Run 按钮（那是 launchd 未装时的退路，见【四】末尾）。
> 🔴 本批与第十三批（`0909Z` 在另一个 session 看护）**前后衔接、不重叠**：本 opener 会自己等第十三批的 `run-lanes.sh` 退出再发车。可以现在就贴，不必等 `0909Z` 收工。

【一、你的角色】
你不亲自执行任何 opener。做五件事：提交编排落档、**等第十三批收敛**、前置自检、**写请求文件发车**、轮询盯守 + 跑完真身核验并出报告。
⛔ 不改代码、不跑 spec-to-plan / run-build / openspec-archive-change。本批不发版（`.51` 六次发版是 `0909P`，另一条不可代路）。

【二、先提交 Cowork 侧的编排落档】
git status --short 预期含：docs/session接力.md、docs/openers/OP-0820-全量编排.md、docs/openers/0909Y-泳道批次看护.md、docs/openers/0909P-51六次发版幂等撞键修复.md。
⚠️ `OP-0820-全量编排.md` 里同时带着第十三批 run-lanes 自动摘标注的改动（`> ✅ 已完成 … 原泳道 归档队列/连接`），一并提交是对的，⛔ 不要试图拆开。
⚠️ `docs/superpowers/plans/2026-09-09-*-unit4-*.md` / `unit7-*.md` 若还是 untracked——那是第十三批泳道自己要提交的，⛔ 不碰。
只 add 这四条路径，⛔ 禁止 git add -A / git add . / commit -a：
  git add docs/session接力.md docs/openers/OP-0820-全量编排.md docs/openers/0909Y-泳道批次看护.md docs/openers/0909P-51六次发版幂等撞键修复.md
  git commit -m "docs(openers): 第十四批编排（群通知 ∥ 留存 ∥ 日志 ∥ 运行守护 ∥ 技术债，launchd 请求文件发车）＋ 0909P 发版 opener 备好"
  git push origin main
push 被拒 → git pull --rebase --autostash origin main 后重试最多 3 次；index.lock 等 5 秒重试 5 次不删。有别的改动 → 单列出来，不提交、不问、继续。

【三、等第十三批收敛（预案覆盖，⛔ 不问我）】
每 5 分钟跑一次：pgrep -f 'run-lanes.*\.sh' >/dev/null && echo RUNNING || echo EXITED
- RUNNING → 顺手 cat .claude/handoff/lanes-20260908-200757/results.tsv 播报一行（第十三批 A/B/C/D/E/F 各到哪了），继续等。上限 5 小时（第十三批 20:07 发车，估 3–4 小时）
- EXITED → 进【四】。⛔ 不管第十三批是 OK / PARTIAL / FAIL——那是 `0909Z` 的活；本批用 `--only` 把它残留的标注隔在外面。只把 results.tsv 全文抄进本批报告的「前置状态」段
- 超过 5 小时仍 RUNNING → 不 kill，出一份「未发车」报告停下，写明 `ps -o pid,etime,command -p $(pgrep -f 'run-lanes.*\.sh')`

【四、前置自检】任一不过就停下报告，不要硬上：
0. 🔴 launchctl print gui/$UID/com.zhuopin.hr.lane-launcher >/dev/null 2>&1; echo $? —— 必须为 0。非 0 → 走本节末尾「退路」
1. 🔴 grep -c "RUN_LANES_COPY" docs/openers/run-lanes.sh ≥ 1；command -v claude；pgrep -f 'run-lanes.*\.sh' 为空
2. git pull --rebase --autostash origin main（第十三批可能刚合入第 4/5/7 章）；git status -sb 记 ahead（应 0）；git log -1 --format=%h 记作 <开跑前 main>
3. bash docs/openers/run-lanes.sh --dry-run --only 0909H,0909I,0909J,0909K,0909L,0909M,0909N,0909O
   预期 **5 条泳道、8 条 opener**，末尾「条目总数自检：Σ=N=8（--only 生效，M=… 不参与判据）」（M 是文件里全部标注数，含第十三批残留，随它）：
     ◆ 群通知    ✓ 0909H 正文 23 行 ／ ✓ 0909I 正文 25 行
     ◆ 留存      ✓ 0909J 正文 20 行 ／ ✓ 0909K 正文 22 行
     ◆ 日志      ✓ 0909L 正文 19 行 ／ ✓ 0909M 正文 22 行
     ◆ 运行守护  ✓ 0909N 正文 19 行
     ◆ 技术债    ✓ 0909O 正文 17 行
   数字对不上 / 任一 ✗ / exit 13 / exit 15 → 停下报告，⛔ 不改编排文件
4. 🔴 pytest 基线：./venv/bin/python -m pytest -q 2>&1 | tail -3（python 不在 PATH；⛔ 不抄别人的数）。有失败项 → 停下报告
5. 记第十三批留下的实数（不作闸，只记）：grep -c '^- \[x\]' openspec/changes/hr-wecom-aibot-liaison/tasks.md；按章：for c in 4 5 6 7 8; do echo "ch$c=$(grep -c "^- \[x\] $c\." openspec/changes/hr-wecom-aibot-liaison/tasks.md)"; done
   ⚠️ ch5 ≠ 10 或 ch7 ≠ 9 时 `0909I` 会自己「⏸ 留步」（它开工前确认第 2 条），不是你的红灯，照发
6. ls docs/superpowers/plans/ | grep -c "unit6-group-notify\|unit8-1-retention\|unit8-4-logging" 应为 0
7. ls .claude/handoff/launch/ —— 不应有 *.request（有 ＝ 上一份请求没被消费，先读它的 launchd.log 再说，⛔ 不叠加写）
8. 看一眼 /usage：**五条泳道并行**，三条 run-build 各按 $25 上限估；余量不够就把 `--max-parallel 5` 降到能承受的数（仍全量 --only），并在报告里写明

【五、发车——写请求文件，⛔ 不调 Bash 起脚本、不贴 Run 按钮】
1. TS=$(date +%Y%m%d-%H%M%S)；记下
2. 用 **Write 工具**（⛔ 不用 Bash heredoc——走 Bash 又回到分类器那条路）写文件 .claude/handoff/launch/<TS>.request，**内容就一行、无引号、无换行以外的东西**：
   --full-auto --yes --only 0909H,0909I,0909J,0909K,0909L,0909M,0909N,0909O --max-parallel 5
3. 轮询最多 60 秒：ls .claude/handoff/launch/<TS>.*
   - 出现 <TS>.started → cat 它，读 pid= 与 boot_log=；从 boot_log 头几行读日志目录（只认半角冒号「日志目录：」那行；第一行「▶ 自拷贝执行」不是）
   - 出现 <TS>.rejected → cat 它贴进报告（参数没过白名单），停下报告
   - 出现 <TS>.deferred → 第十三批还没真退出（与【三】判据不一致），等 5 分钟后换新 TS 重写一份 .request，最多 3 次；仍 deferred → 停下报告
   - 60 秒什么都没出现 → cat .claude/handoff/launch/launchd.log 尾 30 行贴进报告；launchd 没触发 → 走「退路」
退路（仅当【四】0 非 0 或 launchd 没触发）：把下面这段原样作为 bash 代码块发给 Shao Peishen 点 Run，并附一句「Terminal 跑一次 python3 /Users/paulshao/Projects/HumanResource/scripts/install_lane_launcher.py 可根治」：
```bash
cd /Users/paulshao/Projects/HumanResource && nohup bash docs/openers/run-lanes.sh --full-auto --yes --only 0909H,0909I,0909J,0909K,0909L,0909M,0909N,0909O --max-parallel 5 > /tmp/run-lanes-boot.log 2>&1 & echo "PID=$!"
```

【六、轮询盯守】每 5 分钟：
  LOGDIR=<日志目录>; cat "$LOGDIR/results.tsv" 2>/dev/null; ls -la "$LOGDIR"/*.log; ps -p <pid> >/dev/null && echo RUNNING || echo EXITED
每轮播报一行。预案（不要问我）：
- PARTIAL → 正常，找「⏸ 留步」记原因继续盯。已知合法留步：I 的「真实投递留步：等群 webhook 写进 .env」；I 开工前确认 ch5/ch7 不满而留步
- FAIL / NO-SENTINEL → 该泳道停、别的不受影响。读末尾 50 行；`API Error: 529` → 查 status.claude.com，解除后换新 TS 写一份 .request，内容 `--full-auto --yes --only <剩余编号逗号分隔> --max-parallel 5`（记得带上其泳道内的后续编号），新日志目录另记
- `Budget limit reached ($25)` 中断在终审/合并前 → ⛔ 不重开；报告里写 worktree 名与未合 commit 数，推荐「利旧 worktree 续跑收口」
- 🔴 results.tsv 里同一编号出现两行 → 立即停止盯守，贴 head -5 <boot_log> 给我
- NO-BODY → 停止盯守，cat "$LOGDIR/manifest.tsv" | od -c | head 贴给我
- 「调不到 superpowers」→ 已知，按预案手工走；记下各条是停了还是走完了
- __main__.py 合并冲突（留存 vs 日志）→ 泳道自己按「两处都保留」解，你只记录；解错了（丢了一头）真身核验第 8 条会抓
- 超过 4.5 小时仍未收敛 → 不 kill，记录现状出报告

【七、跑完的真身核验 —— 逐条跑，不许跳】
  cd /Users/paulshao/Projects/HumanResource && git pull --rebase --autostash origin main
1. git log --oneline -60 对照 results.tsv（H/J/L：各 1 个 plan；I/K/M：各 合并 + 回勾；N/O：各 合并）
2. git status --short 与基线比；git status -sb ahead 应 0（> 0 补推）
3. git worktree list；逐分支真未合：
   for b in $(git branch --format='%(refname:short)' | grep -v '^main$'); do echo "$b rev-list=$(git rev-list --count main..$b) 真未合=$(git cherry -v main $b | grep -c '^+')"; done
4. ls docs/superpowers/plans/ 应恰好多出 unit6-group-notify / unit8-1-retention / unit8-4-logging 三个，`### Task` 数各 ≥ 3（unit6 ≥ 4）
5. checkbox 按章：ch6 应 10；ch8 应 5（8.1–8.5；8.6–8.9 留人）；按各条 OK/PARTIAL 分别判；核 `<<<<<<<` 计数为 0
6. 🔴 落点守卫：git diff --stat <开跑前 main>..main -- app/ scripts/ requirements.txt pyproject.toml 必须为空；`.env.example` 只允许多**一行** `HR_LIAISON_GROUP_WEBHOOK=`（git diff <开跑前 main>..main -- .env.example 只有这一行 +）
7. 🔴 密钥/地址守卫：git grep -nE "HR_LIAISON_(BOT_SECRET|GROUP_WEBHOOK)\s*=\s*\S{8,}" -- ':!*.md' 应为空；git grep -nE "webhook/send\?key=" 应为空
8. 🔴 边界守卫（git log --format=%s <开跑前 main>..main -- <路径> 看 commit 主题归属）：storage/effects.py、storage/schema.py 只被群通知泳道改过；whitelist.py 与 tests/test_liaison_effects.py 的扫描器只被技术债泳道改过；__main__.py 末尾有 cleanup 子命令**且** main() 首行有 setup_logging()（两头都在）；tools/liaison/README.md 只被运行守护泳道改过
9. 🔴 TD-18 证伪仍在：grep -n "_conn" tools/liaison/tests/test_liaison_effects.py 能找到「with self._conn」必红的那条测试
10. pytest 与【四】4 基线比：≥ 开跑前且 0 失败；单测离线（⛔ 不得有真网络）
11. git status 里不得出现 data/ 下任何文件；ls .claude/handoff/launch/ 里 <TS>.consumed 与 <TS>.started 都在
12. ⛔ 本批不做 worktree 清理；⛔ 不执行 tools/liaison/scripts/install_launchd.py（那是 8.6 灰度时 Shao Peishen 自己跑）

【八、出看护报告】写成 .claude/handoff/lanes-<时间戳>-第十四批看护报告.md（gitignore 内，不提交），含：
  - 前置状态（第十三批 results.tsv 全文 + 开跑前 checkbox 按章实数）
  - 编号 / 脚本判定 / 真身核验结论 / 分歧
  - 🔴 launchd 发车实证一段：.request 写入时刻 → .started 出现时刻 → pid → 是否与 boot_log 一致（这是首批走这条路，正反都写）
  - PARTIAL 留步逐条；红灯清单（Task 数不足、真未合 ≠ 0、pytest 失败、tasks.md 冲突标记、落点/密钥/边界守卫、__main__.py 丢一头、data/ 进 git、ahead ≠ 0、同编号双行）
  - superpowers 是否真调到（八条各一行）
  - 下一步：**一条推荐动作 + 理由**（同结果只写一种；只有结果不同且属 Shao Peishen 必须拍的才列选项并标推荐）

【九、收工】把报告路径和那张表贴给我。
