# HR 招聘智能体 · 任务驱动上线路线图（2026-09-17 重排）

> Cowork·HR业务线-接力0917D 按 Shao Peishen 2026-09-17 21:4x 口径编写：**改排期驱动为任务驱动**——任务无前置阻碍即开工、能开尽开；Shao Peishen 全程在环（G1–G5）；只在「需他决策」「等外部必要信息」两类断点停。
> 取代 `02-系统架构与MVP范围.md` §4 的 W1–W12 周排期作为推进依据（原文保留作范围与验收口径）。逐条状态以 `docs/roadmap/任务台账.yaml` 与 `docs/roadmap/定夺队列.md` 为准。

## 一、产能实测（估算基准）

| 指标 | 实测 | 来源 |
|---|---|---|
| 单日泳道数 | 09-16：18 条（16 OK）；09-17：49 条（35 OK，其余 PARTIAL 多为等外部输入／待定夺，非失败） | `.claude/handoff/lanes-2026091*/results.tsv` |
| 单条耗时 | 中位约 10 分钟；建造段 20–30 分钟；最长 87 分钟（U0 收口） | 同上 |
| 单日机器工时 | 09-17 约 9 泳道·小时（并发上限 3） | 同上 |
| 链路自动化 | intent(grill，15 分钟超时默认) → propose → plan → build → 合 main → 发版（`.51` 无头 SSH 已实证）→ 跟进信 → 回件拆件，全部可无头；人只在 G1–G5 与外部输入处 | `任务驱动workflow设计.md` 在环闸门节、`docs/findings/2026-09-17-无头会话51可达性探测.md` |

**估算口径**：一个交付单元（tasks 约 8–12 项）≈ plan 1 条 ＋ build 2–3 段 ＋ review/合并 1 条 ≈ **半天机器时间**。所以**机器已不是瓶颈**；决定上线时间的是：① 你在闸门上的答复速度 ② 人事部／法务／`.51` 环境的外部输入。

## 二、瓶颈清单（真正决定上线日期的东西）

| # | 外部输入 | 挡住什么 | 最快路径 |
|---|---|---|---|
| X1 | 人事部 10 个历史岗位＋双方评估 | M1 9.1 验收 → M1 正式试点 | 人事部#2 决策点 a（回件期限 09-24，可催早） |
| X2 | 评测集标注（首批 20 份，终 200 份，历史离职脱敏） | M2 解析/排序验收（D9） | 人事部#2 决策点 b/c ＋ U6 判例批改表；按批 ≤20 份滚动 |
| X3 | 合规验收 #1 三份文档法务签认（谷雨） | M2 真实简历入库 | 草稿已出（`0917AT`）→ 发谷雨 → G5 签 |
| X4 | `.51` VC++ 运行库升级（R-9/Q-27） | BGE-M3 在 `.51` 本地召回（U4 上线） | 你放行即可无头装 |
| X5 | M3 语音链路硬件/网络条件（`.51` 为 Windows CPU） | M3 live 段 | 现在就可做技术探针（不依赖 M2 数据） |
| X6 | M2 真实使用数据（原设计：真实招聘 2 周） | M3 prep 题库校准质量 | 可先用画像＋脱敏样本起步，上线后再校准 |

## 三、上线波次（按依赖排，不按周）

### 波次 0 · 现在立即并发（无前置阻碍）
- **M1 收尾**：Q-22/23 已放行 → plan/build 剩余 13 项；9.1 验收等 X1。
- **M2 可见薄片**：`0917BA` 重排到 G2 → 你「定」→ U1 子集＋U2 上传解析＋U5 上传/结果/证据高亮/校对页（脱敏样本、入库闸关）。
- **发版 ②**：Q-14 每日快照（Q-28 待「发」）；**R-9** VC++（Q-27 待放行）。
- **合规 #1**：草稿 → 给谷雨的跟进信（G4 你审发）。
- **M3 需求收敛 grill**＋**MVP 外场景 grill**（面试排期、Offer、入职、渠道对接）：grill 输入只要场景描述＋你本人，零阻碍 → 产出 intent 到 G1。
- **M3 技术探针**（X5）：LiveKit／FunASR／CosyVoice 在 `.51` 或候选机的可装性与延迟基线。

### 波次 1 · 人事部第一次看到 M2（依赖：G2 薄片定稿）
- 薄片 build ≈ 1–1.5 天机器时间 → 发版（G3）→ 给汤丽萍的「来试上传脱敏简历」跟进信（G4）。
- **估计：G2 放行后 2 个工作日内上线 `.51`。**

### 波次 2 · M2 全量（依赖：X2 首批标注、X4）
- U3 硬门槛引擎、U4 召回＋rubric 精排＋evidence、U5 其余（复核/批量确认）、U6 评测导入与指标、U7 合规断言：机器时间 ≈ 3–4 天，可与波次 1 并行起 plan。
- 指标验收随标注批次滚动（每到一批 20 份重算）。

### 波次 3 · M2 真实岗位试运行（依赖：X3 合规签认 ＋ D9 指标达标）
- 试运行岗位：底层软件工程师（Q-03b）。入库闸由你亲自开（不可代）。
- **估计：取决于 X2/X3——若人事部 09-24 前回件、标注每周 1–2 批、法务 1 周内签，约 10 月上中旬。**

### 波次 4 · M3 实时语音面试（intent 与探针在波次 0 已开）
- prep/post 离线段可在 M2 试运行开始后即建（用试运行数据校准）；live 段依赖 X5 探针结论。
- 合规验收 #2（同意、录音留存、身份核验隔离）与 X3 同一法务通道提前起草。
- **估计：M2 试运行起 3–4 周内可试点。**

### 波次 5 · MVP 外场景（排期／Offer／入职／渠道对接）
- intent 在波次 0 收敛；集成类活依赖人事部现用系统信息（X 类外部输入，grill 时列出），主链路验证后按 intent 顺序开建。

## 四、调度器执行口径（已接线）
- 排序：闸门/依赖满足的就绪集内，**人事部可见 > 场景优先级 M0>M1>M2>M3 > 阶段先后**（`0917BA` 接线）。
- 新场景发现：路线图里零阻碍的场景直接生成 grill 任务（不再以「排期未到」为由等待），intent 落档后停 G1。
- 到期日只作兜底硬闸，不作驱动。
