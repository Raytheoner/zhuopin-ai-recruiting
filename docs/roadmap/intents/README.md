# intents/ —— 场景 intent 草稿落点

> 2026-09-17 `[Mac]0917AL` 建目录（`.claude/skills/task-dispatcher/rules.md` §4 规则 ③）。

- Shao Peishen 在本线直接提出新场景 ⇒ Cowork 当场写 `<场景短名>.md`（结构同 `docs/roadmap/M2-intent.md`：背景／目标／不做／待答题），并在 `docs/roadmap/定夺队列.md` 追加「是否启动场景 X 的需求收敛」。
- 启动获批 ⇒ `requirement-grill` 在本线逐题问答 ⇒ 待答题清零 ⇒ 调度器判 `intent` 阶段完成 ⇒ `openspec-propose`。
- 已立包的场景（如 M2）其 intent 正本在 `openspec/changes/<pkg>/intent.md`，本目录只放**未立包**的草稿；立包时 `git mv` 过去。
