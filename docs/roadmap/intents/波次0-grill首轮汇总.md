---
status: 汇总·非 intent   # 本文件是五份 intent 的首轮待答题合并清单，⛔ 不进 G1、不 propose
场景: 波次0-grill首轮汇总
grill会话: "[Mac]0917BC（备料，无头）"
---
# 波次 0 · 五场景 grill 首轮汇总（待 Shao Peishen 在本线作答）

> 2026-09-17 `[Mac]0917BC` 备料（无头，⛔ 未向任何人提问）。按 `.claude/skills/requirement-grill/SKILL.md` M1 设计树 ＋ M2「可查事实自己查」执行到「待问前沿」为止；追问由 Cowork·HR业务线 在本线向 Shao Peishen 发起。每场景 intent 骨架（`status: 草稿·待 G1`）：`S-M3-intent.md`、`S-排期-intent.md`、`S-Offer-intent.md`、`S-入职-intent.md`、`S-渠道-intent.md`（均在本目录）。
> 设计树落在各 intent 的「设计树与前沿」节内（未另立 `docs/roadmap/<场景>-需求树草稿.md`——opener 只要 intent 骨架＋本汇总；答题后若需分轮重塑树，由 Cowork 再拆出）。
> ⚠️ 机器行为提示：`scripts/dispatcher_backlog.py parse_intents` 会把本目录下所有非 README 的 `.md` 当 intent 解析。本文件保留「## 待答题」节（非空）⇒ 生成器把它记为 `propose:波次0-grill首轮汇总` **阻塞／决策**，⛔ 不会进 ready 集、不会追加 G1 行；待五场景 intent 全部到闸后，本文件应删除或迁出本目录（登记见文末）。

## 总数

| 场景 | 自答决策 | 待答题 | 其中「无默认」 | 待专员（判例批改待办） | 技术探针 |
|---|---|---|---|---|---|
| S-M3 实时语音结构化面试 | 11 | 5 | 3（Q1 宿主、Q2 身份核验、Q4 云 ASR/TTS） | 4 | 5（P1–P5） |
| S-排期 面试排期 | 6 | 5 | 1（Q2 联系方式口径） | 4 | 0 |
| S-Offer Offer 生成 | 7 | 4 | 1（Q2 薪资入库） | 4 | 0 |
| S-入职 入职流程 | 7 | 4 | 1（Q3 入职后数据处置） | 4 | 0 |
| S-渠道 渠道对接与简历自动汇总 | 7 | 3 | 0 | 4 | 1（轻量，随 M2 U2 顺带） |
| **合计** | **38** | **21** | **6** | **20** | **6** |

**推荐先答顺序**：S-M3 → S-排期 → S-Offer → S-入职 → S-渠道。理由：S-M3 是波次 4 的场景且探针 X5 等 Q1/Q4 的口径才能定方向；S-排期 Q2（联系方式口径）是 S-Offer／S-入职 的共同前置；S-渠道 三题全部有默认、最不急。

**跨场景共用题（答一次即可）**：S-排期 Q2「候选人联系方式存储口径」——S-Offer D7、S-入职 树节 F 均引用它，⛔ 不重问。

## 待答题
> 与各 intent 逐字一致；选项、代价、推荐、默认项在各 intent「待答题」节。**标「无默认」的题必须明确答复**，其余 15 分钟无答复按推荐项生效（SKILL §二）。

### S-M3 实时语音结构化面试
- Q1 · live 段宿主与公网暴露：(a) 另立境内候选机／云主机跑 LiveKit＋TURN ｜(b) 一期只做到访面试（内网） ｜(c) 一期先做文本异步面试 ➡️ 推荐 a ｜ **无默认**（预算／对外通道）
- Q2 · 身份核验一期范围：(a) 不做活体／证件比对，手机号验证码弱核验＋人工终面兜底 ｜(b) 按 02 做活体＋证件比对（本地隔离） ➡️ 推荐 a ｜ **无默认**（合规口径）
- Q3 · 首批面试对象：(a) 先内部模拟再对 M2 试运行岗位真实候选人（开闸另定夺） ｜(b) 直接真实候选人 ➡️ 推荐 a ｜ 默认 a
- Q4 · ASR／TTS 来源口径：(a) 只允许自托管 FunASR／CosyVoice ｜(b) 允许境内云 ASR/TTS 作一期实现 ➡️ 推荐「a，探针不过转 b」｜ **无默认**（预算／数据流向）
- Q5 · prep 题目须业务经理确认后冻结：(a) 是 ｜(b) 否 ➡️ 推荐 a ｜ 默认 a

### S-排期 面试排期
- Q1 · 范围档：(a) 内部登记 ｜(b) 半自动（生成邀约文案经门禁，HR 发） ｜(c) 候选人自助选时段＋自动外发 ➡️ 推荐 b ｜ 默认 b
- Q2 · 候选人联系方式存储口径（跨场景共用）：(a) 维持哈希、联系方式系统外自管 ｜(b) 进入面试阶段后明文加密入库＋访问留痕＋到期删除 ➡️ 推荐 b ｜ **无默认**（合规口径／处理范围）
- Q3 · 面试官日历来源：(a) 工作台登记时段 ｜(b) 企微日历 API（自建应用） ➡️ 推荐 a ｜ 默认 a
- Q4 · 进入排期的入口：(a) 只从 M2 批量确认进入 ｜(b) 允许手工新增候选人 ➡️ 推荐 a ｜ 默认 a
- Q5 · 启动时点：(a) 波次 5 ｜(b) 提前到 M2 薄片上线后 ➡️ 推荐 a ｜ 默认 a

### S-Offer Offer 生成
- Q1 · 范围档：(a) 文书生成 ｜(b) ＋内部审批流 ｜(c) ＋对候选人发送与签收 ➡️ 推荐 b ｜ 默认 b
- Q2 · 薪资等敏感字段是否入库：(a) 不入库 ｜(b) 加密入库 ➡️ 推荐 a ｜ **无默认**（合规口径）
- Q3 · 拒信是否并入本场景：(a) 并入（共用门禁与模板引擎，TD-8 一次还清） ｜(b) 不并入 ➡️ 推荐 a ｜ 默认 a
- Q4 · 候选人答复回收：(a) HR 人工回填 ｜(b) 一次性链接自助答复 ➡️ 推荐 a ｜ 默认 a

### S-入职 入职流程
- Q1 · 范围档：(a) 清单跟踪 ｜(b) ＋材料收集（敏感信息） ｜(c) ＋现有系统对接 ➡️ 推荐 a ｜ 默认 a
- Q2 · 使用者范围：(a) 仅 HR ｜(b) ＋用人部门经理只读 ｜(c) ＋新员工本人自助 ➡️ 推荐 b ｜ 默认 b
- Q3 · 入职后候选人招聘数据处置：(a) 按留存策略到期删除／脱敏 ｜(b) 保留 ➡️ 推荐 a ｜ **无默认**（合规口径，联动法务）
- Q4 · 提醒机制一期是否要：(a) 不要 ｜(b) 要（先建 TD-11 定时基础设施） ➡️ 推荐 a ｜ 默认 a

### S-渠道 渠道对接与简历自动汇总
- Q1 · 范围档：(a) 导出包汇总 ｜(b) ＋招聘邮箱收件 ｜(c) ＋商务签约 ATS 对接 ➡️ 推荐 a ｜ 默认 a
- Q2 · 启动时点：(a) 波次 5 末位 ｜(b) 提前随 M2 全量 ➡️ 推荐 a ｜ 默认 a
- Q3 · 渠道效果报表一期是否要：(a) 只记 source 字段 ｜(b) 一期出渠道漏斗 ➡️ 推荐 a ｜ 默认 a

## 一行答复模板（改字母即答；「无默认」6 题不可留空）

```
S-M3：Q1a，Q2a，Q3a，Q4a，Q5a ｜ S-排期：Q1b，Q2b，Q3a，Q4a，Q5a ｜ S-Offer：Q1b，Q2a，Q3a，Q4a ｜ S-入职：Q1a，Q2b，Q3a，Q4a ｜ S-渠道：Q1a，Q2a，Q3a
```

## 答后动作（Cowork 在本线执行，⛔ 无头会话不做）
1. 把答复字面转写进各 intent「决策」节（D 条编号续接），从「待答题」勾掉或删行；超时默认生效的题按 SKILL §二 固定措辞落档。
2. 某场景待答题清零 ⇒ `python3 scripts/gates.py request G1 <场景> --scene <场景> --artifact docs/roadmap/intents/<场景>-intent.md --tasks propose:<场景>`（去重；调度器 `sweep --apply` 也会补）。⛔ 不改 `status`——G1 放行后才改「已确认」。
3. 答复若牵出新分叉（如 S-M3 Q1a ⇒ 候选机规格／预算题；S-排期 Q1c ⇒ 对外通道题），Cowork 重算前沿出第 2 轮，树落 `docs/roadmap/<场景>-需求树草稿.md`。
4. 20 条待专员条目：待 `人事部#2` 闭环（串行闸）后，按 SKILL §三 判例批改链起草 `人事部#3`（≤10 行真实案例表、`决策点:` 字段、`criteria --add` 登口径点台账）；对外发送＝不可代，Shao Peishen 拍板。
5. 五场景全部到闸后，删除或迁出本汇总文件（`git rm`／移入 `docs/roadmap/归档/`），避免生成器长期留一条 `propose:波次0-grill首轮汇总` 阻塞条目。

## 技术探针（不进待答题）
- **S-M3 X5**（现在可做，前置 Q-27 VC++ 升级）：P1 LiveKit server 在 Windows Server 2019 可运行性＋LAN WebRTC 建连＋TURN 需求；P2 FunASR 在 `.51` 同款 venv（Python 3.14.5、CPU）可装性与流式首字延迟；P3 CosyVoice 可装性与 TTS 首帧延迟；P4 追问 LLM（DeepSeek 现网关）TTFT；P5 `livekit-agents` 对 3.14 兼容性。落 `docs/m3-voice-probe.md`。探针属环境探测、不改 `.51` 生产目录（隔离 venv 同 `C:\apps\m2-smoke` 形态）；若需新装系统组件，登记后等本人放行（同 Q-27 口径）。
- **S-渠道**（轻量，随 M2 U2 顺带）：各平台导出 PDF 文本层可读率抽样。
