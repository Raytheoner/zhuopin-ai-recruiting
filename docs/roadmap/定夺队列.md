# 定夺队列（R5）

> 2026-09-17 `[Mac]0917AL` 建立（任务驱动 workflow 设计 §四 R5）。**只有两种断点会进这里：需 Shao Peishen 决策、等外部输入**。
> 调度器（`task-dispatcher`）只追加不删；Cowork 在本线收到答复 ⇒ 填「答复」列、状态改「已答」⇒ 经提交通道提交 ⇒ 触发调度器。
> 「阻塞的任务 id」对应 `docs/roadmap/任务台账.yaml` 的 `id`；答复落档后由调度器把对应条目的阻塞解除。
> 迁入来源：`docs/session接力.md`（§三悬置、§五待办、拆件会话待人项）、路线图第六节（四项均已答，无遗留）、`openspec/changes/m2-resume-parse-and-rank/tasks.md` 🔴 项、`docs/superpowers/plans/2026-09-17-m2-unit0-model-comparison.md`「待裁决」、`docs/tech-debt.md`「需 Shao Peishen」项。
> 答复模板（Desktop 一键复制，改字母即答；「外部输入」类只需回「已提供」或「改期 <日期>」）：
>
> ```
> Q-01a，Q-02a，Q-03b
> ```

## 一、待答

| 编号 | 场景 | 阻塞类型 | 问题 | 选项与代价 | 推荐 | 来源 | 阻塞的任务 id | 状态 | 答复 |
|---|---|---|---|---|---|---|---|---|---|
| Q-01 | M2 | 外部输入（Shao Peishen 或汤丽萍：私信机器人一个测试文件） | G1／TD-51：私信文件帧的附件字段未经真实帧确认，附件映射表为空（fail-closed），专员回的文件永远进不了归档 | (a) 本人现在用企微私信机器人发一个 xlsx／pdf，值守日志打出帧键结构后派一条泳道填 `ATTACHMENT_FIELD_PATHS_BY_MSGTYPE` 并重启值守；(b) 等汤丽萍下次回件带附件——首批标注（`人事部#2`）会白发一次 | a | `openspec/changes/m2-resume-parse-and-rank/tasks.md` 0.1；`docs/tech-debt.md` TD-51 | `m2-resume-parse-and-rank/0.1`、`m2-resume-parse-and-rank/U6` | 待答 | |
| Q-02 | M2 | 决策（合规红线相关，不可代） | 合规验收 #1 启动：PIA 报告 ＋ 候选人同意条款单列 AI 评估 ＋ 留存与删除策略 ⇒ 法务结论。U7 的 7.8 真实简历入库闸以此为前置 | (a) 现在发起：本人指定法务对接人，Cowork 起草三份文档草稿进 `docs/compliance/`，本人签认；(b) 推迟到 U5 工作台合 main 后再启动——U7 会等在门口，M2 上线顺延法务周期 | a | `tasks.md` 0.2 | `m2-resume-parse-and-rank/0.2`、`m2-resume-parse-and-rank/U7` | 已答 | a：现在启动合规验收 #1；法务对接人＝谷雨（2026-09-17 18:0x 给定），Cowork 起草 PIA／同意条款／留存策略草稿进 `docs/compliance/`，本人签认（Shao Peishen 2026-09-17 17:5x） |
| Q-03 | M2 | 决策（design D10） | 试运行岗位选定：从 M1 试点三岗中选 1 个**仍在招**的；选定后其冻结画像版本作为 U6 评测集与 U7 试运行的 `job_id` | (a) 供应链总监；(b) 底层软件工程师；(c) 非标产品采购员。哪个仍在招只有本人知道，本项无默认 | 无默认（须本人明确答复） | `tasks.md` 0.3；`design.md` D10；`intent.md` D10 | `m2-resume-parse-and-rank/0.3`、`m2-resume-parse-and-rank/U6`、`m2-resume-parse-and-rank/8.10` | 已答 | b：底层软件工程师（仍在招）；其冻结画像版本作 U6 评测集与 U7 试运行 job_id（Shao Peishen 2026-09-17 18:0x 确认） |
| Q-04 | M2 | 外部输入（汤丽萍：`人事部#2` 回件，9 月 24 日前） | 评测集标注安排：按 Q-03 岗位分批 ≤20 份，脱敏或历史离职样本；模板由 U6 6.1 提供 | 跟进信 `人事部#2` 已于 2026-09-17 推送，只等回件。回件到达 ⇒ 拆件会话自动回灌；逾期未回 ⇒ 调度器登记「超期」，Cowork 起草催办稿待本人审 | — | `tasks.md` 0.4；`docs/跟进信/README-跟进信清单.md` `人事部#2` | `m2-resume-parse-and-rank/0.4`、`m2-resume-parse-and-rank/7.6` | 待答 | |
| Q-05 | M2 | 决策（不可代：定型签认） | U0 1.7 定型确认：`docs/m2-model-comparison.md`「决策」节。现状：DeepSeek 两款在 20 份**合成**样本上实跑完毕，BGE-M3 本地 CPU 召回已 bench，PaddleOCR 按 D14 退路；抽取／精排／阈值三行等真实脱敏样本复算 | (a) 先签认「候选范围与管线」（抽取与精排在 DeepSeek 两款中定、embedding 用本地 BGE-M3、扫描件走 D14 退路），真实样本到位后复算数字并二次签认——U1 数据模型可立即出 plan；(b) 等 Q-06 真实样本跑完一并签认——U1 起步顺延到样本到位后 | a | `tasks.md` 1.7；`docs/superpowers/plans/2026-09-17-m2-unit0-model-comparison.md`「待裁决」#1 | `m2-resume-parse-and-rank/1.7`、`m2-resume-parse-and-rank/U0`、`m2-resume-parse-and-rank/U1/plan` | 已答 | b：先只定抽取模型＝deepseek-flash，其余待真实样本（Shao Peishen 2026-09-17 17:0x；落档 484d5ba） |
| Q-06 | M2 | 决策（真实简历数据处理范围，不可代） | 真实脱敏样本 ≥20 份（含 ≥3 扫描件、≥3 Word）的来源与脱敏方式。仓库与 `data/` 无库存；合成样本上的指标不作 D9 验收 | (a) 历史离职候选人简历经脱敏（需 HR 提供＋脱敏脚本先过本人审）；(b) 汤丽萍批改表首批 20 份（与 Q-04 合并，等回件）。两者都要 M2 门槛「登录＋访问留痕」就位后才能入库，本项无默认 | 无默认（须本人明确答复） | U0 计划「待裁决」#2；`tasks.md` 1.2 | `m2-resume-parse-and-rank/1.2`、`m2-resume-parse-and-rank/1.6` | 已答 | a：历史离职候选人简历脱敏（Shao Peishen 2026-09-17 17:0x；落档 484d5ba） |
| Q-07 | M2 | 决策（预算与外部采购，不可代） | 第 3／4 家境内 LLM 的 API key：火山方舟 `ARK_API_KEY`、阿里百炼 `DASHSCOPE_API_KEY`（M1 时未注册）。无 key 则 1.4「≥3 家」只能跑 DeepSeek 两款 | (a) 注册两家并把 key 放进 `.env`（按量计费，20 份样本实跑成本很低，可信度中；需本人实名注册）；(b) 不注册，1.4 降格为「DeepSeek 两款」并在对比表如实标跳过 | a | U0 计划「待裁决」#3；`tasks.md` 1.4 | `m2-resume-parse-and-rank/1.4` | 已答 | 改：近期只用 DeepSeek（两款）起步，火山方舟／阿里百炼以后再补，不阻塞主航道（Shao Peishen 2026-09-17 21:3x）；原答 a（注册两家）撤回 |
| Q-08 | M2 | 外部输入（需 `.51` 访问） | `.51` 同款 Windows venv 冒烟（`python -m scripts.smoke_m2_deps`）未跑；结果决定 PaddleOCR 走不走 D14 退路、以及发版白名单要加哪些依赖 | (a) 本人回 Mac 后用 CC opener 走 ssh 在 `.51` 跑一次（opener 由调度器生成，属「在 .51 上跑的命令」类，不是发版）；(b) 随下次 `.51` 发版一起跑——1.1 挂到发版那天 | a | U0 计划「待裁决」#4；`tasks.md` 1.1 | `m2-resume-parse-and-rank/1.1` | 已答 | 已提供：0920M 实跑，见 data/eval/m2-pilot/smoke-win51.json 与 docs/m2-model-comparison.md「环境」节 |
| Q-09 | M2 | 决策（技术方案审查，代理人未设 ⇒ 挂起等本人） | PaddleOCR 在 Python 3.14 装不上（`paddlepaddle` 只发 cp313 wheel，项目与 `.51` 都是 3.14） | (a) 按 D14 退路：一期扫描件进「不可读」人工队列＋登记技术债，Paddle 发 cp314 wheel 再接（零新依赖、零架构变化）；(b) `.51` 另建 Python 3.13 sidecar venv 只跑 OCR，主进程 subprocess 调（多一套解释器与计划任务，发版白名单要改）。未答前 U0 已按 (a) 执行 | a | U0 计划「待裁决」#6；design D14 | `m2-resume-parse-and-rank/U2`（3.x 扫描件路径） | 已答 | a：一期扫描件进不可读队列＋技术债，不建 3.13 sidecar（Shao Peishen 2026-09-17 14:1x；见 M2-intent） |
| Q-10 | M0 | 外部输入（Shao Peishen 本人凭据） | 8.6 灰度三条验收面只过 1/3：群通知回推需真实 `HR_LIAISON_GROUP_WEBHOOK`，⛔ 泳道不代填凭据 | (a) 本人在 `.env` 填真实群 webhook 并 `launchctl kickstart` 重启值守 ⇒ 下一条入站即可验「群通知→回推」；(b) 该验收面延后到 8.8 观察窗（09-24）结束再验——8.6 与 8.9 归档一起顺延 | a | `openspec/changes/hr-wecom-aibot-liaison/tasks.md` 8.6；`docs/session接力.md` R-5 | `hr-wecom-aibot-liaison/8.6`、`hr-wecom-aibot-liaison/8.9` | 待答 | |
| Q-11 | M0 | 决策（对外发送属红线①，不可代） | 拆件子会话（`d703cd3` 那轮）越界写了两份章程外文件 `docs/跟进信/人事部-汤丽萍-答复单-2026-09-17-人事部#1决策点a.md/.docx`（拟发汤丽萍的答复单草稿），未跟踪、未提交、未发送。三问：① 发不发 ② 文件删／留／移出 ③ 是否开 TD 登记「`--force` 真实运行下 Write 无路径约束」 | (a) 不发＋删除＋开 TD；(b) 不发＋移出仓库保留（`~/Desktop/` 之类）＋开 TD；(c) 发（须走人工通道，⛔ 不可由拆件会话代发）＋保留＋开 TD | b | `docs/session接力.md`「2026-09-17 拆件会话（自动，验收起活）」待人项；`docs/findings/2026-09-17-打标即开班真实起活实测.md` 5.3 ⑥ | `relay:tag#1297fb1e` | 作废 | 作废：两份答复单为 Cowork 所写并经本人审核，非拆件越界（0917R findings 已更正）；人事部#1 已闭环 |
| Q-12 | M0 | 决策（`.51` 发版决定，不可代） | H-1：~~`.51` 现网~~ **Mac 侧 liaison**（`data/liaison.db`，liaison 永不上 `.51`；`0917AW` 核实误标）`liaison_group_notify` 重建表补跨字段 CHECK（方案已答 `1a`，**执行时机**属发版决定）。✅ **已执行 2026-09-17**（`0917AX` 随发版 ① 同日在 Mac 侧做，备份 `liaison.db.bak-20260917-2135`） | (a) 随下次 `.51` 发版一并执行（当前 0 行则直接重建）；(b) 单独开一次 `.51` 窗口现在做——多一次发版留痕 | a | `docs/session接力.md` H-1；`docs/tech-debt.md` TD-28 | `relay:H-1` | 已答 | a：随下次 `.51` 发版一并执行（Shao Peishen 2026-09-17 17:5x） |
| Q-13 | 构建自动化 | 决策 | worktree 被未落档清理已发生两次（08-30、09-03 前），机制不明；代码没丢，丢的是 worktree 内 `.superpowers/sdd/` 台账。要不要查清是谁在清、加护栏 | (a) 查：翻 launchd／run-lanes 清理逻辑与 `git worktree prune` 调用点，找到就加「有未转写台账不许删」护栏；(b) 不查：只靠 09-03 已立的「收口前转写台账」规矩——复发时无线索 | b | `docs/session接力.md` §三 #2 | — | 已答 | b：不查，沿用收口前转写台账规矩（Shao Peishen 2026-09-17 17:5x） |
| Q-14 | `.51` | 决策（`.51` 运维） | §5-1 备份任务常态化：现在只有 09-03 一次性快照 `C:\apps\backups\20260903-1003`，不是常态化备份 | (a) 加一条 Windows 计划任务每日快照（随下次发版一并装，脚本由泳道写）；(b) 保持一次性快照——现网库损坏只能回到 09-03 | a | `docs/session接力.md` §三 #4；`docs/audit-and-outbound-ops.md` 第五节 | — | 已答 | a：随下次 `.51` 发版装每日快照计划任务（Shao Peishen 2026-09-17 17:5x） |
| Q-15 | `.51` | 决策（生产窗口，不可代） | `.51` 整机重启：阻断已清、`CBS RebootPending=True`、已 85 天未重启，爆炸半径 7 个服务（含门户网关） | (a) 定一个低峰窗口（写明日期与时段，opener＝编排文件 `[Mac] 0820-9R`）；(b) 继续不重启——挂起补丁持续累积。本项无默认 | 无默认（须本人给窗口） | `docs/session接力.md` §三 #5 | — | 已答 | a：窗口 2026-09-20（周日）22:00–23:00；opener＝编排文件 `[Mac] 0820-9R`，须在内网执行，执行前仍经本线确认（Shao Peishen 2026-09-17 18:0x 确认） |
| Q-16 | 门户 | 外部输入（本人在 Win 笔记本上操作） | 阶段 C 门户导航：加板块「HR·招聘智能体」外链 `http://192.168.100.51:8095/hr/recruit-agent/`；与 `.51` 服务无关 | (a) 本人在 Win 笔记本改门户 HTML（改法见 `04-部署与门户挂载.md`）；(b) 等统一门户网关上线一起挂——期间业务经理只能记 IP 端口 | a | `docs/session接力.md` §三 #6 | — | 待答 | |
| Q-17 | M1 | 决策 | 06 清单剩余：3.3 企微 webhook（无挂载点）、9.1／9.2／9.3 沟通线——是否作废并入 M1 验收线 | (a) 作废，沟通线并入 `人事部#2` 之后的跟进信；(b) 保留原条目待 M1 验收线再看。本项无默认 | 无默认 | `docs/session接力.md` §三 #8 | — | 已答 | a：3.3／9.1–9.3 作废，沟通线并入 `人事部#2` 之后的跟进信（Shao Peishen 2026-09-17 18:0x 确认） |
| Q-18 | M1 | 决策（`.51` 现网表结构） | TD-1：`job_profile` 两列时序留痕删列改的是现网表结构，需拍板后另开变更包；触发条件＝M2 U1 建表时 | (a) 随 M2 U1 数据模型一起另开变更包删列；(b) 继续不删（两列作对照留着） | b | `docs/tech-debt.md` TD-1 | `m2-resume-parse-and-rank/U1/plan` | 已答 | b：不删列（Shao Peishen 2026-09-17 17:5x） |
| Q-19 | M2 | 决策（合规红线相关，不可代） | TD-5：`raw_response` 逐字存，M2 评分 prompt 若要求引文，简历原文会进留痕。触发条件＝M2 开始处理真实简历且评分要证据引文 | ① 评分 prompt 只返回 offset 区间不返回引文，`evidence_ref` 承担定位；② `raw_response` 落盘前按简历文本脱敏；③ 接受并在留痕访问控制上补齐 | ① | `docs/tech-debt.md` TD-5 | `m2-resume-parse-and-rank/U4/plan` | 已答 | ①：评分 prompt 只返回 offset 区间不返回引文，`evidence_ref` 承担定位（Shao Peishen 2026-09-17 17:5x） |
| Q-20 | M1 | 决策（技术方案审查） | TD-13：丢弃岗位两次删除无原子性；方案 C（调用点 catch `discard_thread_checkpoints` 异常、ERROR 日志、照常返回引导语）约 5 行，只动 `app/web/server.py` | (a) 接受 C，派一条小泳道；(b) 不改，保持现状（表改名时每条离题首轮消息报错，部署当场暴露） | a | `docs/tech-debt.md` TD-13 | — | 已答 | a：接受方案 C，派小泳道（Shao Peishen 2026-09-17 17:5x） |
| Q-21 | 构建自动化 | 决策 | requirement-grill 是否移植 Win 端「答复模板发出 15 分钟无答复 ⇒ 按推荐生效」（SKILL.md §九 ①） | (a) 移植：grill 往返压成一轮，G1 intent 定稿仍是人闸，超时生效条目逐项标出；(b) 不移植：未答题一律等本人，G1 不到闸 | a | `[Mac]0917AN` 收工报告 | — | 已答 | a：移植 15 分钟无答复按推荐生效；G1 intent 定稿仍为人闸，超时生效条目逐项标出；由下一个动 requirement-grill SKILL.md 的会话落档＋补测试（Shao Peishen 2026-09-17 17:5x） |
| Q-22 | M1 | 决策（G2 闸门·design／spec 定稿，不可代） | 【G2 design／spec 定稿】`m1-intake-quality-fixes`：包 `openspec validate` 过、无 Open Questions ｜ 产出 `openspec/changes/m1-intake-quality-fixes/design.md` | (a) 答「定」⇒ 放行，spec-to-plan → build；(b) 答「改：…」⇒ 按批注改后再追加一行回闸，或逐条改后答「定」；(c) 答「作废」⇒ 该 subject 的下游条目作废 | 无默认（闸门须本人放行；答复列由 Cowork 转写为字面「定」，⛔ 不填字母 a/b） | `scripts/gates.py` ｜ `openspec/changes/m1-intake-quality-fixes/design.md` | `m1-intake-quality-fixes/U8/plan` | 已答 | 定 |
| Q-23 | M1 | 决策（G2 闸门·design／spec 定稿，不可代） | 【G2 design／spec 定稿】`m1-job-profile-intake`：包 `openspec validate` 过、无 Open Questions ｜ 产出 `openspec/changes/m1-job-profile-intake/design.md` | (a) 答「定」⇒ 放行，spec-to-plan → build；(b) 答「改：…」⇒ 按批注改后再追加一行回闸，或逐条改后答「定」；(c) 答「作废」⇒ 该 subject 的下游条目作废 | 无默认（闸门须本人放行；答复列由 Cowork 转写为字面「定」，⛔ 不填字母 a/b） | `scripts/gates.py` ｜ `openspec/changes/m1-job-profile-intake/design.md` | `m1-job-profile-intake/U1/plan` | 已答 | 定 |
| Q-24 | .51 | 决策（G3 闸门·发布，不可代） | 【G3 发布】`.51`：场景全部单元合 main ｜ 产出 `docs/deploy-51-server.md` ｜ 发版包：`docs/releases/2026-09-发版清单-51.md`；放行范围＝清单「发版 ①」（2026-09-17 21:1x Shao Peishen 本线答「发」；发版 ② Q-14 另过 G3） | (a) 答「发」⇒ 放行，动作通道 `deploy-51`（后续）或无头 `.51` 发版；(b) 答「改：…」⇒ 按批注改后再追加一行回闸，或逐条改后答「发」；(c) 答「作废」⇒ 该 subject 的下游条目作废 | 无默认（闸门须本人放行；答复列由 Cowork 转写为字面「发」，⛔ 不填字母 a/b） | `scripts/gates.py` ｜ `docs/deploy-51-server.md` | `answer:Q-12` | 已答 | 发 |
| Q-25 | M2 | 决策（G5 闸门·口径签认，不可代） | 【G5 口径签认】`合规验收#1`：回件拆件判为口径点 ｜ 产出 `docs/compliance/` | (a) 答「签」⇒ 放行，口径点台账转已签认；(b) 答「改：…」⇒ 按批注改后再追加一行回闸，或逐条改后答「签」；(c) 答「作废」⇒ 该 subject 的下游条目作废 | 无默认（闸门须本人放行；答复列由 Cowork 转写为字面「签」，⛔ 不填字母 a/b） | `scripts/gates.py` ｜ `docs/compliance/` | `m2-resume-parse-and-rank/0.2` | 已答 | 改：待法务谷雨审阅三份草稿后再签（Shao Peishen 2026-09-18 07:2x） |
| Q-26 | M2 | 决策（答复→任务映射缺失） | 【答复→任务映射缺失】Q-07：已答「改：近期只用 DeepSeek（两款）起步，火山方舟／阿里百炼以后再补，不阻塞主航道（Shao Peishen 2026-09-17 21:3x）；原答 a（注」，`rules.md` §7 无 `Q-07` 映射，该行「阻塞的任务 id」保持阻塞（保守），⛔ 调度器不猜后续动作 | (a) 补映射：在 `scripts/dispatcher_answers.py` `ANSWER_MAP` 与 `rules.md` §7 增加 `Q-07`（新任务／保持阻塞原因）后重跑生成器；(b) 答「无新动作」⇒ 只解阻塞，不生成任务 | a | `scripts/dispatcher_answers.py` ｜ `.claude/skills/task-dispatcher/rules.md` §7 | — | 已答 | 同意：Q-07 改答映射为「tasks 1.4 解除对下游阻塞，第 3／4 家改为后补非阻塞」（Shao Peishen 2026-09-17 21:5x） |
| Q-27 | M2/`.51` | 决策（环境操作，改 `.51` 系统组件，代理人未设 ⇒ 等本人放行） | R-9：`.51` 装 VC++ 2015–2022 x64 Redistributable（现 v14.27 过旧，torch 2.14 `c10.dll` WinError 1114），装完后重跑 `smoke_m2_deps` 与 `bench_bge_m3`，填 `docs/m2-model-comparison.md`「BGE-M3 本地 CPU 召回」`.51` 耗时。当前卡在 M2 U3 召回上线 | (a) 现在派一条另起 session 的 opener 去 `.51` 装该运行库并重跑两个探针（触碰 .51 系统组件，非发版但同属生产环境变更）；(b) 暂缓，M2 U3 召回先按合成样本推进，`.51` 环境问题留到真上线前一并处理 | 无默认（须本人明确答复） | `docs/session接力.md:79` R-9 | `relay:R-9` | 已答 | 放行：派无头任务在 `.51` 装 VC++ 2015–2022 x64 Redistributable（不重启服务），装完重跑 `smoke_m2_deps` 与 `bench_bge_m3`（Shao Peishen 2026-09-17 21:5x） |
| Q-28 | .51 | 决策（G3 闸门·发布，不可代） | 【G3 发布】`发版 ② Q-14 每日快照计划任务`：场景全部单元合 main ｜ 产出 `docs/releases/Q14-计划任务安装说明.md` | (a) 答「发」⇒ 放行，动作通道 `deploy-51`（后续）或无头 `.51` 发版；(b) 答「改：…」⇒ 按批注改后再追加一行回闸，或逐条改后答「发」；(c) 答「作废」⇒ 该 subject 的下游条目作废 | 无默认（闸门须本人放行；答复列由 Cowork 转写为字面「发」，⛔ 不填字母 a/b） | `scripts/gates.py` ｜ `docs/releases/Q14-计划任务安装说明.md` | `answer:Q-14` | 已答 | 发 |
| Q-29 | M2/`.51` | 决策（答复→任务映射缺失） | 【答复→任务映射缺失】Q-27：已答「放行：派无头任务在 `.51` 装 VC++ 2015–2022 x64 Redistributable（不重启服务），装完重跑 `smoke_m2_deps` 与 `bench_bge_m3`」，`rules.md` §7 无 `Q-27` 映射，该行「阻塞的任务 id」`relay:R-9` 保持阻塞（保守），⛔ 调度器不猜后续动作；该 opener 涉 `.51` 系统组件变更，不属可自动发车范围（红线①仅拦发版，环境变更仍需人派专门 opener） | (a) 补映射：在 `scripts/dispatcher_answers.py` `ANSWER_MAP` 与 `.claude/skills/task-dispatcher/rules.md` §7 增加 `Q-27`（新任务／保持阻塞原因）后重跑生成器；(b) 答「无新动作」⇒ 只解阻塞，不生成任务，改由人工另起 opener 派 `.51` VC++ 安装 | a | `scripts/dispatcher_answers.py` ｜ `.claude/skills/task-dispatcher/rules.md` §7 | `relay:R-9` | 已答 | a：补 Q-27 映射（`scripts/dispatcher_answers.py` ANSWER_MAP 与 rules.md §7），VC++ 安装已由 0917BB 完成，映射登记为已完成并解阻塞 relay:R-9（Shao Peishen 2026-09-18 07:2x）；映射已由 `0918V` 落地 |
| Q-30 | M2 | 决策（G2 闸门·design／spec 定稿，不可代） | 【G2 design／spec 定稿】`m2-resume-parse-and-rank`：包 `openspec validate` 过、无 Open Questions ｜ 产出 `docs/roadmap/M2-可见薄片重排说明.md` | (a) 答「定」⇒ 放行，spec-to-plan → build；(b) 答「改：…」⇒ 按批注改后再追加一行回闸，或逐条改后答「定」；(c) 答「作废」⇒ 该 subject 的下游条目作废 | 无默认（闸门须本人放行；答复列由 Cowork 转写为字面「定」，⛔ 不填字母 a/b） | `scripts/gates.py` ｜ `docs/roadmap/M2-可见薄片重排说明.md` | `m2-resume-parse-and-rank/U1/plan`、`m2-resume-parse-and-rank/U2/plan`、`m2-resume-parse-and-rank/U5/plan` | 已答 | 定 |
| Q-31 | 人事部 | 决策（G4 闸门·发信，不可代） | 【G4 发信】`人事部#3`：跟进信 md＋docx 起草完、自检过 ｜ 产出 `docs/跟进信/人事部-汤丽萍-跟进-2026-09-17-需求讨论会结论回收单.md` | (a) 答「审核通过·发」⇒ 放行，Cowork 经动作通道 `send-followup` 发出；(b) 答「改：…」⇒ 按批注改稿、自检后再追加一行回闸；(c) 答「作废」⇒ 该信作废 | 无默认（闸门须本人放行；答复列由 Cowork 转写为字面「发」，⛔ 不填字母 a/b） | `scripts/gates.py` ｜ `docs/跟进信/人事部-汤丽萍-跟进-2026-09-17-需求讨论会结论回收单.md` | — | 已答 | 审核通过·发 |
| Q-32 | S-M3 | 决策（G1 闸门·intent 定稿，不可代） | 【G1 intent 定稿】`S-M3`：grill 产出 intent 且无未答题 ｜ 产出 `docs/roadmap/intents/S-M3-intent.md` | (a) 答「定」⇒ 放行，propose；(b) 答「改：…」⇒ 按批注改后再追加一行回闸，或逐条改后答「定」；(c) 答「作废」⇒ 该 subject 的下游条目作废 | 无默认（闸门须本人放行；答复列由 Cowork 转写为字面「定」，⛔ 不填字母 a/b） | `scripts/gates.py` ｜ `docs/roadmap/intents/S-M3-intent.md` | `propose:S-M3` | 已答 | 定 |
| Q-33 | S-排期 | 决策（G1 闸门·intent 定稿，不可代） | 【G1 intent 定稿】`S-排期`：grill 产出 intent 且无未答题 ｜ 产出 `docs/roadmap/intents/S-排期-intent.md` | (a) 答「定」⇒ 放行，propose；(b) 答「改：…」⇒ 按批注改后再追加一行回闸，或逐条改后答「定」；(c) 答「作废」⇒ 该 subject 的下游条目作废 | 无默认（闸门须本人放行；答复列由 Cowork 转写为字面「定」，⛔ 不填字母 a/b） | `scripts/gates.py` ｜ `docs/roadmap/intents/S-排期-intent.md` | `propose:S-排期` | 已答 | 定 |
| Q-34 | S-Offer | 决策（G1 闸门·intent 定稿，不可代） | 【G1 intent 定稿】`S-Offer`：grill 产出 intent 且无未答题 ｜ 产出 `docs/roadmap/intents/S-Offer-intent.md` | (a) 答「定」⇒ 放行，propose；(b) 答「改：…」⇒ 按批注改后再追加一行回闸，或逐条改后答「定」；(c) 答「作废」⇒ 该 subject 的下游条目作废 | 无默认（闸门须本人放行；答复列由 Cowork 转写为字面「定」，⛔ 不填字母 a/b） | `scripts/gates.py` ｜ `docs/roadmap/intents/S-Offer-intent.md` | `propose:S-Offer` | 已答 | 定 |
| Q-35 | S-入职 | 决策（G1 闸门·intent 定稿，不可代） | 【G1 intent 定稿】`S-入职`：grill 产出 intent 且无未答题 ｜ 产出 `docs/roadmap/intents/S-入职-intent.md` | (a) 答「定」⇒ 放行，propose；(b) 答「改：…」⇒ 按批注改后再追加一行回闸，或逐条改后答「定」；(c) 答「作废」⇒ 该 subject 的下游条目作废 | 无默认（闸门须本人放行；答复列由 Cowork 转写为字面「定」，⛔ 不填字母 a/b） | `scripts/gates.py` ｜ `docs/roadmap/intents/S-入职-intent.md` | `propose:S-入职` | 已答 | 定 |
| Q-36 | S-渠道 | 决策（G1 闸门·intent 定稿，不可代） | 【G1 intent 定稿】`S-渠道`：grill 产出 intent 且无未答题 ｜ 产出 `docs/roadmap/intents/S-渠道-intent.md` | (a) 答「定」⇒ 放行，propose；(b) 答「改：…」⇒ 按批注改后再追加一行回闸，或逐条改后答「定」；(c) 答「作废」⇒ 该 subject 的下游条目作废 | 无默认（闸门须本人放行；答复列由 Cowork 转写为字面「定」，⛔ 不填字母 a/b） | `scripts/gates.py` ｜ `docs/roadmap/intents/S-渠道-intent.md` | `propose:S-渠道` | 已答 | 定 |
| Q-37 | M3 | 决策（G1 闸门·intent 定稿，不可代） | 【G1 intent 定稿】`M3`：grill 产出 intent 且无未答题 ｜ 产出 `docs/roadmap/intents/S-M3-intent.md` | (a) 答「定」⇒ 放行，propose；(b) 答「改：…」⇒ 按批注改后再追加一行回闸，或逐条改后答「定」；(c) 答「作废」⇒ 该 subject 的下游条目作废 | 无默认（闸门须本人放行；答复列由 Cowork 转写为字面「定」，⛔ 不填字母 a/b） | `scripts/gates.py` ｜ `docs/roadmap/intents/S-M3-intent.md` | `propose:S-M3` | 已答 | 定 |
| Q-38 | M2 | 决策（预算与外部采购，不可代） | Q-07 生成的后续任务 `answer:Q-07`（注册火山方舟 ARK_API_KEY ＋阿里百炼 DASHSCOPE_API_KEY 并补对比表第 3／4 家）需本人实名注册付费账号，调度器不可派车；是否现在办理 | (a) 现在办理：本人注册两家并把 key 写入 `.env`（按量计费，随时可撤销）；(b) 继续搁置：维持 Q-07 原答「以后再补」，主航道不受影响，本条无限期挂起也不追问 | 无默认（Q-07 已选择「以后再补」，此条只在他想动手时答） | `docs/roadmap/任务台账.yaml` `answer:Q-07` 备注 | `answer:Q-07` | 已答 | b：继续搁置，维持 Q-07「以后再补」，⛔ 不再追问（Shao Peishen 2026-09-18 07:2x） |
| Q-39 | channel-resume-intake | 决策（G2 闸门·design／spec 定稿，不可代） | 【G2 design／spec 定稿】`channel-resume-intake`：包 `openspec validate` 过、无 Open Questions ｜ 产出 `openspec/changes/channel-resume-intake/design.md` | (a) 答「定」⇒ 放行，spec-to-plan → build；(b) 答「改：…」⇒ 按批注改后再追加一行回闸，或逐条改后答「定」；(c) 答「作废」⇒ 该 subject 的下游条目作废 | 无默认（闸门须本人放行；答复列由 Cowork 转写为字面「定」，⛔ 不填字母 a/b） | `scripts/gates.py` ｜ `openspec/changes/channel-resume-intake/design.md` | `channel-resume-intake/U1/plan` | 已答 | 改：等人事部#3 回件补齐现用流程与样例后再定稿（Shao Peishen 2026-09-18 07:2x） |
| Q-40 | interview-scheduling | 决策（G2 闸门·design／spec 定稿，不可代） | 【G2 design／spec 定稿】`interview-scheduling`：包 `openspec validate` 过、无 Open Questions ｜ 产出 `openspec/changes/interview-scheduling/design.md` | (a) 答「定」⇒ 放行，spec-to-plan → build；(b) 答「改：…」⇒ 按批注改后再追加一行回闸，或逐条改后答「定」；(c) 答「作废」⇒ 该 subject 的下游条目作废 | 无默认（闸门须本人放行；答复列由 Cowork 转写为字面「定」，⛔ 不填字母 a/b） | `scripts/gates.py` ｜ `openspec/changes/interview-scheduling/design.md` | `interview-scheduling/U1/plan` | 已答 | 改：等人事部#3 回件补齐现用流程与样例后再定稿（Shao Peishen 2026-09-18 07:2x） |
| Q-41 | offer-generation | 决策（G2 闸门·design／spec 定稿，不可代） | 【G2 design／spec 定稿】`offer-generation`：包 `openspec validate` 过、无 Open Questions ｜ 产出 `openspec/changes/offer-generation/design.md` | (a) 答「定」⇒ 放行，spec-to-plan → build；(b) 答「改：…」⇒ 按批注改后再追加一行回闸，或逐条改后答「定」；(c) 答「作废」⇒ 该 subject 的下游条目作废 | 无默认（闸门须本人放行；答复列由 Cowork 转写为字面「定」，⛔ 不填字母 a/b） | `scripts/gates.py` ｜ `openspec/changes/offer-generation/design.md` | `offer-generation/U1/plan` | 已答 | 改：等人事部#3 回件补齐现用流程与样例后再定稿（Shao Peishen 2026-09-18 07:2x） |
| Q-42 | onboarding-flow | 决策（G2 闸门·design／spec 定稿，不可代） | 【G2 design／spec 定稿】`onboarding-flow`：包 `openspec validate` 过、无 Open Questions ｜ 产出 `openspec/changes/onboarding-flow/design.md` | (a) 答「定」⇒ 放行，spec-to-plan → build；(b) 答「改：…」⇒ 按批注改后再追加一行回闸，或逐条改后答「定」；(c) 答「作废」⇒ 该 subject 的下游条目作废 | 无默认（闸门须本人放行；答复列由 Cowork 转写为字面「定」，⛔ 不填字母 a/b） | `scripts/gates.py` ｜ `openspec/changes/onboarding-flow/design.md` | `onboarding-flow/U1/plan` | 已答 | 改：等人事部#3 回件补齐现用流程与样例后再定稿（Shao Peishen 2026-09-18 07:2x） |
| Q-43 | 构建自动化 | 决策（工具链） | `docs/openers/号池台账.md` 已到 60 KB 体积闸上限；该文件按日历月轮转（`scripts/archive_docs.py`），当月内无更早月份可搬出，追加任何新号都会使 `tests/test_doc_size_budget.py` 变红，提交通道逐条核过整套测试、红即拒收——当前无自动化手段能在月内让它回到闸下，而 CLAUDE.md 收录规则禁止为过闸删内容或调上限。本条产生时**已顶格阻塞注册**：`[Mac]0918B`（S-M3 立包 openspec-propose，已用 `launch-lanes` 派车、非空跑）本应登记进号池台账，因登记会使文件从 59.85 KB 变 60.05 KB 越过闸值、连累提交通道拒收本轮全部台账/队列更新，故本条已把该行登记撤回、只在此处存档：`\| 09-18 \| [Mac]0918B \| S-M3 立包 openspec-propose（结构化语音面试）\| 编排文件无头块（泳道「M3立包」，Opus），task-dispatcher·decision-20260918-070856 派，G1（Q-37）已放行 \| 作废 | 作废：Cowork 已于 2026-09-17 23:1x 把 09-15 之前 127 行迁入 `docs/openers/归档/号池台账-归档.md`，号池现 22 KB，闸下无阻塞（Shao Peishen 2026-09-18 08:1x） | (a)：按周轮转能长期维持闸内，且不改变现有「按月」心智太多，只是把粒度调细；一次性改 `scripts/archive_docs.py` 的轮转判据 | task-dispatcher·decision-20260918-070856（本次唤醒，写 `0918B` 号时触发，61283→61495 字节，59.85→60.05 KB） | — | 作废 | 作废（Shao Peishen 2026-09-18 08:1x）：Cowork 已于 09-17 23:1x 把 09-15 之前 127 行迁入 `docs/openers/归档/号池台账-归档.md`，号池现 22 KB，闸下无阻塞，本条前提消失。状态列由 Cowork·0918G 09-18 09:1x 同步（此前答复误写入「选项与代价」列，状态未改） |
| Q-44 | M2 | 决策（G3 闸门·发布，不可代） | 【G3 发布】`M2 可见薄片`：场景全部单元合 main ｜ 产出 `docs/roadmap/M2-可见薄片重排说明.md`；预授权条件（Shao Peishen 2026-09-18 07:3x「三单元开跑，上线」）：① U1/U2/U2.5 全部合 main 且回勾 ② 全量 pytest 0 失败 ③ 按 `docs/releases/2026-09-发版清单-51.md` 流程快照→依赖→sync→G-e→冒烟，不过即回滚 ④ 真实简历入库闸保持关闭 ⑤ 发版后在本线报结果 | (a) 答「发」⇒ 放行，动作通道 `deploy-51`（后续）或无头 `.51` 发版；(b) 答「改：…」⇒ 按批注改后再追加一行回闸，或逐条改后答「发」；(c) 答「作废」⇒ 该 subject 的下游条目作废 | 无默认（闸门须本人放行；答复列由 Cowork 转写为字面「发」，⛔ 不填字母 a/b） | `scripts/gates.py` ｜ `docs/roadmap/M2-可见薄片重排说明.md` | `m2-resume-parse-and-rank/U2.5` | 已答 | 发 |
| Q-45 | M2 | 决策（答复→任务映射缺失） | 【答复→任务映射缺失】Q-38：已答「b：继续搁置，维持 Q-07「以后再补」，⛔ 不再追问」，`rules.md` §7 无 `Q-38` 映射（台账生成器按答复文本首字符解析出的缺映射键显示为 `Q-38b`，实际定夺队列编号是 `Q-38`——映射时请一并核对该键归一化有无偏差，避免以后再错配），该行「阻塞的任务 id」`answer:Q-07` 保持阻塞（保守），⛔ 调度器不猜后续动作；该答复本身即「不再追问、维持搁置」，映射只需登记为空操作（不生成新任务），使后续台账刷新不再反复列为缺映射 | (a) 补映射：在 `scripts/dispatcher_answers.py` `ANSWER_MAP` 与 `.claude/skills/task-dispatcher/rules.md` §7 为 `Q-38` 增加空操作映射（不生成任务，仅消除缺映射提示）；(b) 答「无新动作」⇒ 暂不补，接受调度器每次刷新台账都重复提示这条 | a | `scripts/dispatcher_answers.py` ｜ `.claude/skills/task-dispatcher/rules.md` §7 | `answer:Q-07` | 已答 | a：为 Q-38 增加空操作映射（不生成任务，仅消除缺映射提示），并核对答复键归一化（Shao Peishen 2026-09-18 08:1x）；映射已由 `0918V` 落地 |
| Q-46 | M3 | 决策（G2 闸门·design／spec 定稿，不可代） | 【G2 design／spec 定稿】`voice-structured-interview`：包 `openspec validate` 过、无 Open Questions ｜ 产出 `openspec/changes/voice-structured-interview/design.md` | (a) 答「定」⇒ 放行，spec-to-plan → build；(b) 答「改：…」⇒ 按批注改后再追加一行回闸，或逐条改后答「定」；(c) 答「作废」⇒ 该 subject 的下游条目作废 | 无默认（闸门须本人放行；答复列由 Cowork 转写为字面「定」，⛔ 不填字母 a/b） | `scripts/gates.py` ｜ `openspec/changes/voice-structured-interview/design.md` | `voice-structured-interview/U0/plan`、`voice-structured-interview/U1/plan`、`voice-structured-interview/U2/plan`、`voice-structured-interview/U3/plan`、`voice-structured-interview/U5/plan`、`voice-structured-interview/U4/plan`、`voice-structured-interview/U6/plan`、`voice-structured-interview/U7/plan` | 已答 | 定 |
| Q-47 | 构建自动化 | 决策（改调度器自身脚本，红线③要求专门 opener，非判断题） | `scripts/dispatcher_backlog.py` 的 `TAG4_RE` 正则只认「【谁做：x】【状态：y】【判据：z】【不做会怎样：w】」内联冒号格式；`docs/session接力.md` 当前全部 7 条四标签待办行已漂移为「【谁做】x｜【状态】y｜【判据】z｜【不做会怎样】w」（标签内不带冒号、用 `｜` 分隔），正则 0 匹配，导致这些行完全不进台账（本次唤醒发现其中 2 条真实待派发项 `docs/session接力.md:75`／`:76` 被漏派，已由本会话手工补登记为 `relay:R-11`／`relay:R-12` 并派 `0918AF`／`0918AG`；另外 5 条内容各异，未逐条人工核实，若正则一直不修，后续每次新增的四标签待办都会持续静默漏派） | (a) 现在派一条专门 opener 修 `TAG4_RE`（改认「｜」分隔、标签内无冒号的格式，且要向后兼容原内联冒号格式，防止后续再漂移一次又漏一次），同时人工核对 `docs/session接力.md` 现存 7 条里另外 5 条（第 79/83/84/89/95 行）有无遗漏未登记的真实待办；(b) 暂缓，接受后续四标签待办继续需要人工/调度器逐次手工识别补登记 | a（低风险重构，只影响解析、不改判据语义，且已有真实漏派实证） | `scripts/dispatcher_backlog.py` `TAG4_RE` ｜ `docs/session接力.md` | — | 已答 | a：现在派 `0919P` 修（Shao Peishen 2026-09-19 22:0x，经 Cowork·0918AO 巡检转达）。**执行结果**（`0919P` 2026-09-19）：`commit 53ed69a` 已实现 `TAG4_RE_OLD`/`TAG4_RE_NEW` 双格式兼容，`tests/test_dispatcher*.py` 68 passed（0 新增失败）。人工核对 `session接力.md` 现存四标签行（本条起笔时 7 条，执行时因 `0919O` 新增 2 条已增至 9 条）：`relay:R-11`（行 86，`effect_persist_flags`）／`relay:R-12`（行 87，`funasr`/`livekit` 探针依赖）均已核实完成（专属测试转绿），已在 `session接力.md` 标【已闭环】；S-M3 G2 行（94）因 `Q-46` 已答「定」判过时，已标【已闭环】；四包 G2 行（100）因 `Q-39`–`Q-42` 已答「改」（非「定」，待人事部#3 回件）判描述过时，已加注由 `Q-39`–`42` 覆盖跟踪；`U2/U4` 9 条 Minor 行（90，`relay:R-17`）U4 尚未开工，真实仍待，维持原状；重复派发防护建议行（95，`relay:R-18`）`run-lanes.sh::mark_done` 机制已存在但 `0918B` 实证发生在其后、根治证据不足，维持原状待复核；`0919O` 当日新增的 2 条（77/78，`relay:R-15`/`relay:R-16`）真实待派发，Task 1 修复后阻塞类型判「无」，下次台账刷新即自动进 `ready`，无需额外登记。**新发现缺口（`relay:R-19`，未修）**：行 90/94/95/100 实际用的是第三种漂移格式（标签间无 `｜` 分隔，如「【谁做】X【状态】Y」），超出本条 `Q-47` 授权的「两种格式」范围，`TAG4_RE` 对其仍 0 命中，需下一条专门 opener 补第三格式（红线③，需另申请授权，本条不擅自扩大范围处理） |
| Q-48 | 构建自动化 | 决策（改调度器自身脚本，红线③要求专门 opener，非判断题） | `scripts/dispatcher_backlog.py::_match_plans` 的前缀匹配正则 `(?<![\w]){prefix}-unit{u}(?!\.?\d)` 会误命中**不相关变更包名字中间**恰好包含该子串的 plan 文件：`interview-scheduling` 的前缀 `interview` 与 plan 文件 `2026-09-18-voice-structured-interview-unit1-data-model.md`（stem 中含 `...structured-interview-unit1-...`，连字符前不是 `\w` 字符，负向后顾断言不拦）撞了正则，导致生成器把 `interview-scheduling/U1/plan` 判「真身完成」（本次台账刷新仍在 conflicts 里，未被台账采信，未造成误派）、更严重的是把 `interview-scheduling/1.1`–`1.7`（U1 面试域模型，7 个条目，`interview-scheduling/tasks.md` 全部未勾选、且该变更包自己从未跑过 spec-to-plan、`docs/superpowers/plans/` 下无任何 `interview-scheduling` 命名的 plan 文件）的依赖错绑成了 `voice-structured-interview` 的三个拆段（`plan:2026-09-18-voice-structured-interview-unit1-data-model/seg1\|2\|3`），三段本次 `lanes-done-20260918-163444` 事件里合 main 完成后，这 7 条被判「依赖已满足」进了 `--show ready`。本会话已核实真相（`interview-scheduling` 无自己的 plan、G2 闸门 Q-40 答复是「改」非「定」，尚未放行）并手工排除，本轮**未**派发这 7 条；但只要该正则不修，`interview` 前缀与其他变更包名字的类似撞车会反复发生，且不保证下次都被人工拦住 | (a) 现在派一条专门 opener 修 `_match_plans`：前缀匹配收紧为只认 `^{prefix}-unit{u}` 或本来设计要覆盖的 `{prefix}-unit{u}` 出现在**文件名起始的变更包前缀段**，⛔ 不允许出现在词间任意位置；顺带跑一次 `--resolve-conflicts` 前的全量 conflicts 复核确认无其它场景已被这条误判污染；(b) 暂缓，接受调度器每次刷新台账都可能再产出同类误绑依赖，需人工在派车前逐条核 | a（正确性 bug，已产生一次真实的误判「ready」，虽然本次被拦下但下次不一定） | 已答 | 无需派工·就地销号：`0920R` 核实 `_match_plans` 首段前缀回退已限定「紧接日期戳之后」，docstring 写死 `voice-structured-interview` 反例，由 `commit 5c8ed9c`（09-18 10:13，**早于本条登记**）修掉（Shao Peishen 2026-09-20 答 1a，核实后未派工） | 已答 | 核实为已修复：`commit 5c8ed9c`（2026-09-18 10:13，`fix(dispatcher): 收紧前缀回退匹配+拆段兜底改3个一段（0918AI relay R-13/R-14）`）已把 `_match_plans` 前缀回退分支改成 `re.match` 锚定字符串开头＋可选日期戳前缀，早于本条待答登记（`095d1df`，09-18 09:21），本条原文引用的旧正则已不在代码里，无需重复修（Cowork·0918AO 2026-09-19 巡检核实并回填；Shao Peishen 22:0x 原「(a) 现在派」授权因此收窄为「核实关闭，不派车」） |
| Q-49 | M2 | 决策（G3 闸门·发布，不可代） | 【G3 发布】`M2 U3 硬门槛引擎（标记＋依据＋申诉）`：单元合 main 且 `tasks.md` 4.1–4.6 全部回勾 ｜ 产出：`0918X/Y/Z` 三条 opener 建造记录，合入 commit `182383d`；全量 pytest 3322 passed／5 failed（均为已知无关既有缺口，非本单元回归）／6 skipped；已接线到 `.51` 现网既有的上传/重解析流程（触发点：上传后判定＋只读查询接口＋申诉登记/流转接口，鉴权已接入）；新增表 `appeal_event`（`CREATE TABLE IF NOT EXISTS`，非破坏性迁移）。⚠️ 与 Q-44 不同：Q-44 预授权仅覆盖 U1/U2/U2.5，未覆盖本单元，代码虽随 `0918AM` 同步已落到 `.51` 磁盘但未接线激活（上次发版报告标「超范围携带项，未改变现网行为」） | (a) 答「发」⇒ 放行，走与 `0918AM` 同款流程（快照→依赖→sync→G-e→冒烟，不过即回滚）；(b) 答「改：…」⇒ 按批注改后再追加一行回闸，或逐条改后答「发」；(c) 答「作废」⇒ 暂不发，U3 代码留在 main／`.51` 侧保持惰性不接线 | 无默认（闸门须本人放行；答复列由 Cowork 转写为字面「发」，⛔ 不填字母 a/b） | 本轮建造实核（`0918X/Y/Z` 日志＋`tasks.md` 直读，Cowork·0918G 2026-09-19） | `m2-resume-parse-and-rank/U3` | 已答 | 发（Shao Peishen 2026-09-19 22:0x，经 Cowork·0918AO 巡检转达；执行 opener `0919O`）。执行结果：`0919O` 自核第一步（全量 pytest 重跑比对）拦停，**未发版**——本行「产出」列引用的「5 failed 均已知无关」与实测不符（重跑得 4 failed，逐条比对无一条在原名单内，原名单里唯一「真回归」项也从未修过），`.51` 未动、无需回滚。详见 `docs/session接力.md`「二、下一步」`0919O` 条目；需按其登记的两项待办修完 M3 语音面试遗留缺口、重新核实失败名单后，再请本人对新名单重新答一次「发」 |
| Q-50 | 构建自动化 | 决策（答复→任务映射缺失） | 【答复→任务映射缺失】Q-47：已答「a：现在派 `0919P` 修（Shao Peishen 2026-09-19 22:0x，经 Cowork·0918AO 巡检转达）」，`rules.md` §7 无 `Q-47a` 映射，该行「阻塞的任务 id」保持阻塞（保守），⛔ 调度器不猜后续动作 | (a) 补映射：在 `scripts/dispatcher_answers.py` `ANSWER_MAP` 与 `rules.md` §7 增加 `Q-47a`（新任务／保持阻塞原因）后重跑生成器；(b) 答「无新动作」⇒ 只解阻塞，不生成任务 | a | `scripts/dispatcher_answers.py` ｜ `.claude/skills/task-dispatcher/rules.md` §7 | — | 已答 | b：无新动作——`Q-47` 已由 Cowork 于 22:23 经 `launch` 通道手动派发 `0919P`（未经自动化 pipeline），无需调度器再生成任务；仅解阻塞，不补 `ANSWER_MAP`/`rules.md` §7 映射。代批：Cowork·0918AO，2026-09-19 22:3x，依据 CLAUDE.md「可代」§不改变外部可观察行为的工具链操作（本决策不涉合规/生产发版/对外通道/预算）。如需以后自动化同类映射，请回这行改答 (a) |
| Q-51 | M2 | 决策（G3 闸门·发布，不可代） | 【G3 发布·重新核实】`M2 U3 硬门槛引擎` 沿用 Q-49 同一发布请求，`0919O` 拦停时指出该行「产出」列引用的判据失真（未发版）；`0919T` 已把 `0919O` 登记的四条真实失败（M3 语音面试 U2/U3/U4 合入带来的 effect 测试覆盖缺口：`_ADDED_COLUMNS` 护栏未更新、迁移测试缺两张空壳表＋一处陈旧断言、`effect_send_verification_code` 缺装饰器、`EFFECT_NODE_MANIFEST` 漏登 18 个新节点）全部修复转绿，`git merge --no-ff` 回 main（merge commit `a6df909`）并 `push` 成功。全量 `pytest -q` 重新核实为 **3716 passed／0 failed／10 skipped**（详见 `docs/session接力.md`「二、下一步」`0919T` 条目）。是否现在放行发布？ | (a) 答「发」⇒ 放行，走与 `0918AM` 同款流程（快照→依赖→sync→G-e→冒烟，不过即回滚）；(b) 答「改：…」⇒ 按批注改后再追加一行回闸；(c) 答「作废」⇒ 暂不发，U3 代码留在 main 保持惰性不接线 | 无默认（闸门须本人放行；答复列请写明字面「发」，⛔ 不填字母 a/b/c） | `0919T` 全量 pytest 实测（`venv` 内 `python -m pytest -q`，3716 passed/0 failed/10 skipped） | `m2-resume-parse-and-rank/U3` | 已答 | 发（Shao Peishen 2026-09-20 00:4x，经 Cowork·0918AO 转达）；执行完成：`0920A` 发版 commit `8596829`，冒烟全过未回滚，见 `docs/releases/2026-09-20-发版四-M2U3硬门槛引擎执行记录.md` |
| Q-53 | M3 | 决策（预算与外部采购，不可代） | `voice-structured-interview/0.4` 语音主机采购与预算（规格依 1.2/1.3 资源占用实测）尚未拍板，台账已标「阻塞·决策」但此前未登记进本队列。本轮唤醒 `voice-structured-interview/U4-seg5`（0920H 前一批）已合 main，`5.1–5.10` 全部完成，下游 `5.11 live e2e（内部模拟 1 场）` 因此在依赖图上转为「就绪」，但其真实前置「0.2 P1–P3 通过 ＋ 0.4 主机到位」里的 0.4 仍未满足——本会话未派车（判据：任一前置不满足本章应「⏸ 留步」，不判整包失败）。附带发现：`voice-structured-interview/5.11` 在台账依赖图里没有正式挂 `0.4` 作为依赖，只在「单元」说明文字里提，导致自动就绪计算漏了这层前置；这是 `dispatcher_backlog.py` 依赖图的结构性缺口（同类此前见 Q-47/Q-48），修复需专门 opener（红线③），本条不代为处理，只记录现象以防下次自动派车误判 | (a) 现在采购：本人指定规格并下单／开通语音主机（公网 IP＋域名可用），到位后回填 `docs/m3-voice-probe.md`「目标机」节，`0.4` 转完成，`5.11` 才真正就绪；(b) 暂缓：M3 语音链路继续停在代码就绪、e2e 待机器阶段，`5.11` 及以下（7.x 视图、8.x 上线）保持不派车 | 无默认（须本人明确答复；预算与外部采购不可代） | `docs/roadmap/任务台账.yaml` `voice-structured-interview/0.4`；`docs/m3-voice-probe.md` | `voice-structured-interview/0.4`、`voice-structured-interview/5.11` | 已答 | b：暂缓采购（Shao Peishen 2026-09-21）。语音链路停在代码就绪态，`voice-structured-interview/0.4` 与 `5.11` live e2e 继续挂起，⛔ 不因此阻塞 M3 章节 7 等其余单元 |
| Q-54 | 构建自动化 | 决策（opener 机制变更，是否现在派工改） | 【`0920I` 事后复盘】`0920I` 在四条完成判据里有一条实测为 0（`grep -c 'upeak'` 应 ≥1）的情况下仍自报 `OPENER_DONE` 并合了 main；`0920J` 已补齐那两处遗漏。根因与当天治的「上下文守卫是提醒型、没有消费者」同族：**opener 的完成判据也是提醒型、没有消费者**——判据只写在正文靠泳道自觉核对，`run-lanes.sh` 只认哨兵字样，泳道自报 `OPENER_DONE` 就算数；`0920G`/`0920H` 没出事是因为它们的判据落成了 pytest（有消费者）。`docs/session接力.md`「二、下一步」`0920I` 条目已把此项标记「待 Shao Peishen 定夺后派 opener」 | (a) 现在派 opener：opener 正文新增固定一节「## 机器判据」写成可执行 shell 块（每行一条、附期望值），`run-lanes.sh` 收工时自己跑一遍该块，任一条不符 ⇒ 状态降为 `PARTIAL`（附不符项），泳道自报的 `OPENER_DONE` 不算数；无该节的 opener 维持现状不拦；(b) 暂缓：维持现状，opener 完成判据继续靠泳道自觉核对，同类「判据没过照样 DONE」可能再发生但不报错 | a（同日 `0920H`/`0920I`/`0920J` 均已按「现在派」处理，属同族收尾） | `docs/session接力.md`「二、下一步」`0920I` 条目 | `relay:tag#5c585390` | 已答 | a：现在派——泳道收工时自跑一遍 opener 的「## 机器判据」，不符即降 PARTIAL，自报完成不算数（`0920K` 已落地该闸，本条为同族收尾） |
| Q-55 | 构建自动化 | 决策（答复→任务映射缺失） | 【答复→任务映射缺失】Q-48：已答「无需派工·就地销号：`0920R` 核实 `_match_plans` 首段前缀回退已限定「紧接日期戳之后」，docstring 写死 `voice-struc」，`rules.md` §7 无 `Q-48` 映射，该行「阻塞的任务 id」保持阻塞（保守），⛔ 调度器不猜后续动作 | (a) 补映射：在 `scripts/dispatcher_answers.py` `ANSWER_MAP` 与 `rules.md` §7 增加 `Q-48`（新任务／保持阻塞原因）后重跑生成器；(b) 答「无新动作」⇒ 只解阻塞，不生成任务 | a | `scripts/dispatcher_answers.py` ｜ `.claude/skills/task-dispatcher/rules.md` §7 | — | 已答 | a：补上 `Q-48` 的答复→任务映射，消除重复提示 |
| Q-56 | M2 | 决策（答复→任务映射缺失） | 【答复→任务映射缺失】Q-08：已答「已提供：0920M 实跑，见 data/eval/m2-pilot/smoke-win51.json 与 docs/m2-model-comparison.md」，`rules.md` §7 无 `Q-08` 映射，该行「阻塞的任务 id」保持阻塞（保守），⛔ 调度器不猜后续动作 | (a) 补映射：在 `scripts/dispatcher_answers.py` `ANSWER_MAP` 与 `rules.md` §7 增加 `Q-08`（新任务／保持阻塞原因）后重跑生成器；(b) 答「无新动作」⇒ 只解阻塞，不生成任务 | a | `scripts/dispatcher_answers.py` ｜ `.claude/skills/task-dispatcher/rules.md` §7 | — | 已答 | a：补上 `Q-08` 的答复→任务映射，消除重复提示 |
| Q-57 | M3 | 决策（答复→任务映射缺失） | 【答复→任务映射缺失】Q-53：已答「b：暂缓采购（Shao Peishen 2026-09-21）。语音链路停在代码就绪态，`voice-structured-interview/0.4` 与 `」，`rules.md` §7 无 `Q-53b` 映射，该行「阻塞的任务 id」保持阻塞（保守），⛔ 调度器不猜后续动作 | (a) 补映射：在 `scripts/dispatcher_answers.py` `ANSWER_MAP` 与 `rules.md` §7 增加 `Q-53b`（新任务／保持阻塞原因）后重跑生成器；(b) 答「无新动作」⇒ 只解阻塞，不生成任务 | a | `scripts/dispatcher_answers.py` ｜ `.claude/skills/task-dispatcher/rules.md` §7 | — | 待答 | |
| Q-58 | 构建自动化 | 决策（答复→任务映射缺失） | 【答复→任务映射缺失】Q-54：已答「a：现在派——泳道收工时自跑一遍 opener 的「## 机器判据」，不符即降 PARTIAL，自报完成不算数（`0920K` 已落地该闸，本条为同族收尾）」，`rules.md` §7 无 `Q-54a` 映射，该行「阻塞的任务 id」保持阻塞（保守），⛔ 调度器不猜后续动作 | (a) 补映射：在 `scripts/dispatcher_answers.py` `ANSWER_MAP` 与 `rules.md` §7 增加 `Q-54a`（新任务／保持阻塞原因）后重跑生成器；(b) 答「无新动作」⇒ 只解阻塞，不生成任务 | a | `scripts/dispatcher_answers.py` ｜ `.claude/skills/task-dispatcher/rules.md` §7 | — | 待答 | |
| Q-59 | 人事部 | 决策（跟进信回件处置口径，不可代） | `人事部#3`（整信合并 A/B/C/D 场景＋`人事部#2` 决策点a/b/c）收到两条部分命中回件：决策点a（0920 回件，仅标题「10个岗位需求清单」无实际清单内容）、决策点b（0921 回件，仅答「选择：1」，缺投入小时数或牵头人姓名）。章程 §二 只有「实质回件⇒转闭环」「非实质回件⇒还原」两种整信判据，没有「合并信里只部分命中」的处置规则，拆件会话（`0920` 一轮、`0921C`）均判「待人」，两条 `unpack-signal.json` pending 均未清 | (a) 两条均视为不完整，退回汤丽萍补齐（决策点a要清单实际内容、决策点b要小时数或牵头人姓名），`人事部#3` 维持整信待齐 A/B/C/D 场景与决策点a/b/c 全部到齐后一次性转闭环；(b) 两条均视为已足够，接受当前答案就地关闭决策点a/b，`人事部#3` 允许按决策点分批部分关闭，不必等 A/B/C/D 与决策点c 到齐 | 无默认（回件是否够格构成完整答复只有本人能判） | `docs/session接力.md`（2026-09-20 决策点a 登记、2026-09-21 `0921C` 决策点b 登记） | `relay:tag#4391ebf4`、`relay:tag#8f75eddb` | 待答 | |

## 二、远期定夺（前置未齐，暂不催答；前置齐了由调度器移入「待答」）

| 编号 | 场景 | 阻塞类型 | 问题 | 前置 | 来源 | 阻塞的任务 id |
|---|---|---|---|---|---|---|
| Q-F1 | M2 | 决策（真实简历处理范围） | 7.6 首批标注回件导入结果确认 | Q-04 回件到达＋U6 合 main | `tasks.md` 7.6 | `m2-resume-parse-and-rank/7.6` |
| Q-F2 | M2 | 决策（不可代） | 8.8 真实简历入库闸开启（本人亲自改配置） | 0.2 通过（Q-02）＋3.2 上线＋8.4 绿 | `tasks.md` 8.8 | `m2-resume-parse-and-rank/8.8` |
| Q-F3 | M2 | 决策（`.51` 发版，不可代） | 8.9 M2 发版决定（含 U0 冒烟通过的新依赖、`sync-to-server.sh` 白名单） | U7 合 main＋Q-08 | `tasks.md` 8.9 | `m2-resume-parse-and-rank/8.9` |
| Q-F4 | M3 | 决策（新场景启动） | 是否启动场景 M3 实时语音面试的需求收敛（grill） | 路线图：M2 上线后 | 路线图 §二 M3 行 | `scene:M3` |
| Q-F5 | M0 | 外部输入（时间） | 8.8 一周观察窗 09-24 到期后收观察结论 ⇒ 勾 8.8 ⇒ 归档 `hr-wecom-aibot-liaison` | 2026-09-24 | `docs/session接力.md` R-6 | `relay:R-6`、`hr-wecom-aibot-liaison/8.8` |
| Q-52 | M2 | 决策（现网复现方式，外部输入依赖他本人操作） | 【`0920B` 诊断结论待定后续】Shao Peishen 现场实测 `0920A` 发版后「岗位列表」标签页读取失败；`0920B` 只读诊断已排除服务端问题（`index.html` 与仓库逐字节一致、`/api/jobs` 两次实测均 200＋24 条真实数据、`app.log` 自 08:44 重启以来零 ERROR/Traceback），关键证据：他 08:53:31 那次浏览器会话之后日志里再没出现过任何 `/api/jobs` 请求——即请求根本没发到服务器，根因大概率在客户端（浏览器缓存了发版前旧 JS，或该次点击遇到客户端侧异常），但未接触他本人浏览器 DevTools，不能 100% 排除边缘可能。是否需要他 `Ctrl+Shift+R` 硬刷新后重新点一次「岗位列表」，同时开 DevTools 网络面板看那次点击到底发没发请求、发到哪个 URL？　⇢ 2026-09-20 Shao Peishen 答「挂起到回 LAN」：本机 off-LAN（`.51:8095` 探针不可达），出不了客户端侧一手证据，转 LAN 收口 ⇢ 2026-09-20 15:5x `0920R` 经他本机 Chrome 实证推翻 off-LAN 前提：`.51:8095` **可达**（Mac 一直在网；先前的 off-LAN 判定源自挂载侧探针代理 403 假阴，即 `0920L` 刚修掉的那条失效判据）。点「岗位列表」后 4 个 XHR 全部 `net::ERR_BLOCKED_BY_CLIENT`（`/api/jobs`×3、`/api/queues/needs-manual`×1）——**请求被客户端拦下，根本没出浏览器**，与 `0920B`「服务端日志里再无该请求」逐字吻合，根因指向浏览器扩展/策略拦截而非缓存旧 JS。⚠️ 尚存一个混淆项：Claude 浏览器面板对私网站点逐次授权，也可能拦子请求；排除它只需他在自己常用窗口点一次「岗位列表」看 DevTools。 ⇢ **2026-09-20 16:1x 收口**：他本人窗口实测岗位列表 24 条正常，面板那次 `ERR_BLOCKED_BY_CLIENT` 系面板逐次授权假阳、归因作废；现网正常、08:53 那次不可复现，本条关闭，再现时当场抓 DevTools。 | 回 LAN 在网（`.51:8095` 可达）＋ 普通浏览器一次刷新+截图 | `0920B` 诊断日志 `.claude/handoff/lanes-20260920-085836/诊断岗位列表读取失败-0920B.log` | — |

## 三、已答 / 作废

| 编号 | 问题 | 答复 | 落档 |
|---|---|---|---|
| （路线图 §六 四项 G3／G4／G7／M2 需求树） | 2026-09-17 答 `1a，2a，3a，4a` | G3 `0917Y`、G4 `0917AA`／`0917AE`、G7 `0917AC`、M2 `0917AD`／`0917AF` | 已落档，不入队 |
| （M2 design 六问 Q1–Q7） | 2026-09-17 答 `1a，2a，3是，4b，5是，6是` | `tasks.md` 0.5／0.6 | 已落档，不入队 |
| Q-F4 | 是否提前启动 M3 实时语音面试的需求收敛 | 2026-09-17 17:5x 答「否」：维持路线图 M2 上线后再问 | 前置表 Q-F4 行保留，到前置时调度器再入待答 |
| Q-F4（改答） | 按任务驱动口径是否现在启动 M3 及 MVP 外四场景的需求收敛 | 2026-09-17 21:5x 答「是」：取代 17:5x「否」，grill 零阻碍即开，intent 落档停 G1（依据 docs/roadmap/任务驱动上线路线图.md 波次 0） | — |
