# 定时回查正文 · `0918H` 受限会话投递中继

> 由 Cowork·`0918G` 2026-09-18 09:2x 建立（引用式回查：定时任务只指向本文件，改判据改这里，⛔ 不反复更新定时任务本身）。
> 仓库根：`/Users/paulshao/Projects/HumanResource`（本机 zpzndemac-studio-local）

## 一、这次回查要判什么

`0918H`（泳道「投递中继」，分支 `lane-0918h-relay`）是否已收敛。它由 `0918I` 落的发车请求带起，起跑前会排在 `0918F`／`0918E` 之后。

## 二、怎么看（只读，不写）

本机没有 device_bash 时用 `device_list_dir` ＋ `device_stage_files` 读，⛔ 不要试图写 `.claude/**`（远端工具对该目录只读）。

1. `device_list_dir` 列 `.claude/handoff/`，找**文件名含 `投递中继-0918H.log` 的最新 `lanes-*` 批次目录**。
2. 找到了 ⇒ 读该目录的 `summary.txt`、`results.tsv`、`投递中继-0918H.log`（日志读尾部 80 行即可，⛔ 不整读）。
3. 没找到 ⇒ 列 `.claude/handoff/launch/` 与 `launch/queue/`，确认那条 `--only 0918H` 的请求还在排队；同时看 `dispatcher/launchd.log` 尾部确认发车器还活着。

## 三、按结果分档

- **OK**：把收工三行（合入 commit ／ passed·failed ／ 第五节两条真身自检的实际结果）原样报给 Shao Peishen，并补一句「受限会话投递中继已生效，Cowork 侧此后可自行提交与发车」。报完 `list_triggers` 找到本回查任务、`delete_trigger` 销掉它。
- **FAIL ／ `OPENER_PARTIAL`**：报状态、失败步骤与日志里的原因原文，列出留步项。⛔ 不自行修、不重投发车、不改台账状态。销掉本回查任务（改由 Shao Peishen 定夺下一步）。
- **仍在队列或在跑**：⛔ **静默退出，不推送、不打扰**。除非距建立本回查已超过 4 小时仍未起跑 —— 那时报一次「疑似发车链停摆」，附 `launch/queue/` 里还剩哪些件、`dispatcher/launchd.log` 尾部三行。

## 四、红线

⛔ 不越闸（G1–G5 一律不碰）、不改 `docs/roadmap/任务台账.yaml` 状态、不改 `docs/roadmap/定夺队列.md`、不发任何对外消息或跟进信、不在仓库跑 `git` 命令。本回查只读 ＋ 报告 ＋ 销号。
