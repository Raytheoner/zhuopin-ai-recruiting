# 环境就绪清单

> 面向：项目全体成员（5-6 人）
> 工作界面：**Claude Cowork + Claude Code GUI**，尽量不碰命令行

---

## 0. 一句话

装两样东西（OpenSpec CLI、Superpowers 插件），跑一次 `env-ready` 自检。之后日常只有三个技能名要记。

---

## 1. 三个日常技能

调用方式：输入 `/` 选，或者直接用中文说（skills 会按描述自动匹配）。

| 技能 | 干什么 | 什么时候用 |
|---|---|---|
| `openspec-propose` | 出需求与契约（proposal · specs · design · tasks） | 开一个新模块 |
| `spec-to-plan` | 把 spec 转成实现计划 | 契约确认后 |
| `run-build` | 子代理驱动开发 + 两阶段 review | 计划确认后 |

另有 `env-ready` 做环境自检，`openspec-archive-change` 做收口。

**不要用 `openspec-apply-change` / `/opsx:apply`** —— 它没有 TDD、没有 review gate。原因见 `03-工具链协作规则.md`。

### 为什么是 skills 而不是斜杠命令

`.claude/commands/` 是**遗留格式，只在 Claude Code 终端生效**。在 Desktop 的 Chat / Cowork 里输入会得到「Some commands only work in the Claude Code terminal」。

`.claude/skills/` 在**终端、Desktop、Cowork 都能用**，而且支持按描述自动触发。所以本项目的规则真源全部放在 skills 里；`.claude/commands/` 下只留了三个一行的入口文件供终端用户使用。

> 旁证：OpenSpec 自己也同时装了两套（`.claude/commands/opsx/*` 和 `.claude/skills/openspec-*`），就是因为知道 commands 覆盖不全。

---

## 1.5 哪个界面做哪件事

这是本项目最容易踩的坑，先看清楚：

| 阶段 | 在哪做 | 要不要 worktree | 为什么 |
|---|---|---|---|
| 需求与契约（propose） | **Cowork 或 Desktop 均可** | ❌ 不要 | 只读写项目文件 + 跑 openspec |
| 文档、调研、方案讨论 | **Cowork** | ❌ 不要 | 长任务、后台跑、不占你的界面 |
| 出实现计划（spec-to-plan） | **Desktop Code tab / 终端** | ❌ **不要** | 只产出一份 md 计划，不碰代码 |
| 写代码（run-build） | **Desktop Code tab / 终端** | ✅ **要** | 真正写代码、提交、合并 |
| git 提交与推送 | **Desktop Code tab / 终端** | — | Cowork 沙箱对 `.git/` 无写权限 |

**建了 worktree 之后**，记得把 `.env`、本地 SQLite/`data/`、Python venv 补进去——这三样都被 gitignore 挡着，git 不会带过去，而缺失时的报错看起来完全不像"文件没拷"。

**关键区别**：Cowork 的 bash 跑在隔离 Linux VM 里，装的东西不落到你 Mac 上，git 操作也不可靠。它适合"读写文件 + 想清楚"，不适合"真刀真枪写代码"。

---

## 2. 首次安装（每台机器一次）

### 2.1 Superpowers 插件

在 Claude Code 里输入：

```
/plugin install superpowers@claude-plugins-official
```

插件安装必须由人操作，agent 代劳不了。

### 2.2 OpenSpec CLI

**这一步容易漏，漏了 `/opsx:*` 全部会报 command not found。**

在 Claude Code 里直接说：

> 帮我全局安装 OpenSpec CLI

Claude Code 会自己跑 `npm install -g @fission-ai/openspec@latest`。若遇权限错误它会改用 `~/.npm-global` 前缀并提示你配 PATH。

前置：Node ≥ 20.19.0。

### 2.3 自检

在 Desktop 里说「跑一下环境自检」，或输入 `/` 选 `env-ready`。

它会逐项检查 Node / OpenSpec / Superpowers / Python / Git / 脏文件，**能自己修的当场修**，把需要你动手的和需要换界面做的分开列出来。

---

## 3. 仓库

```
git clone <repo-url> ~/Projects/HumanResource
```

**硬红线**：clone 目录必须在本地磁盘，**不能放 iCloud Drive 或任何云同步目录**（文件锁冲突 + 同步滞后会让 git 和 worktree 行为诡异）。

Superpowers 用 git worktree 做隔离开发，这条尤其重要。

---

## 4. 凭据（不入库）

`.env` 已被 `.gitignore` 保护，**绝不能走 GitHub 或任何公开管道传输**。走加密 U 盘、1Password 共享库或内网点对点。

拿到后设权限：

```bash
chmod 600 .env
```

需要的凭据（W1 陆续到位）：

| 变量 | 用途 | 谁申请 |
|---|---|---|
| `LLM_PRIMARY_*` / `LLM_FALLBACK_*` | 主/备 LLM 供应商，W1 实测后定型 | 技术负责人 |
| `WECOM_CORP_ID` / `WECOM_AGENT_ID` / `WECOM_SECRET` | 企业微信自建应用 | IT，**尽早启动，是外部依赖** |
| `WECOM_CALLBACK_TOKEN` / `WECOM_ENCODING_AES_KEY` | 回调签名校验 | IT |
| `DATABASE_URL` | Postgres | 技术负责人 |

企微回调需要**公网 HTTPS 可达地址**，这是 W2 的外部依赖，卡住会拖累整条链路。

---

## 5. 数据红线

| 东西 | 能不能入库 | 放哪 |
|---|---|---|
| 真实简历、候选人信息 | ❌ 绝对不行 | 本地 `data/`，已 gitignore |
| 200 份评测集 | ❌ | 本地 `evalset/`，已 gitignore，走安全通道分发 |
| 面试录音 | ❌ | 对象存储，有留存期限 |
| `.env` | ❌ | 本地，chmod 600 |
| `openspec/` 全部产物 | ✅ 必须入库 | 这是需求真源 |
| `docs/superpowers/plans/` | ✅ 入库 | 实现计划，可追溯 |
| `.superpowers/` | ❌ | 运行时 scratch，已 gitignore |

---

## 6. 日常流程

```
新模块  →  openspec-propose      →  人工审阅 proposal 与 specs        [Cowork 或 Desktop]
        →  spec-to-plan          →  人工审阅计划（尤其 Global Constraints 段）  [Code tab]
        →  run-build             →  子代理执行，两阶段 review          [Code tab]
        →  final review 通过      →  回勾 tasks.md
        →  全部勾完               →  openspec-archive-change
```

**两个人工闸门**，别跳：

1. **specs 出来后**：这是给法务看、将来要变成验收测试的东西。写错了后面全错。
2. **计划出来后**：确认 Global Constraints 段在、且 spec 每条 Requirement 都有 Task 覆盖。这段是 reviewer 的注意力透镜，缺了后面每轮 review 都是瞎的。

---

## 7. 常见问题

**输入 `/xxx` 报「isn't a recognized command here」**
你在 Desktop 的 Chat/Cowork 里用了终端专属的斜杠命令。改用技能名（`env-ready` / `spec-to-plan` / `run-build`），或者直接用中文描述你要干什么。

**openspec 相关操作报 command not found**
OpenSpec CLI 没装在你本机。见 2.2。注意 Cowork 里装的不算——那是隔离 VM。

**`spec-to-plan` 说调不到 superpowers**
你在的界面没暴露 superpowers 插件。换到 Desktop 的 Code tab 或 Claude Code 终端。**不要让它退化成手写计划**——手写的产物没有 `### Task N:` 结构，后面 `scripts/task-brief` 会解析失败。

**生成的计划没有 Global Constraints 段**
别往下走，重跑 `spec-to-plan`。缺这段等于关掉了所有质量门，而且不会报错。

**`run-build` 跑到一半会话断了**
直接重跑，指同一个计划路径。进度台账在 `.superpowers/sdd/progress.md`，标记完成的 Task 不会重跑。**不要 `git clean -fdx`**，那会毁掉台账。

**spec 改了怎么办**
重跑 `/plan` 重新生成计划，**不要手改计划文件**。需求真源永远是 `openspec/specs/`。

**小改动也要走全流程吗**
不用。判断标准是有没有改变外部可观察行为：改了行为走全流程；纯重构、修 bug、调参数直接 `/plan` + `/build`，甚至直接改。
