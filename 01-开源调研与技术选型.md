# 卓品智能 AI 招聘智能体 · 开源调研与技术选型论证

> 日期：2026-08-06 ｜ 方法：先调研、后选型、不写代码（依 `Prompt.md` 方法论）
> 状态：**待 Paul 确认后进入实现阶段**
> 数据可信度：本文所有 star 数与许可证均由子代理实际抓取，另经独立事实核查代理复核。**凡未通过复核的结论已用 ⚠️ / ❓ 明确标注，不得作为决策依据。**

---

## 0. 一句话结论

**这个赛道没有可以直接 fork 的东西。** 开源 ATS 的 star 天花板只有 ~720（OpenCATS，2009 年的 PHP），且许可证几乎全线踩雷；AI 招聘应用层则全是 demo。
**正确做法：借数据模型 + 站在文档解析和编排框架的肩膀上 + 自建匹配层。** 匹配层是唯一的差异化护城河，也是开源空白。

---

## 1. 开源项目盘点

### 1.1 ATS / 招聘管理系统

| 仓库 | Star | 最后 push | 许可证 | 栈 | 判定 |
|---|---:|---|---|---|---|
| [opencats/OpenCATS](https://github.com/opencats/OpenCATS) | 717 | 2026-08-03 | ⚠️ MPL-2.0 **+ CATS PL 1.1a：明文禁 SaaS、强制"Powered by CATS"角标** | PHP8+MySQL | 只抄数据模型 |
| [odoo/odoo](https://github.com/odoo/odoo) `hr_recruitment` | 53,519 | 2026-08-06 | LGPL-3（SaaS 可用） | Python+自研ORM+PG | 抄 stage 设计 |
| [horilla/horilla-hr](https://github.com/horilla/horilla-hr) | 1,298 | 2026-07-24 | LGPL-2.1 | Django5+HTMX | 抄 stage_type |
| [frappe/hrms](https://github.com/frappe/hrms) | 8,259 | 2026-07-23 | GPL-3 | Frappe+MariaDB | 传染性许可，排除 |
| [ever-co/ever-gauzy](https://github.com/ever-co/ever-gauzy) | 3,775 | 2026-07-12 | ⚠️ AGPL-3 | NestJS+Angular | 排除 |
| [reqcore-inc/reqcore](https://github.com/reqcore-inc/reqcore) | 11 | 2026-08-05 | ⚠️ AGPL-3 + open-core | Nuxt4+Drizzle+PG16 | **抄 AI 评分三件套** |
| [Assystant/SpotAxis](https://github.com/Assystant/SpotAxis) | 31 | 2025-10-06 | ✅ MIT | Django1.9 / **Py2.7 已 EOL** | 只抄阶段评分模型 |
| [nueno-co/nueno](https://github.com/nueno-co/nueno) | 265 | **2022-07** | AGPL-3 | Next+Prisma | 事实归档 |
| [WuKongOpenSource/Wukong_HRM](https://github.com/WuKongOpenSource/Wukong_HRM) | 1,579 | **2023-11** | 🚨 **非开源，企业内部使用即需付费授权** | SpringCloud | 法务红线 |
| [jeecgboot/JeecgBoot](https://github.com/jeecgboot/JeecgBoot) | 47,302 | 2026-07-30 | ⚠️ Apache-2.0 尾部私加"不得开发竞品" | SpringBoot+Vue3 | source-available，慎用 |
| [yangzongzhuan/RuoYi-Vue](https://github.com/yangzongzhuan/RuoYi-Vue) | 3,163 | 2026-08-04 | ✅ MIT | SpringBoot4+Vue2 | Java 栈可做基座 |
| [xiaonuobase/Snowy](https://github.com/xiaonuobase/Snowy) | 807 | 2026-08-05 | ✅ 纯 Apache-2.0 | SpringBoot+Vue3 | Java 栈可做基座 |

**中文圈无一个招聘域模型**：RuoYi / Jeecg / Snowy 是通用脚手架，国产 HRM 项目只做员工档案，不做 candidate/application/stage。

### 1.2 数据模型：五条值得抄、三个坑要避

**抄**

1. **Application 必须是独立实体**，不能把状态挂在 Candidate 上。OpenCATS `candidate_joborder(candidate_id, joborder_id, status, rating)`；OrangeHRM 已把 `Candidate.status` 标 `@deprecated` 并迁到 `CandidateVacancy.status` —— 实证"状态属于投递不属于人"。
2. **阶段流转事实表** `application_stage_history(stage_from, stage_to, at, actor)`。漏斗转化率、阶段停留时长全靠它。Reqcore 和 Horilla 都没建，所以做不出任何报表。
3. **Stage 语义标签**：Horilla 的 `Stage.stage_type ∈ {initial, applied, test, interview, cancelled, hired}` + `sequence` —— 显示名随便改，系统只认固定类型驱动逻辑。Odoo 的 `stage.job_ids` 空 = 全局 / 非空 = 限定该岗位，是"全局池 + 逐岗定制"的最佳折中。
4. **双状态分离**：Odoo 的 `kanban_state`（阶段内红黄绿）与 `application_status`（ongoing/hired/refused）分开存。
5. **自定义字段用 JSON 不用 EAV**：Reqcore 的 `propertyDefinition / propertyValue`（`jobId IS NULL` = 组织级，非空 = 按岗追加）。OpenCATS 的 `extra_field` EAV 是反面教材。

**坑**

- Horilla 把 Candidate 和 Application 合并（`unique(email, recruitment_id)`）→ 同一人投 3 个岗 = 3 条互不关联的记录 → **人才库彻底做不了**。这是最容易犯的错。
- `ihopeit/recruitment` 把三轮面试硬编码成 `first_* / second_* / hr_*` 宽表列，加一轮就得改表。
- OpenCATS 的阶段字典表全局唯一，无法按岗位定制。

### 1.3 简历解析层

| 仓库 | Star | 最后 push | 许可证 | 说明 |
|---|---:|---|---|---|
| [PaddlePaddle/PaddleOCR](https://github.com/PaddlePaddle/PaddleOCR) | 87,065 | 2026-07-22 | Apache-2.0 | 中文最优；❓ "PaddleOCR-VL 0.9B" 未通过复核，需自行确认版本 |
| [opendatalab/MinerU](https://github.com/opendatalab/MinerU) | 66,614 | 2026-06-05 | Apache **+ 附加条款**（对外提供服务须显著标注） | 输出带 bbox 的中间 JSON，利于证据回指 |
| [docling-project/docling](https://github.com/docling-project/docling) | 61,148 | 2026-06-07 | MIT | 自带 schema 抽取（beta）；中文版面能力未验证 |
| [Unstructured-IO/unstructured](https://github.com/Unstructured-IO/unstructured) | 15,269 | 2026-08-04 | Apache-2.0 | 格式覆盖最广；开源版被刻意弱化，双栏简历阅读顺序易乱 |

**简历专用开源项目全线阵亡**：`open-resume` 8.7k★ 但 AGPL + 停更 2024-10 + 只处理文本层；`pyresparser` GPL + 作者自弃；`OmkarPathak/ResumeParser` 迁到 Qwen2.5-1.5B 但**上下文仅 2048、输入截断约 1500 字符，中文简历一页都装不下**。
**结论：没有任何一个活跃维护的中文简历解析开源项目。这一层只能用通用文档解析 + LLM schema 抽取自己拼。**

### 1.4 匹配与评分层（开源空白）

- **`srbhr/Resume-Matcher` 27.2k★ Apache-2.0** —— 看起来最像，但是**求职者侧**工具，无多候选人排序。⚠️ 子代理称其算法为纯正则关键词加权（`0.55×keyword + 0.25×skills + 0.20×completeness`，无 embedding），**该结论未通过独立复核，采信前必须自己读一遍源码**。可确定的是：Python 正则 `\w` 匹配 CJK，`(?<!\w)算法(?!\w)` 在"推荐算法工程师"中会匹配失败——**中文场景需要实测**。
- **`interviewstreet/hiring-agent` 6.8k★ MIT（HackerRank 官方）** —— 纯 LLM rubric 打分，**可解释性是本次调研最强的**：schema 强制每个维度返回非空 `evidence`，另有 breakdown、扣分理由、strengths/gaps。坑：rubric 硬编码为"软件实习生"岗；重度偏 GitHub 开源画像，**内网私有仓的候选人直接归零**——对 ECU 嵌入式岗位是致命错配。
- **`re-cinq/hiring-bias` MIT** —— 不是匹配器，是 LLM 简历筛选的**反事实偏见审计台架**（Bertrand–Mullainathan 配对法，含盲筛轴）。**本次调研唯一的合规资产**，可直接改造成年度 bias audit 回归夹具。

**关键判断：未找到任何经验证的、生产可用的 bi-encoder + cross-encoder 招聘匹配开源实现。**「embedding 语义匹配」在开源侧基本是幻觉。且现存三个项目**都只给自然语言 evidence，没有一个做到证据回指简历原文 span/offset** —— 而这恰恰是 PIPL 说明权的硬要求。

**对抗性现实**：`AIHawk` 29.9k★ 和 `Resume-Matcher` 27.3k★ 都在帮候选人针对 JD 改写简历。**关键词匹配和语义相似度这两个维度已经被打穿了**，筛选必须建在不可伪造的证据上（量产项目、具体 MCU 型号、可追问的技术细节）。

### 1.5 AI 面试层

基础设施成熟、许可证友好；应用层无生产级标杆。

- [pipecat-ai/pipecat](https://github.com/pipecat-ai/pipecat) 13,947★ / BSD-2 · [livekit/agents](https://github.com/livekit/agents) 12,697★ / Apache-2.0（Agent 作为 participant 入房，天然支持录制回放 = 证据链）
- 中文语音：[FunASR](https://github.com/modelscope/FunASR) 19,503★ MIT · SenseVoice 9,018★ MIT（带情绪与音频事件标签）· [CosyVoice](https://github.com/FunAudioLLM/CosyVoice) 22,086★ Apache-2.0
- ⚠️ 中文 CER 对比（SenseVoice 7.81% / Paraformer 10.18% vs Whisper-large-v3 20.02%）来自 funasr.com，**属利益相关方 benchmark，须自测复核**。但方向可信：中文面试不要用 Whisper 系做主 ASR。
- ✅ [FoloUp/FoloUp](https://github.com/FoloUp/FoloUp) 1.2k★ **MIT**（已复核）—— 唯一像产品的招聘方 AI 语音面试系统（JD→自动出题→自适应面试→逐题打分），可 fork，把 Retell 换成 pipecat/livekit。
- [ngoanpv/DeepInterview](https://github.com/ngoanpv/DeepInterview) 19★ Apache-2.0 —— **prep / live / post 三段切分**共享 InterviewContext：prep 阶段离线预生成题目、难度曲线、rubric 和预埋追问，因此 live 链路只跑轻模型。**只抄这个设计，不要依赖这个项目。**
- **没有任何开源项目做了评分一致性校准（inter-rater reliability），全是 prompt 直出。**

### 1.6 Agent 编排框架

| 框架 | Star | 最后 push | 许可 | 持久化中断 | 判定 |
|---|---:|---|---|---|---|
| **LangGraph** | 39,034 | 2026-08-06 | MIT | `interrupt()` 落库，进程可关，PG/SQLite | ✅ **主选** |
| Dify | 151,348 | 2026-08-04 | 改良 Apache（仅限多租户与 LOGO） | Human Input 节点，默认超时 7 天 | ✅ 快速验证备选 |
| CrewAI | 56,697 | 2026-08-06 | MIT | `from_pending().resume()`，**仅内置 SQLite** | ⚠️ 仅 MVP |
| Mastra | 26,981 | 2026-08-06 | Apache | `suspend/resume` 跨重启 | TS 栈才考虑 |
| Temporal | 22,142 | 2026-08-06 | MIT | 最强，signal 无时长上限 | ❌ 二期（见下） |
| n8n | 199,570 | 2026-08-06 | Sustainable Use（非 OSI） | Wait node 有未修 bug | ❌ |
| LlamaIndex Workflows | 49,982 | 2026-06-04 | MIT | `WorkflowCheckpointer` 已移除 | ❌ |
| Pydantic AI | 17,767 | 2026-06-14 | MIT | ❓ "2.x 删除 persistence 包"**未通过复核，需重查** | ❌ 暂排除 |
| AutoGen | 59,586 | 2026-04-15 | MIT | ✅ 微软 2025-10-02 官宣停止新功能（[#7066](https://github.com/microsoft/autogen/discussions/7066)） | ❌ 已死 |

---

## 2. 技术选型结论

### 2.1 编排层：LangGraph + Postgres checkpointer

理由：**唯一为"持久化中断"原生设计**的框架。`interrupt()` 抛异常落库、`Command(resume=...)` 按 `thread_id` 恢复，挂起数天完全可行——这正是招聘场景的核心形态（简历筛完等 HR 确认 → 再发邀约；面试评完等面试官复核 → 再进下一轮）。

**必须遵守的四条铁律（均已独立核验）：**

1. ✅ **恢复时节点从头整个重跑。** 官方原文："the runtime **restarts the entire node from the beginning**—it does not resume from the exact line where interrupt was called."（[docs](https://docs.langchain.com/oss/python/langgraph/interrupts)）
   → **铁律：每个有副作用的动作（发邮件、建工单、写 ATS）独占一个节点。** 已有踩坑案例：一个节点里同时 `create_ticket` + `send_email`，恢复后建了两张工单还看不出异常。
2. ✅ **升级到 ≥1.0.10。** GHSA-g48c-2wqr-h844（CWE-502 msgpack 反序列化）。⚠️ 修正：CVSS 仅 **6.8 Moderate**，`AV:A/PR:H`，官方定性为 post-exploitation / defense-in-depth，需攻击者已能写入 checkpoint 存储。**升级是对的，但不是 P0 阻塞。**
3. checkpoint 会膨胀且**无官方 TTL 清理**，需自写清理任务；避开 pgbouncer / Supabase pooler。
4. checkpoint ≠ durable execution（无失败检测、两进程 resume 同一 thread 无锁）。**做法：把"人工审批"抽成独立接口，二期被打疼了再迁 Temporal。**

**为什么不选 Temporal**：✅ 已核验 [Temporal Cloud 无中国大陆节点](https://docs.temporal.io/cloud/regions)（亚太仅东京/首尔/孟买/海得拉巴/新加坡/悉尼/雅加达）→ 简历出境踩 PIPL → 只能自建 → 4 个服务 + PG + ES，4 人团队要分 0.5 人常驻运维。**能力够，成本不划算。**

**Dify 是真备选**：许可证对内部自用零约束，Human Input 节点 pause 状态落库、Celery 恢复。若团队 Python 弱或要两周出 demo，走 Dify；**要做成产品，走 LangGraph**。

### 2.2 解析层：通用文档解析 + LLM schema 抽取

`PaddleOCR`（或 MinerU）做版面 → Markdown/带 bbox 的 JSON → 境内 LLM + Pydantic/Outlines **schema 约束抽取** → 规则校验（日期连续性、枚举、必填）→ 低置信度进人工队列。

理由：Boss直聘/猎聘导出的简历常为图片型、带平台水印 → 纯文本层方案直接归零，必须走 VLM/OCR。量小不值得训模型。
**不要选** open-resume（AGPL + 停更 + 无中文）、pyresparser（GPL + 废弃）。

⚠️ **上线前必做**：用 [OmniDocBench](https://github.com/opendatalab/OmniDocBench) 的框架搭一个 **200 份真实简历的私有评测集**再定型。Boss直聘/猎聘导出格式的解析效果**全网无公开评测数据，必须自测**。

### 2.3 匹配层：自建三段式（这是唯一值得投入的差异化）

```
① 硬门槛规则  → ② embedding 召回 → ③ LLM rubric 精排
```

- **① 硬门槛**：ECU 岗技能词表（AUTOSAR CP/AP、MCAL、CAN-FD/UDS、Simulink、ISO 26262 ASIL、MISRA C、TC3xx/S32K/TI）+ 年限 + 学历。300 人企业岗位数少，**词表人工维护完全可行，且可解释性最好**。
- **② 召回**：BGE-M3 embedding。
- **③ 精排**：抄 `hiring-agent` 的 schema（每维度强制非空 evidence + 分项硬上限 + 显式扣分理由），**并补上开源界都缺的一环——evidence 回指简历原文 span/offset**。

**ECU 行业特化字段**（所有开源项目都没有，必须自建成结构化 `propertyDefinition`，而不是塞进全文让 LLM 硬猜）：功能安全资质（ISO 26262 ASIL 等级 / FuSa 工程师认证）、AUTOSAR CP/AP 经验、MCU 平台族（英飞凌 Aurix / NXP S32 / TI）、Bootloader/UDS/CAN-FD 诊断栈、量产项目经验（SOP 时间、定点车型）。

**合规硬要求**：`temperature=0`、固定模型版本、持久化「模型版本 + prompt 版本 + 输入哈希 + 输出 evidence」。**AI 只做排序，不做拒绝，终审必须人工。**

### 2.4 面试层：实时语音做，但放阶段二

**已决策**：LiveKit Agents + FunASR + CosyVoice **级联**，**不走端到端 speech-to-speech** —— 转写只是副产物、证据链弱、难以中途插 rubric 控制，而面试恰恰需要留痕。

放阶段二（W9-12）的理由不是工期，是质量：**`prep` 阶段的出题依赖阶段一跑出的真实评分数据**。先用简历评分跑两周真实招聘，才知道哪些能力维度简历上看不出来、必须靠面试问。

**延迟的关键在架构不在框架**：第三方实测 LiveKit 750-900ms、Pipecat 800-950ms，差异不大——真正决定延迟的是 live 段跑多重的模型。所以 `prep` 阶段离线预生成题目、难度曲线、rubric 与预埋追问，live 段只做轻量的追问选择。分段预算：endpointing+ASR 150-300ms、LLM TTFT ~500ms、TTS 首帧 100-200ms。

🚫 **人脸/表情分析不做**（见 3.3 与下方说明）。
🚫 **声学情绪信号（语速/停顿/静默）不进评分**，只作为参考信息展示给面试官。

> **为什么表情分析不做，第一理由不是法律**：用面部表情推断情绪状态在心理学上已被推翻——面部动作与情绪状态之间不存在跨情境、跨文化的可靠映射。面试场景更糟：紧张、思考、听不懂的表情高度相似，系统会系统性地给内向者、非母语者、有面部神经差异的人打低分。你会拿到一列**看起来精确、实际是噪声的分数**，然后用它做录用决策——一旦被质疑，举不出任何有效性证据。这正是构成就业歧视指控的形态。
> 想判断沟通表达 → 用答题**内容**的结构化评分（逻辑性、术语准确度、能否讲清复杂系统）。想防替考 → 那是**身份核验**，与情绪分析是两码事，活体+证件比对可做，但需单独同意、本地处理、与评分链路物理隔离。
> ⚠️ 另需法务确认：欧盟 AI Act 明确禁止工作场所情绪识别，若卓品有欧洲 Tier1/OEM 客户，可能成为供应商合规审查项（此条本次未验证到一手条文）。

### 2.5 模型选型

⚠️ **本节可信度最低，采购前必须重新核价。** 事实核查发现子代理给的型号有过时：

| 用途 | 建议 | 核验状态 |
|---|---|---|
| 字段结构化抽取（主力） | 豆包 **Seed2.1 Turbo**（官方写法无连字符） | ⚠️ 型号存在，**价格与 model ID 未取证** |
| 批量兜底 | DeepSeek-V4-Flash | ✅ 存在，但旗舰是 `deepseek-v4-pro`；⚠️ 注意峰谷价 |
| 读图/扫描件 | 阿里通义 | ❌ **"Qwen3.5-Plus" 已是旧版模型**。当前旗舰 `qwen3.8-max`，官方首推 `qwen3.7-plus` |
| 长上下文 | Kimi K3（1M 上下文） | ✅ 准确 |

⚠️ 另有一条待验证但影响很大的说法：**豆包在 `json_schema` 下守字段类型接近 100%，而 Qwen、DeepSeek 仅支持 `json_object`**（来源 V2EX 实测帖）。若属实，抽取主力必须用豆包。**建议第一周就自己跑一轮对比测试再定。**

**私有化**：日均数百份简历 ≈ 200 万 token/天，API 成本一天几块钱，**一期不建议自建 GPU**。若数据政策强制本地：单卡消费级 + Qwen 27B 级 AWQ-INT4 量化，整机 ¥5-8 万够用。

### 2.6 渠道对接：一期不买、不爬，手工导入

⚠️ **事实核查修正**：不是"四大平台没有 API"，而是"**没有自助注册的开放平台，但有商务签约制的 ATS 渠道对接**"——北森官网明示已对接智联、前程、58招聘、BOSS直聘等 170+ 渠道（但明确不提供简历）。

**已决策：一期不买 ATS。** HR 现状是 Excel + 微信/邮件，年招几十到百人，HR 本来就在各平台逐个看简历，导出一批 PDF 批量上传的成本极低。买 ATS 会在还不知道自己真正需要哪些接口时就锁定供应商，还要搭进采购周期与集成工时。等阶段一跑完一个招聘季，缺哪个口子会非常清楚，那时再谈判地位也不同。

🚫 **自建爬虫始终不做**——高合规风险路径（巧达科技案 ❓ 具体数字未能核实，但事件本身广为流传，方向明确）。

### 2.7 内部通道：企业微信 > 飞书

零迁移、审批 API 免费、能触达微信侧候选人。要点：

- 审批按钮必须走**自建应用 `template_card` + 公网 HTTPS 回调**（群机器人 webhook 无按钮无回调，限 20 条/分）
- 回调**只推一次、5 秒无响应即丢弃** → 必须先落库再处理
- **大坑**：2022-06-20 后新建应用的通讯录接口不再返回手机/邮箱，**候选人手机号必须自建库**

---

## 3. 合规红线（这一节比技术更容易翻车）

### 3.1 PIPL 第 24 条

⚠️ 修正：条文本身准确（第二十四条第三款：个人有权拒绝仅通过自动化决策方式作出决定），**但 PIPL 全文 74 条不出现"就业""招聘"任何一处**，"重大影响"无场景清单。

→ 合规文书中应写：「依第 24 条，**招聘录用等决定一般被认为构成"对个人权益有重大影响"**」，**不可作为法条明文引用**。
→ 工程含义不变：**淘汰必须有人工确认节点并留痕，AI 只做排序推荐。**

另注意：《算法推荐规定》第 20 条管的是**在岗调度**（报酬、工时），不是入职筛选，混引是常见错误。

### 3.2 PIA 前置评估

PIPL 第 55/56 条：自动化决策**必须事前做个人信息保护影响评估并保存 3 年**。这是 300 人企业最容易漏、又最容易被查的一条。**❓ 建议由法务复核条号后执行。**

### 3.3 人脸识别 —— 直接砍掉

✅ 已核验：《人脸识别技术应用安全管理办法》（**第 19 号令**）第 20 条自 **2025-06-01** 施行；第 6 条要求单独同意；第 8 条"应当存储于人脸识别设备内，不得通过互联网对外传输"（单独同意是该条的**例外**之一，非并列）。

→ **原方案第 5 节里"结合语气、表情生成评估报告"这条不能做。** 表情分析是最高风险、最低收益的功能。

### 3.4 AI 生成内容标识

✅ 已核验：《人工智能生成合成内容标识办法》第 14 条自 **2025-09-01** 施行，由网信办、工信部、公安部、广电总局**四部门联合发布**（国信办通字〔2025〕2 号，形式为通知）。

→ **AI 生成的 JD、拒信、邀约话术须标注。**

### 3.5 其他

- **无需大模型备案**：《生成式 AI 暂行办法》第 2 条第 3 款「未向境内公众提供…不适用本办法」。但公开官网上线开放注册的 AI 面试入口有被认定"向公众提供"的空间 → **用一次性邀请链接**。
- **跨境**：年处理不满 10 万人可豁免申报，但**含敏感个人信息即不豁免** → 模型全走境内，别赌。
- ❓ 未检索到国内 AI 招聘歧视的行政处罚案例。国外 Mobley v. Workday 已把工具供应商列为被告，自研意味着自己举证。
- **绝不用历史录用结果做监督信号**（Amazon 2018 年因此废弃整个项目），改用显式岗位能力 rubric。

---

## 4. 与原方案的差异（哪些要改）

| 原方案 | 最终决定 |
|---|---|
| AI 视频面试官，含语气/表情分析 | ✅ 实时语音面试**做**（阶段二，LiveKit 级联）；🚫 **表情/情绪分析砍掉**（无效性 + 合规）；声学情绪信号只展示不计分 |
| Agent 自动发拒信 | ⚠️ 改为 Agent 起草、HR 确认。固化为**系统强制断点**而非流程约定 |
| RPA 爬取简历 | 🚫 不爬也不买。一期手工导出批量上传，跑完一季再决定是否采购渠道对接 |
| "简历智能评分自动过滤明显不符项" | ✅ 拆成两层：**规则硬门槛自动过滤**（可解释、可申诉、写 `rejection_record`）；**AI 评分只排序**，HR 批量勾选确认淘汰。效率收益拿到 95%，每批多花 HR 两分钟 |
| GPT-4/文心一言 | 改为境内模型（豆包/DeepSeek/通义），W1 先跑一轮 schema 遵循度实测再定型 |
| 自动化率 80-90% | ⚠️ **KPI 改为"人时下降"而非"自动化率"**。有强制人工断点时，"自动化率"是个会误导决策的指标 |
| ATS 选型 Moka/北森/自研三选一 | **完全自研**。渠道对接留到二期按需采购 |
| 一次性做完七个 Agent | **分两段交付**：W1-8 简历闭环上线（M1+M2），W9-12 实时语音（M3）。出题质量依赖第一段跑出的真实数据 |

---

## 5. 已决策事项（2026-08-06 与 Paul 确认）

| 问题 | 决定 |
|---|---|
| ATS 底座 | 完全自研，一期不买 Moka/北森；简历走手工导出批量上传 |
| 技术栈 | Python + LangGraph |
| 工期与人力 | 分两段：W1-8 交 M1+M2 并真实上线，W9-12 交 M3 实时语音；4 人起，W3 加到 5-6 人 |
| 实时语音面试 | 做，放阶段二 |
| 表情/情绪分析 | **不做**，改用答题内容的结构化评分；防替考单独做身份核验（隔离链路） |
| 自动淘汰 | 规则硬门槛自动过滤 + AI 排序后 HR 批量确认 |

详见 `02-系统架构与MVP范围.md`。

---

## 6. 本文的可信度自评

| 结论 | 可信度 |
|---|---|
| 开源 ATS 盘点、star、许可证 | 高（实抓 + 部分复核） |
| 数据模型设计建议 | 高（基于实际 schema） |
| LangGraph 选型与四条铁律 | 高（官方文档逐字核验） |
| 合规条款（人脸、AI 标识、Temporal 区域） | 高（一手来源核验） |
| PIPL 24 条适用性 | 中（条文准确，场景归属为学理解释） |
| Resume-Matcher 算法内部实现 | **低（未通过复核，采信前必须读源码）** |
| 2026 年模型型号与定价 | **低（已发现型号过时，必须重新核价）** |
| 巧达科技案数字、海淀法院判例 | **极低（查无来源，不得写入任何对外材料）** |
| 中文 ASR CER 对比数据 | 低（利益相关方 benchmark，须自测） |
