# 发布运行手册

> 决策与约束见 `04-部署与门户挂载.md`（**为什么**）；本文是**怎么做**。
> 每一步都标了在哪个界面执行，照做即可，不需要自行判断。
>
> **硬约束**：本项目任何工具都不得读写 `OneDrive-Personal/Projects/企业AI转型/`。
> 门户导航那一行由 Paul 在 Win 笔记本上改——那是该项目的主机，他改没问题；不能由 Mac 侧跨端写入。

---

## 目录

- [阶段 A · 一次性准备](#阶段-a--一次性准备)（SSH 打通，可与开发并行）
- [阶段 B · 首次部署](#阶段-b--首次部署)（只做一次）
- [阶段 C · 门户接入](#阶段-c--门户接入)
- [阶段 D · 日常发版](#阶段-d--日常发版)
- [验收清单](#验收清单)
- [日志](#日志)
- [故障排查](#故障排查)
- [回滚](#回滚)
- [技术债登记](#技术债登记)

---

## 阶段 A · 一次性准备

**这一步有等待环节（公钥要 RDP 上去部署），建议与 run-build 并行做，别等开发完才开始。**

### A1. 现状与缺口

`supplychain-server` 这个 SSH 别名配在 **Win 笔记本**上，Mac 这边是空白。而本项目仓库在 Mac 上，发版要从 Mac 推——所以必须先补上。

### A2. Mac 侧生成密钥

```
界面：Claude Code tab
Session：当前的就行
分支：main
Worktree：❌ 取消勾选
工作目录：/Users/paulshao/Projects/HumanResource
```

整块复制：

```
在这台 Mac 上配置到 192.168.100.51 的 SSH 免密登录。

要求：
1. 生成一对新的 ed25519 密钥，不要从别的机器拷私钥过来
   ssh-keygen -t ed25519 -C "mac-studio-hr" -f ~/.ssh/id_ed25519_zp51
   私钥不设 passphrase（供脚本自动化用），文件权限 600

2. 在 ~/.ssh/config 追加一段（用户名先留占位，我确认后再填）：
   Host zp51
       HostName 192.168.100.51
       User <待填>
       IdentityFile ~/.ssh/id_ed25519_zp51
       IdentitiesOnly yes

3. 把公钥内容打印出来给我，我要手工放到服务器上

不要尝试自动连接服务器，公钥还没部署上去。
不要修改任何已有的 ssh 配置项。
```

> **为什么生成新密钥而不是拷贝**：私钥在机器之间传输是没必要的风险。公钥可以随便传，私钥永远只待在生成它的那台机器上。

### A3. 服务器侧安装公钥

**界面：RDP 到 192.168.100.51，管理员 PowerShell。**

公钥放哪取决于账户权限，**这是 Windows OpenSSH 最常见的坑**：

| 部署账户类型 | 公钥位置 |
|---|---|
| 普通用户 | `C:\Users\<用户名>\.ssh\authorized_keys` |
| **管理员组成员**（你们大概率是这种） | `C:\ProgramData\ssh\administrators_authorized_keys` |

**账户是管理员时，普通用户目录下那个文件会被 sshd 直接忽略**——放错地方不会报错，只会一直连不上。

放好后必须修文件权限，多一个用户 sshd 就静默拒绝：

```powershell
icacls C:\ProgramData\ssh\administrators_authorized_keys /inheritance:r /grant "Administrators:F" /grant "SYSTEM:F"
```

### A4. 回 Mac 验证

```
界面：Claude Code tab · main · 不勾 worktree
```

```
验证 SSH 到 192.168.100.51 是否通：
  ssh zp51 'hostname'
能打出服务器名就算通。
不通的话把完整报错给我，不要自己反复重试。
```

### A5. 发版脚本 —— 已解决（2026-08-09）

Task 11 原本只产出 PowerShell 脚本，但 macOS 默认没有 PowerShell。已处理：

| 脚本 | 在哪跑 | 状态 |
|---|---|---|
| `deploy-server.ps1` | **.51 服务器上**（RDP，管理员 PowerShell） | ✅ 保留不动，服务器是 Windows |
| `sync-to-server.sh` | **Mac 开发机上** | ✅ 已补，bash 原生 |
| ~~`sync-to-server.ps1`~~ | ~~Windows 开发机~~ | ❌ **已删除** |

**为什么删掉 `.ps1` 版而不是两份都留**：部署脚本是"改了一份忘了另一份"的重灾区。里面有一条关键防护——**拒绝从开发机同步 `.env`**（服务器的 `.env` 是独立维护的生产凭据，被开发机本地 `.env` 覆盖会静默发生、毫无提示，这是最终 review 揪出的 5 个 bug 之一）。两份脚本漂移的话，某天 bash 版丢了这条防护，你会在毫无察觉的情况下覆盖生产凭据。

开发机只有 Mac 一台，`.ps1` 版是死代码——**看起来权威的死代码比没有更危险**。将来若有 Windows 开发机加入，用 Git Bash / WSL 跑 `.sh` 即可。

**用法**：

```bash
./sync-to-server.sh                             # 用 ~/.ssh/config 里的 zp51 别名
SERVER=Administrator@192.168.100.51 ./sync-to-server.sh   # 直接指定
```

可用环境变量覆盖：`SERVER`、`REMOTE_APP_DIR`、`TASK_NAME`、`HEALTH_URL`。

---

## 阶段 B · 首次部署

**只做一次。**前置：run-build 跑完、代码已合进 main 并推送、阶段 A 已完成。

### B1. 合并并推送

```
界面：Claude Code tab · worktree 会话内
```

由 `superpowers:finishing-a-development-branch` 处理。合并完确认 main 上有 `app/`、`tests/`、`scripts/`、`deploy-server.ps1`。

### B2. 代码送上服务器

两条路，**取决于 .51 能不能访问 GitHub——这条还没验证过**：

- **能** → 服务器上直接 `git clone`，后续 `git pull` 发版
- **不能** → 从 Mac scp 一份过去

先验证：

```
界面：Claude Code tab · main · 不勾 worktree
```

```
验证 .51 能否访问 GitHub：
  ssh zp51 'curl.exe -sS -o NUL -w "%{http_code}" --max-time 10 https://github.com'
任何 HTTP 状态码都算通；超时或 DNS 失败算不通。
结果告诉我，这决定部署走 git clone 还是 scp。
```

### B3. 服务器上执行首装

**界面：RDP 到 192.168.100.51，管理员 PowerShell。**

`deploy-server.ps1` 做四件事，**必须以管理员身份跑**（注册计划任务和改防火墙都需要）：

1. 建 Python venv
2. 按 `requirements.txt` 精确版本装依赖
3. 注册 Windows 计划任务：**SYSTEM 账户 + AtStartup 触发 + 失败重启 3 次（间隔 1 分钟）**
4. 开防火墙 8095 端口，作用域 LAN 全网段

> **为什么是 SYSTEM + AtStartup**：2026-07-23 你们有过一次因为用 AtLogOn + 交互式账户导致断线的故障。这个教训直接采纳，不要改。

### B4. 服务器上放 `.env`

**`.env` 不在 git 里，必须手工放。**

在服务器的部署目录下创建 `.env`：

```
LLM_PROVIDER=deepseek
LLM_API_KEY=<手工填>
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_MODEL=deepseek-chat
LLM_SUPPORTS_JSON_SCHEMA=false
DB_PATH=data/demo.db
ROOT_PATH=/hr/recruit-agent
```

> **key 只能手工填**，不要贴进任何 AI 对话框、不要写进任何会进 git 的文件。

### B5. 验收

见下方[验收清单](#验收清单)。**第 2 条别跳**，它是验证路径前缀真的做对了的唯一方法。

---

## 阶段 C · 门户接入

**顺序：先完成阶段 B 并验收通过，再做这一步。** 反过来的话同事点进去是死链，第一印象就砸了。

### C1. 接口规格（本项目提供）

```
板块名称：HR·招聘智能体
所属域：hr
路由规范：/hr/recruit-agent
挂载方式：外链跳转（不用 iframe）
过渡期地址：http://192.168.100.51:8095/hr/recruit-agent/
状态标注：演示中
```

> **为什么外链不用 iframe**：界面是多轮对话 + 表单，iframe 里空间局促；跨端口有 Cookie/SameSite 问题；将来接 SSO 时顶层跳转更干净。现有板块里 QD-B 和 FI2 就是外链，跟它们一致。

### C2. 执行

**界面：Win 笔记本的 Claude Code，工作目录 = 企业AI转型 仓库。**

整块复制：

```
在 AI 运营指挥中心门户里新增一个板块入口。

文件：1-转型规划/AI运营指挥中心/AI运营指挥中心-框架原型-v0.1.html
（服务器上对应 index.html）

新板块规格：
  名称：HR·招聘智能体
  域：hr
  挂载方式：外链跳转（参照 QD-B 立项门禁 和 FI2 三单匹配 的写法，不要用 iframe）
  链接地址：http://192.168.100.51:8095/hr/recruit-agent/
  状态标注：演示中

要求：
1. 先搜索现有板块（QD-B、FI2）的链接写法作为模板，照它的结构加，
   不要自创一套样式
2. 只改导航区域，不要动其他板块的任何内容
3. 改完把 diff 给我看，我确认后再部署
4. 部署按你们现有流程：sync 到 .51 并重启门户服务（8092）

注意：链接地址里的路径前缀 /hr/recruit-agent/ 不能省，
新服务是路径前缀感知的，直接访问根路径 http://192.168.100.51:8095/ 会 404。
```

---

## 阶段 D · 日常发版

```
界面：Claude Code tab · main · 不勾 worktree
```

1. 改完代码，合进 main 并推送
2. 从 Mac 推送到服务器（`sync-to-server.sh` 或 `.ps1`，见 A5 结论）
3. 远程重启计划任务
4. 跑一遍[验收清单](#验收清单)的第 1、2 条

> 依赖有变动时（`requirements.txt` 改了），日常 sync 不会重装依赖，需要在服务器上重跑一次装包步骤。

---

## 验收清单

每次部署后都跑一遍。**第 2 条最容易被跳过，也最容易出问题。**

- [ ] **1. 页面能打开**
      `http://192.168.100.51:8095/hr/recruit-agent/`

- [ ] **2. 静态资源全部加载成功** ⚠️
      浏览器 F12 → Network → 刷新，**不能有任何 404**。
      这是验证路径前缀真的做对了的唯一方法——前端硬编码 `/api/` 或 `/static/` 这类问题，本地开发一切正常，只有挂到子路径下才暴露。

- [ ] **3. 走通全流程**
      提交一次真实用人需求 → 多轮追问 → 画像确认 → JD 产出

- [ ] **4. JD 带 AI 生成标识**
      法规要求（《AI 生成合成内容标识办法》2025-09-01 施行）

- [ ] **5. 页面标注「演示环境，不进入正式招聘流程」**

- [ ] **6. 计划任务已注册且开机自启**
      服务器上 `schtasks /query /tn <任务名> /v` 确认账户是 SYSTEM、触发器是 AtStartup

- [ ] **7. 重启服务器后服务自动起来**
      这一条最好在正式交给 HR 之前验证一次

- [ ] **8. `.env` 权限正确且不在 git 里**
      `git status` 干净，服务器上 `.env` 不可被其他用户读

---

## 日志

日志落在服务器的 `C:\apps\zhuopin-recruit-agent\logs\app.log`，按天轮转，
默认保留 30 天，单文件超过 50MB 也会切分（文件名带 `.1` 序号）。

查看最近 50 行：

```powershell
Get-Content C:\apps\zhuopin-recruit-agent\logs\app.log -Tail 50
```

按请求标识捞一次请求的全部日志（标识从响应头 `X-Request-ID` 或 500 响应体拿）：

```powershell
Select-String -Path C:\apps\zhuopin-recruit-agent\logs\*.log* -Pattern "<请求标识>"
```

确认日志子系统健康：

```powershell
curl.exe http://localhost:8095/hr/recruit-agent/health
```

`status` 为 `degraded` 表示日志目录不可写、日志没有落盘——服务仍在正常提供
业务功能，但排障证据在丢失，需尽快处理 `logs\` 目录的权限。

### 日志目录的写权限从哪来（2026-08-20 核实）

计划任务以 **SYSTEM** 运行，所以 `logs\` 必须 SYSTEM 可写，否则应用降级为仅
stdout——而计划任务没有控制台，等于零日志，且**丢日志本身不会报警**。

`.51` 现状：`logs\` 的六条 ACE **全部是继承来的**，`NT AUTHORITY\SYSTEM =
FullControl (IsInherited=True)`，继承链是 `C:\`（显式给了 SYSTEM 完全控制、
且 `AreAccessRulesProtected=True`）→ `C:\apps` → 应用目录 → `logs\`。
**权限是对的，来源是 Windows 默认继承，不是部署脚本设的。**

`deploy-server.ps1` 第 68-83 行那段 `Set-Acl` 在 `.51` 上**属于冗余保险，但保留
不删**：它挂在创建目录的 `if` 分支**外面**，每次运行都无条件重设，是有意的自愈
设计（有人手工收回 SYSTEM 写权限时能找回来）；换一台继承被切断的机器、或把应用
装到 `C:\Program Files` 这类自带受限 ACL 的路径下，它就不再冗余。

该段此前从未真实执行过（见 `07` 第三节坑 15）。2026-08-20 已在 `.51` 的临时目录
里**逐字跑过一次**做验证：先断继承、只留 Administrators，再执行该段——正确写入
`NT AUTHORITY\SYSTEM = Modify, Synchronize` 显式 ACE，随后的写探针通过，临时目录
已清理。结论：这段代码本身可用，且裸写的 `"SYSTEM"` 在 `.51` 的中文版 Windows 上
能正确解析（不必改成 well-known SID）。

排查命令（只读）：

```powershell
(Get-Acl C:\apps\zhuopin-recruit-agent\logs).Access |
  Format-Table IdentityReference, FileSystemRights, IsInherited
```

看到 SYSTEM 有 `FullControl` 或 `Modify` 即正常；**一条 SYSTEM 的 ACE 都没有**才是
故障——此时 RDP 登录服务器重跑 `deploy-server.ps1` 即可修复（它对已存在的 venv 和
目录都是幂等的）。

### ⚠️ 两条不可违背的前提

1. **给 uvicorn 加 `--workers` 之前必须先更换轮转方案。** 当前按天轮转依赖
   单进程：Windows 上轮转要重命名当前日志文件，若有第二个进程持有该文件
   句柄，重命名会失败。计划任务里的 uvicorn 现在没有 `--workers`，改之前
   先换掉 `app/observability/handlers.py` 的 `DailyRotatingFileHandler`。

2. **运行日志不是合规举证依据。** 它会被轮转和留存期清掉。需要长期保存以
   供举证的记录——决策留痕、外发审批、简历访问记录——必须由独立的、不受
   日志轮转影响的持久化机制承载（`ai-audit-trail-and-outbound-gate`），
   **绝不能只存在于运行日志里**。

---

## 故障排查

| 症状 | 大概率原因 | 处理 |
|---|---|---|
| 页面能开但 JS/CSS 全 404 | 前端硬编码了绝对路径 | 违反部署约束 1，回去改成相对路径，不要靠反代打补丁 |
| 访问根路径 `:8095/` 得到 404 | 正常现象 | 应用把路由挂在 `/hr/recruit-agent` 下，必须带前缀 |
| 报"无 API key" | 服务器上 `.env` 没放或 key 为空 | 见 B4 |
| `ModuleNotFoundError` | venv 没建或依赖没装全 | 重跑首装的装包步骤 |
| 服务重启后不起来 | 计划任务用了 AtLogOn + 交互式账户 | 改成 SYSTEM + AtStartup |
| SSH 连不上，无明显报错 | 公钥放错位置或权限不对 | 见 A3，管理员账户要放 `administrators_authorized_keys` |
| LLM 调用超时 | 出网被挡 | 三家域名 2026-08-09 实测全通，先查 `.env` 里 base_url 拼写 |
| 门户点进来是死链 | 服务没起 / 路径前缀漏了 | 确认链接带 `/hr/recruit-agent/` |

---

## 回滚

**本服务不影响任何现有流程**（HR 现状是 Excel + 微信/邮件），所以回滚很简单：

1. **停服务**：服务器上停掉计划任务
2. **摘链接**：Paul 在 Win 笔记本上把门户导航那一行去掉，重新部署门户
3. **数据**：demo 阶段是 SQLite，不影响任何外部系统；需要保留就把 `data/demo.db` 拷出来

没有数据迁移负担，没有对外依赖，**停用即回滚**。

---

## 技术债登记

| 项 | 触发条件 |
|---|---|
| 过渡端口 8095 → 统一网关路由 | 统一门户网关上线 |
| 共享口令 → 可识别到人的登录 + 简历访问留痕 | **M2 处理真实简历前，硬门槛** |
| SQLite → Postgres | M2 |
| `AuditHook` no-op → 落 `analysis_run` 表 | M2 |
